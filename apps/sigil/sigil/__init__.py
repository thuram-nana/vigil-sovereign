"""SIGIL — Sovereign Intelligence · Governance · Integration Layer.

A local-first, offense-free personal AI orchestrator: an append-only, hash-chained, Ed25519-signed
memory spine; a Rust authorization kernel (WARDEN); a mesh of gated agents; and a cheap-local → frontier
cognition cascade. Every action carries a proof of authorization. See the repository README.
"""
__version__ = "0.9.0"
"""Single source of truth for the package version (pyproject reads this dynamically). Kept in step with
the Rust kernel crate (kernel/Cargo.toml)."""
