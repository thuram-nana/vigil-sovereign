{
  "id": "w4b-sensitive-aws-credentials",
  "bug_class": "sensitive_exposure",
  "title": "Deployed AWS credentials file (.aws/credentials) served",
  "severity": "High",
  "applies_when": {
    "always": true
  },
  "insertion_kinds": [],
  "oracle": {
    "kind": "signature",
    "probe_path": "/.aws/credentials",
    "signature": "aws_secret_access_key",
    "http_method": "GET"
  },
  "references": [
    "CWE-200",
    "CWE-538",
    "CAPEC-95"
  ],
  "remediation": "Never deploy cloud credential files into the web root; keep secrets out of the served tree and rotate any that leaked. Confirmed only when the distinctive 'aws_secret_access_key' INI key is actually served (a 404 or a signature-less response does not fire), so it is a real disclosure, not a guess from the path alone.",
  "payload_family": "exposed-cloud-credentials"
}
