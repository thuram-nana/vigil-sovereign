---
name: unified-autonomy-program
description: "CRUCIBLE master roadmap — Unified Provable Autonomy: 7-wave program to make it the #1 cross-layer provable autonomous security tool; Wave 1 = AI reasoning/autonomy core"
metadata:
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
---

Operator directive (verbatim intent): make CRUCIBLE the #1 research + active cybersecurity tool for web apps AND far beyond — matching the giants of every discipline (map networks like Nmap, find bugs better than Burp, vuln-scan at its peak, exploitation + appsec, network monitoring + packet analysis, defense + IR) — "all built into our architecture to gold standards ... combines the strongest ideas from each discipline into a coherent architecture," with AI reasoning "far beyond ordinary." Plan = `~/.claude/plans/parsed-popping-lemur.md` (the approved master architecture + roadmap; overwrote the completed Speed program X1-X6 plan).

**Operator decisions (AskUserQuestion, all "recommended"):** North-star = UNIFIED PROVABLE AUTONOMY (one world-model + prove-don't-guess + AI reasoning across the whole kill chain, every finding a portable proof — the moat vs siloed giants); posture = KEEP DOCTRINE, GATE ALL new power (authorized/fail-closed/correlatable, NO anti-defender/evasion, weaponization+post-ex stay gated); Wave 1 FIRST = the AI reasoning + autonomy core; deliverable = master architecture + phased multi-wave roadmap.

**Grounded by 3 Explore + graph audit.** Crown jewels to BUILD ON (never rewrite): prove-don't-guess 12-oracle layer (verify/) + re-runnable certs (verify/reverify.py); unified Bayesian world-model (worldmodel/ — SCHEMA already models HOST/SERVICE/NETWORK_SEGMENT/NETBLOCK/ASN/CERTIFICATE/PRINCIPAL/CLOUD_RESOURCE/PACKAGE + edges + Beta beliefs + pathsearch); veracity firewall (veracity/firewall.py admit — only demotes); fail-closed authority/sovereignty/entitlement stack; data-driven eval-gated check library (scanner/library.py, make gate); reasoning core (confidence/decision.py SCE, agents/critics.py, reflection.py, cognitive_refusal.py, calibration/ RL+reward+meta-monitor+conformal, kernel/binding.py governance preamble); signed evidence (evidence/, spine_chain); purple-team seam (defender/, detection_cost).

**KEY ARCHITECTURAL LINCHPIN (REFINED by operator mid-session — the REASONING-OS / SENSOR-FUSION pivot):** Two layers, invest asymmetrically. LAYER 1 (instrumentation: nmap/masscan, tshark/zeek/suricata, burp/zap/nuclei, cloud APIs, trivy/syft, semgrep, MISP/STIX, log feeds) = SOLVED engineering — INTEGRATE mature engines as interchangeable GATED SENSORS, do NOT reimplement (native engine only where DEMONSTRABLY better, behind the same interface). LAYER 2 (intelligence: evidence graph, Bayesian world-model, oracle/proof engine, multi-agent reasoning, veracity firewall, calibration, planner, memory, transfer, risk reasoning, correlation, orchestration, explanation) = BUILD from scratch — the MOAT (mostly already exists as crown jewels). CRUCIBLE = the reasoning OS; every tool = a sensor. Defining innovation: EVERY observation regardless of origin normalized into ONE evidence model, reasoned over consistently, backed by verifiable proof. PROVABILITY PRESERVED under integration: a 3rd-party claim enters as a PROVENANCE-TAGGED ATTESTATION; our deterministic ORACLES RE-VERIFY it → promotes to `fact` only if our oracle re-fires over retained evidence, else stays a labeled lead (veracity firewall + refutation channel). The gap is PRODUCERS+ORACLES not representation — generalize intel/collectors/base.py::Collector → intel/models.py::Observation → intel/project.py::project_observation (today OSINT-only) into the UNIVERSAL SENSOR interface (Sensor protocol + gated Adapter subprocess/API + Normalizer + capability registry) = Wave 2 linchpin. Operator emphasis: FULL-SPECTRUM (every sector, NOT web/Burp-only), framed as AUTHORIZED SECURITY-ASSESSMENT SENSORS governed by a **3-TIER model** (mapped onto authority/sovereignty/entitlement/scope/egress stack): **TIER 1 passive observation** (gate=scope+egress+killswitch): network-discovery nmap/masscan/naabu/rustscan→service-reachability oracle, packet/flow zeek/suricata/tshark/ntopng→flow-signature oracle, wireless kismet, ICS/OT read-only analyzers, DNS/email dnsrecon/amass, cloud-posture scoutsuite/prowler→policy-eval, K8s/containers kube-bench/kubescape/trivy/grype, directory/AD bloodhound/pingcastle/purple-knight→privilege-path oracle, SAST semgrep/codeql, SBOM syft/grype/osv→version-range oracle, runtime eBPF, threat-intel MISP/STIX/NVD, detection-eng sigma+ATT&CK+gap_report, TLS-posture pqc_scan→TLS-weakness oracle, OSINT[done]. **TIER 2 active non-destructive validation** (gate=+entitlement+throttle; OUR 12 ORACLES RE-VERIFY→fact): web burp/zap/nuclei/sqlmap+our arsenal, API postman/newman/RESTler, fuzzing AFL++/libfuzzer/jazzer→crash-repro oracle, active-TLS testssl/sslyze. **TIER 3 high-impact adversary-simulation** ISOLATED authorized-validation layer (gate=per-action operator approval+audit+strict scope), NOT routine: identity-resilience evilginx/modlishka (authorized sim), cloud-exploit-validation pacu, controlled PoC execution. **EXCLUDED from reasoning engine (doctrine unchanged):** full exploitation frameworks, credential-attack suites, C2/persistence — only ever in the isolated Tier-3 layer, never anti-defender/evasion, always correlatable.

**GAP MAP (evidence):** Nmap — schema exists, NO producers below L7 (no host-discovery/port-scan/service/OS/topology/network-service/TLS-analysis); passive DNS/CT/RDAP/ASN recon strong (intel/), one live TLS probe (scanner/quantum_era.py::pqc_scan). Wireshark/Zeek/Suricata — ~nothing. Burp — strong crawler/insertion(10)/intruder/collaborator/browser-DOM-exec BUT SPA/WS/smuggling/race/discovery/intruder NOT wired into default WebScanCampaign, browser scan-only not engage, no intercepting proxy. Nuclei/Nessus — 167 checks/16 classes vs thousands, NO template ingestion, verifier routes ~40 classes but library covers 16 (under-covers its own substrate). Metasploit — oracle-confirmation not weaponization (BY DOCTRINE). SIEM/EDR/SOAR/MISP — self-detection 5-rule toy, gap_report.py purple-team synthesizer BUILT but UNWIRED, no real log/IOC ingestion. AI frontier — greedy myopic planner (planner/goal_tree.py score=prior*value/(requests+1)) NOT in default loop; exact-match transfer (memory/embed.py unused for smoothing); no agentic tool-use/MCP; single-target; nervous-system primitives NOT in default engage loop. Platform — no plugin/MCP/API, no external-tool importer, exec/remediation reports human-authored. DEBT: two Beta learners (scanner/learning.py vs memory/priors.py), duplicate seed checks (DEFAULT_CHECKS vs JSON), two fingerprint stacks, two claims.py, quantum_era annealing heavy, injection-micro-variant library skew.

**INVARIANTS every wave:** prove-don't-guess extends to new layers (deterministic oracle over observed data OR labeled lead — never rubber-stamp; new layers get new oracle families); oracle authority (AI advises/re-ranks/defers/abstains, never promotes/skips); doctrine+fail-closed gating on ALL new power; determinism/replay make-gate byte-identical (inject rng, caller-seq, no wallclock; DryRun byte-identical); additive+default-safe. Cadence = build→suite+gate→adversarial review (distinct-lens finders→3 refuters survive if <2)→fix→PR→merge→sync→memory; graphify update (stage GRAPH_REPORT+graph.json NOT cache/); commits end Co-Authored-By: Claude Opus 4.8 (1M context); PR bodies end 🤖 Generated with Claude Code.

**PROGRESS:** W1.1 DONE — built + reviewed + **PR #63 OPEN/MERGEABLE off main 5dec54e, awaiting operator merge** (I'm blocked from self-merging by the auto-mode classifier — merging my own PR needs human approval; operator merges each PR as in the Speed program). W1.1 = `engage.py::_run_reasoning_pass` (runs ONLY under --spine → make gate byte-identical f1=1.000): per finding runs multi-critic panel (run_panel/aggregate_panel, verdicts endorse|object|abstain never confirm) + cognitive refusal (epistemic_refusal fed `_scanner_verification_claim(f)` = the SCAN's own cert claim, NOT the post-grounding mirror which made it inert — the ONE real bug the review caught; carries NO param so admit()'s require_entities can't false-refuse) + reward-bus credit_outcome (spine_sink ONLY → no persistent learner mutation, non-circular label replaces flat 1.0) + reflection (no-op on pure-scanner spine today). GOTCHA: NO Coordinator constructed in any default runner + NO calibration ledger in engage path → engage --spine is the SOLE live target; **meta_monitor DEFERRED to W1.3** (no ledger fed; wiring inert would overclaim). Advisory pass never alters report.active_findings/oracle verdict; extra oracle re-exec is OFFLINE (verify/reverify) no new egress. Adversarial review found+fixed the inert-refusal bug; re-review 0 survivors. GOTCHA: running the suite/gate DELETES tracked framework/v2/.dryrun/*.txt fixtures — `git checkout -- framework/v2/.dryrun/` before staging; stage ONLY the wave's files.

**W1.2 DONE — PR #64 OPEN off main, awaiting operator merge** (branch w1.2-lookahead-planner). Opt-in deterministic budget-bounded lookahead leaf selector `GoalTree.best_open_leaf_lookahead` wired into `Planner.step()` behind `use_lookahead` (default False→legacy greedy/VoI/pathaware BYTE-IDENTICAL). Picks the best budget-feasible PLAN (set of open leaves, cost≤remaining budget, size≤depth) whose value = Σ path-boosted myopic scores + bonus per crown-jewel route (pathsearch.best_paths) it COMPLETES, returns plan's highest-value step (receding horizon). KEY GOTCHA (review pass-1 caught, 0/3 refuted): route bonus is BACK-LOADED (lands only when route's last leaf added) → a greedy BEAM prunes low-value route PREFIXES before completion → silently degrades to myopic for ≥3-leaf routes. FIX = EXACT enumeration of ≤2^k subsets of the ≤k (capped `_MAX_LOOKAHEAD_ROUTES=8`) best routes: force-include combo's leaves, greedy-fill rest of budget, score. DETERMINISM GOTCHA (earlier self-caught): incremental per-sequence float sums are NON-ASSOCIATIVE (same set, two orders ≠ bit-identical) → `value==best` tie-break silently failed → order-dependent pick; FIX = value summed over sorted(plan) canonical order + exact (value,-size,id-tuple) tie-break. Verified 100/100 identical. SCOPE HONESTY: the ACP Planner is DORMANT (no non-test constructor, NOT on make-gate path) → W1.2 gate-neutral by construction (f1=1.000), advances the core for when the planner drives campaigns, changes NO live behavior today. Reviewed twice (pass-1 beam-efficacy bug fixed; re-review stale-comment fixed). GOTCHA: adversarial review is HIGH-VALUE for algorithmic correctness (caught a bug my unit tests missed because the 3-leaf fixture was degenerate — always add a test at the SCALE where the bug bites, e.g. a 3-hop route).

**W1.3 DONE — PR #66 OPEN off main, awaiting operator merge** (branch w1.3-transfer-learning). Cross-engagement TRANSFER: `memory/priors.py` adds `get_prior_smoothed`/`smoothed_priors_for`/`SmoothedPrior` — a sparse local prior for (archetype,bug_class,surface) is blended from LEXICALLY SIMILAR archetypes' priors, weighted by cosine(embed) * 0.5 discount, using the PINNED deterministic stdlib LexicalEmbedder (NOT env-selected get_embedder which may pick nondeterministic ST). Never fabricates: borrowed labelled (is_transferred+sources), under-evidenced blend (<2.0 eff attempts) WITHHELD = honesty gate. SmoothedPrior is Prior-shaped (fractional counts) → warm-starts the bandit via EXISTING seed_from_priors bridge (NO bandit change — its gate-load-bearing rank/mean/serialization untouched). engage.py opt-in `--transfer-archetype NAME`/run_engagement(transfer_archetype=): loads smoothed set when set + no explicit priors + EXISTING memory DB; default→priors=None→byte-identical. Transfer only REORDERS checks (bandit), never gates coverage. KEY GOTCHA (review pass-1, the survivor): W1.3 setting priors ACTIVATED the dormant campaign `_seed_transfer`; with a PERSISTED `--bandit-file`, `_resolve_bandit` loads+re-seeds+saves EVERY run → borrowed pseudo-counts COMPOUND unboundedly, diluting real learning. FIX (scanner/campaign.py::_seed_transfer): ONE-TIME COLD START — seed (context,arm) only when `bandit.observations(ctx,arm)==0` (untouched Beta(1,1); observations=(alpha-1)+(beta-1)); skip any arm with evidence (real OR reloaded transfer). Gate-safe (early-returns on priors=None). GATE FACTS: memory/priors.py NOT on make-gate path; the bandit IS (rank/mean/serialization byte-load-bearing — DON'T touch); adding warm-start paths safe while gate passes priors=None/bandit_path=None. meta_monitor (deferred from W1.1) NOT wired live: assess_learner_health needs INDEPENDENT-label ledger; engage is all-single-oracle→DISPUTED→inert; realized its 'gather_evidence' doctrine as the transfer evidence-gate instead; assess_learner_health stays on self-improve/eval. Determinism verified byte-identical repeated runs. make gate f1=1.000. Reviewed twice; pass-1 compounding bug fixed, re-review 0 survivors. GOTCHA: `get_embedder()` auto-selects sentence-transformers if importable → NONDETERMINISTIC; always pin LexicalEmbedder for replay-sensitive similarity.

**W1.4 DONE — PR #67 OPEN off main, awaiting operator merge** (branch w1.4-agentic-tool-use). The gated agentic tool-use / SENSOR-DRIVING seam = the interface Wave 2 formalizes to plug in sensors. NEW `agents/tools/` package: `Tool` protocol + `ToolRegistry` (base.py, mirrors scanner.Check/agents.Executor rosters; tool declares gating metadata tier/capability/destructive/egress_hosts read defensively); `invoke_tool` (invoker.py) runs the tool through the SAME fail-closed chain the HTTP path uses, COMPOSED from standalone gates (NO unified non-http gate abstraction exists): kill-switch→entitlement(require_capability)→scope(validate_action)→destructive-confirm→egress(build_engagement_allowlist.permits) — every gate fail-closed (denial OR internal error REFUSES, tool never runs); emits tool_call BEFORE + tool_result AFTER (provenance-linked parent_id, mirrors Action→Result) + a refusal event. Reference tool `reverify_finding` (builtin.py): safe/no-egress/deterministic, re-verifies a finding's oracle cert OFFLINE via veracity firewall → PROVENANCE-LABELLED OBSERVATION (GROUNDED/not), NEVER a Finding. TWO ADDITIVE spine EventKinds tool_call/tool_result added in LOCKSTEP: agents/models.py (EventKind Literal + ALL_EVENT_KINDS + payload classes + PAYLOAD_BY_KIND) + agents/schema.sql (CHECK). KEY FACTS (from map): adding EventKinds is 2-FILE additive — spine_chain.event_digest is GENERIC over kind + EXCLUDES posted_at (so digest-stable); no other kind enumerator; tests use subset ≤ (not exact set); SpineSink gets tool_call/tool_result helpers. Determinism: invoker is pure fn of (tool,args,gates,ctx), no wallclock/rng, payloads carry NO timestamps. make gate f1=1.000 (spine NOT on gate path; tools imported nowhere in scan/engine). SCOPE: this is the SEAM (infra + reference tool), NOT wired into a live driving loop (Coordinator dormant; engage --spine mirrors scan) — Wave 2 plugs sensors in behind it. Reviewed TWICE: pass-1 caught egress_hosts crash (`tuple(getattr(...))` on a non-iterable raises TypeError escaping invoke_tool + str iterated per-char) → FIX `_as_hosts` normalizer; re-review caught residual raising-`__bool__` crash (truthiness test outside try) → FIX wrap whole body in try/except→sentinel. GOTCHA: defensive-read contract = invoke_tool must NEVER crash to caller; wrap EVERY metadata coercion, sentinel "<malformed-egress-hosts>" (no dots/wildcards) can't match any allowlist→fail-closed egress refusal.

**W1.5 DONE — PR #68 OPEN off main, awaiting operator merge** (branch w1.5-self-consistency). Extends kernel.consistency.run_consistent to the OTHER no-oracle LLM bindings (only hypothesize was wrapped). NEW: kernel/consistency.py::sample_workers (shared serial-vs-parallel picker extracted from hypothesize_consistent's idiom — DRYRUN→workers=1 SERIAL byte-identical + avoids timestamped-dump filename race; LIVE→bounded pool CRUCIBLE_LLM_MAX_WORKERS + optional RateLimiter; probes backend ONLY to pick serial/parallel, passes caller's ORIGINAL backend into every sample so failover preserved + dryrun samples still hit dryrun). Three wrappers mirroring hypothesize_consistent: decide_consistent (kernel/decide.py, key_fn=(severity,worth_reporting)), pivot_consistent (kernel/pivot.py, key_fn=sorted distinct move.kind — the LLM lateral-move NARRATIVE; the deterministic attack-path search is separate + untouched), threat_model_consistent (kernel/threat_model.py, key_fn=CATEGORICAL skeleton: STRIDE-class multiset + asset-priority multiset + assets/actors/boundaries cardinality, prose EXCLUDED; samples default 3 = ~8k-token call). Exported from kernel/__init__.py. ADVISORY only: abstained/entropy penalty NEVER enters oracle/SCE/calibration (self-consistency = LLM stability not world truth). Determinism: key_fns sorted/categorical; run_consistent gathers submission-order so modal byte-stable; iterable args (blockers/chain_candidates/known_concerns) MATERIALIZED before the sample lambda (else sample 2+ sees exhausted generator). GOTCHA: this env's DEFAULT backend is LIVE ClaudeCodeBackend (is_dryrun=False) NOT DryRun — any smoke/test MUST pass DryRunBackend() explicitly (a backend=None smoke test HUNG on the live CLI). make gate f1=1.000 (kernel NOT on the LLM-free scanner gate path). Additive: existing bindings + hypothesize_consistent untouched; no import cycle (consistency lazy-imports get_backend in sample_workers). Reviewed once: 0 survivors.

**==WAVE 1 (AI reasoning/autonomy CORE) — COMPLETE + ALL MERGED TO MAIN @ a304a19 (operator "merge all pr then proceed"; PRs #63 W1.1, #64 W1.2, #65 README, #66 W1.3, #67 W1.4, #68 W1.5 all MERGED). W1.6 = SCOPE-ONLY design delivered (no code): multi-target campaigns over ONE shared world-model+priors+ledger+spine (per-target charter-gated, cross-target attack paths, W1.3 transfer campaign-wide, per-target seq bands); richer HITL = new additive `operator_directive` spine event, GATED (re-prioritize/narrow/defer only, never expand scope/relax a gate → refused; kill-switch stays ultimate halt), read by reflection/planner to re-rank. Integration verified on merged main: engage.py carries BOTH W1.1 reasoning pass + W1.3 transfer (different regions, clean merge); make gate f1=1.000. W1.6 design is for a LATER wave/on-demand. W1.2 planner + W1.4 tool-seam are DORMANT infra (not in a live default runner) — ready for when the ACP loop / sensor-fusion drives them. NEXT: WAVE 2 = Universal Sensor/Producer framework (Sensor protocol + gated Adapter + Normalizer + registry generalizing intel/collectors) driven via the W1.4 tool seam; reference INTEGRATION = Nmap (gated invoke→parse XML→HOST/SERVICE observations→worldmodel) + NEW service-reachability oracle (port open iff a real handshake reproduces → fact). Then W3 packets, W4 web, W5 cloud/defense, W6 platform, W7 hygiene.==**

**W2.1 DONE — PR #69 MERGED to main @ f7875db** (branch w2.1-sensor-framework). The UNIVERSAL SENSOR/PRODUCER framework = Wave-2 linchpin. NEW `framework/v2/sensors/` pkg: base.py (`Sensor` Protocol = a gated W1.4 Tool + a `normalize()`→list[intel.Observation]; `SensorResult`; and `service_observations()` = the SHARED HOST+SERVICE+HOST--HOSTS-->SERVICE(+SERVICE--RUNS-->APPLICATION) minter every network-service sensor reuses so the SERVICE/HOSTS schema is produced ONE way — reuses intel.Observation/project_observation/refs/host_ref UNCHANGED); pipeline.py (`run_sensor`: invoke_tool[gated]→normalize→IntelIngest.ingest into the shared world-model; refused/failed sensor mints NOTHING; best-effort normalize/ingest); builtin.py (`DeclaredServiceSensor` = safe T1 reference producer, operator-declared inventory, no egress/no entitlement, the FIRST minter of the port-bearing SERVICE/HOSTS structure). Output is GROUNDING_INTEL (provenance `intel:`), NEVER a fact/Finding until an oracle re-verifies (W2.3). Adversarial review (4 distinct-lens finders→3 refuters) found **4 REAL survivors, ALL fixed + re-verified 0/3 broken**: (1) **charter-scope gate was STRUCTURALLY DEAD for every sensor** — invoke_tool derived the scope target from `args['target']` ONLY but sensors act on `args['host']` → FIX `agents/tools/invoker.py::_scope_target` checks target THEN host (gate only ever TIGHTENS; W1.4 tools that name neither key unchanged); (2) **wrong-tier DOMAIN--HOSTS-->SERVICE edge** — host_ref maps a hostname→DOMAIN and HOSTS src must be host/netblock → FIX draw HOSTS only when `subj.kind in (HOST,NETBLOCK)`, still mint SERVICE + tier-valid SERVICE--RUNS-->APP for a domain; (3) **positional itertools counter in obs_id → order-dependent → double-counted the Beta belief** on reorder/intra-batch-dup → FIX obs_id = the claim key `{source}:{seq}:{subject}|{rel}|{obj}` (no positional idx; IntelIngest dedups on obs_id so dup/reorder collapse); (4) **service descriptor minted on the HOST-subject HOSTS-edge obs** → project_observation copies node-claim attrs onto the SUBJECT so port/product smeared onto the HOST order-dependently + SERVICE node left bare → FIX mint the descriptor on the SERVICE node's OWN subject-claim + keep HOSTS/RUNS edges BARE (mirrors from_scan.py:96 app-node-attrs pattern). GOTCHAS: activating the scope gate for sensors means sensor TESTS must write a SIGNED IN-SCOPE charter (monkeypatch paths.charter_path + write charter.md w/ operator attestation + in-scope host table; ethics.require_in_scope prepends https:// so a bare IP matches a literal charter entry); the ONLY non-test invoke_tool caller is run_sensor (reverify_finding keys on args['finding'] so unaffected). Additive+default-safe: sensors imported nowhere on the scanner/benchmark path → make gate BYTE-IDENTICAL f1=1.000; full framework/v2 suite green. NEXT = W2.2 Nmap sensor (gated subprocess shutil.which+argv+timeout mirroring analysis/analyzers/external.py + XML normalizer REUSING service_observations → offline-fixture + skipif-live tests), then W2.3 service-reachability oracle.

**W2.2 DONE — PR #70 MERGED to main @ fdd45d4** (branch w2.2-nmap-sensor). The Nmap network-service sensor = the FIRST mature external engine driven as a gated CRUCIBLE sensor (the reference CLI adapter). NEW `framework/v2/sensors/nmap.py`: `parse_nmap_xml` (PURE/total: nmap -oX → (host,services); [] on any parse error — no XXE risk relevant, degrades not crashes) + `NmapServiceSensor` (run: shutil.which+fixed-argv+no-shell+bounded-timeout mirroring analysis/analyzers/external.py; normalize: REUSES service_observations unchanged → same HOST/SERVICE/HOSTS/RUNS shape as declared, as GROUNDING_INTEL IntelSourceKind.SCAN A2, never a fact until W2.3 oracle). ACTIVE → gated harder: capability=Capability.ACTIVE_RECON (entitlement gate refuses unentitled; require_capability RAISES on denial→invoke_tool catches→refusal), args['target'] scope-gated by the invoker. Registered in default_registry (registration≠invocation; still gated at run_sensor). Adversarial review (4 distinct-lens finders→3 refuters) found a REAL authorization defect — **6 survivors but 4 were the SAME headline HIGH bug** (found by 4 lenses), ALL fixed + re-verified 0/3 broken + 4 adversarial break-it agents found NO residual bypass: (1) **SCOPE-BYPASS via target syntax (HIGH)** — the invoker scope gate validates `urlparse(target).hostname` but nmap honours richer target syntax the URL parser DROPS: a CIDR '10.0.0.5/24' passed the gate on its base IP yet nmap probes all 256 hosts ('/0'→whole internet); also octet-ranges/lists/wildcards → FIX `NmapServiceSensor.run` refuses any non-single-host target via `_is_single_host_target` (accepts ONLY a single IPv4 literal or a letter-containing DNS hostname; rejects '/',',','*',whitespace,leading-'-',numeric-ranges,IPv6) BEFORE the network, so nmap scans EXACTLY the gate-validated host; (2) **ARG/FLAG INJECTION (MEDIUM)** — a '-'/'--'-leading target (e.g. '--script=win.example.com' slipping a wildcard-scope charter) is parsed as an nmap OPTION → FIX single-host guard rejects it + argv ends with `--` end-of-options separator + args['ports'] must match a port-spec regex; (3) **IPv6 (LOW fail-closed)** — URL gate truncates bare IPv6 → documented + explicitly rejected for now. KEY LESSON: **a URL-shaped scope gate is UNSOUND for a tool with richer target syntax (nmap CIDR/range/wildcard) — an active sensor MUST validate its target to a single host at the SENSOR boundary so what it scans == what the gate authorized; never rely on the gate alone.** GOTCHA: `ipaddress.ip_address("0x0a000005")` is a str→ValueError so it's treated as a hostname (accepted) but it's still ONE host the scope gate independently bounds to the charter — not a multi-host bypass. make gate BYTE-IDENTICAL f1=1.000 (nmap.py off the scanner/benchmark path; entitlement.models import = no cycle). Full suite green; live 127.0.0.1 scan opt-in CRUCIBLE_LIVE_NMAP=1. NEXT = W2.3 service-reachability oracle (re-verify an nmap "open" OBSERVATION → a FACT iff a real handshake reproduces).

**W2.3 DONE — PR #71 MERGED to main @ 8f8c27f** (branch w2.3-reachability-oracle). **WAVE 2 COMPLETE** — the sensor-fusion prove-don't-guess loop end-to-end: W2.1 interface → W2.2 Nmap OBSERVES "open" (GROUNDING_INTEL) → W2.3 an INDEPENDENT handshake RE-VERIFIES it to a FACT. The service-reachability oracle = 5 oracle-layer edits + 1 module: verify/models.py (+OracleKind.SERVICE_REACHABILITY), verify/oracles.py (`service_reachability_oracle` = PURE judge of a retained handshake dict; fires only when connected is True + concrete host+port; 0.90 bare TCP connect, 0.97 with a real BANNER), verify/verifier.py (BUG_CLASS_ORACLES["service_reachable"] + aliases port_open/reachable/open_port + _run branch on ctx key "handshake"), verify/adapter.py (FindingContext + `handshake` field + `from_handshake` builder + to_verifier_context passthrough — the offline-re-verify CARRIER), verify/reachability.py NEW (`capture_handshake` = GATED [slug REQUIRED → kill-switch → single-host → ACTIVE_RECON entitlement → charter scope, fail-closed] + BOUNDED [one attempt, hard timeout, 256B banner] TCP connect returning JSON-SAFE evidence [banner decoded to str so reverify_context re-executes offline]; `connect` injectable for tests; `confirm_reachable`/`reachable_context`). KEY FACTS: verify/ IS on the make-gate path but adding an OracleKind + bug_class is GATE-NEUTRAL (f1=1.000) because the oracle only runs when ctx carries "handshake" (no benchmark finding does) + _ALL_ORACLES=tuple(OracleKind) fallback still skips it (no inputs); is_known_bug_class is a bool predicate (additive). Adversarial review (4 lenses→3 refuters; NOTE this round was NOISY — 7 agents API-errored + classifier flaked, judged on merits) found 3, ALL fixed + re-verified 0/3: (1) **tautological peer-corroboration** — `peer.endswith(":{port}")` is ALWAYS true (getpeername returns the dialled port) so every bare connect scored 0.97 + the 0.90 tier was dead → FIX only a real banner→0.97, bare→0.90 (honest); (2) **IPv6 scope mismatch** — _is_single_host admitted IPv6 but require_in_scope urlparse-truncates bare IPv6 (fe80::1→fe80) validating a DIFFERENT string than dialled → FIX reject IPv6 (`.version==4`) matching W2.2 until scope supports bracketed IPv6; (3) **laundering guardrail (design/honesty)** — oracle keys off `connected` (the conclusion) so a hand-built from_handshake({connected:True} from nmap's row) could launder an observation→fact; NO shipped path does this (only capture_handshake produces handshakes) + a completed TCP handshake IS legitimate reachability proof → FIX honest docstrings (oracle + from_handshake: evidence MUST come from a real gated capture, never a scanner row) NOT a banner-requirement (would wrongly under-report silent open ports). LESSON: grounding for an evidence-judge oracle is PROCEDURAL (evidence must originate from a real gated capture) — same trust model as all oracles; don't add crypto-provenance no other oracle has. GATE-order lesson (self-caught pre-review): an active probe with slug="" must NOT skip charter scope — REQUIRE the engagement slug (no charter context = refuse). Reused precedents: pqc_scan (bounded socket.create_connection), reverify_context (offline re-exec over JSON-safe oracle_context via FindingContext.model_validate; bug_class binding). NEXT = Wave 3 packet/flow sensors (tshark/Zeek/Suricata gated capture → flows/DNS/TLS/JA3 observations) + TLS-weakness oracle (extend quantum_era.pqc_scan) + flow-signature oracle; defensive framing, capture is a gated capability.

**W3.1 DONE — PR #72 MERGED to main @ d9420fb** (branch w3.1-tls-weakness-oracle). Wave 3 begins. The TLS-weakness oracle = the EXACT mirror of W2.3 (gated capture + pure oracle + verifier wiring + FindingContext carrier + offline re-verify), REUSING verify.reachability._authorize (same audited gate) + scanner.quantum_era.pqc_scan's ssl approach. 5 edits + verify/tls.py: OracleKind.TLS_WEAKNESS; `tls_weakness_oracle(observed_tls)` PURE judge fires only when connected is True AND deprecated protocol (_DEPRECATED_TLS_VERSIONS {SSLV2,SSLV3,TLSV1,TLSV1.1}, uppercased; note ssl.version() returns "TLSv1" for TLS1.0)→0.95 OR weak cipher (_WEAK_CIPHER_TOKENS substring match, DES-CBC3 ordered BEFORE DES-CBC so 3DES vs single-DES classify right)→0.92; strong TLS1.2/1.3+modern suite does NOT fire (good posture ≠ finding); BUG_CLASS_ORACLES["weak_tls"]+aliases+_run branch ctx key "tls"; FindingContext.tls+from_tls_handshake+passthrough; `capture_tls_handshake` GATED+BOUNDED ssl handshake (check_hostname=False/CERT_NONE = posture-probe-not-trust-check, JSON-safe {tls_version,cipher,cipher_bits}, connect injectable). Adversarial review (crypto-classification lens enumerated real suite names) = **0 survivors / 11 raw** — no false-positive on AES-GCM/CHACHA20/TLS1.3, no false-negative on weak suites. make gate BYTE-IDENTICAL f1=1.000 (only fires with a "tls" ctx key). Full suite green; real self-signed loopback TLS handshake tested. LESSON: pattern reuse (W2.3→W3.1) + a dedicated crypto-classification review lens = clean pass. NEXT = W3.2 tshark packet/flow sensor (mature-engine integration mirroring W2.2 nmap: gated subprocess reading a pcap → flows/DNS/TLS observations reusing service_observations; offline pcap-JSON fixture + graceful-absence + skipif-live).

**W3.2 DONE — PR #73 MERGED to main @ 93c52d8** (branch w3.2-tshark-sensor). The mature-packet-engine integration: Wireshark `tshark` driven as a gated PASSIVE sensor over an operator-provided pcap (offline, no network — like SBOM/cloud file-ingest), reusing the W2.1 framework + service_observations. Mints 3 families into the world-model as GROUNDING_INTEL (IntelSourceKind.PACKET_CAPTURE, NEW enum value — gate-neutral since normalize_response stays total via .get + learn.py is store-based): OPEN SERVICES (from TCP SYN-ACKs = responder accepted → open), DNS RESOLUTIONS (DOMAIN + DOMAIN--RESOLVES_TO-->HOST), TLS SNI (DOMAIN). NEVER a fact until W2.3/W3.1 oracle re-verifies. sensors/tshark.py: `parse_tshark_fields` (PURE parser of `tshark -T fields` fixed -e column order) + `_observations_from_records` (pure normalize) + `TsharkFlowSensor.run` (gated subprocess shutil.which+os.path.isfile(pcap)+fixed-argv [pcap is -r's VALUE not a flag]+no-shell+bounded-timeout; T1/no-egress/no-capability). **KEY EMPIRICAL LESSON (caught a real bug BEFORE review by generating a real SYN-ACK pcap + running actual tshark): tshark `-T fields` renders BOOLEAN flag fields as "True"/"False" NOT "1"/"0" — the naive `== "1"` would have minted ZERO services from any real pcap; my synthetic fixture used "1"/"0" and MASKED it. FIX `_flag_set` accepts both + a real-tshark end-to-end test (craft SYN-ACK pcap → assert service minted) pins it. ALWAYS verify an external tool's ACTUAL output format empirically; a hand-written fixture can lie.** Adversarial review found 3 name-handling defects (ALL fixed + re-verified 0/3): (1) **multi-question DNS/mDNS** — tshark COMMA-JOINS dns.qry.name so a LAN mDNS packet minted one garbage `domain:a,b,c` node → FIX comma-split qname (like SNI/answers) + pair answers only to a SINGLE question; (2)+(3) **_clean_name over-accepted** — IP-literal SNI minted wrong-tier `domain:<ip>` + commas/backslashes/control-bytes minted garbage → FIX reject IP-literals (an IP is a HOST) + validate every name against a strict DNS-name regex `\A(LDH+underscore labels)\Z` (underscore allowed for SRV/mDNS; \Z not $ so no trailing-newline match). make gate BYTE-IDENTICAL f1=1.000. Full suite green. **==WAVE 3 CORE DELIVERED: W3.1 TLS-weakness oracle (#72) + W3.2 tshark packet/flow sensor (#73). W3.3 (flow-signature oracle + wire defender/gap_report) DEFERRED as lower-value. NEXT = Wave 4 web breadth (HIGH value: wire the already-BUILT arsenal SPA/WS/smuggling/race/discovery/intruder into the default WebScanCampaign + engage w/ CDP allowlist; integrate Nuclei/Burp/ZAP as web sensors via eval/adapters.py our oracles re-verify; close the verifier↔library gap for the ~24 routed-but-uncovered classes; gated proxy). Wave 4 touches the make-gate SCANNER path directly — extra care for byte-identical.==**

**==WAVE 4 (web breadth) COMPLETE — ALL 4 tracks MERGED to main @ 8764548 (graph refresh @ f85d782). Operator directive: "deploy DIFFERENT specialised agents to handle ALL of Wave 4 AT THE SAME TIME, nothing left out" → I dispatched 4 general-purpose agents each in an ISOLATED git worktree (isolation:"worktree" — parallel build, no working-tree collisions), each opening its own PR; then I reviewed + fixed + merged SEQUENTIALLY.==** PRs: **W4-A #75** Nuclei/ZAP/Burp as gated web SENSORS (sensors/web_scanner.py; wraps eval.adapters parse_* UNCHANGED; findings enter as LEADS IntelSourceKind.WEB_SCANNER/GROUNDING_INTEL, never facts; scope-tight minter; Nuclei active=ACTIVE_RECON scope-gated, Burp=REST-pull egress-gated). **W4-B #74** closed the verifier↔library gap = 8 new JSON library entries (scanner/library_entries/w4b_*.json) for routed-but-uncovered classes (time_based/time_based_cmdi/ldap_injection/xpath_injection/el_injection/sensitive_exposure/xxe) — PURE additive data, each routes to an oracle its check produces; gate verdict byte-identical 9/0/0 f1=1.000 (reqs 796→853, "probe more, prove nothing without the oracle"). **W4-C #77** wired the built arsenal (smuggling/CSWSH/discovery/race) into WebScanCampaign+engage behind OPT-IN `--arsenal`/enable_arsenal (DEFAULT OFF → byte-identical; raw-socket modules host-gated via `_arsenal_host_allowed`=killswitch+scope+posture; race never auto-fires; findings oracle-confirmed or leads-only) + CDP fail-closed Fetch request-allowlist (ARCH §8). **W4-D #76** gated intercepting proxy/REPEATER (framework/v2/repeater/): capture→mutate→replay DOUBLE-GATED (invoke_tool EXPLOIT_EXECUTION entitlement → HttpExecutor.gated_fetch scope/budget/rate-limit/egress/redirect-regate/evidence), correlatable OBSIDIAN UA, NOT evasive. **ORCHESTRATION LESSONS:** (1) worktree agents committed to their branch but ONE stalled before push/PR — I finished it from its worktree (git -C <worktree>); another finished cleanly with a thorough self-report. Always verify each agent actually PUSHED + opened a PR (check `git worktree list`, `gh pr list`); an agent's final message can be a mid-thought fragment. (2) MERGE SEQUENTIALLY not in parallel + re-run `make gate` on main AFTER EACH merge (gate-path changes can interact); the 4 PRs had NO file overlap so merged cleanly. (3) Prune worktrees after: `git worktree remove --force` + `git branch -D` + `git worktree prune`. **REVIEW SURVIVORS (each fixed + re-verified):** A had a real SCOPE LEAK — `_location_host` mis-classified scheme-less/exotic finding-location strings as in-scope, planting FOREIGN hosts in the world-model. Took 3 break-it rounds to harden: hand-rolling WHATWG URL parsing LEAKS (protocol-relative //host, Unicode IDNA dots evil。net U+3002/FF0E/FF61, userinfo@host admin@localhost, decimal/hex IP 2130706433/0x7f000001, %2e, interior tab/newline \<TAB>/evil.net, ≥3 slashes ///evil.net, backslash \@evil.net) → FINAL FIX = FAIL-CLOSED: any location with a C0 control char / backslash / unresolvable-`//` returns an `_OFF_HOST` sentinel (dropped), a real authority resolves via urlsplit and is kept ONLY if host==scoped-host, only a single-slash-path/query/fragment is in-scope; 5/5 break-it agents then found NO leak. **LESSON: for a scope/security classifier over untrusted strings, don't hand-roll URL parsing — FAIL-CLOSED (drop anything not provably in-scope) + delegate host extraction to urlsplit; adversarial break-it fuzzing is essential and found real leaks TWICE after my first two "fixes".** B: dropped a duplicate /server-status entry. D: repeater built HttpExecutor WITHOUT auto_load_authority=True → signed EngagementAuthority time-box/ceiling/environment SKIPPED (only killswitch ran) → FIX add auto_load_authority=True (matches engage.py); a replay after the authorization window closed now refuses. C: review 0 survivors. NEXT = Wave 5 (cloud/IAM/SBOM/SAST/identity/threat-intel + defense/IR sensors — live cloud APIs/trivy/syft/semgrep/IdP as gated sensors upgrading today's file-ingest; MISP/STIX/NVD→IOC/CVE; wire defender/gap_report into engage; Sigma/log ingestion; all DEFENSIVE, no evasion).

**==WAVE 5 (cloud/IAM/SBOM/threat-intel + defense/IR sensors) COMPLETE — ALL 4 tracks MERGED to main @ 3688dce. Operator directive "deploy multiple agents to work in different parts so we work faster" → 4 specialised worktree agents built in parallel (several I finished by hand after mid-suite-wait stalls), each its own PR; batched adversarial-review workflow across all 4 → 4 survivors (3 in my W5-B comparator, 1 LOW in W5-D) fixed + re-verified; then MERGED SEQUENTIALLY w5d→w5b→w5a→w5c reconciling ADDITIVE enum conflicts, re-gate byte-identical after each; full framework/v2 suite green (exit 0), gate 9/0/0 f1=1.000 reqs=853.==** PRs: **W5-B #78** version-range-membership oracle + SBOM/SCA sensor (supply-chain prove-don't-guess): sensors/sbom.py (grype/osv-scanner JSON offline-ingest → PACKAGE obs + vulnerable-dependency LEADS) + verify/version.py (PURE PEP440-via-packaging-first / loose-fallback comparator + OSV-range/comparator-string membership, FAIL-CLOSED) + OracleKind.VERSION_RANGE + version_range_oracle (fires 0.95 iff version PROVABLY in affected range). **W5-A #79** cloud/IAM/CSPM sensors + OracleKind.POLICY_PATH policy-path/privilege-path oracle (verify/policy_path.py builds the grant graph; oracle re-derives a real reachability path → the IAM privesc becomes a FACT only via the retained raw policy, never the sensor's minted beliefs) + IntelSourceKind.CLOUD_POSTURE. **W5-C #80** threat-intel sensors (intel/from_threatintel.py: MISP/STIX/NVD/OSV → IOC + CVE observations; IntelSourceKind.MISP/STIX; CVE/advisory obs use VULN_DB) — LEADS/corroboration, never facts. **W5-D #81** defense/IR: purple-team gap synthesizer + Sigma runtime + log ingestion wired into engage (defender/logsource.py, sigma.py, efficacy.py); engage.py +143 additive lines (defensive, gate byte-identical). **W5-B REVIEW SURVIVORS (3, all fixed @ 5d27513 + regression tests):** (1) FALSE-POSITIVE — loose comparator sorted a pre-release ABOVE its base (`1.2.0-SNAPSHOT` > `1.2.0`, Go pseudo-version), the "sorts below" flag was DEAD → FIX split release/pre-release on the FIRST semver `-`/Debian `~`, final release (no pre tail) sorts ABOVE same release WITH one. (2) FALSE-NEGATIVE — grype's versionConstraint ALWAYS has a trailing ` (unknown)`/`(python)` format tag → the whole (real) constraint fail-closed as unparseable → every single-constraint match silently DROPPED → FIX strip the tag in sensors/sbom.py (`_GRYPE_FMT_SUFFIX`). (3) FALSE-POSITIVE — un-modelled `~=X.Y.Z` was widened to unbounded `>=` → FIX fail-close every un-modelled operator (a missed vuln/lead is safer than a fabricated fact). **W5-D REVIEW SURVIVOR (1 LOW, fixed @ 3ea20af + regression test):** parse_evtx_json's `_absorb_map`↔`_absorb_data` mutual recursion had NO depth bound → a crafted deeply-nested `{"Data":{"Data":...}}` chain RecursionError'd, violating logsource's "every parser is TOTAL, a malformed record is skipped never raised" contract → FIX `_MAX_DEPTH=64` cap on the descent + per-record try/except around `_flatten_evtx_record` in parse_evtx_json. **MERGE GOTCHA (the Wave-5 sequential merge):** all of w5b/w5a touch verify/{models,__init__,adapter,oracles,verifier}.py and w5a/w5c touch intel/models.py — every conflict was an ADDITIVE UNION (keep BOTH enum members / dict entries / _run branches / classmethods / __all__ exports; for two appended oracle sections in oracles.py, insert a fresh `# ---` section-border between them; add a missing `@classmethod` when unioning two classmethods). w5d touched NO shared enum file → merged first, cleanest. LESSON: parallel additive work on shared enums (OracleKind/IntelSourceKind) reconciles MECHANICALLY as unions; merge the no-shared-enum branch first, re-gate after EACH. NEXT = Wave 6 platformization (plugin registry #82, MCP expose+consume #84, gated loopback API + importers #85, report automation #83) — batched adversarial review IN PROGRESS focusing on gate-bypass-over-MCP/API, loopback-default, untrusted-input safety, leads-not-facts, gate-neutrality; then merge sequentially reconciling __main__ _DISPATCH + intel/models additive conflicts.

**==WAVE 6 (platformization) COMPLETE — ALL 4 tracks MERGED to main @ f21aadf. 4 specialised worktree agents built in parallel, each its own PR; ONE batched adversarial-review WORKFLOW (7 distinct-lens finders across all 4 PRs → each finding refuted by 3 verifiers, survive if <2 refute; 16 agents, 0 errors) → 5 findings, 2 CONFIRMED REAL (both 0/3 refuted) fixed + regression-tested; then MERGED SEQUENTIALLY w6a→w6b→w6c→w6d reconciling ADDITIVE conflicts (__main__ _DISPATCH handler-defs + dispatch table, intel/models IntelSourceKind, docs/CRUCIBLE-AI.md rows), re-gate byte-identical after EACH; full framework/v2 suite green (exit 0), gate 9/0/0 f1=1.000 reqs=853.==** PRs: **W6-A #82** unified plugin/capability REGISTRY (framework/v2/plugins/: discoverable+extensible catalog of sensors/tools/oracles/operators/commands; CLI subcommand `capabilities [--json] [--kind ...]`; additive, gate-neutral). **W6-B #84** MCP tool-server (framework/v2/mcp/): EXPOSE CRUCIBLE as gated tools (server.py — every tools/call routes through the SAME agents.tools.invoke_tool gate chain; DEFAULT-SAFE allowlist; charter slug SERVER-FIXED never request-chosen; loopback stdio) + CONSUME external MCP tools as gated sensors (sensor.py → world-model as IntelSourceKind.MCP_TOOL LEADS, never facts; client.py/protocol.py stdlib JSON-RPC-2.0, all wire input untrusted/bounded/safe-parse). CLI `mcp serve/list --slug`. **W6-C #85** gated loopback external API (framework/v2/api/: server.py/guard.py/actions.py/reads.py — loopback-bound, gated mutation) + external-tool IMPORTERS (framework/v2/imports/: parsers.py/importer.py/to_observations.py — untrusted third-party report files → world-model LEADS, safe-parse). CLI `api`, `imports`. **W6-D #83** report automation (framework/v2/report/: generate.py/grounding.py/priority.py — executive+technical+remediation from CONFIRMED findings, prove-don't-guess OUTPUT: facts labelled PROVEN, leads labelled LEAD-unconfirmed; DETERMINISTIC no-wallclock render). CLI `report [slug] / --from-json`. **REVIEW SURVIVORS (2, both fixed + regression tests):** (1) **HIGH scope-gate bypass @ mcp/sensor.py (w6b, @ 7abac64):** MCPSensor.run applied the scoped host via `payload.setdefault("host", host)` — a NO-OP when the caller's `remote_args` already carried a 'host'. The invoker's charter-scope gate validates ONLY the top-level args['host'], so `args={"host":"10.0.0.5","remote_args":{"host":"8.8.8.8"}}` passed the gate on the in-scope host while the external MCP tool was driven against OUT-OF-SCOPE 8.8.8.8 (authorization bypass on the real outbound probe + normalize() misattributed 8.8.8.8's services under 10.0.0.5). FIX = refuse a conflicting remote host (fail-closed+visible) + FORCE `payload["host"] = host` (never setdefault). LESSON: a nested caller-controlled arg (remote_args) can smuggle a target PAST a gate that only inspects top-level args — the sensor MUST re-bind the scoped target at its own boundary, never merely default it. (2) **LOW robustness @ report/cli.py (w6d, @ 48cadf2):** `--from-json` caught JSON syntax errors but a well-formed doc with an invalid finding object raised a pydantic ValidationError deep in generate_reports that escaped main() as a raw traceback → FIX wrap generate_reports in try/except ValidationError → clean error + exit 2. **WORKFLOW LESSON: a single batched adversarial-review Workflow (finders pipelined into 3-refuter verification, survive-if-<2-refute) over 4 parallel PRs is the right tool — caught the real HIGH scope-bypass that unit tests missed, auto-refuted 3 false-positive finder claims, cost ~1M subagent tokens for exhaustive coverage. Reachability nuance the refuters surfaced: MCPSensor is CONSUME-only + NOT in the EXPOSE default registry, so no external MCP client reaches it — but the internal planner/operator path DOES, and scope binding must hold against planner-chosen args, so the bug is real regardless.** ==WAVE 6 COMPLETE. Waves 1-6 ALL MERGED to main. NEXT = Wave 7 hygiene (behavior-preserving debt paydown: unify the two Beta learners scanner/learning.py vs memory/priors.py; de-dup seed checks DEFAULT_CHECKS vs JSON + fingerprint stacks + two claims.py; right-size quantum_era; rebalance library — make gate byte-identical throughout). Then FUTURE frontier = AEGIS runtime AI-attack-detection API (see [[aegis-runtime-defense-frontier]]).==

**==WAVE 7 (consolidation / behavior-preserving hygiene) COMPLETE — ALL 5 tracks MERGED to main @ 7c547b1. THE 7-WAVE UNIFIED-AUTONOMY PROGRAM IS COMPLETE.== Operator: "deploy multiple deep-reasoning agents to handle EVERYTHING in full Wave 7 hygiene." → 5 specialised worktree agents in parallel, each proving `make gate` BYTE-IDENTICAL (9/0/0 f1=1.000, reqs=853, found=9) or DEFERRING what can't hold; then ONE batched behavior-preservation review WORKFLOW (3 finders on the substantive PRs A/B/E → 3-refuter verification; C/D pre-confirmed inert) → 1 confirmed survivor fixed; then merged all 5 sequentially, re-gate byte-identical after each; full suite green (exit 0).** PRs: **W7-A #88** unified the two Beta learners' MEAN arithmetic into a new pure `common/beta.py` (`beta_mean`=α/(α+β), `beta_mean_from_counts`=(s+1)/(a+2)) that scanner/learning.py::BetaPosterior.mean + memory/priors.py::Prior/SmoothedPrior.mean delegate to — a pure move-the-implementation refactor; bandit sampling/updates/SERIALIZATION untouched (byte-load-bearing on gate). **DEFERRED collapsing to ONE function** because float non-associativity drifts the last bit for SmoothedPrior's FRACTIONAL counts (demonstrated `1/7,6/7`: `(s+1)/(a+2)=0.39999999999999997` vs reassociated `0.4`) — pinned by a test. **W7-B #89** removed the OOB JSON mirrors of DEFAULT_CHECKS code seeds; **safe because OOB checks send 0 requests on the gate** (enable_oob=False) + don't touch the bandit + each bug_class stays represented; DEFERRED the request-sending dups (boolean_sqli/reflected_xss) + injection micro-variants (each is a distinct benchmark request) + m2/m3 SSRF (different insertion_kinds = real coverage). **W7-C #90** determined the two claims.py are DISTINCT-BY-DESIGN (veracity firewall verdict models vs evidence prose→ReportClaim decomposition; one-directional evidence→veracity dep) → NO merge, docstring-only clarification. **W7-D #86** determined the two fingerprint stacks (scanner/ scan-path vs intake/ pre-scan) are genuinely divergent at every output-affecting layer → DEFERRED unification, design-note only (docs/FINGERPRINT-STACKS.md). **W7-E #87** replaced quantum_era.py's ~120-line simulated-annealing `anneal_path_portfolio` with an EXACT dominance-pruned DP (Pareto-frontier 0/1 knapsack) — provably optimal, fully deterministic, API-preserved (rng/restarts accepted-but-ignored); tie resolves to the cheapest/stealthiest subset. **REVIEW SURVIVOR (1, W7-B, fixed @ 193fae6):** deleting ssrf.json (id "ssrf-oob") silently changed the report references/remediation of confirmed SSRF-OOB findings — because SSRF_OOB code seed shares the EXACT id "ssrf-oob" and scanner/report.py::_meta_for looks up report metadata by check_id (lib.get(check_id)); removing the entry fell back to report._CLASS_META["ssrf"], DROPPING CAPEC-664 + changing remediation prose on any enable_oob=True run. The gate (OOB off) never fired it + no test asserted it → SILENT. FIX = RESTORE ssrf.json (keep the 2 genuinely-safe deletions whose ids rce-oob/xxe-oob never collide) + PIN its references [CWE-918,CAPEC-664] in test_library.py (closes the untested gap) + doc the deferral. **KEY WAVE-7 LESSONS: (1) behavior-preserving refactoring is HARDER than additive work — 3 of 5 tracks correctly DEFERRED (fingerprint whole, beta single-fn-collapse, most library dups) because unifying would change observable output; a well-justified deferral IS the success. (2) An id COLLISION between a code seed and a data entry means deleting the "duplicate" data silently changes a metadata-by-id lookup elsewhere (report references) — a duplicate in ONE dimension (detection) is NOT a duplicate in another (report metadata). (3) The gate byte-identical is necessary but NOT sufficient for behavior-preservation — a change OFF the gate path (enable_oob reports, engage path-selection) passes the gate + suite while silently differing; the adversarial behavior-preservation review is what catches these. (4) float non-associativity is a real byte-identity constraint (W7-A's deferred single-fn collapse).** ==**THE ENTIRE PROGRAM (Waves 1-7) IS COMPLETE — CRUCIBLE is the reasoning-OS + sensor-fusion platform: W1 AI reasoning/autonomy core, W2 universal sensor framework + Nmap + reachability oracle, W3 TLS-weakness oracle + tshark, W4 web breadth, W5 cloud-IAM/SBOM-version/threat-intel/defense-IR, W6 platform (registry/MCP/API+importers/reports), W7 hygiene. Every wave: parallel worktree agents → batched adversarial review workflow → fix survivors → sequential merge → gate byte-identical 9/0/0 f1=1.000. NEXT (future frontier) = AEGIS embeddable AI-attack-detection API (see [[aegis-runtime-defense-frontier]]) — the defensive dual; stand up when the operator directs.==**

**DOC:** README.md rewritten to a COMPLETE SELF-CONTAINED 1384-line first-principles explainer (reason-for-existence, 14-step mechanistic data-flow + worked SQLi example, per-subsystem what/why/how/wiring, doctrine, honest shipped-vs-inprogress tags incl W1.1-on-branch-not-merged) — operator wants README to explain EVERYTHING inline, no refer-outs. Shipping as own PR off main (branch docs-readme-full-architecture).

**WAVES:** W1 AI autonomy core [W1.1 wire nervous-system advisory into default engage loop; W1.2 deterministic budget-bounded lookahead planner; W1.3 embedding-smoothed cross-engagement transfer + bridge 2 Beta learners; W1.4 agentic tool-use layer (Tool protocol+registry+spine tool_call/tool_result, seam for W6 MCP); W1.5 self-consistency for no-oracle bindings; W1.6 scope-only multi-target+HITL]. W2 network producers (Nmap-grade, new service-reachability+TLS-weakness oracles). W3 packet/flow+Zeek/Suricata detection (gated capture, flow/signature oracle). W4 web breadth (wire arsenal+engage w/ CDP allowlist, Nuclei-YAML compiler, close verifier↔library, gated proxy). W5 defense/IR (wire gap_report, real Sigma/MISP/STIX/log ingestion, SOAR-lite). W6 platform (plugin registry, MCP expose+consume, gated API/daemon, importers, report automation). W7 hygiene (behavior-preserving debt paydown). GATED behind entitlement: active net scan, packet capture, live cloud/k8s, remote browser+proxy, external API; weaponization/post-ex stay out of default posture.

See [[speed-program]] (the just-completed X1-X6 predecessor + its cadence/gotchas), [[nervous-system]] (N0-N7 — the primitives W1.1 wires), [[anti-hallucination]] (P0-P7 veracity), [[intel-engine]] (the collector/Observation/project substrate W2 generalizes).
