---
name: sigil-phase9-companion
description: "SIGIL Phase 9 COMPANION — the phone as a signed remote-control + approval + gesture surface over WireGuard; MERGED (PR #10)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
  modified: 2026-07-19T07:40:09.554Z
---

SIGIL Phase 9 "COMPANION" — **MERGED to `thuram-nana/sigil` main @ 98b8532 (PR #10)**. The owner's phone as a signed remote-control + approval + perception/gesture surface over WireGuard; the engine stays on the PC, the phone holds ONLY its own Ed25519 device key, the owner trust-root never leaves the desktop. Built end-to-end with PARALLEL specialized agents in 4 waves (this is the session where multi-agent parallel building was used per the owner's request). Full suite **315 Python tests across 25 suites + Rust 26/26**, all green. Plan @ ~/.claude/plans/parsed-popping-lemur.md (rewritten as the Phase 9 plan). Builds on Phases 0-8 (see [[sigil-phase8-frontier]], [[sigil-phase7-embodiment]]).

**The load-bearing discovery:** Phase 7 WS-D already shipped the crypto/doctrine CORE for a phone (device-auth ledger, DeviceApprover, verify_approval(extra_pubkeys=), BridgeDaemon, PushNotifier, bind_ok, device-key-gated execution — all tested). Phase 9 supplied only the MISSING reach: transport + PWA + pairing + gesture-over-wire + mobile backend.

**What shipped:** `bridge/envelope.py` (device-signed replay-resistant request envelope — no wire bearer secret; auth IS an Ed25519 sig by an owner-authorized device; per-device monotonic-nonce highwater) → `bridge/server.py` (WireGuard-bound transport, forks the WS-C two-plane server: `bind_ok`-only [+Tailscale CGNAT 100.64/10], anti-rebind Host/Origin, endpoint↔action binding, owner-pinned self-signed TLS via `bridge serve` for PWA secure-context) → `bridge/webapp/` (self-contained installable PWA, strict CSP no-CDN, WebCrypto Ed25519 non-extractable key, node-verified canonical_json parity with Python, approve/deny·panic·NL-relay·recall·**arm**·live-SSE·offline-outbox) → pairing (`sigil mesh authorize` fingerprint-confirmed) + `sigil bridge serve` CLI → `gesture/remote.py` (phone-as-trackpad: streams tiny `List[Hand]` batches, decode→honest-[]-on-malformation, session-bound + monotonic-seq drop; bounded by tiers.rs A1-pointer/A2-queue) → **`SessionGate.arm_by_device`** (THE TRUST-WIDENING: device-signed remote arm, fail-closed kill-switch→owner-minted-key+sig→freshness→replay→single-session→TTL-clamp-300s; wired end-to-end via `pending_device_arms`+`run_gesture(device_arm=True)`) → `platform/android.py` (Termux backend) + the latent `egresses`-gate fix in run_gesture.

**REVIEW RIGOR (the real story — a trust-widening cannot self-certify; recurring lesson reinforced hard):** dual review (red-pen + independent sweep) found **4 BLOCK + 3 HIGH + 6 more** — the standouts, all repro'd: **concurrent writes forked the hash chain** (threaded server + fresh-SpineStore-per-request + unlocked append → fixed: process-wide re-entrant `spine_lock` + flock + re-read-tip); **signed NaN ts/ttl defeated freshness AND the TTL clamp** (`nan>30`=False, `min(nan,300)=nan`, `nan<=0`=False → never-expiring device session; fixed: math.isfinite); **per-frame O(n) kill-switch scan = self-DoS** (fixed: 0.5s cache); **remote-arm was INERT end-to-end** (built but nothing consumed a recorded request; fixed: wired). Then the **adversarial re-check confirmed all 7 held under re-run repros (24-thread spine-stress intact, no deadlock) but caught defects MY OWN FIXES introduced** — `int(float('inf'))` raises OverflowError NOT ValueError → an authorized device signing nonce=Infinity crashed the effectful handler (RemoteDisconnected); the dedup scans ran O(spine) UNDER the append lock (stalled all writers); dedup bypass via out-of-range target_seq; arm starvation (newest stale request shadowed an older valid one). All fixed. A **focused 3rd re-check** then found 2 pre-existing effectful-path crash vectors (non-dict signed `args`, unhashable `device`) refuting the "no-crash" claim — closed. **Every finding got a control that fails pre-fix/passes post-fix.** Convergence was clean: each round smaller, soundness held throughout; the tail was robustness hygiene.

**POST-MERGE FOLLOW-UPS (same session, all MERGED to main):** **PR #11 @ e18c0fe — two tightenings + a runnable demo.** (1) Panic latency: replaced the fixed 0.5s kill-switch cache with a SIZE-CHANGE-triggered rescan (a panic APPENDS → spine grows → halt honored in ~1-2 frames/~66ms; pure-movement gesture appends nothing → O(1) no-scan; 0.05s rescan floor) — red-pen PASS (6 vectors). (2) Dedup cost: my first attempt (bounded `SpineStore.tail()` window) was a MED DoS REGRESSION the red-pen caught — a rotated pool of >window distinct captured VALID bodies re-opened an unbounded bloat sink, and my comment falsely claimed "no amplification". FIXED per the reviewer's guidance: arm requests carry a signed `ts` → `submit_arm_request` enforces ARM_FRESHNESS at RECORD time (stale replay refused-not-recorded → aged pool can't bloat), approvals have no freshness field → reverted to the correct FULL-SCAN (bounds bloat to distinct-bodies-forever, off-lock/human-paced). Re-check PASS: 6147 aged submissions → 0 records; 2 documented non-block residuals (key-holder amplification needs the DEVICE key; keyless window-eviction is gated behind the pre-existing unauthenticated panic path). **`demo/companion_demo.py`** = a runnable end-to-end walkthrough vs the REAL BridgeServer on loopback (pair→approve→relay→recall→arm→panic, real Ed25519), + `demo/README.md` (real WireGuard/Tailscale operator guide). **Then a COMPREHENSIVE GitHub README @ 2c9fcc1** (what SIGIL is + all Phase-0..9 capabilities + setup + real CLI usage + companion + security model + disclaimer; fixed a stale `consolidate --provider agent` → the real choice is `claude`). LESSON REINFORCED: a "tightening"/optimization can itself be a security regression — the bounded-window dedup traded correctness (bloat bound) for cost and mis-claimed it; the red-pen + re-check caught it. 319 Py + 26 Rust green throughout.

**MULTI-AGENT ORCHESTRATION LESSONS (this session):** (a) `isolation:"worktree"` on the Agent tool did NOT actually create separate worktrees in this env — AND hardcoding `cd /home/kali/sigil` in a builder's commit command defeats isolation anyway; the 5 wave-1 builders all committed to the SHARED checkout concurrently and their `git add -A` cross-labeled each other's files (all code landed correctly; only commit ATTRIBUTION scrambled). **FIX for later waves: parallel builder agents write+test-their-own-file-ONLY and run NO git; the orchestrator (me) owns ALL commits** — eliminates the contention. (b) parallelize only DISJOINT-file + DISJOINT-import-graph slices within a wave; sequence dependent waves (server needs envelope; PWA needs server), integrating + full-suite + committing between waves. (c) build the trust-widening LAST, solo (not racing agents), then MANDATORY dual review + re-check before merge. **Owner explicitly authorized the merge ("yes proceed") — merged as PR #10.** gh account=thuram-nana. NOTE two documented honest caveats live in-code: a ≤0.5s injection window after a phone panic (the kill-switch-cache tradeoff, tunable via `_KS_CACHE_TTL`), and O(spine) dedup scans (now outside the write lock).
