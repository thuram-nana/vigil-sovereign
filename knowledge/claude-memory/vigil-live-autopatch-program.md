---
name: vigil-live-autopatch-program
description: "VIGIL live auto-patch runner (LAP) — wiring real executors onto the sovereign run_codefix/autopatch libraries; the operator's \"build the above\" (open gated PRs to fix confirmed findings)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
  modified: 2026-07-26T15:17:26.793Z
---

**LAP — the live auto-patch runner.** After the [[vigil-command-ui-program]] finished, the operator said "build the above" = build the deferred live auto-patch runner (the capability that actually clones, edits, sandbox-builds, and opens **gated PRs** to fix oracle-confirmed findings). Repo `/home/kali/vigil`, remote thuram-nana/vigil-sovereign.

**Seam map (4-agent UNDERSTAND workflow):** the whole downstream is ALREADY built + hardened — the PURE, fully-INJECTED `autopatch()` (integration/vigil_integration/autopatch/loop.py:434) and `run_codefix()` (remediation/codefix.py:399) pipelines (enforce: confirmed-FACT-only via `may_remediate`; per-file A2 approval with **timeout=REJECT** flip; **m-of-n for the PR**; `remediated` type-impossible without opened_pr+signed evidence_ref; remediated only when the exploit oracle goes SILENT), the full m-of-n `destruction_gate.py` (DestructionAuthority/DestructiveAction/DestructionAuthorization/SignedDestructionAuthorization/sign_authorization/authorize_destruction; mandatory-owner bound in `mandatory_signer_ids`), `conjunctive_gate.build_offense_gate`, `live/executor.subprocess_runner` (argv-list, shell=False, total), the `fsjob` sandbox, `live/think_claude` (Claude client), `live/spine_identity` (stable offense-spine signer). **The ONLY gap: no live caller supplied real executors** (autopatch/run_codefix were called only by tests with fakes). Executor call sites: `clone(request)`, `build(request, approved)` [autopatch passes list[PatchFile], run_codefix passes list[str] — DIVERGENCE], `open_pr(request, approved)`, `approve_block/approval(pf)`, `quorum(request)→QuorumOutcome{approved,signer_ids}`, `oracle/exploit_oracle(request, patched_build)`, `now()→numeric`. **STRICT `is True`** on `.ok`/`.approved`; workdir NOT threaded (session must stash it); patched_build = build_ref str.

**PRE-EXISTING GATE BUGS the map found (LAP-2 must fix before a sound destructive run):** (1) `build_offense_gate` forwards ONE `now` to BOTH legs but authorize_action(CRUCIBLE) needs `now: datetime` and authorize_destruction needs `now: float` — incompatible; (2) CRUCIBLE scope leg does `host_matches_scope(host_of(target))` but codefix target=repo (git URL/local path) → scope mismatch; (3) the PR leg has TWO m-of-n mechanisms (build_offense_gate's destruction conjunct AND run_codefix's separate `quorum` callable) → unify to ONE signed authorization. FIX DIRECTION: a codefix-specific gate WRAPPER (don't modify the shared build_offense_gate → avoids blast-radius on the live scan engine), calling each leg with the right now-type + supplying the destruction material for the `github_pr` leg.

**DECOMPOSITION (each: build→red-pen→re-check→6-job CI→merge):**
- **LAP-1 MERGED (PR #72)** — the NON-DESTRUCTIVE foundation. `integration/vigil_integration/live/codefix_runner.py` (stdlib + injected seams; imports NO framework/strix/sigil): `CodefixSession` real executors — propose (REAL Claude unified-diff coder; no key⇒''⇒no-patch), clone (real `git clone` into a DISPOSABLE workdir + fix branch; SOURCE never modified), build (git-apply approved diffs into the clone, `--check` first = the honest "does the fix apply"), open_pr DISABLED. gate = WARDEN tier + killswitch for non-destructive legs; destructive⇒DENY (no m-of-n wired here); operator-present upgrades a WARDEN queue→allow for a reversible local action. `autopatch_live(finding,*,config,client,...)`: edits apply ONLY with `apply_edits` opt-in (default timeout⇒reject); quorum denies; a LEAD refused; `remediated` impossible without a PR. PROVEN END-TO-END over a real local git repo (fix lands on the clone, source stays vulnerable, no PR). Red-pen HOLD (no BLOCK): fixed HOLD-1 (`_safe_workdir` refuses a `../`/separator remediation_id from escaping base_dir — sink guard, was non-local) + HOLD-2 (`_repo_ok` rejects leading-dash / `ext::`-transport-helper / non-allowlisted URL scheme + `--` before the clone repo arg). ToolDecision field is `.outcome` NOT `.decision`. Boundary test must SUBPROCESS (inherit parent PYTHONPATH=integration:gateway — the live layer imports vigil_gateway) + be order-independent. `git` present on box; NO sovereign venv/browser/GitHub-remote here (can't test live PR).
- **LAP-2 MERGED (PR #73)** — fixed the SHARED gate-wiring bugs FIRST (operator directive): (1) build_offense_gate now-type mismatch (one `now` broke both legs — authorize_action wants datetime|None, authorize_destruction wants epoch float) → `_as_datetime`/`_as_epoch` adapt per-leg; (2) live/wiring._build_gate never wired the kill-switch → now passes KillSwitch(slug) (fail-soft). Red-pen PASS (no reachable fail-open; every hostile now denies or falls back to real clock; the fix removed a fail-CLOSED availability bug). +doc NOTE-1 (`now` MUST be trusted-caller — load-bearing for the window) + NOTE-2 (why the killswitch fail-soft is safe). The scope-as-host + double-m-of-n are codefix-USAGE (not shared bugs) → handled in LAP-3.
- **LAP-2b MERGED (PR #74)** — Settings gets a GitHub-token field (+ a SECRET_META per-secret table; the UI renders one sealed card per SECRET_NAME). Same P4 sealing path (keyring→TPM→0600 env, fingerprint-only spine event, redacted status, `export-runtime-env --include-secrets` delivers it to the offense children). SECRET_NAMES = (ANTHROPIC_API_KEY, GITHUB_TOKEN).
- **LAP-3 MERGED (PR #75)** — the destructive PR leg, OFF by default. subprocess_runner gains additive `cwd`/`env` (token via ENV never argv). codefix_runner: `pr_enabled` master switch; gate DENIES destructive github_pr unless pr_enabled (then allow-on-tier, m-of-n orthogonal via the pipeline's separate `quorum`); `open_pr` real (stage ONLY explicit approved paths — never `git add -A`, each is_safe_repo_path re-checked — commit, push, `gh pr create`, token in child ENV); `build_destruction_quorum(authority, signed, slug, is_consumed)` = the m-of-n over authorize_destruction; `autopatch_live` gains pr_enabled + injectable quorum (defaults hard-DENY). No verify oracle → PR opens as PROPOSAL (opened-pr-unverified); `remediated` type-impossible without a silent oracle + signed cert. Red-pen PASS (3 independent fail-closed barriers pr_enabled+token+m-of-n; 15 hostile m-of-n variants denied; token never in argv/log; PATH preserved; no autonomous fire). +github_token field(repr=False). **196 tests green.** LESSON: destruction_gate authorization window must be ≤900s (max_authorization_lifetime dead-man's-switch); the destructive PR gate defers m-of-n to the pipeline's `quorum` (ONE m-of-n, not double).
- **LAP-3b(1) MERGED (PR #76)** — closed the LAP-3 red-pen HOLD (single-use CHECKED but not RECORDED). `live/nonce_ledger.py` `NonceLedger(dir)`: `is_consumed(nonce)` (advisory, blank⇒True fail-closed) + `try_consume(nonce)` (**ATOMIC** reserve via `os.open(marker, O_WRONLY|O_CREAT|O_EXCL, 0600)`, marker=`sha256(nonce)`). codefix_runner: `build_destruction_quorum(..., consume=)` (was `record_consumed`) — on authorize it atomically reserves; `won is not True`⇒DENY(replay), raise⇒DENY(fail-closed); STRICT `is True`. `file_backed_quorum(..., ledger_path=)` wires `consume=ledger.try_consume`. **The FIRST cut was BLOCK'd by red-pen: a non-atomic check-then-record TOCTOU let 2 concurrent runs of the SAME owner-signed authorization both authorize (proven 40/40 trials >1 approval).** destruction_gate §5 REQUIRES the caller record consumption ATOMICALLY as the serialization point — a plain append file dropped that. Fix = make the CONSUME the serialization point (O_EXCL exclusive-create IS the uniqueness guarantee; of N concurrent callers exactly one wins). sha256 filename ALSO killed a secondary newline-poisoning bug (a `\n`-bearing nonce could mark a distinct nonce spent) + any path-escape. Re-check PASS: 0/40 double-winners (was 40/40), 0 across 260 primitive + 80 end-to-end concurrent trials; durable across restart (marker 0600, dir 0700, both fsync'd); advisory-is_consumed staleness harmless (try_consume is authoritative). 19 codefix tests green. **LESSON: "record consumption" is NOT enough — it must be an ATOMIC check-and-consume; a check-then-append is a TOCTOU that fails under concurrency. Hashing the key into a per-key marker filename gives atomicity (O_EXCL) AND immunity to key-content injection in one move.**
- **LAP-3b(2) MERGED (PR #77) — the final "arm it" connect; LAP PROGRAM COMPLETE.** `vigil patch` native offense verb (execs `.venv-offense`) runs the gated auto-patch ladder over a finding with SOUND PROVENANCE as the hard invariant. THE HAZARD (proven): a raw `TriageFinding` from JSON is trivially forgeable — its validator accepts `confirmed=True` with ANY non-empty `evidence_ref` STRING (`evidence_ref="TOTALLY-FAKE"` passes `may_remediate`). So the CLI NEVER builds its finding from argv/stdin/file; `confirmed=True` is set ONLY after crypto verification. `live/trusted_finding.py` (import-clean: vigil_core + offense-local, NO framework/strix/sigil): `finding_from_envelope` (a signed inert finding envelope — m-of-n governance sig [anchor 1] verified w/ vigil_core alone under a trust root DERIVED from an OWNER-signed `verify_delegation`; cert's own engagement_slug BOUND to the delegated scope [anti-laundering]; mirrors `FindingReceiver.ingest`) · `finding_from_spine` (rebuild a fact from the engagement's OWN `{slug}.spine` — `VigilCoreSpine.verify()` whole-file audit fail-closed THEN `.rebuild()` re-verifies each rec sig + fact/evidence validator; readonly binder never mutates) · `load_destruction_authority`/`load_signed_authorization` (provision the `--open-pr` m-of-n from files; sigs re-verified by `authorize_destruction`, not trusted at load). Default = NON-destructive dry run (propose→clone→sandbox-build in a DISPOSABLE clone; source untouched, no PR; prints the exact `action_id=pr-<remediation_id>`+slug+target to sign). `--apply-edits` applies into the clone; `--open-pr` (OFF default) wires the ATOMIC single-use `file_backed_quorum` (LAP-3b(1)) + needs ALL of {signed-authorization, authority-trust-root, mandatory-signer, ledger} + `GITHUB_TOKEN` in ENV (double fail-closed: deny-quorum + `pr_enabled=False`). Coder key from `ANTHROPIC_API_KEY` env (never argv). 20 tests. **Review: first red-pen STALLED on infra mid-stream (known intermittent) → recovered via my EXHAUSTIVE self-fuzz (13/13 forgery axes refused: 8 envelope [wrong-owner/tampered-cert-old-sigs/out-of-scope/expired/below-threshold/truthy-non-True-verify/attacker-root/wrong-role] + 5 spine [content/evidence-ref tamper, truncation, wrong-key, appended-forged-fact]) THEN a fresh INDEPENDENT red-pen PASS (verified 9 MORE layers: quorum-inflation resistance, wildcard-scope-can't-be-widened, empty-digest-fails-CLOSED, strict-is-True everywhere, all-failure-paths-raise, LEAD-exclusion + verify-before-rebuild, action_id agrees across 3 sites, --open-pr double fail-closed, tests not green-washed).** LESSON: recovery from a stalled red-pen = exhaustive self-fuzz OR relaunch fresh — do BOTH for authorization-core (self-fuzz for coverage, independent agent for blind spots). **To go LIVE end-to-end the operator provisions the m-of-n destruction quorum keys (GitHub token already in Settings).**
- **MODEL-PICKER CORRECTNESS MERGED (PR #78)** — operator: "research the internet deeply + ensure the Claude section model actually works." Confirmed the live Claude catalog (via the `claude-api` skill + docs). TWO real bugs: (1) `AnthropicBackend._call` always sent `temperature=` → **400 on the CURRENT models** (opus-5/sonnet-5/opus-4.8/4.7/fable/mythos REMOVE sampling params) → the picker's own default (claude-opus-5) hard-failed every call; only the legacy sonnet-4-6 default "worked". Fix: `_supports_sampling(model)` attaches temperature ONLY for sampling-capable models (sonnet-4.x/haiku/older). (2) `vigil engage` (via `think_claude.think()`, called w/ no model) + `vigil patch` (hardcoded `--model`) IGNORED the Settings-selected model. Fix: `think_claude.resolve_model()` (explicit > CRUCIBLE_ANTHROPIC_MODEL/SIGIL_LLM_MODEL env > DEFAULT); `think()` + `vigil patch --model` resolve through it. Refreshed all built-in defaults to `claude-opus-5`. Picker IDs were already valid. LESSON: a picker offering a model the call path can't actually use (sends a now-rejected param) is a silent 400 — check the CALL SHAPE against the current model surface, not just the model IDs.
- **QUORUM-IN-SETTINGS + GUIDE MERGED (PR #79)** — operator: "put the destruction-quorum keys in Settings like the API key + a guide on how to get them." Settings `VIGIL_DESTRUCTION_OWNER_KEY` paste-card (auto-rendered from SECRET_NAMES; sealed/redacted). `live/destruction_provision.py` (vigil_core+destruction_gate only): `generate_authority` (owner+N co-signers, m-of-n TrustRoot, owner mandatory, threshold-validated) · `sign_action` (one action; window ≤900s dead-man's-switch enforced; blank nonce refused) · `load_worker_key_file` (co-signer keys from FILE not argv). CLI `vigil provision-destruction` (mint → prints keys ONCE, writes PUBLIC trust root, prints ceremony + solo note) + `vigil authorize-destruction` (sign one action; owner key from `VIGIL_DESTRUCTION_OWNER_KEY` env never argv; writes single-use signed-auth 0600). `vigil patch --open-pr` AUTO-DISCOVERS trust-root+ledger under --base-dir. Manual "Arming live auto-patch" guide. **Crypto red-pen PASS: 19/19 hostile bypasses refused** (below-threshold, owner-absent, replay/single-use, action-swap, expired/>900s window, forged key, secret handling), offense-consumes-only, import-clean. Red-pen obs (fixed as honesty, NOT security-weakening): the `vigil up` uiproxy secret allowlist EXCLUDES the owner key (deliberate — keeps the signing key OFF the offense broadcast; a compromised offense child can't self-authorize), so `authorize-destruction` reads it from the EXPORTED env — corrected the Settings/guide wording that implied paste-alone wires it. LESSON: sealing a secret in Settings ≠ delivering it to a manually-run offense CLI; the uiproxy allowlist gates what `vigil up`-spawned children get — keep signing keys OUT of it (offense-can't-self-authorize) and document the export step honestly.
- **LAP PROGRAM COMPLETE** — #72 (LAP-1) → #73 (LAP-2) → #74 (LAP-2b) → #75 (LAP-3) → #76 (LAP-3b(1)) → #77 (LAP-3b(2)) + #78 (model-picker correctness) + #79 (quorum-in-Settings + guide). Operator's full "build the above" delivered: gate-wiring bugs fixed first, both destructive slices built off-by-default, GitHub token in Settings, single-use closed, usable `vigil patch` entry. Every slice: build→adversarial red-pen→re-check-on-fixed-branch→6-job CI→merge; EVERY review caught a real defect or a stall-recovery. Remaining is LIVE-fire only (operator provisions m-of-n keys + runs against a real GitHub remote — not testable on this box: no sovereign venv, no GitHub remote).

**INVARIANTS held throughout:** off-by-default, cannot fire autonomously (per-file approval + m-of-n both fail-closed), only oracle-confirmed FACTs, offense-side/two-env boundary, secret-free. Continue the same build→red-pen→re-check→CI→merge discipline; red-pen every slice (LAP-2 is authorization-core → treat like the crypto slices).
