---
name: vigil-production-hardening-program
description: "VIGIL de-hardcode targets + remote hosting + one-file bootstrap — WS-A/B/C merged (PRs #58-#61)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
  modified: 2026-07-25T20:16:22.893Z
---

VIGIL (`/home/kali/vigil`, repo thuram-nana/vigil-sovereign) "production standard on any system we own" program — de-hardcode the loopback-only pins so the fused system engages any owner-authorized target AND is hostable/bootstrappable anywhere. Plan: `/home/kali/.claude/plans/parsed-popping-lemur.md`. **WS-A/B/C ALL MERGED; WS-D satisfied-via-C; WS-E deferred.** Builds on the completed [[vigil-unification-program]] (one control plane over two isolated processes).

- **WS-A — engage owner-authorized REMOTE targets (PR #58 @747b113, cleanup #59).** The offense loopback pin lived in ONE place: `executor._resolve_loopback` → renamed `_resolve_scoped_target(target, *, scope, allowed_ips)`. Safety property is "never outside the SIGNED scope + never the metadata/link-local/reserved floor," NOT "only 127.0.0.1." Split loopback out of `gateway/denylist.py`'s absolute floor behind a keyword-only `loopback_allowed_if_scoped` (default-off; only the executor passes it — proxy/url_guard/SSRF-gate/nftables keep loopback hard-denied; `_ALWAYS_DENY_NETS`/`is_hard_denied`/nftables byte-identical). `scope is None` = legacy loopback-only byte-identical; `scope` provided = host must match the SIGNED authority scope (via `_offense_scope_source`→`load_authority_for_gate`, fail-closed empty→deny-all) AND every resolved IP clears the floor, then pin the exact resolved IP (TOCTOU/rebind). AUDIT-G4 preserved: gate scopes on the validated HOSTNAME (`_scope_target`), not the pinned IP. `vigil engage --scope`. Dual review (crypto-notary + red-pen) PASS: 18 metadata encodings (opt-in+allowlisted) all denied; allow-set is the signature-verified authority, not the flag. Honesty lesson: it's "the signed authority the gate enforces" (machine-local governance key), NOT "owner-signed" unless the S7b `sigil delegate-offense` ceremony blessed it.
- **WS-B — host the cockpit behind a domain (PR #60 @428faf2/4e71ba1).** `apps/sigil/sigil/ui/server.py` reuses `bridge/daemon.bind_ok` (never-public; fail-closed exit 2 on public/0.0.0.0). Anti-rebind Host/Origin allowlist = real bound addr + loopback-pair (loopback binds only) ∪ operator reverse-proxy domain forms; action-plane token gate + CSP byte-identical. `sigil serve --host/--allow-host/--allow-origin` (+ `SIGIL_UI_*` env). Caddy/nginx templates in `apps/sigil/deploy/reverse-proxy/` (forward original Host; SSE buffering off). `REMOTE-HOSTING.md`. Doctrine: tunnel/proxy is the boundary, never a public listener; phone bridge stays WG-direct, CRUCIBLE console/api stay loopback. Red-pen + re-check: added REAL IPv6 tunnel binds (AF_INET6 for v6 literals + bracketed `[::1]:port` allowlist — Tailscale defaults to v6) AND closed a genuine `bind_ok` v6 hole: Python's `is_private` is TRUE for Teredo `2001::/32` + 6to4 `2002::/16` (globally routable) → replaced the v6 arm with a POSITIVE allowlist (loopback/ULA `fc00::/7`/link-local `fe80::/10` only). Lesson: don't trust `is_private` for IPv6.
- **WS-C — one-file `bootstrap.sh` + `docker-compose.yml` + `Makefile` + `.env.example` + `.dockerignore` + `.gitignore` fix + `docs/DEPLOY.md` (PR #61 @5109357/1d3bd9f/46340df).** `./bootstrap.sh`: preflight (Python 3.12/3.13; Rust via rustup WITH consent; Docker/TPM probe) → `envs/build_envs.sh` (two venvs + Rust kernel + boundary) → compose services (Qdrant, guarded) → config (`~/.sigil/sigil.env` + repo `.env` 0600 never-clobbered; `~/.sigil/venv->.venv-sovereign` symlink for systemd; `~/.local/bin/{vigil,sigil}` launchers; `VIGIL_ROOT`/`CRUCIBLE_ROOT`) → TPM-seal via `sigil vault provision` (`vigil_core.kek.tpm_available`) else loud plaintext warning → smoke. Compose: profiled (qdrant default; neo4j `graph`; otel `observability`; strix-sandbox `strix` BUILD-ONLY → `vigil/strix-sandbox:local`), EVERY host port 127.0.0.1. Offense stays NATIVE (containerizing it needs the host docker socket → weakens the boundary — documented, not shipped). `.gitignore` now ignores `.venv-sovereign/`/`.venv-offense/`/`.env`/`.vigil-live/`/`.vigil-data/`/`.strix/` (was only `.venv/`).
  - **RED-PEN CAUGHT A REAL BLOCK I introduced:** the `vigil` launcher pointed at `.venv-sovereign`, but `vigil`'s NATIVE verbs (engage/provision/verify/identity/detect) run IN-PROCESS and import `framework.*` which lives ONLY in `.venv-offense` (offense.txt has `-e ./engine/crucible`+`-e ./integration`; sovereign.txt omits crucible). Both venvs ship a `vigil` console script but only the offense one works for native verbs. FIX: launcher `vigil`→`.venv-offense`, `sigil`→`.venv-sovereign` (per `docs/architecture/S8-control-plane.md`). Boundary still holds: offense `vigil` is keyless (provision mints a MACHINE-LOCAL governance key, never the owner key); `vigil sigil …` re-execs a SEPARATE sovereign process. **Lesson: `--help` passes before the lazy framework import → a `--help` smoke green-lit the mis-wired launcher; a substantive smoke must run a native verb (`vigil provision`, residue-free via `CRUCIBLE_ROOT`+`--base-dir`→throwaway dir with a `CLAUDE.md` sentinel).** Also fixed: fail-open rustup consent on non-tty (`ask()` now declines w/o `--yes`); `docker compose up` no longer aborts the whole run; `make smoke` was fail-OPEN (`;`-chain judged by trailing `rm`) → if/then/else exit-1.
- **WS-D — codebase/source targets: SATISFIED by WS-C.** `vigil strix --target <github-url|local-dir|url|ip>` + `--mount` already works (dispatch routes `strix`→`.venv-offense/bin/strix`; verified strix's own CLI accepts `--target`). DEPLOY.md documents the flow + prerequisites (build `vigil/strix-sandbox:local`, set `STRIX_IMAGE`) + the honest **"strix output is LEADS, not signed FACTs"** caveat. Deliberately NO `vigil scan-code` forwarder — it would over-imply first-class-scan semantics for a leads-only tool; the raw `strix` name is more honest (`vigil --help` epilog already lists it).
- **WS-E — CIDR scope in `ethics.host_matches_scope`: DEFERRED** (not requested; no CIDR — enumerate literal hosts or `*.`wildcards).

**Process every slice: build → adversarial dual-review/red-pen (opus) → fix → RE-CHECK on the fixed branch → PR → 6-job CI green → merge.** The re-check on the FIXED branch repeatedly caught defects the fix itself introduced (WS-B Teredo/6to4; WS-C fail-open `make smoke`) — never trust the fixer's self-report. This box has only a PARTIAL `.venv-offense` (vigil-core only; tests run via `PYTHONPATH=integration:engine/crucible:gateway`), so a full bootstrap can't run here — validated native verbs by supplying `framework` via PYTHONPATH.
