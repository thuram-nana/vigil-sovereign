---
name: aegis-runtime-defense-frontier
description: "AEGIS — the embeddable DEFENSIVE DUAL of CRUCIBLE: prove-don't-guess runtime AI/web-attack defense. MVP (prompt-injection/disclosure/honeypot) MERGED @ 62d4af3. AEGIS GATEWAY ('provable firewall': inline reverse-proxy + /detect API + WSGI middleware; SQLi/cmdi parse-proof + reflected-XSS/error-SQLi; blocks only PROVEN attacks with a re-runnable cert, fail-open) BUILT 2026-07-12 (G3 dd1adb2 → G7 fixes b4b4bb6). DEPTH (SSTI+path-traversal blocks reusing EVALUATION/SIDE_EFFECT, G5 graduated challenge/throttle, header/cookie surface) + autonomy-loop first-slices + opt-in sensor fusion shipped same day as the 3-workstream program (review-fixes aa89a49). KEY LESSON: adversarial review found 17 (then +7) FP/overclaim defects my own tests missed — near-zero-FP can't be self-certified; belief thresholds must be calibrated against the MEASURED curve, not guessed."
metadata: 
  node_type: memory
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
---

**Operator idea (2026-07-10, verbatim intent):** phishing + attacker tooling are now AI-powered; what if CRUCIBLE shipped an API/SDK a web app embeds that DETECTS AI attacks in real time "and a lot more" — reason it deeper, advance to the max, treat as a FUTURE feature.

**The reframe (the deep idea): this is the DEFENSIVE DUAL of CRUCIBLE.** CRUCIBLE is the attacker's reasoning OS (authorized). This is the same Layer-2 moat INVERTED into the defender's reasoning OS, embedded in the owner's app: a runtime layer that does not pattern-match requests (that's a WAF, and capable agents defeat it) but REASONS about the ACTOR's intent across a session, backed by re-verifiable evidence. Prove-don't-guess is the killer differentiator: flag an actor adversarial ONLY with a calibrated, evidence-backed verdict — because the cost of a false positive here is blocking a real user (or a legitimate AI assistant acting for a user), which is unacceptable at web scale.

**Capability stack (advanced):**
1. **Agentic-actor fingerprinting** — is this a human, a benign AI assistant acting for a user, or an autonomous adversarial agent? Signals: timing/entropy distributions (LLM-driven browsing has inhuman regularity/bursts), headless/CDP + JA4/TLS + automation-framework tells, goal-directed-but-inhuman navigation, tool-use cadence. The frontier tension: the web is shifting to agent traffic — the goal is INTENT classification, not "block all bots."
2. **LLM firewall** (highest-value, underserved) — for apps exposing their OWN AI features: detect prompt injection, jailbreaks, data-exfil-via-prompt, tool/agent abuse against the app's LLM. A reasoning gate over LLM I/O, not a regex denylist.
3. **AI-phishing / impersonation / brand-abuse detection** — honeytokens, coordinated-inauthentic-behavior, deepfake/synthetic-identity signals in onboarding/support/KYC.
4. **Session-intent world-model** — reuse worldmodel/ to model the ACTOR (evidence graph of their behavior + Beta belief + refutation channel), so a verdict is an explainable evidence chain, not an opaque score.
5. **Adaptive, proportionate response** — challenge / throttle / deceive (tarpit/honeypot the confirmed adversarial agent) / alert, scaled to confidence; correlatable, defensive, never harming real users.

**Why CRUCIBLE is uniquely positioned (the moat transfers + a purple-team LOOP):** worldmodel (now the defender's actor-model), verify/ oracles (deterministic detections), sensor fusion (fuse app/behavioral/network/LLM-I/O telemetry into ONE model — exactly the Wave-2 interface), calibration/ (confidence-scored verdicts, conformal bands), the signed event spine (tamper-evident detection audit trail), cognitive-refusal (abstain when evidence is thin — the anti-false-positive gate). THE UNIQUE LOOP: CRUCIBLE attacks (authorized) → learns how AI attackers REASON → the defensive layer detects that same reasoning in the wild; the offensive playbooks become the defensive detections. Same core, both directions.

**Hard problems (be honest):** benign-assistant vs malicious-agent disambiguation (false-positive risk as agent traffic grows); privacy (observes owner's traffic under owner authority only); adaptive adversary (attackers evolve — needs the learning loop); real-time LATENCY budget (a reasoning verdict must fit an inline request path — tiered: cheap inline signal + async deep reasoning); and the prove-don't-guess bar for a live verdict (a defensive fact still needs re-verifiable grounding, calibrated, abstain-when-unsure).

**Product forms:** (a) a JS/SDK client snippet (behavioral/client signals) + a server-side reasoning API the app calls per session ("score this actor + evidence + recommended action"); (b) a reverse-proxy/middleware sidecar; (c) an MCP/LLM-gateway "LLM firewall" in front of the app's AI features (ties to Wave 6 MCP/API). All DEFENSIVE, correlatable, owner-authorized — squarely within doctrine (protecting the owner's own app/users, NOT anti-defender).

**Roadmap placement:** a MAJOR new frontier — a "AEGIS / AI-Threat Shield" program AFTER the current sensor-fusion waves, BUILT ON: Wave 5 defender/IR (gap_report, Sigma, ATT&CK, detection-efficacy) as the detection substrate + Wave 6 platform (MCP tool-server, gated API/daemon) as the delivery vehicle + the Wave-2 sensor interface generalized to RUNTIME telemetry sensors. Sequence: finish Waves 5–7 (which build the exact substrate), then stand up AEGIS as Wave 8 / a parallel program. Name TBD (AEGIS / SENTINEL). See [[unified-autonomy-program]].

**==AEGIS GATEWAY — the "Provable Firewall" — BUILT this session (2026-07-12), committed to main (G3 dd1adb2 → G7 fixes b4b4bb6).==** Operator asked (verbatim intent): make AEGIS embeddable so a web app "adds" it and is protected "totally... in the form of API or another way." **Honest reframe delivered (no "total protection" claim):** a firewall whose every BLOCK is a re-runnable proof (`CertRef.reverify()`), NOT a regex/ML guess → near-zero false positives + an auditable reason per decision. **Operator decisions (AskUserQuestion):** reverse-proxy gateway + `/detect` API; **fail-OPEN provable-block** (block only proven attacks, allow everything else); full first slice (AI-classes + web parse-proof + response-confirm). **Built (G1–G7, all gate byte-identical 9/0/0/853/9):** **G3** request-side PARSE-PROOF oracles in `verify/oracles.py` — `sql_injection_breakout_oracle` (proves a string-literal break-out into query STRUCTURE) + `command_injection_breakout_oracle` (shell command construct); new `OracleKind.SQL_INJECTION_BREAKOUT`/`COMMAND_INJECTION_BREAKOUT` + bug-classes `sqli_attempt`/`command_injection_attempt`, kept OUT of frozen `_ALL_ORACLES`, keyed on a new `FindingContext.request_payload` no benchmark finding carries → gate-safe. Honest label: proves an "structured injection ATTEMPT", NOT exploitation. **G1/G2** `aegis/gateway.py` — stdlib `ThreadingHTTPServer` reverse-proxy (httpx forward), blocks only on `decision=="confirmed"`+cert+`action=="block"` under enforce; **fail-open everywhere** (error/kill-switch/unproven→forward; upstream-down→502); new `Capability.AEGIS_RESPOND` (STANDARD tier) entitlement gate at startup + per-request kill-switch pass-through + audited blocks + no forward-SSRF. **G4** `aegis/inspect.py::inspect_response` — response-side reuse of `reflection_context_oracle` (reflected-XSS; extract the payload's executable TOKEN as the marker since the oracle is canary-oriented) + `error_signature_oracle` (error-based SQLi); an HTML-ENCODED reflection correctly does NOT block. **G6** deploy surfaces: `aegis gateway` CLI + `middleware.inspect_http` sidecar API (any language) + `AegisEnforceMiddleware` WSGI (buffers+restores body) + `docs/aegis/DEPLOYMENT.md`. **G5 (belief/LEAD observability) DEFERRED to roadmap** (never blocks under fail-open). **★ THE CRITICAL LESSON — the adversarial-review workflow (5 lenses→refuters, 23 agents) found 17 EXECUTION-CONFIRMED defects the unit tests + my own FP corpus MISSED, all in the "near-zero-FP" cardinal property:** a contraction apostrophe (don't/it's/I'll) was treated as a SQL string-literal break-out, then ANY SQL-ish word ANYWHERE in the tail matched → "Don't drop the ball; I'll update you" / "5 or 6 options" / "credit union...select" all BLOCKED; `_SHELL_CMDS` listed common English words (sleep/id/cat) → "dog|cat"/"Name | Age | ID" blocked; error-SQLi fired on any quote + any DB-error string (no linkage); reflected-XSS fired on the site's OWN `<script>`. PLUS a **forward-SSRF** (`@evil.com` target spliced userinfo@host), a **ReDoS** (8192 newlines→14.7s via a separator-class-vs-`\s*` overlap), oversized-body truncation+desync, a malformed-target crash, and no client timeout. **FIXES (b4b4bb6):** ANCHOR SQL structure to the break-out quote + require a SELF-comparison tautology (X=X, not a bare number); require a shell-ARGUMENT indicator next to a command + split-on-separators (kills ReDoS, 2.6ms); require VERBATIM reflection (XSS) + value-near-error-signature linkage (error-SQLi); force a leading "/" in `_forward_url`; 413+close on oversize; top-level fail-safe → 502 not a crash; `timeout` attr. **META-LESSON: for a defensive BLOCKING tool, near-zero-FP is a property you CANNOT self-certify with your own test corpus — an adversarial review that EXECUTES benign-input hypotheses is essential; "prove-don't-guess + gate-byte-identical + my own tests pass" is necessary but NOT sufficient. The prior MVP taught the same about the privacy/retention axis; this taught it about the false-positive axis.** Deploy: `python3 -m framework.v2 aegis gateway --upstream URL --mode observe|enforce`. See [[unified-autonomy-program]].

**==HELD-ITEM (design-first): AEGIS PASSIVE-OOB BELIEF ELEVATION built + merged @ e7dfe41 (2026-07-13). The SSRF/XXE frontier, done doctrine-clean.==** Operator chose "tackle a held item carefully"; I brought a DESIGN + DOCTRINE ANALYSIS first, got approval, THEN built. **What it is:** opt-in `aegis gateway --oob-canary <url>` → a loopback-only `OOBReceiver` (reuse verify/oob.py) + an operator-planted STATIC canary. When an attacker's OWN payload references the canary host (trips the existing SSRF/XXE lead) AND the app dereferences it back to the receiver, AEGIS correlates the unsolicited hit and ELEVATES that actor's per-actor Beta belief (new `feed_oob_correlation`, 0.85 — stronger than a lead 0.70, below confirmed 0.90) toward the EXISTING graduated challenge/throttle. **THE DOCTRINE LINE (translator-not-generator):** AEGIS forwards every inbound request BYTE-FOR-BYTE and NEVER injects/plants a token/callback — the correlation uses ONLY the attacker's own chosen canary-host bytes + the operator's static bait. NEVER a hard block (belief never blocks). No new OracleKind (it's a belief INPUT, not an oracle). Opt-in + AEGIS_RESPOND-gated + fail-open + default-off-byte-identical. **★ REVIEW: a DEDICATED generator-drift lens (high-effort) + a never-block lens attacked the cardinal doctrine by EXECUTION and came back NEGATIVE** — proven byte-identical forwards across 4 attacker shapes, `register_token` never called by aegis, no live request object held, 399 sustained correlations → only challenge/throttle never block. FIXES: [MEDIUM] pending obs are now CONSUMED after they elevate (at-most-once per obs) so a hit STREAM can't re-elevate one lead unboundedly — belief tracks DISTINCT evidence (N leads → N elevations, not 1 lead × N hits); [INFO] throttle-doc calibrated (a sustained OOB-correlated 0.85 belief reaches throttle too, still belief-only). [LOW] multi-actor coarseness (one hit elevates all actors who targeted the canary) is a FUNDAMENTAL limit — precise attribution would need injection = excluded — documented honestly, NOT "fixed" with a token. **DURABLE LESSON: for a doctrine-delicate feature, DESIGN-FIRST + a dedicated adversarial lens whose sole job is to break the cardinal doctrine by execution is how you ship it safely; the honest ceiling of passive OOB for an inline gateway is belief-elevation, NOT a block (a block needs an injected token = generator = excluded).** See [[unified-autonomy-program]].

**==HELD-ITEM (design-first, opt-in extra): SAML CRYPTO XML-DSig VERIFICATION built→reviewed→2-cardinal-FPs-fixed→re-reviewed-clean→MERGED @ 77b2d62 (2026-07-13). OracleKind stays 27 (NO new kind).==** Operator chose "build it as an optional extra." The SAML structural-forgery oracle gained an opt-in `saml` extra (`signxml`): when the operator supplies a TRUSTED IdP cert AND signxml imports, a structurally-COVERED signature (structural NON-FIRE) is escalated to a REAL XML-DSig verification vs the trusted anchor (`_saml_crypto_verdict` in verify/oracles.py), firing `invalid_signature`@0.97 ONLY on a definitive crypto-invalidity (InvalidSignature/InvalidDigest = wrong-signer/tampered-digest); NEVER trusts the doc's embedded KeyInfo cert (pins `x509_cert=`); REFUSES on can't-verify (InvalidCertificate/InvalidInput/parse). No new OracleKind (it's a strengthening of the existing `saml_xml` bug_class), gate byte-identical, structural-only + byte-identical when the extra is absent. **★ THE REVIEW STORY (why held items need this): the first adversarial review (c14n-FP CARDINAL lens) found 2 CONFIRMED HIGH FPs where a GENUINELY-VALID trusted-key-signed assertion fired as a forgery — exactly the near-zero-FP violation this was held for. (1) WHITESPACE/c14n: exclusive-C14N does NOT normalize inter-element whitespace text nodes, so a valid assertion PRETTY-PRINTED in capture (xmllint --format / browser devtools / SAML-tracer — a ROUTINE transform) raised InvalidSignature → fired. FIX: re-verify a whitespace-normalized (lxml `remove_blank_text`) re-serialization before calling a raw definitive-invalid a forgery; if THAT verifies, a trusted key signed it (capture artifact) → verified. Stripping only inter-element whitespace can HEAL a whitespace false-invalid but can NEVER hide a content-tamper. (2) MULTI-REFERENCE: signxml's DEFAULT `expect_references=1` rejects a spec-legal dual-signed (Response AND Assertion) doc as InvalidSignature — a reference-COUNT policy mismatch, not crypto-invalidity. FIX: verify with `SignatureConfiguration(expect_references=False)` (strictly FP-reducing). Both fixes verified by execution: the 2 FP inputs now NON-FIRE; wrong-signer + tampered-content STILL FIRE incl. pretty-printed. Then a broadened RE-REVIEW (residual-transforms + valid-real-world-shapes + fix-induced-false-negative + gate lenses, 10 agents) came back CLEAN: 0 residual FP, 0 false-negative. A KEY RULING: the OTHER transforms that fire (unused-namespace-declaration, prefix-rewrite) are GENUINE inclusive-C14N-1.1 invalidities a CONFORMANT verifier (using the sig's own declared c14n) ALSO rejects — so firing is CORRECT + attack-relevant (namespace/sig-wrapping), NOT an FP; and the reviewer hunted the opposite (oracle firing on a doc a conformant verifier ACCEPTS: comments, redundant xmlns redeclare, leading whitespace, hex charref) → all correctly NON-FIRE. XSW caught by the structural `signature_wrapping` invariant @0.95 BEFORE the lenient crypto verify runs.** DURABLE LESSONS: (a) near-zero-FP CANNOT be self-certified — a valid signed assertion fired 2 realistic ways my own tests missed; the c14n-FP-cardinal review lens (build a genuinely-valid sig, feed it in realistic captured forms) is what caught it. (b) A crypto oracle over CAPTURED bytes must separate benign SERIALIZATION drift (whitespace → heal) from attack-relevant STRUCTURAL drift (namespace/prefix → fire, matching a conformant verifier) — the boundary IS the near-zero-FP property. (c) signxml 5.1.0: `XMLVerifier().verify(xml, x509_cert=PEM, expect_config=SignatureConfiguration(expect_references=False))`; InvalidSignature/InvalidDigest = definitively-invalid (FIRE), InvalidCertificate/InvalidInput/lxml = can't-verify (REFUSE). See [[unified-autonomy-program]] and [[prover-to-discoverer-program]].

**==FUTURE-WAVE BATCH 2 (wire cloud→fusion · NoSQL-injection breakout block · service-mesh posture oracle) 2026-07-12 (merges @ ec7de09). OracleKind now 27.==** All 3 waves SHIPPED (fixable, unlike batch-1's F2). Review found 6 confirmed FPs (every wave), all fixed pre-merge. **G2 NoSQL breakout BLOCK** (`NOSQL_INJECTION_BREAKOUT`, the 3rd AEGIS request-side block, twin of sqli/cmdi breakout): fires only on a KNOWN Mongo operator as a KEY. **DURABLE FP LESSONS:** (a) `$type` and `$regex` are DUAL-USE — `$type` is the **.NET/System.Text.Json polymorphic type-discriminator** (`{"$type":"Ns.Class"}`, ubiquitous benign JSON), `$regex` is the **legacy MongoDB Extended-JSON v1 regex VALUE** / a regex-search body — so they can't be a hard BLOCK (dropped from the allowlist; a $regex injection stays a lead). (b) The JSON-body flatten joins keys with `.`, so a benign body `{"a":{"b.$ne":1}}` flattened to param `a.b.$ne` and a dot-segment rule false-fired → made PATH-1 **bracket-only** (`user[$ne]`), leaving JSON operator-KEY injection to the whole-body scan that reads ACTUAL parsed keys. **G3 mesh posture** (`MESH_POSTURE`, twin of k8s/cloud posture; built a minimal offline Istio/Linkerd ingestion, no substrate existed): FP LESSON — Istio `requestPrincipals: ["*"]` REQUIRES a valid JWT (the RECOMMENDED "require a token" posture), it is NOT anonymous allow-all; only the mTLS-peer `principals: ["*"]` is. Also: don't coerce a non-mapping AuthorizationPolicy rule to `{}` (reads as allow-everyone catch-all → fabricated fact) — drop it. **G1 cloud→fusion wiring** (no new kind): FP LESSON — when re-verifying via a multi-rule oracle, gate on the ACTUAL fired proof (`observed.rule == encryption_at_rest_disabled`), not the aggregate `.confirmed` — else a public/wildcard fire gets mislabeled as an encryption gap. **META (batch 1 vs 2): batch-1 F2 was DROPPED (fundamentally unsound offline); batch-2's FPs were all FIXABLE over-reaches (dual-use tokens, lossy flatten, secure-posture-misread, multi-rule mislabel) — the reviewer's job is to tell those two apart.** See [[unified-autonomy-program]].

**==FUTURE-WAVE BATCH 1 (cloud/CSPM oracle · JWT header-attack depth · fold discovered findings) 2026-07-12 (merges @ 4b39288). OracleKind now 25.==** Operator: "deploy multiple agents to take on every future wave" — I run it in REVIEWED BATCHES (roadmap has no terminal state), doctrine-safe only. **F1 CLOUD_POSTURE** (held out of frozen 15; bug_class `cloud_misconfiguration` keyed on `cloud_control`): offline achieved-state promotion (encryption-at-rest-disabled on a sensitive datastore / public exposure / wildcard-anon principal) — the complement to the reachability POLICY_PATH oracle, filling the encryption gap it can't prove; over the existing `sensors/cloud.py` P4 substrate; read-only. MERGED. **F3 fold discovered findings** into `result.report.active_findings` (deterministic sort+dedup, post-print, off-benchmark). MERGED. **★ F2 JWT embedded-key (jwk/x5c) forgery oracle = BUILT then REJECTED IN REVIEW, NOT MERGED.** DURABLE LESSON: "an embedded verification key (jwk/x5c header) that self-verifies the token" is NOT offline-provable as a forgery — a **legitimate CA-chained x5c is the RFC 7515 §4.1.6 NORM** (the leaf IS the cert whose key signed the JWS; the RP validates the chain to a trusted CA — unknowable offline), and legit flows embed a self-verifying **jwk by design (DPoP proofs, SIOP id_tokens)**. The review minted a CA-issued-leaf token (issuer≠subject) and it fired identically to a self-signed forgery at conf 0.99 → would flag real Azure/enterprise/DPoP tokens. Same class of lesson as XXE: **if it's not offline-provable near-zero-FP, it's a LEAD/risk-indicator, never a confirmed FACT — and since an oracle only emits FACTs, the branch is DROPPED (a breadcrumb comment in jwt_forgery_oracle records the rejected design so it's not re-added).** 2 of 3 waves shipped, 1 correctly killed pre-merge — the review has now caught a real defect in EVERY round this session. See [[unified-autonomy-program]].

**==3rd 3-WORKSTREAM PROGRAM (SAML structural-forgery oracle · AEGIS productization · sensor-fusion auto-activate) BUILT→REVIEWED→FIXED→MERGED 2026-07-12 (merges fd7098c/96a0ff6/49deb4d, reconcile d4eb90a, docs 90eb99e, graphify 69e099a). OracleKind now 24.==** Same orchestration; gate byte-identical throughout; pushed through the protected main via owner bypass. **NW-1 SAML** = offline `SAML_STRUCTURAL_FORGERY` oracle (held out of frozen 15; bug_class `saml_structural_forgery` keyed on `saml_xml`): fires on no-signature / a `ds:Reference` not covering the consumed assertion / XSW-wrapping shape, reusing scanner.sso's XXE-safe `safe_parse_xml`; the SAML sibling to the JWT oracle. **NW-2 productization** = AEGIS is now **pip-installable** (a NEW repo-root `pyproject.toml` — the nested `framework/v2/pyproject.toml` can't see the `framework` parent, so a wheel from it wouldn't deliver an importable `framework.v2`) + a `framework/v2/aegis/Dockerfile` (observe-default gateway) + `aegis demo`/QUICKSTART + `crucible`/`aegis` console scripts. **NW-3** = `engage --fuse-sensors` auto-activates on a `targets/<slug>/fusion.json` (byte-identical when absent; benchmark slug carries none). **★ REVIEW caught 4 (2 real): [HIGH] the SAML oracle FALSE-fired `reference_mismatch` (conf 0.9 → a CONFIRMED FACT) on VALIDLY-signed assertions using spec-legal same-document reference forms other than the bare `#id` shorthand — `#xpointer(id('X'))`, `#xpointer(/)`, a URI-less transform-selected `ds:Reference` — because it RAW-STRING-compared `ds:Reference/@URI` against the consumed id set. DURABLE LESSON: when an oracle DISCLAIMS a semantic layer (here c14n/XML-DSig transforms), it must REFUSE-TO-ADJUDICATE on any input it can't fully resolve, NOT assert the negative — an unresolvable reference is not evidence of non-coverage. FIX: `_saml_resolve_ref` resolves the known same-document forms (whole-doc `#xpointer(/)`/URI=""; `#xpointer(id('X'))`+bare `#NCName`→that id) and marks anything else UNKNOWN → invariants (b)/(c) refuse when any ref is unadjudicable. [MEDIUM] the root pyproject declared `license="AGPL-3.0-or-later"` SPDX-string + `license-files` (PEP 639, needs setuptools>=77) under a `setuptools>=61` floor → `pip install` breaks on stock LTS setuptools (~68); fixed to the classic `license={text=...}` table form (LESSON: for installability, don't require a bleeding-edge build backend — match the declared floor). NW-2 also found+declared a latent undeclared `packaging` runtime dep (crashed `status` in a clean venv). 2 info deferred (a pre-existing UTF-16 entity-guard gap in shared `safe_parse_xml`, not reachable via the str-input NW-1 oracle; conftest in the wheel — never imported).** See [[unified-autonomy-program]].

**==2nd 3-WORKSTREAM PROGRAM (in-band XXE block · JWT structural-forgery oracle · autonomy DISCOVERY loop) BUILT→REVIEWED→CORRECTED→MERGED 2026-07-12 (merges 80c4fe9/4f4acdd/005fc4e, reconcile 842ddfb, docs 01b6141, graphify 42bb0d6). Pushed through the now-PROTECTED main via owner bypass.==** Same orchestration (3 worktree agents → batched adversarial review → fix → sequential gated merge B→A→C → finalize). Gate byte-identical 9/0/0 f1=1.000 reqs=853 throughout; **OracleKind now 23** (15 frozen + 8 additive-out-of-frozen: 6 AEGIS + 1 K8S_POSTURE + 1 SSO_ASSERTION_FORGERY). **What shipped:** WS-B = offline **JWT structural-forgery oracle** (`SSO_ASSERTION_FORGERY`, bug_class `jwt_forgeable` keyed on a `jwt_token` ctx no benchmark carries; fires on alg=none / recomputable-HS256 / RS256→HS256 confusion; near-zero-FP). WS-C = first **DISCOVERING** autonomy step (was re-verify-only): opt-in `engage --autonomous-discover` seeds a probe-leaf from an unexplored ENDPOINT → gated `probe_surface` tool runs ONE existing check (REFLECTED_XSS) → the ORACLE mints a NEW finding; localhost + scope/kill-switch/egress-gated; default path byte-identical; discovered findings stay on AutonomyResult+spine, NOT spliced into the authoritative ScanReport (deferred). **★ WS-A = the big lesson: the in-band XXE BLOCK was built, then the adversarial review (10 confirmed defects, 1 CRITICAL) proved it UNSOUND → operator chose to DEMOTE in-band XXE to a LEAD.** DURABLE DOCTRINE: **a single inline request/response exchange cannot causally prove XXE.** Unlike path-traversal (attacker-CHOSEN file path), the XXE "proof" is a generic `/etc/passwd` line that can be REFLECTED user content (a security-KB / paste / code-review page documenting an XXE example and echoing a sample root line); once the DTD is HTML-escaped in the response a whole-body not-reflected guard passes on that benign echo → false block + a "confirmed" cert overclaiming file disclosure; and the marker is read back out of the SAME response it judges (a **circular self-proof**). So in-band XXE joins blind XXE + all SSRF as belief-raising LEADs; the only sound block-path is the OOB callback oracle (a PASSIVE correlation on a token the app already carried — the payload-rewrite/generator variant is doctrine-excluded for a translator-not-generator firewall). **★ 2nd LESSON (content-type gate precision):** a reflected-XSS FACT is only executable under an HTML content-type — but my first WS-C fix used a GLOBAL saw_htmlish flag OR'd across the audit; the re-check caught 2 bypasses (a MISSING Content-Type defeated it since I mirrored `_is_htmlish` which treats missing as htmlish; and on a multi-param endpoint an UNRELATED param's HTML response licensed a different param's inert JSON reflection). FIX: key the gate on the REFLECTING response (record (is_html, body) per response; mint only when the marker `crucible<slug>mark` was reflected INTO an affirmatively-`text/html` response; missing-CT is NOT html here — stricter than `_is_htmlish` on purpose because minting a FACT is a higher bar than a passive header lead). **★ 3rd LESSON: the adversarial RE-CHECK (re-run each finding's EXACT repro on the FIXED branch + hunt NEW bypasses) caught defects my own fixes introduced** — verifying a fix is its own review pass, not a formality. Kept WS-B's gate-guard registration (`_WSB_KINDS`) + a bounded HMAC signing input (65536) as fixes too. See [[unified-autonomy-program]].

**==3-WORKSTREAM PROGRAM (AEGIS depth · autonomy loop · sensor fusion) BUILT+REVIEWED+MERGED 2026-07-12 (merges @ 20cac88, review-fixes @ aa89a49, SHA-stamp @ 2d5f881). Operator: "DEPLOY 3 AGENTS EACH HAVING ITS SWARM ... 1,2,3".==** Same hardened orchestration (3 worktree lead-agents off current main → batched adversarial review → sequential gated merge 3→1→2 reconciling additive enum unions → re-gate byte-identical after each). All opt-in/default-OFF; gate stayed **byte-identical 9/0/0 f1=1.000 reqs=853** through every merge. **WS1 AEGIS depth:** SSTI + path-traversal are now **response-side BLOCK classes** — they reuse the existing `EVALUATION`/`SIDE_EFFECT` oracles (NO new OracleKind); **G5 graduated challenge/throttle** (deferred from the gateway build) is now BUILT — per-actor Beta belief → soft retryable 429 (`challenge`→`throttle`), NEVER a belief-driven hard block, `AEGIS_RESPOND`-gated; request-side oracles extended to **header/cookie** values. SSRF/XXE stay **LEADs** (OOB is the block path — honest first slice). **WS2 autonomy loop:** scheduled the multi-critic/reflection advisors in `engage --spine` (advisory-only), a bounded **depth-2 lookahead** (`engage --autonomous-lookahead`), a read-only **`python3 -m framework.v2 plan <slug>`** projection (no traffic/tools), and a 2nd gated tool (`service_reachability`). **WS3 sensor fusion:** `verify.k8s_posture` oracle (kube-bench LEAD→FACT, read-only) → **`OracleKind` now 22** (15 frozen offensive + 7 additive held OUT of frozen `_ALL_ORACLES`: 4 AEGIS detection + 2 AEGIS gateway block + 1 K8S_POSTURE), plus an opt-in **`engage --fuse-sensors`** flag. **★ REVIEW (distinct-lens finders→refuters executing benign corpora) found 7 defects, all FP/overclaim, ALL FIXED:** SSTI blocked a spaced-verbatim reflection (`{{ 7 * 7 }}` beside a coincidental `49`) — fix: keep the VERBATIM template (`m.group(0)`) so the oracle's `raw in body` guard reads a reflection as not-evaluated + require the result at a DIGIT BOUNDARY; path-traversal blocked a docs/search page echoing `/etc/passwd` — fix: require a real `../` sequence AND skip when the value is reflected verbatim (a real LFI returns file CONTENTS, not the request path); the cmdi oracle blocked `id=https://...` in a Referer/cookie — fix: a command immediately followed by `=` is key=value, not an invocation (guard both branches); the belief escalation challenged a benign actor (1 lead amid benign traffic) — fix: benign = genuine REFUTE(0.7) that DECAYS belief + a MEAN gate. **★ THE CALIBRATION LESSON: the belief thresholds could not be guessed — I had to MEASURE the belief curve and place the gates strictly between the two populations:** benign-dominant actors top out at **mean 0.500** (the exactly-50/50 lead/benign case), genuinely-suspicious start at **0.578** (3 sustained leads), and a pure-lead stream ASYMPTOTES at **~0.63** (never higher, however many). → `CHALLENGE_MEAN=0.55` (separates benign from suspicious with margin), `THROTTLE_MEAN=0.66` (above the pure-lead asymptote, so **lead volume alone reaches only `challenge`, never `throttle`** — throttle is structurally reserved for repeated CONFIRMED attacks). 2 regression tests lock the FP + the leads-never-throttle doctrine. **META: this is the SAME near-zero-FP-can't-be-self-certified lesson as the gateway build, now with a corollary — when a detector has a TUNABLE threshold, tune it against the executed/measured distribution of benign vs attack, not intuition; my first over-correction (0.62/0.72) killed a real feature (sustained leads could never challenge) by sitting ABOVE the TP floor.** See [[unified-autonomy-program]].

**==AEGIS MVP BUILT + MERGED to main @ 62d4af3 (merge), 649b530 (graphify). The frontier is no longer just a vision — the first slice ships.==** Design flow: judge-panel design workflow (3 architectures: MVP-pragmatic / max-moat / detection-depth) → judge chose **max-moat** (deepest reuse of the reasoning core) → doctrine critic returned **REVISE w/ 5 bounded fixes** → built locally (a stalled cloud-remote attempt landed nothing; rebuilt via a local worktree agent) → **adversarial review found 4 real survivors, all fixed** → merged byte-identical. Design artifacts durably committed at `framework/v2/docs/aegis/` (research, doctrine-critique, signal-catalog, reuse-map) + `framework/v2/aegis/AEGIS-DESIGN.md`. **MVP scope:** class-1 **prompt-injection / system-prompt-disclosure vs the app's OWN LLM** (the app controls both LLM I/O ends, so a planted high-entropy canary → a CONFIRMED, offline-re-runnable certificate — the prove-don't-guess dual on day one) + a near-zero-FP **honeypot automated-access tripwire**. **Package `framework/v2/aegis/`** (models/boundary/sensors/actor_graph/guard/pipeline/middleware/cli/registry + 9 doctrine-as-tests) is the detection PIPELINE + embeddable API; the oracle BODIES live in `verify/oracles.py` (the proven 5-edit pattern — `system_prompt_disclosure_oracle`, `prompt_injection_oracle`, `honeypot_hit_oracle`) so there is NO aegis↔verify import cycle. Reuses UNCHANGED: OracleVerifier.confirm + reverify_context certificate, veracity.admit only-demotes, sensor→Observation→project_observation keystone (new IntelSourceKind REQUEST_TELEMETRY/LLM_INTERACTION), per-actor Beta belief, SCE assess_finding, the fail-closed invoke_tool gate. **KEY ENGINEERING LESSONS: (1) 5 doctrine fixes = G1 FREEZE `verifier._ALL_ORACLES` to an explicit pre-AEGIS tuple (not `tuple(OracleKind)`) so additive AEGIS oracle kinds NEVER run on the unknown-class fallback → gate stays byte-identical even as the shared enum grows (this ALSO required updating the existing `test_oracles_for_unknown_class_falls_back_to_all` to the frozen-set invariant); P1 honeypot ⇒ `automated_access` NOT "scraping" + crawler-allowlist ⇒ polarity REFUTES; P2 canary ⇒ `system_prompt_disclosure` (proves LEAK) reserving `prompt_injection` for the control-vs-treatment BEHAVIOR-DELTA path (proves adversarial CAUSE); PR1 the class-1 cert honestly retains the sentinel + a BOUNDED redacted span in plaintext (reverify needs verbatim substrings — hashes can't match); PR2 keyed-HMAC + /24-IP pseudonymization not bare hash; PR3 ReDoS-safe bounded matchers. (2) The 4 REVIEW SURVIVORS were all things the doctrine-as-tests missed: a HIGH cert-retains-WHOLE-transcript leak (the redacted span lived only in OracleSignal.observed and was discarded; the cert kept the full 8192-char output → PII survived) → FIX retain a bounded window around the canary; a MED from_prompt_injection retaining full behavior dicts (raw transcript) → FIX whitelist to {tool,refused,boundary_token_echoed}; a LOW ActorGraph._evict trimming the LRU but NOT the world node → FIX additive WorldModel.remove_node; an INFO honeypot dual-source (guard vs config) desync → FIX sensor reads the guard's live set. LESSON: prove-don't-guess + gate-byte-identical are necessary but NOT sufficient for a defensive detector — the PRIVACY/retention surface (what lands in a certificate that gets SERVED) is a distinct axis the adversarial review is essential for; a cert that's re-runnable can still over-retain PII.** ROADMAP (design §7, each rides the same pipeline + one sensor + one oracle): class-3 credential-stuffing/ATO (SPRT+Holm, stats already exist), class-2 stored/RAG injection (hidden-char + spine-hash-linked end-to-end), class-4 agentic-scraping behavioral, class-8 model-extraction, class-6 phishing/UGC, class-9 synthetic-identity; classes 7 (human-mimic bots), evasion, membership-inference stay LEAD-only PERMANENTLY (shipping them as verdicts would be the "AI detects AI" hype the doctrine forbids). See [[unified-autonomy-program]].
