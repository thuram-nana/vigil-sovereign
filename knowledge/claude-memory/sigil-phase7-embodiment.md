---
name: sigil-phase7-embodiment
description: "SIGIL Phase 7 'Embodiment' — UI + Operator + Live Vision + Cross-platform/Mobile; the advanced-capability program layered on the finished engine"
metadata:
  node_type: memory
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
  modified: 2026-07-18T19:52:36.050Z
---

**SIGIL Phase 7 "EMBODIMENT"** — the advanced-capability program on top of the finished [[sigil-phase6-hardening]] engine (all 6 base phases done). Repo **`thuram-nana/sigil`** (private). Owner wants: a real **UI**, an **Operator** (open folders/files + run terminal commands on request), **live vision** (see/identify objects), and it running on **every PC & phone OS** — each **peak-innovative, not just wired**, strictly inside doctrine. **Master plan in the plan file `~/.claude/plans/parsed-popping-lemur.md` (§"SIGIL PHASE 7").** Ritual per unit: reuse-first → deterministic tests → real proof → **red-pen adversarial review** (it caught real bugs EVERY unit) → fix + negative controls → **adversarial re-check on the fixed code** → PR → merge.

**Two SHARED PRIMITIVES unlock everything (both built):**
- **`KernelClassifier`** (`sigil/agents/kernel_classify.py` + kernel `classify` subcommand) — bridges the Rust `tiers.rs` danger-classifier to Python, closing the Python↔Rust WARDEN gap. `classify(tool)->Tier`, fail-closed A3 on any error; reuses `Warden::decide` (registry raise-only), writes NO action-log record. Used by the Operator (per-step tiering) + Vision (frontier-egress gating).
- **`SpineTailer` + `verify_record`** (`sigil/spine/tail.py`, `verify.py`) — integrity-verifying live tail. **Red-pen HIGH:** unkeyed hashes can't resist a recompute-capable writer → fixed with a TWO-LAYER model: `integrity_ok` (unkeyed binding+derivation+linkage = accidental/naive tamper) + **`anchored`** (KEYED via the owner-signed head `checkpoint.classify_head` + monotonic high-water = tamper-EVIDENT for the notarized prefix; the fresh tail is labelled un-notarized, never served as truth). Powers the UI SSE feed + mobile push. `Broadcaster` fan-out.

**STATUS: PHASE 7 COMPLETE — ALL 5 UNITS MERGED (PRs #2-#6). Full system 231/231 (206 Python + 25 Rust).** Red-pen caught a real (usually HIGH) defect in EVERY unit; all fixed + negative-controlled + re-checked. WS-C @ PR#5 (2cb6792), WS-D @ PR#6 (52bbfe4).

**PROGRESS: P0 + WS-A + WS-B + WS-C + WS-D ALL MERGED to main.**
- **PR #2 @ merged** — P0 shared primitives (13 tests; red-pen HIGH fixed).
- **PR #3 @ merged** — **WS-A Live Vision** (`perception/{vision,egress,veracity,recall,delta}.py`): MoondreamVision local (Ollama, A0) + ClaudeVision frontier; **frontier upload is A2 EGRESS, queued, uploads NOTHING until a verified owner approval bound to the exact (frame,question) egress_token** (models carry `egresses` so perceive/ambient STRUCTURALLY refuse a frontier model); grounded object claims (VLM lead promoted only on verbatim OCR corroboration); spatial `recall` (verbatim OCR line, grounded-only, latest-wins); ambient perceptual delta (OCR-token divergence OR additive-token count, not byte-identity). Red-pen 5 fixed incl. HIGH: the advisory reading was rendered RAW → now GUARD-PREFIXED so it can't forge the column-0 authoritative boundary. 19 tests. `sigil agents perceive [--frontier|--approved <seq>|--recall <subj>]`.
- **PR #4 @ d249137** — **WS-B Operator** (`agents/operator.py` + `operator_scope.py`): two-ring scope sandbox (read ring / narrower auto-write ring, realpath+is_relative_to, empty=deny-all); **PLAN→PREVIEW(no mutation: difflib/manifest/argv)→APPROVE(hash-bound)→EXECUTE(pre-image journal, atomic, mode-preserving, VERIFY by re-reading the world)→ROLLBACK/UNDO**; tier DERIVED per step from the oracle (shell.exec.*/delete=A3; write auto only in auto-write ring else A2-queued). Red-pen **8 fixed** incl. the keystone **BLOCK-2 approval-gate bypass** (execute trusted a stored `plan_tier` the hash didn't cover → now RE-DERIVES tier from the hash-bound preview) + mode-preservation + honest PARTIAL rollback + single-shot/hash-bound undo + **file bytes go to a 0700 plan journal, NEVER the append-only spine (BLOCK-7)** + hardlink-read refusal + two-ring queued-write. 19 tests.

**Full system after WS-B: 186/186 (161 Python + 25 Rust).** Backward compatible each unit.

**RECURRING LESSON (reinforced HARD this program):** the red-pen caught a real, often HIGH, defect in EVERY unit — the security primitive that isn't on the *enforcement path* (P0: unkeyed tail; WS-B: trusting a stored tier field the hash didn't bind). Near-zero-defect can't be self-certified; the adversarial RE-CHECK on the fixed branch (re-run the exact repro) is the merge bar.

**WS-C Real UI (PR #5)** — loopback glass cockpit `sigil/ui/` (`sigil serve`): stdlib two-plane server binds 127.0.0.1 only; READ plane token-gated (192-bit, terminal-printed, page-embedded data-attr); ACTION plane +exact-Origin +Host-allowlist (anti-rebinding); provenance `/api/record` re-verifies each atom LIVE; SSE `/api/stream` read-only-by-construction over the SpineTailer; owner-signed action broker `ui/actions.py` (key never in browser); strict CSP + external 'self' JS/CSS. Red-pen 5 fixed (exact-Origin match; externalized JS/CSS so CSP doesn't inert the page; /api/ask full-gated; body-cap+timeout).

**WS-D Cross-platform + Mobile (PR #6 @ 52bbfe4)** — `sigil/platform/` (HostBackend by sys.platform, Linux delegates verbatim, macOS/Windows additive honest-None; SecretStore keyring-first, secrets never on spine); `sigil/mesh/` (owner-SIGNED host_capability + 1-of-N device-authorization ledger; `verify_approval`/`pending` extended with an owner-minted authorized-device set — **now threaded into the operator-execute + vision-egress gates so an owner-authorized phone genuinely approves real executions**, RP-APPROVAL-2/4 preserved); `sigil/bridge/` (PushNotifier {seq,tier,kind}-only over the shared SpineTailer no-cloud; BridgeDaemon device-signed-approval/panic-halt/KERNEL-relay; bind_ok refuses 0.0.0.0/public — WG HTTP transport is the documented next slice). Red-pen: device-auth crypto HELD a full battery; 2 honesty findings fixed by making the capability real + honest docs.

**SIGIL NOW: all 7 phases (0-6 base engine + 7 embodiment) COMPLETE.** Remaining real-world (not construction): the WG HTTP transport for the bridge + phone PWA; native macOS/Windows manual verification; wiring the Rust kernel's 2 hardcoded paths for full cross-OS; a real VLM/Ollama for live vision; and the "one month, zero unauthorized A2/A3" soak.
