---
name: sigil-phase8-frontier
description: "SIGIL Phase 8 FRONTIER — grounded scraper (SCRIBE), gesture control (SIGIL-HAND), owner-consented web actor (DELEGATE); all 3 merged"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
  modified: 2026-07-18T22:52:06.829Z
---

SIGIL Phase 8 "FRONTIER" = 3 frontier capabilities on the finished engine (Phases 0-7, see [[sigil-phase7-embodiment]]), each its own reviewed PR to `thuram-nana/sigil` main. Plan @ ~/.claude/plans/parsed-popping-lemur.md (Phase 8 section). **ALL 3 WORKSTREAMS MERGED.** Load-bearing discovery reused throughout: the **WARDEN oracle already self-enforces the doctrine** — `account.create`/`login`/`submit.form`/`purchase.item`/`form.fill`/`browser.navigate`/`web.fetch`/`scrape.fetch` all classify **A3** (explicit, per-action, owner-signed, NO promotion) by the real Rust kernel; only clean read-verbs (`http.get`) are A0. Honest tool names make the whole thing gated with near-zero kernel change.

**WS-E SCRIBE (PR #7 merged)** — grounded scraper/web-research @ `sigil/scrape/` + shared `sources.fetch_raw`. VOI-priority crawl frontier, per-host RateLimiter, robots.txt RESPECTED (via SSRF-gated fetch_raw, fail-closed 5xx), 429/503 backoff, stdlib structured extraction, every page a cited `web_page` spine record through the IDENTICAL `gate.admit` demote-only veracity gate → cited `report`. `WebResearcher(Scholar)` A1. Red-pen: BLOCK-1 (SSRF CGNAT — added `or not ip.is_global` to `_vetted_ip`, hardened the SHARED gate), BLOCK-2 (silent max-pages cap → recorded). 13/13.

**WS-F SIGIL-HAND (PR #8 merged)** — fast owner-armed gesture control @ `sigil/gesture/` (shaped like `voice/`). Persistent low-latency camera STREAM (`perception/camera_stream.py`, one warm ffmpeg pipe), Stage-1 OnnxHandLandmarker (`egresses=False` so the loop structurally refuses an egressing model), Stage-2 invariant-feature classifier, debounced fail-safe intent FSM (confirm_frames hysteresis, hand-loss→disarm, session_max→stop). **KEYSTONE:** owner-armed SESSION (signed `gesture.session_armed`/`disarmed`, session-token the ONLY injection authority); tier from a `tiers.rs` extension — EXACT-NAME `INPUT_A1` (`hid.pointer.move/click/scroll/drag`→A1) / `INPUT_A2` (`hid.type`/`hid.combo`/`hid.app.launch`→A2) checked AFTER danger-first (so `hid.pointer.delete`→A3). A gesture can NEVER type a password or launch an app (A2 queues). Per-OS InputBackend. Red-pen: 4 honesty findings (discrete A1 unaudited; no session bound→TTL; arm didn't require owner key; skip-as-pass→real-oracle test). 14/14, 26 Rust.

**WS-G DELEGATE (PR #9 merged @ 1ffe746)** — owner-consented identity/account manager + web actor (LAST, highest-risk: credentials + outbound). Ceiling A2, added to `NO_PROMOTION_AGENTS`. Files: `agents/vault.py` (CredentialVault — NO password field, keyring ref only, manifest 0600 off-spine), `agents/actor_gate.py` (`action_token`+`action_approved`, egress-token mirror), `agents/actor_scope.py` (deny-all origin allowlist + creation cap), `agents/web_engine.py` (HttpEngine/FakeEngine, `detect_block`), `agents/actor.py` (the Delegate agent). Transaction: PLAN→PREVIEW (A0 dry-run GET, block-detect, redacted A3 queue, executable step journaled 0700 off-spine)→per-action owner-signed APPROVE bound to `action_token`→EXECUTE (single-shot; re-fetch; block-before-act; re-derive token; resolve creds last-instant; act). Offense-free BY CONSTRUCTION: no `as_identity` param (impersonation by absence); block STOPS+surfaced never defeated; NO browser path in the actor (HTTP-only); per-service+origin creation cap; credentials never on spine/journal/log/UA. **26/26 (full suite 213).**

**WS-G dual review was the merge bar (highest-risk → red-pen + independent adversarial sweep, MANDATORY).** Each reviewer pulled its weight — caught DISTINCT real defects: **2 HIGH** — (1) `execute()` non-idempotent: the append-only approval never retires → ONE signature authorised UNBOUNDED POSTs (the Operator BLOCK-6 single-shot lesson, missed; fixed with a `target_seq`-bound applied-record scan); (2) creation cap was preview-only → a batch previewed before any execute bypassed cap=1 (fixed: re-check at execute). **1 MEDIUM** — `action_token` didn't bind the URL + journal keyed by token → two same-origin URLs with identical HTML collided so an approved `/login` fired at `/promo` (fixed: URL in the token preimage → distinct tokens/journals). Plus DNS-rebind split (fetch pinned IP-A, act re-resolved IP-B → thread the vetted IP through), silent field-drop, honesty over-claims (phantom BrowserEngine, dead needs_js), vault perms. **Adversarial RE-CHECK on the fixed code PASSED** (guards proven load-bearing: blinding single-shot revived 3 POSTs; reverting url-in-token re-collided the pages) + flagged 3 non-blocking residuals all closed (malformed-URL fail-closed; cap per-ORIGIN not just label; refuse to act on an unretrievable page). Every finding got a negative control that fails pre-fix / passes post-fix.

**LESSONS (reinforced):** (a) a security primitive off the enforcement path is theater — the non-idempotent execute was a *proven-elsewhere* pattern (Operator single-shot) silently dropped when "fusing" transactions; the fusion inherited the page-hash idea but lost the local-pre-image-consumption that made Operator single-shot. (b) near-zero-FP CANNOT be self-certified by one reviewer — the dual review + independent-repro bar caught what a single pass would miss (each reviewer found a different HIGH/MED). (c) the adversarial RE-CHECK (re-run the exact repros on the FIXED branch + hunt guard bypasses) is what proves fixes are load-bearing and catches fix-introduced defects. **gh account = thuram-nana** (auth-status mislabels "Water-Hacker"). NOTE: the auto-mode classifier flagged the self-review-then-merge of PR #9 as merge-without-human-approval — consistent with the owner's standing "push and merge then proceed" directive on their own private repo, but worth surfacing.
