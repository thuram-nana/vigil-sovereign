---
name: vigil-command-ui-program
description: VIGIL COMMAND — one unified modern beginner-friendly UI over the whole system; phased P1..P7 + WS-TOOLS
metadata: 
  node_type: memory
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
  modified: 2026-07-26T09:53:30.191Z
---

VIGIL COMMAND — the operator's "one complete UI for everything" ask on top of [[vigil-production-hardening-program]] (repo thuram-nana/vigil-sovereign, `/home/kali/vigil`). Plan: `/home/kali/.claude/plans/parsed-popping-lemur.md` (approved, overwrote the prod-hardening plan). Operator decisions: **design-slice-first-then-autonomous**, **no-build design system** (plain JS, zero deps, strict-`'self'`-CSP native), **both local + web-hosted**. Later hard adds: **NO hardcoded/placeholder/demo data anywhere in the shipped UI** (real endpoints + honest empty states only); an in-app **Manual** section explaining every part; and **OS-aware tool provisioning** (setup detects the OS + installs every engine tool; UI shows each tool installed/missing/failed).

**Architecture (honors the LOCKED two-process boundary):** ONE origin, ONE no-build bundle in `packages/vigil-ui/` (tokens.css+components.css→style.css, ui.js micro-kit [VUI.h/store/router/getJSON/postJSON/sse/icon/badges], app.js shell+screens, manual.js, index.html with `__VIGIL_TOKEN__/_SOVEREIGN__/_OFFENSE__` body-dataset placeholders, manifest.json allowlist, sync.sh author-time vendoring). A reverse proxy federates `/sovereign/*`→cockpit:8733, `/offense/api/v1/*`→api:8799, `/offense/*`→console:8787. Two visual planes: gold=Owner/sovereign, cool=Work/offense+defense. Aesthetic = theme.png (dark near-black monochrome, big legible type, pill chips, rounded cards) on the img.avif live layout.

**MERGED so far (each: build→red-pen→fix→re-check-on-fixed-branch→6-job CI→merge):**
- **P1 #62** — design system + shell + live Home + Manual + offense federation (strict CSP + exact Host/Origin allowlist on console+api via `--allow-host/--allow-origin`+`$CRUCIBLE_UI_ALLOWED_*`; default byte-identical loopback-only). Red-pen BLOCKs fixed: CSP must NOT go on the legacy console `_static` SPA (breaks its inline handlers) — CSP on data responses only; allowlist-exactness tests were green-washed (negative shared no substring) → added suffix/substring negatives, mutation-verified.
- **WS-TOOLS #63** — OS-aware provisioning in `bootstrap.sh` (`--with-tools`; /etc/os-release ubuntu/debian/kali; consented apt/pipx; fail-soft per-tool with the exact manual command; non-Debian apt-only → honest "missing" not "failed") + `engine/crucible/framework/v2/tools/registry.py` (`probe_tools()` LIVE `command -v`+version-banner; `registry --check NAME`) + `/api/tools` endpoint + a Tools UI screen. Host tools discovered from code: nmap,httpx,nuclei,ffuf,sqlmap,hydra (core) + semgrep,joern,tshark,chromium,nikto,wapiti,zaproxy; Burp excluded (REST, no binary); strix sandbox tools listed separately (image-provided, never host-probed). Red-pen BLOCK: a same-named Python `httpx` on PATH shadows ProjectDiscovery `httpx` → false-green for a REQUIRED tool. Fix: `wrong_markers` banner disambiguation → status **`shadowed`** (installed=False, counted required_missing); the installer shares it via `registry --check`. Mutation-verified. LESSON: `command -v` alone lies for name-colliding tools — the version banner is the disambiguator.
- **P1b #64** — `vigil up`/`down` (native exec-only verbs; import NO framework/strix/sigil — boundary-tested) + `integration/vigil_integration/uiproxy.py` a SELF-CONTAINED stdlib reverse proxy (no external dep for local): binds loopback/tunnel only (inline bind_ok; refuses public — Teredo/6to4 too), routes to the 3 FIXED 127.0.0.1 backends (not an open proxy), STREAMS SSE, forwards original Host/Origin (backends allowlist the proxy authority). `--domain` for hosted + `deploy/reverse-proxy/vigil.{Caddyfile,nginx.conf}` TLS templates. Makefile up/down; bootstrap prints `vigil up`. Red-pen BLOCKs fixed: (1) static path didn't `Connection: close`/drain body → request-smuggling desync (cross-user poisoning behind pooling Caddy) → drain+close, mutation-verified; (2) runtime `.vigil-live/ui/`+index.html+cockpit-log held the sovereign token world-readable → 0700 dir / 0600 token files. Hardened: `--domain` without `CRUCIBLE_API_KEY` now REFUSES (exit 2, `--insecure-no-api-key` override); 16 MiB body cap. Re-check PASS.

- **P2 #65** — New-Assessment wizard (codebase/URL/one-tool/full-suite/AEGIS) + live run view over the 14 blackboard event kinds (FACT/LEAD, inline owner approvals); `/offense/api/launch/assessment` (subprocess dispatch, approve-then-run, scope charter-signed never argv, capabilities=gated engage flags) + `/offense/api/blackboard` SSE (BlackboardTailer, durable Last-Event-ID cursor).
- **P3 #66** — Findings / Attack-Graph / Evidence / Coverage / Timeline (reuse read-only offense endpoints + evidence() cert_id). Red-pen PASS'd the FACT gate/evidence/zero-traffic/CSP/boundary but BLOCKed on a false-RED honesty defect: the Attack-Graph node/edge drawer keyed FACT on the REPORT vocabulary (`grounding==="fact"`) but /api/worldmodel uses a DIFFERENT vocabulary (`classify_provenance`→grounded/intel/ungrounded/unclassified, NO "fact") → every oracle-GROUNDED node rendered LEAD + the legend affirmatively called proven facts "unproven." FIX: `p3WmFact(g)= g==="grounded"` drives edge chip + node legend fires only for non-grounded; LOW-1 re-verify word "sound"→"reproduced"; LOW-2 (why it slipped: no test) → `test_worldmodel_grounding_vocab.py` pins BOTH vocabularies (wm fact tier=="grounded"; report=="fact"; intel/llm/empty NOT fact) so a backend rename fails CI. LESSON: two grounding vocabularies exist (world-model vs report) — a UI predicate must key on the SAME one the endpoint emits, and pin it with a contract test.

- **P4 #67** — Approvals & Safety + Settings (the operator's API-key + model section) on the gold OWNER plane. Reuses the hardened `SecretStore` (keyring→TPM-sealed→0600 envfile). `apps/sigil/sigil/ui/settings.py`: `set_secret` (closed SECRET_NAMES allowlist; rejects empty/oversized/**control-char** [envfile line-injection]; seals + records a **fingerprint-only** owner-signed spine event — value NEVER in return/status/spine/log/HTTP), `set_model` (closed catalog, **served** so UI hardcodes no model list), `settings_status` (redacted), `export_runtime_env`. Writes ride the existing owner-signed broker (`ui/actions.py`; token+exact-Origin+Host); `GET /api/settings` = redacted read. Cross-plane bridge: `sigil settings export-runtime-env --include-secrets` resolved in the SOVEREIGN venv → `vigil up` captures on a PRIVATE pipe (never teed logs), key-allowlisted, fail-soft, injects into the keyless offense children (offense still imports no sigil). UI: Settings (seal-key + served model picker) + Approvals&Safety (queue/kill-switch/caps/live spine feed) over existing endpoints. **Red-pen BLOCK-1 (the operator's forbidden PLACEBO class):** `claude-code` is a BACKEND name not a model id — set_model stuffed it into CRUCIBLE_ANTHROPIC_MODEL → AnthropicBackend got model='claude-code' (every call errors) + never set CRUCIBLE_LLM_BACKEND → flagship keyless option broken on BOTH planes + lied. FIX: per-choice `_env_plan` (keyless→route CRUCIBLE_LLM_BACKEND+clear model-id vars; API-model→set model ids+clear backend; `_persist_env` truly clears line+os.environ.pop; VIGIL_MODEL_CHOICE tracks the picker). Proven end-to-end on both planes (claude-code→get_backend selects claude-code, SDK model never poisoned; API-model→load-bearing). +OBS-2 bridge key-allowlist. Re-check PASS (secrets/auth/injection core held, tests non-tautological). LESSON: a control that writes an env var NOTHING reads (or the WRONG var) = a placebo = a BLOCK; verify the value is actually consumed by the real backend on BOTH planes. Backend-selection vocab: offense CRUCIBLE_LLM_BACKEND (override) → AnthropicBackend reads CRUCIBLE_ANTHROPIC_MODEL / ClaudeCodeBackend reads CRUCIBLE_CLAUDE_CODE_MODEL+uses `claude -p` keyless; sovereign scholar `claude -p --model SIGIL_LLM_MODEL`.

- **P5a #68** — AEGIS Defense dashboard (the "AEGIS gets a UI" ask), designed off a 5-agent parallel UNDERSTAND workflow. `aegis gateway --verdicts-out/--status-out`: thread-safe on_verdict sink appends BROWSER-SAFE verdicts JSONL (drops certificate.oracle_context — a matched-span plaintext — never streamed) + a daemon snapshots effective-mode+per-actor Beta beliefs; files 0600. `ActorGraph.snapshot()` accessor. Console: POST /api/aegis/setup (Popen a MANAGED gateway — persistent serve_forever needs Popen not timeout-bounded subprocess.run; fail-closed validation BEFORE spawn incl. honeypot-path leading-'/' guard [self-fuzz caught a '--'-prefixed honeypot reaching Popen]; returns production edge cmd secret-redacted), /api/aegis/stop (SIGTERM idempotent), GET /api/aegis/status (honest empty state), SSE /api/aegis/verdicts (reuses _sse tailing the JSONL). UI renderDefense: SEMANTIC INVERSION honored (confirmed=ATTACK=danger/red, NOT offense green; "clear"≠safe), honest setup (no phantom canary/HMAC-signing — those are SDK/privacy not the proxy; deployment_secret labeled privacy-not-auth; actor list = raw client source BY DESIGN [gateway `_actor_key`=client IP for defender correlation, not the SDK-path HMAC pseudonymisation]). LIVE SMOKE proved real (3 confirmed automated_access verdicts, oracle_context stripped, status actor belief) + NEGATIVE CONTROL (benign→0 confirmed). **Both red-pen agents STALLED on the infra stream-watchdog (not on findings) — recovered via the documented exhaustive-self-fuzz protocol** (oracle_context-strip, argv/flag-injection reject-before-spawn, gate behind _same_origin_as_console, 50-concurrent thread-safety, negative control, no green-wash). Aegis test file added to CI crucible-core (it already installs httpx+bs4). LESSON: any `framework.v2.aegis.*` import triggers the package __init__ → pulls httpx (so console tests that must stay light keep aegis imports LAZY/off the refusal path); a persistent server needs Popen not subprocess.run; confirm the model/backend value is consumed on BOTH sides.

- **P5b #69** — finding-seam live caller (offense→sovereign, the FATAL-2 inert seam). Offense `integration/vigil_integration/finding_spool.py` (stdlib+inert_finding only, no framework/strix/sigil): `spool_envelope()` writes a pre-built inert envelope atomically (0600 file/0700 dir, content-derived name), fail-closed on non-str/empty/oversized/non-JSON. Sovereign `apps/sigil/sigil/inbound/spool_watcher.py` (no framework/strix; vigil_core verify only) + `sigil inbound drain|watch`: routes by kind to an owner-delegated receiver (finding→from_delegation/OFFENSE_GOVERNANCE_ROLE, detection→from_spine_delegation/OFFENSE_SPINE_ROLE), ingests→spine→cockpit feed auto-surfaces; any failure quarantines to rejected/+.reason, appends nothing; NO cockpit ingest endpoint. **THREE red-pen rounds, each caught a real defect** (the discipline working): boundary HELD every round (no laundering/forgery/routing-bypass/mislabelled-kind) but the NEW wiring had: BLOCK-1 FIFO/non-regular-file DoS (naive stat+read_text blocks forever on a planted named pipe) → fix `_read_regular` O_NOFOLLOW|O_NONBLOCK+S_ISREG; BLOCK-2 double-ingest (failed archive re-ingests + lifecycle re-spool re-ingests; a comment+test overclaimed idempotency) → fix CLAIM-FIRST (incoming→working rename before append) + processed/ dedup ledger; BLOCK-3 non-UTF-8 crash (decode raised UnicodeDecodeError=ValueError, escaped `except OSError`, crashed watch()) → normalise decode→OSError; +OBS-2 dedup keyed on producer FILENAME (suppression vector) → re-derive sha256 from READ BYTES. Final PASS re-attacked dir-as-file/JSON-recursion-bomb/empty-file/O_NONBLOCK-truncation — all held. LESSON: a fix's NEW code is itself new attack surface (BLOCK-1's read path birthed BLOCK-3) — re-check every fixed branch; "never raises / any IO failure quarantines" must cover ValueError-family (decode/recursion) not just OSError; content-addressed dedup must re-hash the bytes, not trust the producer's name.

- **P6 #70** — Fixes screen ("apply fixes after discovery"), done HONESTLY after a 4-agent UNDERSTAND workflow found TWO hard facts: the remediation/autopatch pipeline (vigil_integration.remediation/autopatch) is a FULLY-INJECTED library with NO live runner (every executor — git clone/edit/build/open-PR + gate/quorum/approval/oracle/signer — must be injected), AND it's NOT on the CRUCIBLE console's import path. So P6 does NOT fake a fixer or auto-open PRs (outward-facing/destructive = needs explicit operator authorization + executor provisioning — DEFERRED, offered in the PR). `GET /api/remediate/<run_id>` (api.remediate_plan) composes REAL data: the run's FIXABLE findings (ONLY grounding=="fact" oracle-confirmed; leads counted never offered) each with the report's own `remediation` text, severity-sorted + the served gated ladder-of-record (triage→clone A1→edit A2→build A3→open-PR A3·m-of-n→verify-when-oracle-silent). `live_execution` always False; honest pending/empty. UI Fixes screen (nav ready) + choke-points (reused worldmodel) + honest live-boundary banner; wizard `apply_fixes` (was inert) now surfaces the plan. Red-pen PASS (no fabrication/leak/overclaim; ladder accurate; read-safe vs 9 hostile reports) + flagged a pre-existing console-wide `run_dir` path-traversal-read.
- **P7 #71** — Brain screen + a11y + the run_dir hardening. `run_dir(run_id)` now guards the URL-derived id (regex single-safe-component + reject `..`) → ValueError→404 in do_GET/do_POST; ONE guard covers all console read routes (report/worldmodel/coverage/evidence/remediate/reverify). Brain (nav ready): Memory (`/api/memory` per-archetype/bug-class Beta success priors) · Benchmark (`/api/benchmark` tp/fp/fn incl. safe controls) · Catalog (`/api/capabilities` searchable + advisory-only-reasoning note) · Intel/Planner (run-scoped) — all REAL endpoints, honest empty states, no backend changes. a11y: keyboard-operable nav (tabindex/role/Enter-Space/aria-current), aria-labels on icon buttons, `:focus-visible` ring. Red-pen PASS (45 hostile ids incl. unicode/RTL/NUL/%-encoding all refused; Brain no fabrication; a11y/CSP held) → 3 non-blocking notes all addressed (accurate Memory prior labels, symmetric POST 404, doc comment).

**ENTIRE VIGIL COMMAND UI PROGRAM COMPLETE — P1..P7 + WS-TOOLS, 10 PRs #62-#71, all merged, every 6-job CI green.** One unified no-build CSP-native UI over the whole system (offense engage/scan/strix + AEGIS defense + sovereign governance), federated one-origin via `vigil up`'s stdlib reverse proxy, real-data-only with honest empty states, in-app Manual, OS-aware tool provisioning, API-key+model Settings, Approvals/kill-switch, Fixes, Brain. Two-process boundary + no-fake + oracle-authority held throughout. EVERY slice: build→adversarial red-pen→re-check-on-fixed-branch→6-job CI→merge, and EVERY red-pen caught ≥1 real defect (placebo model control; fact-labeling false-RED; FIFO-DoS/double-ingest/non-UTF8-crash on the FATAL-2 seam over 3 rounds; path-traversal read). DEFERRED (needs explicit operator authorization — all outward-facing/destructive): the LIVE auto-patch runner (real git clone/build/open-PR executors + gate/quorum/oracle wiring); live `vigil up`/full-stack runs are operator-machine only (this box has a partial .venv-offense, no sovereign venv, no browser). Red-pen agents intermittently STALL on the infra stream-watchdog (P5a stalled twice) → recover via the documented exhaustive-self-fuzz OR relaunch fresh; P5b/P6/P7 red-pens completed cleanly.: offense `integration/vigil_integration/finding_spool.py` writes signed inert envelopes to `<spool>/incoming/*.json` [0700 dir/0600 file, atomic] → sovereign `apps/sigil/sigil/inbound/spool_watcher.py` + `sigil inbound watch|drain` verifies anchor-1 via vigil_core under from_delegation[findings=OFFENSE_GOVERNANCE_ROLE]/from_spine_delegation[detections=OFFENSE_SPINE_ROLE], ingests→spine→cockpit feed auto-surfaces; fail-closed=quarantine-never-append; NO cockpit ingest endpoint; sigil.inbound imports NO framework/strix). Uses the F-phase/S7c inert_finding + finding_receiver primitives. Pair with the adversarial-sweep skill. REMAINING after P5b: P6 fixes ladder, P7 brain+polish. GOTCHA: the unified UI bundle is served ONLY via the `vigil up` proxy (assemble_serve_dir copies from packages/vigil-ui); the standalone `sigil serve`/`crucible console` keep their OWN legacy SPAs in their static dirs — do NOT run sync.sh + commit (it clobbers the legacy SPAs). Edit only packages/vigil-ui. sigil tests run on this box via `SIGIL_HOME=/tmp/... PYTHONPATH=apps/sigil .venv-offense/bin/python -m pytest apps/sigil/tests/...` (add new sigil test files to the EXPLICIT list in ci.yml's sigil-governor job); offense framework tests via `PYTHONPATH=engine/crucible` in a SEPARATE process from any sigil import.

**LESSONS / gotchas:** background agents WITHOUT `isolation:"worktree"` SHARE the working tree → the first WS-TOOLS agent collided on my branch (stopped, preserved WIP, re-launched isolated). Even worktree isolation resolves against the SESSION cwd (`/home/kali/Pictures/PENTEST-main`, the CRUCIBLE repo) NOT `/home/kali/vigil` — agents must `cd /home/kali/vigil` + branch there; they end up switching that shared tree's branch (fine if committed). Reference images (theme.png/img.avif) are gitignored so `git add -A` can't sweep them. CI's crucible-core job now also runs `framework/v2/console framework/v2/api` (added in P1). A partial `.venv-offense` (vigil-core only; framework via `PYTHONPATH=engine/crucible`) + no sovereign venv here → full `vigil up`/live runs are operator-machine only; endpoints/SSE/proxy/boundary/registry ARE unit-tested. A visual PREVIEW artifact (self-contained HTML of the shell/wizard/live) was published for the design-first review. Every red-pen + re-check caught a real defect a builder's self-report missed — never trust the self-report.
