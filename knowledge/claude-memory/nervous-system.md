---
name: nervous-system
description: "CRUCIBLE unified event-spine + RL/metacognition/multi-critic program (N0-N7), additive over the merged veracity layer"
metadata:
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
---

Operator directive (verbatim intent): every subsystem (OSINT/intel, world-model, planning, verification, evidence, governance, operator actions, reporting, ...) communicates through ONE immutable event stream; an advanced RL + metacognitive-feedback core with a MULTI-AGENT CRITIC architecture + cognitive-refusal + reflection loops, baked into the CORE **and** the system-prompt/rules layer; plus a detailed integrated AI skill doc. Plan file: `~/.claude/plans/parsed-popping-lemur.md` (overwrote the completed anti-hallucination plan). Grounded by a 6-agent architecture survey (wf_9bf53f3c).

**Operator decisions (confirmed via AskUserQuestion):** RL = *learn + reflect + re-rank/defer only* (never changes which surfaces run, never overrides the oracle); spine = *hash-linked + governance-signed head*; skill doc = *harness-loadable SKILL.md + long-form companion*.

**Key survey finding:** most primitives EXIST but are fragmented. The blackboard (`agents/blackboard.py`) is already an append-only, typed (`PAYLOAD_BY_KIND`), provenance-linked (parent_id/supersedes_id), seq-clocked (rowid), trigger-immutable event log with 8 kinds — but MAO-only; the flagship `engage.py`/scanner path bypasses it (0 refs), keeping ~5 parallel stores. Only `evidence/chain.py` is hash-linked. RL = `scanner/learning.py::ContextualBandit` (orders checks only) + `calibration/` (ledger/calibrate/conformal) + `memory/priors`. One critic (`agents/critique_agent.py`). Refusal scattered (sovereignty/kill-switch/scope/ethics + SCE/consistency/veracity abstain). Prompt layer (`kernel/binding.py::build_system_prompt`) injects only a 1-line preamble + one per-call cognitive doc — the constitution + reasoning-loops.md/self-critique.md are NEVER injected.

**NON-NEGOTIABLE INVARIANTS (every phase):** additive + default-safe (opt-in best-effort sinks; engage byte-identical when off); ORACLE stays sole authority (critics/RL only advise/re-rank/defer/abstain — never promote llm_advisory→confirmed nor gate/skip a surface); non-circular reward (INDEPENDENT ground truth, reuse critique_agent's ≥2-corroborating-kinds DISPUTED rule); LLM signals are a PENALTY only, never into oracle/SCE/calibration inputs; determinism (caller-seq not wallclock, injected rng); append-only (extend EventKind+PAYLOAD_BY_KIND+schema.sql CHECK in LOCKSTEP; supersede-not-edit; hash-link cols nullable); fail-closed refusal propagates (Coordinator re-raises CrucibleError; refusals recorded as events, never swallowed); loopback-only/air-gap-safe.

**PHASES (each own reviewed PR, additive; matches the P0-P7 cadence):**
- **N0 MERGED (PR #48, 9049dda):** vocabulary (reward/critic_verdict/reflection/refusal kinds) + SpineSink (ProgressSink adapter) + blackboard.replay() + engage `spine=` opt-in emission (findings/refusals) + CLI --spine. Review fix: finding-mirror must not claim confirmed/verified_by_oracle without a LIVE grounding verdict (admitted.is_fact only).
**== ALL N0–N7 MERGED TO MAIN (PRs #48–#55). The nervous-system program is COMPLETE. ==** One immutable event spine (blackboard, hash-linked+signed) every subsystem can emit to + advanced RL/metacognition core (non-circular reward fan-out + credit; multi-critic panel; reflection + cognitive-refusal; governance preamble in every prompt; meta-monitor + policy provider) + AI skill doc (.claude/skills/crucible/SKILL.md + framework/v2/docs/CRUCIBLE-AI.md). Every phase additive + default-safe + adversarially reviewed. RECURRING GOTCHAS: (1) bb.read() default limit=1000 silently truncates — any full-set read must PAGE or be cursor-based + fail-closed (bit N1, N3). (2) NEVER let a doc/field/guarantee OVERCLAIM what the deterministic layer enforces — the finding mirror (N0), the coverage number (N6), and the skill doc (N7) all had overclaim bugs caught+fixed. (3) oracle authority at the type level: critic_verdict endorse|object|abstain, never confirm.
- **N5 MERGED (PR #53, eae7cbf):** governance preamble. framework/cognitive/metacognition.md (6 sections: prove-don't-guess/reflect/critics/refuse/self-consistency/learn) quoted verbatim into EVERY LLM system prompt via kernel/binding.py::_governance_preamble (loads RELATIVE to the framework package via Path(__file__).parents[2]/cognitive/ — ships WITH the code, NOT CRUCIBLE_ROOT which is read-only Music), bounded+cache-stable, graceful-empty on missing. Review: 0 survivors.
- **N6 MERGED (PR #54, 36c7534):** meta-monitor (learning about learning). calibration/meta_monitor.py assess_learner_health (n/ECE/Brier + TEMPORAL-split realized conformal coverage) recommends only MORE caution (gather_evidence/trust_confidence_less), never gates. PolicyProvider+BanditPolicyProvider+rank_by_policy (orders, never drops). Review fix: MED — n=16/17 temporal split hit the trivial q>=1.0 [0,1] band pinning realized coverage to false 1.0; fixed to return None (unmeasured).
- **N7 MERGED (PR #55, e9d9a3a):** AI skill doc. .claude/skills/crucible/SKILL.md (loadable) + framework/v2/docs/CRUCIBLE-AI.md (long-form) + test_skill_doc.py (doc-integrity: subcommands/modules real, no overclaim). Cross-ref from CLAUDE.md §XIII + ARCHITECTURE.md. Review fixes: MED x2 — docs cited scan-only --strict-evidence on engage (removed+guarded); Section 4 presented additive primitives as live (added honest Wiring-status section).
- **N4 MERGED (PR #52, 449c3ae):** reflection + cognitive refusal. NEW agents/reflection.py: reflect() pure deterministic detects dead-threads (hypothesis handle w/ no confirming finding) + stalls (>=5 actions, 0 findings), re-orient=re-rank/defer NEVER skip; _all_events pages; ReflectionAgent dedups+quiesces. NEW agents/cognitive_refusal.py: epistemic_refusal refuses to CONCLUDE a finding that claims verified_by_oracle but doesn't re-ground (reuses admit match_confidence=False); emit_refusal posts typed refusal event. Review: 0 survivors (5 refuted, advisory-heuristic nits). GOTCHA: CRUCIBLE_ROOT env = /home/kali/Music/PENTEST/crucible (docs load from THERE via docs.cognitive), but the CODE runs from /home/kali/Pictures/PENTEST-main (git repo) — code+v1-docs are split across two checkouts; Music is NOT writable by me.
- **N3 MERGED (PR #51, 5eea743):** multi-critic panel. NEW agents/critics.py — deterministic lenses (Grounding/Provenance/Calibration), CriticVerdict REJECTS 'confirm' at construction (oracle authority at type level). run_panel + aggregate_panel (MAJOR objection stands; high categorical entropy→abstain; else modal; NEVER confirm). panel_verdict_for (quorum) + MultiCriticAgent (addable to Coordinator, cursor-based). Review fixes: HIGH — MultiCriticAgent re-derived reviewed-set from limit-capped read → re-reviewed forever past ~335 findings; fixed to cursor-based (review once). MED — panel_verdict_for dropped recent verdicts; fixed to page. GOTCHA (recurring): bb.read() default limit=1000 silently truncates — any full-set read must page or be cursor-based (bit N1 AND N3).
- **N2 MERGED (PR #50, 8fa6d87):** reward fan-out + credit assignment. NEW calibration/reward_bus.py: outcome_label() single-sources the NON-CIRCULAR label (EXPLOITABLE only on >=2 distinct corroborating oracle kinds, else DISPUTED; silent oracle never auto-FP) — critique_agent now CALLS it. credit_outcome() fans one outcome to bandit(productivity)/ledger(non-circular label, seq 2*seq/2*seq+1)/priors/spine-reward, only sinks passed in, best-effort. NEW agents/spine_credit.py: credit_finding_path walks parent_id back crediting decision/hypothesis/plan ancestors. engage post-scan emits a spine reward per finding. Review: 0 survivors. GOTCHA: bandit reward=oracle_fired (productivity, orders effort) is fine; the CALIBRATOR label must stay the non-circular corroboration label — never mix them.
- **N1 MERGED (PR #49, bf6d985):** cryptographic tamper-evidence. NEW agents/spine_chain.py reuses evidence/chain.py (hash-links + signed head + anti-rollback); PURELY ADDITIVE (no schema change, on-demand chain over live events; chose this over nullable columns to sidestep the CREATE-TABLE-IF-NOT-EXISTS CHECK-migration gotcha). event_digest excludes wallclock posted_at (replay-stable), binds engagement_id. Review fixes: (1) HIGH — _events paged replay's default limit=100k silently truncated large logs (failed OPEN); now pages to exhaustion + fails CLOSED (count assert, SpineChainError). (2) MED — head not engagement-bound; now binds engagement_id in digest + checks head.engagement_slug in verify. Added 4 EventKinds (reward/critic_verdict/reflection/refusal) + typed payloads (critic_verdict Literal = endorse|object|abstain, NEVER 'confirm' → oracle authority at the type level) to `agents/models.py` + `schema.sql` CHECK in lockstep. NEW `agents/spine_sink.py::SpineSink` implements scanner `ProgressSink` (phase/finding/done→observation events) + typed helpers (finding_event/refusal/reward/decision/reflection/critic_verdict), all best-effort/swallow-on-error. `blackboard.py::replay()` = subscribe/replay cursor. `engage.run_engagement(spine=None)` opt-in: passes SpineSink as campaign progress, records preflight/OOB refusals as refusal events (then re-raises), post-scan mirrors each active finding (with its P3 grounding verdict) as a finding event + summary decision. CLI `--spine`. GOTCHA: `CREATE TABLE IF NOT EXISTS` won't re-apply the new CHECK to a pre-existing .blackboard DB (fresh test DBs fine; spine opt-in) — a persistent DB may need recreation.
- **N1:** spine integrity — hash-link blackboard events + governance-signed head (reuse `evidence/chain.py` `_entry_hash`/`sign_head`/`verify_head`), nullable additive cols.
- **N2:** reward bus + credit assignment (DAG walk over parent_id, non-circular) + close the flagship engage learning loop.
- **N3:** multi-agent Critic panel (soundness/scope-safety/calibration/deception/novelty on `base.Agent`) + Coordinator quorum/abstain via `run_consistent`; oracle authority preserved.
- **N4:** reflection loop (`kernel/reflect.py` + ReflectionAgent wiring reasoning-loops.md/self-critique.md) + unified cognitive-refusal event/primitive.
- **N5:** prompt/rules governance preamble in `build_system_prompt` (bounded, cache-stable, verbatim via `common.docs`) + new `framework/cognitive/metacognition.md`.
- **N6:** meta-monitor (conformal coverage / calibration drift / bandit regret → modulates effort/abstention, never gates) + policy generalization (planner/scheduler, ORDERS-not-GATES).
- **N7:** AI skill doc — `.claude/skills/crucible/SKILL.md` (harness-loadable, structured workflows) + long-form `framework/v2/docs/CRUCIBLE-AI.md`, cross-referenced from CLAUDE.md.

Reuse-not-reinvent anchors: blackboard (spine), evidence/chain (integrity), ContextualBandit+OutcomeLedger+conformal (RL), critique_agent+consistency+SCE+veracity.admit (critics/metacog), sovereignty+ethics+killswitch+scope_gate (refusal), binding.build_system_prompt+common.docs (rules layer). See also [[anti-hallucination]], [[crucible-testing-and-gotchas]].
