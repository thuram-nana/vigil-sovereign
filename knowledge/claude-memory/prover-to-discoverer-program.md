---
name: prover-to-discoverer-program
description: "CRUCIBLE roadmap 'Prover → Discoverer (+ close coverage/integration/scale gaps)' — approved 2026-07-13. Full four-gap program: make CRUCIBLE ROAM and find new attack surface (not just prove what it's pointed at), plus fill coverage/integration/scale gaps. Plan @ ~/.claude/plans/parsed-popping-lemur.md."
metadata:
  node_type: memory
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
---

**Operator framing (2026-07-13, verbatim intent):** "you have less coverage, fewer integrations, and — the key one — it's still a **prover, not yet a discoverer** (it proves things it's pointed at; it doesn't yet roam and find new attack surface on its own). And it's early: barely tested at scale." → plan-mode → APPROVED the **full four-gap program**, discoverer as the spine. Plan file: `~/.claude/plans/parsed-popping-lemur.md`.

**Operator decisions (AskUserQuestion):** (1) discoverer autonomy posture = **BOTH** — ship `discover-and-queue` as the SAFE DEFAULT (roam/enumerate autonomously, human greenlights a batch before active testing) + an explicit `--discover-autotest` / `--autonomous-posture=auto-test` opt-in (enumerate AND test autonomously within charter scope). (2) scope = **full four-gap program**. (3) supporting priorities = **ALL THREE** (coverage, integrations, scale & soak).

**THE GAP (3 exploration passes + 1 architecture pass, all read-only, established the precise shape):**
- **The prover→discoverer wall is REAL and specific.** CRUCIBLE's ONLY roaming enumerator is the recon asset-graph subsystem (`intel/ingest.py::IntelIngest.run_collectors` BFS frontier over crt.sh/DoH/RDAP/RIPEstat; `intel/planner.py::ReconPlanner.plan` VOI ranking; `intel/predict.py::AssetPredictor.predict` gated sibling-subdomain/netblock hypotheses). It is **DELIBERATELY WALLED OFF** from the test loop — `engage.py::_intel_finalize` "never projects predictions, never auto-scans"; recon output is a HUMAN-READ "where to look" queue. The autonomous OODA loop (`engage_autonomous.py::run_autonomous_cycle`) only re-probes `NodeKind.ENDPOINT` nodes the ONE-SHOT pointed crawl already found, with a single hard-wired check.
- **★ KEYSTONE FINDING (Plan agent):** the existing discovery slice is **INERT on a real engagement**. `engage_autonomous.py::_endpoint_probe_targets` requires ENDPOINT nodes carrying a `url` attr, but **NO minting site writes a url-bearing ENDPOINT on the real `engage.run_engagement` path** (`populate_worldmodel` isn't even called there — recon mints DOMAIN/HOST, sensors mint HOST/SERVICE/APPLICATION, campaign mints ENDPOINT with `{param,target}` no `url`). So `engage … --autonomous --autonomous-discover` returns `[]` today. The single missing edge — an **asset→endpoint promotion projector** — turns the already-built OODA plumbing + gate discipline + deterministic fold into a live recon→test feed. THIS IS SLICE 0, the smallest end-to-end proof.
- **Coverage is BROADER than the framing** (27 oracle kinds; most surfaces proven). Real PROVER gaps: **mobile (total), CSRF/clickjacking/postMessage (lead-only), CI/CD-pipeline (only SCA/VERSION_RANGE proven), broad crypto/secrets.** INTEGRATION gaps: **no SARIF *ingest*** (export-only), **no SAST/IaC/secret importers** (Semgrep/CodeQL/Trivy/Checkov/gitleaks fall through the generic escape hatch), **no push sinks** (Jira/DefectDojo/Slack/SIEM). SCALE: **no load/soak harness at all**; spine is single-host SQLite, scan is serial.

**INVARIANTS TO PRESERVE (a discoverer FEEDS the seam, never weakens it):** (A) the oracle seam `verify/verifier.py::OracleVerifier.confirm` is the SOLE fact authority (a fact = ≥1 oracle ≥0.7); discovery only PROPOSES a finding_context. (B) every target routes through the fail-closed `agents/tools/invoker.py::_gate` (killswitch→entitlement→charter-scope→destructive→egress) — roaming stays INSIDE charter scope BY CONSTRUCTION (the scope gate refuses out-of-scope fail-closed; charter wildcards `*.example.com` honored). (C) `make gate` byte-identical — all discovery is default-OFF + STRUCTURALLY UNREACHABLE from `benchmark --gate`; folds reuse the existing `_fold_discovered_into_report` discipline (oracle-confirmed only, dedup `(bug_class,insertion_point,endpoint)`, re-sort to planner-order-independent key).

**PHASE 1 (SPINE) — the Discoverer, 5 reuse-first slices** (new code = 2 small modules `intel/promote.py` + `intel/frontier.py` + a ~30-line crawl wrapper + in-place edits to `ProbeSurfaceTool` & `run_autonomous_cycle`; everything else reused):
- **Slice 0 KEYSTONE:** `intel/promote.py::promote_to_endpoints(world, slug, *, seq)` — promote an IN-SCOPE recon/sensor SERVICE/HOST/DOMAIN (web-ish) into a url-bearing `endpoint:promoted:{canonical}` ENDPOINT. Scope predicate REUSED verbatim: `common/ethics.py::parse_scope`+`host_matches_scope`. Hooked in `run_autonomous_cycle` ORIENT, discover-path only, before `_build_goal_tree`; `_intel_finalize` untouched (reads projected FACTS, never the gated prediction queue). Defense-in-depth pre-filter; `_gate` stays sole authority.
- **Slice 1 DiscoveryFrontier** (`intel/frontier.py`): unify promotion/crawl/sensor sources; ORDER by the SHARED VOI kernel `planner/goal_tree.py::expected_information_gain` (no new ranker; `_select` drains 1 leaf/cycle); dedup `(scheme_host,path,sorted-param-names,bug_class)` (crawler `_location` ∩ fold key); bounds REUSE `--autonomous-budget`/`--autonomous-cycles` + a reported size cap; low prior ≤0.06.
- **Slice 2 in-loop crawl/mine:** thin `expand_endpoint` wrapper REUSING `scanner/crawler.py::Scope.from_seed`+`Crawler.crawl` over the SAME gated `discover_send` + the EXISTING `scanner/discovery.py` miners (parse_robots/sitemap/mine_js/mine_params/parse_openapi/parse_graphql_schema/discover_content). No new crawl/parse logic.
- **Slice 3 generalized probe:** extend `agents/tools/builtin.py::ProbeSurfaceTool`+`probe_surface_registry` over a FROZEN curated near-zero-FP check set (REFLECTED_XSS+CT-gate, OPEN_REDIRECT, PATH_TRAVERSAL, SSTI_EVAL_*, BOOLEAN_SQLI) via the unchanged `AuditEngine.audit`; OOB checks inert unless a receiver is wired. Shared oracle + `WebScanCampaign`/`ScanReport` UNTOUCHED → gate byte-identical.
- **Slice 4 posture seam:** `--autonomous-posture` at the single `is_probe` ACT branch. `discover-queue` (default) = enumerate/promote/crawl but ACT records a queued candidate (spine lead event), ZERO target probe traffic, operator `--approve-batch` flips to test. `auto-test` (opt-in) = today's `_drive_probe`→gated `invoke_tool`→fold. All machinery shared; only this branch differs.

**★ PHASE 1 COMPLETE + MERGED to main (2026-07-13): Slice 0 @ 0938439, Slices 1-4 @ 84ea63e.** CRUCIBLE is now a bounded in-scope DISCOVERER. Slice 1 `intel/frontier.py` (canonical dedup + VOI kernel reuse + size cap), Slice 2 `intel/expand.py` (crawl a promoted root via the existing Crawler → mint param-bearing surfaces; **proven end-to-end: a world with ONLY a recon SERVICE → promote → crawl finds /search?q= → probe → oracle fires → minted finding, nothing human-pointed**), Slice 3 generalized `ProbeSurfaceTool` (curated multi-class set), Slice 4 posture seam (`--autonomous-posture` default `discover-queue` = zero-traffic park + `--discover-autotest` opt-in; also `--autonomous-crawl-expand`/`--autonomous-multi-probe`). All default-OFF, gate byte-identical 9/0/0/853 throughout. **★ BATCHED REVIEW (wcqss59lb) confirmed host-scope + replay-determinism CLEAN and caught 1 CRITICAL + 1 MEDIUM + 3 LOW, all fixed+regression-tested pre-merge: [CRITICAL] the curated multi-probe minted a FALSE `boolean_sqli` fact on benign endpoints — a single-shot DifferentialCheck fires on ANY app that VALIDATES input / WAF-blocks a quote (200 vs 403), ironically flagging apps that DEFEND against SQLi → DROPPED BOOLEAN_SQLI from `curated_probe_checks()` (set now 5; a differential without a multi-round SPRT control can't be near-zero-FP unsupervised). [MEDIUM] `discover-queue` (promises ZERO target traffic) still crawl-expanded (the Slice-2 crawl ran independent of posture) → gated crawl-expand behind `probe_posture != discover-queue` (honor the posture for ALL target contact, not just the probe). [LOW×2] expand could downgrade https→http + a `..` href could escape the seed path-prefix → `expand_endpoint` now pins minted URLs to the seed SCHEME+HOST and requires the NORMALISED path stay under the seed subtree. [LOW] frontier cap dropped surfaces silently → `_build_goal_tree` reports truncation via `out.notes`.** DURABLE LESSONS: (a) a **differential/statistical check is NOT near-zero-FP as an UNSUPERVISED discovery probe** — only evidence-carrying checks (reflected-into-HTML, canary-redirect, content-signature, computed-arithmetic) belong in the curated set; a differential needs a multi-round control (SPRT) + dedicated FP review. (b) A **posture/traffic promise must gate EVERY target-contact path** (crawl is traffic too), not just the obvious one. (c) The **Slice-0 round-trip-authority discipline is HOST-only**: scheme + path-prefix are separate containment axes a crawl can cross same-host (fix in the discovery layer, not the shared crawler, to keep the scan byte-identical).

**★ PHASE 2 (coverage) — feasibility mapped (3 agents) + first lesson (2026-07-13):** client-side classes (clickjacking/CSRF/postMessage) reuse ACHIEVED_STATE/DOM_EXECUTION kinds but are BYTE/DOM-AMBIGUOUS → **honest LEADS offline**; the genuinely SOUND offline FACTs are the **config/artifact** classes. **Built the clickjacking ACHIEVED_STATE oracle (framable+password via predicate_oracle) → adversarial review (we94i13is) found 8 FP variants → REVERTED/DEMOTED to a lead.** DURABLE LESSON: **a byte-level clickjacking FACT is unsound** — `<input type=password>` byte-presence fires on a password marker inside an HTML comment / `<script>` string / `<textarea>` (a docs/paste/KB page), a text/plain page, a disabled/hidden field, or a page framing-protected via meta-CSP / multi-header XFO the regex misses; AND the soundness would live in un-re-verifiable HTML parsing (the predicate AST can't parse HTML), which the certificate can't capture. This is the **XXE-in-paste precedent** (one captured body can't causally prove a live rendered framable login). The SOUND clickjacking FACT path is **DOM rendering via the CDP stack** (a future slice, like the postMessage DOM_EXECUTION reuse). **★ Slice 2A-2 (weak-crypto-artifact oracle) DONE + MERGED to main @ 865274b (2026-07-13):** `verify/weak_crypto.py` + `oracles.py::weak_crypto_artifact_oracle` — a cert signed with MD5/SHA-1 is a FACT (unconditionally collision-forgeable; proven from the retained signatureAlgorithm OID NAME — a pure re-verifiable classification, NO ambiguity). **REUSES `TLS_WEAKNESS`** (no new kind; `crypto_artifact` ctx key + `weak_crypto_artifact` bug_class no benchmark carries → gate byte-identical). The `sha1` matcher uses `(?![0-9])` so it never false-matches sha224/256/384/512. Two adversarial reviews: cardinal near-zero-FP INTACT; one LOW chain false-NEGATIVE (only the leaf was judged) → fixed to judge EVERY cert (catches SHA-1 INTERMEDIATES — the canonical SHAttered case). 23 tests incl. a real SHA-1 cert fixture. **Ships as oracle+seam (like k8s/cloud posture); the capture feed (TLS-scan server cert / operator cert) is the follow-up.** LESSON: this is the SOUND-FACT template — a pure re-verifiable classification over a structured artifact, contrast the un-re-verifiable HTML-parse of clickjacking. **★ Slice 2B (CI/CD posture oracle) DONE + MERGED to main @ c32ebfd (2026-07-13):** NEW `OracleKind.CICD_POSTURE` (held OUT of frozen `_ALL_ORACLES`; 5-edit posture template k8s→cicd) + `verify/cicd_posture.py` (minimal OFFLINE safe-YAML/JSON `ingest_workflow`, mirrors `mesh_posture.ingest_mesh_config`, never execs/clones) + `oracles.py::cicd_posture_oracle` with 3 re-verifiable rules: **unpinned_action** (third-party action, mutable non-SHA ref; first-party actions/github + SHA-pin + local/docker do NOT fire), **pwn_request** (`pull_request_target` + untrusted PR-head checkout), **script_injection** (`run:` interpolating an untrusted `${{ github.event.*.title|body|... }}`). `cicd_control` ctx key + `cicd_misconfiguration` bug_class no benchmark carries → gate byte-identical. Review (wp7kachv5): pwn_request + unpinned confirmed SOUND (unpinned @v3 = a true "not SHA-pinned" fact, refuted as noise-not-FP); ONE MEDIUM script_injection FP → fixed. **LESSON: an untrusted-token match must be BOUNDARY-ANCHORED, not a substring** — the substring `tok in flat` false-fired on a non-injectable array subfield (`commits[0].id`=SHA / `.timestamp` / `pages[0].sha`), a quoted STRING LITERAL (`${{ 'github.event...title' }}` = zero data flow), and a different prefix (`commits_url`). Fix: `(?<![\w.])...(?![\w.])` regexes matching a WHOLE path at an INJECTABLE TEXT LEAF only + strip quoted literals; array contexts match only `.message/.author.name|email/.page_name`, never `.id/.sha/.timestamp/.url`. **Additive-enum reconciliation:** bumping a new kind touches SEVERAL `len(OracleKind)==N` asserts across test files (gate_invariants 27→28 + add the kind to `_ADDITIVE`/`_EXCLUDED_KINDS`/`_EXCLUDED_CLASSES`); de-fragilized the per-slice count asserts in test_weak_crypto + test_saml_forgery_crypto to NOT hard-code the total (only the gate-invariant test pins it). Ships as oracle+seam+ingest (usable via `confirm_workflow(yaml)`); the `sensors/cicd.py` + fusion feed is the follow-up. **★ Slice 2C (mobile MobsfSensor) DONE + MERGED to main @ 3813f35 (2026-07-13):** `sensors/mobile.py::MobsfSensor` mirrors `SbomVulnSensor` method-for-method (Tier-1, kill-switch-gated, reads an operator-supplied MobSF `--json` export, no network). Mints GROUNDING_INTEL LEADS only (never facts): 1 APPLICATION node + 1 CONTROL lead per posture finding keyed `mobile:<check_id>` (secure/passing checks skipped) + 1 ENDPOINT lead per embedded backend URL (with a `url` attr → the discoverer can test it, scope-gated). **REUSES APPLICATION+CONTROL+ENDPOINT node kinds — NO new NodeKind, NO new OracleKind, NO oracle** (leads-first, exactly how kube-bench shipped before k8s-posture). Pure+total (malformed→non-ingestion), idempotent obs_ids, registered in `sensors/builtin.py`. Gate byte-identical (off the scan path); 6 tests. The mobile-posture oracle (promote exported-component-no-permission / cleartext-permitted to FACTs) is the LATER slice. **PHASE 2 SUBSTANTIALLY COMPLETE: 2 sound FACT oracles (weak-crypto @865274b, CI/CD @c32ebfd) + 1 leads sensor (mobile @3813f35) + clickjacking honestly demoted to a lead. Remaining Phase-2 = optional capture-feed follow-ups (sensors/cicd.py+fusion, weak-crypto TLS-cert feed, mobile-posture oracle) + the DOM-rendered client-side FACTs (postMessage/clickjacking via the CDP stack).** NOTE: mobile sensor was committed DIRECTLY to main (single leads-only commit, owner) not via a feature branch — a minor deviation from the branch-merge convention, harmless. **STRATEGY CORRECTION: pick config/artifact classes for sound FACTs, not client-side.** The genuinely-sound Phase-2 FACTs (per the maps): **(1) weak-crypto-artifact** — a cert MD5/SHA1 signatureAlgorithm OID is unconditionally weak, fully re-verifiable from the cert bytes, no context ambiguity (reuse `scanner/quantum_era.py::_leaf_signature_algorithm`); **(2) CI/CD posture** — literal YAML token co-occurrence (unpinned `uses:@main`, `pull_request_target`+secrets, self-hosted-on-PR = config-posture template k8s→cicd; `run: ${{github.event.*}}` = AEGIS request-side parse-proof) needs new `CICD_POSTURE` kind + a `sensors/cicd.py` + offline YAML ingest (reuse `verify/mesh_posture.py::ingest_mesh_config`); **(3) mobile** — `MobsfSensor` LEADS-first (reuse `SbomVulnSensor`), oracle later. **The oracle-adding pattern:** reuse an existing kind (ACHIEVED_STATE via `predicate_oracle` / `FindingContext.from_predicate` — dispatch `_run` at `"predicate" in ctx and "observed_evidence" in ctx`) needs only a `BUG_CLASS_ORACLES` row (no new kind); a genuinely-new proof shape = the 5-edit posture template (models.py kind held OUT of frozen `_ALL_ORACLES` + verifier row + dispatch arm gated on a ctx key no benchmark carries + oracles.py fn + adapter `from_*` + confirm seam). **The benchmark app has no forms/password/XFO/cert-artifacts, so config/artifact oracles on the default path stay gate byte-identical.**

**★ Phase 3 Slice 3A (inbound integrations) DONE + MERGED to main @ 36afa20 (2026-07-13):** `imports/parsers.py::parse_sarif` — SARIF 2.1.0 ingest (was export-only). Walks `runs[].results[]`; **maps the rule's CWE tag → CRUCIBLE bug_class** (`external/cwe/cwe-079`→xss, CWE-89→sqli, 78→command_injection, 22→path_traversal, 918→ssrf, 502→deserialization, 798→hardcoded_secret, …) so a **URL-located DAST finding routes to the oracle that RE-VERIFIES it** (host derived), while a file-located SAST finding is an aggregation lead (no host); severity from `level`; `kind=pass/notApplicable` skipped. **KEY LEVERAGE: one SARIF importer covers Semgrep/CodeQL/Trivy/Checkov/gitleaks — they all emit SARIF natively — closing the SAST/IaC/secret-scanner gap in one slice.** + wired the already-written-but-unwired `parse_nikto`/`parse_wapiti` (`eval/adapters_ext`) into `imports/` with detect_format sniffs (wapiti `vulnerabilities`=dict; nikto=list of `{msg}`). `available_formats` = burp/generic/nikto/nuclei/sarif/sqlmap/wapiti/zap. All imports are LEADS (`tool_confirmed=False`). Gate byte-identical (imports off the scan path). Leads-only → merged without a full FP-review (no FACT to false-fire). **★ Phase 3 Slice 3B (outbound push sinks) DONE + MERGED to main @ f3254e2 (2026-07-13) — PHASE 3 COMPLETE.** `report/push.py` delivers a GRADED report to an operator-configured OUTBOUND sink (webhook = the universal POST primitive Jira/DefectDojo/SIEM/custom accept; slack = `{"text":...}`). SAFE BY CONSTRUCTION: **opt-in** (nothing sent without `--push-url`); **never raw egress** (`push_via_urllib` = a bounded POST to EXACTLY the sink URL, **redirects DISABLED** via a `_NoRedirect` handler → no redirect-to-internal SSRF, http(s)-only, timeout); **proven-fact discipline** (payload = the export doc → a FACT levelled by severity, a LEAD marked or dropped with `facts_only`; carries NO raw evidence/oracle-context/PII, only a certificate DIGEST — pushing it doesn't leak); **correlatable UA**; **best-effort** (never raises). CLI: `report --push-url URL [--push-sink slack] [--push-facts-only] [--push-dry-run] [--push-header K:V]`. Review (w9dag6plf): **cardinal egress-safety + no-leak confirmed CLEAN** (the whole point of reviewing an outbound feature); one LOW best-effort gap (grading/build was OUTSIDE the try → malformed `--from-json` raised) → fixed (grade once, build INSIDE the guard; CLI no longer aborts the render). 12 tests incl. a loopback POST asserting UA+auth-header+REDIRECT-REFUSED. Gate byte-identical. **DOCTRINE for outbound: opt-in + injected-gated-send (never raw) + redirects-off + proven-fact-levelled + evidence-free-payload + correlatable + best-effort.** **PHASE 3 (integrations) COMPLETE: inbound SARIF/nikto/wapiti @36afa20 + outbound push @f3254e2.** **REMAINING = Phase 4 (the missing scale/soak harness + twice-run byte-identical ScanReport at 1000+ endpoints + single-host-SQLite/serial-scan breakpoint characterization) + optional capture-feed follow-ups (posture-oracle sensors/fusion, weak-crypto TLS-cert feed, DOM client-side FACTs).** **Phase 4 = the missing scale/soak harness + twice-run byte-identical ScanReport assertion at 1000+ endpoints.**

**★★ PHASE 4 (scale/soak) DONE + MERGED @ f7dc508 (2026-07-13) — THE ENTIRE 4-GAP PROGRAM IS COMPLETE + MERGED TO MAIN.** `eval/soak.py` = the missing load harness (was "barely tested at scale"): an N-endpoint loopback fixture + a real `WebScanCampaign` driver measuring throughput/peak-RSS/wall-clock + `scan_fingerprint`. **Proves the load-bearing invariant at scale: the ScanReport is a PURE FUNCTION of its inputs** (byte-identical fingerprint across replays — the replay-determinism the discoverer + gate rest on). LESSON: building it surfaced that a valid determinism check must EXCLUDE the loopback's EPHEMERAL PORT from finding URLs (the scanner is deterministic modulo the port; pages/reqs/findings/passive all matched). Measured: **throughput FLAT ~99 req/s** regardless of endpoint count (the serial-scan ceiling; a real target adds RTT), **memory sub-linear** (97→102 MB for 25→100 endpoints). `docs/SCALE.md` characterizes the single-host caps (serial scan / single-host-SQLite-one-logical-clock / O(log-size) chain verify) + the ONE byte-identical concurrency win (`CRUCIBLE_RECON_MAX_WORKERS`, ingested-in-input-order) + what's deliberately NOT done (parallel audit / multi-host bus break determinism). CLI `python3 -m framework.v2.eval.soak --endpoints N`. Additive test/measurement harness (no runtime code, no FACT, no egress) → merged without a full review; gate byte-identical. **PROGRAM DONE: P1 discoverer (84ea63e), P2 coverage (weak-crypto 865274b + CI/CD c32ebfd oracles + mobile 3813f35 sensor; clickjacking demoted), P3 integrations (SARIF/nikto/wapiti 36afa20 + push sinks f3254e2), P4 scale (f7dc508).** Remaining = OPTIONAL follow-ups only: posture-oracle sensor/fusion feeds (CI/CD `sensors/cicd.py`; mobile-posture oracle promoting exported-component/cleartext), weak-crypto TLS-cert feed, DOM-rendered client-side FACTs (postMessage/clickjacking via CDP), Jira/DefectDojo REST push sinks.

**★ FOLLOW-UP 1 (CI/CD capture feed) DONE + MERGED to main @ 42fddc2 (2026-07-15).** Completes the CI/CD capability: the Phase-2 CI/CD-posture oracle (c32ebfd) was only reachable via direct `confirm_workflow()` — nothing fed it leads on the engage path. Now `sensors/cicd.py::WorkflowScanSensor` (Tier-1 offline, mirrors `KubeBenchSensor` method-for-method) reads an operator workflow FILE or DIR of `*.yml`/`*.yaml`, parses each via `ingest_workflow`, mints one `NodeKind.CONTROL` LEAD per construct keyed `cicd:<workflow>:<job>:<rule>:<i>` (the check_id namespaced by workflow name so lead+FACT share a node); `engage_fusion._reverify_cicd` (mirrors `_reverify_k8s`) re-fires the oracle over each retained control → oracle-GROUNDED FACT for a re-derived dangerous construct, honest LEAD otherwise. Registered in `sensors.builtin` + the minimal `_fusion_registry` + `_SAFE_SENSORS` (Tier-1); still kill-switch-gated at `run_sensor` (adversarially verified: tripped switch mints 0). Oracle stays SOLE authority; SHA-pinned first-party checkout stays a lead. Gate byte-identical (9/0/0/853), no new OracleKind (still 28). +8 sensor tests, +2 fusion tests. **GOTCHA: a new fusion sensor must be added in FOUR places — `sensors/builtin.py` (default registry), `engage_fusion._fusion_registry()` (the minimal fusion-only registry — the one `fuse_sensors` actually uses), `_SAFE_SENSORS`, and the `_reverify` dispatch; missing `_fusion_registry` silently mints ZERO (the sensor exists but the fusion path can't resolve it).** Usable now: `engage --fuse-sensors` with a `fusion.json` task `{sensor:"cicd_workflows", args:{workflow:".github/workflows"}}`. Note: `Node.id` (not `.node_id` — that's `Observation.subject`) when iterating `world.all_nodes()`. Remaining follow-ups: mobile-posture oracle, weak-crypto TLS-cert feed (needs extending `capture_tls_handshake` to retain the peer-cert DER — the quantum scan grabs it, the weak-TLS handshake doesn't), Jira/DefectDojo REST sinks, DOM client-side FACTs.

**★ FOLLOW-UP 2 (mobile-posture oracle) DONE + MERGED to main @ 969ec2a (2026-07-15, ULTRACODE).** Turns the leads-only MobSF sensor into a real FACT. **Understand workflow (3 agents) was the keystone: adversarial soundness map ruled that of 6 candidate mobile signals, NONE of the general forms is a near-zero-FP FACT** — every Android control is resolved by a precedence/gating chain a MobSF descriptor omits (usesCleartextTraffic → REFUSE without the network_security_config chain [the headline SAML-xpointer case]; general exported-component → near-DROP like BOOLEAN_SQLI [fires on the MANDATORY launcher activity + by-design deep-links]; allowBackup → default-true universality; general secret-scan → publishable-key/entropy FPs; minSdk → min-vs-target/device gating). **Only 2-3 NARROW slices are sound: (1) debuggable + non-debug signing cert [risks assert-a-negative parsing MobSF's cert blob], (2) unguarded exported CONTENT PROVIDER, (3) embedded PRIVATE-KEY PEM.** Built slice (3) as the flagship because it's the SOUNDEST — the oracle RE-EXECUTES (actually LOADS the key via `cryptography`), not a schema-guess or string-match, and fail-closes (REFUSE) if the key can't be reconstructed, so it's safe even under MobSF-schema uncertainty. **CRITICAL PREREQ the evidence agent caught: the mobile sensor retained ONLY free-text (category/title/severity/evidence) + even DROPPED check_id from attrs — an oracle would've string-trusted MobSF's verdict (the exact 'string trust' the veracity layer forbids).** So step 1 was ENRICHING `parse_mobsf` to retain the FULL `pem` verbatim (+ rule/check_id parity). NEW `OracleKind.MOBILE_POSTURE` (9-edit template, 28→29; keyed on `mobile_control` ctx + `mobile_misconfiguration` bug_class no benchmark carries → gate byte-identical) + `verify/mobile_posture.py` seam + `from_mobile_control` + `engage_fusion._reverify_mobile` (mirrors `_reverify_cicd`; `mobsf_static` added to `_SAFE_SENSORS` + `_fusion_registry`). The oracle fires a FACT iff the retained pem loads as an UNENCRYPTED private key (PKCS1/8/SEC1 via load_pem_private_key, OpenSSH via load_ssh_private_key); encrypted/public/cert/masked/garbage all REFUSE. **ADVERSARIAL REVIEW (3 execute-the-corpus lenses → refuters): the FP-hunt lens found NOTHING — no benign input false-fires the FACT (cardinal near-zero-FP HOLDS, unlike clickjacking/BOOLEAN_SQLI). 3 confirmed defects, all safe FALSE-NEGATIVES, all fixed+regression-tested: [MED] OpenSSH keys were extracted-then-refused (load_pem can't parse the OpenSSH container; ssh-keygen's default since 7.8) → added load_ssh_private_key fallback (still a real re-load); [LOW] a list-of-lines secret → str(list) inserted ', ' separators the extractor didn't normalize → join with newlines; [LOW] a no-app-metadata report promoted an ORPHAN fact onto a stub CONTROL the sensor never minted as a lead → _reverify_mobile now guards on the same app_key the sensor short-circuits on.** DURABLE LESSONS: (a) **the mobile-posture space is mostly LEADS by construction** — a manifest attribute is not the effective policy (an override chain the descriptor omits); the sound FACTs are the self-contained artifacts (a loadable private key), not the gated config states. (b) **the SOUNDEST oracle re-EXECUTES over a self-contained artifact** (load the key) rather than parsing a scanner's summarized/schema-dependent findings — it's robust even when you can't trust the scanner's JSON shape. (c) **a lead-only sensor must be ENRICHED to retain a structured machine-checkable value BEFORE an oracle can soundly promote it** (free-text → string-trust). +15 oracle tests, +3 sensor, +3 fusion; gate byte-identical (9/0/0/853). Remaining: unguarded-content-provider + debuggable+cert mobile slices, weak-crypto TLS-cert feed, Jira/DefectDojo sinks, DOM client-side FACTs.

**★ FOLLOW-UP 3 (weak-crypto cert capture feed) DONE + MERGED to main @ 1d03f59 (2026-07-15, ULTRACODE).** Completes the weak-crypto capability: the weak-crypto-artifact oracle (865274b) was only reachable via direct `confirm_weak_crypto_artifact()` — nothing fed it certs on the engage path. `sensors/tls_cert.py::CertScanSensor` (Tier-1 OFFLINE, mirrors WorkflowScanSensor) reads an operator cert FILE (PEM/DER) or a dir of `*.pem/.crt/.cer/.der`, parses each via `signature_descriptors` (a PEM chain → one descriptor per cert so a weak-hash INTERMEDIATE is judged), mints a CONTROL lead keyed `crypto:<source>:<i>` carrying the `{signature_algorithm,oid,subject}` descriptor. `verify/weak_crypto.py::confirm_crypto_descriptor` + `crypto_descriptor_context` route an ALREADY-PARSED descriptor through the verifier (mirror confirm_cicd/mobile). `engage_fusion._reverify_crypto` promotes a broken-hash (MD5/SHA-1) cert to a grounded FACT (oracle_kind=`tls_weakness`, bug_class=`weak_crypto_artifact`); a modern SHA-256+ cert stays a lead. Registered in builtin + `_fusion_registry` + `_SAFE_SENSORS`. **REUSES the already-twice-reviewed weak-crypto oracle + TLS_WEAKNESS kind — NO new OracleKind, gate byte-identical (still 29).** A LIVE TLS scan (capturing the peer-cert DER — the quantum scan already grabs it at `quantum_era.py:191`, but is ALSO direct-call-only) is a separate ACTIVE-gated build, deliberately deferred. **Adversarial review (FP-hunt + FN/integration, BOTH executed real corpora — modern/ECDSA/Ed25519/CN-contains-sha1 certs, DER, chains, node-key, determinism, gate): CLEAN, 0 defects.** First clean review this session — EXPECTED because it reuses a sound already-reviewed oracle + the sensor/fusion pattern is now on its 3rd battle-tested application (the node-key/determinism/scope lessons already baked in). Verified the finders did real work (built cert corpora, ran the gate + suites) before trusting CLEAN. +10 sensor tests, +2 fusion tests. **SESSION TALLY: 3 capture-feed/oracle follow-ups shipped (CI/CD @42fddc2, mobile-posture @969ec2a, weak-crypto cert @1d03f59), each adversarially reviewed + gate-byte-identical. THE SENSOR+FUSION CAPTURE-FEED PATTERN (5 wiring points: sensors.builtin, engage_fusion._fusion_registry, _SAFE_SENSORS, _reverify dispatch, a `confirm_X(descriptor/control)` verifier-routed seam) is now the reusable template for making any offline-artifact oracle reachable in `engage --fuse-sensors`.** Remaining: unguarded-content-provider/debuggable mobile slices (schema-dependent, needs raw-manifest ingest), the LIVE TLS sensor (ACTIVE-gated), Jira/DefectDojo REST sinks (marginal — generic webhook already covers them), DOM client-side FACTs (CDP, big).

**★ FOLLOW-UP 4 (unguarded exported content provider — mobile FACT #2) DONE + MERGED to main @ c4465fb (2026-07-15, ULTRACODE).** Extends the mobile-posture oracle with a 2nd sound rule. **Soundness insight: re-deriving an unguarded exported component from MobSF's SUMMARISED findings = string-trust (forbidden); the sound path is to parse the RAW decoded AndroidManifest.xml — a self-contained artifact CRUCIBLE parses itself (like the workflow YAML + the cert).** NEW `sensors/android_manifest.py::AndroidManifestSensor` (Tier-1 offline, reads apktool-decoded AndroidManifest.xml) mints one CONTROL lead per `<provider>` in the SAME `mobile:` namespace + `{parsed}` shape as `sensors.mobile` → REUSES `mobsf_observations` + the SAME `_reverify_mobile` fusion path (`android_manifest` dispatches to `_reverify_mobile`). `mobile_posture_oracle` rule `exported_content_provider` fires a FACT **ONLY on EXPLICIT `android:exported="true"` + ZERO guards** (no permission/readPermission/writePermission/`<path-permission>`). **KEY SOUNDNESS MOVES: (1) narrowed to CONTENT PROVIDERS only → sidesteps the mandatory-launcher-activity FP that makes the general exported-component check a near-DROP; (2) DELIBERATELY REFUSE the default-export gating chain (export default depends on min/targetSdk) → fire only on EXPLICIT exported=true, default-exported providers stay leads (safe FN); (3) ANY permission attr of any level → guarded → no fire.** `from_mobile_control` retains the export/guard attrs. **REUSES MOBILE_POSTURE — NO new OracleKind, gate byte-identical (still 29).** Adversarial review (FP-hunt + FN/parse, BOTH executed real corpora): **CLEAN, 0 defects** — the finder empirically PROVED XXE/billion-laughs safety (fed an actual entity bomb: 0 controls/0.133s/67MB no-expand; external-entity `file:///etc/passwd` NOT fetched — modern CPython expat has billion-laughs protection + no external-fetch default). **PROACTIVE HARDENING folded in anyway: reject `<!DOCTYPE>`/`<!ENTITY>` outright → makes entity safety EXPLICIT in code, not dependent on the runtime expat version.** +8 oracle, +7 sensor, +1 fusion tests. **SESSION TALLY: 4 sound units shipped (CI/CD feed @42fddc2, mobile private-key oracle @969ec2a, weak-crypto cert feed @1d03f59, mobile provider FACT @c4465fb), each adversarially reviewed + gate-byte-identical. DURABLE LESSON: the sound way to add an oracle over a scanner's domain is to ingest the RAW self-contained artifact (manifest XML / cert bytes / workflow YAML / key PEM) and RE-DERIVE, never string-trust the scanner's summarized verdict.** Remaining: debuggable+cert mobile slice (needs BOTH manifest + signing cert), LIVE TLS sensor (ACTIVE-gated — a different risk class, flag for operator), Jira/DefectDojo sinks (marginal), DOM client-side FACTs (CDP).

**★ FOLLOW-UP 5 (LIVE TLS posture — the first ACTIVE unit) DONE + MERGED to main @ 3a9e428 (2026-07-15, ULTRACODE).** The highest-value remaining item + the ONLY ACTIVE one (opens a REAL TLS connection to a target). Neither the weak-TLS (protocol/cipher) nor the weak-crypto (cert-signature) oracle was reachable on the engage path (`capture_tls_handshake` + the quantum-scan cert grab are both direct-call-only). **KEY ARCHITECTURAL DECISION: do NOT build a new ACTIVE sensor — mirror the PROVEN gated opt-in reachability handshake (`_reverify_reachability`).** So the live probe lives in the FUSION PROMOTION off the OFFLINE `declared_service` sensor, gated + opt-in, NOT in a new `_SAFE_SENSORS` entry (that allowlist stays offline-only). `verify/tls.py::_tls_connect` now also captures the peer-cert DER (`getpeercert(binary_form=True)` returns the presented leaf even under CERT_NONE); `capture_tls_handshake` unpacks the connector result FLEXIBLY (3- or 4-tuple → backward-compatible with every injected test connector) + base64s the cert into `cert_der_b64`. `engage_fusion._reverify_tls_live` (mirrors `_reverify_reachability`): OPT-IN (`args['confirm_tls']`) + the fail-closed `_authorize` gate (slug→killswitch→single-host→ACTIVE_RECON→charter-scope, runs BEFORE connect); for each declared open TLS service (`svc.tls` or a well-known `_TLS_PORTS` port) it does ONE gated handshake and runs BOTH already-reviewed oracles over the retained evidence — `confirm_weak_tls` + `confirm_crypto_descriptor` over the captured cert. **COLLISION FIX: two facts on the SAME service subject use DISTINCT oracle_kind labels (`tls_weakness` vs `weak_crypto_artifact`) because `_project_oracle_fact` keys findings on `{oracle_kind}:{subject}` (the cloud reverify uses the same distinct-label trick).** Injectable `ctx.tls_connect` (mirrors `ctx.reach_connect`) → threaded `_reverify(..., tls_connect=)`. **REUSES the audited active-connect gate + 2 already-reviewed oracles + TLS_WEAKNESS kind — NO new sensor, NO new OracleKind, gate byte-identical (still 29).** Adversarial review (gate-escape + FP/correctness lenses, BOTH executed real corpora): **CLEAN, 0 defects.** The gate-escape finder INSTRUMENTED the injected connector and PROVED **zero connector calls** on every unauthorized path: out-of-scope host (calls:0, "not in charter scope"), missing slug (calls:0), CIDR/multi-host (calls:0), tripped kill-switch (calls:0) — **no traffic reaches an unauthorized host; the gate refuses BEFORE connect.** FP lens: strong TLSv1.3+modern-cert endpoint promotes nothing; the two weak facts are distinct nodes. **GATING FACT LEARNED: entitlement enforcement (`require_capability`) is INACTIVE by default (`enforcement inactive (no trust root provisioned)` → allowed); the ALWAYS-ON active-recon gate is CHARTER SCOPE (fail-closed). Test the gate via an out-of-scope host + tripped killswitch, not entitlement.** +1 tls unit test, +5 fusion tests. **SESSION TALLY: 5 sound units shipped (CI/CD feed @42fddc2, mobile private-key @969ec2a, weak-crypto cert @1d03f59, mobile provider @c4465fb, LIVE TLS @3a9e428), each adversarially reviewed + gate-byte-identical; the last is the first ACTIVE one, gated fail-closed + opt-in, proven zero-connector-call on refusal.** Remaining: debuggable+cert mobile slice, Jira/DefectDojo sinks (marginal — generic webhook covers them), DOM client-side FACTs (CDP).

**★ FOLLOW-UP 6 (debuggable + non-debug-cert mobile FACT) BUILT → KILLED BY REVIEW → DROPPED, NOT MERGED (2026-07-15, ULTRACODE).** Attempted the 3rd sound mobile slice the understand-phase soundness map called "the flagship": debuggable="true" + a signing cert whose subject is NOT the Android debug keystore (`CN=Android Debug`) → FACT. Built it fully (manifest debuggable-flag parse + `_signing_cert_subject` via `signature_descriptors` + `debuggable_release` oracle rule + `from_mobile_control` fields + fusion via `_reverify_mobile` + tests; gate byte-identical, all green). **The adversarial FP-hunt CONFIRMED a HIGH false-positive that KILLS the FACT: the discriminator "subject != Android debug keystore" is UNSOUND — signing a debuggable build with a CUSTOM/shared debug keystore is ROUTINE DOCUMENTED practice (a stable SHA-1 is REQUIRED to register a Google Maps API key / a Facebook key hash; CI pipelines sign debug builds with an internal key). The biconditional "debug-build ⟺ SDK-debug-keystore" is FALSE, so a benign, routinely-scanned DEBUG build (custom-signed) reaches a FACT asserting a distributed release build. No re-derivable RELEASE signal exists in (manifest + signing cert) offline.** DECISION: DROPPED (reverted all changes, branch deleted, nothing merged) — exactly the clickjacking outcome. A demote-to-lead was rejected as redundant (the MobSF sensor already surfaces debuggable as a lead). **CARDINAL LESSON REINFORCED: the understand-phase soundness agent ASSERTED the cert discriminator made this sound; the DEEPER adversarial FP-hunt found the discriminator is defeated by a common benign practice. THE ADVERSARIAL REVIEW CATCHES WHAT THE UNDERSTAND PHASE MISSES — near-zero-FP cannot be self-certified, not even by a dedicated soundness-mapping agent. Prove-don't-guess held: an unsound FACT built + tested + green was NOT shipped because the FP-hunt refuted it.** (Also caught a real 2nd defect — the debuggable_release control used a package-less fixed check_id → cross-app CONTROL node-key collision — moot on drop but a reminder to package-scope any per-app control key.) **SESSION FINAL TALLY: 5 sound units SHIPPED + merged (CI/CD @42fddc2, mobile private-key @969ec2a, weak-crypto cert @1d03f59, mobile provider @c4465fb, LIVE TLS @3a9e428), 1 unsound FACT BUILT-then-KILLED by review (debuggable-release). Mobile coverage = 2 sound FACTs (private-key + unguarded-provider) + leads; debuggable stays a lead via MobSF.** Remaining: Jira/DefectDojo sinks (marginal), DOM client-side FACTs (CDP, big).

**★ FOLLOW-UP 7 (service-mesh capture feed) DONE + MERGED to main @ 83957e3 (2026-07-15, ULTRACODE).** Found by AUDITING which posture oracles still lacked a fusion capture feed: `grep confirm_*_posture` vs `_SAFE_SENSORS` → the **mesh-posture oracle (MESH_POSTURE, Wave-G3) was the LAST posture oracle with NO feed** (direct-call-only, exactly like CI/CD + weak-crypto were). `sensors/mesh.py::MeshConfigSensor` (Tier-1 offline, mirrors WorkflowScanSensor) reads an operator Istio/Linkerd config FILE (JSON, or YAML if PyYAML present; single/multi-doc) or a dir, parses via `ingest_mesh_config`, mints a CONTROL lead per recognised resource keyed `mesh:<source>:<provider>:<kind>:<ns>/<name>`. `engage_fusion._reverify_mesh` (mirrors `_reverify_cicd`) promotes a concrete insecure achieved state (permissive/disabled mTLS, allow-everyone AuthorizationPolicy, unauthenticated Linkerd inbound policy) → grounded FACT; STRICT/scoped/deny stays a lead. Registered in builtin + `_fusion_registry` + `_SAFE_SENSORS`. **REUSES the already-reviewed mesh-posture oracle + MESH_POSTURE kind — NO new OracleKind, gate byte-identical (still 29).** Adversarial review (FP-hunt + FN/integration, both EXECUTED real corpora): FP-hunt found NOTHING (near-zero-FP holds — strict/scoped/requestPrincipals[*]/Linkerd-authenticated all correctly no-fire). ONE confirmed LOW data-integrity defect FIXED + regression-tested: **a cross-file check_id collision — two same-identity resources across files in a multi-cluster/merged-export DIR collapsed to one CONTROL node (mislabeled + a sibling silently lost); NOT a near-zero-FP violation (the promoted FACT was still TRUE) but a real integrity bug → the check_id is now SOURCE-NAMESPACED (`<source>:...`) so distinct resources across files never collide.** LESSON: **package/source-scope any per-resource control key** (the debuggable slice had the same collision class flagged; here it was real for the dir-ingest feature). +9 sensor tests, +2 fusion tests. **SESSION FINAL TALLY: 6 sound units SHIPPED + merged (CI/CD @42fddc2, mobile private-key @969ec2a, weak-crypto cert @1d03f59, mobile provider @c4465fb, LIVE TLS @3a9e428, mesh @83957e3), 1 unsound FACT BUILT-then-KILLED (debuggable). ALL posture oracles now have an engage-path capture feed (sbom/k8s/cloud/cicd/mobile/weak-crypto/mesh + live-TLS + reachability). The sensor+fusion capture-feed pattern is fully applied.** Remaining: Jira/DefectDojo sinks (marginal — generic webhook covers them), DOM client-side FACTs (CDP, big).

**★ FOLLOW-UP 8 (weak-crypto UNDERSIZED-KEY rule) DONE + MERGED to main @ 100f9d1 (2026-07-15, ULTRACODE).** Extends the already-reviewed weak-crypto-artifact oracle with a 2nd re-derivable rule over the SAME cert: an undersized public key (RSA/DSA < 2048 bits, EC curve < 224 bits) is below ~112-bit security (NIST SP 800-131A, deprecated 2013) → FACT; RSA-2048/P-256/Ed25519 never fires. `verify/weak_crypto.py::_key_info(cert)` captures the pubkey (algo, bits) (rsa/dsa `.key_size`, ec `.curve.key_size`; Ed25519/Ed448/unknown → no classical size → never short); `_descriptor_of` carries key_type+key_bits; `weak_crypto_artifact_oracle` gains a 2nd fire path after the broken-hash path (both TLS_WEAKNESS, distinct `observed.reason`). **★ EXCELLENT LEVERAGE: ZERO NEW WIRING — because `from_crypto_artifact` retains the WHOLE descriptor, the short-key rule is reachable through BOTH existing weak-crypto feeds automatically (the offline `tls_cert` sensor AND the gated live-TLS handshake).** REUSES TLS_WEAKNESS — NO new OracleKind, gate byte-identical (still 29). Adversarial review (FP-hunt + FN/correctness, both EXECUTED real corpora): FP-hunt found NOTHING (adequate keys never fire; near-zero-FP holds). ONE confirmed MEDIUM **correctness/labeling** defect FIXED + regression-tested: **the two fusion promoters (`_reverify_crypto` + `_reverify_tls_live`) HARDCODED the "signed with a BROKEN hash — collision-forgeable" evidence string, so a short-key cert with a MODERN SHA-256 hash was promoted with the WRONG vulnerability class + remediation (rotate hash instead of enlarge key). NOT a false-positive (the key is genuinely weak) — a labeling bug the new reachability EXPOSED.** FIX: new `crypto_descriptor_verdict()` surfaces the FIRED oracle signal's OWN per-reason evidence+reason; both promoters now label the FACT with the actual proof (undersized-key vs broken-hash) + carry reason/key_bits in detail. **LESSON: when you make a NEW oracle rule reachable through EXISTING promotion sites, audit those sites' hardcoded human-readable evidence — a promoter that hardcodes one reason's label mislabels the new reason. Prove-don't-guess applies to the FACT's EVIDENCE STRING, not just the fire decision.** +14 oracle tests, +1 fusion test. **SESSION FINAL TALLY: 7 sound units SHIPPED + merged (CI/CD @42fddc2, mobile private-key @969ec2a, weak-crypto cert @1d03f59, mobile provider @c4465fb, LIVE TLS @3a9e428, mesh @83957e3, weak-crypto short-key @100f9d1), 1 unsound FACT BUILT-then-KILLED (debuggable). Every posture oracle has an engage-path feed; the weak-crypto oracle now proves BOTH weak-hash + weak-key over every cert the two feeds capture.** Remaining: Jira/DefectDojo sinks (marginal), DOM client-side FACTs (CDP, big).

**PHASES 2–4 (additive, reuse the plugin/import/export seams):** P2 coverage (CSRF/clickjacking provable oracle, CI/CD-pipeline posture oracle reusing k8s/cloud/mesh pattern, crypto/secrets, mobile APK/IPA importer+sensor first-slice — proven oracle where offline-provable near-zero-FP, else honest LEAD). P3 integrations (SARIF ingest + wire the unwired `parse_nikto`/`parse_wapiti` in `eval/adapters_ext.py`; SAST/IaC/secret importers; gated fail-closed push sinks). P4 scale & soak (NEW `eval/soak/` harness over a many-endpoint fixture; the LOAD-BEARING **twice-run byte-identical `ScanReport` assertion** proves the frontier drain+fold stay replay-deterministic at 1000+ endpoints; characterize single-host-SQLite/serial-scan breakpoints; NO multi-host bus — that breaks determinism, flagged as separate future decision).

**ORCHESTRATION (proven):** parallel worktree agents → batched adversarial review (execute-the-corpus finders → refuters) → fix survivors → sequential gated merge reconciling additive enum unions → finalize (docs/graphify/memory/push/prune). Nothing ships until review finds 0 surviving high/critical + gate byte-identical.

**PROGRESS: Slice 0 (KEYSTONE) DONE + MERGED to main @ 0938439 (2026-07-13).** `intel/promote.py::promote_to_endpoints` built: promotes in-scope DOMAIN/HOST→https-root + web-ish SERVICE→scheme://host[:port]/ into `endpoint:promoted:<url>` ENDPOINT nodes, hooked in `run_autonomous_cycle` ORIENT (discover-path only, before `_build_goal_tree`). 20 unit + 3 integration tests; gate byte-identical (9/0/0/853). **★ Adversarial scope-escape review (wa4kfojcv) found + fixed one real MEDIUM+LOW defect:** promotion gated the raw node-key STRING through `host_matches_scope` (pure suffix/exact match) then embedded that string VERBATIM as the URL authority — so a key that string-matches scope but carries a URL-authority delimiter parses to a DIFFERENT host (`evil.com#.example.com` matches `*.example.com` but dials `evil.com`; a CIDR `10.0.0.0/24` service key dials `10.0.0.0`). The cardinal LIVE-safety held (the per-request `invoker._gate` re-parses the url + refuses fail-closed — reviewer confirmed no out-of-scope packet sent), but "in-scope BY CONSTRUCTION" + world-model integrity were broken. FIX: round-trip the built url through the SAME authority parser the gate uses (`ethics.extract_hostname`) and require the PARSED authority == the gated host + re-pass scope; store `attrs.host` = parsed authority. **DURABLE LESSON: "in-scope by construction" means in-scope BY THE SAME PARSER THE GATE USES — never gate a string that will be re-parsed differently downstream (string-match ≠ authority-match); a defense-in-depth layer must hold even against a malformed/hypothetical producer, not lean on the primary gate.** NEXT: Slice 1 DiscoveryFrontier (VOI kernel `planner/goal_tree.expected_information_gain` reused; dedup `(scheme_host,path,sorted-params,bug_class)`; crawler `Scope.from_seed`/`Crawler.crawl` + `scanner/discovery.py` miners ready for Slice 2).

**Step 0 (done before Phase 1):** landed the reviewed SAML crypto @ 77b2d62 — see [[aegis-runtime-defense-frontier]]. See also [[unified-autonomy-program]] (the completed 7-wave predecessor) and [[coverage-program]].
