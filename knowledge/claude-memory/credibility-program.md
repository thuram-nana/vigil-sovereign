---
name: credibility-program
description: "CRUCIBLE credibility-eval-platform program — corpus benchmark + reasoning-brain wiring; the reflection-FP hardening finding"
metadata:
  node_type: memory
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
---

Branch **credibility-eval-platform** (off coverage-m1). Approved plan (parsed-popping-lemur.md): two tracks, credibility-first. Track A = a reproducible multi-app benchmark platform; Track B = wire the already-built reasoning brain into the live loop. Follows [[coverage-program]]. Prompted by the audit finding that the reasoning brain (exploit-chains/WAF-bypass/constraint-inference/VOI) is BUILT but not wired into scan/engage, and the benchmark is one co-designed in-process app, not reproducible.

**KEY FINDING — reflection-FP hardening (the benchmark-driven pass that mattered).** Running CRUCIBLE against a real container surfaced a latent false-positive bug the co-designed benchmark app had masked (benchmark_app._search only reflects markup-shaped input on purpose). Three checks confirmed their vuln class on MERE PAYLOAD REFLECTION — any reflecting endpoint (search box, error page) tripped all three:
- `checks.py` DEFAULT_CHECKS: `SSTI_REFLECTION`/`PATH_TRAVERSAL`/`ERROR_BASED` (MarkerReflectionCheck→side_effect) → replaced with evidence-carrying `EvaluationCheck`(SSTI, 2 defaults: `{{...}}` + `${...}`), `ContentSignatureCheck`(path_traversal), `ErrorSignatureCheck`(error_based_sqli).
- Library: deleted 4 misclassified reflection entries (ssti.json/path_traversal.json/error_based_sqli.json/php_lfi.json — ids ssti-reflection/path-traversal/error-based-sqli/php-lfi). **Library 171→167.**
- Regression guard test `test_no_reflection_oracle_for_injection_classes` (test_library.py): NO shipped entry may use oracle.kind "reflection" for a non-xss class (reflection is legit ONLY for XSS via reflection_context). Also repointed the seed test to successors (m2-ssti-jinja2/h1-lfi-etc-passwd/m2-errsqli-single-quote) and php-exemplar to m2-ssti-smarty.
Result: node smoke app fp 6→0; benchmark still 9/0/0 (its sinks are genuinely real). Rule: **a reflected canary proves only that input is echoed — an XSS signal, never SSTI/traversal/error-based SQLi.**

**A1 (done)** perf/metrics. `eval/validation.py`: `RunMetrics`(elapsed_s/requests_sent/peak_rss_kb/pages_discovered/findings_reported — None not fake-0 when a tool doesn't report it), `MeasuredBoard`, `comparative_report_measured`, `render_measured_table`, `_attributable_rss` (getrusage child-then-self delta). `score()` gained `class_key` param. `CrucibleAdapter._record` sets `last_metrics`. `ScanReport.elapsed_s`. `benchmark` CLI now prints the measured table + a `## Performance` section. `render_table`/`comparative_report` kept unchanged (tests count exact columns).

**A2 (done)** corpus harness. NEW `eval/corpus_run.py`: `CorpusApp`/`GroundTruth`(curated|owasp|none)/`ScanTuning`, `DockerLauncher`(behind a `Launcher` seam — docker-free testing), `run_corpus_app`, `run_corpus`→`CorpusOutcome`(results+honest skipped), `wait_healthy`, `CorpusCrucibleAdapter`. `class_key_for`: family key (owasp_class_key) for owasp+curated, plain for none. NEW `eval/owasp_benchmark.py`: neutral CSV loader `load_owasp_expectedresults` (true-vuln rows only, `DAST_REACHABLE` subset) + `owasp_class_key` (collapses boolean/error/time_based_sqli→sqli, lfi→path_traversal, rce→command_injection — symmetric, only merges same-family). Loopback opt-in: `CrucibleAdapter(authorized_hosts=...)`. NEW `AuditFinding.endpoint` (locatable findings; `CrucibleAdapter._normalize` emits path?param) — engine sets it only for POINT checks (request-level keeps insertion_point token).

**A3 (done)** 13 descriptors in `eval/corpus_apps/*.json` (owasp-benchmark/juice-shop/dvwa/webgoat/webwolf/vampi/dvga/mutillidae/wordpress/drupal/keycloak/gitlab-ce/mattermost) authored by a Workflow, honest notes (unverified-in-build-env, auth/SPA/DB caveats, curation caveat). `benchmark --corpus [--apps a,b] [--include-heavy]` runs real containers, skips heavy/unavailable with reasons. `eval/corpus_apps/_smoke` = local vuln node app (Dockerfile from cached node base) → `crucible-corpus-smoke:latest`; proves the real DockerLauncher lifecycle (1/0/0).

**B1 (done)** exploit-chains in production. `engage.py`: `EngagementResult`(report+attack_paths+path_portfolio+chained_conclusions); `run_engagement` chains confirmed findings via `AutonomousCampaign.chain_findings` (pure reasoning, BEST-EFFORT — never sinks the engagement), `--no-chaining` flag. `report.py:build_report(attack_paths=...)` serializes the paths. IDOR/deser findings → attacker→datastore/host paths.

**A4 (done)** regression gate. NEW `eval/gate.py`: `Baseline`/`ToolScore`/`GateVerdict`, `snapshot`, `gate` (zero-tolerance on new-FP / newly-missed / precision-drop for CRUCIBLE only; asymmetric — improvements don't fail; missing app/tool = warning not fail). Committed baseline `eval/baselines/benchmark-app.json` (crucible 9/0/0). `benchmark --gate|--update-baseline|--baseline`; corpus gate too. NEW root `Makefile`: `make gate` (CI spine, no docker), bench, bench-corpus, gate-corpus, baseline, test.

**A5 (done)** `docs/BENCHMARK.md` rewritten as a RUNNABLE methodology (rerun commands, three-tier corpus table, neutral vs curated vs none ground-truth, matcher, incumbent versions+invocations, honest limitations). `benchmark --json` → committed `docs/benchmark-results.json` (+ `benchmark-scoreboard.md`). Fixed `docs/ARCHITECTURE.md:261` stale "benchmark spine" → points to eval/ spine. Fresh numbers post-FP-fix: crucible 9/0/0, wapiti 7 FP, nikto 7 FP (sqlmap 1.10.2 / wapiti 3.2.10 / nikto 2.6.0).

**B2 (done)** adaptive WAF-bypass in the live loop. NEW `scanner/waf_evasion.py`: `looks_blocked` + `adaptive_bypass` (tier1 `adaptive.waf_adapt` ladder, tier2 `adaptive.evolve` GA with `fitness.unblocked_gate`×proximity). Checks `MarkerReflectionCheck.adapt` (XSS, sink=executable reflection) + `ContentSignatureCheck.adapt` (LFI, sink=file signature). `AuditEngine(waf_adaptive=...)` calls `check.adapt` when normal confirmation FAILS (not when ctx is None — probe returns a ctx even when the marker is absent; the ORACLE decides). Confirmation stays oracle-gated → precision unchanged. Wired campaign→engage `--waf-adaptive`. GOTCHA: WAF_LADDER is cumulative + SQL-flavored (alt_case/sql_inline_split mangle a text marker); the LFI sink is response-side (file content) so it's robust — test via ContentSignatureCheck, not marker reflection.

**B3 (done)** grammar-fuzz + transfer. Campaign `grammar_fuzz`(N)/`grammar_fuzz_seed` → `_grammar_requests` induces `grammar.infer_grammar` over the crawl + `grammar.generate` N structurally-valid, in-scope (`Scope.in_scope`), deduped NEW requests onto the audit surface. Campaign `priors` → `_seed_transfer` folds cross-engagement `memory.priors.Prior`-shaped rows (bug_class/successes/attempts) into the bandit via `learning.seed_from_priors` keyed `(bandit_context, bug_class)`. Both in `_resolve_bandit`/`run`. Wired engage `--grammar-fuzz`/`priors`. VOI: left STANDALONE (real+tested in `planner/`), NOT folded into live check-ordering — folding it in would need a corpus comparison vs the bandit that the offline env can't run; honestly noted, not faked.

**REAL-CVE TIER (follow-up to the corpus gap):** the sandbox can't pull Docker Hub (daemon IPv6) BUT git clone + npm both work over IPv4 (`NODE_OPTIONS=--dns-result-order=ipv4first`), and node:22-bookworm is cached. So `eval/corpus_apps/_cve/` ships REAL npm-package CVE apps built from source: `st-2014-3744` (st@0.2.4, CVE-2014-3744 encoded path traversal) — CRUCIBLE confirms it via URL_PATH_SEG traversal → file-content oracle, **1/0/0 verified**. `_cve/build.sh` (host npm install + docker build → `crucible-cve-<dir>:local`), descriptor `cve-st-2014-3744.json`, docker+image-gated test `test_real_cve_st_is_detected_when_built`. `_cve/.gitignore` excludes node_modules (build.sh installs the pinned CVE version). Juice Shop from source FAILED here (cypress binary download + slow link + Angular build — impractical; official image unpullable). GOTCHA: st CVE needs %2e%2e-encoded dots + is PATH-based (URL_PATH_SEG), not a query param.

**PROGRAM COMPLETE — all A1-A5 + B1-B3 done + real-CVE tier, full v2 suite green (only joern/semgrep/live-URL skips), `make gate` exit 0. Committed 77ed167 + pushed; PR #28 (branch credibility-eval-platform -> main).** Also added `AuditFinding.endpoint` (locatable findings). VOI + `infer_predicate` (constraints) remain built-but-standalone by choice. NOTE: graphify-out/cache/ is untracked noise — don't commit it (only GRAPH_REPORT.md + graph.json are tracked).

**GOTCHAS:** docker registry pulls HANG here (daemon prefers unreachable IPv6; general IPv4 egress works). Bypass the gpg-backed cred helper with `DOCKER_CONFIG=<dir with {} config.json>`. Build corpus images from CACHED bases (node:22-bookworm, debian:bookworm-slim, etc.) — no registry pull. Don't run the full corpus here (un-cached images hang on pull). See [[crucible-testing-and-gotchas]] for the suite-contention rule.
