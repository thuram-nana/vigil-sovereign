---
name: speed-program
description: "CRUCIBLE Speed/Resilience/Protection program (X1-X6) — caching, at-rest security, IO, LLM failover, recon parallelism, runtime hardening; additive+default-safe over main"
metadata:
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
---

Operator directive (verbatim intent): weave three non-functional properties into CRUCIBLE **where needed** — (1) fast internal+external speed/performance, (2) load-balancing where required, (3) high end-to-end security at rest AND during operations. Plan file `~/.claude/plans/parsed-popping-lemur.md` (6 phases X1-X6). Grounded by a 3-agent survey: system is single-process, offline-first, deterministic, throttled; crypto/integrity/injection/egress/TLS core already strong → targeted hardening, not a rewrite.

**Operator decisions (AskUserQuestion, all "recommended"):** at-rest = *permissions + redaction, NO encryption dependency*; scan path stays *strictly serial* (only recon-collectors + the LLM path get parallelism/failover); deliver *all three phased, caching + at-rest security first*.

**CRITICAL SECURITY CONSTRAINT (persist):** "security" here = DEFENSIVE hardening of CRUCIBLE's OWN data/keys/integrity (protect operator data). Continue to DECLINE anti-defender tradecraft — detection-evasion, identity-rotation, proxy-chaining, stay-hidden-from-defenders. CRUCIBLE is deliberately correlatable + authorized (CLAUDE.md §VI). See [[enterprise-platform]], [[anti-hallucination]].

**NON-NEGOTIABLE INVARIANTS (every phase):** determinism/replay byte-identical (`make gate` unchanged — a cache is valid only as a PURE function of its key; recon parallelism pre-assigns seq by SORTED order + ingests in that order); throttle/target-safety (per-target scan serial; only recon collectors hitting DIFFERENT hosts + LLM path get concurrency); oracle authority + fail-closed safety stack no weaker; additive/default-safe (new flags default OFF, byte-identical when unused, offline-first, no new egress, no encryption dep). Cadence = build → full `framework/v2` suite green + `make gate` PASS → adversarial-review workflow (distinct-lens finders → 3 refuters, survive if <2 refute) → fix survivors → commit (branch off main) → PR → merge → sync main. Commits end `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`; PR bodies end `🤖 Generated with [Claude Code](https://claude.com/claude-code)`. Run `graphify update .` after code changes; stage `graphify-out/GRAPH_REPORT.md`+`graph.json`, NOT `graphify-out/cache/`.

**PHASES:**
- **X1 MERGED (PR #56, 83b997b):** determinism-safe caching. verify/reverify.py memoizes reverify_context() on DEFAULT-verifier path, key = canonical(oracle_context)+bug_class+claimed_confirmed_by+claimed_confidence (lru_cache 4096); collapses the ~5 grounding-assessment re-fires per finding (engage/report/reporter/critics/certify) + the verify_certificate double-refire (reproduction + claims-grounding) to ONE oracle run. Custom verifier bypasses memo; wrapper returns fresh model_copy (no poisoning). scanner/library.py memoizes load_library() for the SHIPPED DEFAULT dir only (~170 JSON parsed once/proc); other dirs read fresh (no staleness); returns private mutable list. Adversarial review 4 lenses → 0 findings. gate f1=1.000.
- **X2 MERGED (PR #57, 8fe1f9b):** data-at-rest. NEW common/paths.py::tighten_umask()/secure_write()/secure_dir()/secure_existing() (stdlib chmod, NO encryption). CRITICAL: secure_dir chmods a dir ONLY when it CREATED it (a pre-existing shared/operator dir is left alone — the ambient log's parent is v2_root, the SOURCE tree; chmod'ing it 0700 was a blast-radius bug). secure_write uses os.open WITH mode (no world-readable window). umask latched in __main__ + entitlement/authority CLIs. Routed: blackboard/memory sqlite (dir 0700 guards WAL sidecars + DB 0600), entitlement/authority/killswitch JSON, worldmodel/ledger/learning, evidence bundle, FINDINGS REPORTS (technical.md/postmortem.md — the HIGHEST-value at-rest data, initially missed), dryrun prompt dumps, intake authz-ledger + captures, logs. NEW common/redact.py: redact_header masks credential header VALUES in archived .http (keeps name; response.body untouched for oracle/manifest byte-fidelity; masking at WRITE time keeps the artifact manifest self-consistent). scrub_log_event masks secret-keyed log fields by EXACT name + whole SEGMENT + secret SUFFIX — NOT bare substring (else 'token' eats tokens_in/tokens_out/token_max telemetry → number→str corruption). Log size-cap 64MiB rotate-once (backup tightened). TWO adversarial review passes: pass1 9 findings (all fixed/mitigated; declined session_id masking = not a credential), pass2 1 finding (rotated-backup perms) fixed → 0 survivors. gate f1=1.000, content byte-identical.
- **X3 MERGED (PR #59, 42fe0c2):** IO efficiency on the MAO spine (engage/scan doesn't use blackboard by default → gate f1=1.000 unchanged). agents/blackboard.py: PRAGMA synchronous=NORMAL on connect (WAL from schema.sql; drops per-commit fsync, still commits per post) + ADDITIVE read(parent_id=) filter (idx_events_parent). agents/critics.py::panel_verdict_for reads read(parent_id=finding_id) not full-scan+Python-filter (O(N²)→O(children)); target_event_id payload check kept as belt+braces. agents/reflection.py: ReflectionAgent dedup via durable since_id cursor + _pending memo keyed on bb.count() (grows on any post). REVIEW HIGH (fixed): SpineSink.critic_verdict posted target_event_id in PAYLOAD but NO parent_id → indexed read DROPPED spine verdicts the old scan caught; fixed by SpineSink setting parent_id=target_event_id (mirrors MultiCriticAgent; FK parent_id REFERENCES events(id) satisfied since target is a real finding event). Review 0 survivors. DEFERRED: OOB poll de-block (scanner/checks.py) — would restructure the probe() protocol on the determinism/throttle-sensitive scan hot path; poll is a WAIT not CPU waste; deserves its own change.
- **X2b MERGED (PR #58, 2472eed):** X2 review follow-up — intake authorization ledger (.intake-authorizations.txt) append site (intake/cli.py) now paths.secure_existing(led) after open('a') so a pre-X2 0644 ledger self-heals to 0600 (creation was already secure_write; the append site never re-chmod'd).
- **X4 MERGED (PR #60, ca70013):** LLM resilience. common/errors.py NEW BackendOverloaded(BackendError)=transient (429/529/5xx/conn/timeout survived backoff; distinct so dispatch fails over — permanent BackendError does NOT). kernel/backends/anthropic.py::_create_with_backoff exp-backoff+full-jitter honoring Retry-After (CAPPED at _MAX_RETRY_AFTER_S=20s — review MEDIUM: uncapped Retry-After=999999 → 11-day hang; fixed), client built max_retries=0 (single explicit policy). kernel/llm.py::complete_with_failover tries cached get_backend() primary → on BackendOverloaded advances to next AVAILABLE in permitted_preference() (never escapes tier; each _construct re-asserts policy), records answering backend in trace, surfaces overload if all fail (no silent drop); env-override used directly. binding._bind: explicit backend=direct (no failover, tests unaffected), backend=None=failover path. kernel/consistency.py::run_consistent gains max_workers(default 1=serial byte-identical)+RateLimiter; parallel via ThreadPoolExecutor.map (SUBMISSION ORDER → byte-stable modal rep). hypothesize_consistent: SERIAL for is_dryrun (n_samples unchanged — test asserts ==5/==3; no dump-filename race), parallel for live; passes backend=None thru lambda so parallel samples keep failover. Env CRUCIBLE_LLM_MAX_WORKERS/MIN_INTERVAL_S (4/0). Review 0 survivors. gate f1=1.000 (dryrun path byte-identical). GOTCHA: could NOT collapse dryrun to 1 sample (test_consistency asserts n_samples==samples for dryrun).
- **X5 MERGED (PR #61, 66d67b0):** OPT-IN parallel recon collectors. intel/ingest.py::run_collectors gains max_workers (default 1=serial); pre-assigns each collector's seq in deterministic cols order BEFORE dispatch → obs_id (pure fn collector+seq+claim) unchanged; ThreadPoolExecutor.map (preserves order) → ingest in cols order → RESULT byte-identical (proven by test: serial vs parallel-8 identical world-model/entities/tallies under roster AND VOI order). Collectors STATELESS. intel/transport.py lazy httpx client build now double-checked LOCK (review LOW: parallel race built N clients, leaked pools). RECON_MAX_WORKERS env DEFAULT 1 (OPT-IN — review LOW survivor: parallel default-on defeats serial fail-fast, so a CollectorEgressRefused raiser no longer short-circuits siblings → parallel emits 1 extra AUTHORIZED request to an allowlisted host on a partial-allowlist misconfig; egress guard still bounds ALL traffic to allowlisted hosts, unauthorized egress impossible; resolved by opt-in default → default byte-identical incl fail-fast). engage.py+intel/cli.py pass max_workers=RECON_MAX_WORKERS. gate f1=1.000. GOTCHA: ex.map eager-submits all → loses serial's fail-fast egress-suppression; the RESULT stays byte-identical (both raise before self.ingest) but exception-path SIDE-EFFECTS (egress) differ under parallelism — inherent, egress-guard-bounded.
**== ENTIRE SPEED PROGRAM X1-X6 MERGED TO MAIN (PRs #56,#57,#58,#59,#60,#61,#62; main @ 5dec54e). COMPLETE. ==** Fast+load-balanced+secure woven in where needed, every phase additive/default-safe/adversarially-reviewed, gate f1=1.000 unchanged throughout, scan path stays serial, anti-defender DECLINED. Recurring pattern that worked: build→full suite+make gate→distinct-lens finders→3 refuters (survive if <2)→fix survivors→PR→merge→sync. Reviews caught real bugs every phase (X2 substring over-mask + source-tree blast-radius; X3 SpineSink parent_id drop; X4 uncapped Retry-After hang; X5 client-build race + parallel-default fail-fast; X6 console CSRF fail-open on no-metadata POST + malformed-Host ValueError). GOTCHA: the suite ran ~15min some late runs (environmental, not hung — pytest state Rl/Sl with accumulating CPU); a refuter occasionally hit the Opus safety-classifier on cyber topics (treated as not-refuted).
- **X6 MERGED (PR #62, 5dec54e):** runtime security. verify/collaborator.py: poll secret in X-Relay-Key HEADER not ?key= query (server accepts header OR legacy query); RelayClient REFUSES non-loopback http:// (require https for remote; loopback http + remote https OK). engage.py: --oob-relay-secret from file(--oob-relay-secret-file)>CRUCIBLE_OOB_RELAY_SECRET>argv(warned). console/server.py::do_POST _same_origin_as_console guard: reject Sec-Fetch-Site not in (same-origin,none), non-loopback Origin, or rebinding Host → 403 (GET/SSE unaffected; console has NO cookie-auth so it's really browser-SSRF/rebinding defense). kernel/sovereignty.py: opt-in CRUCIBLE_SOVEREIGNTY_SEALED latches env tier ONCE into _sealed_policy (later env flip can't relax; fail-safe PIN-only); set_policy(None) clears latch (test isolation via autouse _reset_policy); is_sealed(). __main__::_status: Governance section shows tier(+sealed) + entitlement ENFORCED/⚠UNGOVERNED. All ADD refusals / opt-in → gate f1=1.000 unchanged.

GOTCHA: session started with X1 already partially done as UNCOMMITTED working-tree changes (reverify.py/library.py + 2 test files) from a pre-compaction session — reconciled + completed rather than redone. Recurring: bb.read() default limit truncates (page/cursor+fail-closed); never overclaim what the deterministic layer enforces.
