---
name: sigil-phase0-memory-loop
description: "SIGIL local-first AI orchestrator — Phase 0 \"memory loop\" build state (spine + vectors + MCP recall)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
---

**SIGIL** (spec `/home/kali/Pictures/PENTEST-main/SIGIL.md`, plan `~/.claude/plans/parsed-popping-lemur.md`) is a local-first sovereign AI orchestrator. Built in-repo at **`/home/kali/sigil/`** (net-new package, NOT under PENTEST-main). Started 2026-07-17. NO offensive capability by doctrine — `sigil/reuse/` vendors CRUCIBLE's hash-chain + Ed25519 signing **verbatim, fully decoupled** (imports zero `framework.*`; `assert_no_offense()` enforces it).

**Owner decisions:** nightly extraction runs **agent-driven via headless Claude Code on the Max plan (zero API cost)**, not metered API (API path documented but dormant). Deliverable was a Phase 0 build plan + 6-phase roadmap.

**Phase 0a + 0b + most of 0c COMPLETE & PROVEN.** Spine now **43,333 records**, **13,658 vectors**, **160 graph sessions**, **5 of 8 MCP tools**, chain OK, head signed. Registered in BOTH Claude Code (✓ Connected) and Claude Desktop (`~/.config/Claude/claude_desktop_config.json`, additive). **14/14 tests** (10 integrity + 4 graph).

**Phase 0a + 0b (foundation):**
- **Spine** (`~/.sigil/spine/spine.jsonl`): append-only, hash-chained, **23,944 records** (all of today's session + curated docs + git history). Two-layer integrity: BINDING (payload re-hashes to cert_digest) + CHAIN (entries link). `cert_digest` is **wallclock-free** (excludes ts) → replay-stable. Signed head (`head.json`, owner Ed25519 1-of-1 @ `~/.sigil/spine/keys/owner.priv` 0600). `verify_checkpoint` honestly distinguishes benign GROWTH (chain past anchor → "re-sign") from TAMPERING (truncation/rewrite). **10/10 integrity tests** (`tests/test_integrity.py`).
- **Ingest** (`sigil/ingest/`): incremental per-file cursor (`~/.sigil/cache/ingest_cursor.json`); transcripts resume@offset; `git.py` incremental via `git:<repo>` cursor key (idempotent — re-run adds 0); **live hooks** (post-commit + post-merge) installed in PENTEST-main + sigil repos, fast `--git-only` path.
- **Vectors**: fastembed **BAAI/bge-small-en-v1.5** (384-dim, ONNX, CPU, free). Only EMBEDDABLE_KINDS embedded (message/decision/commitment/document/session/brief/**commit**), NOT raw tool records (~65% volume). **9,154 points**. Honesty gate GROUNDED_SCORE=**0.66** (true-absent floors 0.55–0.64, true-present 0.74+); `grounded()` returns "no grounded match" rather than fabricate.
- **Qdrant SERVER mode** (switched from local single-lock): container `sigil-qdrant` (qdrant/qdrant:v1.12.4, loopback 6333/6334, `--restart unless-stopped`, volume `~/.sigil/qdrant-server`). Migrated 9,153 vectors from local store **without re-embedding**. Concurrent read+write proven (MCP recall + background index coexist). Persisted via **`~/.sigil/sigil.env`** (`SIGIL_QDRANT_URL=...`) which `config.py` loads at import → both CLI and Claude-spawned MCP server use it. Stale local store at `~/.sigil/qdrant` left as cold fallback.
- **MCP**: `sigil-memory` (FastMCP, official `mcp` SDK) **✓ Connected in Claude Code**; tool `memory_search` (grounded, provenance-labelled). Acceptance test PASSES: recalls today's SIGIL build + the mrbeanpanel pentest, cited by spine seq.

**Phase 0c (done so far):**
- **MCP tools** (5/8): added `episodic_range` (contiguous spine window, provenance) + `ingest_status` (health+integrity) — pure spine reads; + `graph_entity` + `graph_query` over the Kùzu graph. Remaining 3 (`threads_open`/`commitments_due`/`contradictions_pending`) need the consolidation layer.
- **Kùzu graph** (`sigil/graph/`, `~/.sigil/graph/current/` with `staging/` atomic-swap + `manifest.json` carrying true `rebuilt_seq`): DETERMINISTIC replay of the spine → Project/Session/Document/Commit nodes + IN_PROJECT edges, every node cites `anchor_seq`+`anchor_hash`. `normalize_project()` collapses transcript-slug ⇄ repo-basename (slug is AMBIGUOUS under dash-split because dir names contain hyphens → map against known repo paths, don't guess). `sigil graph [--status|--entity|--query]`. Double-rebuild → identical (test-locked).
- **Subagent ingestion** (`sigil/ingest/subagents.py`): the 159 subagent transcripts under `<session>/subagents/agent-*.jsonl` (+`.meta.json` = agentType/description/toolUseId) were INVISIBLE before — now each is its OWN session (re-keyed by agentId via `ingest_transcript`'s new `source`+`extra` params), titled by its task description, spawn-linked. 19,165 events. Recall PROVEN (7/12 deep-query hits come from subagents). `ingest` does subagents by default (`--no-subagents` to skip; `--git-only` skips them).

**Gotchas:** Docker `credsStore:"desktop"` in `~/.docker/config.json` breaks pulls on this box → pull with `docker --config <clean-dir-with-empty-config>`. Importing `framework.v2.*` pulls structlog (absent) → that's why reuse is vendored. Server-mode `order_by` needs a payload index on `seq` (created in `_ensure()`, idempotent) or the durable cursor returns -1 and re-embeds everything. The subagent embed of long texts is CPU-slow (~minutes) but one-time (incremental after).

**OWNER REQUIREMENT (2026-07-17, do not forget):** the consolidation extraction provider MUST be user-choosable among **(1) Claude via Max plan** (`claude -p`, the AgentProvider — owner's PREFERENCE, "use claude not local"), **(2) Claude API key** (metered Anthropic API, `ApiProvider`), **(3) local** (Ollama, `LocalProvider`). Plus offline `HeuristicProvider` + `ReplayProvider` for tests. CLI: `sigil consolidate --provider claude|api|local|heuristic|replay`. Owner OK'd running the REAL agent-consolidation over the spine (use claude/Max). Use a FAST model (Haiku) for extraction to keep `claude -p` batches quick/cheap.

**CONSOLIDATION BUILT (`sigil/consolidate/`, 2026-07-17):** ARCHIVIST pass = extract→gate→promote→brief over spine records since a cursor. Providers: `claude`(Max `claude -p`, Haiku default, ~5s/tiny-window), `api`, `local`(Ollama), `heuristic`, `replay`. **KEYSTONE DESIGN — SERVE-THE-QUOTE:** the promoted fact's authoritative content is the **verbatim record quote** (gate re-fetches `store.get(seq).text()` and requires the quote verbatim + ≥2 salient Unicode tokens + cited seq in the fed window); the model's free-text `statement` is **advisory-only, never served as fact**. Demote-only; ungrounded→refusal (recorded, filtered from tools). Idempotent via TWO ledgers (grounded vs refused, grounded-first) keyed on (kind,subject,**quote-content**)+owner/due. Contradictions are **extractor-JUDGED + gate-verified** (not structural). `sigil consolidate --provider ...`. Tools threads_open/commitments_due/contradictions_pending serve grounded+cited only. **REVIEW LESSONS (3 adversarial rounds, 14 real findings):** (1) a token-**subset** check of a model statement is NOT entailment — dropping a negation ("not deploy"→"deploy") or reordering inverts meaning while staying a subset → the ONLY sound fix is serve-the-quote (don't gate a model paraphrase, serve real record text). (2) structural same-subject contradiction detection is unsound — a reaffirmation and a real contradiction have identical lexical overlap; only the LLM can judge opposition. (3) idempotency ledger must separate grounded from refused or a demote becomes demote-permanent. (4) tokenizer must be Unicode-aware + strip trailing punctuation. **near-zero-FP can't be self-certified — each adversarial round caught defects the prior fixes introduced.** 27/27 tests (13 consolidation+10 integrity+4 graph).

**REAL RUN DONE (claude/Max Haiku, 73-record window):** 17 candidates → gate grounded 13 (7 decision + 5 entity + 1 commitment), demoted 4, brief seq 43350, re-signed, chain OK @ 43351. All 3 tools return real cited data (commitments_due empty = the 1 commitment had no due; contradictions empty = no reversals in window). **QUALITY CAVEAT: soundness perfect (every served fact is byte-verbatim real content, cited — triple-reviewed, zero fabrication) but Haiku PRECISION is noisy — it over-labels status updates / task-assignments as "decisions." Fix = better model (Sonnet/Opus) or the tightened prompt (DOCTRINE now defines decision=committed choice NOT status/result/instruction).** The noisy facts are append-only (supersede later, don't delete). Full-corpus consolidation over all 43k records is the nightly-incremental job (not run — would be hours of claude -p); run `sigil consolidate --provider claude` incrementally.

**PHASE 0 (the memory loop) COMPLETE.** NEXT = Phase 1 (Rust KERNEL + WARDEN authorization + T0/T1/T2 router + signed action log — reuse the vendored spine signing + a gate chain). The **remaining agent-driven consolidation** (`sigil/consolidate/`, ARCHIVIST nightly pass) — extract decisions/commitments/entities from spine records via headless `claude -p` on Max (AgentProvider, D5) + a deterministic fallback provider for offline tests; resolve (reuse `intel/resolve.py` flag-don't-force pattern); detect contradictions (reuse `veracity/consistency.py contradicts()`); PROMOTE only spine-traceable facts as new provenance-linked spine records (kind=decision/commitment/entity/contradiction) behind a DEMOTE-ONLY veracity gate (reuse `veracity/firewall.py`); nightly brief. Unblocks the last 3 MCP tools. **Approach: build machinery + prove on a BOUNDED slice, defer full 43k-record Max run until owner OKs the spend.** `claude` CLI is at `/home/kali/.local/bin/claude`. See [[unified-autonomy-program]] for the reuse-doctrine lineage. Then Phase 1 = Rust KERNEL + WARDEN.
