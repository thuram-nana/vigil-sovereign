---
name: anti-hallucination
description: "CRUCIBLE system-wide anti-hallucination veracity layer — P0+P1 MERGED to main; P2-P7 roadmap"
metadata:
  node_type: memory
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
---

Operator wants the MOST advanced anti-hallucination in EVERY part of CRUCIBLE — infrastructural (harness/loop), not prompt engineering. Grounded by a 5-agent map+SOTA-research workflow (wf_e05b47d3-380). Key insight: CRUCIBLE already had the SPINE (oracle sole authority, Bayesian belief, SCE, calibration ledger, signed evidence certs) but it was ENFORCED IN ONE PLACE (scanner promotion); every other claim entry was honor-system. The build makes grounding UNFORGEABLE + universal.

**CORE INVARIANT:** no claim becomes a FACT unless a cited `GroundingToken` RE-EXECUTES to one of 4 grounds — ORACLE (verify.reverify re-fires), CERT (evidence.verify_certificate), WORLDMODEL (node belief_lcb≥floor + provenance traces to oracle/cert), HYPOTHESIS (gated+prior≤cap → labelled hypothesis, NOT fact). Validated by re-running the proof, NEVER string trust. Layer only DEMOTES/abstains, never promotes an oracle-refused claim. Ungrounded → stamped + SHOWN labelled (label-don't-drop), never fact.

**P0+P1 MERGED to main (commit 3c0b4a8, PR #39):**
- NEW `framework/v2/veracity/` package: tokens.py (GroundingToken, from_dryrun), claims.py (Claim/AdmittedClaim/VeracityVerdict GROUNDED|UNGROUNDED|CONTRADICTED|ABSTAIN; is_fact only fact-strength; Claim.bug_class = the subject a ground binds to), consistency.py (contradicts via net-refuted belief), firewall.py::admit (decompose→bind→verify→label).
- P1: `agents/critique_agent.py:201` — LLM-only "confirm" (no oracle) → "llm_advisory" NOT "confirmed" (that word is oracle-only now); consumes trace.is_dryrun→critique_dryrun. `agents/models.py` critique_status Literal += "llm_advisory". reporter_agent reports confirmed+advisory (label-don't-drop), marks dry-run stubs, honest per-strength header (killed the blanket "all oracle-signed-off").
- Adversarial review (5 dims) found 6 real defects (0/3 refuted), ALL FIXED: **BINDING** (a proof must back THIS claim — oracle re-fires for the CLAIM's bug_class, so a SQLi cert can't ground an RCE claim; a fact claim must declare its subject); **WORLDMODEL string-trust** (replaced bypassable denylist with ALLOWLIST oracle:/cert:/finding:/evidence: — intel:/derived: are legit but NOT fact-strength); calibrated_confidence = RE-EXECUTED oracle value; dry-run surfaced in report.
- **GOTCHA/doctrine fix:** 6 integration-test fixtures (mao, acp, full_integration) asserted an LLM-only finding was "confirmed" + mirrored to memory — the EXACT hallucination this closes. Fixed by giving them REAL firing oracle proof: `FindingContext.from_state({achieved},{achieved}, bug_class=X)` fires the achieved_state oracle regardless of bug-class mapping. IDOR/webhook-forgery → achieved_state.

**P2 MERGED to main (commit 08cb152, PR #40):** world-model admission, ADDITIVE + default-safe (belief byte-identical in default mode — no regression). `worldmodel/models.py` `classify_provenance()` = SINGLE SOURCE OF TRUTH (oracle:/cert:/finding:/evidence:→GROUNDED; llm/assume/guess/hallucin→UNGROUNDED; intel:/derived:/infer:→INTEL; else UNCLASSIFIED) + `grounding` field on Node/Edge. `graph.py` add_node/add_edge tag grounding from winning provenance; opt-in `WorldModel(strict_grounding=True)` floors UNGROUNDED belief at 0.2. `veracity/firewall._provenance_traces_to_proof` delegates to classify_provenance. FIX: `intel/project.py` edges now carry obs.attrs (derivation rationale was dropped). Console surfaces grounding (api.worldmodel + node-drawer badge). GOTCHA: Node/Edge use extra="forbid" → grounding must be a REAL field (not @computed_field) to round-trip.

**P3 BUILT + ADVERSARIALLY REVIEWED — PR #41 OPEN on branch `veracity-live-admission`, AWAITING USER MERGE** (auto-mode classifier blocks self-merge of agent-authored PRs; P0-P2 landed before that gate). P3 = the firewall runs LIVE over findings (closes the P0-review "admit() has zero callers" gap). NEW `veracity/adapters.py` (claim_from_finding + admit_finding): builds a Claim from a real AuditFinding (oracle_context→ORACLE token bound to bug_class; entity=`endpoint:<param>`) and runs admit() against the CHAINED world. `engage.py` new `EngagementResult.grounding` (index-aligned w/ active_findings), populated AFTER chaining by `_assess_grounding` (world holds endpoint nodes then); CLI ⚠-marks any finding not grounded-as-fact. Additive/default-safe/no-traffic/best-effort. `consistency.py` contradicts() gained a 2nd conservative trigger: belief LOWER-BOUND (lcb<0.20) alongside mean<0.35. **Adversarial review (12 agents) found+fixed 2 real defects (both 0/3 refuted, empirically reproduced): (1) CRITICAL binding-forgery — `reverify_context` SILENTLY IGNORED the requested bug_class because confirm_finding re-derives it from the context's own field (confirmation.py:126 `if not ctx.get("bug_class")`), so a finding relabelled sqli→rce ground as an RCE FACT on a SQLi-only proof. FIXED at the re-execution boundary: reverify now refuses when requested class≠evidence's embedded class — hardens verify_certificate too. (2) MEDIUM node-id-mismatch — `_endpoint_node_id` fell back to insertion_point for empty-param findings, minting a phantom id the chainer never makes→missed CONTRADICTED. FIXED to mirror the chainer's `endpoint:<param>` exactly.** GOTCHA (reusable): the oracle re-fires under the CONTEXT's embedded bug_class, NOT the caller's requested one — binding must be enforced by comparing requested vs embedded class, done now in reverify.py. Full framework/v2 suite green. NEXT once merged:
**P4-core MERGED to main (commit adca82d, PR #42).** Two coupled pieces: (A) report sentences bound INTO the EvidenceCertificate: NEW `evidence/models.py::ReportClaim{sentence,bug_class,render_as}` + `report_claims: list|None=None` on EvidenceCertificate w/ `@model_serializer(mode="wrap")` that DROPS the field when empty → existing signed certs BYTE-IDENTICAL (the whole model is signed so bound sentences are covered free). `certify.py` verify_certificate gains 5th layer `claims_grounded` (each render_as="fact" claim re-admitted through the firewall vs the authenticated oracle_context; a claim whose declared bug_class doesn't re-verify FAILS the cert closed → folds into .ok + verify_bundle.ok). NEW `evidence/claims.py` (deterministic prose split, no LLM). (B) `agents/reporter_agent.py` RE-EXECUTES each finding's oracle at report time instead of trusting stored `verified_by_oracle`; non-reproducing finding → "unverified at report time" DEMOTED (distinct header category, not lumped w/ llm_advisory); genuine findings byte-identical. `veracity/adapters.py::claim_from_finding` gained `match_confidence` (default True = P3 path; reporter uses False bc blackboard confidence is CALIBRATED not raw). Reporter does NOT propagate critique_dryrun to the ORACLE token (that flag marks the LLM CRITIQUE dry-run; the oracle ground is a deterministic re-fire). **Adversarial review (30 agents) found+fixed 2 (6 refuted 3/3): (1) HIGH HONESTY BUG — claims_for_finding stamped the finding's bug_class onto ANY fact_prose and the gate only checked the LABEL (auto-inherited), so a SAME-CLASS over-claim ("RCE" on a SQLi finding) grounded as fact, and my docstrings OVERCLAIMED an NL-entailment guarantee a deterministic gate can't provide. FIXED BY REDESIGN: a deterministic cert asserts ONLY the canonical STRUCTURED fact (re-grounds by construction); arbitrary prose is ALWAYS labelled analyst-commentary (retained/signed, never machine-verified fact). Docstrings now precisely scope enforcement (declared-class re-execution + signature tamper-evidence) + DISCLAIM NL entailment; boundary test documents the limit. (2) LOW — reporter header lumped demoted w/ llm_advisory; added distinct "demoted" count+legend.** GOTCHA (reusable): a deterministic gate CANNOT do NL entailment — only certify STRUCTURED re-executable claims; never let a doc/reason string overclaim semantic verification (that's itself a hallucination). GOTCHA: pydantic model_dump ALWAYS emits declared fields incl. defaults → a new optional field on a SIGNED model MUST be dropped-when-empty via @model_serializer or it breaks every existing signature. **P4b MERGED to main (commit 089cb47, PR #43):** scanner export states LIVE grounding — build_report re-executes each active finding's oracle via admit_finding (honest-by-default), stamps ReportFinding.grounding (fact|contradicted|ungrounded|hypothesis|unclassified); `--strict-evidence` withholds non-fact active findings from the RENDERED doc but NOT the raw ScanReport/--reverifiable-out; blanket HTML footer → per-count honest `_footer_note`; SARIF grounding property + HTML badge + summary.by_grounding/strict_evidence. Number-laundering: memory/recall.py WinningHypothesis.score_kind ("unranked" no-text placeholder vs "cosine"); intel/resolve.py Entity.confidence_basis (merge-llr/structural-default/singleton-default). Adversarial review (5 dims) found+fixed 1 (2 refuted): MEDIUM — strict footer said withheld findings "are shown as leads" (false); fixed to say WITHHELD (kept in reverifiable artifact). GOTCHA: match_confidence=True is CORRECT for scanner AuditFindings (their confidence IS the raw oracle value) — a real finding grounds as fact; only the reporter (calibrated confidence) needs match_confidence=False.
**P4c MERGED to main (PR #44):** attack-path certification. NEW `evidence/models.py::PathStep` + `PathCertificate{destination,steps,backing_cert_digests(sorted),seq}` w/ cert_digest; digest rides the SAME signed chain head (chain.py UNCHANGED — path digests just extend the digest chain). `certify.py::build_path_certificate` (duck-typed AttackPath) + verify_bundle `path_certs` param + `_verify_paths` fail-closed + BundleVerification.path_results folded into .ok. cert_set_bound compares (finding+path digests)==chain. Adversarial review (4 dims) found+fixed 1 (0/3 refuted): HIGH AUTH BYPASS — path certs are NOT individually signed, so head=None (unsigned) let an attacker fabricate a route citing a genuine finding + rebuild the chain → ok=True (fail-OPEN). FIXED: paths require a VALID signed head (head_anchored = head is not None AND chain_ok) else fail closed. GOTCHA (reusable): any NEW unsigned content added to a bundle fails OPEN in the head=None branch unless gated on a valid signed head — individually-signed finding certs are safe there, new content is not. Honest-scope docstring: verify proves backing findings verify + tamper-evidence, NOT that findings causally establish the hops (deterministic check ≠ re-run pathsearch). **ALL OF P4 DONE.**
**P5 MERGED to main (PR #45):** kernel self-consistency. NEW `kernel/consistency.py`: categorical_entropy (normalized Shannon, generalizes goal_tree._bernoulli_entropy), run_consistent(run_fn,*,samples,agreement_gate,entropy_gate,key_fn) — samples a no-oracle binding N×, clusters by decision-bearing key_fn (default full model_dump), returns modal + ABSTAINS if agreement<gate (agreement is PRIMARY gate; entropy_gate=1.0 inclusive-off by default), consistency_evidence()=SCE-style PENALTY (never boost, not fed to oracle/SCE). Prompt gained samples=1/agreement_gate=0.6 (default single-shot). hypothesize_consistent() wraps hypothesize (key=sorted distinct bug_classes). NO-ORACLE ONLY; only demotes. Adversarial review (4 dims): 0 survivors (3 refuted). GOTCHA: normalized entropy≤1.0 + strict `>` means entropy_gate=1.0 never fires — agreement gate covers it (documented).
**P6 MERGED to main (PR #46):** bug_class value-membership. verify/verifier.py: known_bug_classes()/is_known_bug_class()/canonical_bug_class()/require_known_bug_class() + reusable pydantic types NormalizedBugClass (canonicalise) + KnownBugClass (canonicalise+reject-unknown). kernel/models.py Hypothesis gains computed_field oracle_provable=is_known_bug_class(bug_class). Adversarial review (3 dims): 0 survivors (3 refuted). **KEY GOTCHA (cost me a regression): do NOT canonicalise Hypothesis.bug_class IN PLACE — the realistic_executor keys scenarios on the RAW (bug_class,surface) spelling (realistic_executor.py:409), memory_agent + planner also read raw bug_class; normalizing broke the MAO end-to-end test (no findings). Kept bug_class RAW + oracle_provable normalizes internally.** Tightened oracle_provable docstring: means "in curated vocab", NOT "no oracle can fire" (oracles_for falls back to ALL oracles for unknowns) — firewall re-fire stays the fact authority.
**P7 MERGED to main (PR #47) — PROGRAM COMPLETE.** NEW `calibration/conformal.py`: conformal_halfwidth (finite-sample split-conformal quantile, rank ceil((n+1)(1−α)), →1.0 when n too small = honest wide band), conformal_band(point,pairs,*,alpha,calibrator,min_labels=MIN_LABELS=8,fallback) — nonconformity |target−prob| over ledger labelled pairs (DISPUTED excluded), GATED on min_labels else Bayesian fallback marked coverage_guaranteed=False, band_for_prediction(query,pairs) = sound integration (centre on query's OWN estimate). Adversarial review (3 dims) found+fixed 2 HIGH (both routes to a FALSE coverage guarantee): (1) train/cal OVERLAP — don't refit a calibrator on the scored pairs; use the fixed raw_score or a DISJOINT-fit calibrator. (2) MISMATCHED PREDICTOR — split-conformal guarantee attaches to a band centred on f(query); centring on the SCE posterior while q comes from the exploitability estimator voids it → replaced band_for_posterior with band_for_prediction (centre = query's own calibrated estimate = same estimator as the scores). GOTCHA (reusable stats lesson): a conformal band's centre MUST be the SAME estimator whose residuals set the half-width, computed on data NOT refit from the scored labels — else coverage_guaranteed is a lie.

**== ALL P0–P7 MERGED TO MAIN (PRs #39,#40,#41,#42,#43,#44,#45,#46,#47). The system-wide anti-hallucination veracity layer is COMPLETE. ==** Spine: veracity firewall (admit, re-execution-only) → live over findings (engage + reporter) → world-model admission → contradiction → report_claims + attack-path certs bound into signed evidence (fail-closed) → scanner export live grounding + honest numbers → kernel self-consistency (no-oracle) → bug_class value-membership → conformal coverage bands. Every phase: additive + default-safe + adversarially reviewed (each found real defects, all fixed). Recurring lesson across phases: NEVER let a doc/field/guarantee OVERCLAIM what the deterministic layer actually enforces (the firewall re-fire is the fact authority; NL entailment, causal hop-linkage, and coverage guarantees must be scoped honestly or they are themselves hallucinations).
**ROADMAP P5-P7 (each own reviewed PR, additive/default-safe; plan at ~/.claude/plans/parsed-popping-lemur.md):** P4 reporting-gate depth (evidence/claims.py atomic-claim binding INTO the cert; --strict-evidence toggle [operator chose BOTH default-label + strict-hide]; number-laundering labels). P5 kernel self-consistency (kernel/consistency.py N-sample+semantic-entropy, NO-ORACLE bindings only [operator chose], abstention). P6 entity-grounded structured outputs (parse_json_response value-membership; normalize_bug_class in schema). P7 calibration/conformal.py (coverage bands on HypothesisPosterior; gate on MIN_LABELS). See also [[enterprise-platform]], [[intel-engine]].
