---
name: sigil-production-hardening
description: "SIGIL production/enterprise hardening — packaging, spine scale+durability, config/logging, robustness, CI; tier 1 MERGED (PR #12), tier 2 MERGED (PR #13 @ a3402a7)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7758e121-f349-47d5-886b-6bb5a1d60e27
  modified: 2026-07-19T11:53:35.810Z
---

SIGIL production/enterprise hardening — **MERGED to `thuram-nana/sigil` main @ 340426b (PR #12)**. Owner asked to take SIGIL from "impressive reviewed prototype with documented seams" to production standard, autonomously. Approach: a **6-dimension structured audit (a Workflow fan-out) → 46 grounded findings (4 critical, 15 high) → fixes in reviewed waves**. Builds on Phases 0-9 ([[sigil-phase9-companion]]). 348 pytest (27 suites) + 26 Rust green; ruff clean; `sigil doctor` works.

**Baseline gaps confirmed:** no pyproject/entry-point, no CI, no lint/type gates, 103 print()/0 logging, 44 broad excepts, 11 hardcoded /home/kali paths, O(file) full-spine reads across ~38 call sites on a 22-92MB spine, and the test `__main__` runners **exit 0 even on failure** (CI green-wash).

**Waves (branch production-hardening):**
- **Wave 1 packaging @ 03fa3aa** (me): `pyproject.toml` (PEP 621, classic setuptools), `sigil` console command + `python -m sigil` (`__main__.py`), extras (voice/secrets/capture/dev/all mirroring lazy-import tiers), single-sourced `__version__=0.9.0` (kernel Cargo.toml reconciled), **numpy** declared (was silently transitive), ruff/mypy/pytest config.
- **Wave 2 @ eb862a1** (4 PARALLEL agents, disjoint files, no-git, I integrate): **(spine, the crown jewel)** seq→byte-offset INDEX (get O(1), iter_records(since_seq) O(returned), full scans bypass index staying chain-BYTE-IDENTICAL) + torn-line tolerance (torn TAIL survives restart, torn MIDDLE still fails verify()) + fsync-on-append + atomic signed-head (temp→fsync→replace) + `KillSwitch.is_engaged()` module-cached (was a full scan per governor decision). **(config)** removed ALL 10 hardcoded /home/kali paths → layered config (env→file→default), new `obs.py` logging, `config.doctor()`/`effective_config()`, `sigil doctor` CLI, `_load_env_file` non-UTF-8 tolerant. **(bridge)** Content-Length guard (non-numeric→4xx), PushNotifier single-pass (no per-event get()), structured request/denial logging (path+pubkey-PREFIX only, never a secret), SIGTERM graceful shutdown. **(robustness/honesty)** kernel_classify non-object-JSON→A3, vectors OUTAGE (raise) vs EMPTY (-1) distinction, **an INERT input backend no longer signs a spine record CLAIMING an injection that never happened**, run_gesture warns when the local landmarker is inert, render/web_engine docstrings describe only what exists.
- **Wave 3 CI @ f18b770** (me): `.github/workflows/ci.yml` — one job that **BUILDS the kernel first so the Python↔Rust oracle tests actually RUN not silently skip** (closing "a skip counts as a pass"), then cargo test + `ruff check` + `pytest` (BLOCKING — pytest exits non-zero, so CI can finally FAIL) + `mypy` (advisory). ruff narrowed to real-bug rules (F/B/E9), style deferred; 12 dead imports auto-fixed + 1 real F841 fixed; mypy pinned 3.13 (numpy stubs), 62 advisory type findings logged.
- **BLOCK-1 fix @ c44ef39:** the spine index got a DEDICATED red-pen — HELD 5/6 (200-trial index-vs-scan divergence, OLD-vs-NEW byte-identical proof, cross-process, torn-MIDDLE-fails-verify, atomic-head, forged-release/cross-trust-root cache) but caught **BLOCK-1 (safety-critical)**: torn-tail READ tolerance + fsync append WITHOUT repairing the tail on the WRITE path → an interrupted write leaves torn bytes at EOF, the next append writes AFTER them, MERGING <torn>+<new> into one unparseable line → the ack'd record is SILENTLY LOST, verify() stays green, and **a lost kill-switch PANIC never halts the mesh** (old code raised loudly). FIX: `append` truncates the dead torn tail (via `_last_valid_boundary`, binary a+b) before writing — only removes never-committed garbage, committed records untouched, clean appends byte-identical. +2 controls (append-after-torn-tail durable; panic-after-torn-tail honored).

**LESSONS:** (a) audit-before-fix (a Workflow fan-out of dimension auditors) grounds the work in file:line evidence, not guesswork. (b) The parallel-agent lesson from Phase 9 held: write+scoped-test-only, NO git, I own commits — BUT store.py (imported everywhere) being edited by one agent while others' tests import it caused a transient import-race (a gesture test hit a half-written `_start_offset_for`); recovered on re-run; **foundational files (store.py, config.py) edited in parallel with other agents running tests is the risk — the full clean re-run at integration is mandatory.** (c) **an optimization can itself be a safety regression** — the torn-tail read-tolerance without write-path repair silently lost ack'd records incl. a panic; only the dedicated red-pen on the crown-jewel data structure caught it. gh=thuram-nana.

**TIER 2 — MERGED @ a3402a7 (PR #13, branch prod-tier2):** the three honestly-deferred items taken to done under the dual-review bar. (1) **mypy now CLEAN (0 errors/122 files) + BLOCKING in CI** (was `mypy sigil/ || true`) — the 62 advisory findings resolved to only 5 justified `# type: ignore` (1 arg-type whose TypeError is caught on the device-arm freshness path; 4 `[override]` for intentionally heterogeneous agent `run()` sigs); fixed real latent type bugs: `ingest/cursor.py` `dict[str,int]`→`dict[str, int|str]` (stores commit-hash STRINGS), `gesture/remote.py` `_is_number` now a `TypeGuard`, `vectors/index.py` bare string→`Direction.DESC` enum. (2) **Kernel distribution via setuptools-rust** — `[[tool.setuptools-rust.bins]]` (target `sigil-kernel`, `strip="Debug"`, NO explicit `--release` — setuptools-rust adds it, doubling errors cargo) builds+ships the kernel binary into the env's script dir on `pip install`; `config.kernel_bin()` gained a venv-bin step (`Path(sys.executable).parent/exe`) AFTER repo-relative so dev is byte-identical; **`MANIFEST.in` (`graft kernel`+`prune kernel/target`)** ships the crate source in the sdist. Proven end-to-end in a THROWAWAY venv from BOTH a wheel and an unpacked sdist (binary lands in bin/, resolves, classifies hid.type→A2/pointer→A1/account.create→A3). (3) **CI oracle tests actually RUN** — 5 test sites hardcoded `/home/kali/sigil/kernel/...` + self-skipped (skip counted as pass); now resolve via `config.kernel_bin()`. Plus the ops layer (RUNBOOK.md + deploy/systemd + logrotate + bridge.env.example, all pass `systemd-analyze verify`). **DUAL REVIEW: behavior-lens CLEAN (every mypy edit annotation-only/provably-neutral); packaging/trust-lens caught 2 real defects — HIGH: sdist shipped ZERO Rust source (no MANIFEST.in) → `pip install` from sdist FAILED at build_rust (headline deliverable broken on the sdist path; wheels+git-checkout were fine) → fixed+re-verified by building a wheel from the unpacked sdist; LOW/defense-in-depth: `kernel_bin()` venv-bin step resolved CWD-RELATIVE when `sys.executable` is empty/bare (`Path('').parent/exe`→`./sigil-kernel`) → a CWD-planted binary could become the WARDEN authorizer → guarded to `is_absolute()`-only, repro confirms old-resolves/new-rejects.** 348 pytest (no skips) + 26 Rust green; ruff+mypy clean. **LESSON reinforced: the "distribution works" claim was true for wheels but FALSE for the sdist — only the packaging red-pen that actually built from the sdist caught it; a green wheel build does NOT prove the sdist install path.**

**STILL DEFERRED (documented, not done):** spine snapshot/compaction/segment-rotation (append-only-safe design-first), budget/token metering. Not started — confirm with operator before beginning (explicitly a later tier).
