<!-- One line per memory: title link + short hook. Detail lives in each topic file. gh=thuram-nana. -->

## SIGIL (local-first personal AI orchestrator @ /home/kali/sigil, repo thuram-nana/sigil)
- [SIGIL hard-prune program](sigil-hardprune-program.md) — cold-archive record DELETION (VAULT-PRUNE); Slices A/B/C MERGED (head-v2, floor.json, SnapshotState+12 bearer folds), D/E (snapshot writer+archive, then the cutover+kill-9 gate) remaining. Every review caught a real defect.
- [SIGIL spine segment rotation](sigil-spine-rotation.md) — MERGED main @9885c6b (PR #14); 400 pytest(+crash-fuzz)+26 Rust green. RETAIN-ALL rotation bounds the 93MB spine WITHOUT tamper-model change (entry_count ABSOLUTE + genesis-rooted → verify/classify_head/signed-head reused byte-identical; C1/C2/state-folding DON'T arise). Designed via a 10-agent UNDERSTAND + 17-agent DESIGN-PANEL workflow. 5 reviewed slices: S0 manifest+inode-lockfile+migrate+change_token · S1 segment-spanning reads (RAISE on unreadable seg)+seam-tip(no seq-0 fork) · S2 reset+`sigil spine` cli · S3 crash-safe seal-swap (write-then-rotate, atomic manifest commit) + **kill-9 crash-fuzz GATE** · S4 gzip-on-seal `compact()` = ~5-8x DISK RECLAIM (unlink-direct + open-fd/retry reader-safety). Dual review+re-check caught a real defect EVERY slice (fork, cross-store contamination, kill-switch un-halt, env-armed os._exit, neg-threshold DoS, tail() replay-dedup bypass, concurrent-compactor crash, buggy non-load-bearing reader-grace). DEFERRED: hard-prune (C1 operator decision), Slice-5 idx-sidecars/cold-archive, live-spine migration (owner runs `sigil spine migrate`), budget metering.
- [SIGIL production hardening](sigil-production-hardening.md) — MERGED main @340426b (PR #12); 348 pytest+26 Rust green, ruff clean, `sigil doctor` works. 6-dim Workflow AUDIT→46 findings→waves: packaging (pyproject+`sigil` entry+extras+numpy) · spine seq→byte-offset INDEX (O(1) get, chain-byte-identical)+fsync+atomic-head+torn-line-tolerance+killswitch-cache · layered config+obs.py logging · bridge Content-Length/PushNotifier/secret-free-logging/SIGTERM · fail-closed classifier+Qdrant-outage≠empty+inert-backend-no-fake-injection · CI (builds kernel so oracle tests RUN; ruff/pytest blocking, mypy advisory). Spine red-pen HELD 5/6 + caught BLOCK-1 (torn-tail+fsync-append w/o write-repair silently lost an ack'd record incl. a MISSED PANIC → truncate torn tail before append). Lessons: audit-before-fix; an OPTIMIZATION can be a safety regression. **TIER 2 MERGED @a3402a7 (PR #13):** mypy CLEAN+BLOCKING (5 justified ignores; fixed real latent type bugs) · **kernel distribution** (setuptools-rust bins + MANIFEST.in ships crate source; `kernel_bin()` venv-bin step; proven from wheel AND sdist in a throwaway venv) · CI oracle tests now RUN (5 hardcoded paths→`kernel_bin()`) · ops layer (RUNBOOK+systemd+logrotate). Dual review caught HIGH (sdist shipped no Rust src→`pip install` from sdist failed) + LOW (resolver CWD-relative for empty/bare `sys.executable`); both fixed+re-verified. Lesson: a green WHEEL build does NOT prove the SDIST install path. STILL DEFERRED (confirm first): spine snapshot/compaction, budget metering.
- [SIGIL Phase 9 COMPANION](sigil-phase9-companion.md) — MERGED @98b8532 (PR #10) + tightenings @e18c0fe (PR #11) + README @2c9fcc1; 315→348 Py green. Phone as signed remote-control/approval/gesture over WireGuard (phone holds only its OWN device key). Envelope→bind_ok+TLS transport→installable PWA (WebCrypto Ed25519, approve/panic/relay/recall/**arm**)→pairing+`bridge serve`/`mesh authorize`→gesture-trackpad→**device-signed remote arm (TRUST-WIDENING, arm_by_device)**→Android backend. 4 parallel-agent waves; dual review+2 re-checks caught 4 BLOCK+3 HIGH+fix-introduced defects (int(inf) crash, chain-fork, dedup DoS regression) — all fixed. Multi-agent lesson: worktree-isolation didn't take → builders write+test-only NO-git, orchestrator owns commits; trust-widening LAST+dual-review.
- [SIGIL Phase 8 FRONTIER](sigil-phase8-frontier.md) — MERGED PRs #7-9; 213 Py+26 Rust. WS-E SCRIBE grounded scraper (VOI+robots+cited); WS-F SIGIL-HAND gesture (tiers.rs INPUT_A1/A2 — gesture can't type/launch); WS-G DELEGATE web-actor (creds off-spine, per-action A3, single-shot, no impersonation). WARDEN oracle ALREADY forces account/login/submit→A3. WS-G dual review caught 2 distinct HIGH (non-idempotent execute→unbounded POSTs; preview-only cap batch-bypass)+MED; re-check PASSED.
- [SIGIL Phase 7 Embodiment](sigil-phase7-embodiment.md) — MERGED PRs #2-6; 231 tests. P0 KernelClassifier+SpineTailer → WS-A Vision (egress-gated frontier) → WS-B Operator (files+terminal, plan→preview→hash-approve→execute→verify→rollback/undo) → WS-C loopback cockpit → WS-D cross-platform+mobile (device-auth ledger, WG bridge). Red-pen caught a real (usually HIGH) defect in EVERY unit.
- [SIGIL Phase 6 Hardening](sigil-phase6-hardening.md) — PR #1 @295440c. Authenticated governor (kill-switch latch+budgets+per-record-KIND promotion, ALL owner-Ed25519-signed vs persisted key) at base._dispatch; signed approval queue (verify_approval on-enforcement-path — was DEAD); SCHOLAR SSRF gate; red-pen 15 fixed. (gh auth-status mislabels "Water-Hacker".)
- [SIGIL Phase 5 Perception+BASTION](sigil-phase5-perception-bastion.md) — screen/camera OCR authoritative + VLM advisory (serve-the-quote); BASTION own-infra-ONLY-by-construction cert/CVE/uptime scanners; `sigil agents bastion|perceive`; red-pen 10 fixed.
- [SIGIL Phase 4 ARTIFICER+SCHOLAR](sigil-phase4-artificer-scholar.md) — bg coding (claude -p, tests-before-PR, PRs-not-pushes, worktree, A2) + research (serve-the-QUOTE not the model's claim); `sigil agents artifice|research`.
- [SIGIL Phase 3 Agent Mesh](sigil-phase3-agent-mesh.md) — ARCHIVIST/SENTINEL/STEWARD/ENVOY, ceiling gate (auto≤A1 else queued), ENVOY drafts-only; `sigil agents brief|triage|sentinel|run`.
- [SIGIL Phase 2 Voice](sigil-phase2-voice.md) — full-duplex wake→VAD→ASR→KERNEL→TTS + barge-in; ElevenLabs JARVIS voice wDsJlOXPqcvIUKdLXjDs pinned; key in sigil.env (rotate!); `sigil voice --mic`; live mic needs AEC.
- [SIGIL Phase 1 KERNEL+WARDEN](sigil-phase1-kernel-warden.md) — Rust kernel @/home/kali/sigil/kernel; token-based fail-closed tiers A0-A3, hash-chained Ed25519 signed action log+HEAD anchor, T0 router, palette CLI.
- [SIGIL Phase 0 memory loop](sigil-phase0-memory-loop.md) — hash-chained spine (43k records) + fastembed vectors (Qdrant server) + deterministic Kùzu graph + live git hooks + 8/8 MCP tools (Claude Code+Desktop) + agent-driven consolidation (serve-the-quote gate; providers claude/api/local/heuristic/replay).

## CRUCIBLE (offensive engine @ /home/kali/Pictures/PENTEST-main/framework/v2)
- [CRUCIBLE anti-hallucination](anti-hallucination.md) — veracity firewall = RE-EXECUTION not string trust; P0-P7 MERGED (PRs #39-47). Recurring lesson: never overclaim what the deterministic layer enforces.
- [CRUCIBLE Intelligence Engine](intel-engine.md) — reason-over-intel; MERGED (PR #34). SCE + entity resolution + gated live collectors (crt.sh/DoH/RDAP) + VOI ReconPlanner + asset-graph inference.
- [CRUCIBLE enterprise platform](enterprise-platform.md) — 4 frontiers MERGED (c8e5325, PRs #35-38): crypto evidence certs, attack-path business impact, evidence browser UX, cloud-IAM+SBOM. DECLINED evasion/rotation (anti-defender).
- [CRUCIBLE unified-autonomy program](unified-autonomy-program.md) — "Unified Provable Autonomy" 7 waves ALL MERGED @845e058: reasoning core, universal sensors+oracles (Nmap/TLS/cloud-IAM/SBOM/threat-intel), web breadth, platform (plugin registry/MCP/importers). Each wave = parallel worktree agents → adversarial-review workflow → sequential merge, gate byte-identical.
- [CRUCIBLE speed program](speed-program.md) — X1-X6 MERGED @5dec54e (PRs #56-62): caching, at-rest perms+redaction, spine indexed reads, backoff+failover, opt-in parallel recon, runtime-sec. Scan stays SERIAL, anti-defender DECLINED; a review caught a real bug per phase.
- [CRUCIBLE nervous-system](nervous-system.md) — immutable event-spine + RL/metacognition/multi-critic/reflection (N0-N7); RL=re-rank/defer only, oracle stays sole authority.
- [CRUCIBLE beyond-SOTA](crucible-beyond-sota-program.md) · [coverage](coverage-program.md) (checks are data → JSON in scanner/library_entries/) · [credibility](credibility-program.md) · [Ops Console](ops-console.md) (loopback read-only UI) · [testing & gotchas](crucible-testing-and-gotchas.md) (bs4/numpy/z3 absent, killswitch path) — assorted MERGED programs.
- [Prover→Discoverer program](prover-to-discoverer-program.md) — APPROVED 2026-07-13; make CRUCIBLE ROAM & find new surface. KEYSTONE: discovery inert until intel/promote.py mints url-bearing endpoints (Slice-0). Oracle=sole authority, fail-closed scope gate, default-OFF byte-identical.
- [Gap-closure 13-workstream](gap-closure-13-workstream-program.md) — 13 swarms closed audited gaps (2 rounds, nothing deferred); survived an API outage via self-driving merge. + integration/learner-loop @9c64351 (OODA loop feeds OutcomeLedger, opt-in --learn, non-circular DISPUTED labels).

## AEGIS (embeddable DEFENSIVE dual of CRUCIBLE)
- [AEGIS runtime-defense frontier](aegis-runtime-defense-frontier.md) — "provable firewall" + oracles, OracleKind now **27**. MVP @62d4af3, GATEWAY @b4b4bb6, then programs @20cac88/42bb0d6/69e099a + batches @4b39288/ec7de09 + held items @e7dfe41/77b2d62. Key lessons: near-zero-FP CAN'T be self-certified (dual review = red-pen + independent sweep is the bar); tunable thresholds must be calibrated vs the MEASURED belief curve; an oracle that disclaims a semantic layer (c14n) must REFUSE-TO-ADJUDICATE, not assert the negative; in-band XXE demoted to a LEAD (OOB is the only sound block-path); packaging: classic setuptools table form (PEP 639 SPDX needs >=77, breaks stock LTS). The adversarial RE-CHECK on the FIXED branch catches defects my own fixes introduce.
