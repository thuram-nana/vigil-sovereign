{
  "summary": "Design the COLD-ARCHIVE hard-prune for the SIGIL spine: signed Merkle snapshot anchor + head-schema v2 + snapshot-aware verify + state-folding + archive-move + durable floor",
  "agentCount": 16,
  "logs": [],
  "result": {
    "recommendation": "I have everything grounded — the three designs, all 12 verdicts, and the real code (`chain.py` `verify_chain`/`_head_payload`/`verify_head`, `checkpoint.py` `classify_head`/`verify_checkpoint`, `models.py` `SignedChainHead`). The retain-all rotation spec I read is the already-merged base; below is the deferred tier, fully specified.\n\n---\n\n# RECOMMENDED — COLD-ARCHIVE HARD-PRUNE for the SIGIL spine\n\n## 0. Decision & lineage\n\nAll three candidate designs converge on the same architecture; they differ only in the ~10 seams where judges found defects. The recommendation is **VAULT-PRUNE's structure and A–E slice spine** (highest scores, two `fatal_flaws:[]` sweeps) **hardened with the correct seam-decisions from HARD-PRUNE and COLD-ARCHIVE and from every judge's `best_change`.** The two designs that scored lower did so on *fixable seam bugs*, not architecture — I graft the fixes rather than the designs.\n\n| Graft (the decisive one) | Taken from | Kills judge finding |\n|---|---|---|\n| **Live window selected BY SEQ (`seq >= base_seq`), not by array prefix** | HARD-PRUNE / COLD-ARCHIVE | VAULT-PRUNE's prefix slice is fragile across crash windows; by-seq is what makes every cutover state verify clean |\n| **`SnapshotState.load` keyed STRICTLY to the *signed* `head.snapshot_seq`; every fold filters `iter_records` to `seq >= head.base_seq`** | all four VAULT-PRUNE judges + both HARD-PRUNE crash judges | the double-count crash-window landmine (fold-seed + live-rescan of the same `[0..K)`); makes the head the single commit marker for BOTH pruning-visibility and fold-seeding |\n| **Left-edge pin is UNCONDITIONAL TAMPERING** (no `live[j].seq==base_seq` OR `live[j].prev_hash!=base_prev_hash` → TAMPER) | COLD-ARCHIVE judges | the one **false-CLEAN** front-drop hole — the `n<entry_count` no-op trap transposed to the left edge |\n| **`verify_head` made base_count-aware** (`entry_count == base_count + len(window)`) | COLD-ARCHIVE judges (×3) | `chain.py:73`'s `entry_count != len(entries)` false-alarms **every** legit prune |\n| **Account-create cap folds a `{(service,origin): count}` PAIR-keyed dict** | COLD-ARCHIVE (only design that got this right) | HARD-PRUNE's flat `{service:n}/{origin:n}` is a **FATAL false-clean** (cap reset → mass-creation via relabelled origin) |\n| **Off-lock archive copy → re-read manifest fresh under re-acquired flock → new set = `fresh.segments − archived_ids`** | HARD-PRUNE judge (lost-ack) + COLD-ARCHIVE judge-2 (panic-block) | concurrent seal-swap during off-lock copy → lost ack; and copy-under-flock blocks kill-switch PANIC appends (D1) |\n| **`prune.intent` written FIRST (K + archived-segment-ids, not the head); reconciler re-drives from on-disk state** | COLD-ARCHIVE judge-2 | the `base_count != head.base_count` reconciler trigger goes stale post-rebase, orphaning floor+unlink |\n| **`prev_head_hash` = sha256 of the prior head's *signing bytes* (distinct hash-space from `head_hash`); every post-first-prune head is v2 and maintains it** | COLD-ARCHIVE judges | `floor.head_hash` overloaded two hash-spaces; routine `sigil sign` between prunes broke the meta-chain |\n| **Unkeyed read paths (`store.verify()`, `SpineTailer`) seed `genesis_prev` from the manifest advisory `base_prev_hash`, cross-checked vs the signed head** | COLD-ARCHIVE judges | pruned store false-tampers in `sigil verify` / dashboard / mobile SSE feed |\n| **Cumulative multi-prune commitment: `fold(load(prior_snapshot), delta)` + chained merkle accumulator** | VAULT-PRUNE judges 3&4 | prune #2's from-genesis rescan needs the offline archive mounted; silently drops earlier nonce/revoke/cap state |\n| **Mandatory exhaustive `iter_records`/`entries` census (~30 sites) before the fold slice** | COLD-ARCHIVE final judge | \"~9 bearers\" is provably incomplete (found `gesture/session` device-arm replay ledger, `budget` daily-cap) |\n\n**Do not use `model_dump(exclude_unset=True)`** for the version-conditional payload (§2). Four judges independently flagged it fragile: `model_validate_json` marks every defaulted field \"set,\" so `exclude_unset` re-emits the six fields and false-alarms every deployment. **Mandate the explicit pop.**\n\n---\n\n## 1. Snapshot anchor — the `kind=\"snapshot\"` record + head v2 fields\n\nTwo coupled owner-signed commitments, cross-checked; the **head is authority**, the record is the durable, replayable intent.\n\n**(A) A spine record `kind=\"snapshot\"`, `actor=\"OWNER\"`**, appended at the LIVE tip `S=T+1` via the normal retain-all path (real `cert_digest` over content, `entry_hash`, `fsync`-before-ack) *before any deletion*, so it is itself hash-chained and covered by every future head. Add `\"snapshot\"` to `KINDS` in `spine/models.py`. Payload commits, over the pruned prefix `[0..K)`:\n\n| field | value |\n|---|---|\n| `base_seq` | `K` — first RETAINED seq = `first_seq` of the oldest KEPT sealed segment (**K MUST be a sealed-segment boundary** so we archive whole segments and `base_prev_hash` is already a stored value) |\n| `base_prev_hash` | `entry_hash(K-1)` = boundary_hash of the newest archived segment = the kept segment's existing `first_prev_hash` — the synthetic genesis-prev the re-based live set links from |\n| `base_count` | absolute count of pruned records `[0..K)` (`==K` when contiguous from genesis; both stored and cross-checked) |\n| `delta_merkle_root` | binary Merkle over **this prune's** new leaves `[entry_hash(K_prev)..entry_hash(K-1)]`, seq order, dup-last-on-odd (C2) |\n| `cumulative_merkle_root` | `H(prior_snapshot.cumulative_merkle_root ‖ delta_merkle_root)` — the running accumulator so prune #N never needs the offline archive mounted |\n| `snapshot_seq` | `S` (its own seq) |\n| `prev_snapshot_seq` | prior snapshot's seq (`-1` first) — the snapshot meta-chain, cross-checked against the head meta-chain |\n| `folded_state` | the cumulative `SnapshotState` (§6), computed as `fold(load(prior_snapshot), delta_records)` |\n\n**(B) `SignedChainHead` v2** copies the SAME six commit fields (`base_seq, base_prev_hash, base_count, cumulative_merkle_root, snapshot_seq, prev_head_hash`) INSIDE the owner-Ed25519 payload; `head_hash=entry_hash(S)`, `last_seq=S`, `entry_count = base_count + len(live)` (stays **ABSOLUTE**). The head — not the record — is the verification authority; the record is what the reconciler replays.\n\n**Descends from exactly one prior head:** `prev_head_hash = sha256_hex(evidence_signing_bytes(_head_payload(prior_accepted_head)))`. A verifier accepts head `H` iff `sha256(signing_bytes(H)) == floor.head_sig_hash` (re-verify the current accepted head) **OR** `H.prev_head_hash == floor.head_sig_hash` (advance to its unique child). A linear meta-chain = exactly one parent, no fork, no skip, no replay of a stale owner-signed head. `classify_head` additionally cross-checks `head.{base_seq,base_count,cumulative_merkle_root} == prev_snapshot_seq` chain consistency; because `snapshot_seq` is in the *signed* head, the head can never claim a boundary the immutable record does not carry.\n\n---\n\n## 2. Head-schema v2 migration (A1) — the flag-day linchpin\n\n`SignedChainHead` (`models.py:56`, keep `extra=\"forbid\"`) gains six fields defaulted to the **no-prune identity**:\n\n```python\nbase_seq: int = 0\nbase_prev_hash: str = _GENESIS_PREV\nbase_count: int = 0\ncumulative_merkle_root: str = \"\"\nsnapshot_seq: int = -1\nprev_head_hash: str = \"\"\n# schema_version bumps 1 -> 2 only at the FIRST prune, and STAYS 2 thereafter\n```\n\n**THE blocking gate — version-conditional signing payload** in `_head_payload` (`chain.py:50`, currently `model_dump(mode=\"json\")` minus `signatures`, and `canonical_json` sorts keys):\n\n```python\ndef _head_payload(head):\n    d = head.model_dump(mode=\"json\")\n    d.pop(\"signatures\", None)\n    if d.get(\"schema_version\", 1) < 2:          # explicit pop — NOT exclude_unset\n        for f in (\"base_seq\", \"base_prev_hash\", \"base_count\",\n                  \"cumulative_merkle_root\", \"snapshot_seq\", \"prev_head_hash\"):\n            d.pop(f, None)\n    return d\n```\n\nA v1-signed head re-dumped under the v2 model then produces **byte-identical** signing bytes — every persisted `head.json` and every old-client verify passes unchanged. `sign_head`/`verify_head` both route through `_head_payload`, so v1/v2 stay symmetric. **Merge gate: a real pre-existing v1 head verifies byte-identically under the v2 model.**\n\n**After the fleet upgrade, once the first prune emits a v2 head, ALL subsequent heads are v2** (routine `sigil sign` too) so they carry `prev_head_hash` and keep `base_*` current — otherwise the meta-chain breaks between prunes.\n\n**Harden the two readers:**\n- `verify_checkpoint` (`checkpoint.py:86`, bare `model_validate_json` — today crashes a v1 client on v2 fields): wrap `model_validate_json + classify_head` in `try/except`; on `ValidationError`/parse failure do a lenient `json.loads` to read `schema_version` — if it exceeds the known max return the DISTINCT `(False, \"schema too new — upgrade sigil\")`; a genuinely corrupt head returns `(False, \"corrupt head\")`. Never silent, never false-clean.\n- `SpineTailer._resolve_head` (swallows all exceptions to `(None,None)` = \"un-notarized\" = a false-CLEAN degrade): peek `schema_version` first; if `> SUPPORTED`, return a THIRD sentinel `\"schema_ahead\"` that `poll()`/`check_anchor` surface as `anchor_ok=False, upgrade_required=True` — never the benign \"un-notarized live tail.\"\n\n**Old mobile client vs a v2 head:** `extra=\"forbid\"` → `ValidationError` → routed to \"upgrade required\" (SAFE: not-proven, never false-clean). **The first prune is a fleet-upgrade FLAG DAY** — gate v2-head emission behind every paired mesh device advertising `max_head_schema >= 2` (piggyback `mesh/registry.capability_map`), plus explicit owner sign-off.\n\n---\n\n## 3. verify_chain / classify_head / verify_head snapshot-awareness\n\n**`verify_chain` (`chain.py:37`)** gains `genesis_prev: str = _GENESIS_PREV` and seeds `prev = genesis_prev` instead of the hardcoded constant. Every existing caller passes the default → byte-identical. The `i>0` seq-contiguity check (`chain.py:44`) is unchanged; a re-based live segment whose first record has `prev_hash == base_prev_hash` links cleanly instead of forking.\n\n**`verify_head` (`chain.py:66`)** gains `genesis_prev` passthrough and becomes base-aware. Replace line 73's `head.entry_count != len(entries)` with:\n```python\nif head.head_hash != exp_hash or head.last_seq != exp_seq \\\n   or head.entry_count != head.base_count + len(entries):\n    return False, \"head does not match the chain (log truncated or head rewritten)\"\n```\n(reuse the existing `prev_highwater` param for the floor high-water check — it is already there).\n\n**`classify_head` (`checkpoint.py:65`)** becomes snapshot-aware and selects the live window **BY SEQ, not array prefix**:\n```python\nbase = head.base_count\nsigned_live = head.entry_count - base\nlive = [e for e in entries if e.seq >= head.base_seq]\n# LEFT-EDGE PIN — UNCONDITIONAL TAMPERING (closes the false-CLEAN front-drop hole):\nif not live or live[0].seq != head.base_seq or live[0].prev_hash != head.base_prev_hash:\n    return False, \"TAMPERING: live window does not start at the signed base (front-truncated/rolled back)\"\n# ANTI-TRUNCATION — the real guard that survives pruning (NOT the no-op n<entry_count):\nif len(live) < signed_live:\n    return False, f\"TAMPERING: live window has {len(live)} records but the head anchors {signed_live} (truncated)\"\nok, msg = verify_head(head, live[:signed_live], tr, genesis_prev=head.base_prev_hash)\nif not ok:\n    return False, f\"TAMPERING: history rewritten at or below the signed head — {msg}\"\nif len(live) > signed_live:\n    return True, f\"anchors {signed_live} live records; {len(live)-signed_live} appended since — run `sigil sign`\"\nreturn True, f\"anchors all {signed_live} live records (current)\"\n```\n`entry_count` stays ABSOLUTE; `base_count` is inside the owner-signed payload and Merkle-bound to exactly `base_count` archived leaves, so an attacker cannot shrink the live window and mask it by inflating `base_count` without an owner signature. Records at `seq < base_seq` still physically present during a crash cutover window are the benign not-yet-pruned prefix — ignored by the by-seq selection, so no crash state reads as TAMPERING.\n\n**Unkeyed paths (required, else the pruned store false-tampers in `sigil verify`/dashboard/mobile feed):** `store.verify()` and `SpineTailer` seed `genesis_prev` from the manifest's advisory `base_prev_hash` (cross-checked against the signed head so the advisory field is never trust-bearing); `SpineTailer` seeds `_last_hash = base_prev_hash` when resuming from a cursor `since_seq < base_seq` (else the first live record reads as a linkage break).\n\n---\n\n## 4. Durable external floor (C1) — `floor.json` contract\n\n`~/.sigil/floor.json`, mode `0600`, at `SIGIL_HOME` root **OUTSIDE `spine/`** so `SpineStore.reset()` / `sigil ingest --reset` (which `rmtree`s the spine dir) can never lower it. Written only via `atomicio.atomic_write_text` (temp→fsync→replace→dir-fsync), and only after a head+manifest have committed.\n\n```jsonc\n{ \"schema_version\": 1,\n  \"scope\": \"<SCOPE>\",\n  \"last_seq\": <int>,            // monotonic up\n  \"base_seq\": <int>,            // monotonic up\n  \"base_count\": <int>,          // monotonic up\n  \"head_sig_hash\": \"<sha256 of the CURRENT accepted head's signing bytes>\",\n  \"updated_ts\": \"<iso>\" }\n```\n\n**Reject rules** (in `classify_head`/`verify_checkpoint`, mirrored into `SpineTailer._high_water`): reject any head/snapshot with `last_seq < floor.last_seq` (rollback), `base_count < floor.base_count` or `base_seq < floor.base_seq` (un-prune / older-snapshot replay), or a v2 head failing the meta-chain rule of §1(B). **Update is upward-only**, after the commit instant (§9-B2); a legitimate `--reset` shortening the spine requires a **separate, explicit, owner-key-gated `sigil floor reset`**, never the routine reset.\n\n**Honest limit:** closed for warm / same-host / returning-paired verifiers by the persisted floor + `SpineTailer._high_water`. **Unclosable from data alone** for a cold verifier bootstrapping off an untrusted mirror — the owner key is FS-resident (`checkpoint.py:9`, accepted §1.3 simplification), so a full-tuple rollback `{short spine, re-signed v2 head, rolled-back floor, matching snapshot}` is class-identical to today's single-file rollback, **not widened**. Bounded (not eliminated) by seeding the initial floor over the authenticated WireGuard pairing channel, never the mirror (§10).\n\n---\n\n## 5. Cold-archive move + live re-base + verifier re-attach\n\n**Which moves:** whole sealed segments only (never the active, never a partial), oldest-first, whose `last_seq < K`, keeping the newest `N` sealed live (`SIGIL_SPINE_LIVE_SEALED`, default keep ≥2 for the warm window) or those younger than a retention window — and only up to the referential floor (§7). **MOVE = copy → verify → drop** (additive-then-atomic; never a destructive rename first).\n\n**Archive layout** — `SIGIL_HOME/spine/archive/` (relocatable to an offline mount via `SIGIL_SPINE_ARCHIVE_DIR`):\n```\narchive/segments/seg-*.jsonl.gz          # moved segments, still gzip, byte-identical\narchive/archive.manifest.json            # APPEND-ONLY across prunes, generation-stamped\n```\n`archive.manifest.json` = one entry per archived segment `{id, file, first_seq, last_seq, first_prev_hash, boundary_hash, sha256, count}` + one entry per snapshot `{snapshot_seq, base_seq, base_count, delta_merkle_root, cumulative_merkle_root, prev_head_hash, head_sig_hash}`. It is a genesis-rooted segment set: `seg-0.first_prev_hash == _GENESIS_PREV`, each `boundary_hash == next.first_prev_hash`, last `boundary_hash == base_prev_hash`.\n\n**Live manifest re-base:** the retain-all `Manifest` gains reserved header fields `base_seq/base_prev_hash/base_count/snapshot_seq/cumulative_merkle_root` (advisory — manifest stays non-load-bearing). The re-based manifest (`generation++`) drops the archived segments; the oldest REMAINING live segment keeps its existing `first_prev_hash` (already `== base_prev_hash` — a whole-segment boundary), so **no segment file is rewritten** — pure metadata.\n\n**Re-attach to verify from genesis:** `sigil spine verify --with-archive <dir>` concatenates `archive/segments…` (in `archive.manifest` order) then the live segments in seq order → one genesis-rooted stream → `verify_chain(genesis_prev=_GENESIS_PREV)`; then recompute each `delta_merkle_root` from the real archived leaves and re-chain the accumulator, asserting `== each snapshot's committed cumulative_merkle_root`. Full genesis→tip integrity for anyone handed the archive. Per-segment `sha256` is the at-rest integrity; the owner keeps ≥1 replica (losing the archive makes the pruned prefix unrecoverable — only the Merkle commitment proves what *was* there).\n\n---\n\n## 6. State-folding — `SnapshotState` + per-bearer contract\n\n**Census mandate (Slice C-0, before any fold code):** enumerate ALL ~30 `iter_records()`/`entries()` call sites and classify each `{windowed since_seq=cursor → prune-safe | monotonic security-bearer → MUST fold | content → MUST route to Q5}`. This is required because \"~9\" is provably incomplete.\n\n`SnapshotState.load(store)` reads the snapshot record **named strictly by the committed `head.snapshot_seq`** (empty identity state when the head is v1 / `snapshot_seq == -1` — an uncommitted/orphan snapshot record is inert). Every folding consumer switches to `st = SnapshotState.load(store); fold(st, [r for r in store.iter_records() if r.seq >= head.base_seq])`. Because both the fold-seed and the live-scan floor derive from the **same signed head**, they flip together at the commit instant — no double-count in any crash window. **Multi-prune:** each snapshot's `folded_state = fold(SnapshotState.load(prior_snapshot), delta_records[K_prev..K))`; all folds are associative (max / union / last-write / count-add / boundary-latch), so this is exact and never rescans from genesis. Seed an **empty** snapshot first (Slice C) and prove `fold == pure-scan` byte-identical *before any deletion*; extend to a two-prune `fold-of-fold == full-scan` proof.\n\n| # | Bearer (consumer) | Folds (over pruned `[0..K)`) | Fail direction if missed |\n|---|---|---|---|\n| 1 | `envelope.device_nonce_highwater` | `{device_pubkey: max(nonce)}` (semilattice, split-exact) | replay — **critical**, mandatory |\n| 2 | warden checkpoint (`cli.py`/dashboard) | `{pubkey: [best_count, best_head_hash]}` highest anchored per key | anti-rollback regress |\n| 3 | `killswitch._scan_engaged` | `engaged: bool` = the verified latch state at `K-1` (engage=any, release=owner-signed-verified, in order); ambiguous → `True` | fail-safe halt |\n| 4 | `actor_scope.creation_allowed` | **`{(service,origin): count}` PAIR-keyed** of applied `account.create` in `[0..K)`; consumer `n = sum(c for (s,o),c in st if s==S or o==O) + live` | flat `{service}/{origin}` under-counts the OR-predicate → cap reset → **FATAL false-clean**; pair-key is split-exact |\n| 5 | `mesh.authorized_devices` + `capability_map` | `{device_pubkey: authorized|revoked}` and `{host_id: cap}` latest-VERIFIED (owner-signed only) at `K-1`; snapshot MUST carry **every** key's state, consumer overlays live | dropped `revoked` re-authorizes — **critical** |\n| 6 | `promotion.is_promoted` | `{\"agent\\x00scope\": granted|revoked}` latest-verified at `K-1` | dropped `revoked` re-grants auto-approval |\n| 7 | `approvals.pending` | set of resolved `target_seq < K`; **assert** (referential floor guarantees no open pair straddles the boundary) | over-restrict (safe) |\n| 8 | `revise` archivist current-view | the non-superseded-at-`K-1` archivist records **with full fields** — `supersedes_id, subject, quote, statement, grounding, alpha, beta, source_seqs, verified_seqs, conflicting_seqs, resolved, due_iso, owner, promotion_key` — so `queries.py` open_threads/due_commitments/**pending_contradictions** reconstruct identically; **+** `grounded_keys`/`refused_keys` idempotency SET-union | dropped `conflicting_seqs`/`verified_seqs` silently strips actionable contradictions/citations with **no verify() signal** — heaviest, needs a dedicated `iter_current` equivalence test |\n| 9a | `gesture/session` device-arm `(device_pubkey, nonce)` **replay ledger** (HID-injection gate) | fold the consumed-nonce set **OR** assert-and-test `ARM_FRESHNESS << retention` (a prunable arm can never be replayed) | HID-injection replay — **critical**, do not leave implicit |\n| 9b | `governor/budget` per-UTC-day cap | assert-and-test retention always keeps the current UTC day live (no fold needed) | cap reset |\n\n`queries.py` open threads/commitments/contradictions are a VIEW over #8 → covered. **Supersession needs no separate fold** (`supersedes_id` is forward-only). Graph/dashboard totals route to Q5 (§8), not silently shrink.\n\n---\n\n## 7. Referential integrity + prune-point guards\n\nRead-only under the flock BEFORE any write; any failure ABORTS the whole prune (no bytes touched):\n\n- **(a) Dangling parent/supersedes:** scan retained `[K..T]`; if any `parent_id`/`supersedes_id < K` is UNRESOLVED (not collapsed by the archivist fold and not recoverable from the archive), refuse. A dangling ref into *closed* history is benign (`resolve()→PrunedRef`); an *open-workflow* one blocks.\n- **(b) Oldest-open-workflow floor:** `K ≤ min(seq)` over every open loop — `pending()` unresolved approval targets + their proposals, `queries` open threads/due commitments + cited `source_seqs`, unresolved contradictions, any operator plan/preview lacking its execute/verify. Every open workflow and everything it references stays live. This is what makes bearer #7 an assertion.\n- **(c) Never mutate a retained binding:** the re-base rewrites zero record bytes; `base_prev_hash` equals the already-stored `prev_hash` of seq `K`, so no `prev_hash`/`parent_id`/`supersedes_id`/`entry_hash` is ever altered.\n- **(d)** `K` aligns to a sealed-segment `first_seq` (whole segments only).\n\n---\n\n## 8. Q5 honest resolve\n\n`get(seq)` keeps its `SpineRecord | None` signature (hot path byte-identical); `None` is reserved for genuinely-never-existed (`seq < 0` or `seq > tip`). Add:\n```python\nresolve(seq) -> SpineRecord | PrunedRef\nPrunedRef = frozen{pruned=True, seq, base_seq, snapshot_seq, cumulative_merkle_root, archive_hint}\n```\n`seq >= base_seq` → live `get()`. `0 <= seq < base_seq` → if the archive is mounted, `get_archived(seq)` (via `archive.manifest` `first_seq/last_seq` range → owning segment → gunzip → return the byte-identical `SpineRecord`, verified against the segment chain **and** a Merkle inclusion proof against `cumulative_merkle_root`); else `PrunedRef`, never a bare `None`. Migrate the old-seq resolvers (`actor_gate`, `actor`, `operator`, `perception/egress`, `bridge /api/record`, `ui /api/record`, `mcp episodic_range`, `consolidate/gate.py`, `transcript` parent_id) to `resolve()` → HTTP `410 Gone` with `{base_seq, snapshot_seq, \"attach archive/ or run sigil archive fetch <seq>\"}`, never a misleading `404`. **`episodic_range` with `since_seq < base_seq` returns the live tail PLUS a `PrunedRef` boundary marker** — an honest range, never silent truncation. Graph rebuild / dashboard totals surface the \"compacted below N\" marker too.\n\n---\n\n## 9. Crash-safe cutover — singular commit, forward-only reconciler\n\nAll coordination under the path-stable flock; the O(file) archive copy is **lock-free** on immutable sealed segments.\n\n**PHASE A (durable, reversible; nothing observable changes — live head is still the prior head over the full set):**\n- **A0** Acquire flock. Choose `K` (segment-aligned). Run §7 guards. Compute `delta_merkle_root`, `cumulative_merkle_root`, and `folded_state = fold(load(prior), delta)`.\n- **A1** Write `prune.intent` (fsync): `{K, base_seq, base_prev_hash, base_count, snapshot_seq, archived_segment_ids, delta_merkle_root, cumulative_merkle_root}` — the durable idempotency driver, written **before** the snapshot append.\n- **A2** Append the `kind=\"snapshot\"` record (normal retain-all append → **fsync+ack**). Durable, nothing pruned yet.\n- **A3** *(lock-free)* Copy the archived segments → `archive/segments/`, append `archive.manifest.json`, fsync each + dir-fsync; re-hash-verify every `sha256` and that the archive re-attaches (`boundary_hash == base_prev_hash`). **The pruned prefix is a complete, verified genesis→K-1 chain in the archive before any live drop.**\n- **A4** Sign v2 head `H2` over the live window `[K..S]`; atomic-write to **`head.json.next` (staging — NOT live)**.\n\n**PHASE B (commit + idempotent reclaim; re-acquire flock):**\n- **B1** `os.replace(head.json.next → head.json)`. **← THE COMMIT INSTANT.** After it, both verify (by-seq window) and every fold (seed keyed to `head.snapshot_seq`, scan floored at `head.base_seq`) see the pruned state — regardless of manifest membership.\n- **B2** Advance `floor.json` upward (monotonic, meta-chain: `head_sig_hash := sha256(signing_bytes(H2))`).\n- **B3** Re-read the manifest **fresh under the held flock**; new live set = `fresh.segments − archived_segment_ids` (set-difference — safe because a concurrent seal-swap only ADDS segments); atomic manifest re-base (`generation++`), set `base_*` advisory fields.\n- **B4** Move the now-unreferenced archived originals to `trash/` (reader-grace) then unlink; dir-fsync. — physical reclaim.\n- **B5** Delete `prune.intent`.\n\n**Why B1 is the singular commit:** live-scan pruning and fold-seeding are BOTH gated on the signed head (`base_seq` floor + `snapshot_seq` key), not on manifest membership — so they flip together at B1. B3/B4 are pure reclamation. This closes the double-count window that sank HARD-PRUNE's crash score.\n\n**RECONCILER (at open AND on every flock acquisition):** if `prune.intent` exists, re-drive idempotently from on-disk state — if `head.json.next` is a valid v2 head matching the intent, forward-complete B1→B5; else (Phase-A crash, head still prior) discard `head.json.next` and the intent (the orphan snapshot record is inert because no committed head names its `snapshot_seq`). Orphan archived originals not in the re-based manifest are GC'd like the existing seal orphan-GC. Set derivation is **manifest-only, never a directory glob**. If the owner key is unavailable (read-only verifier), stay in fail-safe TAMPER and report \"prune in progress — run `sigil sign`\", never false-clean.\n\n**No-lost-ack proof:** the snapshot record is fsync-acked before commit; every pruned record is in the verified archive (A3) before the live manifest drops it (B3) and before any unlink (B4); a crash at any barrier leaves each acked record either still live (`pre-B3`) or in the archive (`post-A3`). **No-false-tamper proof:** by-seq selection makes `(prior head + full manifest)`, `(v2 head + full manifest)`, and `(v2 head + re-based manifest)` all verify clean; there is no reachable valid state that reads as TAMPERING. **No panic-block:** the archive copy is off-lock, so a kill-switch PANIC append is never blocked (D1). **Kill-9 crash-fuzz at each A/B barrier — asserting verify()-clean, no lost ack, no double-count (fold-equivalence, not just chain-clean), archive re-attaches — is a required Slice-E merge gate.**\n\n---\n\n## 10. Honest cold-verifier residual (C1)\n\nBecause the owner key is FS-resident on the host and NOT on the mirror, **a keyless mirror adversary cannot forge a prune that drops specific real records** — that needs the Ed25519 signature over `base_prev_hash/base_count/cumulative_merkle_root`. The only feasible keyless attack is **replay of a whole prior owner-signed `{spine, head, floor, snapshot}` tuple** — a genuine stale rollback, undetectable from data alone, fundamental to any prunable single-owner-key log, and **class-identical to today's single-file rollback — not widened**.\n\nCold-archive bounds it on two axes: **INTEGRITY** — a verifier handed the archive re-attaches `[0..K)+[K..]`, re-verifies from true genesis, and re-derives the Merkle accumulator from the real leaves (any single pruned record carries a Merkle inclusion proof); **FRESHNESS** — the durable external floor + `prev_head_hash` meta-chain, with the initial floor **and** the archive delivered over the authenticated WireGuard pairing channel, never the mirror. The exposure narrows to exactly one case: **a verifier that consumes only an untrusted mirror and never authenticates the floor even once** — for whom freshness (not integrity) is unprovable. Every other failure flags false-tamper. Do not overclaim: bounded, not eliminated. The archive is a **new hard availability dependency** for full-genesis verification (lose it → pruned prefix unrecoverable, only the commitment survives) — an explicit MAX-RECLAIM tradeoff to state to the operator.\n\n---\n\n## 11. Slice plan (empty-snapshot-first; head-v2 behind a flag day; crash-fuzz gate on the cutover)\n\n- **Slice A — head-v2 + verify seam (NO prune).** 6 defaulted fields + version-conditional `_head_payload`; `verify_chain`/`verify_head` `genesis_prev` param; `verify_head` base-aware; `classify_head` by-seq + left-edge pin + base-aware anti-truncation; `verify_checkpoint` try/except; `SpineTailer` schema-ahead tri-state + unkeyed `genesis_prev` wiring. `base_count=0` everywhere → byte-identical. **Gate:** real persisted v1 head verifies byte-identical; a synthetic `base_count=0` v2 head behaves as v1; old client fails closed to \"upgrade\", never \"un-notarized\". **Ship to the whole fleet + all mobile clients FIRST.**\n- **Slice B — `floor.json`** (outside `spine/`, monotonic, meta-chain), seeded from the current head (`base_count=0`), wired as reject rules. Still no prune. **Gate:** replayed old head / short-spine rejected; `--reset` cannot lower the floor.\n- **Slice C — census (C-0) + `SnapshotState` + all bearer folds** keyed strictly to `head.snapshot_seq`, filtered at `head.base_seq`; seed an EMPTY snapshot first. **Gate:** `verify()` and every classified bearer byte-identical to pure-scan; two-prune `fold-of-fold == full-scan`; per-bearer adversarial re-check (a dropped device-revoke / nonce-highwater / cap-count / device-arm nonce MUST be caught).\n- **Slice D — `kind=snapshot` writer + Merkle accumulator + §7 guards + archive COPY + `archive.manifest` + re-attach verify — but NO live drop** (archive is a redundant copy, live unchanged). **Gate:** archive round-trips and re-verifies full chain from genesis; prune ABORTS on a dangling binding or open workflow, no bytes touched.\n- **Slice E — the tamper-model-changing cutover:** staging-head commit (B1), floor advance, manifest re-base (fresh-read set-difference), reconciler, unlink, `resolve()`/`PrunedRef` + archive fetch, `--with-archive` full re-attach. **Required merge gate: kill-9 crash-fuzz at each A/B barrier (verify-clean, no lost ack, fold-equivalence, panic-never-blocked).** Owner-key-gated, fleet-upgrade-gated, explicit owner sign-off.\n\n**Ship Slices A–D freely; gate Slice E on the crash-fuzz + fold-equivalence harness and the flag-day fleet check. This tier is built ONLY on explicit operator sign-off — retain-all rotation already bounds per-file size and latency, so hard-prune's cold-verifier residual is a real tamper-model tradeoff, not a free win.**",
    "scoreboard": [
      {
        "name": "VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine",
        "avg": {
          "tamper_evidence": 4.8,
          "crash_safety": 4.5,
          "cold_verifier_exposure": 4.5,
          "consumer_correctness": 4,
          "implementability": 4
        },
        "fatal_flaws": []
      },
      {
        "name": "HARD-PRUNE (cold-archive, MAX-RECLAIM)",
        "avg": {
          "tamper_evidence": 4.3,
          "crash_safety": 3.5,
          "cold_verifier_exposure": 4.3,
          "consumer_correctness": 3,
          "implementability": 3.5
        },
        "fatal_flaws": [
          "Bearer 4 (creation-cap) fold is a FALSE-CLEAN defect as-specified: the flat {service_key|origin_key:n} representation cannot faithfully reconstruct creation_allowed's OR-predicate count over (service, origin), so a prune can reset the origin-based cap and a relabelled-origin replay bypasses the mass-account-creation guard — this violates the design's own 'never false-clean' invariant and MUST be fixed before any prune ships (fixable: store joint counts, or over-approximate by summing both dimensions so the cap only ever over-counts)."
        ]
      },
      {
        "name": "COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
        "avg": {
          "tamper_evidence": 4,
          "crash_safety": 4,
          "cold_verifier_exposure": 4.3,
          "consumer_correctness": 3.3,
          "implementability": 3.5
        },
        "fatal_flaws": []
      }
    ],
    "designs": [
      {
        "name": "VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine",
        "one_line": "Prune the oldest whole sealed segments from the live set behind an owner-signed, Merkle-committed kind=\"snapshot\" record, MOVE those segments to a cold archive (the recoverability proof, handed over the authenticated WireGuard channel), re-base the live manifest to a synthetic genesis-prev, and keep every invariant fail-safe — with head-schema v2's six fields added under a version-conditional signing payload so all existing v1 heads still verify byte-identically.",
        "snapshot_anchor": "A real spine record kind=\"snapshot\" (add \"snapshot\" to spine/models.py KINDS) is appended at the LIVE tip via the normal acked append path BEFORE any deletion, so it chains from the current tip and verify()/the chain are undisturbed at write time. Its payload commits exactly: base_seq=K (first RETAINED live seq; all seqs [0..K) are pruned), base_prev_hash=entry_hash(K-1) (the last pruned record's entry_hash = the synthetic genesis-prev the re-based live segment links to), base_count=K (absolute count of pruned records — equals base_seq for a contiguous log, both stored for robustness), merkle_root over the pruned leaves [entry_hash(0)..entry_hash(K-1)] (C2), snapshot_seq=this record's own seq (in the live window, > K, near the tip), prev_head_hash=the sha256 of the previously-accepted head's signing bytes (the meta-chain link), plus the folded_state block (see state_folding). Because it is a normal record its cert_digest (digest_payload over content, reuse/chain._entry_hash) binds the whole commitment into the hash chain, and the owner-signed head then notarizes it — so the merkle_root/base fields are tamper-evident even between `sigil sign` runs. The head's six v2 fields (below) are set to the SAME values, and classify_head cross-checks head.{base_seq,base_count,merkle_root} == the snapshot record at head.snapshot_seq, so the head cannot claim a different prune boundary than the immutable committed record. A verifier confirms the head descends from exactly one prior accepted head via the prev_head_hash meta-chain: head.prev_head_hash must equal floor.json's stored head_doc_hash (the single accepted parent); on accept, floor.head_doc_hash is advanced to sha256(evidence_signing_bytes(_head_payload(this head))). A linear meta-chain = exactly one parent, no forks.",
        "head_v2_migration": "SignedChainHead (reuse/models.py, extra=\"forbid\") gains six fields, all defaulted so a v1 head validates unchanged: base_seq:int=0, base_prev_hash:str=_GENESIS_PREV, base_count:int=0, merkle_root:str=\"\", snapshot_seq:int=-1, prev_head_hash:str=\"\". schema_version bumps to 2 only when a prune actually sets them. THE blocking gate: _head_payload (reuse/chain.py) becomes version-conditional — after model_dump(mode=\"json\") and popping \"signatures\", if head.schema_version < 2 it also pops the six new keys, so a v1-signed head's signing bytes are byte-identical to today and every persisted head.json still verifies; without this every deployment false-alarms to TAMPERING until the next `sigil sign`. sign_head grows a v2 path: entry_count = base_count + len(live_entries) (stays ABSOLUTE), head_hash/last_seq from the live tip, seeds the six fields from the snapshot record. verify_head derives genesis_prev = head.base_prev_hash (=_GENESIS_PREV for v1) and passes it to verify_chain; its count check becomes head.entry_count != head.base_count + len(entries) (v1: base_count=0 → identical). Harden verify_checkpoint (checkpoint.py:86) with try/except around model_validate_json + classify_head: a parse failure does a lenient json.loads to read schema_version — if it exceeds the known max, return a DISTINCT (False, \"schema too new — upgrade sigil\") state (fail-closed, never silent-un-notarized, never false-clean); a genuinely corrupt head returns (False, \"corrupt head\"). SpineTailer._resolve_head (tail.py:110) mirrors this: instead of swallowing every exception to (None,None) (\"un-notarized\"), it returns a third tri-state \"schema_ahead\" that poll()/check_anchor surface as \"upgrade required\", so a too-new head is never dressed up as a merely-unsigned tail. OLD mobile clients (v1 code) reading a v2 head hit extra=\"forbid\" ValidationError → they show un-notarized (SAFE: the tail is treated as not-proven, never false-clean) — which is exactly why the first prune is a fleet-upgrade flag-day gated on all verifiers running v2 code + explicit owner sign-off.",
        "verify_snapshot_aware": "verify_chain (reuse/chain.py:37) gains genesis_prev: str = _GENESIS_PREV and seeds prev = genesis_prev instead of the hard-coded constant; its i>0 contiguity check is unchanged (the first live record's seq=base_seq is never checked against 0, only successive-seq contiguity within the window), so a re-based live segment whose first record has prev_hash == base_prev_hash links cleanly instead of forking. store.verify() passes genesis_prev = manifest.base_prev_hash for the live-only view; a re-attached (archive+live) view passes _GENESIS_PREV (true genesis). classify_head (checkpoint.py:65) becomes snapshot-aware: base = head.base_count; live_expected = head.entry_count - base; n = len(entries) (the live window). The anti-truncation guard is REWRITTEN from `if n < signed_n` to `if n < live_expected: return TAMPERING (live window truncated)` — this is the exact fix for the no-op trap: entry_count stays ABSOLUTE, but the comparison subtracts base_count so a legit prune (n == live_expected) is clean while a real live-region truncation (n < live_expected) still fires. It then verify_head(head, entries[:live_expected], tr) (verify_head self-seeds genesis_prev=head.base_prev_hash), and cross-checks the snapshot record at head.snapshot_seq carries matching base_seq/base_count/merkle_root. The pruned-region truncation is caught by TWO independent guards: (a) floor.json monotonic {last_seq, base_count} + prev_head_hash meta-chain rejects any head that lowers last_seq or base_count or doesn't descend from the accepted head; (b) merkle_root binds the pruned leaves, so a cold verifier holding the archive re-derives base_count from the actual archived record count and checks merkle_root == head.merkle_root. entry_count NEVER decreases and is never renumbered.",
        "durable_floor": "~/.sigil/floor.json (SIGIL_HOME/floor.json, 0600, OUTSIDE spine/ so SpineStore.reset()/`sigil ingest --reset` — which only rmtree's spine-dir artifacts, store.py:414 — cannot delete it). JSON {schema_version, scope, last_seq, base_seq, base_count, head_doc_hash, updated_ts}. Written only atomic (atomicio.atomic_write_text) and only AFTER a head is signed+verified. MONOTONIC: a write is refused if last_seq < current.last_seq or base_count < current.base_count. Reject rules, evaluated inside classify_head/verify_checkpoint (and SpineTailer.check_anchor) after loading floor: reject (→ TAMPERING/fail-closed) any head with head.last_seq < floor.last_seq (rollback), head.base_count < floor.base_count (pruned-region rollback / un-prune claiming fewer deletions), or head.prev_head_hash != floor.head_doc_hash (meta-chain break — not the single accepted descendant). prev_head_hash meta-chain: each v2 head commits prev_head_hash = floor.head_doc_hash at signing; verifier checks it; on accept floor.head_doc_hash = sha256(evidence_signing_bytes(_head_payload(head))). A rebuild that legitimately shortens the spine (`--reset`) must NOT silently defeat the floor — that requires a separate explicit owner-key-gated `sigil floor reset`, never the routine reset. HONEST LIMIT (C1): a cold verifier bootstrapping off an UNTRUSTED mirror gets {spine, head, floor.json, snapshot record} all from the mirror, and a filesystem-write adversary can present a self-consistent OLD tuple rolled back together (floor.json is just a file the mirror controls, and the owner key is FS-resident per checkpoint.py:9) — this full-tuple rollback is unclosable from data alone, fundamental to any prunable log. COLD-ARCHIVE bounds it: the archive (the full genesis..K-1 chain) and the initial floor are seeded over the AUTHENTICATED WireGuard device-pairing channel, never a mirror; with the archive the cold verifier re-derives base_count from actual archived records, checks merkle_root, and verifies from true genesis. Warm/same-host/returning-paired verifiers are fully closed by the persisted floor + SpineTailer._high_water (tail.py:60,90-93).",
        "cold_archive_move": "Which moves: only WHOLE sealed segments (never the active, never a partial segment), strictly oldest-first, whose last_seq < K, keeping the newest N live segments (config SIGIL_PRUNE_KEEP_SEGMENTS) or those younger than a retention window — and only up to the referential floor (see referential_integrity). MOVE = copy→verify→drop-from-live, so it is additive-then-atomic, never a destructive rename first. Archive layout (default ~/.sigil/spine/archive/, configurable to an offline mount SIGIL_ARCHIVE_DIR): archive/segments/seg-*.jsonl.gz (the moved segments, still gzip, byte-identical) + archive/archive.manifest.json — an ordered list of archived segments {id, file, codec, first_seq, last_seq, first_prev_hash, boundary_hash, sha256, count} plus a header {merkle_root, base_seq, base_count} matching the snapshot record. The archive is itself a genesis-rooted chain segment set: seg-0.first_prev_hash=_GENESIS_PREV, each boundary_hash==next first_prev_hash, last boundary_hash==base_prev_hash. LIVE manifest re-base: the Manifest model (spine/manifest.py) gains reserved-null header fields base_seq/base_prev_hash/base_count/snapshot_seq/merkle_root; the re-based manifest (generation++) drops the archived segment entries, sets those header fields, and the oldest REMAINING live segment keeps its existing first_prev_hash (which already equals base_prev_hash == the archive's last boundary_hash — a rename/copy is byte-preserving, so NO segment file is rewritten; the re-base is pure manifest metadata). Reader re-attach: point a SpineStore (or a `sigil archive verify` view) at the union [archive/segments... ++ live segments...]; iter_records concatenates archive-then-live in seq order, and store.verify(genesis_prev=_GENESIS_PREV) over the whole set re-verifies the FULL chain from genesis — the archive→live seam matches because base_prev_hash == archive boundary_hash. Archive integrity is the per-segment sha256 in archive.manifest.json; the owner keeps >=1 copy (losing the archive makes the pruned prefix unrecoverable, though merkle_root still proves what WAS there).",
        "state_folding": "A new SnapshotState.load(store) reads the kind=\"snapshot\" record at head.snapshot_seq (or the newest snapshot record) and returns its folded_state; every monotonic-state consumer switches from a pure full-scan to `state = SnapshotState.load(store); fold state over store.iter_records()` (which now yields only live records). Empty snapshot (base_count=0, empty folds) is seeded FIRST so the fold path is proven == pure-scan before any deletion. The nine bearers and exactly what each folds from the pruned range [0..K): (1) NONCE HIGH-WATER (bridge/envelope.py device_nonce_highwater): {device_pubkey: max_nonce} — consumer starts hi from folded[device] then max()es live receipts; losing it would let an old nonce replay, so max-fold is mandatory and safe. (2) WARDEN CHECKPOINT (cli.py cmd_warden_anchor_get): {pubkey: [best_count, best_head_hash]} highest anchored count per key — consumer continues the >= scan from the fold; anti-rollback high-water. (3) ACCOUNT.CREATE CAP (agents/actor_scope.py creation_allowed): {by_service:{service:n}, by_origin:{origin:n}} counts of applied account.create steps — consumer ADDS live counts before comparing creation_cap; losing it resets the cap (mass-creation). (4) DEVICE AUTHORIZE/REVOKE + CAPABILITY (mesh/registry.py authorized_devices/capability_map): {device_pubkey: \"authorized\"|\"revoked\"} and {host_id: capability} — the latest VERIFIED state per key from the pruned range (only owner-signed records folded); consumer overlays live latest-state on top; losing a \"revoked\" that was latest in-pruned re-authorizes a revoked device. (5) KILL-SWITCH LATCH (governor/killswitch.py _scan_engaged): engaged:bool = the running latch state at the K-1 boundary (any engage→True, only owner-signed-verified release→False, in order); consumer continues folding live; dropping a release keeps it engaged (fail-safe halt). (6) PROMOTION GRANTS (governor/promotion.py is_promoted): {[agent,scope]: \"granted\"|\"revoked\"} latest verified per key; consumer overlays live; losing a \"revoked\" re-grants auto-approval. (7) QUEUED APPROVALS (agents/approvals.py pending): resolved_target_seqs:set of verified-approval targets in the pruned range; consumer unions live; the referential floor guarantees no OPEN (still-pending) item's pair straddles the boundary, so this only ever carries fully-resolved pairs. (8) ARCHIVIST PROMOTION-KEY LEDGER (consolidate/revise.py promotion_ledgers): {grounded:[keys], refused:[keys]} idempotency keys — consumer unions live; prevents re-promoting an already-grounded fact. (9) OPEN THREADS/COMMITMENTS/CONTRADICTIONS + CITATIONS (consolidate/queries.py): NOT folded — protected instead by the oldest-open-workflow floor (their source/current records are simply never pruned); a live grounded fact's source_seqs pointing into the pruned range degrade to PrunedRef via resolve() while the verbatim quote (self-contained in the grounded record's own payload) still serves. NOTE on supersession (revise.py iter_current): needs NO fold — supersedes_id always points BACKWARD (superseder seq > superseded seq), so a pruned superseded record's superseder is either also pruned or a live record that already excludes it; no double-serve.",
        "referential_integrity": "Three prune-point guards computed under the flock before choosing K. (a) OLDEST-OPEN-WORKFLOW FLOOR: K <= min seq over every open loop — pending() unresolved approval targets and their queued proposals (approvals.py), current grounded open_threads/due_commitments and their cited source_seqs (queries.py), unresolved contradictions, and any operator plan/preview without its execute/verify record (agents/operator.py supersedes chains). An open workflow and everything it references stays live. (b) DANGLING BINDING GUARD: refuse to advance K past a boundary where a RETAINED record's parent_id or supersedes_id points into [0..K) AND that reference is part of an unresolved workflow; a dangling parent_id/supersedes_id whose target is closed history is BENIGN (resolve()→PrunedRef, threading/current-view unaffected because supersession is forward-only and the quote is embedded), but an unresolved one blocks the prune. (c) NEVER MUTATE A RETAINED BINDING: the re-base rewrites NO segment bytes — the oldest live segment's first record keeps its stored prev_hash (already == base_prev_hash); the re-base is manifest-header metadata only, so no retained record's prev_hash/parent_id/supersedes_id/entry_hash is ever altered (which would break the chain or forge a link). Only whole sealed segments strictly below the referential floor and below any monotonic-bearer live high-water are eligible.",
        "q5_resolve": "Add SpineStore.resolve(seq) -> SpineRecord | PrunedRef (get() keeps its SpineRecord|None signature for the byte-identical common path). PrunedRef is a small frozen dataclass {pruned=True, seq, base_seq, snapshot_seq, merkle_root, archive_hint}. Contract: seq >= base_seq → live get() (SpineRecord); 0 <= seq < base_seq → if the archive is mounted/reachable, fetch it (archive.manifest.json maps the seq into its owning archived segment → decompress the gz → return the SpineRecord, exactly the iter path but over archive/segments); else return PrunedRef (never a bare None). None stays reserved for a genuinely-never-existed seq (seq < 0 or seq > tip). The ~9 old-seq resolver call sites where \"pruned vs absent\" matters (agents/actor_gate.py:22, agents/actor.py:192, agents/operator.py, perception/egress, bridge/server /api/record, ui/server /api/record, mcp episodic_range, consolidate/gate.py grounding, ingest/transcript.py parent threading) migrate to resolve(); the referential floor guarantees no OPEN workflow's resolve() ever lands in the pruned range, so they hit PrunedRef only for closed history. HTTP consumers map PrunedRef → 410 Gone with the {base_seq, snapshot_seq, \"attach archive/ or run `sigil archive fetch <seq>`\"} pointer, never a misleading 404.",
        "crash_safe_cutover": "All under the cross-process flock (_crossproc_lock) except the heavy archive copy which runs off-lock on immutable sealed segments. Ordering: (1) choose K from the three referential/bearer floors; (2) scan the to-be-pruned sealed segments (immutable) to compute merkle_root over [entry_hash(0)..entry_hash(K-1)] and the folded_state; (3) APPEND the kind=\"snapshot\" record (normal append → fsync+ack, store.py:696) carrying base_seq/base_prev_hash/base_count/merkle_root/snapshot_seq(self)/prev_head_hash/folded_state — durable, but nothing pruned yet; (4) COPY the archived segments to archive/segments + write/extend archive.manifest.json, fsync each; (5) VERIFY every archived sha256 and that the archive re-attaches (archive boundary_hash == base_prev_hash) — the pruned prefix must be a complete, verified genesis..K-1 chain in the archive BEFORE any live drop; (6) atomic LIVE manifest re-base (write_manifest, generation++) dropping the archived segments and setting the base_* header — physical live reclaim; (7) sign the v2 head (entry_count=base_count+live, six fields from the snapshot record) and atomic-write head.json — THE COMMIT INSTANT; (8) advance floor.json (monotonic, meta-chain) to the new head. THE COMMIT INSTANT is the os.replace of head.json in step 7: before it, the live manifest may already be re-based (step 6) while head.json is still v1 anchoring the pre-prune absolute count → verify_checkpoint reports FALSE-TAMPER (n < live_expected with base_count=0) — the SAFE direction, never false-clean. RECONCILER (runs at store open and on the next flock acquisition): if the manifest has base_count>0 (re-based) but head.json is v1 or head.entry_count != base_count+live, an interrupted prune is detected; it deterministically FORWARD-completes by re-deriving the v2 head from the durable snapshot record (which committed base_seq/base_prev_hash/base_count/merkle_root/folded_state) + the live entries, re-signs (owner key is FS-resident), atomic-writes head.json, then advances floor. If the owner key is unavailable (a read-only verifier), it stays in the fail-safe TAMPER state and reports \"prune in progress — run `sigil sign`\", never false-clean. NO-LOST-ACK PROOF: the snapshot record is fsync'd before any deletion (step 3), and every pruned record exists in the verified archive (steps 4-5) before the live manifest drops it (step 6); a crash at any barrier leaves each acked record either still in the live spine (pre-6) or in the archive (post-4). NO-FALSE-CLEAN / NO-STUCK-FALSE-TAMPER: the new v2 head satisfies the OLD floor (last_seq grew via the snapshot append, base_count grew from 0, prev_head_hash==old head_doc_hash), so a crash between step 7 and step 8 reopens consistent (verify passes against the old floor, floor then re-advances); a crash before step 7 reopens as fail-safe TAMPER and the reconciler completes it. Kill-9 crash-fuzz at each barrier (mirroring the existing _maybe_crash harness, store.py:55) is a required merge gate.",
        "c1_honest_limit": "The residual cold-verifier exposure: a filesystem-write adversary who controls an untrusted MIRROR can hand a cold verifier a fully self-consistent OLD tuple — {short spine, matching v2 head re-signed with the FS-resident owner key, a rolled-back floor.json, a matching snapshot record} — all rolled back together. From DATA ALONE this full-tuple rollback is undetectable, because floor.json is a mirror-controlled file and the owner signing key lives on the runtime host (checkpoint.py:9, an accepted §1.3 simplification); this is fundamental to any prunable log and is NOT widened beyond the pre-existing single-file rollback exposure for the pruned region — it is the same class of attack. COLD-ARCHIVE BOUNDS it two ways. First, the archive plus the initial floor are transferred over the AUTHENTICATED WireGuard device-pairing channel (the phone holds its own device key; mesh authorize is owner-minted), never from the mirror — so a returning paired/warm verifier has an authenticated floor high-water that a mirror rollback cannot lower. Second, when the archive is handed over, the cold verifier re-attaches [archive ++ live], re-derives base_count from the ACTUAL archived record count, recomputes merkle_root over the real pruned leaves and checks it == head.merkle_root, and re-verifies the entire chain from true genesis (base_prev_hash == the archive's last boundary_hash) — collapsing the pruned-region blind spot to zero for any verifier holding both the archive and an authenticated-channel floor seed. The only irreducible residual is a cold verifier with the mirror alone (no archive, no authenticated floor), which is exactly the case the owner is told not to trust.",
        "slice_plan": [
          "Slice A — Head v2 schema + version-conditional _head_payload + verify_chain genesis_prev param + classify_head base_count-aware anti-truncation + verify_head count/seed + verify_checkpoint/SpineTailer try/except with the distinct schema-too-new state. NO pruning. base_count=0 everywhere → byte-identical to today; merge gate: the real persisted head.json verifies byte-identical, and a synthetic base_count=0 v2 head behaves exactly as v1.",
          "Slice B — Durable floor.json (SIGIL_HOME/floor.json 0600, monotonic {last_seq,base_seq,base_count,head_doc_hash}, prev_head_hash meta-chain), seeded from the CURRENT head at first run (base_count=0), wired into classify_head/verify_checkpoint/SpineTailer as reject rules. Still no pruning; exercises rollback/meta-chain reject paths and the reset-cannot-delete-floor property.",
          "Slice C — SnapshotState + the nine bearer fold contracts + SnapshotState.load(store) + switch each consumer to load-then-fold, seeding an EMPTY snapshot (base_count=0, empty folds) FIRST so every seed path runs and is proven byte-identical to the pure full-scan BEFORE any record is deleted; per-bearer adversarial re-check (a dropped device-revoke / nonce-highwater / cap-count must be caught).",
          "Slice D — kind=snapshot record writer + merkle_root computation + the three referential-integrity guards (oldest-open-workflow floor, dangling-binding, never-mutate-retained) + archive COPY + archive.manifest.json + re-attach verify — but STILL no live drop (archive is a redundant copy, live unchanged). Proves the archive round-trips and re-verifies the full chain from genesis.",
          "Slice E — the tamper-model-changing cutover: atomic live-manifest re-base (drops archived segments) + v2 head sign (commit instant) + floor advance + reconciler + resolve()/PrunedRef + archive fetch path. Kill-9 crash-fuzz at each barrier is the required merge gate. Owner-key-gated, fleet-upgrade-gated (all verifiers on Slice-A code), explicit owner sign-off."
        ],
        "invariants_at_risk": [
          "Append-only / no-lost-ack: records are physically removed for the first time (mitigated by snapshot-durable-before-drop + verified-archive-before-drop).",
          "entry_count-absolute anti-rollback: the classify_head `n < entry_count` guard must become `n < entry_count - base_count` or it collapses into a no-op that can't distinguish a legit prune from a live truncation.",
          "Genesis-rooted chain: the re-based live segment's first record links to base_prev_hash, not _GENESIS_PREV, and forks unless verify_chain is seeded via genesis_prev.",
          "v1 head byte-identical verification: adding six model fields breaks every persisted head's signing bytes unless _head_payload is version-conditional.",
          "Nine monotonic security-state high-waters (nonce replay floor, WARDEN count, account-create cap, device authorize/revoke, kill-switch latch, promotion grants, approval resolution, promotion-key idempotency): each silently regresses if its pruned-range contribution is not folded.",
          "Referential integrity: dangling parent_id/supersedes_id into the pruned range and the oldest-open-workflow floor.",
          "Crash-atomicity across five artifacts (snapshot record, archive, live manifest, head.json, floor.json) with a single well-defined commit instant.",
          "Fail-safe direction: every residual must be false-TAMPER, never false-clean.",
          "Cold-verifier bound: the archive + floor must arrive over the authenticated channel, never a mirror."
        ],
        "risks": [
          "Fleet flag-day: an un-upgraded (v1-code) verifier — notably an old mobile client — against a v2 head shows un-notarized (safe but degraded); the first prune must be gated on all verifiers running Slice-A code plus explicit owner sign-off.",
          "Owner key is FS-resident (checkpoint.py:9), so floor.json gives no protection against a same-host FS-write adversary who also rewrites floor.json and re-signs — a pre-existing limit for the warm case, but the prune adds the merkle/archive attack surface for the pruned region.",
          "The cold archive becomes a hard availability dependency for full-genesis verification: losing the offline archive makes the pruned prefix unrecoverable (only the merkle_root commitment survives); mitigated by per-segment sha256 in the archive manifest and the owner keeping >=1 copy.",
          "Fold-logic bugs are silent SECURITY regressions (a dropped device-revoke re-authorizes a revoked device, a dropped nonce-highwater permits replay); mitigated by the Slice-C empty-snapshot fold==scan byte-identical proof and per-bearer adversarial re-check before any deletion.",
          "The reconciler re-signs on open with the owner key; a read-only verifier that opens an interrupted prune stays in fail-safe TAMPER until `sigil sign` — noisy but never false-clean.",
          "resolve()/PrunedRef changes the read-contract at ~9 call sites; a caller that still expects None-means-absent could misread a PrunedRef — mitigated by keeping get() unchanged and migrating only the sites where pruned-vs-absent matters.",
          "Merkle-over-millions-of-leaves and the archive copy are one-time O(pruned) work under partial lock windows; bounded to whole-sealed-segments and run oldest-first with the heavy copy off-lock."
        ]
      },
      {
        "name": "HARD-PRUNE (cold-archive, MAX-RECLAIM)",
        "one_line": "Prune the oldest sealed segments out of the LIVE spine behind an owner-signed, Merkle-committed kind=\"snapshot\" anchor + v2 head, MOVE the pruned gz segments to a re-verifiable archive/ (the recoverability + genesis-verify proof), re-base the live manifest/verify to the snapshot, and fold all ~9 monotonic bearers into the snapshot so live-only scans stay correct — every crash window and every attack resolves in the false-tamper (never false-clean) direction.",
        "snapshot_anchor": "A kind=\"snapshot\" spine record (add to KINDS in spine/models.py) is appended to the LIVE active segment like any record (digest_payload over content, real entry_hash, fsynced+acked) so it is itself chained and covered by verify()/the head. Its payload commits, over the pruned prefix [0..K): base_seq=K (first RETAINED seq); base_prev_hash=entry_hash(K-1) (the last pruned record's hash — the synthetic genesis-prev the re-based live set links from; for a contiguous genesis chain base_count==base_seq==K, cross-checked as a guard); base_count=K (absolute count of pruned records); merkle_root=Merkle root over the leaves [entry_hash(0)..entry_hash(K-1)] (leaf=entry_hash, binary tree, duplicate-last on odd — C2); snapshot_seq=its own seq S (K<=S); prev_head_hash=sha256_hex(evidence_signing_bytes(_head_payload(prior_accepted_head))) (the meta-chain link); plus the folded SnapshotState (state_folding). These same six commit fields are ALSO copied onto SignedChainHead v2 so verify never has to trust the record's own payload — the head (owner-signed) is authority, the record is the durable, replayable INTENT. Descent from exactly one prior head: on accept, require the new head's prev_head_hash == sha256 of the currently-accepted head stored in floor.json/head.json; combined with floor monotonicity this makes the head lineage a hash chain with exactly one predecessor (no fork, no skip, no replay). The meta-chain is verified in classify_head/verify_checkpoint against floor.prev_head_hash and by re-hashing the persisted predecessor head.",
        "head_v2_migration": "SignedChainHead (reuse/models.py) keeps extra=\"forbid\" and gains six defaulted fields: base_seq:int=0, base_prev_hash:str=_GENESIS_PREV, base_count:int=0, merkle_root:str=\"\", snapshot_seq:int=-1, prev_head_hash:str=\"\" (and schema_version bumps 1->2 only when a snapshot is committed; entry_count STAYS ABSOLUTE = base_count + live). The blocking gate is version-conditional signing payload: _head_payload (chain.py:50) must DROP the six fields when schema_version<2 — `if d.get(\"schema_version\",1)<2: for f in (base_seq,base_prev_hash,base_count,merkle_root,snapshot_seq,prev_head_hash): d.pop(f,None)` — so an existing v1-signed head re-dumped under the v2 model produces byte-identical signing bytes and verifies unchanged (do NOT use model_dump(exclude_unset=True): model_validate_json marks every field 'set', so it would re-include the defaults and false-alarm every deployment to TAMPERING). sign_head and verify_head both route through _head_payload so v1/v2 stay symmetric. Harden verify_checkpoint (checkpoint.py:86 has no try/except): wrap model_validate_json + classify_head in try/except returning (False,\"anchor unverifiable / schema too new — upgrade\") — a v2 head fed to a v1-only client raises pydantic ValidationError (extra fields forbidden) and today crashes it. Harden SpineTailer._resolve_head (tail.py:110-119, which swallows ALL exceptions to (None,None) = \"un-notarized\", a false-CLEAN degrade): peek schema_version first; if it exceeds the client's max, return a DISTINCT \"schema too new — upgrade\" sentinel that check_anchor surfaces as anchor_ok=False + upgrade_required=True (never the benign \"un-notarized live tail\"). Old mobile clients vs a v2 head: MUST fail closed to \"upgrade required\", never \"clean/un-notarized\"; and the first prune (first v2 head emission) is a fleet-upgrade FLAG DAY — gate it behind every mesh device advertising max_head_schema>=2 (piggyback on mesh/registry capability_map) so no paired client is left unable to verify.",
        "verify_snapshot_aware": "verify_chain (chain.py:37) gains genesis_prev:str=_GENESIS_PREV and seeds prev=genesis_prev instead of the hardcoded _GENESIS_PREV; all v1 callers pass the default => byte-identical. verify_head gains the same passthrough. classify_head (checkpoint.py:65) becomes snapshot-aware: it selects the LIVE WINDOW BY SEQ, not by array-prefix — live=[e for e in entries if e.seq>=head.base_seq] — and computes expected_live=head.entry_count-head.base_count. THE anti-truncation guard that survives pruning: `if len(live) < expected_live: return TAMPERING (truncated/rolled back)` — this is NOT the no-op `n<entry_count` (which post-prune is always true => false-tamper) and NOT a disabled check (which hides real truncation); it compares the live count against the SIGNED expected_live. Then require live and live[0].seq==head.base_seq and live[0].prev_hash==head.base_prev_hash (a front-truncation makes min live-seq>base_seq or breaks the prev-linkage => TAMPERING), and verify_head(head, live[:expected_live], tr, genesis_prev=head.base_prev_hash) (a tail-truncation makes head_hash!=last entry_hash; a middle deletion breaks verify_chain contiguity). entry_count stays ABSOLUTE so a rotated/pruned store still exposes the true physical height. Selecting by seq (not prefix) is what makes every crash cutover window verify cleanly (see crash_safe_cutover). n>expected_live stays the benign \"appended since — run sigil sign\" branch.",
        "durable_floor": "~/.sigil/floor.json (0600), located at SIGIL_HOME/floor.json — OUTSIDE spine/ so SpineStore.reset()/`sigil ingest --reset` (which rmtrees spine/ + segments) can NEVER lower the anti-rollback floor. Contents: {schema_version, last_seq, base_seq, base_count, head_hash, prev_head_hash, snapshot_seq, merkle_root}. Update is MONOTONIC and upward-only, via atomic_write_text, and ONLY after a new head+manifest have committed: refuse to write a floor whose last_seq<old, base_count<old.base_count, or base_seq<old.base_seq. Reject rules (enforced in classify_head/verify_checkpoint and mirrored into SpineTailer._high_water): reject any head/snapshot with last_seq<floor.last_seq (rollback) OR base_count<floor.base_count OR base_seq<floor.base_seq (un-prune/older-snapshot replay), and require head.prev_head_hash==sha256(evidence_signing_bytes(_head_payload(the head floor names))) — the prev_head_hash meta-chain, so a new head must descend from exactly the last accepted head. Honest limit: a cold verifier bootstrapping off an UNTRUSTED MIRROR has no local floor and can be handed an internally-consistent (v2 head + live + archive) triple that is actually an OLD owner-signed state (a full rollback to a prior snapshot) — this is unclosable from data alone for ANY prunable log. Bounded by cold-archive: the archive is handed over the AUTHENTICATED WireGuard device-pairing channel and the initial floor {last_seq,base_count,head_hash,prev_head_hash} is SEEDED over that same channel, never from the mirror; given that one authenticated seed the verifier rejects every rollback below it, and the archive independently proves genesis->tip INTEGRITY. Residual = only the freshness of a verifier that NEVER authenticates the floor even once.",
        "cold_archive_move": "MAX-RECLAIM retention: keep only the last N=1 sealed segment + the active segment LIVE (config SIGIL_SPINE_LIVE_SEALED, default 1); everything older moves. Only WHOLE sealed segments move (never split one), so K = first_seq of the oldest RETAINED sealed segment (a clean seam boundary => base_prev_hash = that segment's first_prev_hash = the moved segment's boundary_hash, no record is ever rewritten). Archive layout: ARCHIVE_DIR = SIGIL_HOME/spine/archive/ (relocatable to an external/offline mount via SIGIL_SPINE_ARCHIVE_DIR); moved segments stay gzip (already compressed); an APPEND-ONLY archive/archive.manifest.json accumulates across successive prunes (never destructively rewritten) with one entry per archived segment {id, file, first_seq, last_seq, first_prev_hash, boundary_hash, sha256} plus one entry per snapshot {snapshot_seq, base_seq, base_count, merkle_root, prev_head_hash, head_hash}. Live manifest re-base: after the segments are COPIED into archive/ and the archive manifest fsynced, atomic_write_text a new live Manifest (generation++) that drops the archived segments, keeps [K..tip], sets the oldest retained segment's first_prev_hash=base_prev_hash, and records base_seq/base_prev_hash/base_count/snapshot_seq/merkle_root as advisory manifest fields (authority stays the signed head). Re-attach for FULL genesis verify: `sigil spine verify --with-archive <dir>` concatenates archive segments in seq order (from archive.manifest) THEN the live segments -> one genesis-rooted stream -> verify_chain(all, genesis_prev=_GENESIS_PREV); cross-checks that the last archived boundary_hash == live base_prev_hash and that the Merkle root recomputed over the archived entry_hashes == every snapshot's committed merkle_root. So integrity is provable from genesis for anyone handed the archive.",
        "state_folding": "New SnapshotState (typed) is serialized into the snapshot record payload and loaded by SnapshotState.load(store) (reads the newest kind=\"snapshot\" record; empty/zero default when none). Every ~9 full-scan consumer changes from an empty accumulator to a SEEDED one — `st=SnapshotState.load(store)` then fold over store.iter_records() (which now yields only LIVE records). Exact per-bearer fold over pruned [0..K): (1) envelope.device_nonce_highwater -> nonce_highwater:{device_pubkey:max_nonce}; consumer seeds hi=st.nonce_highwater.get(device,-1) (was -1). (2) governor/killswitch._scan_engaged -> killswitch_engaged:bool = the verified latch state at K-1 (engage=any, release=owner-signed-verified); consumer seeds engaged=st.killswitch_engaged (was False). (3) mesh/registry.authorized_devices -> authorized_devices:{device_pubkey:\"authorized\"|\"revoked\"} latest-verified at K-1, AND capability_map -> host_caps:{host_id:{...}} latest-verified; consumers seed the state dict from st then override with live. (4) agents/actor_scope.creation_allowed -> account_create_counts:{service_key|origin_key:n} count of status=\"applied\" account.create in the pruned range; consumer n=st.account_create_counts.get(key,0)+live count. (5) governor/promotion.is_promoted -> promotions:{\"agent\\x00scope\":\"granted\"|\"revoked\"} latest-verified at K-1; consumer seeds state from st. (6) agents/approvals.pending -> resolved_targets:[seq...] (verified approvals whose target_seq<K) and open_baseline:[] (empty by the referential-integrity floor, asserted); consumer seeds resolved=set(st.resolved_targets). (7) consolidate/revise.iter_current + promotion_ledgers -> current_archivist:[{seq,entry_hash,kind,ts,supersedes_id,subject,quote,statement,grounding,alpha,beta,source_seqs,due_iso,owner,promotion_key}] = the pruned archivist records still NON-SUPERSEDED at K-1 (they must keep appearing, with citations intact), plus grounded_keys:[...] and refused_keys:[...] for idempotency; consumer seeds the current list + ledgers from st then folds live supersessions. (8) consolidate/queries open_threads/due_commitments/contradictions are a VIEW over iter_current -> automatically covered by (7), no separate fold. (9) warden_checkpoint (cli.py:284, dashboard.py:29) -> warden_checkpoint:{latest record fields per pubkey} = the newest warden_checkpoint in the pruned range (monotonic anti-rollback cross-anchor); consumer newest=max(st.warden_checkpoint, live). SnapshotState.load also exposes base_seq/base_count so consumers know the live floor.",
        "referential_integrity": "Three prune-point guards run under the flock BEFORE anything moves; failing any ABORTS the prune (no bytes touched). (a) Dangling parent/supersedes: scan the retained [K..tip] records for any parent_id or supersedes_id < K; each must resolve — either it names a pruned record still projected in SnapshotState.current_archivist (an archivist supersession the fold already collapses) or it is resolvable from the archive (cold-archive keeps it a PrunedRef, so consumers like consolidate/gate.py:93 store.get(s) and transcript parent_id derefs still work). Refuse if a retained record has an UNRESOLVED dangling binding into the pruned range that is neither folded nor archived. (b) Oldest-open-workflow floor: K <= min(seq of every currently-open workflow) — computed as min over pending() queued-but-unresolved approval seqs and any open thread/commitment source seq — so no open workflow's originating record is ever pruned; this is what guarantees approvals fold (6) has zero items crossing the boundary. (c) Never mutate a retained record's binding: the re-base rewrites NO record bytes — base_prev_hash equals the ALREADY-STORED prev_hash of the first retained record (it was written that way originally at seq K), so verify links from base_prev_hash without any edit to prev_hash/parent_id/supersedes_id; the manifest re-base and head re-sign are the only writes.",
        "q5_resolve": "store.get(seq) keeps returning None ONLY for seq beyond the tip (genuinely nonexistent). For a pruned seq (0<=seq<base_seq, from the head/manifest base fields) add resolve(seq) -> SpineRecord | PrunedRef where PrunedRef={pruned:True, seq, base_seq, snapshot_seq, archive_hint} — never a bare None/404. The ~9 old-seq resolvers (actor_gate, actor, operator, perception/egress, bridge/server /api/record, ui/server /api/record, mcp episodic_range, consolidate/gate.py:93, transcript parent_id) map PrunedRef to a 410 Gone \"compacted below N — see snapshot X / archive\" instead of a 404. Archive fetch path: resolve(seq, with_archive=True) (or get_archived(seq)) reads archive/archive.manifest.json -> the owning archived segment by first_seq/last_seq range -> gzip-decompress -> returns the byte-identical SpineRecord whose entry_hash is independently checkable against the snapshot's merkle_root (a Merkle inclusion proof over the archived leaves), so a drilled-in citation is still cryptographically verifiable even though it left the live set.",
        "crash_safe_cutover": "All under the path-stable flock. PHASE A (durable, reversible intent): compute K + run the three referential guards; compute merkle_root and the full SnapshotState fold; append the kind=\"snapshot\" record to the live active (fsync+ack); COPY the to-be-archived gz segments into archive/ + append the archive manifest (fsync + dir-fsync); write the new v2 head to a STAGING path head.json.next (not yet live). Nothing observable changed — live manifest still full, live head still the valid v1 head over the full set, verify passes. A crash in Phase A leaves a harmless extra snapshot record + idempotent archive copies + a staging head; the reconciler discards the staging head and treats an uncommitted prune as NO prune. PHASE B (commit): step 1 os.replace(head.json.next -> head.json) publishes the v2 head — THE COMMIT INSTANT (after it the store is logically pruned: verify selects the live window BY SEQ>=base_seq and seeds genesis_prev=base_prev_hash); step 2 atomic manifest re-base (generation++) drops the archived segments; step 3 atomic monotonic floor.json update; step 4 (last) unlink the archived segments from live segments/ — reclaim. The manifest re-base + unlink are pure space reclamation the reconciler finishes; even if they never run, verify is already correct off the v2 head. RECONCILER (at open AND on every flock acquire): if head.schema_version==2 and head.snapshot_seq set and manifest.base_count!=head.base_count -> deterministically re-derive+re-base the manifest from the snapshot record and finish unlink+floor (forward recovery); if a snapshot record exists but the live head is still v1 (Phase A only) -> leave it (uncommitted prune = no prune). NO-LOST-ACK proof: every acked record is in the live set [K..tip] OR was fsync-copied to archive before any live deletion; the snapshot record is acked before commit; live [0..K) segments are unlinked only after archive copies are durable AND the manifest re-based — so no acked record is ever unreferenced. NO-FALSE-TAMPER proof: because classify_head selects the live window by SEQ (not prefix) and seeds genesis_prev=base_prev_hash from the SIGNED head, every crash window verifies cleanly — (v1 head + full manifest) is the consistent old state; (v2 head + full manifest) still yields exactly [K..tip] by seq, first prev_hash==base_prev_hash => links; (v2 head + re-based manifest) is the committed state — there is no reachable state where a valid store reads as TAMPERING.",
        "c1_honest_limit": "The irreducible residual: hard-prune deletes records from the LIVE set, so the live spine alone can no longer re-hash the pruned prefix — a cold verifier handed ONLY an untrusted mirror (live + v2 head, no archive, no authenticated floor) can be shown an internally-consistent OLD owner-signed snapshot state (a full rollback to a prior valid head). This is fundamental to any prunable, single-owner-key log (the owner's key is FS-resident, checkpoint.py:9) and CANNOT be closed from data alone. Cold-archive bounds it on two axes: INTEGRITY — the archive (moved gz segments + archive manifest + per-snapshot merkle_root) lets ANY verifier re-attach and re-verify genesis->tip cryptographically, and any single pruned record carries a Merkle inclusion proof, so nothing pruned is unverifiable; FRESHNESS/ANTI-ROLLBACK — the durable external floor.json (outside spine/, monotonic, prev_head_hash meta-chain) rejects every rollback below the last accepted head for warm/same-host/returning-paired verifiers, and the initial floor + the archive are handed over the AUTHENTICATED WireGuard pairing channel, never the mirror. So the exposure narrows to exactly one case: a verifier that consumes only the untrusted mirror and NEVER authenticates the floor even once — for whom freshness (not integrity) is unprovable. Every other failure flags false-tamper, never false-clean.",
        "slice_plan": [
          "HP-0: Head v2 schema (6 defaulted fields, extra=forbid kept) + version-conditional _head_payload + verify_chain/verify_head genesis_prev param + verify_checkpoint try/except + SpineTailer._resolve_head distinct 'schema-too-new/upgrade' state. NO prune. Merge gate: v1-signed heads verify BYTE-IDENTICAL under the v2 model on the real spine; old-client fails closed to 'upgrade', never 'un-notarized'.",
          "HP-1: Durable floor.json (SIGIL_HOME/floor.json, 0600, monotonic upward-only, prev_head_hash meta-chain) read+enforced in classify_head/verify_checkpoint + mirrored to SpineTailer high-water. Still base_count=0. Merge gate: replayed old head/short-spine pair rejected; --reset cannot lower the floor.",
          "HP-2: SnapshotState schema + SnapshotState.load/fold + the ~9 consumer seed edits, driven by an EMPTY snapshot (base_seq=0,base_count=0, all folds empty) appended to a real spine — exercises every seed/fold path with ZERO deletion. Merge gate: verify() and all 9 consumers byte-identical to pre-snapshot.",
          "HP-3: Merkle commitment (leaf=entry_hash) + kind=snapshot record emission + classify_head live-window-BY-SEQ + the exact anti-truncation guard (len(live)<entry_count-base_count). Emit a NON-empty snapshot but DO NOT move/delete segments (base fields set, all segments retained). Merge gate: verify uses the live window; full-scan still spans; truncation/rollback/front-cut/middle-delete all flag TAMPERING.",
          "HP-4: Referential-integrity guards (dangling parent/supersedes, oldest-open-workflow floor K<=oldest open, whole-sealed-segment-only prune-point selection). Merge gate: prune ABORTS on a dangling binding or an open workflow below K; no bytes touched on abort.",
          "HP-5: Cold-archive MOVE — copy->archive + append archive manifest, then Phase-B commit (staging head -> os.replace head, atomic manifest re-base, monotonic floor update, unlink) + reconciler + REQUIRED crash-fuzz harness (kill-9 at each Phase-A/Phase-B barrier) asserting a verify()-clean, non-lossy, never-false-TAMPERING store at every window. THIS reclaims disk.",
          "HP-6: Q5 resolve() -> PrunedRef + get_archived() Merkle-inclusion fetch + the ~9 old-seq resolvers switch None->410/PrunedRef + `sigil spine verify --with-archive` full genesis re-attach."
        ],
        "invariants_at_risk": [
          "Anti-truncation (checkpoint.py:71 n<signed_n): must become len(live)<entry_count-base_count with live selected BY SEQ — a naive n<entry_count is false-tamper every prune; a disabled check hides real truncation.",
          "Genesis-rooted chain (chain.py:38 hardcoded _GENESIS_PREV): the re-based live set links from base_prev_hash, so verify_chain MUST take genesis_prev or the first live record's prev_hash mismatches => false-TAMPERING.",
          "v1 head byte-identical verification (chain.py:50 _head_payload): 6 new defaulted fields MUST be version-conditionally dropped or every existing deployment false-alarms.",
          "State-scanner fail-CLOSED (invariant 15, store.py:255 iter_records RAISES on a missing referenced segment): archived segments must be DROPPED from the live manifest at commit, else a full-scanner raises SpineUnavailable post-prune.",
          "Monotonic anti-rollback floor: floor.json must live OUTSIDE spine/ (reset() rmtrees spine/) and update upward-only, or a rollback/replay of an old snapshot head goes undetected.",
          "No acked-record loss: live [0..K) segments unlink ONLY after archive copies are fsynced AND the manifest re-based; the snapshot record acked before commit.",
          "Never mutate a retained binding (referential integrity): base_prev_hash must equal the already-stored prev_hash of seq K so no record byte is rewritten.",
          "Kill-switch/nonce/promotion/device high-waters must be SEEDED from the fold — an un-seeded live-only scan silently regresses a security floor (a lost kill-switch latch is a missed halt).",
          "Head-lineage single-predecessor (prev_head_hash meta-chain): a new v2 head must descend from exactly the accepted head, or an attacker forks the head lineage.",
          "extra=forbid on SignedChainHead: an old v1 client MUST fail closed on a v2 head (ValidationError -> 'upgrade'), never silently to 'un-notarized/clean'."
        ],
        "risks": [
          "MAX-RECLAIM (keep 1 sealed + active live) makes the offline archive a HARD availability dependency for any full genesis verify and for resolving any pruned citation — if the archive mount is lost, integrity of pruned history is unrecoverable (mitigate: archive is the recoverability proof, replicate it; the live spine still verifies from the snapshot forward).",
          "First prune is a fleet-upgrade FLAG DAY: any paired mobile/mesh client still on v1 cannot verify a v2 head — must gate emission behind every device advertising max_head_schema>=2, else a stale client shows 'upgrade required' (fail-closed but a UX/availability hit).",
          "The archivist current-view fold (bearer 7) is the heaviest and most error-prone: mis-projecting the non-superseded-at-K-1 set silently drops a grounded fact from open_threads/due_commitments with no verify() signal — needs a dedicated equivalence test (iter_current before vs after an empty then non-empty prune).",
          "The reconciler must be exercised by the crash-fuzz harness at EVERY Phase-A/Phase-B barrier; the head-vs-manifest window is only safe because classify_head selects by seq — a regression to prefix-selection reintroduces false-TAMPERING that only a kill-9 test catches.",
          "prev_head_hash meta-chain + floor monotonicity are the ONLY cross-restart rollback defenses once records are deletable; a bug that lets floor go backward (or a reset that reaches it) reopens the full-rollback hole silently.",
          "A never-authenticated cold verifier off an untrusted mirror still cannot prove FRESHNESS (only integrity via the archive) — this is honest and irreducible, not a bug, but must be documented so no one over-claims cold-verify completeness.",
          "Recommendation stance: build this tier ONLY on explicit owner sign-off; retain-all rotation already bounds per-file size + latency, so hard-prune's cold-verifier residual is a real tamper-model tradeoff, not a free win."
        ]
      },
      {
        "name": "COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
        "one_line": "Prune the oldest WHOLE sealed segments from the LIVE spine behind an owner-signed, Merkle-committed snapshot; MOVE them to an offline archive that IS the recoverability + full-chain re-verify proof; re-base the manifest and sign a v2 head with a version-conditional payload so v1 clients verify byte-identically, and a cutover whose only intermediate states are superset-benign or orphan-GC, never false-TAMPERING.",
        "snapshot_anchor": "Two coupled owner-signed commitments, cross-checked. (1) A spine record kind=\"snapshot\", actor=\"OWNER\", appended as a NORMAL retain-all append at tip seq S=T+1 BEFORE any prune, so it is hash-chained and fsync'd-before-ack (store.py:696) and covered by every future head. Payload commits: base_seq (=K, first RETAINED seq = first_seq of the oldest KEPT sealed segment); base_prev_hash (= boundary_hash of the newest ARCHIVED segment = the kept segment's first_prev_hash already in the manifest, manifest.py:47 — the synthetic genesis-prev); base_count (absolute count of pruned records [0..K), ==K when contiguous from genesis); merkle_root (C2: binary Merkle over pruned entry_hash leaves, seq order, dup-last-on-odd); snapshot_seq (=S); prev_snapshot_seq (snapshot meta-chain, -1 first). K MUST equal some sealed segment's first_seq so we archive WHOLE segments only — base_prev_hash is then a value already in the manifest, reusing the seam machinery at store.py:521-525. (2) SignedChainHead v2 commits the SAME six fields inside the owner-Ed25519 payload; head_hash=entry_hash(S), last_seq=S, entry_count ABSOLUTE=base_count+live. \"Descends from exactly one prior head\": prev_head_hash = sha256_hex of the previously accepted head's signing bytes; a verifier accepts H2 iff H2.prev_head_hash == floor.head_hash (durable_floor) — so a substituted/older signed head is rejected even though owner-signed, and the snapshot's prev_snapshot_seq gives an independent second meta-chain cross-checked against snapshot_seq.",
        "head_v2_migration": "SignedChainHead (models.py:56, extra=\"forbid\") gains 6 fields defaulted to the no-prune identity: base_seq=0, base_prev_hash=_GENESIS_PREV, base_count=0, merkle_root=\"\", snapshot_seq=-1, prev_head_hash=\"\", and schema_version already defaults 1 (bump to 2 only when signing a prune). THE FLAG-DAY FIX: _head_payload (chain.py:50) currently does model_dump(mode=\"json\") minus signatures; canonical_json sorts keys, so a v1 head loaded from disk would now serialize WITH the 6 defaulted fields → different bytes → every existing signature fails → false TAMPERING fleet-wide. Make the payload VERSION-CONDITIONAL: in _head_payload, if d[\"schema_version\"]==1 pop the six v2 keys before evidence_signing_bytes; for v2 keep them. Result: v1 heads produce byte-identical signing bytes as today (verify unchanged); v2 heads bind the anchor fields. (exclude_unset is fragile across model_validate_json defaulting — use the explicit version-conditional pop.) Harden verify_checkpoint (checkpoint.py:82) with try/except around model_validate_json + classify_head so a v3-from-the-future head returns (\"schema too new — upgrade\") not a crash. SpineTailer._resolve_head (tail.py:110) already swallows to unkeyed; add a DISTINCT state: if the parsed schema_version > SUPPORTED, return a sentinel so poll() emits anchored=False with reason \"head schema vN unsupported — upgrade this client\", never silent-clean and never a rewrite alarm. Old mobile client vs a v2 head: extra=\"forbid\" would REJECT the unknown fields on model_validate_json → it must treat parse-failure as \"unverifiable/upgrade\", NOT tamper — the SpineTailer sentinel path delivers exactly that; the un-anchored live tail still streams (honest, fail-closed). Fleet upgrade is gated BEFORE the first prune (empty-snapshot slice ships the v2 model + reader everywhere first).",
        "verify_snapshot_aware": "verify_chain (chain.py:37) gains genesis_prev=_GENESIS_PREV param seeding prev; default keeps every existing call byte-identical. For a re-based live segment, genesis_prev=base_prev_hash so the first live record (prev_hash==base_prev_hash) links cleanly. classify_head (checkpoint.py:65) becomes snapshot-aware: signed_live = head.entry_count - head.base_count; locate j where live[j].seq==head.base_seq; require live[j].prev_hash==head.base_prev_hash (LEFT-EDGE PIN to the committed base); then n_live_from_j = len(live)-j; if n_live_from_j < signed_live → TAMPERING (truncated/rolled back); verify_head over live[j:j+signed_live] with genesis_prev=base_prev_hash. The anti-truncation guard that SURVIVES pruning: entry_count stays ABSOLUTE and base_count is inside the OWNER-SIGNED payload, so an attacker cannot shrink the live window and mask it by inflating base_count without a valid owner signature over that base_count AND a matching merkle_root over that many archived leaves. Crucially classify does NOT do the old bare n<entry_count (which post-prune ALWAYS fires → false tamper) NOR let n<entry_count become a no-op — it compares n_live vs the base-adjusted signed_live. Records at live[0:j] (seq<base_seq) are the not-yet-pruned prefix during cutover: they chain genesis-rooted and are classified BENIGN (superset/stale), which is what removes the false-TAMPERING window.",
        "durable_floor": "~/.sigil/floor.json (SIGIL_HOME root, 0600), OUTSIDE spine/ so reset()'s rmtree of the spine dir (store.py:432) can't delete it. Monotonic JSON {last_seq, base_seq, base_count, head_hash, snapshot_seq, prev_head_hash}, written via atomicio.atomic_write_text (temp→fsync→replace→dir-fsync). REJECT RULES enforced in verify_checkpoint/classify_head and in the append/anchor path: reject any head or snapshot with last_seq<floor.last_seq OR base_count<floor.base_count OR base_seq<floor.base_seq (a rollback to a smaller/older prune), and reject a v2 head whose prev_head_hash != floor.head_hash (the META-CHAIN: only a head that names the currently-accepted head as its parent is admissible — blocks re-installing a stale owner-signed head). Floor updates UPWARD only, after a successful full anchor (step 6 of the cutover), never on read. prev_head_hash chains head→head; snapshot.prev_snapshot_seq chains snapshot→snapshot; the two are cross-checked. HONEST LIMIT: a cold verifier bootstrapping off an untrusted mirror has no prior floor, so it must trust the owner signature over base_prev_hash/base_count/merkle_root — an FS-write attacker holding the owner key (which is FS-resident, checkpoint.py:9) could sign a different pruning and the cold verifier can't tell from data alone. This is fundamental to any prunable log.",
        "cold_archive_move": "WHICH MOVES: only sealed segments entirely below K (last_seq < K); K is chosen = first_seq of the oldest sealed segment to RETAIN, and only segments older than a retention window (keep the newest N sealed + the active) are eligible. ARCHIVE LAYOUT: ~/.sigil/spine/archive/ (or an offline mount) holding the segment files unchanged (still gzip, byte-decompress-identical) PLUS archive.manifest.json = an append-only, generation-stamped list of every archived Segment in seq order with id/first_seq/last_seq/first_prev_hash/boundary_hash/sha256, so the archive is self-describing and immutable-append. LIVE MANIFEST RE-BASE: drop the archived Segments from manifest.segments, generation++, and record advisory base_seq/base_prev_hash/base_count/snapshot_seq copies (manifest stays NON-load-bearing, manifest.py:1-14); the oldest retained segment's first_prev_hash already == base_prev_hash (no mutation needed). RE-ATTACH TO VERIFY FROM GENESIS: a verifier concatenates archive segments [0..K) (in archive.manifest order) THEN the live segments [K..], feeds ONE stream to verify_chain(genesis_prev=_GENESIS_PREV) — full genesis-rooted verification, and recomputes the Merkle over the archived leaves and checks == the snapshot's committed merkle_root (binding archive↔snapshot, closing C2). Live-only verify uses genesis_prev=base_prev_hash + the anti-truncation guard.",
        "state_folding": "SnapshotState.load(store) reads the snapshot record (via head.snapshot_seq / manifest) and returns a typed object; each consumer switches to state=SnapshotState.load(store); fold(state, store.iter_records()). The snapshot payload folds each bearer's pruned-[0..K) contribution (computed AT PRUNE TIME while the records are still present): (1) envelope.device_nonce_highwater (envelope.py:88) → {device_pubkey: max nonce over [0..K)}; consumer seeds hi=state.nonce.get(dev,-1) then max over live (a pruning-resets-nonce bug would re-open replay — critical). (2) killswitch latch _scan_engaged (killswitch.py:70) → the engaged BOOLEAN as-of-K, computed with the same owner-signed-release rule; consumer seeds engaged=state.killswitch_engaged then applies live; if ambiguous fold engaged=True (halt is the safe direction). (3) mesh authorized_devices (registry.py:66) → {pubkey: authorized|revoked} latest-verified as-of-K; consumer overlays live. (3b) capability_map (registry.py:42) → {host_id: latest verified cap}. (4) actor_scope creation counts (actor_scope.py:44) → {(service,origin): count of applied account.create in [0..K)}; consumer seeds n from state then adds live (a reset would let mass account creation outrun the cap — critical). (5) promotion is_promoted (promotion.py:44) → {(agent,scope): granted|revoked} latest-verified as-of-K. (6) approvals pending (approvals.py:53) → set of resolved target_seqs verified in [0..K); by the open-workflow floor every pruned queued proposal is already resolved, so this is an assertion (refuse to prune if any pruned queued item is still unresolved). (7) archivist supersession (revise.py:24) → set of superseded seqs in [0..K) so a live record's supersedes_id below K reads as resolved. (8) archivist promotion-key ledgers (revise.py:28) → the grounded/refused promotion_key SETS as-of-K (idempotency — else a pruned grounding gets re-promoted). (9) newest warden_checkpoint → the signed head's last_seq folded into floor.json (not a record scan). Threads/commitments/contradictions (queries.py) need no fold: they are protected by the oldest-open-workflow floor (referential_integrity) and closed ones' content lives in archive (Q5).",
        "referential_integrity": "Prune-point guards, all checked read-only BEFORE writing the snapshot (refuse the whole prune if any fails, fail-closed): (a) DANGLING PARENT/SUPERSEDES: scan retained records [K..T]; if any retained record's parent_id or supersedes_id points into [0..K) AND is not resolved by the archivist supersession/ledger fold, refuse — never leave a retained record pointing at a vanished binding. (b) OLDEST-OPEN-WORKFLOW FLOOR: K <= min seq among open threads / due commitments / pending contradictions (queries.py) and pending approvals (approvals.py:53) and any not-yet-superseded consolidation record still referenced by an open item — so no open/unresolved workflow is ever pruned (its content stays live). (c) NEVER MUTATE A RETAINED RECORD'S BINDING: the re-base only DROPS archived segments and edits base_* advisory fields; it never rewrites a retained record, its prev_hash, entry_hash, parent_id, or supersedes_id (append-only preserved). (d) K aligns to a segment boundary (first_seq) so no sealed segment is split. Because archived records are recoverable (cold-archive), a dangling reference is a hard REFUSE, not a lossy prune.",
        "q5_resolve": "get(seq) for a pruned seq must not return a bare None/404 (store.py:767 currently returns None for a below-base seq). Add resolve(seq) -> SpineRecord | PrunedRef where PrunedRef = {pruned: True, seq, base_seq, base_count, snapshot_seq, archive_hint} meaning \"compacted below base_seq — see snapshot S / archive\". get() stays returning None for genuinely-absent seqs but the seq<base_seq case is distinguished via the manifest/head base fields; callers that must serve content (bridge /api/record, ui /api/record, mcp episodic_range, consolidate/gate grounding, transcript parent_id) surface a 410-Gone-style PrunedRef, never a silent 404. ARCHIVE FETCH PATH: PrunedRef.archive_hint names the archived segment (via archive.manifest first_seq/last_seq range → owning segment id); resolve_from_archive(seq) opens archive/<seg>.jsonl.gz, seeks the seq, re-verifies the record's entry_hash against the segment's chain and (optionally) the whole prefix Merkle against merkle_root before returning it — so a served archived record is proven, not trusted.",
        "crash_safe_cutover": "ORDER (all guards read-only first): 1) choose K (segment-aligned), run referential_integrity + floor guards. 2) compute merkle_root over [0..K) and the 9 folds from the to-be-archived segments (read). 3) append the kind=snapshot record S (fsync+ack, retain-all — reversible). 4) COPY the archived segments to archive/ + write archive.manifest, fsync, verify re-hash == merkle_root (originals still in live — nothing lost). 5) sign v2 head H2 {schema_version=2, last_seq=S, entry_count=S+1, base_seq=K, base_prev_hash, base_count=K, merkle_root, snapshot_seq=S, prev_head_hash=hash(old head)} and atomic_write head.json. 6) update floor.json upward (atomic, monotonic). 7) THE COMMIT INSTANT: atomic manifest re-base (write_manifest → os.replace, manifest.py:137, generation++) dropping the archived segments. 8) unlink the now-unreferenced original sealed segments + fsync dir (reclaim). 9) delete the durable prune.intent. A single prune.intent file (written at step 3, carrying K + the signed H2 + archived ids) makes every step idempotently re-drivable. RECONCILER (at open AND on each flock, like store.py:348): if prune.intent exists, re-drive from whatever is on disk (copy/head-write/re-base/unlink are all idempotent), then delete it; orphan original segments not in the re-based manifest are GC'd exactly like the existing seal orphan-GC (store.py:509-512). NO-LOST-ACK: every append (incl. S) is fsync'd-before-ack; the prune only DROPS whole archived segments that were COPIED to archive and verified first, and whose records are all <K (superseded by the snapshot base + preserved in archive) — no acked record is deleted, only relocated + recoverable. NO-FALSE-TAMPER: the ONLY intermediate windows are (i) H2 written + manifest still full = a SUPERSET (pre-prune prefix [0..K) still present) which classify_head classifies BENIGN via the left-edge-pin-with-superset rule, and (ii) manifest re-based + originals still present = originals are orphans not in the manifest → GC'd, live=[K..S] matches H2 → CURRENT. Signing H2 BEFORE re-basing the manifest is what guarantees no window where an old head sees a shrunk live set.",
        "c1_honest_limit": "Residual: a COLD verifier bootstrapping off an UNTRUSTED mirror sees a v2 head committing base_prev_hash/base_count/merkle_root but, WITHOUT the archive, cannot recompute the pruned [0..K) prefix — it must trust the owner signature over those fields. An attacker holding the FS-resident owner key (checkpoint.py:9) could sign a different (base_seq', merkle_root') that prunes a real record, and the cold verifier can't detect it from data alone. floor.json + the prev_head_hash meta-chain close this for WARM / same-host / returning-paired verifiers (they remember the last accepted head_hash and reject a fork/rollback). COLD-ARCHIVE BOUNDS THE REST: hand the cold verifier the archive over the AUTHENTICATED WireGuard device-pairing channel (never the mirror); it re-attaches archive [0..K) + live [K..], verifies the whole chain from _GENESIS_PREV, and recomputes the Merkle over the archived leaves == committed merkle_root. That reduces the cold verifier's trust to exactly the owner-key trust today's single-file spine already requires — pruning widens the rollback blast radius by zero bits FOR A VERIFIER GIVEN THE ARCHIVE, and the archive is always available because the prune refuses unless the copy+re-hash succeeded (crash_safe_cutover step 4).",
        "slice_plan": [
          "Slice A (migration flag-day, NO prune): add the 6 defaulted v2 fields to SignedChainHead + the VERSION-CONDITIONAL _head_payload (v1 heads verify byte-identically); harden verify_checkpoint try/except and SpineTailer._resolve_head with a distinct 'schema too new — upgrade' sentinel; add verify_chain genesis_prev param (defaulted, byte-identical) and base-aware classify_head (base_count=0 → identical). Ship to the whole fleet + all mobile clients FIRST. Merge gate: every existing v1 head + old client still verifies green.",
          "Slice B (empty-snapshot-first): SnapshotState.load + the fold contract for ALL 9 bearers; consumers switch to load+fold; seed an EMPTY snapshot (base_count=0, base_seq=0) so every seed path is exercised with ZERO deletion — byte-identical behavior across all consumers.",
          "Slice C: floor.json durable external floor + monotonic reject rules + prev_head_hash meta-chain, wired into verify_checkpoint/classify_head and the anchor path. Still no prune.",
          "Slice D: snapshot-record writer + Merkle builder + referential_integrity + open-workflow floor guards + segment-aligned K selection. Writes the snapshot and re-bases the manifest but KEEPS segments (no move yet) — reclaims nothing, fully reversible, exercises the anti-truncation guard on a real re-based live window.",
          "Slice E (safety-critical): cold-archive COPY + archive.manifest + re-attach verifier + the crash-safe cutover ordering + prune.intent reconciler + orphan-GC + the unlink-reclaim. REQUIRED merge gate: a kill-9-at-each-barrier crash-fuzz harness asserting no lost ack, no false TAMPERING, verify()-clean at every barrier, and archive re-attach verifies from genesis.",
          "Slice F: Q5 resolve/PrunedRef + archive fetch path for the get(seq) resolvers (bridge/ui /api/record, mcp episodic_range, gate grounding, transcript parent_id)."
        ],
        "invariants_at_risk": [
          "Inv 7/13 (append targets the true chain tip / no seq-0 fork): the snapshot record and the re-based live[0] must link via base_prev_hash, never build_chain-from-genesis (store.py:686-687 seam logic must extend to the base).",
          "Inv 9/A4 (rotation-aware change token): a prune bumps manifest.generation so killswitch (killswitch.py:60) and gesture session caches invalidate — a stale un-halting verdict must be impossible across a prune.",
          "Inv 12 (one contiguous genesis-rooted chain to consumers): post-prune the LIVE chain is genesis-prev=base_prev_hash rooted; only archive re-attach restores true genesis.",
          "Inv 15 (no state-scanner fail-open): iter_records already RAISES on a missing referenced segment (store.py:255); the fold consumers must fail-closed if SnapshotState.load fails rather than scanning a truncated live set as if complete.",
          "Inv 16 (referential integrity): retained parent_id/supersedes_id must never dangle into the pruned range.",
          "NEW: absolute entry_count + owner-signed base_count is the sole anti-truncation guard once the pruned prefix is no longer re-hashed.",
          "NEW: prev_head_hash meta-chain — only one prior head is a valid parent (blocks stale-head substitution).",
          "NEW: monotonic floor.json outside spine/ (reset-proof)."
        ],
        "risks": [
          "Version-conditional payload is the whole flag-day: if _head_payload does NOT drop the 6 fields for schema_version==1, every v1 signature fails → fleet-wide false TAMPERING. Mitigation: explicit pop + a test asserting a real pre-existing v1 head verifies byte-identically under the v2 model.",
          "extra='forbid' on SignedChainHead (models.py:58) means an OLD client rejects a v2 head at parse time — must be routed to the 'unverifiable/upgrade' sentinel, never a tamper alarm; ship Slice A everywhere before the first prune.",
          "A prune that resets a monotonic bearer (nonce high-water, account-creation cap) would re-open replay / mass-creation — the folds for bearers (1) and (4) are security-critical, not cosmetic.",
          "Cold-verifier residual is unclosable from data alone; do not overclaim — it is bounded, not eliminated, and only by handing the archive over the authenticated channel.",
          "Crash between head-sign and manifest re-base MUST be classified benign (superset); a bug making it TAMPERING would false-alarm on every prune. The crash-fuzz harness at each barrier is the merge gate.",
          "If K is not segment-aligned or the copy+re-hash isn't verified before unlink, an acked record could be lost — K must equal a sealed first_seq and unlink happens only after archive verify + manifest re-base (steps 4→7→8).",
          "Merkle over a large pruned prefix must be recomputable deterministically (dup-last-on-odd, seq order) or archive re-attach fails to bind — pin the tree construction in a shared, tested helper."
        ]
      }
    ],
    "judged": [
      {
        "design": {
          "name": "VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine",
          "one_line": "Prune the oldest whole sealed segments from the live set behind an owner-signed, Merkle-committed kind=\"snapshot\" record, MOVE those segments to a cold archive (the recoverability proof, handed over the authenticated WireGuard channel), re-base the live manifest to a synthetic genesis-prev, and keep every invariant fail-safe — with head-schema v2's six fields added under a version-conditional signing payload so all existing v1 heads still verify byte-identically.",
          "snapshot_anchor": "A real spine record kind=\"snapshot\" (add \"snapshot\" to spine/models.py KINDS) is appended at the LIVE tip via the normal acked append path BEFORE any deletion, so it chains from the current tip and verify()/the chain are undisturbed at write time. Its payload commits exactly: base_seq=K (first RETAINED live seq; all seqs [0..K) are pruned), base_prev_hash=entry_hash(K-1) (the last pruned record's entry_hash = the synthetic genesis-prev the re-based live segment links to), base_count=K (absolute count of pruned records — equals base_seq for a contiguous log, both stored for robustness), merkle_root over the pruned leaves [entry_hash(0)..entry_hash(K-1)] (C2), snapshot_seq=this record's own seq (in the live window, > K, near the tip), prev_head_hash=the sha256 of the previously-accepted head's signing bytes (the meta-chain link), plus the folded_state block (see state_folding). Because it is a normal record its cert_digest (digest_payload over content, reuse/chain._entry_hash) binds the whole commitment into the hash chain, and the owner-signed head then notarizes it — so the merkle_root/base fields are tamper-evident even between `sigil sign` runs. The head's six v2 fields (below) are set to the SAME values, and classify_head cross-checks head.{base_seq,base_count,merkle_root} == the snapshot record at head.snapshot_seq, so the head cannot claim a different prune boundary than the immutable committed record. A verifier confirms the head descends from exactly one prior accepted head via the prev_head_hash meta-chain: head.prev_head_hash must equal floor.json's stored head_doc_hash (the single accepted parent); on accept, floor.head_doc_hash is advanced to sha256(evidence_signing_bytes(_head_payload(this head))). A linear meta-chain = exactly one parent, no forks.",
          "head_v2_migration": "SignedChainHead (reuse/models.py, extra=\"forbid\") gains six fields, all defaulted so a v1 head validates unchanged: base_seq:int=0, base_prev_hash:str=_GENESIS_PREV, base_count:int=0, merkle_root:str=\"\", snapshot_seq:int=-1, prev_head_hash:str=\"\". schema_version bumps to 2 only when a prune actually sets them. THE blocking gate: _head_payload (reuse/chain.py) becomes version-conditional — after model_dump(mode=\"json\") and popping \"signatures\", if head.schema_version < 2 it also pops the six new keys, so a v1-signed head's signing bytes are byte-identical to today and every persisted head.json still verifies; without this every deployment false-alarms to TAMPERING until the next `sigil sign`. sign_head grows a v2 path: entry_count = base_count + len(live_entries) (stays ABSOLUTE), head_hash/last_seq from the live tip, seeds the six fields from the snapshot record. verify_head derives genesis_prev = head.base_prev_hash (=_GENESIS_PREV for v1) and passes it to verify_chain; its count check becomes head.entry_count != head.base_count + len(entries) (v1: base_count=0 → identical). Harden verify_checkpoint (checkpoint.py:86) with try/except around model_validate_json + classify_head: a parse failure does a lenient json.loads to read schema_version — if it exceeds the known max, return a DISTINCT (False, \"schema too new — upgrade sigil\") state (fail-closed, never silent-un-notarized, never false-clean); a genuinely corrupt head returns (False, \"corrupt head\"). SpineTailer._resolve_head (tail.py:110) mirrors this: instead of swallowing every exception to (None,None) (\"un-notarized\"), it returns a third tri-state \"schema_ahead\" that poll()/check_anchor surface as \"upgrade required\", so a too-new head is never dressed up as a merely-unsigned tail. OLD mobile clients (v1 code) reading a v2 head hit extra=\"forbid\" ValidationError → they show un-notarized (SAFE: the tail is treated as not-proven, never false-clean) — which is exactly why the first prune is a fleet-upgrade flag-day gated on all verifiers running v2 code + explicit owner sign-off.",
          "verify_snapshot_aware": "verify_chain (reuse/chain.py:37) gains genesis_prev: str = _GENESIS_PREV and seeds prev = genesis_prev instead of the hard-coded constant; its i>0 contiguity check is unchanged (the first live record's seq=base_seq is never checked against 0, only successive-seq contiguity within the window), so a re-based live segment whose first record has prev_hash == base_prev_hash links cleanly instead of forking. store.verify() passes genesis_prev = manifest.base_prev_hash for the live-only view; a re-attached (archive+live) view passes _GENESIS_PREV (true genesis). classify_head (checkpoint.py:65) becomes snapshot-aware: base = head.base_count; live_expected = head.entry_count - base; n = len(entries) (the live window). The anti-truncation guard is REWRITTEN from `if n < signed_n` to `if n < live_expected: return TAMPERING (live window truncated)` — this is the exact fix for the no-op trap: entry_count stays ABSOLUTE, but the comparison subtracts base_count so a legit prune (n == live_expected) is clean while a real live-region truncation (n < live_expected) still fires. It then verify_head(head, entries[:live_expected], tr) (verify_head self-seeds genesis_prev=head.base_prev_hash), and cross-checks the snapshot record at head.snapshot_seq carries matching base_seq/base_count/merkle_root. The pruned-region truncation is caught by TWO independent guards: (a) floor.json monotonic {last_seq, base_count} + prev_head_hash meta-chain rejects any head that lowers last_seq or base_count or doesn't descend from the accepted head; (b) merkle_root binds the pruned leaves, so a cold verifier holding the archive re-derives base_count from the actual archived record count and checks merkle_root == head.merkle_root. entry_count NEVER decreases and is never renumbered.",
          "durable_floor": "~/.sigil/floor.json (SIGIL_HOME/floor.json, 0600, OUTSIDE spine/ so SpineStore.reset()/`sigil ingest --reset` — which only rmtree's spine-dir artifacts, store.py:414 — cannot delete it). JSON {schema_version, scope, last_seq, base_seq, base_count, head_doc_hash, updated_ts}. Written only atomic (atomicio.atomic_write_text) and only AFTER a head is signed+verified. MONOTONIC: a write is refused if last_seq < current.last_seq or base_count < current.base_count. Reject rules, evaluated inside classify_head/verify_checkpoint (and SpineTailer.check_anchor) after loading floor: reject (→ TAMPERING/fail-closed) any head with head.last_seq < floor.last_seq (rollback), head.base_count < floor.base_count (pruned-region rollback / un-prune claiming fewer deletions), or head.prev_head_hash != floor.head_doc_hash (meta-chain break — not the single accepted descendant). prev_head_hash meta-chain: each v2 head commits prev_head_hash = floor.head_doc_hash at signing; verifier checks it; on accept floor.head_doc_hash = sha256(evidence_signing_bytes(_head_payload(head))). A rebuild that legitimately shortens the spine (`--reset`) must NOT silently defeat the floor — that requires a separate explicit owner-key-gated `sigil floor reset`, never the routine reset. HONEST LIMIT (C1): a cold verifier bootstrapping off an UNTRUSTED mirror gets {spine, head, floor.json, snapshot record} all from the mirror, and a filesystem-write adversary can present a self-consistent OLD tuple rolled back together (floor.json is just a file the mirror controls, and the owner key is FS-resident per checkpoint.py:9) — this full-tuple rollback is unclosable from data alone, fundamental to any prunable log. COLD-ARCHIVE bounds it: the archive (the full genesis..K-1 chain) and the initial floor are seeded over the AUTHENTICATED WireGuard device-pairing channel, never a mirror; with the archive the cold verifier re-derives base_count from actual archived records, checks merkle_root, and verifies from true genesis. Warm/same-host/returning-paired verifiers are fully closed by the persisted floor + SpineTailer._high_water (tail.py:60,90-93).",
          "cold_archive_move": "Which moves: only WHOLE sealed segments (never the active, never a partial segment), strictly oldest-first, whose last_seq < K, keeping the newest N live segments (config SIGIL_PRUNE_KEEP_SEGMENTS) or those younger than a retention window — and only up to the referential floor (see referential_integrity). MOVE = copy→verify→drop-from-live, so it is additive-then-atomic, never a destructive rename first. Archive layout (default ~/.sigil/spine/archive/, configurable to an offline mount SIGIL_ARCHIVE_DIR): archive/segments/seg-*.jsonl.gz (the moved segments, still gzip, byte-identical) + archive/archive.manifest.json — an ordered list of archived segments {id, file, codec, first_seq, last_seq, first_prev_hash, boundary_hash, sha256, count} plus a header {merkle_root, base_seq, base_count} matching the snapshot record. The archive is itself a genesis-rooted chain segment set: seg-0.first_prev_hash=_GENESIS_PREV, each boundary_hash==next first_prev_hash, last boundary_hash==base_prev_hash. LIVE manifest re-base: the Manifest model (spine/manifest.py) gains reserved-null header fields base_seq/base_prev_hash/base_count/snapshot_seq/merkle_root; the re-based manifest (generation++) drops the archived segment entries, sets those header fields, and the oldest REMAINING live segment keeps its existing first_prev_hash (which already equals base_prev_hash == the archive's last boundary_hash — a rename/copy is byte-preserving, so NO segment file is rewritten; the re-base is pure manifest metadata). Reader re-attach: point a SpineStore (or a `sigil archive verify` view) at the union [archive/segments... ++ live segments...]; iter_records concatenates archive-then-live in seq order, and store.verify(genesis_prev=_GENESIS_PREV) over the whole set re-verifies the FULL chain from genesis — the archive→live seam matches because base_prev_hash == archive boundary_hash. Archive integrity is the per-segment sha256 in archive.manifest.json; the owner keeps >=1 copy (losing the archive makes the pruned prefix unrecoverable, though merkle_root still proves what WAS there).",
          "state_folding": "A new SnapshotState.load(store) reads the kind=\"snapshot\" record at head.snapshot_seq (or the newest snapshot record) and returns its folded_state; every monotonic-state consumer switches from a pure full-scan to `state = SnapshotState.load(store); fold state over store.iter_records()` (which now yields only live records). Empty snapshot (base_count=0, empty folds) is seeded FIRST so the fold path is proven == pure-scan before any deletion. The nine bearers and exactly what each folds from the pruned range [0..K): (1) NONCE HIGH-WATER (bridge/envelope.py device_nonce_highwater): {device_pubkey: max_nonce} — consumer starts hi from folded[device] then max()es live receipts; losing it would let an old nonce replay, so max-fold is mandatory and safe. (2) WARDEN CHECKPOINT (cli.py cmd_warden_anchor_get): {pubkey: [best_count, best_head_hash]} highest anchored count per key — consumer continues the >= scan from the fold; anti-rollback high-water. (3) ACCOUNT.CREATE CAP (agents/actor_scope.py creation_allowed): {by_service:{service:n}, by_origin:{origin:n}} counts of applied account.create steps — consumer ADDS live counts before comparing creation_cap; losing it resets the cap (mass-creation). (4) DEVICE AUTHORIZE/REVOKE + CAPABILITY (mesh/registry.py authorized_devices/capability_map): {device_pubkey: \"authorized\"|\"revoked\"} and {host_id: capability} — the latest VERIFIED state per key from the pruned range (only owner-signed records folded); consumer overlays live latest-state on top; losing a \"revoked\" that was latest in-pruned re-authorizes a revoked device. (5) KILL-SWITCH LATCH (governor/killswitch.py _scan_engaged): engaged:bool = the running latch state at the K-1 boundary (any engage→True, only owner-signed-verified release→False, in order); consumer continues folding live; dropping a release keeps it engaged (fail-safe halt). (6) PROMOTION GRANTS (governor/promotion.py is_promoted): {[agent,scope]: \"granted\"|\"revoked\"} latest verified per key; consumer overlays live; losing a \"revoked\" re-grants auto-approval. (7) QUEUED APPROVALS (agents/approvals.py pending): resolved_target_seqs:set of verified-approval targets in the pruned range; consumer unions live; the referential floor guarantees no OPEN (still-pending) item's pair straddles the boundary, so this only ever carries fully-resolved pairs. (8) ARCHIVIST PROMOTION-KEY LEDGER (consolidate/revise.py promotion_ledgers): {grounded:[keys], refused:[keys]} idempotency keys — consumer unions live; prevents re-promoting an already-grounded fact. (9) OPEN THREADS/COMMITMENTS/CONTRADICTIONS + CITATIONS (consolidate/queries.py): NOT folded — protected instead by the oldest-open-workflow floor (their source/current records are simply never pruned); a live grounded fact's source_seqs pointing into the pruned range degrade to PrunedRef via resolve() while the verbatim quote (self-contained in the grounded record's own payload) still serves. NOTE on supersession (revise.py iter_current): needs NO fold — supersedes_id always points BACKWARD (superseder seq > superseded seq), so a pruned superseded record's superseder is either also pruned or a live record that already excludes it; no double-serve.",
          "referential_integrity": "Three prune-point guards computed under the flock before choosing K. (a) OLDEST-OPEN-WORKFLOW FLOOR: K <= min seq over every open loop — pending() unresolved approval targets and their queued proposals (approvals.py), current grounded open_threads/due_commitments and their cited source_seqs (queries.py), unresolved contradictions, and any operator plan/preview without its execute/verify record (agents/operator.py supersedes chains). An open workflow and everything it references stays live. (b) DANGLING BINDING GUARD: refuse to advance K past a boundary where a RETAINED record's parent_id or supersedes_id points into [0..K) AND that reference is part of an unresolved workflow; a dangling parent_id/supersedes_id whose target is closed history is BENIGN (resolve()→PrunedRef, threading/current-view unaffected because supersession is forward-only and the quote is embedded), but an unresolved one blocks the prune. (c) NEVER MUTATE A RETAINED BINDING: the re-base rewrites NO segment bytes — the oldest live segment's first record keeps its stored prev_hash (already == base_prev_hash); the re-base is manifest-header metadata only, so no retained record's prev_hash/parent_id/supersedes_id/entry_hash is ever altered (which would break the chain or forge a link). Only whole sealed segments strictly below the referential floor and below any monotonic-bearer live high-water are eligible.",
          "q5_resolve": "Add SpineStore.resolve(seq) -> SpineRecord | PrunedRef (get() keeps its SpineRecord|None signature for the byte-identical common path). PrunedRef is a small frozen dataclass {pruned=True, seq, base_seq, snapshot_seq, merkle_root, archive_hint}. Contract: seq >= base_seq → live get() (SpineRecord); 0 <= seq < base_seq → if the archive is mounted/reachable, fetch it (archive.manifest.json maps the seq into its owning archived segment → decompress the gz → return the SpineRecord, exactly the iter path but over archive/segments); else return PrunedRef (never a bare None). None stays reserved for a genuinely-never-existed seq (seq < 0 or seq > tip). The ~9 old-seq resolver call sites where \"pruned vs absent\" matters (agents/actor_gate.py:22, agents/actor.py:192, agents/operator.py, perception/egress, bridge/server /api/record, ui/server /api/record, mcp episodic_range, consolidate/gate.py grounding, ingest/transcript.py parent threading) migrate to resolve(); the referential floor guarantees no OPEN workflow's resolve() ever lands in the pruned range, so they hit PrunedRef only for closed history. HTTP consumers map PrunedRef → 410 Gone with the {base_seq, snapshot_seq, \"attach archive/ or run `sigil archive fetch <seq>`\"} pointer, never a misleading 404.",
          "crash_safe_cutover": "All under the cross-process flock (_crossproc_lock) except the heavy archive copy which runs off-lock on immutable sealed segments. Ordering: (1) choose K from the three referential/bearer floors; (2) scan the to-be-pruned sealed segments (immutable) to compute merkle_root over [entry_hash(0)..entry_hash(K-1)] and the folded_state; (3) APPEND the kind=\"snapshot\" record (normal append → fsync+ack, store.py:696) carrying base_seq/base_prev_hash/base_count/merkle_root/snapshot_seq(self)/prev_head_hash/folded_state — durable, but nothing pruned yet; (4) COPY the archived segments to archive/segments + write/extend archive.manifest.json, fsync each; (5) VERIFY every archived sha256 and that the archive re-attaches (archive boundary_hash == base_prev_hash) — the pruned prefix must be a complete, verified genesis..K-1 chain in the archive BEFORE any live drop; (6) atomic LIVE manifest re-base (write_manifest, generation++) dropping the archived segments and setting the base_* header — physical live reclaim; (7) sign the v2 head (entry_count=base_count+live, six fields from the snapshot record) and atomic-write head.json — THE COMMIT INSTANT; (8) advance floor.json (monotonic, meta-chain) to the new head. THE COMMIT INSTANT is the os.replace of head.json in step 7: before it, the live manifest may already be re-based (step 6) while head.json is still v1 anchoring the pre-prune absolute count → verify_checkpoint reports FALSE-TAMPER (n < live_expected with base_count=0) — the SAFE direction, never false-clean. RECONCILER (runs at store open and on the next flock acquisition): if the manifest has base_count>0 (re-based) but head.json is v1 or head.entry_count != base_count+live, an interrupted prune is detected; it deterministically FORWARD-completes by re-deriving the v2 head from the durable snapshot record (which committed base_seq/base_prev_hash/base_count/merkle_root/folded_state) + the live entries, re-signs (owner key is FS-resident), atomic-writes head.json, then advances floor. If the owner key is unavailable (a read-only verifier), it stays in the fail-safe TAMPER state and reports \"prune in progress — run `sigil sign`\", never false-clean. NO-LOST-ACK PROOF: the snapshot record is fsync'd before any deletion (step 3), and every pruned record exists in the verified archive (steps 4-5) before the live manifest drops it (step 6); a crash at any barrier leaves each acked record either still in the live spine (pre-6) or in the archive (post-4). NO-FALSE-CLEAN / NO-STUCK-FALSE-TAMPER: the new v2 head satisfies the OLD floor (last_seq grew via the snapshot append, base_count grew from 0, prev_head_hash==old head_doc_hash), so a crash between step 7 and step 8 reopens consistent (verify passes against the old floor, floor then re-advances); a crash before step 7 reopens as fail-safe TAMPER and the reconciler completes it. Kill-9 crash-fuzz at each barrier (mirroring the existing _maybe_crash harness, store.py:55) is a required merge gate.",
          "c1_honest_limit": "The residual cold-verifier exposure: a filesystem-write adversary who controls an untrusted MIRROR can hand a cold verifier a fully self-consistent OLD tuple — {short spine, matching v2 head re-signed with the FS-resident owner key, a rolled-back floor.json, a matching snapshot record} — all rolled back together. From DATA ALONE this full-tuple rollback is undetectable, because floor.json is a mirror-controlled file and the owner signing key lives on the runtime host (checkpoint.py:9, an accepted §1.3 simplification); this is fundamental to any prunable log and is NOT widened beyond the pre-existing single-file rollback exposure for the pruned region — it is the same class of attack. COLD-ARCHIVE BOUNDS it two ways. First, the archive plus the initial floor are transferred over the AUTHENTICATED WireGuard device-pairing channel (the phone holds its own device key; mesh authorize is owner-minted), never from the mirror — so a returning paired/warm verifier has an authenticated floor high-water that a mirror rollback cannot lower. Second, when the archive is handed over, the cold verifier re-attaches [archive ++ live], re-derives base_count from the ACTUAL archived record count, recomputes merkle_root over the real pruned leaves and checks it == head.merkle_root, and re-verifies the entire chain from true genesis (base_prev_hash == the archive's last boundary_hash) — collapsing the pruned-region blind spot to zero for any verifier holding both the archive and an authenticated-channel floor seed. The only irreducible residual is a cold verifier with the mirror alone (no archive, no authenticated floor), which is exactly the case the owner is told not to trust.",
          "slice_plan": [
            "Slice A — Head v2 schema + version-conditional _head_payload + verify_chain genesis_prev param + classify_head base_count-aware anti-truncation + verify_head count/seed + verify_checkpoint/SpineTailer try/except with the distinct schema-too-new state. NO pruning. base_count=0 everywhere → byte-identical to today; merge gate: the real persisted head.json verifies byte-identical, and a synthetic base_count=0 v2 head behaves exactly as v1.",
            "Slice B — Durable floor.json (SIGIL_HOME/floor.json 0600, monotonic {last_seq,base_seq,base_count,head_doc_hash}, prev_head_hash meta-chain), seeded from the CURRENT head at first run (base_count=0), wired into classify_head/verify_checkpoint/SpineTailer as reject rules. Still no pruning; exercises rollback/meta-chain reject paths and the reset-cannot-delete-floor property.",
            "Slice C — SnapshotState + the nine bearer fold contracts + SnapshotState.load(store) + switch each consumer to load-then-fold, seeding an EMPTY snapshot (base_count=0, empty folds) FIRST so every seed path runs and is proven byte-identical to the pure full-scan BEFORE any record is deleted; per-bearer adversarial re-check (a dropped device-revoke / nonce-highwater / cap-count must be caught).",
            "Slice D — kind=snapshot record writer + merkle_root computation + the three referential-integrity guards (oldest-open-workflow floor, dangling-binding, never-mutate-retained) + archive COPY + archive.manifest.json + re-attach verify — but STILL no live drop (archive is a redundant copy, live unchanged). Proves the archive round-trips and re-verifies the full chain from genesis.",
            "Slice E — the tamper-model-changing cutover: atomic live-manifest re-base (drops archived segments) + v2 head sign (commit instant) + floor advance + reconciler + resolve()/PrunedRef + archive fetch path. Kill-9 crash-fuzz at each barrier is the required merge gate. Owner-key-gated, fleet-upgrade-gated (all verifiers on Slice-A code), explicit owner sign-off."
          ],
          "invariants_at_risk": [
            "Append-only / no-lost-ack: records are physically removed for the first time (mitigated by snapshot-durable-before-drop + verified-archive-before-drop).",
            "entry_count-absolute anti-rollback: the classify_head `n < entry_count` guard must become `n < entry_count - base_count` or it collapses into a no-op that can't distinguish a legit prune from a live truncation.",
            "Genesis-rooted chain: the re-based live segment's first record links to base_prev_hash, not _GENESIS_PREV, and forks unless verify_chain is seeded via genesis_prev.",
            "v1 head byte-identical verification: adding six model fields breaks every persisted head's signing bytes unless _head_payload is version-conditional.",
            "Nine monotonic security-state high-waters (nonce replay floor, WARDEN count, account-create cap, device authorize/revoke, kill-switch latch, promotion grants, approval resolution, promotion-key idempotency): each silently regresses if its pruned-range contribution is not folded.",
            "Referential integrity: dangling parent_id/supersedes_id into the pruned range and the oldest-open-workflow floor.",
            "Crash-atomicity across five artifacts (snapshot record, archive, live manifest, head.json, floor.json) with a single well-defined commit instant.",
            "Fail-safe direction: every residual must be false-TAMPER, never false-clean.",
            "Cold-verifier bound: the archive + floor must arrive over the authenticated channel, never a mirror."
          ],
          "risks": [
            "Fleet flag-day: an un-upgraded (v1-code) verifier — notably an old mobile client — against a v2 head shows un-notarized (safe but degraded); the first prune must be gated on all verifiers running Slice-A code plus explicit owner sign-off.",
            "Owner key is FS-resident (checkpoint.py:9), so floor.json gives no protection against a same-host FS-write adversary who also rewrites floor.json and re-signs — a pre-existing limit for the warm case, but the prune adds the merkle/archive attack surface for the pruned region.",
            "The cold archive becomes a hard availability dependency for full-genesis verification: losing the offline archive makes the pruned prefix unrecoverable (only the merkle_root commitment survives); mitigated by per-segment sha256 in the archive manifest and the owner keeping >=1 copy.",
            "Fold-logic bugs are silent SECURITY regressions (a dropped device-revoke re-authorizes a revoked device, a dropped nonce-highwater permits replay); mitigated by the Slice-C empty-snapshot fold==scan byte-identical proof and per-bearer adversarial re-check before any deletion.",
            "The reconciler re-signs on open with the owner key; a read-only verifier that opens an interrupted prune stays in fail-safe TAMPER until `sigil sign` — noisy but never false-clean.",
            "resolve()/PrunedRef changes the read-contract at ~9 call sites; a caller that still expects None-means-absent could misread a PrunedRef — mitigated by keeping get() unchanged and migrating only the sites where pruned-vs-absent matters.",
            "Merkle-over-millions-of-leaves and the archive copy are one-time O(pruned) work under partial lock windows; bounded to whole-sealed-segments and run oldest-first with the heavy copy off-lock."
          ]
        },
        "verdicts": [
          {
            "design_name": "VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine",
            "tamper_evidence": {
              "score": 5,
              "why": "entry_count stays effectively ABSOLUTE and no keyless truncation-as-prune path exists for a warm verifier. The prune boundary (base_seq/base_count/base_prev_hash/merkle_root) is entirely inside the OWNER-SIGNED head, so a keyless attacker cannot mint a head with a different boundary; verify_head checks the signature over the v2 payload. The classify_head guard is correctly rewritten from `n < entry_count` to `n < entry_count - base_count`, which is NOT a no-op: a legit prune gives n==live_expected (clean) while a live-window truncation gives n<live_expected (fires), and base_count is authenticated so it can't be inflated to swallow a truncation. Anti-rollback of the PRUNED region is caught two independent ways: floor.json monotonic on BOTH last_seq and base_count (rejects lowering either) plus the prev_head_hash meta-chain (exactly one accepted descendant, linear = no fork). Cryptographically the pruned prefix is double-committed: base_prev_hash = entry_hash(K-1) IS the chain tip of [0..K) (recompute-from-genesis must land on it), and merkle_root independently binds every pruned leaf so a cold verifier re-derives base_count from the archived leaf count. verify_chain's genesis_prev seeding forces entries[0].prev_hash == base_prev_hash (checked at i=0), so the re-based live segment cannot fork to a forged prefix. Every failure direction I traced flags false-TAMPER, never false-clean — including an un-upgraded v1 client, whose hardcoded _GENESIS_PREV seed makes the first re-based record's prev_hash mismatch and fire TAMPERING. The one residual (cold verifier bootstrapping off an untrusted MIRROR getting a self-consistent old {spine,head,floor,snapshot} tuple) is fundamental to any prunable log with an FS-resident owner key (checkpoint.py:9), is class-identical to the pre-existing single-file rollback, and is genuinely bounded — not widened — by the COLD-ARCHIVE + authenticated-channel floor seed."
            },
            "crash_safety": {
              "score": 5,
              "why": "Clean single commit instant (os.replace of head.json, step 7) with a durable snapshot record fsync'd BEFORE any live drop (step 3) and every pruned record verified-present in the archive BEFORE the live manifest drops it (steps 4-6). No-lost-ack holds at every barrier: a crash leaves each acked record in the live spine (pre-6) or the verified archive (post-4). All crash windows fail in the SAFE direction: pre-step-7 reopens as false-TAMPER (v1 head anchoring pre-prune count vs a re-based live set) and the reconciler forward-completes deterministically from the durable snapshot record; a crash between step 7 and 8 reopens consistent because the new v2 head still satisfies the OLD floor (last_seq grew via the snapshot append, base_count grew 0->K, prev_head_hash==old head_doc_hash). Kill-9 crash-fuzz at each barrier is a required merge gate. I confirmed the reconciler trigger (manifest base_count>0 XOR head still v1) does not mis-fire on a crash BEFORE step 6, which leaves only a harmless orphan snapshot record + redundant archive."
            },
            "cold_verifier_exposure": {
              "score": 4,
              "why": "The COLD-ARCHIVE variant is a real improvement over delete-forever: the archive is a genesis-rooted, per-segment-sha256'd, re-attachable chain segment set (seg-0.first_prev_hash==_GENESIS_PREV, boundary_hash==next first_prev_hash, last boundary_hash==base_prev_hash), so a verifier holding it re-verifies the FULL chain from true genesis and independently re-derives base_count + merkle_root. The C1 honesty is exemplary: the design does NOT claim to close the cold-mirror full-tuple rollback, correctly identifies it as unclosable from data alone given the FS-resident key, and bounds it by transferring the archive + initial floor over the authenticated WireGuard pairing channel (never the mirror), collapsing the pruned-region blind spot to zero for any warm/returning-paired verifier. Not a 5 because two residuals remain: (a) a cold verifier with the mirror ALONE (no archive, no authenticated floor seed) is genuinely exposed — mitigated only by 'don't trust that case,' which is an operational not a cryptographic control; (b) full-genesis verification now hard-depends on archive availability — losing the single offline archive makes the pruned prefix unrecoverable, with only merkle_root surviving to prove what WAS there."
            },
            "consumer_correctness": {
              "score": 4,
              "why": "State-folding is enumerated bearer-by-bearer with the right fold algebra (max for nonce/warden high-water, add for create-cap counts, latest-verified-state for device/promotion, latch-OR for kill-switch, set-union for approvals/promotion-key, and correctly NOT folding supersession because supersedes_id is forward-only, nor open-workflow state which is protected by the oldest-open floor instead). The empty-snapshot-first proof (fold==pure-scan before any deletion) plus per-bearer adversarial re-check is the right gate, and every fold failure direction is fail-safe (dropped revoke would re-authorize -> so it IS folded; a dropped create-cap over-counts -> more restrictive). Q5 resolve()->PrunedRef (410, never bare 404) with a preserved get() signature is clean. Docked one point for a latent hazard I traced: SnapshotState.load is specified as 'the record at head.snapshot_seq OR the newest snapshot record.' The 'newest snapshot record' fallback is unsafe in the crash-before-step-6 orphan window — an appended-but-unpruned snapshot record would fold its [0..K) contribution against a live set that STILL contains [0..K), double-applying every count-bearer. It fails SAFE (the one non-idempotent bearer, create-cap, merely over-restricts) so it is not fatal, but it is an avoidable correctness landmine."
            },
            "implementability": {
              "score": 4,
              "why": "Every code claim checks out against the actual files: _head_payload version-conditional pop, verify_chain gaining genesis_prev (default _GENESIS_PREV keeps all existing callers byte-identical), verify_head's count check becoming entry_count != base_count + len(entries), classify_head's base_count-aware guard, and the try/except hardening of verify_checkpoint (currently a bare model_validate_json at checkpoint.py that WOULD crash a v1 model on v2 fields) + SpineTailer._resolve_head's silent (None,None) swallow becoming a distinct schema-ahead tri-state. The 'no segment bytes rewritten' re-base is correct because K is a whole-sealed-segment boundary, so the oldest remaining live record already carries prev_hash == entry_hash(K-1) == base_prev_hash. The 5-slice plan is well-sequenced (schema+floor+fold all proven at base_count=0 before the tamper-model-changing cutover). Docked for surface size and coordination cost: 9 bearer folds + ~9 resolve() call-site migrations + a fleet flag-day gating the first prune on all verifiers running Slice-A code, plus classify_head's head<->snapshot cross-check which as written implies an out-of-band store read (better done over the in-window `entries` to keep the function pure). None of these are blockers, but the margin for a silent fold bug is real."
            },
            "fatal_flaws": [],
            "verdict": "SHIP as the explicitly operator-gated, fleet-upgrade-gated, owner-key-gated deferred hard-prune tier. From the tamper-evidence lens this design is fail-CLOSED end to end: entry_count stays effectively absolute (base_count-aware comparison + dual-field monotonic floor), no keyless attacker can present a truncation as a legit prune against a warm/returning-paired verifier, the Merkle commitment + prev_head_hash meta-chain + durable floor + base_prev_hash chain-tip form a layered and mutually-reinforcing anchor, and every crash window plus every un-upgraded client flags false-TAMPER rather than false-clean. The only exposure (cold-verifier-with-mirror-alone full-tuple rollback) is fundamental, class-identical to the pre-existing single-file rollback, honestly disclosed, and genuinely bounded by the authenticated-channel COLD-ARCHIVE. No fatal flaw survives.",
            "best_change": "Pin SnapshotState.load strictly to head.snapshot_seq (the reconciled, owner-SIGNED authority) and delete the 'or the newest snapshot record on disk' fallback, so the fold base and the manifest-re-base cutover are bound to the SAME committed instant. Otherwise a crash between step 3 (snapshot record fsync'd) and step 6 (live manifest re-base) leaves an orphan snapshot record whose [0..K) fold gets double-applied against a live set that still physically contains [0..K); it happens to fail safe (only the non-idempotent create-cap bearer, and it over-restricts), but keying the fold to the signed head.snapshot_seq — which is still v1/-1 in that window, yielding an empty SnapshotState folded over the full scan — makes the fold provably consistent with the commit instant and removes the landmine entirely. (Secondary: make classify_head's head<->snapshot cross-check read the snapshot record from the in-window `entries` rather than an out-of-band store call, keeping the function pure.)"
          },
          {
            "design_name": "VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine",
            "tamper_evidence": {
              "score": 5,
              "why": "No false-clean survives my attack. The anti-truncation rewrite (n < entry_count - base_count) correctly closes the no-op trap, and crucially base_count/base_prev_hash are read from the SIGNED head, not the unsigned manifest (store.verify()'s use of manifest.base_prev_hash is only the unkeyed layer, consistent with the existing non-load-bearing-manifest doctrine at manifest.py). Live-window truncation → n<live_expected → TAMPER; re-based-fork → verify_chain seeded with head.base_prev_hash catches the first-live-record prev_hash mismatch. Pruned region is DUAL-guarded: floor.json base_count monotonic + prev_head_hash linear meta-chain (single parent, no fork) AND merkle_root re-derived from the actual archived record count. classify_head cross-checks head.{base_seq,base_count,merkle_root} == the immutable snapshot record at head.snapshot_seq, so the head can't claim a different boundary than the chained commitment. Version-conditional _head_payload (pop the 6 keys when schema_version<2) genuinely restores byte-identical v1 signing bytes against the real chain.py:50 code. Only residual is the acknowledged, un-widened FS-resident-key rollback."
            },
            "crash_safety": {
              "score": 4,
              "why": "The single-writer walk is sound: commit instant is singular (os.replace of head.json, step 7, via the audited atomic_write_text temp→fsync→replace→dir-fsync); no-lost-ack proof holds (snapshot fsync'd before any deletion, every pruned record copied+VERIFIED into the archive at steps 4-5 before the live manifest drops it at step 6, physical unlink deferred to reader-grace GC); the one intermediate (manifest re-based + head still v1 → n=N-K<live_expected=N) fails SAFE (false-TAMPER) with a deterministic forward-completing reconciler off the durable snapshot record; crash 7→8 reopens consistent against the old floor. But TWO intermediates are under-specified. (1) LOST-ACK vector: the flock is RELEASED for the off-lock archive copy (step 4); a concurrent seal-swap can add a segment+bump generation during that window, and if step 6 builds the re-based Manifest from the pre-copy in-memory self._manifest (as _seal_active_locked does) it omits the rotation's new segments → orphaned acked records + generation collision. Must re-read the manifest under the re-acquired flock and set-difference. (2) A crash between snapshot-append (step 3) and manifest re-base (step 6) leaves an abandoned head-unreferenced snapshot record the reconciler's base_count>0 detector never cleans. Both fixable, neither fatal, but both must be in the kill-9 gate."
            },
            "cold_verifier_exposure": {
              "score": 5,
              "why": "Honest and correctly bounded — this is the strongest dimension and the reason the COLD-ARCHIVE variant is the right call. It correctly identifies the residual as a full-tuple {short spine, re-signed v2 head, rolled-back floor.json, matching snapshot record} rollback that is unclosable FROM DATA ALONE (floor.json is a mirror-controlled file, owner key is FS-resident per checkpoint.py:9's accepted §1.3 simplification), and correctly argues it is NOT widened beyond the pre-existing single-file rollback class. The archive collapses the pruned-region blind spot to zero for any verifier holding both the archive and an authenticated-WireGuard-channel floor seed: re-derive base_count from the ACTUAL archived record count, recompute merkle_root over the real leaves, re-verify from true genesis (base_prev_hash == archive last boundary_hash). The archive is a genesis-rooted chain segment set (seg-0.first_prev_hash=_GENESIS_PREV, each boundary==next first_prev). The only irreducible residual (mirror-alone, no archive/no channel) is exactly the case the owner is told not to trust."
            },
            "consumer_correctness": {
              "score": 4,
              "why": "resolve()→PrunedRef with get() kept byte-identical (SpineRecord|None) is the right split; 410-Gone-not-404 with a {base_seq,snapshot_seq,fetch-hint} pointer is honest; the referential floor guarantees no OPEN workflow's resolve() ever lands in the pruned range so the ~9 migrated call sites hit PrunedRef only for closed history. The 9-bearer fold enumeration is thorough and each is individually fail-safe-reasoned (max/latest-state folds are idempotent; kill-switch stays engaged; a dropped revoke re-authorizes → so mandatory). The supersession no-fold argument (forward-only supersedes_id) is correct. BUT the stated SnapshotState.load resolution 'reads the snapshot record at head.snapshot_seq (or the newest snapshot record)' is unsound: the newest-record fallback, crossed with the step-3→step-6 crash window, folds an abandoned head-unreferenced commitment on top of the still-live [0..K) records, double-counting the one NON-idempotent additive bearer (account.create cap → creation_allowed denies too early). Fail-safe direction but a real regression. Load must key strictly on head.snapshot_seq, empty when v1."
            },
            "implementability": {
              "score": 4,
              "why": "Well-grounded in the actual code: reuses atomicio.atomic_write_text, the _crossproc_lock flock, the manifest generation-swap pattern, and the existing _crash_hook / _maybe_crash fault-injection harness for the kill-9 gate. The version-conditional _head_payload is a small precise change to chain.py:50; the 6 defaulted fields on SignedChainHead with extra='forbid' behave as claimed (old v1 clients ValidationError→un-notarized, safe). The A→E slice plan is genuinely default-safe (base_count=0 everywhere → byte-identical, empty-snapshot-first proves fold==scan before any deletion). Realistic but LARGE: state-folding touches 9 security-critical consumers where a fold bug is a silent (fail-safe-direction) regression, and the off-lock/flock-reacquire discipline in the cutover must be implemented exactly. verify_checkpoint hardening (try/except around checkpoint.py:86) and the SpineTailer tri-state (tail.py:110) are necessary and specified. Deducting for the two under-specified intermediates and the sheer 9-consumer surface."
            },
            "fatal_flaws": [],
            "verdict": "CONDITIONAL PASS — ship Slices A–D, gate Slice E (the tamper-model-changing cutover) on two must-fix crash/concurrency items. As the crash adversary I found NO false-clean, NO double-commit, and NO single-writer lost-ack: the commit instant is singular (os.replace of head.json, step 7), the pruned records are copied+verified into the archive before the live manifest drops them, and the sole dangerous intermediate (manifest re-based while head is still v1) fails in the SAFE direction with a deterministic forward-completing reconciler. The anti-truncation rewrite, the head-sourced (not manifest-sourced) base fields, the floor+merkle dual guard on the pruned region, and the version-conditional signing payload are all correct against the real code. But two intermediate states are under-specified and, resolved naively, each breaks a cardinal rule — a lost acked record (concurrent seal-swap during the off-lock archive copy) and a fail-safe-but-wrong double-count (abandoned snapshot record + newest-record fold fallback). Both are trivially closable within the stated structure; close them and add both as barriers to the kill-9 crash-fuzz merge gate before the first prune.",
            "best_change": "Close the off-lock gap in step 6 (the lost-ack vector — highest severity in my adversary lane). The flock is RELEASED for the heavy archive copy (step 4), so a concurrent append can trigger a seal-swap that appends a NEW sealed segment + new active ABOVE K and bumps the on-disk manifest to generation g+1 during that window. If step 6 builds its re-based Manifest from the prune's pre-copy in-memory self._manifest (generation g) — exactly what _seal_active_locked does with `m = self._manifest` — write_manifest publishes a generation-g+1 manifest that OMITS the rotation's new segments, orphaning acked records that are in neither the live view nor the archive: a true LOST ACK plus a generation collision. Fix: step 6 MUST re-read the manifest fresh under the RE-ACQUIRED flock (mirror _refresh_active_under_lock) and compute the new segment set as (fresh manifest.segments) MINUS (archived-below-K set), never a captured list; since a rotation only ADDS segments this set-difference is provably safe. Bundle the coupled second fix: SnapshotState.load must resolve STRICTLY via head.snapshot_seq (empty state when the head is v1/snapshot_seq==-1), never the stated 'or the newest snapshot record' fallback — otherwise a crash between the snapshot append (step 3) and the manifest re-base (step 6) leaves a durable, head-unreferenced snapshot record (invisible to the reconciler's base_count>0 detector) that the fallback folds on top of the still-live [0..K) records, double-counting the account.create cap and decoupling the fold base from the signed commitment. Add barriers at snapshot-append and concurrent-rotation-during-copy to the existing _crash_hook harness as required Slice-E merge gates.\""
          },
          {
            "design_name": "VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine",
            "tamper_evidence": {
              "score": 5,
              "why": "The core tamper model is coherent and fail-safe. entry_count stays ABSOLUTE and the anti-truncation guard is correctly rewritten from checkpoint.py:71's `n < signed_n` (which would collapse to a no-op once the live window is sliced) to `n < entry_count - base_count`, so a legit prune (n==live_expected) is clean while a real live truncation still fires — this is the exact trap the spec flagged, and it's closed. C2 is real: merkle_root over [entry_hash(0)..entry_hash(K-1)] is bound into the chain via the snapshot record's cert_digest (reuse/chain._entry_hash) and then notarized by the owner-signed head, so the prune boundary is tamper-evident even between `sigil sign` runs. Pruned-region rollback is caught by two independent guards (durable floor monotonic base_count + prev_head_hash meta-chain; merkle_root re-derived from the archived leaf count). base_prev_hash is triangulated across the head field, the snapshot payload, and the first live record's prev_hash (verify_chain seeded via the new genesis_prev param, whose i>0 seq-contiguity check at chain.py:44 correctly does NOT fork the re-based window). Every residual flags false-TAMPER, never false-clean. Minor: the claimed classify_head cross-check of head-fields vs the snapshot RECORD payload is not reachable at classify_head's current signature (it only gets ChainEntry, no payload) — but this is defense-in-depth on top of the head's own signed v2 fields + transitive head_hash binding, so its awkward plumbing doesn't open a tamper hole."
            },
            "crash_safety": {
              "score": 5,
              "why": "Ordering is sound and the commit instant is singular and correct: (3) fsync the durable snapshot record BEFORE any deletion, (4-5) copy+verify the full genesis..K-1 chain into the archive BEFORE any live drop, (6) atomic manifest re-base, (7) os.replace(head.json) = THE commit instant, (8) advance floor. No-lost-ack holds: at every barrier each acked record is either still live (pre-6) or verified-in-archive (post-4). The false-TAMPER window between step 6 (manifest re-based, base_count>0) and step 7 (head still v1, base_count=0 → n<live_expected fires) is explicitly the SAFE direction. The reconciler forward-completes deterministically from the durable snapshot record (which committed base_seq/base_prev_hash/base_count/merkle_root/folded_state) and re-signs with the FS-resident owner key; a read-only verifier that can't re-sign stays fail-safe TAMPER, never false-clean. Kill-9 crash-fuzz at each barrier mirrors the existing _maybe_crash harness (store.py:58) and is a merge gate. Orphaned physical segment files after a crash between re-base and unlink are benign (disk not fully reclaimed; GC-able), not a safety issue."
            },
            "cold_verifier_exposure": {
              "score": 5,
              "why": "The adversary analysis is precise and honest, and matches my independent read. A PURE mirror adversary (no owner key — the normal sync never ships ~/.sigil/keys/owner.priv) CANNOT fabricate a forged prune that drops real records: forging a NEW valid v2 snapshot head with a bogus merkle_root/base_count requires the Ed25519 owner signature. So the only feasible mirror attack is REPLAY of a genuinely-owner-signed OLDER (spine+head+floor+snapshot) tuple rolled back together — a stale-not-tampered state. From DATA ALONE a brand-new cold verifier with no prior high-water and no authenticated anchor cannot establish FRESHNESS; this full-tuple rollback is fundamental to any prunable log and is correctly identified as the irreducible residual, NOT widened beyond today's pre-existing single-file rollback. The bound is correctly split: the authenticated-channel FLOOR seed closes freshness (a returning/paired phone's _high_water rejects any last_seq/base_count below it), and the authenticated-channel ARCHIVE closes pruned-region CONTENT (re-derive base_count from the actual archived record count, recompute merkle_root over the real leaves, re-verify from true genesis since base_prev_hash == the archive's last boundary_hash). The design correctly requires BOTH — archive alone proves what-was-pruned but not freshness. Slight phrasing muddle: c1_honest_limit says the adversary 're-signed with the FS-resident owner key', conflating a same-host key-compromise with a pure mirror; for the pure-mirror case the residual is even tighter (replay-only, no forged prune). But the conclusion — the mirror-only, no-archive, no-authenticated-floor verifier is the exact case the owner is told not to trust — is the correct answer."
            },
            "consumer_correctness": {
              "score": 4,
              "why": "Broad and mostly correct. All nine monotonic bearers get a fold contract with the correct DIRECTION and each preserves fail-safe on a miss (max-fold for nonce high-water, latest-verified-state for device authorize/revoke and promotion grants, add for account-create caps, latch-True for kill-switch, unions for approvals/promotion-key ledger, >= continuation for WARDEN) — and the design is honest that a MISSED fold regresses UNSAFE (dropped revoke re-authorizes, dropped nonce permits replay), which is why the empty-snapshot fold==pure-scan byte-identical proof BEFORE any deletion plus per-bearer adversarial re-check is the right gate. The forward-only supersession argument (revise.py) correctly shows no double-serve. resolve()->PrunedRef (never bare None, 410 not 404) with get() kept byte-identical for the hot path is clean, and the referential floor (oldest-open-workflow + dangling-binding + never-mutate-retained) guarantees no OPEN workflow's resolve() lands in the pruned range. Deduction: the SECOND-and-later prune's derivation source is under-specified. Folds are monotonic-composable so prune#N can compose from SnapshotState.load(prune#N-1) + the delta [K_{N-1}..K_N) live records — but the crash-safe step 2 says 'scan the to-be-pruned sealed segments', which at prune#2 is only the delta, so composing-from-prior-snapshot must be stated (a naive from-[0..K) rescan would need the offline archive mounted). Left implicit, an implementer could get the fold source wrong."
            },
            "implementability": {
              "score": 4,
              "why": "Grounded in the real code and staged well: five slices, each with a concrete byte-identical merge gate (Slice A/base_count=0 == v1; Slice C empty-snapshot fold==scan; Slice D archive-round-trip with no live drop; Slice E the tamper-model-changing cutover behind kill-9 fuzz + fleet-upgrade + owner sign-off). The version-conditional _head_payload, verify_chain genesis_prev param, verify_head count/seed edits, and the SpineTailer tri-state are all precisely located and correct against chain.py/checkpoint.py/tail.py. Deductions: (1) classify_head's claimed head-vs-snapshot-record cross-check needs a signature change to receive the record payload (ChainEntry carries none) — it's currently pure(head, entries, tr); (2) the cumulative merkle_root over [0..K_N) at a SECOND prune, as literally specified ('over the real pruned leaves'/'from true genesis'), requires all prior archived leaves to be online at prune time unless an incremental accumulator is committed — the design doesn't specify one; (3) large blast radius: 9 consumer migrations, 9 resolve() call sites, a flag-day fleet upgrade where old v1 mobile clients hit extra=forbid and degrade to un-notarized (safe but visible). All completable, none blocking, but non-trivial."
            },
            "fatal_flaws": [],
            "verdict": "APPROVE (build the slices in order; do NOT run Slice E's live drop until the best-change and the two implementability nits below are specified). This is the safest sound design for the COLD-ARCHIVE variant: it reclaims live file size + read latency while keeping the archive as the recoverability/re-verification proof, and every residual failure flags false-TAMPER never false-clean. The headline adversary question is answered correctly and honestly — a pure mirror cannot feed a cold verifier a FORGED prune (that needs the owner key), only a genuine stale rollback, and the authenticated-channel floor (freshness) + archive (pruned-region content) collapse the blind spot for any verifier holding both; the mirror-only-no-archive verifier is the fundamentally-unclosable residual the owner is told not to trust. Tamper-evidence and crash-safety are effectively airtight (the classify_head no-op trap is fixed, entry_count stays absolute, the commit instant is singular, no acked record can be lost). The only real gap is the multi-prune derivation of the cumulative merkle_root/folded_state; consumer-correctness and implementability carry the modest 4s for that plus the classify_head payload-plumbing nit.",
            "best_change": "Specify the SECOND-and-later prune's cumulative commitment so it never depends on the offline archive being mounted: commit an INCREMENTAL merkle accumulator (a Merkle Mountain Range peak set, or a running chained root H_K = H(H_{K-1} || entry_hash(K-1))) inside each snapshot record, so prune#N derives merkle_root by composing snapshot#(N-1)'s committed accumulator with only the delta leaves [K_{N-1}..K_N) — matching how folded_state already composes monotonically from SnapshotState.load(prior) + the delta. Then state, in one sentence, that both merkle and folds at every prune are derived as load(prior-snapshot) + fold/absorb the newly-pruned delta segments (never a from-genesis rescan), and that a verifier holding the full archive still re-derives the same accumulator from the real leaves. This removes the only latent correctness dependency (needing all prior archived leaves online at each prune) and makes the multi-prune story fully deterministic. Second-order: change classify_head's signature to also receive the snapshot SpineRecord (or move the head-vs-snapshot cross-check into verify_checkpoint) so the claimed cross-check is actually reachable.\""
          },
          {
            "design_name": "VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine",
            "tamper_evidence": {
              "score": 4,
              "why": "Sound and mostly closed. head-v2 six fields + version-conditional _head_payload (drop the six when schema_version<2) preserves byte-identical v1 verification; classify_head cross-checks head.{base_seq,base_count,merkle_root} against the immutable kind=snapshot record at head.snapshot_seq so the head can't claim a different boundary than the hash-chained commitment; entry_count stays absolute; the anti-truncation guard correctly becomes n<entry_count-base_count (not the no-op n<entry_count); pruned-region rollback is caught by two independent guards (floor.json monotonic last_seq/base_count + prev_head_hash meta-chain, AND merkle_root re-derived from archived leaves). Verified against reuse/chain.py verify_chain(prev seed) and checkpoint.py. Not a 5 only because the residual FS-resident-owner-key full-tuple rollback (checkpoint.py:9) stays open — honestly acknowledged, not widened, but real."
            },
            "crash_safety": {
              "score": 4,
              "why": "Well-ordered with a single defined commit instant (os.replace of head.json, step 7) and a deterministic forward-completing reconciler that re-derives the v2 head from the already-fsync'd snapshot record (which committed base_*, merkle_root, folded_state). No-lost-ack holds: snapshot fsync'd before any drop (step 3) and every pruned record is in the verified archive (steps 4-5) before the live manifest drops it (step 6); a crash at any barrier leaves each acked record in live (pre-6) or archive (post-4). A crash between 6 and 7 reports FALSE-TAMPER (n<live_expected with head still v1 base_count=0) — the safe direction. Kill-9 fuzz at each barrier (mirroring store.py:55 _maybe_crash) is a stated merge gate. Minor deduction: the reconciler re-deriving folded_state must read the durable snapshot's cumulative fold, which ties into the multi-prune composition gap noted under consumer correctness."
            },
            "cold_verifier_exposure": {
              "score": 4,
              "why": "Honestly bounded, does not overclaim. The archive (full genesis..K-1 chain) plus initial floor are seeded over the authenticated WireGuard pairing channel, never a mirror; a verifier holding the archive re-derives base_count from the actual archived record count, recomputes merkle_root over the real pruned leaves, and re-verifies from true genesis (base_prev_hash == archive last boundary_hash), collapsing the pruned-region blind spot to zero. The irreducible residual — a cold verifier with an untrusted mirror ALONE (no archive, no authenticated floor) getting a self-consistent rolled-back {spine,head,floor,snapshot} tuple — is correctly identified as fundamental to any prunable log with an FS-resident key and NOT widened beyond the pre-existing single-file rollback exposure. Correct that warm/same-host/returning-paired verifiers are fully closed by the persisted floor + SpineTailer._high_water."
            },
            "consumer_correctness": {
              "score": 4,
              "why": "As the transparency judge: 7 of 9 bearers fold EXACTLY and fail-safe, verified against code — nonce max-fold (device_nonce_highwater), warden max-fold, device authorize/revoke last-write-fold with verify_signed filter (the security-critical latest-in-pruned REVOKE is preserved, no re-auth), kill-switch boundary-state left-fold (dropped release stays engaged = fail-safe halt), promotion last-write-fold, archivist ledger union-fold, approvals resolved-union (redundant under the oldest-open floor, harmless). Supersession correctly needs NO fold: iter_current builds superseded from live recs and supersedes_id is strictly backward, so no double-serve. Q5 resolve is HONEST: get() stays Record|None and every get-based gate fails safe (action_approved None->deny, _target None->already-resolved); no security consumer is permissive-on-None (all are scan/fold-based); display/read sites migrate to resolve()->PrunedRef->410 Gone, never a misleading 404. TWO deductions from a 5: (a) the account.create cap fold is INEXACT — the live predicate is service==S OR origin==O, but splitting into independent by_service/by_origin counters reconstructs |S|+|O| >= |S∪O|, an overcount (over-restrictive, fail-safe, never a bypass; identical behavior at the default cap=1) rather than the same value; (b) the multi-prune cumulative composition is under-specified (see best_change)."
            },
            "implementability": {
              "score": 4,
              "why": "Large but cleanly decomposed into 5 slices each with a concrete merge gate, and it reuses existing machinery precisely: atomicio.atomic_write_text, the _crossproc_lock, the _maybe_crash kill-9 harness (store.py:55), gzip byte-preservation for zero-rewrite re-base, and the reuse/chain.py verify_chain/verify_head/sign_head seam. The load-bearing change (version-conditional _head_payload) is small and exact. Fold contracts are enumerable and each maps to a real consumer function I confirmed exists. Deductions: the multi-prune folded_state composition and the cap inclusion-exclusion both need tightening before Slice C/E, and SnapshotState + kind=snapshot are greenfield (confirmed absent today), so the empty-snapshot fold==scan byte-identical proof is doing a lot of load-bearing verification work."
            },
            "fatal_flaws": [],
            "verdict": "APPROVE WITH CONDITIONS. On the consumer-transparency axis the design is sound: after a single prune all nine bearers reconstruct their security-relevant value via SnapshotState.load + fold(iter_records), and every fold that could regress a security floor (nonce replay, device revoke, kill-switch latch, promotion revoke) is fail-safe. Q5 resolve is honest for every old-seq consumer, with get() kept fail-safe and read/display sites returning PrunedRef/410 rather than a misleading 404. Two non-fatal conditions must be closed before the tamper-model-changing Slice E: (1) specify that folded_state is CUMULATIVE across prunes — computed as fold(prior snapshot state, newly-pruned records), not a fresh live-scan of [0..K) — or prune #2 silently drops the pre-old_K nonce/revoke/cap state (a real security regression, though trivially fixable given the folds are associative and the read contract already assumes cumulative); (2) make the account.create cap fold exact (store the OR-predicate as distinct-record counts, or accept and document the |S|+|O| overcount as an intentional over-restrictive approximation). Neither survives as fatal — both are safe-direction and fixable without redesign.\"",
            "best_change": "Nail down MULTI-PRUNE cumulative fold composition: cutover step 2 says compute folded_state and merkle_root over [entry_hash(0)..entry_hash(K-1)], but on the 2nd+ prune records [0..old_K) are already in the cold archive and unscannable from the live set. Specify that each prune computes folded_state = fold(SnapshotState.load(prior_snapshot), newly-pruned live records [old_K..new_K)) so the newest snapshot's folded_state always covers the full [0..K_new) — which is exactly what SnapshotState.load's read contract (newest snapshot's folded_state, then consumers fold live on top) already assumes. Likewise the merkle_root must chain the prior archived leaves (or commit a merkle-of-merkles) rather than re-hash from 0. Without this, a literal fresh live-scan on prune #2 loses every earlier nonce high-water, device-revoke, cap count and kill-latch history — a silent replay/re-authorization/un-halt regression. The fix is trivial because all nine folds are associative (max/union/last-write/count-add/boundary-latch), but it must be stated as the write-path invariant, and the empty-snapshot fold==scan proof in Slice C should be extended to a two-prune (fold-of-fold == full-scan) proof."
          }
        ]
      },
      {
        "design": {
          "name": "HARD-PRUNE (cold-archive, MAX-RECLAIM)",
          "one_line": "Prune the oldest sealed segments out of the LIVE spine behind an owner-signed, Merkle-committed kind=\"snapshot\" anchor + v2 head, MOVE the pruned gz segments to a re-verifiable archive/ (the recoverability + genesis-verify proof), re-base the live manifest/verify to the snapshot, and fold all ~9 monotonic bearers into the snapshot so live-only scans stay correct — every crash window and every attack resolves in the false-tamper (never false-clean) direction.",
          "snapshot_anchor": "A kind=\"snapshot\" spine record (add to KINDS in spine/models.py) is appended to the LIVE active segment like any record (digest_payload over content, real entry_hash, fsynced+acked) so it is itself chained and covered by verify()/the head. Its payload commits, over the pruned prefix [0..K): base_seq=K (first RETAINED seq); base_prev_hash=entry_hash(K-1) (the last pruned record's hash — the synthetic genesis-prev the re-based live set links from; for a contiguous genesis chain base_count==base_seq==K, cross-checked as a guard); base_count=K (absolute count of pruned records); merkle_root=Merkle root over the leaves [entry_hash(0)..entry_hash(K-1)] (leaf=entry_hash, binary tree, duplicate-last on odd — C2); snapshot_seq=its own seq S (K<=S); prev_head_hash=sha256_hex(evidence_signing_bytes(_head_payload(prior_accepted_head))) (the meta-chain link); plus the folded SnapshotState (state_folding). These same six commit fields are ALSO copied onto SignedChainHead v2 so verify never has to trust the record's own payload — the head (owner-signed) is authority, the record is the durable, replayable INTENT. Descent from exactly one prior head: on accept, require the new head's prev_head_hash == sha256 of the currently-accepted head stored in floor.json/head.json; combined with floor monotonicity this makes the head lineage a hash chain with exactly one predecessor (no fork, no skip, no replay). The meta-chain is verified in classify_head/verify_checkpoint against floor.prev_head_hash and by re-hashing the persisted predecessor head.",
          "head_v2_migration": "SignedChainHead (reuse/models.py) keeps extra=\"forbid\" and gains six defaulted fields: base_seq:int=0, base_prev_hash:str=_GENESIS_PREV, base_count:int=0, merkle_root:str=\"\", snapshot_seq:int=-1, prev_head_hash:str=\"\" (and schema_version bumps 1->2 only when a snapshot is committed; entry_count STAYS ABSOLUTE = base_count + live). The blocking gate is version-conditional signing payload: _head_payload (chain.py:50) must DROP the six fields when schema_version<2 — `if d.get(\"schema_version\",1)<2: for f in (base_seq,base_prev_hash,base_count,merkle_root,snapshot_seq,prev_head_hash): d.pop(f,None)` — so an existing v1-signed head re-dumped under the v2 model produces byte-identical signing bytes and verifies unchanged (do NOT use model_dump(exclude_unset=True): model_validate_json marks every field 'set', so it would re-include the defaults and false-alarm every deployment to TAMPERING). sign_head and verify_head both route through _head_payload so v1/v2 stay symmetric. Harden verify_checkpoint (checkpoint.py:86 has no try/except): wrap model_validate_json + classify_head in try/except returning (False,\"anchor unverifiable / schema too new — upgrade\") — a v2 head fed to a v1-only client raises pydantic ValidationError (extra fields forbidden) and today crashes it. Harden SpineTailer._resolve_head (tail.py:110-119, which swallows ALL exceptions to (None,None) = \"un-notarized\", a false-CLEAN degrade): peek schema_version first; if it exceeds the client's max, return a DISTINCT \"schema too new — upgrade\" sentinel that check_anchor surfaces as anchor_ok=False + upgrade_required=True (never the benign \"un-notarized live tail\"). Old mobile clients vs a v2 head: MUST fail closed to \"upgrade required\", never \"clean/un-notarized\"; and the first prune (first v2 head emission) is a fleet-upgrade FLAG DAY — gate it behind every mesh device advertising max_head_schema>=2 (piggyback on mesh/registry capability_map) so no paired client is left unable to verify.",
          "verify_snapshot_aware": "verify_chain (chain.py:37) gains genesis_prev:str=_GENESIS_PREV and seeds prev=genesis_prev instead of the hardcoded _GENESIS_PREV; all v1 callers pass the default => byte-identical. verify_head gains the same passthrough. classify_head (checkpoint.py:65) becomes snapshot-aware: it selects the LIVE WINDOW BY SEQ, not by array-prefix — live=[e for e in entries if e.seq>=head.base_seq] — and computes expected_live=head.entry_count-head.base_count. THE anti-truncation guard that survives pruning: `if len(live) < expected_live: return TAMPERING (truncated/rolled back)` — this is NOT the no-op `n<entry_count` (which post-prune is always true => false-tamper) and NOT a disabled check (which hides real truncation); it compares the live count against the SIGNED expected_live. Then require live and live[0].seq==head.base_seq and live[0].prev_hash==head.base_prev_hash (a front-truncation makes min live-seq>base_seq or breaks the prev-linkage => TAMPERING), and verify_head(head, live[:expected_live], tr, genesis_prev=head.base_prev_hash) (a tail-truncation makes head_hash!=last entry_hash; a middle deletion breaks verify_chain contiguity). entry_count stays ABSOLUTE so a rotated/pruned store still exposes the true physical height. Selecting by seq (not prefix) is what makes every crash cutover window verify cleanly (see crash_safe_cutover). n>expected_live stays the benign \"appended since — run sigil sign\" branch.",
          "durable_floor": "~/.sigil/floor.json (0600), located at SIGIL_HOME/floor.json — OUTSIDE spine/ so SpineStore.reset()/`sigil ingest --reset` (which rmtrees spine/ + segments) can NEVER lower the anti-rollback floor. Contents: {schema_version, last_seq, base_seq, base_count, head_hash, prev_head_hash, snapshot_seq, merkle_root}. Update is MONOTONIC and upward-only, via atomic_write_text, and ONLY after a new head+manifest have committed: refuse to write a floor whose last_seq<old, base_count<old.base_count, or base_seq<old.base_seq. Reject rules (enforced in classify_head/verify_checkpoint and mirrored into SpineTailer._high_water): reject any head/snapshot with last_seq<floor.last_seq (rollback) OR base_count<floor.base_count OR base_seq<floor.base_seq (un-prune/older-snapshot replay), and require head.prev_head_hash==sha256(evidence_signing_bytes(_head_payload(the head floor names))) — the prev_head_hash meta-chain, so a new head must descend from exactly the last accepted head. Honest limit: a cold verifier bootstrapping off an UNTRUSTED MIRROR has no local floor and can be handed an internally-consistent (v2 head + live + archive) triple that is actually an OLD owner-signed state (a full rollback to a prior snapshot) — this is unclosable from data alone for ANY prunable log. Bounded by cold-archive: the archive is handed over the AUTHENTICATED WireGuard device-pairing channel and the initial floor {last_seq,base_count,head_hash,prev_head_hash} is SEEDED over that same channel, never from the mirror; given that one authenticated seed the verifier rejects every rollback below it, and the archive independently proves genesis->tip INTEGRITY. Residual = only the freshness of a verifier that NEVER authenticates the floor even once.",
          "cold_archive_move": "MAX-RECLAIM retention: keep only the last N=1 sealed segment + the active segment LIVE (config SIGIL_SPINE_LIVE_SEALED, default 1); everything older moves. Only WHOLE sealed segments move (never split one), so K = first_seq of the oldest RETAINED sealed segment (a clean seam boundary => base_prev_hash = that segment's first_prev_hash = the moved segment's boundary_hash, no record is ever rewritten). Archive layout: ARCHIVE_DIR = SIGIL_HOME/spine/archive/ (relocatable to an external/offline mount via SIGIL_SPINE_ARCHIVE_DIR); moved segments stay gzip (already compressed); an APPEND-ONLY archive/archive.manifest.json accumulates across successive prunes (never destructively rewritten) with one entry per archived segment {id, file, first_seq, last_seq, first_prev_hash, boundary_hash, sha256} plus one entry per snapshot {snapshot_seq, base_seq, base_count, merkle_root, prev_head_hash, head_hash}. Live manifest re-base: after the segments are COPIED into archive/ and the archive manifest fsynced, atomic_write_text a new live Manifest (generation++) that drops the archived segments, keeps [K..tip], sets the oldest retained segment's first_prev_hash=base_prev_hash, and records base_seq/base_prev_hash/base_count/snapshot_seq/merkle_root as advisory manifest fields (authority stays the signed head). Re-attach for FULL genesis verify: `sigil spine verify --with-archive <dir>` concatenates archive segments in seq order (from archive.manifest) THEN the live segments -> one genesis-rooted stream -> verify_chain(all, genesis_prev=_GENESIS_PREV); cross-checks that the last archived boundary_hash == live base_prev_hash and that the Merkle root recomputed over the archived entry_hashes == every snapshot's committed merkle_root. So integrity is provable from genesis for anyone handed the archive.",
          "state_folding": "New SnapshotState (typed) is serialized into the snapshot record payload and loaded by SnapshotState.load(store) (reads the newest kind=\"snapshot\" record; empty/zero default when none). Every ~9 full-scan consumer changes from an empty accumulator to a SEEDED one — `st=SnapshotState.load(store)` then fold over store.iter_records() (which now yields only LIVE records). Exact per-bearer fold over pruned [0..K): (1) envelope.device_nonce_highwater -> nonce_highwater:{device_pubkey:max_nonce}; consumer seeds hi=st.nonce_highwater.get(device,-1) (was -1). (2) governor/killswitch._scan_engaged -> killswitch_engaged:bool = the verified latch state at K-1 (engage=any, release=owner-signed-verified); consumer seeds engaged=st.killswitch_engaged (was False). (3) mesh/registry.authorized_devices -> authorized_devices:{device_pubkey:\"authorized\"|\"revoked\"} latest-verified at K-1, AND capability_map -> host_caps:{host_id:{...}} latest-verified; consumers seed the state dict from st then override with live. (4) agents/actor_scope.creation_allowed -> account_create_counts:{service_key|origin_key:n} count of status=\"applied\" account.create in the pruned range; consumer n=st.account_create_counts.get(key,0)+live count. (5) governor/promotion.is_promoted -> promotions:{\"agent\\x00scope\":\"granted\"|\"revoked\"} latest-verified at K-1; consumer seeds state from st. (6) agents/approvals.pending -> resolved_targets:[seq...] (verified approvals whose target_seq<K) and open_baseline:[] (empty by the referential-integrity floor, asserted); consumer seeds resolved=set(st.resolved_targets). (7) consolidate/revise.iter_current + promotion_ledgers -> current_archivist:[{seq,entry_hash,kind,ts,supersedes_id,subject,quote,statement,grounding,alpha,beta,source_seqs,due_iso,owner,promotion_key}] = the pruned archivist records still NON-SUPERSEDED at K-1 (they must keep appearing, with citations intact), plus grounded_keys:[...] and refused_keys:[...] for idempotency; consumer seeds the current list + ledgers from st then folds live supersessions. (8) consolidate/queries open_threads/due_commitments/contradictions are a VIEW over iter_current -> automatically covered by (7), no separate fold. (9) warden_checkpoint (cli.py:284, dashboard.py:29) -> warden_checkpoint:{latest record fields per pubkey} = the newest warden_checkpoint in the pruned range (monotonic anti-rollback cross-anchor); consumer newest=max(st.warden_checkpoint, live). SnapshotState.load also exposes base_seq/base_count so consumers know the live floor.",
          "referential_integrity": "Three prune-point guards run under the flock BEFORE anything moves; failing any ABORTS the prune (no bytes touched). (a) Dangling parent/supersedes: scan the retained [K..tip] records for any parent_id or supersedes_id < K; each must resolve — either it names a pruned record still projected in SnapshotState.current_archivist (an archivist supersession the fold already collapses) or it is resolvable from the archive (cold-archive keeps it a PrunedRef, so consumers like consolidate/gate.py:93 store.get(s) and transcript parent_id derefs still work). Refuse if a retained record has an UNRESOLVED dangling binding into the pruned range that is neither folded nor archived. (b) Oldest-open-workflow floor: K <= min(seq of every currently-open workflow) — computed as min over pending() queued-but-unresolved approval seqs and any open thread/commitment source seq — so no open workflow's originating record is ever pruned; this is what guarantees approvals fold (6) has zero items crossing the boundary. (c) Never mutate a retained record's binding: the re-base rewrites NO record bytes — base_prev_hash equals the ALREADY-STORED prev_hash of the first retained record (it was written that way originally at seq K), so verify links from base_prev_hash without any edit to prev_hash/parent_id/supersedes_id; the manifest re-base and head re-sign are the only writes.",
          "q5_resolve": "store.get(seq) keeps returning None ONLY for seq beyond the tip (genuinely nonexistent). For a pruned seq (0<=seq<base_seq, from the head/manifest base fields) add resolve(seq) -> SpineRecord | PrunedRef where PrunedRef={pruned:True, seq, base_seq, snapshot_seq, archive_hint} — never a bare None/404. The ~9 old-seq resolvers (actor_gate, actor, operator, perception/egress, bridge/server /api/record, ui/server /api/record, mcp episodic_range, consolidate/gate.py:93, transcript parent_id) map PrunedRef to a 410 Gone \"compacted below N — see snapshot X / archive\" instead of a 404. Archive fetch path: resolve(seq, with_archive=True) (or get_archived(seq)) reads archive/archive.manifest.json -> the owning archived segment by first_seq/last_seq range -> gzip-decompress -> returns the byte-identical SpineRecord whose entry_hash is independently checkable against the snapshot's merkle_root (a Merkle inclusion proof over the archived leaves), so a drilled-in citation is still cryptographically verifiable even though it left the live set.",
          "crash_safe_cutover": "All under the path-stable flock. PHASE A (durable, reversible intent): compute K + run the three referential guards; compute merkle_root and the full SnapshotState fold; append the kind=\"snapshot\" record to the live active (fsync+ack); COPY the to-be-archived gz segments into archive/ + append the archive manifest (fsync + dir-fsync); write the new v2 head to a STAGING path head.json.next (not yet live). Nothing observable changed — live manifest still full, live head still the valid v1 head over the full set, verify passes. A crash in Phase A leaves a harmless extra snapshot record + idempotent archive copies + a staging head; the reconciler discards the staging head and treats an uncommitted prune as NO prune. PHASE B (commit): step 1 os.replace(head.json.next -> head.json) publishes the v2 head — THE COMMIT INSTANT (after it the store is logically pruned: verify selects the live window BY SEQ>=base_seq and seeds genesis_prev=base_prev_hash); step 2 atomic manifest re-base (generation++) drops the archived segments; step 3 atomic monotonic floor.json update; step 4 (last) unlink the archived segments from live segments/ — reclaim. The manifest re-base + unlink are pure space reclamation the reconciler finishes; even if they never run, verify is already correct off the v2 head. RECONCILER (at open AND on every flock acquire): if head.schema_version==2 and head.snapshot_seq set and manifest.base_count!=head.base_count -> deterministically re-derive+re-base the manifest from the snapshot record and finish unlink+floor (forward recovery); if a snapshot record exists but the live head is still v1 (Phase A only) -> leave it (uncommitted prune = no prune). NO-LOST-ACK proof: every acked record is in the live set [K..tip] OR was fsync-copied to archive before any live deletion; the snapshot record is acked before commit; live [0..K) segments are unlinked only after archive copies are durable AND the manifest re-based — so no acked record is ever unreferenced. NO-FALSE-TAMPER proof: because classify_head selects the live window by SEQ (not prefix) and seeds genesis_prev=base_prev_hash from the SIGNED head, every crash window verifies cleanly — (v1 head + full manifest) is the consistent old state; (v2 head + full manifest) still yields exactly [K..tip] by seq, first prev_hash==base_prev_hash => links; (v2 head + re-based manifest) is the committed state — there is no reachable state where a valid store reads as TAMPERING.",
          "c1_honest_limit": "The irreducible residual: hard-prune deletes records from the LIVE set, so the live spine alone can no longer re-hash the pruned prefix — a cold verifier handed ONLY an untrusted mirror (live + v2 head, no archive, no authenticated floor) can be shown an internally-consistent OLD owner-signed snapshot state (a full rollback to a prior valid head). This is fundamental to any prunable, single-owner-key log (the owner's key is FS-resident, checkpoint.py:9) and CANNOT be closed from data alone. Cold-archive bounds it on two axes: INTEGRITY — the archive (moved gz segments + archive manifest + per-snapshot merkle_root) lets ANY verifier re-attach and re-verify genesis->tip cryptographically, and any single pruned record carries a Merkle inclusion proof, so nothing pruned is unverifiable; FRESHNESS/ANTI-ROLLBACK — the durable external floor.json (outside spine/, monotonic, prev_head_hash meta-chain) rejects every rollback below the last accepted head for warm/same-host/returning-paired verifiers, and the initial floor + the archive are handed over the AUTHENTICATED WireGuard pairing channel, never the mirror. So the exposure narrows to exactly one case: a verifier that consumes only the untrusted mirror and NEVER authenticates the floor even once — for whom freshness (not integrity) is unprovable. Every other failure flags false-tamper, never false-clean.",
          "slice_plan": [
            "HP-0: Head v2 schema (6 defaulted fields, extra=forbid kept) + version-conditional _head_payload + verify_chain/verify_head genesis_prev param + verify_checkpoint try/except + SpineTailer._resolve_head distinct 'schema-too-new/upgrade' state. NO prune. Merge gate: v1-signed heads verify BYTE-IDENTICAL under the v2 model on the real spine; old-client fails closed to 'upgrade', never 'un-notarized'.",
            "HP-1: Durable floor.json (SIGIL_HOME/floor.json, 0600, monotonic upward-only, prev_head_hash meta-chain) read+enforced in classify_head/verify_checkpoint + mirrored to SpineTailer high-water. Still base_count=0. Merge gate: replayed old head/short-spine pair rejected; --reset cannot lower the floor.",
            "HP-2: SnapshotState schema + SnapshotState.load/fold + the ~9 consumer seed edits, driven by an EMPTY snapshot (base_seq=0,base_count=0, all folds empty) appended to a real spine — exercises every seed/fold path with ZERO deletion. Merge gate: verify() and all 9 consumers byte-identical to pre-snapshot.",
            "HP-3: Merkle commitment (leaf=entry_hash) + kind=snapshot record emission + classify_head live-window-BY-SEQ + the exact anti-truncation guard (len(live)<entry_count-base_count). Emit a NON-empty snapshot but DO NOT move/delete segments (base fields set, all segments retained). Merge gate: verify uses the live window; full-scan still spans; truncation/rollback/front-cut/middle-delete all flag TAMPERING.",
            "HP-4: Referential-integrity guards (dangling parent/supersedes, oldest-open-workflow floor K<=oldest open, whole-sealed-segment-only prune-point selection). Merge gate: prune ABORTS on a dangling binding or an open workflow below K; no bytes touched on abort.",
            "HP-5: Cold-archive MOVE — copy->archive + append archive manifest, then Phase-B commit (staging head -> os.replace head, atomic manifest re-base, monotonic floor update, unlink) + reconciler + REQUIRED crash-fuzz harness (kill-9 at each Phase-A/Phase-B barrier) asserting a verify()-clean, non-lossy, never-false-TAMPERING store at every window. THIS reclaims disk.",
            "HP-6: Q5 resolve() -> PrunedRef + get_archived() Merkle-inclusion fetch + the ~9 old-seq resolvers switch None->410/PrunedRef + `sigil spine verify --with-archive` full genesis re-attach."
          ],
          "invariants_at_risk": [
            "Anti-truncation (checkpoint.py:71 n<signed_n): must become len(live)<entry_count-base_count with live selected BY SEQ — a naive n<entry_count is false-tamper every prune; a disabled check hides real truncation.",
            "Genesis-rooted chain (chain.py:38 hardcoded _GENESIS_PREV): the re-based live set links from base_prev_hash, so verify_chain MUST take genesis_prev or the first live record's prev_hash mismatches => false-TAMPERING.",
            "v1 head byte-identical verification (chain.py:50 _head_payload): 6 new defaulted fields MUST be version-conditionally dropped or every existing deployment false-alarms.",
            "State-scanner fail-CLOSED (invariant 15, store.py:255 iter_records RAISES on a missing referenced segment): archived segments must be DROPPED from the live manifest at commit, else a full-scanner raises SpineUnavailable post-prune.",
            "Monotonic anti-rollback floor: floor.json must live OUTSIDE spine/ (reset() rmtrees spine/) and update upward-only, or a rollback/replay of an old snapshot head goes undetected.",
            "No acked-record loss: live [0..K) segments unlink ONLY after archive copies are fsynced AND the manifest re-based; the snapshot record acked before commit.",
            "Never mutate a retained binding (referential integrity): base_prev_hash must equal the already-stored prev_hash of seq K so no record byte is rewritten.",
            "Kill-switch/nonce/promotion/device high-waters must be SEEDED from the fold — an un-seeded live-only scan silently regresses a security floor (a lost kill-switch latch is a missed halt).",
            "Head-lineage single-predecessor (prev_head_hash meta-chain): a new v2 head must descend from exactly the accepted head, or an attacker forks the head lineage.",
            "extra=forbid on SignedChainHead: an old v1 client MUST fail closed on a v2 head (ValidationError -> 'upgrade'), never silently to 'un-notarized/clean'."
          ],
          "risks": [
            "MAX-RECLAIM (keep 1 sealed + active live) makes the offline archive a HARD availability dependency for any full genesis verify and for resolving any pruned citation — if the archive mount is lost, integrity of pruned history is unrecoverable (mitigate: archive is the recoverability proof, replicate it; the live spine still verifies from the snapshot forward).",
            "First prune is a fleet-upgrade FLAG DAY: any paired mobile/mesh client still on v1 cannot verify a v2 head — must gate emission behind every device advertising max_head_schema>=2, else a stale client shows 'upgrade required' (fail-closed but a UX/availability hit).",
            "The archivist current-view fold (bearer 7) is the heaviest and most error-prone: mis-projecting the non-superseded-at-K-1 set silently drops a grounded fact from open_threads/due_commitments with no verify() signal — needs a dedicated equivalence test (iter_current before vs after an empty then non-empty prune).",
            "The reconciler must be exercised by the crash-fuzz harness at EVERY Phase-A/Phase-B barrier; the head-vs-manifest window is only safe because classify_head selects by seq — a regression to prefix-selection reintroduces false-TAMPERING that only a kill-9 test catches.",
            "prev_head_hash meta-chain + floor monotonicity are the ONLY cross-restart rollback defenses once records are deletable; a bug that lets floor go backward (or a reset that reaches it) reopens the full-rollback hole silently.",
            "A never-authenticated cold verifier off an untrusted mirror still cannot prove FRESHNESS (only integrity via the archive) — this is honest and irreducible, not a bug, but must be documented so no one over-claims cold-verify completeness.",
            "Recommendation stance: build this tier ONLY on explicit owner sign-off; retain-all rotation already bounds per-file size + latency, so hard-prune's cold-verifier residual is a real tamper-model tradeoff, not a free win."
          ]
        },
        "verdicts": [
          {
            "design_name": "HARD-PRUNE (cold-archive, MAX-RECLAIM)",
            "tamper_evidence": {
              "score": 5,
              "why": "entry_count stays effectively absolute because base_count and entry_count both live in the owner-signed head, and the floor makes base_count monotonic-up — so the true physical height is committed and can only grow. The anti-truncation guard is correctly NOT the post-prune no-op (n<entry_count): it selects the live window BY SEQ (seq>=base_seq), compares len(live) against the SIGNED expected_live=entry_count-base_count, then pins live[0].seq==base_seq, live[0].prev_hash==base_prev_hash, and runs verify_head over live[:expected_live] seeded with genesis_prev=base_prev_hash. I attacked every truncation: tail-cut -> len(live)<expected_live -> TAMPERING; front-cut -> min live seq>base_seq or prev-link break -> TAMPERING; middle-delete -> verify_chain seq-gap/prev mismatch -> TAMPERING (unforgeable without re-hashing the chain); pad-with-junk -> extra records fall in the benign 'appended since' branch, never hiding a delete. A keyless attacker cannot present a truncation as a legit prune: base_seq/base_count/base_prev_hash/merkle_root/entry_count are all in the Ed25519-signed payload, and the version-conditional _head_payload keeps v1 heads byte-identical (the design correctly REJECTS the spec's exclude_unset suggestion, which model_validate_json would defeat). Archive-swap is bound by signed merkle_root + base_prev_hash==last archived boundary_hash. The meta-chain (prev_head_hash) + monotonic floor make head lineage a single-predecessor hash chain, blocking fork/replay/un-prune for warm verifiers. The only false-clean is the never-authenticated cold verifier's freshness, which is irreducible for any prunable single-owner-key log and already exists for full (spine+head) rollback today."
            },
            "crash_safety": {
              "score": 4,
              "why": "The verify-cleanliness across every cutover window is airtight precisely because classify_head selects by SEQ, not array prefix: (v1 head + full manifest) is the consistent old state; (v2 head + full manifest, pre-rebase) still yields exactly [K..tip] by seq with first prev_hash==base_prev_hash so it links and verifies; (v2 head + rebased manifest) is the committed state — there is no reachable valid store that reads as TAMPERING. Commit instant = os.replace(head.json.next->head.json); manifest re-base + unlink are pure reclamation a reconciler finishes forward. No-lost-ack holds: snapshot record fsync-acked before commit, archive copies fsync+dir-fsync before any live unlink, unlink is last. Ding: the state-fold path is NOT crash-clean the way the chain is. The snapshot record is acked in Phase A before the head commits, and the head commits (step 1) before the manifest re-bases (step 2), so there is a window where SnapshotState.load sees a snapshot while iter_records still yields the pruned [0..K). A consumer reading in that window (or after a Phase-A crash that left an uncommitted snapshot record) seeds the fold AND rescans the same pruned records. Mostly fail-closed (over-restricts caps; max/latch are idempotent) and reconciler-closed, but it is a real unaddressed crash-window correctness bug, not covered by verify()."
            },
            "cold_verifier_exposure": {
              "score": 4,
              "why": "Honestly bounded and honestly scoped. INTEGRITY is fully recoverable: the archive (moved gz segments + append-only archive manifest + per-snapshot merkle_root) lets any verifier concatenate archive-then-live into one genesis-rooted stream and re-verify with verify_chain(genesis_prev=_GENESIS_PREV), and any single pruned record carries a Merkle inclusion proof against the signed root — so nothing pruned is unverifiable. FRESHNESS/anti-rollback is closed for warm/same-host/returning-paired verifiers by the external monotonic floor + prev_head_hash meta-chain, with the initial floor and the archive seeded over the authenticated WireGuard channel, never the mirror. The residual is exactly one case — a verifier consuming only an untrusted mirror that NEVER authenticates the floor even once — which can be shown an old owner-signed pruned state; this is the same full-rollback residual that already exists for the single-file spine (FS-resident owner key), not a new false-clean. Not a 5 only because hard-prune converts pruned-history integrity into a hard archive-availability DEPENDENCY: lose the offline archive and pruned-range integrity is unrecoverable (the live spine still verifies from the snapshot forward). That is a real availability/tamper-model tradeoff vs retain-all, correctly flagged as an owner-sign-off decision."
            },
            "consumer_correctness": {
              "score": 3,
              "why": "The per-bearer fold enumeration is precise and mostly in the fail-safe direction (nonce=max, killswitch=OR-latch, promotions/devices=latest-verified-at-K-1, counts additive). But two correctness gaps survive. (1) SnapshotState.load is specified as 'reads the newest kind=snapshot record' rather than the snapshot named by the COMMITTED head.snapshot_seq — combined with the append-before-commit and commit-before-manifest-rebase ordering, this double-counts the pruned prefix in the crash/cutover window; for additive bearers (account_create_counts) and especially the archivist current-view (bearer 7) this yields wrong, not merely over-restrictive, state, with NO verify() signal. Fold should key off head.snapshot_seq and filter iter_records by seq>=base_seq, not rely on manifest-rebase timing. (2) The design itself flags bearer 7 (non-superseded-at-K-1 projection) as the heaviest and most error-prone: a mis-projection silently drops a grounded fact from open_threads/due_commitments with no chain-verify signal — it needs the dedicated before/after equivalence test the design names but does not make a hard merge gate. Second-prune merkle_root over the full [0..K2) prefix also requires reading the already-archived leaves at prune time, unaddressed."
            },
            "implementability": {
              "score": 4,
              "why": "Every decision is anchored to real code (chain.py:50 _head_payload, checkpoint.py:65/86 classify_head/verify_checkpoint, tail.py:110-119 swallow, store.py:255 iter_records raise, reset() rmtree at 434) and the invariants are correctly identified. The slice plan HP-0..HP-6 is well-sequenced: schema+version-conditional payload first (no prune), then floor, then empty-snapshot to exercise every seed path with zero deletion, then non-empty snapshot with all segments retained (base fields set, no move), then referential guards, then the actual MOVE with a REQUIRED kill-9 crash-fuzz harness at each barrier, then Q5/archive fetch. Each slice has a concrete byte-identical merge gate. Buildable as written. Held back from 5 by the SnapshotState.load keying fix (must be made before HP-2/HP-5, not discovered in the fuzz harness) and the second-prune merkle-over-archive detail, both of which need to be specified before code."
            },
            "fatal_flaws": [],
            "verdict": "APPROVE (build on explicit owner sign-off, with one fix landed before HP-2). As the tamper-evidence purist I found no surviving false-clean path: entry_count is effectively absolute (signed base_count+live, floor-monotonic), the live window is selected BY SEQ with a real signed-expected_live guard plus first-record prev-linkage, and every truncation/rollback/middle-delete/archive-swap resolves to false-TAMPERING. A keyless attacker cannot dress a truncation as a legit prune without the owner key; the Merkle root + prev_head_hash meta-chain + external monotonic floor are airtight for warm verifiers, and the one open case (never-authenticated cold verifier freshness) is the pre-existing, irreducible full-rollback residual, correctly disclosed rather than over-claimed. The cold-archive genuinely bounds C1 to integrity-recoverable + freshness-only-open. The non-fatal deductions are all in the consumer/crash-window fold layer, in the fail-safe direction, and do not reopen the truncation-indistinguishability hole this tier exists to close.\"",
            "best_change": "Make SnapshotState.load(store) load the snapshot record named by the COMMITTED head.snapshot_seq (not 'the newest kind=snapshot record'), and have every bearer fold filter iter_records by seq>=head.base_seq rather than trusting manifest-rebase timing. This closes the crash/cutover double-count window (Phase-A snapshot appended-and-acked before the head commits, and head-commit before manifest re-base) in which a consumer would seed from the fold AND rescan the still-live pruned [0..K) — the one place the otherwise-clean state layer can silently corrupt a security floor (or drop a grounded archivist fact) with no verify() signal. It also makes an uncommitted/crashed snapshot record inert to consumers, matching the reconciler's 'uncommitted prune = no prune' rule.\""
          },
          {
            "design_name": "HARD-PRUNE (cold-archive, MAX-RECLAIM)",
            "tamper_evidence": {
              "score": 4,
              "why": "The KEYED path is well-built and crash-clean: classify_head selects the live window BY SEQ (>=base_seq), the anti-truncation guard is len(live)<entry_count-base_count (not the no-op n<entry_count), front-cut is caught by live[0].seq==base_seq + prev_hash==base_prev_hash, tail/middle by verify_head/verify_chain(genesis_prev=base_prev_hash), and base_prev_hash is taken from the Ed25519-SIGNED head (not the unsigned manifest) so it is unforgeable. C2 Merkle + prev_head_hash meta-chain + monotonic floor + extra=forbid v1-client fail-closed are all correctly reasoned. One real gap: the UNKEYED store.verify() (store.py:882) still seeds verify_chain from the hardcoded _GENESIS_PREV. Post-B2 (re-based live manifest [K..S]), record K's prev_hash==entry_hash(K-1)!=_GENESIS_PREV, so store.verify() returns 'chain break at seq K' = false-TAMPERING on every pruned store. The design enumerates classify_head/verify_head as snapshot-aware but never says who feeds store.verify() the base_prev_hash. It is the SAFE direction (false-tamper, not false-clean) but it gratuitously breaks `sigil spine verify` on a valid pruned store and is unspecified."
            },
            "crash_safety": {
              "score": 2,
              "why": "Strong where it counts: NO acked record is ever lost (snapshot record acked before commit; archive copies fsync'd before any live unlink; live [0..K) present until B4), and the KEYED tamper path verifies clean in every crash window because it selects by seq from the signed head. But two central claims are false. (1) The commit instant is NOT singular. Live-scan pruning is gated on the manifest re-base (B2), because iter_records reads segments strictly from manifest.ordered() (store.py:247/738) — it has NO head-based seq floor. Fold SEEDING is gated on the snapshot RECORD's mere presence (A3), because SnapshotState.load 'reads the newest kind=snapshot record'. These flip on different events, so across the whole window A3..B2 (spanning the B1 head-publish 'commit instant') BOTH are active: SnapshotState.load seeds the [0..K) contribution AND iter_records still yields [0..K). For non-idempotent bearers this DOUBLE-COMMITS state: account_create_counts = st(pruned) + live-count(which still includes the pruned records) over-counts the cap; the archivist current-view list (bearer 7) appends the still-yielded pruned records a second time = duplicate facts in open_threads/due_commitments. Idempotent max/latest bearers (nonce, warden, promotion, device) survive; the additive ones do not. (2) The reconciler's single trigger `manifest.base_count != head.base_count` is FALSE once B2 lands, so a crash between B2->B3 (floor never advanced) or B3->B4 (archived segments never unlinked) is NEVER finished by any reconciler pass — the MAX-RECLAIM disk is silently not reclaimed and the anti-rollback floor silently lags. Both defects fail SAFE (over-count=fail-closed cap; duplicate facts=cosmetic/over-report; floor-lag=pre-existing window, not new; false-tamper not false-clean; no lost ack), so not fatal — but 'singular commit instant' and 'reconciler for every intermediate state' are both demonstrably untrue as written."
            },
            "cold_verifier_exposure": {
              "score": 4,
              "why": "Honestly costed and correctly bounded: the archive re-attaches for a genesis->tip cryptographic re-verify, any single pruned record carries a Merkle inclusion proof against the signed merkle_root, and freshness is bounded by the durable floor.json (outside spine/, monotonic, prev_head_hash meta-chain) plus seeding the initial floor + archive over the authenticated WireGuard channel. The residual (a verifier that consumes only an untrusted mirror and NEVER authenticates the floor even once cannot prove freshness) is correctly identified as irreducible for any prunable single-owner-key log. Docked one point because the 'archive IS the recoverability proof' claim is undermined by the crash path: the reconciler unlinks the authoritative live [0..K) copy without re-verifying the archive segments' stored sha256 / boundary_hash re-link first, so silent archive bit-rot after A4 makes pruned history unrecoverable while the live chain still verifies clean — the recoverability proof can rot unnoticed."
            },
            "consumer_correctness": {
              "score": 3,
              "why": "Steady-state design is right: all ~9 bearers are enumerated with a concrete fold, Q5 resolve()->PrunedRef/410 (never a bare None/404) is correct, the whole-sealed-segment-only prune-point keeps base_prev_hash == the already-stored prev_hash of seq K so no retained binding is mutated, and the referential-integrity guards (dangling parent/supersedes, K<=oldest-open-workflow) are sound. But two consumer-facing defects survive: the A3..B2 double-count window (above) silently corrupts non-idempotent bearers with no verify() signal — exactly the bearer-7 archivist hazard the design's own risks flag, plus the account-create cap; and store.verify() false-tampering on a pruned store affects every consumer that calls it as a standalone integrity check. The design correctly notes bearer 7 is the most error-prone but attributes it only to mis-projection, missing the crash-window double-yield."
            },
            "implementability": {
              "score": 3,
              "why": "Large but well-staged surface: v2 head + version-conditional _head_payload (the byte-identical-v1 fix is correct — model_dump then pop the six fields when schema_version<2, NOT exclude_unset), floor.json, snapshot record + Merkle, 9 folds, cold-archive move, reconciler, and a REQUIRED kill-9 crash-fuzz harness in HP-5. The slice plan is genuinely incremental with real merge gates (empty snapshot exercises every seed path before any deletion; HP-3 sets base fields without moving bytes). Docked because three fixes are not yet in the design and are load-bearing: unifying the fold-seed and live-scan floor under the signed head, making store.verify() snapshot-aware, and widening the reconciler trigger to cover the post-B2 floor/unlink tail. The crash-fuzz harness must additionally assert fold-EQUIVALENCE (not just verify()-clean) across every barrier, or the double-count window ships undetected."
            },
            "fatal_flaws": [],
            "verdict": "CONDITIONAL GO for HP-0..HP-4; HP-5 (the actual delete/reclaim) BLOCKED until fixed. The no-lost-ack and keyed-tamper cores are sound and every defect I found fails SAFE (over-count/fail-closed cap, duplicate/over-reported facts, floor-lag, false-tamper — never false-clean, never a lost ack). But the crash-adversary verdict is that the design's two headline safety claims are overclaimed: the commit instant is NOT singular (live-scan pruning commits at manifest B2 via iter_records, fold-seeding commits at snapshot-record A3 via SnapshotState.load — a double-count window straddles the B1 'commit'), and the reconciler does NOT cover every intermediate state (its base_count!=head.base_count trigger goes false at B2, orphaning the floor-advance and segment-unlink with no finisher). Both are fixable within the stated slice plan.",
            "best_change": "Make live-scan pruning AND state-fold seeding key off the SAME committed marker — the signed v2 head — so they flip together at exactly one instant. Concretely: (a) iter_records gains a `seq >= head.base_seq` floor derived from the signed head (not just from the manifest re-base), and (b) SnapshotState.load honors a snapshot ONLY when the signed head's snapshot_seq/base_count names it (ignore an uncommitted snapshot record whose head hasn't been published). Then B1 (os.replace head.json) becomes the true singular commit instant for BOTH pruning and seeding, B2/B3/B4 collapse to pure idempotent disk-reclaim the reconciler can re-run from the head at any time, and the A3..B2 double-count window closes — no bearer, idempotent or additive, can ever double-count in any crash window. Fold this equivalence assertion (iter_current / cap-counts identical before vs. after each kill-9 barrier) into the HP-5 crash-fuzz gate, and while there, seed store.verify() from head.base_prev_hash and widen the reconciler trigger so the floor-advance + unlink always finish.\""
          },
          {
            "design_name": "HARD-PRUNE (cold-archive, MAX-RECLAIM)",
            "tamper_evidence": {
              "score": 4,
              "why": "The signed-head tamper model is sound against a keyless mirror adversary. For any one owner-signed v2 head, every surgical record drop flags: tail via len(live)<entry_count-base_count (a real guard, not the no-op n<entry_count the design correctly rejects), front via live[0].seq>base_seq / prev-linkage, middle via verify_chain seq-gap+prev-mismatch (chain.py:44,40); any repair re-derives the signed head_hash. merkle_root over leaf=entry_hash commits the pruned prefix (C2), needed because classify_head stops re-hashing [0..K). The version-conditional _head_payload is correct AND its rejection of model_dump(exclude_unset=True) is a genuine subtle catch: model_validate_json marks every default 'set', so exclude_unset would re-emit the six fields and false-alarm every v1 deployment; the explicit pop-when-schema_version<2 keeps v1 heads byte-identical over the real chain (chain.py:50 confirms current payload is model_dump+pop signatures). DEDUCTION: the design proves classify_head safe but the UNKEYED layer is not covered — store.verify() (store.py:882) and SpineTailer.poll() (tail.py:71) both seed _GENESIS_PREV and will read the steady-state pruned live set [K..tip] (first prev_hash==base_prev_hash) as a chain/linkage break. Safe direction, but it contradicts the explicit 'no reachable false-TAMPERING state' claim."
            },
            "crash_safety": {
              "score": 4,
              "why": "Ordering is disciplined and matches the existing crash-safe primitives (atomic_write_text temp->fsync->replace->dir-fsync; _seal_active_locked already treats the manifest os.replace as the commit instant, store.py:520). Phase A is fully reversible (extra snapshot record + idempotent archive copies + staging head.json.next), commit instant is the single os.replace(head.json.next->head.json), reconciler forward-recovers the manifest re-base+unlink+floor, and it runs at open AND on every flock acquire mirroring the existing _reconcile_orphan_migration pattern. No-lost-ack holds: snapshot record fsync-acked before commit; [0..K) segments unlinked only after archive copies are durable AND manifest re-based, so every acked record is always referenced by the live manifest or the archive manifest. The key insight that classify_head selects live BY SEQ (not array prefix) genuinely makes the (v2 head + still-full manifest) window verify cleanly. DEDUCTION: the store.verify()/tailer false-tamper is a steady-state issue, not merely transient — it does not threaten durability but does dent the crash-window 'always verify()-clean' assertion, since verify() is one of the things the crash-fuzz harness would assert."
            },
            "cold_verifier_exposure": {
              "score": 5,
              "why": "This is the strongest, most honest dimension and it answers my adversarial mandate correctly. Because the owner key is FS-resident on the host and NOT on the mirror (checkpoint.py:9), a mirror adversary cannot sign a new head, so a forged prune that drops specific real records is impossible — it would require re-deriving base_prev_hash/merkle_root/head_hash under the owner key. The only keyless attack is replay of a whole prior owner-signed (head, live, [archive]) triple: a full rollback to an earlier legitimately-signed snapshot. The design names this precisely as the irreducible residual (fundamental to ANY prunable single-owner-key log), and bounds it on the two correct axes: INTEGRITY via the archive (re-attach genesis->tip, per-record Merkle inclusion) and FRESHNESS via the durable floor.json seeded over the authenticated WireGuard pairing channel, never the mirror. It correctly narrows the unclosable case to exactly one: a verifier consuming only the untrusted mirror that NEVER authenticates the floor even once — for whom freshness (not integrity) is unprovable. It does not over-claim: it explicitly states merkle_root is only re-checkable WITH the archive, and every other failure flags false-tamper. I could not construct a keyless drop that evades classify_head."
            },
            "consumer_correctness": {
              "score": 3,
              "why": "The 9-bearer fold enumeration is thorough and seeds each security floor in the fail-safe direction (kill-switch latch, nonce high-water, promotion grants, device authorize/revoke, account-create caps, approvals resolved-set, archivist non-superseded projection + ledgers, warden_checkpoint), and the referential-integrity guards (dangling parent/supersedes into pruned range, K<=oldest-open-workflow-seq, whole-sealed-segment-only seam so no record byte is rewritten) are well-reasoned and match how consumers actually deref (consolidate/gate.py get(seq), transcript parent_id). Q5 resolve()->PrunedRef (never bare None) with a Merkle-inclusion archive fetch is a correct honest-resolve. TWO REAL GAPS: (1) store.verify() (store.py:863, the unkeyed layer used by `sigil verify`, dashboards, tailer integrity) is NOT wired to seed genesis_prev from the manifest's advisory base_prev_hash, so it false-tampers in steady state post-prune; (2) SpineTailer initial _last_hash and poll() linkage (tail.py:54,71) likewise false-flag the first pruned-base record as a rewind/fork in the mobile/UI feed. Both are safe-direction but are functional breaks in shipped read paths the design claims stay transparent. Also the archivist current-view fold is self-flagged as the heaviest and fails silently (no verify() signal) if mis-projected, and the kill-switch fold fails OPEN if wrong (missed halt) — the one place an error is dangerous rather than safe."
            },
            "implementability": {
              "score": 4,
              "why": "Grounded in the real code and incrementally sliceable (HP-0..HP-6), each slice default-safe with a concrete merge gate; the empty-snapshot-first strategy (HP-2/HP-3) exercises every seed/fold and the seq-selecting classify_head with ZERO deletion before any bytes move, and HP-5 gates disk reclaim behind a required kill-9 crash-fuzz harness — the correct place to put the risk. The version-conditional payload, floor.json outside spine/ (reset() rmtrees spine/, confirmed store.py:414-438), archive move, and manifest re-base all map cleanly onto existing primitives (Manifest generation++, atomic_write_text, _crossproc_lock). DEDUCTIONS: the prev_head_hash meta-chain says 're-hash the persisted predecessor head' but head.json is overwritten each checkpoint (checkpoint.py:61) — it works only if floor stores H(prior head) as a scalar, which overloads the name 'head_hash' (already meaning the chain-tip entry_hash in SignedChainHead); and the two unenumerated edits (store.verify/tailer genesis_prev wiring) must be added or the steady-state pruned store fails `sigil verify`. Large but tractable surface; the fleet-upgrade flag-day gating is real operational cost but correctly identified."
            },
            "fatal_flaws": [],
            "verdict": "SOUND, ship on explicit owner sign-off with one required addition. The cold-archive variant correctly makes integrity keyless-unforgeable and reduces the cold-verifier residual to pure freshness for a never-authenticated mirror-only verifier — which is fundamental and honestly bounded by the authenticated floor+archive seed. Every attack I could mount without the owner key resolves to a whole-state rollback (freshness), never a surgical record drop; the classify_head anti-truncation guard (len(live)<entry_count-base_count, live selected by seq) plus genesis_prev linkage plus the signed merkle_root close every in-head drop. No fatal (false-clean or data-loss) flaw survives. The one substantive defect is safe-direction but real and contradicts the design's own completeness claim: the UNKEYED verify() and the SpineTailer live-feed linkage are not wired for the re-based genesis, so a pruned store false-tampers in `sigil verify`, the dashboard, and the mobile feed. Add those two edits (both trivially sourced from the manifest's advisory base_prev_hash, cross-checked against the signed head) and the 'no reachable false-TAMPERING state' invariant actually holds. Also add a dedicated iter_current equivalence test for the archivist fold (bearer 7) and treat the kill-switch latch fold as fail-open-critical.",
            "best_change": "Wire genesis_prev through the UNKEYED read paths, not just classify_head: seed store.verify() (store.py:882 verify_chain) and SpineTailer's initial _last_hash / poll() linkage (tail.py:54,71) from the manifest's advisory base_prev_hash — cross-checked against the signed head's base_prev_hash so the advisory field is never trust-bearing. Without this, the committed steady-state pruned store (records [0..K) unlinked, first live record's prev_hash==base_prev_hash) permanently reads as a chain/linkage break in `sigil verify`, the dashboard, and the mobile SSE feed — the exact false-TAMPERING the design claims is unreachable. It is the single gap between the design's stated completeness and the actual code."
          },
          {
            "design_name": "HARD-PRUNE (cold-archive, MAX-RECLAIM)",
            "tamper_evidence": {
              "score": 4,
              "why": "The head-v2 core is sound and verify-complete: version-conditional _head_payload drops the 6 fields when schema_version<2 so v1 heads (chain.py:50) stay byte-identical; verify_chain gains genesis_prev (fixes the hardcoded _GENESIS_PREV at chain.py:38); classify_head selects the live window BY SEQ (>=base_seq) with the exact anti-truncation guard len(live)<entry_count-base_count instead of the post-prune no-op n<entry_count (checkpoint.py:71); prev_head_hash meta-chain + monotonic floor give single-predecessor head lineage; Merkle commits the pruned prefix that is no longer re-hashed; extra=forbid + hardened verify_checkpoint/_resolve_head fail closed. The irreducible gap the score reflects: verify() only proves the HASH chain, never the FOLDED state — so any mis-folded security bearer is a false-clean that no tamper check can catch. Tamper-evidence of the chain is strong (4); tamper-evidence of the STATE it protects rides entirely on state_folding correctness, which is where this design is weakest."
            },
            "crash_safety": {
              "score": 4,
              "why": "Cutover ordering is disciplined and the commit instant is unambiguous: Phase A (snapshot record acked, archive copies fsync+dir-fsync, staging head.json.next) changes nothing observable; Phase B step 1 os.replace(head.json.next->head.json) is the commit; manifest re-base + floor + unlink are pure reclamation the reconciler finishes. No-lost-ack holds because live [0..K) segments unlink ONLY after archive copies are durable AND the manifest re-based, and iter_records (store.py:733) RAISES on a missing referenced segment (invariant 15) so a half-dropped manifest fails closed not open. No-false-tamper holds because seq-selection makes every window ((v1+full),(v2+full),(v2+rebased)) verify cleanly. Docked one point: correctness is asserted, not yet proven — it stands or falls on the REQUIRED HP-5 kill-9 fuzz harness at every barrier, and a regression from seq-selection back to prefix-selection silently reintroduces false-TAMPERING that only that harness catches."
            },
            "cold_verifier_exposure": {
              "score": 4,
              "why": "Honest and bounded, not over-claimed. The residual is stated plainly: a verifier consuming ONLY an untrusted mirror and never authenticating the floor even once cannot prove FRESHNESS (a full rollback to a prior owner-signed snapshot) — irreducible for any prunable single-owner-key log (owner key FS-resident, checkpoint.py:9). Cold-archive genuinely bounds it on two axes: INTEGRITY is fully recoverable (re-attach archive gz segments in seq order + per-snapshot merkle_root cross-check + per-record Merkle inclusion proof), and ANTI-ROLLBACK is closed for warm/same-host/returning-paired verifiers via floor.json outside spine/ (reset() rmtrees spine/ so it can't be lowered) seeded over the authenticated WireGuard channel. This is a real tamper-model regression vs retain-all (which had zero cold exposure) but it is the deliberate, signed-off MAX-RECLAIM tradeoff and it is documented as such."
            },
            "consumer_correctness": {
              "score": 3,
              "why": "Five of nine bearers reconstruct exactly: nonce high-water (max composes), warden_checkpoint (max), kill-switch latch (the fold's whole state IS one bool = latch at K-1, and continuing the same left-fold over live is identical to a full scan — killswitch.py:70-83), device/capability ledgers (latest-state map, seed-then-override), promotion (latest-state over two FIXED keys agent/scope and agent/* — reconstructable). Bearer 6 approvals is correct but COUPLED to referential guard (b): pending() over live alone is complete ONLY because K<=oldest-open-workflow guarantees no unresolved queued item below K; a bug in (b) silently drops a pending approval (false-clean). Two concrete DEFECTS: (A) bearer 4 creation-cap — creation_allowed (actor_scope.py:49-56) counts a record if service==S OR origin(url)==O, an OR-predicate; a flat {service_key|origin_key:n} dict CANNOT reconstruct an OR-count without the joint (service,origin) distribution — a single get(service) UNDER-counts records that only match by origin, which is exactly the relabelled-origin bypass the cap exists to stop, so a prune can RESET the origin cap = mass-account creation (false-CLEAN, the forbidden direction). (B) bearer 7/8 archivist view — the fold projection omits conflicting_seqs and resolved, so a pruned still-open contradiction reconstructs via pending_contradictions (queries.py:71-82) STRIPPED of the conflicting seqs that make it actionable; it also omits verified_seqs (queries.py _cite prefers verified_seqs over source_seqs), degrading citation fidelity. Q5 gap: episodic_range (mcp/server.py:88) does iter_records(since_seq=lo-1) — a start_seq below base_seq silently returns only the live tail with NO PrunedRef marker, so pruned history reads as if it never existed (dishonest range). Bearer 7 cross-boundary supersession (a live record superseding a pruned record) is reconstructable but is the heaviest, most error-prone fold and, like bearer 4, produces no verify() signal when wrong."
            },
            "implementability": {
              "score": 3,
              "why": "HP-0..HP-6 slicing is genuinely good — each slice is independently gated and mergeable (empty-snapshot-first at HP-2 exercises every seed path with zero deletion; the crash-fuzz harness is mandatory at HP-5). But two folds need real redesign before they are correct, not just implementation: bearer 4 must store a joint (service,origin) count structure (or, minimally, seed by summing service-count AND origin-count WITHOUT inclusion-exclusion so the cap can only OVER-count = fail toward denial), and bearer 7's projection must be completed to carry conflicting_seqs/resolved/verified_seqs plus honest range handling in episodic_range. The archivist current-view fold (non-superseded-at-K-1 with live cross-boundary supersession + full citation projection) is the single riskiest piece and needs a dedicated iter_current equivalence test (empty then non-empty prune) as the design itself flags. Doable, but the state-folding surface is broad and two bearers are wrong as-specified."
            },
            "fatal_flaws": [
              "Bearer 4 (creation-cap) fold is a FALSE-CLEAN defect as-specified: the flat {service_key|origin_key:n} representation cannot faithfully reconstruct creation_allowed's OR-predicate count over (service, origin), so a prune can reset the origin-based cap and a relabelled-origin replay bypasses the mass-account-creation guard — this violates the design's own 'never false-clean' invariant and MUST be fixed before any prune ships (fixable: store joint counts, or over-approximate by summing both dimensions so the cap only ever over-counts)."
            ],
            "verdict": "APPROVE-WITH-CHANGES. The tamper/crash/cold-verifier architecture is sound and honestly bounded — seq-selected live window, version-conditional head, durable external floor, and cold-archive as the integrity proof are all correct and the crash cutover is safe. But as a consumer-transparency judgment it does NOT yet pass: of the nine bearers, five reconstruct exactly and one (approvals) is correct-but-coupled, while bearer 4 (creation cap) is a genuine false-clean regression (cap reset -> mass-account bypass) and bearer 7/8 (archivist/contradictions) drops actionable fields (conflicting_seqs, resolved, verified_seqs) with no verify() signal. Q5 is honest per-seq but dishonest for a boundary-spanning episodic_range. None of these are architecturally fatal — all are fixable within the design — but bearer 4 breaks the core safety promise and blocks merge until fixed.",
            "best_change": "Fix the bearer-4 creation-cap fold so a prune can never RESET the origin-scoped cap: creation_allowed matches on service==S OR origin==O, which a flat {service:n}/{origin:n} pair cannot reconstruct without double-counting or under-counting. Either fold the joint distribution — a dict keyed by (service, origin) pair -> count so the pruned contribution for any runtime (S,O) query is the exact count of pruned records with pair.service==S or pair.origin==O — or, as a minimal safe fallback, seed n = st.service_counts.get(S,0) + st.origin_counts.get(O,0) WITHOUT inclusion-exclusion so the reconstructed count can only ever be >= the true count (the cap fails toward denial, never toward reset). This converts the one false-clean bearer into a false-tamper-direction one, restoring the design's stated invariant; then extend the same discipline to complete bearer 7's projection (conflicting_seqs, resolved, verified_seqs) and add an honest PrunedRef marker to boundary-spanning episodic_range.\""
          }
        ]
      },
      {
        "design": {
          "name": "COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
          "one_line": "Prune the oldest WHOLE sealed segments from the LIVE spine behind an owner-signed, Merkle-committed snapshot; MOVE them to an offline archive that IS the recoverability + full-chain re-verify proof; re-base the manifest and sign a v2 head with a version-conditional payload so v1 clients verify byte-identically, and a cutover whose only intermediate states are superset-benign or orphan-GC, never false-TAMPERING.",
          "snapshot_anchor": "Two coupled owner-signed commitments, cross-checked. (1) A spine record kind=\"snapshot\", actor=\"OWNER\", appended as a NORMAL retain-all append at tip seq S=T+1 BEFORE any prune, so it is hash-chained and fsync'd-before-ack (store.py:696) and covered by every future head. Payload commits: base_seq (=K, first RETAINED seq = first_seq of the oldest KEPT sealed segment); base_prev_hash (= boundary_hash of the newest ARCHIVED segment = the kept segment's first_prev_hash already in the manifest, manifest.py:47 — the synthetic genesis-prev); base_count (absolute count of pruned records [0..K), ==K when contiguous from genesis); merkle_root (C2: binary Merkle over pruned entry_hash leaves, seq order, dup-last-on-odd); snapshot_seq (=S); prev_snapshot_seq (snapshot meta-chain, -1 first). K MUST equal some sealed segment's first_seq so we archive WHOLE segments only — base_prev_hash is then a value already in the manifest, reusing the seam machinery at store.py:521-525. (2) SignedChainHead v2 commits the SAME six fields inside the owner-Ed25519 payload; head_hash=entry_hash(S), last_seq=S, entry_count ABSOLUTE=base_count+live. \"Descends from exactly one prior head\": prev_head_hash = sha256_hex of the previously accepted head's signing bytes; a verifier accepts H2 iff H2.prev_head_hash == floor.head_hash (durable_floor) — so a substituted/older signed head is rejected even though owner-signed, and the snapshot's prev_snapshot_seq gives an independent second meta-chain cross-checked against snapshot_seq.",
          "head_v2_migration": "SignedChainHead (models.py:56, extra=\"forbid\") gains 6 fields defaulted to the no-prune identity: base_seq=0, base_prev_hash=_GENESIS_PREV, base_count=0, merkle_root=\"\", snapshot_seq=-1, prev_head_hash=\"\", and schema_version already defaults 1 (bump to 2 only when signing a prune). THE FLAG-DAY FIX: _head_payload (chain.py:50) currently does model_dump(mode=\"json\") minus signatures; canonical_json sorts keys, so a v1 head loaded from disk would now serialize WITH the 6 defaulted fields → different bytes → every existing signature fails → false TAMPERING fleet-wide. Make the payload VERSION-CONDITIONAL: in _head_payload, if d[\"schema_version\"]==1 pop the six v2 keys before evidence_signing_bytes; for v2 keep them. Result: v1 heads produce byte-identical signing bytes as today (verify unchanged); v2 heads bind the anchor fields. (exclude_unset is fragile across model_validate_json defaulting — use the explicit version-conditional pop.) Harden verify_checkpoint (checkpoint.py:82) with try/except around model_validate_json + classify_head so a v3-from-the-future head returns (\"schema too new — upgrade\") not a crash. SpineTailer._resolve_head (tail.py:110) already swallows to unkeyed; add a DISTINCT state: if the parsed schema_version > SUPPORTED, return a sentinel so poll() emits anchored=False with reason \"head schema vN unsupported — upgrade this client\", never silent-clean and never a rewrite alarm. Old mobile client vs a v2 head: extra=\"forbid\" would REJECT the unknown fields on model_validate_json → it must treat parse-failure as \"unverifiable/upgrade\", NOT tamper — the SpineTailer sentinel path delivers exactly that; the un-anchored live tail still streams (honest, fail-closed). Fleet upgrade is gated BEFORE the first prune (empty-snapshot slice ships the v2 model + reader everywhere first).",
          "verify_snapshot_aware": "verify_chain (chain.py:37) gains genesis_prev=_GENESIS_PREV param seeding prev; default keeps every existing call byte-identical. For a re-based live segment, genesis_prev=base_prev_hash so the first live record (prev_hash==base_prev_hash) links cleanly. classify_head (checkpoint.py:65) becomes snapshot-aware: signed_live = head.entry_count - head.base_count; locate j where live[j].seq==head.base_seq; require live[j].prev_hash==head.base_prev_hash (LEFT-EDGE PIN to the committed base); then n_live_from_j = len(live)-j; if n_live_from_j < signed_live → TAMPERING (truncated/rolled back); verify_head over live[j:j+signed_live] with genesis_prev=base_prev_hash. The anti-truncation guard that SURVIVES pruning: entry_count stays ABSOLUTE and base_count is inside the OWNER-SIGNED payload, so an attacker cannot shrink the live window and mask it by inflating base_count without a valid owner signature over that base_count AND a matching merkle_root over that many archived leaves. Crucially classify does NOT do the old bare n<entry_count (which post-prune ALWAYS fires → false tamper) NOR let n<entry_count become a no-op — it compares n_live vs the base-adjusted signed_live. Records at live[0:j] (seq<base_seq) are the not-yet-pruned prefix during cutover: they chain genesis-rooted and are classified BENIGN (superset/stale), which is what removes the false-TAMPERING window.",
          "durable_floor": "~/.sigil/floor.json (SIGIL_HOME root, 0600), OUTSIDE spine/ so reset()'s rmtree of the spine dir (store.py:432) can't delete it. Monotonic JSON {last_seq, base_seq, base_count, head_hash, snapshot_seq, prev_head_hash}, written via atomicio.atomic_write_text (temp→fsync→replace→dir-fsync). REJECT RULES enforced in verify_checkpoint/classify_head and in the append/anchor path: reject any head or snapshot with last_seq<floor.last_seq OR base_count<floor.base_count OR base_seq<floor.base_seq (a rollback to a smaller/older prune), and reject a v2 head whose prev_head_hash != floor.head_hash (the META-CHAIN: only a head that names the currently-accepted head as its parent is admissible — blocks re-installing a stale owner-signed head). Floor updates UPWARD only, after a successful full anchor (step 6 of the cutover), never on read. prev_head_hash chains head→head; snapshot.prev_snapshot_seq chains snapshot→snapshot; the two are cross-checked. HONEST LIMIT: a cold verifier bootstrapping off an untrusted mirror has no prior floor, so it must trust the owner signature over base_prev_hash/base_count/merkle_root — an FS-write attacker holding the owner key (which is FS-resident, checkpoint.py:9) could sign a different pruning and the cold verifier can't tell from data alone. This is fundamental to any prunable log.",
          "cold_archive_move": "WHICH MOVES: only sealed segments entirely below K (last_seq < K); K is chosen = first_seq of the oldest sealed segment to RETAIN, and only segments older than a retention window (keep the newest N sealed + the active) are eligible. ARCHIVE LAYOUT: ~/.sigil/spine/archive/ (or an offline mount) holding the segment files unchanged (still gzip, byte-decompress-identical) PLUS archive.manifest.json = an append-only, generation-stamped list of every archived Segment in seq order with id/first_seq/last_seq/first_prev_hash/boundary_hash/sha256, so the archive is self-describing and immutable-append. LIVE MANIFEST RE-BASE: drop the archived Segments from manifest.segments, generation++, and record advisory base_seq/base_prev_hash/base_count/snapshot_seq copies (manifest stays NON-load-bearing, manifest.py:1-14); the oldest retained segment's first_prev_hash already == base_prev_hash (no mutation needed). RE-ATTACH TO VERIFY FROM GENESIS: a verifier concatenates archive segments [0..K) (in archive.manifest order) THEN the live segments [K..], feeds ONE stream to verify_chain(genesis_prev=_GENESIS_PREV) — full genesis-rooted verification, and recomputes the Merkle over the archived leaves and checks == the snapshot's committed merkle_root (binding archive↔snapshot, closing C2). Live-only verify uses genesis_prev=base_prev_hash + the anti-truncation guard.",
          "state_folding": "SnapshotState.load(store) reads the snapshot record (via head.snapshot_seq / manifest) and returns a typed object; each consumer switches to state=SnapshotState.load(store); fold(state, store.iter_records()). The snapshot payload folds each bearer's pruned-[0..K) contribution (computed AT PRUNE TIME while the records are still present): (1) envelope.device_nonce_highwater (envelope.py:88) → {device_pubkey: max nonce over [0..K)}; consumer seeds hi=state.nonce.get(dev,-1) then max over live (a pruning-resets-nonce bug would re-open replay — critical). (2) killswitch latch _scan_engaged (killswitch.py:70) → the engaged BOOLEAN as-of-K, computed with the same owner-signed-release rule; consumer seeds engaged=state.killswitch_engaged then applies live; if ambiguous fold engaged=True (halt is the safe direction). (3) mesh authorized_devices (registry.py:66) → {pubkey: authorized|revoked} latest-verified as-of-K; consumer overlays live. (3b) capability_map (registry.py:42) → {host_id: latest verified cap}. (4) actor_scope creation counts (actor_scope.py:44) → {(service,origin): count of applied account.create in [0..K)}; consumer seeds n from state then adds live (a reset would let mass account creation outrun the cap — critical). (5) promotion is_promoted (promotion.py:44) → {(agent,scope): granted|revoked} latest-verified as-of-K. (6) approvals pending (approvals.py:53) → set of resolved target_seqs verified in [0..K); by the open-workflow floor every pruned queued proposal is already resolved, so this is an assertion (refuse to prune if any pruned queued item is still unresolved). (7) archivist supersession (revise.py:24) → set of superseded seqs in [0..K) so a live record's supersedes_id below K reads as resolved. (8) archivist promotion-key ledgers (revise.py:28) → the grounded/refused promotion_key SETS as-of-K (idempotency — else a pruned grounding gets re-promoted). (9) newest warden_checkpoint → the signed head's last_seq folded into floor.json (not a record scan). Threads/commitments/contradictions (queries.py) need no fold: they are protected by the oldest-open-workflow floor (referential_integrity) and closed ones' content lives in archive (Q5).",
          "referential_integrity": "Prune-point guards, all checked read-only BEFORE writing the snapshot (refuse the whole prune if any fails, fail-closed): (a) DANGLING PARENT/SUPERSEDES: scan retained records [K..T]; if any retained record's parent_id or supersedes_id points into [0..K) AND is not resolved by the archivist supersession/ledger fold, refuse — never leave a retained record pointing at a vanished binding. (b) OLDEST-OPEN-WORKFLOW FLOOR: K <= min seq among open threads / due commitments / pending contradictions (queries.py) and pending approvals (approvals.py:53) and any not-yet-superseded consolidation record still referenced by an open item — so no open/unresolved workflow is ever pruned (its content stays live). (c) NEVER MUTATE A RETAINED RECORD'S BINDING: the re-base only DROPS archived segments and edits base_* advisory fields; it never rewrites a retained record, its prev_hash, entry_hash, parent_id, or supersedes_id (append-only preserved). (d) K aligns to a segment boundary (first_seq) so no sealed segment is split. Because archived records are recoverable (cold-archive), a dangling reference is a hard REFUSE, not a lossy prune.",
          "q5_resolve": "get(seq) for a pruned seq must not return a bare None/404 (store.py:767 currently returns None for a below-base seq). Add resolve(seq) -> SpineRecord | PrunedRef where PrunedRef = {pruned: True, seq, base_seq, base_count, snapshot_seq, archive_hint} meaning \"compacted below base_seq — see snapshot S / archive\". get() stays returning None for genuinely-absent seqs but the seq<base_seq case is distinguished via the manifest/head base fields; callers that must serve content (bridge /api/record, ui /api/record, mcp episodic_range, consolidate/gate grounding, transcript parent_id) surface a 410-Gone-style PrunedRef, never a silent 404. ARCHIVE FETCH PATH: PrunedRef.archive_hint names the archived segment (via archive.manifest first_seq/last_seq range → owning segment id); resolve_from_archive(seq) opens archive/<seg>.jsonl.gz, seeks the seq, re-verifies the record's entry_hash against the segment's chain and (optionally) the whole prefix Merkle against merkle_root before returning it — so a served archived record is proven, not trusted.",
          "crash_safe_cutover": "ORDER (all guards read-only first): 1) choose K (segment-aligned), run referential_integrity + floor guards. 2) compute merkle_root over [0..K) and the 9 folds from the to-be-archived segments (read). 3) append the kind=snapshot record S (fsync+ack, retain-all — reversible). 4) COPY the archived segments to archive/ + write archive.manifest, fsync, verify re-hash == merkle_root (originals still in live — nothing lost). 5) sign v2 head H2 {schema_version=2, last_seq=S, entry_count=S+1, base_seq=K, base_prev_hash, base_count=K, merkle_root, snapshot_seq=S, prev_head_hash=hash(old head)} and atomic_write head.json. 6) update floor.json upward (atomic, monotonic). 7) THE COMMIT INSTANT: atomic manifest re-base (write_manifest → os.replace, manifest.py:137, generation++) dropping the archived segments. 8) unlink the now-unreferenced original sealed segments + fsync dir (reclaim). 9) delete the durable prune.intent. A single prune.intent file (written at step 3, carrying K + the signed H2 + archived ids) makes every step idempotently re-drivable. RECONCILER (at open AND on each flock, like store.py:348): if prune.intent exists, re-drive from whatever is on disk (copy/head-write/re-base/unlink are all idempotent), then delete it; orphan original segments not in the re-based manifest are GC'd exactly like the existing seal orphan-GC (store.py:509-512). NO-LOST-ACK: every append (incl. S) is fsync'd-before-ack; the prune only DROPS whole archived segments that were COPIED to archive and verified first, and whose records are all <K (superseded by the snapshot base + preserved in archive) — no acked record is deleted, only relocated + recoverable. NO-FALSE-TAMPER: the ONLY intermediate windows are (i) H2 written + manifest still full = a SUPERSET (pre-prune prefix [0..K) still present) which classify_head classifies BENIGN via the left-edge-pin-with-superset rule, and (ii) manifest re-based + originals still present = originals are orphans not in the manifest → GC'd, live=[K..S] matches H2 → CURRENT. Signing H2 BEFORE re-basing the manifest is what guarantees no window where an old head sees a shrunk live set.",
          "c1_honest_limit": "Residual: a COLD verifier bootstrapping off an UNTRUSTED mirror sees a v2 head committing base_prev_hash/base_count/merkle_root but, WITHOUT the archive, cannot recompute the pruned [0..K) prefix — it must trust the owner signature over those fields. An attacker holding the FS-resident owner key (checkpoint.py:9) could sign a different (base_seq', merkle_root') that prunes a real record, and the cold verifier can't detect it from data alone. floor.json + the prev_head_hash meta-chain close this for WARM / same-host / returning-paired verifiers (they remember the last accepted head_hash and reject a fork/rollback). COLD-ARCHIVE BOUNDS THE REST: hand the cold verifier the archive over the AUTHENTICATED WireGuard device-pairing channel (never the mirror); it re-attaches archive [0..K) + live [K..], verifies the whole chain from _GENESIS_PREV, and recomputes the Merkle over the archived leaves == committed merkle_root. That reduces the cold verifier's trust to exactly the owner-key trust today's single-file spine already requires — pruning widens the rollback blast radius by zero bits FOR A VERIFIER GIVEN THE ARCHIVE, and the archive is always available because the prune refuses unless the copy+re-hash succeeded (crash_safe_cutover step 4).",
          "slice_plan": [
            "Slice A (migration flag-day, NO prune): add the 6 defaulted v2 fields to SignedChainHead + the VERSION-CONDITIONAL _head_payload (v1 heads verify byte-identically); harden verify_checkpoint try/except and SpineTailer._resolve_head with a distinct 'schema too new — upgrade' sentinel; add verify_chain genesis_prev param (defaulted, byte-identical) and base-aware classify_head (base_count=0 → identical). Ship to the whole fleet + all mobile clients FIRST. Merge gate: every existing v1 head + old client still verifies green.",
            "Slice B (empty-snapshot-first): SnapshotState.load + the fold contract for ALL 9 bearers; consumers switch to load+fold; seed an EMPTY snapshot (base_count=0, base_seq=0) so every seed path is exercised with ZERO deletion — byte-identical behavior across all consumers.",
            "Slice C: floor.json durable external floor + monotonic reject rules + prev_head_hash meta-chain, wired into verify_checkpoint/classify_head and the anchor path. Still no prune.",
            "Slice D: snapshot-record writer + Merkle builder + referential_integrity + open-workflow floor guards + segment-aligned K selection. Writes the snapshot and re-bases the manifest but KEEPS segments (no move yet) — reclaims nothing, fully reversible, exercises the anti-truncation guard on a real re-based live window.",
            "Slice E (safety-critical): cold-archive COPY + archive.manifest + re-attach verifier + the crash-safe cutover ordering + prune.intent reconciler + orphan-GC + the unlink-reclaim. REQUIRED merge gate: a kill-9-at-each-barrier crash-fuzz harness asserting no lost ack, no false TAMPERING, verify()-clean at every barrier, and archive re-attach verifies from genesis.",
            "Slice F: Q5 resolve/PrunedRef + archive fetch path for the get(seq) resolvers (bridge/ui /api/record, mcp episodic_range, gate grounding, transcript parent_id)."
          ],
          "invariants_at_risk": [
            "Inv 7/13 (append targets the true chain tip / no seq-0 fork): the snapshot record and the re-based live[0] must link via base_prev_hash, never build_chain-from-genesis (store.py:686-687 seam logic must extend to the base).",
            "Inv 9/A4 (rotation-aware change token): a prune bumps manifest.generation so killswitch (killswitch.py:60) and gesture session caches invalidate — a stale un-halting verdict must be impossible across a prune.",
            "Inv 12 (one contiguous genesis-rooted chain to consumers): post-prune the LIVE chain is genesis-prev=base_prev_hash rooted; only archive re-attach restores true genesis.",
            "Inv 15 (no state-scanner fail-open): iter_records already RAISES on a missing referenced segment (store.py:255); the fold consumers must fail-closed if SnapshotState.load fails rather than scanning a truncated live set as if complete.",
            "Inv 16 (referential integrity): retained parent_id/supersedes_id must never dangle into the pruned range.",
            "NEW: absolute entry_count + owner-signed base_count is the sole anti-truncation guard once the pruned prefix is no longer re-hashed.",
            "NEW: prev_head_hash meta-chain — only one prior head is a valid parent (blocks stale-head substitution).",
            "NEW: monotonic floor.json outside spine/ (reset-proof)."
          ],
          "risks": [
            "Version-conditional payload is the whole flag-day: if _head_payload does NOT drop the 6 fields for schema_version==1, every v1 signature fails → fleet-wide false TAMPERING. Mitigation: explicit pop + a test asserting a real pre-existing v1 head verifies byte-identically under the v2 model.",
            "extra='forbid' on SignedChainHead (models.py:58) means an OLD client rejects a v2 head at parse time — must be routed to the 'unverifiable/upgrade' sentinel, never a tamper alarm; ship Slice A everywhere before the first prune.",
            "A prune that resets a monotonic bearer (nonce high-water, account-creation cap) would re-open replay / mass-creation — the folds for bearers (1) and (4) are security-critical, not cosmetic.",
            "Cold-verifier residual is unclosable from data alone; do not overclaim — it is bounded, not eliminated, and only by handing the archive over the authenticated channel.",
            "Crash between head-sign and manifest re-base MUST be classified benign (superset); a bug making it TAMPERING would false-alarm on every prune. The crash-fuzz harness at each barrier is the merge gate.",
            "If K is not segment-aligned or the copy+re-hash isn't verified before unlink, an acked record could be lost — K must equal a sealed first_seq and unlink happens only after archive verify + manifest re-base (steps 4→7→8).",
            "Merkle over a large pruned prefix must be recomputable deterministically (dup-last-on-odd, seq order) or archive re-attach fails to bind — pin the tree construction in a shared, tested helper."
          ]
        },
        "verdicts": [
          {
            "design_name": "COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
            "tamper_evidence": {
              "score": 4,
              "why": "No false-CLEAN path survives. entry_count stays ABSOLUTE (base_count+live) and base_count lives INSIDE the owner-signed head payload AND is bound to a merkle_root over exactly base_count archived leaves. A keyless attacker who truncates the live tail trips classify_head's n_live_from_j < signed_live -> TAMPERING; to mask a shrunk window by inflating base_count they'd need an owner signature over that base_count AND a matching Merkle over that many leaves -> keyless-impossible. Replaying an EARLIER genuine v2 prune head is caught two ways: floor.json monotonic (base_count/base_seq/last_seq) and the current genuine head still matches the current live set while an old head's smaller signed_live classifies as stale-benign, never clean-truncation. The old bare 'n < entry_count' is correctly replaced by the base-adjusted compare, not turned into a no-op. Merkle dup-last-on-odd (CVE-2012-2459 leaf-count ambiguity) is neutralized because base_count is independently signed. Archive re-attach binds leaves via the owner-signed merkle_root + seq-ordered chain re-verify from _GENESIS_PREV -> a tampered/removed/reordered archived record breaks either the root or a seam. Not a 5 because 'meta-chain airtight' is asserted but under-specified: floor.head_hash is overloaded across two hash-spaces (entry_hash(S) vs sha256 of head signing-bytes) and the single acceptance rule 'H.prev_head_hash == floor.head_hash' lacks the re-verify-current disjunct -> steady-state re-verification of the CURRENT head after the floor advances false-alarms. That is still the SAFE direction (never fails open), so tamper-evidence is sound but its airtightness is not demonstrated as-written."
            },
            "crash_safety": {
              "score": 4,
              "why": "Ordering is disciplined and fail-safe. The snapshot record S is a normal retain-all append, fsync'd-before-ack (store.py:696) and REVERSIBLE before commit. Archive segments are COPIED + re-hashed == merkle_root BEFORE any original is unlinked (no acked record can vanish). H2 is signed BEFORE the manifest re-base, so the only intermediate windows are (i) superset-benign (full [0..K) prefix still present -> classify_head left-edge-pin+slice classifies BENIGN, not TAMPERING) and (ii) orphans-not-in-manifest -> GC'd, live matches H2 -> CURRENT. Reconciler is lock-triggered (mirrors store.py:348) and re-drives from an idempotent prune.intent; set derivation is manifest-only (never a glob), avoiding double-count. K segment-aligned means whole sealed segments only, no split, so unlink can never orphan a needed record. One wrinkle costing the 5: floor.json is updated (step 6) before the manifest-re-base commit (step 7); combined with the unresolved meta-chain hash-space/re-verify ambiguity, a crash between 6 and 7 leaves a verifier reading a floor.head_hash whose comparison against the live H2 is ill-defined -> could false-alarm (safe) until the reconciler runs. No lost-ack path found."
            },
            "cold_verifier_exposure": {
              "score": 5,
              "why": "This is the design's strongest axis and the whole reason the COLD-ARCHIVE variant was chosen. The residual is stated with full honesty: a cold verifier bootstrapping off an UNTRUSTED mirror with no prior floor must trust the owner signature over base_prev_hash/base_count/merkle_root, and an attacker holding the FS-resident owner key (checkpoint.py:9) could sign a different pruning undetectably from data alone -- correctly identified as fundamental to ANY prunable log, present in today's single-file spine, not widened by pruning. The archive genuinely BOUNDS it: handed over the authenticated WireGuard device-pairing channel (never the mirror), the verifier concatenates archive [0..K) + live [K..], runs verify_chain from _GENESIS_PREV, and recomputes the Merkle over archived leaves == the owner-signed committed root -- reducing cold-verifier trust to exactly the owner-key trust the single-file spine already required. crash_safe step 4 (copy+re-hash must succeed or the prune refuses) guarantees the archive always exists, so the bound is not hypothetical. No overclaim: it says bounded, not eliminated."
            },
            "consumer_correctness": {
              "score": 3,
              "why": "State-folding is thorough and the security-critical bearers are correctly flagged: nonce high-water (envelope.py:88) and account.create cap (actor_scope.py:44) folds are called out as replay/mass-creation-reopening if reset -- exactly right. Kill-switch latch folds engaged=True on ambiguity (safe halt direction). Merkle/promotion-key ledger idempotency, mesh authorize/revoke latest-state, and the open-workflow floor protecting threads/commitments/contradictions are all sound. Q5 resolve()->PrunedRef (never bare None/404) with a proven archive fetch path is good. What drops it to 3: two verification functions would false-alarm in normal operation as specified. (1) verify_head (chain.py:73) hard-checks head.entry_count != len(entries); the design calls it over the sliced live window of length signed_live while entry_count is absolute -> EVERY legit prune trips 'log truncated or head rewritten' -> TAMPERING, and the design never specifies patching verify_head to compare entry_count == base_count + len(window). (2) The meta-chain re-verify-current disjunct is missing, so steady-state verify_checkpoint false-alarms after the floor advances. Both are safe-direction but would make a pruned deployment constantly cry TAMPERING to consumers until fixed."
            },
            "implementability": {
              "score": 3,
              "why": "The single highest-risk item -- the v2 flag-day -- is nailed with the right judgment: explicit version-conditional pop of the six fields when schema_version==1 (not exclude_unset, correctly noted as fragile across model_validate_json defaulting) so real pre-existing v1 heads verify byte-identically, with a merge-gate test asserting exactly that. Slice ordering is disciplined: migration+model everywhere first, empty-snapshot to exercise every seed path with zero deletion, floor, snapshot-writer-keeps-segments, then the kill-9-at-each-barrier crash-fuzz gate before the actual move. extra='forbid' old-client-vs-v2-head is routed to the upgrade/unverifiable sentinel (tail.py already degrades to un-notarized, safe). Held to 3 by concrete unaddressed blockers that WILL surface at build time: verify_head is not made base_count-aware (legit prunes false-alarm), the meta-chain acceptance rule and floor.head_hash hash-space are ambiguous (need the re-verify-current disjunct + a pinned hash definition), and routine `sigil sign`/checkpoint() between prunes must itself maintain prev_head_hash and advance floor.head_hash -- unspecified, so post-prune re-anchoring is undefined."
            },
            "fatal_flaws": [],
            "verdict": "TAMPER-MODEL SOUND, APPROVE WITH BLOCKERS. From the purist lens the design holds: there is NO path where a keyless attacker presents a truncation as a legit prune, entry_count stays effectively absolute (base_count is owner-signed and Merkle-bound to a signed leaf count), the meta-chain + monotonic floor block stale-head substitution and paired rollback for warm/returning verifiers, and the cold-archive re-attach makes the Merkle+chain binding airtight against a keyless attacker even with the full archive in hand. Every failure I traced flags in the SAFE direction -- false-TAMPER, never false-CLEAN. It is NOT ready to build: two verification routines (verify_head's absolute-entry_count equality, and the meta-chain re-verify-current disjunct + floor.head_hash hash-space) as specified would false-alarm normal post-prune operation, and post-prune routine re-anchoring is unspecified. These are correctness blockers, not tamper holes -- fix them before Slice E's crash-fuzz gate and the design is safe to ship.\"",
            "best_change": "Fully specify the post-prune verification triad so a LEGIT prune verifies while truncation still fails closed: (1) make verify_head/classify_head base_count-aware -- compare head.entry_count == head.base_count + len(live_window) instead of == len(entries), and verify only live[j:j+signed_live] with genesis_prev=base_prev_hash after the left-edge pin (treat 'base_seq not found in live' as TAMPERING); (2) pin the meta-chain acceptance rule to 'accept H iff H.head_hash == floor.head_hash (re-verify current) OR H.prev_head_hash == floor.head_hash (advance to child)', and disambiguate floor.head_hash to ONE hash-space (the sha256 of the head's canonical signing-bytes, distinct from the record entry_hash(S)); (3) require checkpoint()/`sigil sign` to maintain prev_head_hash and advance the floor on EVERY head post-first-prune, not only on a prune. Without these, verify_head's absolute-entry_count check trips TAMPERING on every prune and steady-state re-verification false-alarms once the floor advances -- safe-direction, but it makes a pruned deployment unusable and would mask real regressions behind constant false alarms.\""
          },
          {
            "design_name": "COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
            "tamper_evidence": {
              "score": 4,
              "why": "Anti-truncation survives pruning: entry_count stays ABSOLUTE, seq stays absolute, base_count + merkle_root are inside the owner-signed payload, and classify's left-edge-pin (live[j].seq==base_seq, prev_hash==base_prev_hash, n_live vs base-adjusted signed_live) replaces the post-prune-no-op `n<entry_count`. prev_head_hash meta-chain + floor.json block stale-head substitution. Verified in code that seq/count are the surviving monotonic guards (tail.py:88-93 uses seq, not physical length), so pruning trips neither. Docked one point: (a) verify_head hard-checks `head.entry_count != len(entries)` (chain.py:73) which the signed_live slice violates — verify_head must also be made base-aware and the spec omits this; (b) the j-not-found case (front of the live window truncated so no record has seq==base_seq) must be pinned to fail-closed TAMPERING and is only implied."
            },
            "crash_safety": {
              "score": 3,
              "why": "Core is sound: the atomic manifest re-base (write_manifest→atomic_write_text, verified atomic in atomicio.py) is a genuinely singular commit instant, signing H2 BEFORE shrinking the manifest kills the primary false-TAMPERING window (a shrunk manifest under an old head never occurs), all intermediate states classify safe (superset-benign or orphan), and NO acked record is lost — only the oldest IMMUTABLE segments move, copied+re-hash-verified to archive before unlink, while appends/rotations only ever mutate the newest active. But the ordering is under-specified in crash-relevant ways: (1) floor.json advances at step 6 BEFORE the commit at step 7 — if the intent is lost in that gap, floor asserts a prune the manifest never enacted and later rejects an honest full `sigil sign` as rollback (false-tamper, self-heals only if the reconciler always runs); (2) the prune.intent is described as carrying a 'signed H2' at step 3, but H2 isn't signed until step 5 — the reconciler contract is internally inconsistent; (3) snapshot-append idempotency isn't listed among the idempotent re-drive steps (double-commit risk); (4) the archive COPY's lock scope vs a kill-switch PANIC append is unaddressed — under the flock it blocks panics (D1 violation), lock-free it needs a drop-archived-ids-from-fresh-manifest re-base the spec doesn't state; (5) orphan-GC of archived originals must be intent-id-driven, not a manifest-diff glob that would race the rotation's create-before-commit window. All safe-direction, but not airtight as written."
            },
            "cold_verifier_exposure": {
              "score": 4,
              "why": "Honestly bounded, not overclaimed. The archive re-attach (concatenate archive[0..K)+live[K..], verify from _GENESIS_PREV, recompute Merkle over archived leaves == committed merkle_root) reduces the cold verifier's trust to EXACTLY the owner-key trust today's single-file spine already requires — the residual (untrusted mirror + no archive + FS-resident owner key, checkpoint.py:9) is correctly called fundamental to any prunable log and mitigated only by handing the archive over the authenticated WireGuard channel. Docked one: the bound is now a NEW availability dependency — offline/removable archive media that silently drops after a lying fsync leaves a cold verifier unable to re-verify [0..K); it degrades to the honest C1 limit (live still verifies from base forward, no false-clean), but that widening-when-archive-absent isn't foregrounded."
            },
            "consumer_correctness": {
              "score": 3,
              "why": "Fold enumeration is thorough and correctly flags the security-critical seeds (nonce high-water and account-create cap would re-open replay / mass-creation if reset; killswitch latch folds engaged=True on ambiguity). Referential-integrity + oldest-open-workflow floor + Q5 resolve are well-reasoned. But: (1) the base-aware classify_head MUST ship fleet-wide before the first prune or every un-upgraded consumer hits `n<signed_n` (checkpoint.py:71) → TAMPERING — a documented but high-blast-radius coupling; (2) UNADDRESSED: a SpineTailer resuming from a since_seq that was pruned reads get(since_seq)=None → _last_hash=_GENESIS_PREV, then the first live record's prev_hash!=_last_hash triggers a false linkage break (tail.py:56,73) — safe direction but a real un-handled false-alarm for lagging tailers; (3) verify_head's entry_count check breaks the sliced verify. Solid on the folds, gaps on the tail/anchor read paths."
            },
            "implementability": {
              "score": 3,
              "why": "Large but sensibly sliced surface: v2 head with version-conditional _head_payload (the flag-day — the version-conditional pop is correctly identified as fleet-wide-or-false-tamper, and Ed25519 determinism makes byte-identical v1 re-verify achievable), snapshot-aware classify/verify_chain/verify_head, floor.json, archive copy + re-attach, SnapshotState + 9 folds, crash-safe cutover + reconciler + crash-fuzz gate, Q5 across 6 consumers. Slice A-F front-loads migration safely (empty-snapshot exercises every seed path at zero deletion). Realistic. Docked: several load-bearing under-specs must be resolved before code — verify_head base-awareness, the copy lock scope, intent timing/contents, intent-driven orphan-GC, the empty-active seam-seed post-prune (must seed from base_prev_hash/base_seq, not read archived bytes) — and the whole thing rests on a crash-fuzz harness that doesn't exist yet."
            },
            "fatal_flaws": [],
            "verdict": "Sound core, SHIP-WITH-FIXES — not autonomous. The crash cutover has a genuinely singular, atomic commit instant (the manifest re-base via atomic_write_text), the head-signed-before-manifest-shrink ordering eliminates the primary false-TAMPERING window, and I found NO crash path that loses an acked record (the oldest immutable segments are copied + re-hash-verified to the archive before any unlink, and only the newest active segment ever mutates under concurrent appends/rotations). Every failure mode I could construct flags in the SAFE (false-tamper) direction; no false-CLEAN survived. But three crash-ordering issues must be fixed pre-merge: floor.json advances before the commit instant (floor-ahead-of-reality can reject honest future heads), the prune.intent's stated contents ('signed H2') cannot exist at the step it is written, and the archive-copy lock scope vs kill-switch panic appends is unspecified (D1). Plus consumer gaps: fleet-wide base-aware classify_head is a hard gate, verify_head must be made base-aware, and SpineTailer's lagging-cursor-across-a-prune false linkage is unhandled. Fixable within the slice plan; the crash-fuzz-at-each-barrier harness (Slice E gate) is the right and necessary bar.",
            "best_change": "Make the atomic manifest re-base the SOLE commit instant and pin the intent protocol around it: (1) write a durable prune.intent FIRST — before appending the snapshot record — carrying K + the explicit archived-segment-ids (NOT a not-yet-signed H2); let the reconciler recompute H2 deterministically (Ed25519 is byte-stable) and skip the snapshot-append if the intent's snapshot seq is already present; (2) move the floor.json advance to STRICTLY AFTER the manifest re-base, never before, so floor can never assert a prune the manifest hasn't enacted; (3) run the archive COPY lock-free (like gzip compaction) so it never blocks a panic append, and make step-7 re-base a drop-archived-ids-from-a-freshly-read manifest operation — sound because only the oldest immutable segments move — with orphan-GC driven by the intent's id list, never a manifest-diff glob. This collapses the three crash-ordering hazards into one coherent, self-healing, single-commit cutover."
          },
          {
            "design_name": "COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
            "tamper_evidence": {
              "score": 4,
              "why": "Core guard is sound and grounded: entry_count stays ABSOLUTE while base_count/base_prev_hash/merkle_root live INSIDE the owner-signed v2 payload, so shrinking the live window without a matching owner signature is caught — a mirror attacker without the key can forge no novel prune. The version-conditional _head_payload pop is correctly identified as the flag-day linchpin (model_dump→canonical_json sorts keys, chain.py:50-53, so the 6 defaulted fields would otherwise break every v1 signature). prev_head_hash meta-chain + floor close rollback for warm verifiers. Two real gaps keep it off 5: (a) reused verify_head (chain.py:73) hard-checks entry_count!=len(entries) against an ABSOLUTE count over a sliced live window → false TAMPERING on every prune unless verify_head is made base_count-aware — the design implies but never states this; (b) the left-edge pin's 'base_seq not found in live' branch is left implicit — if it falls through benign it is FALSE-CLEAN on a front-drop, the exact no-op trap the brief warns about, moved to the left edge."
            },
            "crash_safety": {
              "score": 4,
              "why": "Cutover ordering is well-reasoned and reuses the already-merged, crash-fuzzed seal-swap pattern (store.py:485-539: _maybe_crash barriers, orphan-GC reconciler, atomic manifest re-base as the commit instant, fsync-before-ack at store.py:697). No acked record can be lost: originals persist until AFTER archive copy+re-hash-verify (step 4) AND manifest re-base (step 7), unlink only at step 8. The only intermediate windows are superset-benign (head signed, manifest still full → classify CURRENT via left-edge-pin) or orphan-GC (re-based, originals unreferenced). prune.intent makes it idempotently re-drivable. Minor wrinkle: intent is said to be written at step 3 'carrying the signed H2' but H2 is not signed until step 5 — resolvable (Ed25519 re-sign is deterministic, or write intent post-sign), not structural. Slice E's kill-9-at-each-barrier gate is the right merge bar."
            },
            "cold_verifier_exposure": {
              "score": 4,
              "why": "The most honestly-handled dimension. Residual is precisely stated and shown unclosable from data alone: a truly cold device with no out-of-band anchor cannot distinguish the current owner-signed (head,prune) from a genuine OLDER one (rollback); without the owner key an untrusted mirror can only replay a genuine past prune, never forge a novel one. Pruning does not widen this beyond today's full-spine rollback. Closure is correctly attributed to seeding floor/head_hash over the authenticated WireGuard pairing channel, never the mirror; the archive re-attaches [0..K)+[K..] and rebinds via merkle_root. Does not overclaim ('bounded, not eliminated'). Two dings: it slightly conflates the archive (which proves prefix COMPLETENESS) with anti-rollback (which needs the fresh anchor, i.e. freshness) in the 'zero bits given the archive' sentence; and a mobile client resuming from a now-pruned cursor (tail.py:56 get→None→GENESIS) throws a false linkage break at seq K, forcing a full cold resync from the mirror — safe direction but it routes users onto exactly the weakest path."
            },
            "consumer_correctness": {
              "score": 3,
              "why": "The 9-bearer fold enumeration is thorough and flags the two security-critical ones (nonce high-water re-opening replay; account.create cap outrunning) as non-cosmetic — folds are protected because the snapshot record sits at tip seq S, covered by head_hash (entry_hash(S)), so non-owner tampering breaks the chain. Referential-integrity refuse-if-dangling is safe (archive makes it recoverable). But real risks remain: (a) fail-closed on SnapshotState.load is a 9-consumer surface stated once but unproven — a single consumer that seeds default on load-miss re-opens its floor (Inv 15); (b) SpineTailer resume-from-pruned-cursor false-tamper is unaddressed; (c) the get()→None vs new resolve()→PrunedRef split means every existing get(pruned_seq) caller silently gets None unless individually migrated, and enumerating them exhaustively is error-prone; (d) no explicit archive↔snapshot fold cross-check (recompute folds from archive == snapshot payload)."
            },
            "implementability": {
              "score": 4,
              "why": "Well-sliced (A migration flag-day → B empty-snapshot fold → C floor → D snapshot+guards keeping segments → E cold-archive+cutover → F Q5), each independently shippable with a concrete merge gate, and it builds directly on the merged retain-all rotation (segments/manifest/seal/reconciler already exist and are crash-fuzzed). The heavy lift is real: fleet+mobile must ship Slice A everywhere BEFORE the first prune (extra='forbid' means an old client rejects a v2 head at parse → must route to the 'upgrade' sentinel, not tamper), and the 9-fold + fail-closed surface is the bulk of the risk. verify_head/classify_head/tailer edits are moderate and localized. Slice D's real-re-based-window gate would surface the verify_head entry_count bug; the left-edge not-found=TAMPERING branch needs an explicit adversarial test or it ships as a false-clean hole."
            },
            "fatal_flaws": [],
            "verdict": "APPROVE with required pre-merge conditions. This is a strong, code-grounded design that keeps every failure in the safe (false-tamper) direction and honestly bounds the one unclosable residual. No flaw is design-killing; each sharp edge is reparable within the stated approach and its slice gates. Required before Slice D/E merge: (1) make verify_head base_count-aware (compare len(entries) to entry_count-base_count) or every prune false-alarms TAMPERING; (2) make classify_head treat 'base_seq absent from the live window OR live[j].prev_hash!=base_prev_hash' as unconditional TAMPERING — this is the false-CLEAN front-drop hole and must have an adversarial test; (3) prove fail-closed on SnapshotState.load across all 9 folds; (4) seed SpineTailer._last_hash from base_prev_hash on a resume-from-pruned cursor. The version-conditional payload (already the top risk) needs the 'real pre-existing v1 head verifies byte-identical under the v2 model' test as its Slice A gate.",
            "best_change": "Pin down the anti-truncation guard on BOTH edges as the one false-clean-critical invariant: (a) make verify_head/classify_head subtract base_count from the absolute entry_count when checking the live window length (else every real prune false-alarms), and (b) specify that 'no live record has seq==base_seq, or its prev_hash!=base_prev_hash' is UNCONDITIONAL TAMPERING — not a benign no-op. Without (b) a front-drop attack (present [K+6..S], nothing at seq K) slips through as clean, which is the exact 'n<entry_count no-op' trap the brief warns about, transposed to the left edge — the only failure here that is false-CLEAN rather than false-tamper, so it is the single highest-value thing to nail down and cover with an adversarial test."
          },
          {
            "design_name": "COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
            "tamper_evidence": {
              "score": 4,
              "why": "The load-bearing pieces are correct and grounded. The version-conditional _head_payload fix is real: chain.py:_head_payload does model_dump(mode='json') and canonical_json sorts keys, so without popping the 6 v2 keys for schema_version==1 every existing v1 signature would fail fleet-wide — popping them reproduces byte-identical v1 signing bytes (schema_version stays 1, included in both; no other field changes). The anti-truncation guard survives pruning: entry_count stays ABSOLUTE and base_count is inside the owner-signed payload, so shrinking the live window requires a valid owner signature over a matching (base_count, merkle_root). The left-edge pin (require live[j].prev_hash==base_prev_hash) plus n_live vs signed_live correctly avoids letting n<entry_count degrade into a truncation-masking no-op. Merkle C2 binds the archive to the snapshot. Not a 5 because the deepest residual is honest, not closed: a cold verifier off an untrusted mirror still reduces to trusting the FS-resident owner key over base_prev_hash/base_count/merkle_root — pruning does not widen it, but nor is it eliminated by the tamper machinery alone."
            },
            "crash_safety": {
              "score": 5,
              "why": "The cutover ordering is carefully reasoned against the ACTUAL machinery. Append-snapshot is a normal retain-all fsync-before-ack append (store.py append path), so it can't lose an ack; COPY+re-hash-verify the archive BEFORE any unlink means originals are still live until proven-recoverable; the commit instant = atomic manifest re-base via os.replace (matching manifest.py's write_manifest); orphan originals not in the re-based manifest are GC'd exactly like the existing seal orphan-GC (store.py:509-512). Signing H2 BEFORE re-basing the manifest is the key insight that eliminates any window where an old head sees a shrunk live set — the only intermediate states are (i) H2-written-manifest-full = a SUPERSET (pre-prune prefix still present) classified BENIGN by the left-edge-pin-with-superset rule, and (ii) manifest-rebased-originals-present = orphans → GC, live matches H2 → CURRENT. A crash after snapshot-append but before head-sign leaves the ordinary 'head lags live tail' benign state, which classify_head already treats as BENIGN, and prune.intent makes every step idempotently re-drivable by the reconciler that already runs at open/flock. No false-TAMPERING window, no lost ack."
            },
            "cold_verifier_exposure": {
              "score": 4,
              "why": "Honestly bounded, not overclaimed. The design correctly states the residual is unclosable from data alone for a cold verifier bootstrapping off an untrusted mirror (fundamental to any prunable log), and bounds it by handing the archive over the AUTHENTICATED WireGuard device-pairing channel: re-attach archive[0..K)+live[K..], verify from _GENESIS_PREV, recompute Merkle over archived leaves == committed merkle_root. That reduces cold-verifier trust to exactly the owner-key trust today's single-file spine already requires (owner key is FS-resident, checkpoint.py:9), so pruning widens the rollback blast radius by zero bits FOR A VERIFIER GIVEN THE ARCHIVE. The prune refuses unless the copy+re-hash succeeded, so the archive is always available. Not a 5 only because it depends on the operator actually delivering the archive over the trusted channel; a verifier handed only the mirror is strictly weaker than a same-host warm verifier."
            },
            "consumer_correctness": {
              "score": 4,
              "why": "For the ENUMERATED bearers the folds are mathematically correct and I verified each against source. nonce high-water (envelope.py:88) = per-device max, a semilattice fold, split-exact — the critical one, correctly seeded state.nonce.get(dev,-1) then max over live so a pruned high-water can't regress and re-open replay. killswitch _scan_engaged (killswitch.py:74) is a pure boolean LEFT-fold (engage→True, owner-signed release→False); carrying the as-of-K boolean is exactly equivalent, and 'ambiguous→engaged=True' is safe-direction. authorized_devices/capability_map/promotion (registry.py:66, promotion.py:44) are last-writer-wins maps; overlaying live over the as-of-K map preserves latest-wins PROVIDED the snapshot carries EVERY key's state (not just live ones). creation-cap (actor_scope.py:44) keyed by the (service,origin) PAIR is the right key: the OR-match query reconstructs exactly by summing pair-buckets where bucket.service==S OR bucket.origin==O, with no double-count — subtle but sound, must be pinned in a test. promotion_ledgers grounded/refused SET-union fold (revise.py:28) is necessary and correct for idempotency. approvals-pending assertion via the open-workflow floor is sound (a pruned queued item can't be unresolved; a pruned approval can't resolve a live/newer proposal since supersedes points backward). BUT the design's '~9' enumeration is PROVABLY INCOMPLETE: I found full-scan iter_records() security consumers it did not fold or prove-safe — (a) gesture/session.py pending_device_arms + the arm replay gate (line ~225) is a (device_pubkey,nonce) REPLAY ledger for HID-injection arming, the SAME class as nonce high-water; it happens to be safe only because ARM_FRESHNESS rejects any replay old enough to have its SESSION_ARMED record pruned — a real but UNSTATED side-condition; (b) governor/budget.py BudgetLedger.spent() sums an agent's records for the UTC day (a monotonic cap 'the spine IS the ledger'), safe only because it filters ts to TODAY and retention keeps the current day live — again unstated. Neither produces an actual false-clean under a sane retention window, so not fatal, but the design asserted a self-critique 'finds nothing missing' and it missed a replay gate. Docking to 4 for enumeration incompleteness — completeness is the whole guarantee against false-clean."
            },
            "implementability": {
              "score": 4,
              "why": "Six well-ordered slices, each grounded in real functions: Slice A flag-day (version-conditional payload + verify_chain genesis_prev + base-aware classify_head, all defaulted byte-identical) with a merge gate that a real pre-existing v1 head verifies green; Slice B empty-snapshot-first exercises every seed path at zero deletion; C floor.json; D snapshot+re-base keeping segments (reversible); E the safety-critical cold-archive+cutover behind a kill-9-at-each-barrier crash-fuzz gate; F Q5. The fiddly bits are real but scoped: the explicit version-conditional pop (exclude_unset is correctly rejected as fragile), SnapshotState.load fail-closed (invariant 15 — a consumer that folds WITHOUT loading state silently regresses nonce to -1), and the OR-sum creation-cap reconstruction. Docked one point because the incomplete bearer census (gesture, budget, and the graph-rebuild Q5 gap) means the true consumer-migration surface is larger than the design scoped — the audit of all ~30 iter_records call sites is unbudgeted work that must precede Slice B."
            },
            "fatal_flaws": [],
            "verdict": "STRONG — SHIP with one required pre-condition. The design is tamper-sound, crash-safe, and honestly bounds the cold-verifier residual; the version-conditional head payload, absolute-entry_count anti-truncation guard, sign-head-before-manifest-rebase cutover, and prune.intent reconciler are all correct against the actual code. On my lens (consumer-transparency), every ENUMERATED fold reconstructs the true full-history value — nonce max, killswitch boolean left-fold, last-writer-wins device/promotion maps, the (service,origin)-pair creation-cap, and the grounded/refused idempotency sets are all mathematically split-exact, and Q5 resolve correctly replaces store.py:768's bare None with a PrunedRef+archive-fetch. The gap is not a wrong fold but an INCOMPLETE census: the codebase has full-scan security consumers the '~9' missed — notably gesture/session.py's device-arm (device,nonce) REPLAY ledger (an HID-injection gate) and governor/budget.py's daily-cap accumulator — both safe in practice only via unstated side-conditions (ARM_FRESHNESS, and retention keeping the current UTC day live), plus graph/rebuild.py + graph_query which silently shrink post-prune without a Q5 'compacted, see archive' signal. None yields a false-clean under a sane retention window, so no fatal flaw survives, but the design's claim that a self-critique 'finds nothing missing' is falsified.",
            "best_change": "Before Slice B, run an EXHAUSTIVE audit of ALL ~30 iter_records()/entries() call sites (I enumerated them) and classify each as {windowed since_seq=cursor → prune-safe | monotonic-security bearer → MUST fold | content → MUST route to Q5}, then explicitly fold-or-prove-safe the two I found missing: (1) gesture/session.py's device-arm (device_pubkey,nonce) replay ledger — either fold its consumed-nonce set into the snapshot or assert-and-test that ARM_FRESHNESS << retention so a prunable arm can never be replayed; (2) governor/budget.py's per-UTC-day cap — assert-and-test that retention always keeps the current day live. Also add graph_query/graph_entity and dashboard totals to the Q5 'compacted below N — see archive' surface. Rationale: consumer completeness is the SOLE guarantee against a false-clean, and the design's '~9' is provably not the full set — a folded-but-unenumerated replay gate is exactly the un-halt/replay failure class this tier must never regress."
          }
        ]
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Design"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Judge"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "design:minimal-C1-conservative",
      "phaseIndex": 1,
      "phaseTitle": "Design",
      "agentId": "a76c6d81f7f9e2fef",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784490246198,
      "queuedAt": 1784490234371,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGI…",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784490699696,
      "tokens": 108138,
      "toolCalls": 21,
      "durationMs": 453498,
      "resultPreview": "{\"name\":\"VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine\",\"one_line\":\"Prune the oldest whole sealed segments from the live set behind an owner-signed, Merkle-committed kind=\\\"snapshot\\\" record, MOVE those segments to a cold archive (the recoverability proof, handed over the authenticated WireGuard channel), re-base the live manifest to a synthetic genesis-prev, and keep every in…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "design:max-reclaim",
      "phaseIndex": 1,
      "phaseTitle": "Design",
      "agentId": "a86273a77adb60f20",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784490243771,
      "queuedAt": 1784490234372,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "HARD-PRUNE (cold-archive, MAX-RECLAIM)",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784490651520,
      "tokens": 118182,
      "toolCalls": 20,
      "durationMs": 407747,
      "resultPreview": "{\"name\":\"HARD-PRUNE (cold-archive, MAX-RECLAIM)\",\"one_line\":\"Prune the oldest sealed segments out of the LIVE spine behind an owner-signed, Merkle-committed kind=\\\"snapshot\\\" anchor + v2 head, MOVE the pruned gz segments to a re-verifiable archive/ (the recoverability + genesis-verify proof), re-base the live manifest/verify to the snapshot, and fold all ~9 monotonic bearers into the snapshot so l…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "design:migration-safety",
      "phaseIndex": 1,
      "phaseTitle": "Design",
      "agentId": "a1fc6403dcec46453",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784490245558,
      "queuedAt": 1784490234372,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491183798,
      "tokens": 115694,
      "toolCalls": 22,
      "durationMs": 938240,
      "resultPreview": "{\"name\":\"COLD-ARCHIVE HARD-PRUNE for the SIGIL spine\",\"one_line\":\"Prune the oldest WHOLE sealed segments from the LIVE spine behind an owner-signed, Merkle-committed snapshot; MOVE them to an offline archive that IS the recoverability + full-chain re-verify proof; re-base the manifest and sign a v2 head with a version-conditional payload so v1 clients verify byte-identically, and a cutover whose o…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "judge:tamper-purist:VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine",
      "phaseIndex": 2,
      "phaseTitle": "Judge",
      "agentId": "a228cd4d95a6f6611",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491217184,
      "queuedAt": 1784491186963,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGI…",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491521256,
      "tokens": 51902,
      "toolCalls": 4,
      "durationMs": 304072,
      "resultPreview": "{\"design_name\":\"VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine\",\"tamper_evidence\":{\"score\":5,\"why\":\"entry_count stays effectively ABSOLUTE and no keyless truncation-as-prune path exists for a warm verifier. The prune boundary (base_seq/base_count/base_prev_hash/merkle_root) is entirely inside the OWNER-SIGNED head, so a keyless attacker cannot mint a head with a different bound…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "judge:crash-adversary:VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine",
      "phaseIndex": 2,
      "phaseTitle": "Judge",
      "agentId": "a9cf8f0d39ba08a4f",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491215498,
      "queuedAt": 1784491186965,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGI…",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491578525,
      "tokens": 108363,
      "toolCalls": 11,
      "durationMs": 363026,
      "resultPreview": "{\"design_name\":\"VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine\",\"tamper_evidence\":{\"score\":5,\"why\":\"No false-clean survives my attack. The anti-truncation rewrite (n < entry_count - base_count) correctly closes the no-op trap, and crucially base_count/base_prev_hash are read from the SIGNED head, not the unsigned manifest (store.verify()'s use of manifest.base_prev_hash is only…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "judge:cold-verifier-adversary:VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine",
      "phaseIndex": 2,
      "phaseTitle": "Judge",
      "agentId": "a4f134d138e9ce8a1",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491217160,
      "queuedAt": 1784491186966,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGI…",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491506010,
      "tokens": 59208,
      "toolCalls": 11,
      "durationMs": 288850,
      "resultPreview": "{\"design_name\":\"VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine\",\"tamper_evidence\":{\"score\":5,\"why\":\"The core tamper model is coherent and fail-safe. entry_count stays ABSOLUTE and the anti-truncation guard is correctly rewritten from checkpoint.py:71's `n < signed_n` (which would collapse to a no-op once the live window is sliced) to `n < entry_count - base_count`, so a legit p…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "judge:consumer-transparency:VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine",
      "phaseIndex": 2,
      "phaseTitle": "Judge",
      "agentId": "a926707d3df9b9b2a",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491216647,
      "queuedAt": 1784491186967,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGI…",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491540479,
      "tokens": 61626,
      "toolCalls": 8,
      "durationMs": 323832,
      "resultPreview": "{\"design_name\":\"VAULT-PRUNE — the COLD-ARCHIVE hard-prune tier for the SIGIL spine\",\"tamper_evidence\":{\"score\":4,\"why\":\"Sound and mostly closed. head-v2 six fields + version-conditional _head_payload (drop the six when schema_version<2) preserves byte-identical v1 verification; classify_head cross-checks head.{base_seq,base_count,merkle_root} against the immutable kind=snapshot record at head.snap…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "judge:tamper-purist:HARD-PRUNE (cold-archive, MAX-RECLAIM)",
      "phaseIndex": 2,
      "phaseTitle": "Judge",
      "agentId": "aeb4470cd93e874c0",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491214741,
      "queuedAt": 1784491187024,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "HARD-PRUNE (cold-archive, MAX-RECLAIM)",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491436797,
      "tokens": 54167,
      "toolCalls": 4,
      "durationMs": 222055,
      "resultPreview": "{\"design_name\":\"HARD-PRUNE (cold-archive, MAX-RECLAIM)\",\"tamper_evidence\":{\"score\":5,\"why\":\"entry_count stays effectively absolute because base_count and entry_count both live in the owner-signed head, and the floor makes base_count monotonic-up — so the true physical height is committed and can only grow. The anti-truncation guard is correctly NOT the post-prune no-op (n<entry_count): it selects …"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "judge:crash-adversary:HARD-PRUNE (cold-archive, MAX-RECLAIM)",
      "phaseIndex": 2,
      "phaseTitle": "Judge",
      "agentId": "a3de4efefc71065d5",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491216868,
      "queuedAt": 1784491187025,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "HARD-PRUNE (cold-archive, MAX-RECLAIM)",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491545918,
      "tokens": 90680,
      "toolCalls": 9,
      "durationMs": 329050,
      "resultPreview": "{\"design_name\":\"HARD-PRUNE (cold-archive, MAX-RECLAIM)\",\"tamper_evidence\":{\"score\":4,\"why\":\"The KEYED path is well-built and crash-clean: classify_head selects the live window BY SEQ (>=base_seq), the anti-truncation guard is len(live)<entry_count-base_count (not the no-op n<entry_count), front-cut is caught by live[0].seq==base_seq + prev_hash==base_prev_hash, tail/middle by verify_head/verify_ch…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "judge:cold-verifier-adversary:HARD-PRUNE (cold-archive, MAX-RECLAIM)",
      "phaseIndex": 2,
      "phaseTitle": "Judge",
      "agentId": "a9d1620d0bc20444c",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491215011,
      "queuedAt": 1784491187025,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "HARD-PRUNE (cold-archive, MAX-RECLAIM)",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491461932,
      "tokens": 86080,
      "toolCalls": 7,
      "durationMs": 246920,
      "resultPreview": "{\"design_name\":\"HARD-PRUNE (cold-archive, MAX-RECLAIM)\",\"tamper_evidence\":{\"score\":4,\"why\":\"The signed-head tamper model is sound against a keyless mirror adversary. For any one owner-signed v2 head, every surgical record drop flags: tail via len(live)<entry_count-base_count (a real guard, not the no-op n<entry_count the design correctly rejects), front via live[0].seq>base_seq / prev-linkage, mid…"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "judge:consumer-transparency:HARD-PRUNE (cold-archive, MAX-RECLAIM)",
      "phaseIndex": 2,
      "phaseTitle": "Judge",
      "agentId": "a8a3bd5a21204b505",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491218109,
      "queuedAt": 1784491187025,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "HARD-PRUNE (cold-archive, MAX-RECLAIM)",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491541310,
      "tokens": 78760,
      "toolCalls": 20,
      "durationMs": 323201,
      "resultPreview": "{\"design_name\":\"HARD-PRUNE (cold-archive, MAX-RECLAIM)\",\"tamper_evidence\":{\"score\":4,\"why\":\"The head-v2 core is sound and verify-complete: version-conditional _head_payload drops the 6 fields when schema_version<2 so v1 heads (chain.py:50) stay byte-identical; verify_chain gains genesis_prev (fixes the hardcoded _GENESIS_PREV at chain.py:38); classify_head selects the live window BY SEQ (>=base_se…"
    },
    {
      "type": "workflow_agent",
      "index": 12,
      "label": "judge:tamper-purist:COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
      "phaseIndex": 2,
      "phaseTitle": "Judge",
      "agentId": "a68fa780a2315b569",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491215427,
      "queuedAt": 1784491187110,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491477080,
      "tokens": 69373,
      "toolCalls": 6,
      "durationMs": 261653,
      "resultPreview": "{\"design_name\":\"COLD-ARCHIVE HARD-PRUNE for the SIGIL spine\",\"tamper_evidence\":{\"score\":4,\"why\":\"No false-CLEAN path survives. entry_count stays ABSOLUTE (base_count+live) and base_count lives INSIDE the owner-signed head payload AND is bound to a merkle_root over exactly base_count archived leaves. A keyless attacker who truncates the live tail trips classify_head's n_live_from_j < signed_live ->…"
    },
    {
      "type": "workflow_agent",
      "index": 13,
      "label": "judge:crash-adversary:COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
      "phaseIndex": 2,
      "phaseTitle": "Judge",
      "agentId": "a92ef7f438924bf6c",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491217373,
      "queuedAt": 1784491187110,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491581297,
      "tokens": 89172,
      "toolCalls": 9,
      "durationMs": 363923,
      "resultPreview": "{\"design_name\":\"COLD-ARCHIVE HARD-PRUNE for the SIGIL spine\",\"tamper_evidence\":{\"score\":4,\"why\":\"Anti-truncation survives pruning: entry_count stays ABSOLUTE, seq stays absolute, base_count + merkle_root are inside the owner-signed payload, and classify's left-edge-pin (live[j].seq==base_seq, prev_hash==base_prev_hash, n_live vs base-adjusted signed_live) replaces the post-prune-no-op `n<entry_cou…"
    },
    {
      "type": "workflow_agent",
      "index": 14,
      "label": "judge:cold-verifier-adversary:COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
      "phaseIndex": 2,
      "phaseTitle": "Judge",
      "agentId": "a281c38ded8344557",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491214984,
      "queuedAt": 1784491187111,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491501374,
      "tokens": 84809,
      "toolCalls": 7,
      "durationMs": 286389,
      "resultPreview": "{\"design_name\":\"COLD-ARCHIVE HARD-PRUNE for the SIGIL spine\",\"tamper_evidence\":{\"score\":4,\"why\":\"Core guard is sound and grounded: entry_count stays ABSOLUTE while base_count/base_prev_hash/merkle_root live INSIDE the owner-signed v2 payload, so shrinking the live window without a matching owner signature is caught — a mirror attacker without the key can forge no novel prune. The version-condition…"
    },
    {
      "type": "workflow_agent",
      "index": 15,
      "label": "judge:consumer-transparency:COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
      "phaseIndex": 2,
      "phaseTitle": "Judge",
      "agentId": "a449826e5191b84ae",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491215079,
      "queuedAt": 1784491187111,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "COLD-ARCHIVE HARD-PRUNE for the SIGIL spine",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784491588063,
      "tokens": 75578,
      "toolCalls": 11,
      "durationMs": 372984,
      "resultPreview": "{\"design_name\":\"COLD-ARCHIVE HARD-PRUNE for the SIGIL spine\",\"tamper_evidence\":{\"score\":4,\"why\":\"The load-bearing pieces are correct and grounded. The version-conditional _head_payload fix is real: chain.py:_head_payload does model_dump(mode='json') and canonical_json sorts keys, so without popping the 6 v2 keys for schema_version==1 every existing v1 signature would fail fleet-wide — popping them…"
    },
    {
      "type": "workflow_agent",
      "index": 16,
      "label": "synthesizer",
      "phaseIndex": 3,
      "phaseTitle": "Synthesize",
      "agentId": "ac82146047212b786",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784491632565,
      "queuedAt": 1784491615469,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/kali/sigil/sigil/reuse/models.py",
      "promptPreview": "You are designing HARD-PRUNE (record deletion for a hard disk cap) for the SIGIL spine — the append-only, hash-chained, Ed25519-signed source of truth. Retain-all segment rotation is ALREADY MERGED (segments + manifest + gzip compaction; see /home/kali/sigil/sigil/spine/{store.py,manifest.py,checkpoint.py,atomicio.py} and sigil/reuse/{chain.py,canonical.py,models.py}). Hard-prune is the ONE tamper…",
      "lastProgressAt": 1784492004586,
      "tokens": 121351,
      "toolCalls": 4,
      "durationMs": 372021,
      "resultPreview": "I have everything grounded — the three designs, all 12 verdicts, and the real code (`chain.py` `verify_chain`/`_head_payload`/`verify_head`, `checkpoint.py` `classify_head`/`verify_checkpoint`, `models.py` `SignedChainHead`). The retain-all rotation spec I read is the already-merged base; below is the deferred tier, fully specified.\n\n---\n\n# RECOMMENDED — COLD-ARCHIVE HARD-PRUNE for the SIGIL spine…"
    }
  ],
  "totalTokens": 1373083,
  "totalToolCalls": 174
}