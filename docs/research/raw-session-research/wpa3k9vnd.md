{
  "summary": "Adversarial design panel for the SIGIL+CRUCIBLE+Strix capability-fusion monorepo (Claude-powered), + risk synthesis",
  "agentCount": 5,
  "logs": [],
  "result": {
    "designs": [
      {
        "dimension": "architecture",
        "recommended_approach": "FOUR COMPONENTS, THREE PROCESSES, ONE SHARED PACKAGE, DATA-ONLY across the SIGIL boundary. This is the cleanest acyclic architecture and it repairs the two theses that break as literally stated.\n\nTHESIS 1 (layering) — VERIFIED, stronger than claimed. The shared core is not hypothetical: `sigil/reuse/{canonical,chain,crypto,models}.py` are, by their own headers, \"VENDORED VERBATIM from CRUCIBLE `framework/v2/evidence/canonical.py` / `entitlement/crypto.py`\" — byte-identical, same domain tag `crucible-evidence-v1\\x00`. CRUCIBLE's own `agents/spine_chain.py` already reuses `evidence/chain.py` to sign its blackboard. SIGIL's v2 head (`reuse/models.py:56` SignedChainHead with base_seq/base_prev_hash/base_count/cumulative_merkle_root; `reuse/chain.py:86-88`) is a strict backward-compatible SUPERSET of CRUCIBLE v1 (`evidence/models.py:194`): base_count=0 reduces `entry_count==base_count+len(entries)` to v1's `entry_count==len(entries)` exactly. So adopting SIGIL v2 as the single source is correct and low-risk. DO THIS FIRST: factor v2 into a standalone `core-integrity` package with NO `framework.*` namespace, re-point CRUCIBLE `evidence/{canonical,chain,models}` + `entitlement/crypto` at it, delete the three vendored copies. The domain tag is unchanged so every existing signature stays valid; add a re-verify gate proving every current v1 signed head still verifies under v2.\n\nTHESIS 2 (no-hallucinated-findings) — PARTIALLY REFUTED as written, repairable. The oracle does NOT re-execute `poc_script_code` or a raw Caido request. `verify/reverify.py:reverify_context` re-runs a PURE deterministic oracle over a RETAINED, bug_class-BOUND `oracle_context` (a serialized `FindingContext`: baseline/mutated dicts, probe_rounds, timing samples, expected/observed state, oob_hits — see `verify/verifier.py:606 _run`). Strix's `tools/reporting/tool.py:create_vulnerability_report` records free-text fields + a PoC string + optional endpoint/method, with NO `FindingContext` and NO machine-readable link to the Caido request IDs that form the differential; its `cwe` (\"CWE-89\") is not a canonical bug_class (`verifier.py:238 _ALIASES` has `sql_injection→sqli`, no CWE keys). Feed a Strix finding to reverify as-is and you get \"finding carries no oracle_context to re-verify\" (`reverify.py:199`). REPAIR (needed together): (a) build an `OracleConfirmationAdapter` on the CRUCIBLE side modeled exactly on `agents/oracle_probe_executor.py:OracleProbeExecutor` — it re-drives a scope-gated baseline+probe pair, builds `FindingContext.from_http_responses(...)` (or the right builder per class), calls `OracleVerifier.confirm(ctx)`, and on confirm calls `evidence/certify.py:build_certificate`+`sign_certificate`; (b) extend `create_vulnerability_report` to carry `{bug_class|cwe, injection_point/param, baseline_request_id, probe_request_id}` referencing Caido captures, plus a CWE→canonical-bug_class map. ONLY oracle-mappable classes (sqli/xss/idor/ssrf/rce/lfi/... in BUG_CLASS_ORACLES) become signed FACTS; everything else is honestly demoted to a labelled lead per `veracity/firewall.py` (\"UNGROUNDED — labelled analyst commentary, never fact\"). Claiming every Strix finding becomes a signed fact would itself be the hallucination the system exists to kill. Dedup: deterministic dedup by `oracle_context_digest`/`cert_digest` (`evidence/models.py:90,119`) replaces the LLM-judge for CONFIRMED findings only; keep Strix `report/dedupe.py` for unconfirmed leads.\n\nTHESIS 3 (hard gates) — SOUND intent, wrong choke point + a WARDEN impedance mismatch. exec_command/write_stdin are SDK-emitted `ShellTool` running IN-CONTAINER (shell/README.md), not host-side `@function_tool`s — there is no Python function to wrap. All HTTP egress rides container env `http_proxy=https_proxy=ALL_PROXY=http://127.0.0.1:48080` → in-container Caido sidecar (`runtime/session_manager.py:101`), and the sandbox holds NET_ADMIN/NET_RAW (`runtime/docker_client.py:215`, for `nmap -sS`). So: (1) The HARD gate must be an EXTERNAL egress gateway, not in-container — a sandbox with NET_ADMIN can rewrite its own nft rules, and raw/DNS/SYN traffic bypasses the HTTP proxy. Put the Strix container on an internal-only Docker network whose sole route out is a `warden-gw` running nftables default-DROP (allow only charter-scope IPs from `common/ethics.py:parse_scope`) + a filtering forward-proxy that calls `agents/scope_gate.py:validate_action(url)` per request and DROPs off-scope before the wire; Caido stays for CAPTURE, not enforcement. (2) WARDEN + a WardenGateHooks(RunHooks) on the SDK `on_tool_start`/`on_tool_end` seam (ReportUsageHooks in `core/hooks.py` already subclasses RunHooks) records EVERY tool call into the Rust signed action-log (`kernel/src/warden.rs:record`) and blocks auto-run of A2/A3 SEMANTIC host tools (repeat_request→A2, scope_rules→A2/A3) into the owner-signed approval queue. (3) IMPEDANCE MISMATCH: `tiers.rs:classify(\"exec_command\")` → token \"exec\" ∈ A3_TOKENS → A3/ExplicitRequired, and registry is RAISE-ONLY (can't lower) → naive WARDEN-by-name halts ALL shell. So exec_command's TARGET authorization is delegated to the gateway; WARDEN name-classification is used only for the semantic named tools plus a coarse destructive-verb block. Three complementary layers: gateway=wire enforcement (target), WARDEN=tool-class authorization + signed audit, scope_gate=exact per-URL refuse for host-side tools.\n\nBOUNDARY DISCIPLINE (the acyclic rule): the adapter, PCF signing, and oracle re-exec all live in the CRUCIBLE-side process (which may import framework.*). The confirmed `SignedEvidence` crosses to SIGIL as INERT JSON over SIGIL's existing WG bridge/queue; SIGIL does `spine.store.append(kind=\"finding\", payload=<SignedEvidence>)` (`spine/store.py:648`; `kind=\"finding\"` already in `spine/models.py:14`) WITHOUT importing any framework.* — preserving `assert_no_offense()`. Prefer the narrow `OracleVerifier.confirm`+`evidence.certify` path over `veracity/firewall.py:admit()` for black-box findings (admit imports intel.predict+worldmodel at module load and needs entity_refs/a world-model a bare finding lacks).",
        "key_integration_points": [
          "SHARED CORE: sigil/reuse/{canonical,chain,crypto,models}.py (v2, source of truth) ⇄ framework/v2/evidence/{canonical,chain,models}.py + entitlement/crypto.py (v1, byte-identical vendored copies). Same domain tag crucible-evidence-v1\\x00; v2 verify_head(...,genesis_prev=) reduces to v1 at base_count=0. Factor into one no-framework-namespace package.",
          "ORACLE RE-EXEC SEAM: verify/reverify.py:reverify_context(oracle_context, bug_class=...) → verify/confirmation.py:confirm_finding → verify/verifier.py:OracleVerifier.confirm/_run. Consumes a serialized verify/adapter.py:FindingContext, NOT a PoC string. Adapter must PRODUCE this (model: agents/oracle_probe_executor.py:OracleProbeExecutor.execute → FindingContext.from_http_responses).",
          "STRIX FINDING CONTRACT: strix/tools/reporting/tool.py:create_vulnerability_report / _do_create (free-text + poc_script_code + endpoint/method). Extend to emit {bug_class|cwe, injection_point, baseline_request_id, probe_request_id} + add CWE→canonical map (verifier.py:_ALIASES has none).",
          "CAIDO EVIDENCE SUBSTRATE: strix/tools/proxy/tools.py:list_requests/view_request/repeat_request (caido_api). The two request IDs a finding references are the baseline/mutated pair the adapter pulls to build oracle_context.",
          "PCF CERT: evidence/certify.py:build_certificate/sign_certificate/verify_certificate (SIGNATURE+BINDING oracle_context_digest+REPRODUCTION); evidence/models.py:SignedEvidence/EvidenceCertificate.cert_digest. This SignedEvidence is the inert payload handed to SIGIL.",
          "SPINE SIGN (data-only, offense-free): sigil/spine/store.py:SpineStore.append(kind='finding', payload=<SignedEvidence dict>); sigil/spine/checkpoint.py:checkpoint (owner Ed25519 1-of-1 anchor). kind='finding' + 'warden_checkpoint' already exist in spine/models.py:14.",
          "HARD EGRESS GATE: external warden-gw on an internal Docker network (Strix container joins via runtime/docker_client.py _apply_sandbox_network / STRIX_SANDBOX_NETWORK); nft default-DROP keyed on common/ethics.py:parse_scope + forward-proxy calling agents/scope_gate.py:validate_action. Container proxy env set at runtime/session_manager.py:101.",
          "WARDEN TOOL GATE: add WardenGateHooks(RunHooks) on SDK on_tool_start/on_tool_end (peer of core/hooks.py:ReportUsageHooks; wired via run_config in core/runner.py:220) → kernel/src/warden.rs:decide/record (tiers.rs classify). Cross-anchor the Rust action-log head into the spine via kind='warden_checkpoint'.",
          "OFFENSE-FREE INVARIANT: sigil/reuse/__init__.py:26 assert_no_offense() — the constraint that forbids importing framework.* in any SIGIL process; dictates the process boundary."
        ],
        "risks": [
          {
            "severity": "fatal",
            "risk": "A subtree monorepo that installs SIGIL and CRUCIBLE into one venv/process makes `framework.*` importable, tripping sigil/reuse/__init__.py:26 assert_no_offense() at import of sigil.mcp/perception/mesh/voice/etc — SIGIL won't start. If someone 'fixes' it by relaxing the guard so SIGIL can import framework.v2.evidence to sign findings, that silently destroys SIGIL's offense-free-BY-CONSTRUCTION guarantee (its entire identity).",
            "mitigation": "Keep SIGIL and CRUCIBLE as separate processes/venvs. Factor only the integrity primitives into a standalone `core-integrity` package with NO framework.* namespace, imported by both. Confirmed findings cross to SIGIL as inert SignedEvidence JSON over the existing WG bridge/queue; SIGIL never imports framework.*. Add a CI test asserting assert_no_offense() passes in the SIGIL process post-merge."
          },
          {
            "severity": "high",
            "risk": "Thesis-2 pipeline is a no-op against Strix findings as-recorded: the oracle needs a bug_class-bound FindingContext, but create_vulnerability_report emits free text + a PoC string, no machine-readable Caido link, and CWE ≠ canonical bug_class. reverify returns 'carries no oracle_context'. 'Oracle re-executes the finding' does nothing.",
            "mitigation": "Build the OracleConfirmationAdapter (re-drive model = oracle_probe_executor) that reconstructs oracle_context from the referenced Caido requests; extend the Strix reporting contract to carry the structured probe refs; add a CWE→canonical map. Confirm only oracle-mappable classes; honestly DEMOTE the rest to labelled leads — never sign unconfirmed as facts."
          },
          {
            "severity": "high",
            "risk": "In-container egress enforcement is defeatable and incomplete: the sandbox holds NET_ADMIN/NET_RAW (docker_client.py:215) so the agent can rewrite in-container nft rules, and nmap -sS/DNS/raw sockets bypass the HTTP proxy entirely. A Caido/proxy-only gate is not a hard gate.",
            "mitigation": "Enforce at an EXTERNAL warden-gw on an internal-only Docker network (the container's sole route out). gw runs nft default-DROP (allow charter-scope IPs only) + a scope_gate forward-proxy. Caido is capture, not enforcement. The sandboxed agent cannot reconfigure a gateway it has no access to."
          },
          {
            "severity": "high",
            "risk": "WARDEN-by-name gate halts autonomy: tiers.rs classify('exec_command') → 'exec' ∈ A3_TOKENS → A3/ExplicitRequired, and registry.rs is raise-only (cannot lower), so every shell command demands owner approval. Strix runs hundreds per scan.",
            "mitigation": "Delegate exec_command's TARGET authorization to the egress gateway; use WARDEN name-classification only for the semantic host-side tools (repeat_request→A2, scope_rules→A2/A3, create_vulnerability_report→A1) plus a coarse destructive-verb block. Do NOT feed the literal string 'exec_command' to WARDEN as the gating decision."
          },
          {
            "severity": "med",
            "risk": "Shared-core migration touches the crypto trust boundary across three divergent copies (CRUCIBLE evidence v1 + entitlement, SIGIL reuse v2). A wrong reduction or a domain-tag change invalidates every existing signed head/cert.",
            "mitigation": "Adopt v2 as source; the domain tag is already identical (crucible-evidence-v1\\x00) so signatures survive. Land behind a re-verify gate that proves every existing v1 SignedChainHead verifies under v2 with base_count=0 (the reuse/chain.py:86-88 reduction). Byte-identical or it doesn't merge."
          },
          {
            "severity": "med",
            "risk": "Two trust roots collide semantically: PCF certs use CRUCIBLE's m-of-n governance TrustRoot (entitlement); the SIGIL spine head is anchored 1-of-1 by the owner Ed25519 key (checkpoint.py). Ambiguity over 'who signs the finding as authoritative' can produce a cert nobody can fully verify with one key.",
            "mitigation": "Adopt an explicit TWO-ANCHOR model: CRUCIBLE governance signs the finding EvidenceCertificate (verify_certificate uses its trust_root); the SIGIL owner key signs the spine head that chains it (verify_checkpoint uses the owner root). Independent, both verifiable, defensible defense-in-depth. Document it; neither verifier needs the other's key."
          },
          {
            "severity": "med",
            "risk": "Routing black-box Strix findings through veracity/firewall.py:admit() drags in intel.predict + worldmodel at module load and expects entity_refs/a world-model a bare finding does not have — added coupling and failure modes for no grounding benefit.",
            "mitigation": "Black-box path uses the narrow OracleVerifier.confirm + evidence.certify only. Reserve admit() for when a world-model is actually populated (e.g. a CRUCIBLE-native scan), where its contradiction/fabrication checks add value."
          }
        ],
        "sequencing": "STRICT ORDER (each depends on the prior): 1) SHARED-CORE REFACTOR FIRST — factor SIGIL v2 reuse/ into a standalone no-framework-namespace `core-integrity` package; re-point CRUCIBLE evidence/entitlement at it; land behind a byte-identical re-verify gate over all existing v1 signed heads. Everything downstream needs one canonical chain/crypto. Ship-standalone: yes (invisible if byte-identical). 2) PROCESS/VENV BOUNDARY + inert-data channel — stand up the CRUCIBLE→SIGIL SignedEvidence channel over SIGIL's WG bridge/queue; CI-assert assert_no_offense() still passes in the SIGIL process. Ship-standalone: yes (no finding flow yet). 3) EGRESS HARD GATE — external warden-gw container, internal Docker network, nft default-DROP + scope_gate forward-proxy; repoint the Strix container proxy. THIS IS THE HIGHEST SECURITY ROI AND SHIPS ALONE — it makes Strix scope-safe immediately, before any oracle work, closing the 'prompt-scope only, no egress firewall' weakness the fusion exists to fix. 4) WARDEN TOOL GATE — WardenGateHooks on SDK on_tool_start/on_tool_end recording into the Rust action-log + owner-approval routing for semantic A2/A3 tools + exec-delegation policy; cross-anchor the action-log head into the spine. Ship-standalone: yes. 5) ORACLE CONFIRMATION ADAPTER (thesis 2 headline) — depends on 1 (core) + 3 (scope-gated re-drive). Start with ONE bug class end-to-end: boolean_sqli via FindingContext.from_http_responses (the exact path oracle_probe_executor already proves), then widen class-by-class; extend the Strix reporting contract + CWE map alongside. 6) SPINE-SIGN confirmed PCF certs (kind='finding') + checkpoint anchoring — depends on 2+5. INCREMENTALLY SHIPPABLE INDEPENDENTLY: 3 (egress) delivers value on day one with zero dependency on the oracle pipeline; 1 is a prerequisite invisible refactor; 5/6 are the marquee pipeline but land last and one bug-class at a time. What MUST come first: the shared-core refactor (1) and the process boundary (2) — get the layering/offense-free invariant right before any code flows between trees.",
        "open_decisions": [
          "PROCESS TOPOLOGY (blocking): confirm SIGIL and CRUCIBLE run as SEPARATE processes exchanging inert JSON, NOT one venv. RECOMMEND yes — it is forced by assert_no_offense(); a single-venv 'true fusion' either won't boot SIGIL or silently voids its offense-free guarantee.",
          "EXTEND THE STRIX FINDING CONTRACT? RECOMMEND yes — add {bug_class|cwe, injection_point, baseline_request_id, probe_request_id} to create_vulnerability_report. Parsing free-text evidence to reconstruct the differential is too leaky to be load-bearing for a signed-fact pipeline.",
          "SCOPE OF 'SIGNED FACT': only oracle-mappable classes (BUG_CLASS_ORACLES) become facts; all others = labelled leads. RECOMMEND accept the honest demotion — signing unconfirmed findings as facts reintroduces exactly the hallucination the veracity firewall forbids (firewall.py: demote-only).",
          "exec_command GATING: command/target-aware Python policy above WARDEN vs delegate target-authz to the egress gateway. RECOMMEND delegate to gateway (WARDEN can't lower its raise-only A3, and per-command shell parsing is unsound); WARDEN handles named semantic tools + a coarse destructive-verb block only.",
          "TRUST-ROOT MODEL: one root or two? RECOMMEND two anchors — CRUCIBLE governance m-of-n signs the finding PCF cert; SIGIL owner Ed25519 signs the spine head that chains it. Both independently verifiable; matches the two systems' existing key models with no reconciliation.",
          "AGENT RUNTIME (adjacent to this dimension): keep openai-agents (Claude via LiteLLM/StrixProvider) vs port to Claude Agent SDK. RECOMMEND keep openai-agents for the fusion — the Kali sandbox, ShellTool exec transport, Caido proxy, and the whole egress-gateway seam all ride on SandboxRunConfig; porting rebuilds the exec/gateway choke point from scratch for no architectural gain."
        ]
      },
      {
        "dimension": "claude-migration",
        "recommended_approach": "DEPTH DECISION: Keep the openai-agents SDK; force Claude through the existing LiteLLM route + a small hardening set. Do NOT port to the Claude Agent SDK for this fusion. Rationale grounded in the tree: the entire Strix body is welded to openai-agents 0.14.6 — build_strix_agent() returns agents.sandbox.SandboxAgent with capabilities=[Filesystem(), Shell()] (factory.py:454-471); the run loop is Runner.run_streamed(...) with RunConfig+SandboxRunConfig(client, session) (execution.py:363, runner.py:220-224); persistence is agents.memory.SQLiteSession (sessions.py:26); usage/budget is agents.lifecycle.RunHooks.on_llm_end (hooks.py:38); stop logic is tool_use_behavior=_finish_tool_use_behavior returning ToolsToFinalOutputResult (factory.py:307-326,458). The Kali Docker sandbox executes exec_command/write_stdin/filesystem tools THROUGH the SDK's Shell/Filesystem capabilities and SandboxRunConfig — the Claude Agent SDK has no equivalent that runs those tools in the same container, so option (b) means rebuilding the sandbox execution layer, the multi-agent graph (core/agents.py AgentCoordinator + atomic snapshot/resume), and the streaming/hooks plumbing from scratch. That is a multi-week rewrite that touches every fusion seam and buys nothing the LiteLLM path can't, since Strix already lists anthropic/claude-opus-4-8 in RECOMMENDED_MODEL_NAMES (models.py:88) and routes non-openai prefixes through StrixProvider(MultiProvider)->LitellmModel (models.py:39-60) with model=None on the agent (factory.py:459).\n\nPHASED PATH:\n- Phase 1 (days): pin STRIX_LLM=anthropic/claude-opus-4-8 (settings.py:23 default is None) and land the ~8 hardenings below. This delivers 100%-Claude-driven Strix on the provider-agnostic runtime with zero SDK swap.\n- Phase 2 (optional, ~1-2 wks, HIGH value): replace ONLY the transport for anthropic/* — write a StrixAnthropicModel implementing the openai-agents Model interface (get_response/get_streamed_response) backed by the anthropic SDK (not currently a dependency — uv.lock has zero `anthropic` entries), and return it from StrixProvider._resolve_prefixed_model for the \"anthropic\" prefix instead of the LiteLLM fallback (models.py:45-60). This is a localized change that leaves SandboxAgent/Runner/hooks/sessions untouched but gives first-class adaptive thinking, output_config.effort, prompt caching, and real usage — fixing the LiteLLM-mediation risks (thinking-off/budget_tokens-400, zeroed cost, no caching) that Phase 1 can only paper over. This is the sweet spot: keep the agentic body, swap the model wire for Claude.\n\nThe ~8 hardening points ARE CONFIRMED present in the code as real seams:\n(1) dedupe.py Responses-shaped text extraction: _extract_text() iterates response.output and matches openai.types.responses.ResponseOutputMessage (dedupe.py:12,260-269) — the exact spot Claude output must round-trip through.\n(2) reasoning_effort->Anthropic: make_model_settings() wraps openai.types.shared.Reasoning(effort=reasoning_effort) gated on model_supports_reasoning() which reads litellm.model_cost[...][\"supports_reasoning\"] (inputs.py:8-9,138-145; models.py:222-233); ReasoningEffort literal includes non-Anthropic values \"minimal\" and \"xhigh\" (settings.py:11).\n(3) tool_choice gate: _accepts_required_tool_choice() returns True ONLY for openai/ or known-openai bare models, so force_required_tool_choice is a no-op on Claude (inputs.py:27-33,146-147).\n(4) chat-completions tool-schema wrapping: uses_chat_completions_tool_schema() returns True for any \"/\"-prefixed non-openai model (models.py:212-219), which flips chat_completions_tools=True and rewrites the Responses freeform CustomTool apply_patch into a JSON function tool via _custom_tool_as_function_tool (factory.py:124-167,463). REQUIRED for Claude and already handled.\n(5) prompt caching: NONE — grep for cache_control/anthropic_beta/extra_headers across strix/ returns nothing, while system_prompt.jinja alone is 32,008 bytes and always-on skills add ~33KB (deep 7214 + agent_browser 19331 + python 4108 + root_agent 2790).\n(6) telemetry: set_tracing_disabled(True) + trace_include_sensitive_data=False are set (models.py:119; runner.py:225), but TelemetrySettings.enabled defaults True (settings.py:66) — posthog/scarf still live.\n(7) extended thinking: tied to (2) — LiteLLM mediates thinking blocks; multi-turn replay rides on SQLiteSession item round-trip (sessions.py, execution.py:369 session=session).\n(8) cost/budget: litellm success_callback litellm_cost_callback feeds ReportUsageHooks budget gate (models.py:191-202; hooks.py:66-71; state.py:505-540).",
        "key_integration_points": [
          "StrixProvider._resolve_prefixed_model (config/models.py:45-60) — the single routing seam; Phase-2 direct-Anthropic Model provider is returned here for the 'anthropic' prefix instead of _get_fallback_provider('litellm'). Every Claude call, including dedupe.py:310 StrixProvider().get_model(), flows through it.",
          "make_model_settings (core/inputs.py:125-148) — where Reasoning(effort=...) and tool_choice='required' are attached; the reasoning_effort->Anthropic-effort/thinking translation and the xhigh/minimal-value handling live here and in model_supports_reasoning (config/models.py:222-233).",
          "configure_sdk_model_defaults (config/models.py:116-131) + _configure_litellm_compatibility (153-165) — sets litellm.drop_params=True/modify_params=True and set_default_openai_api('responses'|'chat_completions'); the place to inject Anthropic prompt-caching headers/cache_control and to stop silently dropping xhigh.",
          "report/dedupe.py check_duplicate (272-363) + _extract_text (260-269) — the LLM-judge dedup the fusion replaces with CRUCIBLE oracle re-execution; ResponseOutputMessage extraction on Claude output is the fragile join, and create_vulnerability_report requires poc_script_code (reporting/tool.py:149) which is exactly the artifact CRUCIBLE re-executes.",
          "ReportUsageHooks.on_llm_end (core/hooks.py:38-71) + litellm_cost_callback/_estimate_response_cost (report/state.py:505-540,585-621) — the spend-accounting seam; this is where signed budget accounting should feed SIGIL's governor, and where the budget gate silently zeroes on unpriced Claude models.",
          "build_strix_agent SandboxAgent construction (agents/factory.py:454-472) with capabilities=[Filesystem, Shell], tool_use_behavior, model=None — the openai-agents coupling surface a Claude-Agent-SDK port would have to replace wholesale; leave it intact.",
          "proxy repeat_request + target-touching tools (tools/proxy/tools.py) and exec_command wrapper (factory.py:208-232) — the wire-touching actions that CRUCIBLE scope_gate + SIGIL WARDEN must wrap; the model migration must not change their signatures so the hard-gate wrappers slot in unchanged.",
          "config/settings.py LlmSettings (11,23,38) — pin STRIX_LLM default to anthropic/claude-opus-4-8 and constrain ReasoningEffort to Anthropic-valid levels; TelemetrySettings.enabled (66) forced off for the governed fusion."
        ],
        "risks": [
          {
            "severity": "high",
            "risk": "reasoning_effort mistranslation on Claude: make_model_settings sends Reasoning(effort=...) (inputs.py:143-144) only when model_supports_reasoning() finds the model in litellm.model_cost (models.py:230). LiteLLM 1.90.1's cost/capability map predates claude-opus-4-8/fable-5/sonnet-5, so either (a) the model is absent -> supports_reasoning False -> extended thinking is SILENTLY NEVER SENT (Opus 4.8 then leaks reasoning into visible text per the migration guide), or (b) LiteLLM's Anthropic adapter emits the legacy thinking:{type:enabled,budget_tokens:N}, which returns HTTP 400 on Opus 4.8/4.7/Sonnet 5/Fable 5. Both are runtime-only; test_models.py only string-matches names and never issues a live reasoning call.",
            "mitigation": "Phase 1: verify against a live Claude endpoint which branch occurs; if 400, pin a LiteLLM version whose Anthropic adapter emits output_config.effort/adaptive, or set STRIX_REASONING_EFFORT=none to disable. Phase 2: the direct-Anthropic Model provider sends thinking:{type:'adaptive'}+output_config.effort natively, removing the dependency on LiteLLM's model map."
          },
          {
            "severity": "high",
            "risk": "Budget gate silently disarms on Claude. ReportUsageHooks raises BudgetExceededError only when report_state.get_total_llm_cost() >= max_budget_usd (hooks.py:66-70). That total comes from litellm_cost_callback/_estimate_response_cost, whose own docstring (state.py:589) says LiteLLM 'returns no response_cost for models missing from its cost map' and completion_cost then yields 0/raises. For the recommended future-dated Claude IDs, cost stays 0.0 -> --max-budget-usd never fires. A 'governed capability-fusion' whose spend cap is a no-op on its default model is a governance hole.",
            "mitigation": "Add an Anthropic price table (or read anthropic usage+known per-MTok rates) so cost is nonzero; assert at startup that get_total_llm_cost increments after turn 1 for the configured model. Phase 2 provider reports real usage/cost directly. Feed this into SIGIL's governor budget as the authoritative meter."
          },
          {
            "severity": "high",
            "risk": "No Anthropic prompt caching on a ~65KB static system prompt sent every turn. system_prompt.jinja=32KB + always-on skills ~33KB is rebuilt identically across max_turns=500 x every agent (factory.py:428-435, runner.py DEFAULT_MAX_TURNS=500), and grep shows zero cache_control/extra_headers in strix/. On Claude via LiteLLM this is full-price input tokens every call — a large silent cost/latency regression unique to Claude (OpenAI auto-caches; Anthropic needs explicit cache_control breakpoints).",
            "mitigation": "Inject cache_control:{type:'ephemeral'} on the system/tools prefix (LiteLLM supports Anthropic cache_control; or set it in the Phase-2 provider). Verify usage.cache_read_input_tokens>0 across turns. Keep the prompt+tool list byte-stable (it already is) so the prefix caches."
          },
          {
            "severity": "high",
            "risk": "dedupe fails OPEN on Claude. _extract_text matches only openai.types.responses.ResponseOutputMessage (dedupe.py:263). If Claude-via-LiteLLM returns the answer in a shape the normalizer doesn't map to ResponseOutputMessage (e.g. reasoning-only turn, refusal, or content nested differently), content='' -> check_duplicate returns is_duplicate:False 'Empty response from LLM' (dedupe.py:336-342) -> duplicate findings leak into the report. This is exactly the LLM-judge fragility fusion thesis #2 targets.",
            "mitigation": "Phase 1: log+alert on empty extraction and fall back to a conservative same-endpoint/same-CVE heuristic rather than silently declaring non-duplicate; the deterministic _check_dependency_duplicate path (dedupe.py:173-229) already shows the pattern. Phase 2/fusion: route dedup through CRUCIBLE oracle re-execution of poc_script_code (reporting/tool.py:149) so dedup no longer depends on an LLM judge's output shape at all."
          },
          {
            "severity": "med",
            "risk": "xhigh/minimal reasoning levels dropped or errored. ReasoningEffort literal includes 'minimal' and 'xhigh' (settings.py:11); litellm.drop_params=True (models.py:157) means any effort value LiteLLM's Anthropic adapter doesn't recognize is SILENTLY dropped, so a scan configured for xhigh may run at Claude's default effort with no warning — a stealth quality regression on the highest-value setting.",
            "mitigation": "Constrain ReasoningEffort to Anthropic-valid {low,medium,high,xhigh,max} for anthropic/* and map 'minimal'->'low'; log the resolved effort. Phase-2 provider passes output_config.effort verbatim (xhigh/max are valid on Opus 4.8)."
          },
          {
            "severity": "med",
            "risk": "tool_choice='required' is a no-op on Claude. _accepts_required_tool_choice returns True only for openai models (inputs.py:27-33), so STRIX_FORCE_REQUIRED_TOOL_CHOICE never applies to Claude. This is CORRECT (forced tool_choice conflicts with extended thinking; Bedrock even requires thinking disabled), but it means Claude's 'always call a lifecycle tool' guarantee rests entirely on the prompt + the _run_noninteractive_until_lifecycle recovery loop (execution.py:270-335), which burns up to max_turns retries if Claude emits text-only turns.",
            "mitigation": "Keep the gate off for Claude but document it; ensure the recovery-injection message (execution.py:482-499) is tuned for Claude, and consider tool_choice:{type:'any'} only on non-thinking utility calls. Do not force it with thinking on."
          },
          {
            "severity": "med",
            "risk": "Thinking-block replay across turns. Strix persists/replays conversation via SQLiteSession (sessions.py:26; execution.py:369) and out-of-band rewrites items (strip_all_images_from_session, enforce_image_budget). If Claude extended-thinking blocks must be echoed back unchanged on the same model (per the migration guide) but LiteLLM normalization or Strix's session rewrites drop/alter them, later turns can 400 on signature/ordering or silently lose reasoning continuity.",
            "mitigation": "Confirm LiteLLM preserves thinking blocks in the SQLiteSession round-trip; ensure _rewrite_session (sessions.py:70-92) never touches thinking items. Phase-2 provider controls block echo directly."
          },
          {
            "severity": "low",
            "risk": "apply_patch fidelity rides on JSON string round-trip. Because chat_completions_tools=True for Claude, apply_patch becomes a single JSON 'patch' string field (factory.py:67-91,124-159) and _extract_custom_input does json.loads then .get('patch') (94-103). Claude 4.x may escape tool-input JSON (Unicode/forward-slash) differently; a malformed escape yields '' -> 'patch must be a non-empty string' and a wasted turn.",
            "mitigation": "Already parses via json.loads (correct, not raw-string-matching). Add a targeted test feeding Claude-style escaped patch payloads through _extract_custom_input; keep strict_json_schema=False (already set)."
          },
          {
            "severity": "low",
            "risk": "Strix product telemetry still on. set_tracing_disabled(True) kills SDK tracing, but TelemetrySettings.enabled defaults True (settings.py:66) -> posthog/scarf. For an offense-capable tool folded under SIGIL's governed, local-first posture this is an unwanted egress.",
            "mitigation": "Default STRIX_TELEMETRY off in the fused build; assert no telemetry egress in the sandbox's scope/egress gate."
          }
        ],
        "sequencing": "Do this dimension FIRST among the Claude-migration work and mostly BEFORE the hard-gate/oracle fusion, because the gates wrap the same tool signatures and you want the model transport stable underneath them. Order:\n\n1. Config default + valid effort (half-day, shippable alone): pin STRIX_LLM=anthropic/claude-opus-4-8 (settings.py:23), constrain/map ReasoningEffort for anthropic/*, force STRIX_TELEMETRY off. Independently shippable — makes Strix Claude-default with no behavior risk.\n\n2. Live-fire the LiteLLM Claude path (1 day, GATES everything): run one real scan turn against Claude and capture whether reasoning_effort produces a 400 (budget_tokens) or silently no-thinking, whether cache_read_input_tokens is ever >0, and whether get_total_llm_cost increments. These three runtime facts decide how much of Phase 2 is mandatory vs optional. Tests cannot substitute — test_models.py/test_inputs.py never issue a live call.\n\n3. Hardenings on the LiteLLM path (2-3 days, incremental): (a) prompt-cache the system prefix; (b) fix cost so the budget gate fires; (c) make dedupe fail-closed on empty extraction; (d) log resolved effort. Each ships independently.\n\n4. Phase 2 direct-Anthropic Model provider (1-2 wks, optional but recommended): only needed if step 2 shows the LiteLLM path can't cleanly do adaptive thinking + effort + caching + real cost. Localized to StrixProvider._resolve_prefixed_model (models.py:45-60) + a new Model subclass + adding the anthropic SDK to deps; does NOT touch SandboxAgent/Runner/hooks/sessions, so it's parallelizable with the sandbox-gate and oracle-fusion dimensions.\n\n5. NOT NOW / possibly never: port to the Claude Agent SDK. It would rebuild the sandbox execution + agent graph and is the wrong place to spend during a fusion whose value is CRUCIBLE truth + SIGIL governance, not a new agent harness.\n\nThe Claude-migration dimension is almost entirely self-contained inside Strix; its only cross-fusion couplings are the two seams the OTHER dimensions consume unchanged: dedupe.check_duplicate (replaced by CRUCIBLE oracle) and ReportUsageHooks/cost (fed into SIGIL governor). Keep those signatures stable so this dimension can land before or in parallel with the gate/oracle work.",
        "open_decisions": [
          "Depth: LiteLLM-only (Phase 1) vs add the direct-Anthropic Model provider (Phase 2)? RECOMMEND: land Phase 1 now, then do Phase 2 unless the step-2 live-fire shows LiteLLM 1.90.1 already emits adaptive-thinking + effort + caching + real cost correctly for the target Claude IDs. The migration guide's budget_tokens-removed / effort-only rules make it likely Phase 2 is needed for the newest models.",
          "Add `anthropic` as a dependency? It is currently NOT in the tree (uv.lock has zero anthropic entries) — Claude runs purely through LiteLLM. RECOMMEND: yes for Phase 2; it is the only way to get first-class thinking/effort/caching/usage and to stop depending on LiteLLM's model_cost map for correctness and budget.",
          "Default model: claude-opus-4-8 vs claude-fable-5? Both are in RECOMMENDED_MODEL_NAMES (models.py:87-88). RECOMMEND: default claude-opus-4-8 (cheaper, same request surface, no refusal-fallback/ZDR/30-day-retention constraints); offer fable-5 as an opt-in for the hardest long-horizon scans, but note it adds refusal-handling and data-retention requirements the offense pipeline must handle.",
          "Force reasoning on or leave adaptive? RECOMMEND: adaptive thinking with output_config.effort=high as the Strix default (settings.py default is already 'high'); do NOT hardcode budget_tokens anywhere. Reserve xhigh for deep scan_mode only.",
          "tool_choice on Claude: keep gated off (current) or enable {type:'any'} for non-thinking utility calls? RECOMMEND: keep off globally; forced tool_choice + extended thinking is unsupported/awkward on Anthropic. Rely on the existing non-interactive recovery loop.",
          "Dedup ownership: harden the LLM judge (Phase 1) or hand it to CRUCIBLE oracle re-execution now (fusion thesis #2)? RECOMMEND: do the cheap fail-closed fix in Phase 1 so dedup is safe immediately, but treat the CRUCIBLE oracle re-execution of poc_script_code as the real target — the LLM judge should become a pre-filter, not the authority.",
          "Budget accounting authority: keep Strix's litellm cost callback or make SIGIL's signed governor the source of truth? RECOMMEND: compute a reliable Anthropic cost in Strix (fixing the zeroing bug) AND emit it to SIGIL's governor so the signed spine holds the authoritative, non-bypassable budget — aligning the migration with the fusion's governance thesis."
        ]
      },
      {
        "dimension": "monorepo-core",
        "recommended_approach": "MONOREPO = uv workspace of independently-buildable members, each keeping its own backend; SHARED CORE = SIGIL's v2 chain promoted to source of truth; RUNTIME = two isolated environments, not one venv.\n\nLAYOUT (verified against the three build files):\n- `packages/core/` → new member `vigil_core` = SIGIL's `sigil/reuse/*` (canonical.py, chain.py, crypto.py, models.py, __init__.py; 341 LoC I read) lifted verbatim. Pure-Python, deps = cryptography + pydantic only. Imports NO `framework.*` and NO `strix.*` — this is the property that keeps SIGIL's `assert_no_offense()` (reuse/__init__.py:26) sound. Backend: hatchling or setuptools (pure Python, no Rust).\n- `apps/sigil/` ← `git subtree add --prefix=apps/sigil` from github thuram-nana/sigil. Keeps its own `pyproject.toml` (setuptools>=61 + setuptools-rust>=1.9, the `[[tool.setuptools-rust.bins]] target=sigil-kernel path=kernel/Cargo.toml`) and `MANIFEST.in` (`graft kernel`). Rust WARDEN crate rides in the wheel unchanged.\n- `engine/crucible/` ← `git subtree add --prefix=engine/crucible` from github thuram-nana/PENTEST. Its `framework/` top-level package (0-byte `framework/__init__.py`, importable `framework.v2`) sits at `engine/crucible/framework/`; the root pyproject there (`name=crucible`, setuptools, `packages.find include=[\"framework*\"]`) still builds `python3 -m framework.v2`. The nested `framework/v2/pyproject.toml` stays as mypy/pytest config.\n- `vendor/strix/` ← `git subtree add --prefix=vendor/strix` from usestrix/strix (Apache-2.0). Keep `strix/` pkg + `containers/Dockerfile` + hatchling backend; RETAIN `LICENSE` and ADD a `NOTICE` stating modifications (Apache-2.0 §4 — required because \"Claude everywhere\" modifies the work).\n\nWHICH v-schema wins — decided by the code, not taste: SIGIL's `reuse/chain.py` + `reuse/models.py` are a STRICT, BACKWARD-COMPATIBLE SUPERSET of CRUCIBLE's `evidence/chain.py` + `evidence/models.py::SignedChainHead`. SIGIL's `_head_payload` (chain.py:55-65) drops the six `_HEAD_V2_FIELDS` when `schema_version < 2`, so a v1 head signs BYTE-IDENTICALLY; `verify_head` uses `entry_count == base_count + len(entries)` which reduces to CRUCIBLE's v1 `entry_count == len(entries)` when `base_count==0`; and `verify_chain(genesis_prev=…)` defaults to `_GENESIS_PREV`. The domain tag `b\"crucible-evidence-v1\\x00\"` is already byte-identical in both trees (SIGIL reuse/canonical.py:14 == CRUCIBLE evidence/canonical.py:21). Therefore: adopt SIGIL v2 as the shared core; CRUCIBLE UPGRADES `evidence.SignedChainHead`+`evidence.chain` to it WITHOUT changing the domain tag and WITHOUT invalidating a single existing signature. The reverse is impossible and is the whole reason the direction is forced: CRUCIBLE's v1 `SignedChainHead` is `extra=\"forbid\"`, so it cannot even PARSE a v2 pruned head, and its non-version-conditional `_head_payload` would sign different bytes — v1 can never verify v2.\n\nWHAT IS shared vs NOT: the core is only the leaf primitives — `canonical_json`/`sha256_hex`/`digest_payload`/`evidence_signing_bytes`, the crypto (`sign`/`verify_one`/`verify_threshold`/`generate_keypair`), and the chain+trust models `{Signature, AuthorizerKey, TrustRoot, ChainEntry, SignedChainHead(v2)}` + `build_chain`/`append_entry`/`verify_chain`/`sign_head`/`verify_head`. Everything offense-specific STAYS in CRUCIBLE and imports the core: `EvidenceCertificate`/`ReportClaim`/`ArtifactRef`/`PathCertificate` (evidence/models.py:28-179), `pcf.py`, `certify.py`, all of `entitlement/` (the `crucible-entitlement-v1`/`crucible-revocation-v1` domains), `authority/` (`crucible-authority-v1`), `aegis/`, `veracity/`, `agents/spine_chain.py`. The higher layers keep their OWN domain-tagged `*_signing_bytes` wrappers and just call the shared `canonical_json`.\n\nTWO ENVIRONMENTS (the load-bearing build decision): do NOT co-install SIGIL and the offense engine in one interpreter. `env-sovereign` = vigil_core + sigil (+extras), with NEITHER `framework` NOR `strix` installed, so `assert_no_offense()` is structurally unfailable. `env-offense` = vigil_core + crucible (framework.v2) + strix + openai-agents + docker + caido-sdk-client. They interoperate over signed artifacts / the spine / IPC, never in-process. This is not only cleaner — it is the only way out of an unsatisfiable single lock (SIGIL pins cryptography==49.0.0 & pydantic==2.13.4 and drags fastembed/onnxruntime/kuzu/qdrant; Strix drags openai-agents[litellm]==0.14.6 + openai + litellm; forcing all into one uv lock is a resolver minefield and semantically forbidden).",
        "key_integration_points": [
          "SHARED-CORE SEAM: extract to `packages/core/vigil_core/` from SIGIL's `sigil/reuse/{canonical,chain,crypto,models,__init__}.py` (v2, 341 LoC). CRUCIBLE rewire is a mechanical import-path change (no logic change) across ~25 modules found by grep: evidence/{chain,models,pcf,certify,claims,cli,__init__}.py, entitlement/{crypto,models,canonical,policy}.py, authority/{signing,gate,store,models}.py, aegis/{rampart,models}.py, veracity/{firewall,claims}.py, agents/spine_chain.py, sensors/{nmap,fuzz,web_scanner,cloud}.py, repeater/tool.py, improve/merge_gate.py, verify/reachability.py, imports/to_observations.py, __main__.py.",
          "SIGNATURE-COMPAT PIVOT: `evidence/chain.py::_head_payload` (currently chain.py:64-68, unconditional dump) must become SIGIL's version-conditional form (reuse/chain.py:55-65) and `evidence/models.py::SignedChainHead` (models.py:194-205, v1 fields only) must gain the six v2 fields with NO-PRUNE-identity defaults (reuse/models.py:56-73). Domain tag `_EVIDENCE_DOMAIN=b\"crucible-evidence-v1\\x00\"` MUST NOT change.",
          "assert_no_offense INVARIANT: `sigil/reuse/__init__.py:28` matches the literal string `\"framework\"`/`framework.`. In the fused repo it must be widened to also bar `strix`/`strix.*` (the new agentic-offense body) and made a parametrized offense-namespace list; and CRUCIBLE must stay under the `framework.*` namespace (or the guard updated in lockstep) or it silently passes.",
          "WARDEN GATE SEAM (thesis #3): `sigil/governor/__init__.py:16` and `sigil/mesh/__init__.py:7` CALL `assert_no_offense()` at import. env-offense CANNOT `import sigil.governor` to reuse the tier gate — it would crash the moment `framework.v2` is loaded. The correct seam is the Rust binary: `sigil/config.py::kernel_bin()` (config.py:114) resolves the `sigil-kernel` executable shipped in SIGIL's wheel script-dir; Strix's exec_command/repeat_request tools must invoke that binary as a SUBPROCESS (A0-A3 fail-closed authorizer), never the Python package.",
          "ERROR-TYPE RECONCILE: shared `crypto.py` raises a core-local `IntegrityError` (reuse/crypto.py:29) because it MUST NOT import CRUCIBLE's `EntitlementError` (framework/v2/common/errors.py:129 → subclass of CrucibleError → pulls `framework`, breaks the core's offense-freeness). CRUCIBLE call sites that currently `except EntitlementError` around `verify_one`/`_b64decode_exact` must also catch the core `IntegrityError` (make it a shared base or alias).",
          "BUILD/WORKSPACE ROOT: root `pyproject.toml` with `[tool.uv.workspace] members=[...]` + `[tool.uv.sources] vigil_core={workspace=true}`; each member keeps its own build-system (setuptools+setuptools-rust / setuptools / hatchling). Strix already ships `uv.lock` (490 KB) — reuse uv as the workspace driver.",
          "STRIX SANDBOX RUNTIME: `vendor/strix/containers/Dockerfile` is `FROM kalilinux/kali-rolling:latest`, bakes Caido CLI v0.56.0 fetched from `https://caido.download` at image-build, installs `caido-sdk-client` in-image; `strix/runtime/docker_client.py:168` PULLS a prebuilt image from a registry. This is a runtime/registry artifact + Docker daemon dependency, orthogonal to the pip build — env-offense needs Docker + a published sandbox image, not just wheels."
        ],
        "risks": [
          {
            "severity": "high",
            "risk": "assert_no_offense() is hardcoded to the literal `framework` (reuse/__init__.py:28). The fusion adds a SECOND offensive body — Strix (`strix.*`) — that the guard does not bar, and if CRUCIBLE is ever renamed out of the `framework.*` namespace the guard silently passes with offense fully loaded. SIGIL's headline 'offense-free by construction' guarantee evaporates silently.",
            "mitigation": "Before any Strix code lands, widen the guard to a parametrized set {`framework`,`strix`,...} and add a unit test that imports each offense top-level and asserts RuntimeError. Freeze CRUCIBLE's `framework.*` namespace (documented invariant) or gate any rename behind updating the guard."
          },
          {
            "severity": "high",
            "risk": "Thesis #3 (SIGIL WARDEN wraps every Strix action) collides head-on with SIGIL's own sovereignty guard: `sigil/governor/__init__.py:16` calls assert_no_offense() at import, so importing the Python governor inside env-offense (where framework.v2 is loaded) raises immediately. A naive in-process wiring crashes the offense engine on startup.",
            "mitigation": "Wire the hard gate to the RUST kernel binary via subprocess (config.kernel_bin() → `sigil-kernel`), which is process-isolated and namespace-agnostic. Never `import sigil.governor`/`sigil.mesh` from env-offense. Ship the kernel bin as a standalone artifact resolvable on env-offense's PATH."
          },
          {
            "severity": "high",
            "risk": "Single shared venv/lock is unsatisfiable or brittle. SIGIL pins cryptography==49.0.0 & pydantic==2.13.4 and drags fastembed/onnxruntime/kuzu/qdrant; Strix drags openai-agents[litellm]==0.14.6 + openai>=2.26,<2.45 + litellm. openai-agents/litellm can pin pydantic-core/httpx against CRUCIBLE's httpx>=0.28,<1 or against SIGIL's exact pins, and a cryptography security bump is blocked by SIGIL's ==49.0.0.",
            "mitigation": "Two runtime environments with two locks (env-sovereign vs env-offense), both path-depending on packages/core. This is also mandated by the sovereignty model (offense must not be importable in SIGIL's interpreter). Keep vigil_core's own deps minimal (cryptography>=42,<50, pydantic>=2.11,<3) so it satisfies BOTH locks."
          },
          {
            "severity": "high",
            "risk": "Ordering hazard on the v2 upgrade: because v1 CRUCIBLE verifiers/parsers CANNOT read a v2 pruned head (SignedChainHead is extra=forbid; _head_payload non-conditional), if SIGIL/any producer emits a v2 (pruned) head that a not-yet-upgraded CRUCIBLE consumer or an old external verifier must read, verification fails closed on real data.",
            "mitigation": "Sequence S3 (upgrade CRUCIBLE evidence to v2) BEFORE enabling any prune/v2-head emission in the fused pipeline. Gate S3 on a regression test: an OLD pre-fusion v1 signed evidence bundle must verify byte-identically under the new v2 verifier."
          },
          {
            "severity": "med",
            "risk": "EntitlementError→IntegrityError behavior change: CRUCIBLE's entitlement/crypto.py raises `EntitlementError` on malformed key/sig; the shared core raises `IntegrityError`. CRUCIBLE code doing `except EntitlementError` around threshold/verify calls would stop catching malformed-material errors, changing failure semantics (provisioning errors become uncaught).",
            "mitigation": "Define the core error as the shared base and re-export/alias `EntitlementError` from it in CRUCIBLE (or add IntegrityError to the except tuples). Audit the ~handful of call sites; malformed-material is a provisioning path, not attacker-controlled, so blast radius is small but must be explicit."
          },
          {
            "severity": "med",
            "risk": "Python floor mismatch: Strix requires-python>=3.12, SIGIL & CRUCIBLE declare >=3.11 (CRUCIBLE mypy pins 3.11). A unified env-offense floor of 3.12 drops CRUCIBLE's 3.11 support and its mypy target is stale.",
            "mitigation": "Set env-offense floor to 3.12 (both SIGIL and CRUCIBLE already advertise 3.12/3.13 classifiers). Bump CRUCIBLE's `[tool.mypy] python_version` to 3.12. env-sovereign (sigil) can keep 3.11 if desired since it doesn't co-run with Strix."
          },
          {
            "severity": "med",
            "risk": "Strix sandbox supply-chain/reproducibility: Dockerfile fetches Caido v0.56.0 from https://caido.download at image build and PULLS a prebuilt Kali image from a registry (docker_client.py:168). Neither is a pinned/hash-verified pip artifact; the running sandbox is outside the signed-evidence trust boundary the fusion promises.",
            "mitigation": "Pin the sandbox image by digest, mirror/checksum the Caido tarball, and treat the sandbox image build as a separate signed release artifact. Document that the Caido-captured request (fed to the oracle for re-execution) is the trust anchor, not the sandbox image itself."
          },
          {
            "severity": "low",
            "risk": "Subtree hygiene: CRUCIBLE working tree carries cruft (688 untracked graphify-out/cache files, a .coverage, targets/, a 137 KB README). Subtree only imports TRACKED content so untracked cache won't come, but tracked cruft (targets/, graphify-out tracked files) pollutes the prefix.",
            "mitigation": "Clean/gitignore graphify-out, .coverage, targets/ in the source repos (or a throwaway cleaned branch) before `git subtree add`. Prefer full-history subtree for the two owned private repos (provenance matters to a signed-spine project); --squash acceptable for Strix but full history enables later `git subtree pull` from upstream."
          }
        ],
        "sequencing": "Strictly incremental, each slice independently green. S0 SCAFFOLD: create the monorepo, `git subtree add` all three at current HEAD into apps/sigil, engine/crucible, vendor/strix; add uv workspace root. No code change. GATE: each member's existing suite passes unchanged under its own backend (SIGIL ~400 py + 26 Rust; CRUCIBLE full; Strix pytest). S1 EXTRACT CORE: add `packages/core/vigil_core` = SIGIL reuse/* verbatim (v2); tests = union of SIGIL spine/reuse tests (test_spine_hardprune_headv2.py already exists) + CRUCIBLE evidence/chain + entitlement/crypto tests re-pointed at vigil_core. GATE: vigil_core green incl. a v1-head→byte-identical-signing regression. S2 SIGIL ADOPTS CORE: replace reuse/ internals with re-export from vigil_core (or delete + import); WIDEN assert_no_offense to bar strix.* too. GATE: SIGIL full suite green + a guard test proving both framework.* and strix.* raise. S3 CRUCIBLE ADOPTS CORE (make-or-break): rewire the ~25 modules to import canonical/crypto/chain primitives from vigil_core; upgrade evidence.SignedChainHead + evidence.chain to the v2 form; reconcile EntitlementError/IntegrityError. GATE: CRUCIBLE full suite green AND a pre-fusion v1 signed evidence bundle verifies unchanged (signature-compat regression). S4 STRIX VENDOR+BUILD: retain LICENSE, add NOTICE of modifications, convert to workspace member, resolve env-offense lock (crucible+strix+vigil_core co-resolve). GATE: Strix tests green under workspace; env-offense lock resolves. S5 GATE-WIRING (other agents' dimensions; build seam only here): env-offense invokes the sigil-kernel Rust subprocess for the WARDEN gate and imports crucible scope_gate — never sigil.governor. Shippable increments: after S1 the core is reusable; after S2 SIGIL is fully on the core with the strengthened guard; after S3 CRUCIBLE and SIGIL share ONE signed-chain implementation (the actual 'shared CORE' thesis realized) with zero signature breakage; Strix (S4/S5) can lag without blocking the core unification. Nothing is big-bang; the two make-or-break gates are the v1→v2 byte-identical checks at S1 and S3.",
        "open_decisions": [
          "Single uv workspace lock vs two locks. RECOMMEND two locks / two runtime environments (env-sovereign, env-offense) both path-depending on packages/core — mandated by the sovereignty model and the only escape from the SIGIL-exact-pins vs openai-agents co-resolution conflict. Use the uv workspace only for dev-tooling/path wiring, not one runtime lock.",
          "Relax SIGIL's exact pins (cryptography==49.0.0, pydantic==2.13.4) in the workspace? RECOMMEND keep exact pins inside the sigil member's own lock for sovereign reproducibility, but make vigil_core declare compatible RANGES (cryptography>=42,<50, pydantic>=2.11,<3) so it satisfies both env locks; do NOT let SIGIL's exact pin leak into env-offense's resolution.",
          "Where the shared IntegrityError lives and whether to alias EntitlementError. RECOMMEND vigil_core defines IntegrityError as the canonical base; CRUCIBLE re-exports `EntitlementError = IntegrityError` (or subclasses) so existing `except EntitlementError` sites keep catching malformed-material errors.",
          "Keep CRUCIBLE under the `framework.*` namespace vs rename to `crucible.*`. RECOMMEND keep `framework.*` frozen — a rename silently defeats assert_no_offense unless the guard is updated in lockstep; if a rename is ever wanted, gate it behind updating the parametrized offense-namespace list + its test.",
          "Subtree squash vs full history, and Strix vendoring depth. RECOMMEND full-history subtree for both owned private repos (thuram-nana/sigil, thuram-nana/PENTEST) for provenance; full-history for Strix too (enables later `git subtree pull` from usestrix/strix upstream and satisfies Apache attribution alongside retained LICENSE+NOTICE). Clean graphify-out/.coverage/targets cruft before subtreeing.",
          "Monorepo root name/location. Not derivable from the three trees (the /home/kali/vigil-apex working dirs in the env are a different TS/blockchain stack, not this fusion). OWNER MUST NAME IT; layout above is name-agnostic."
        ]
      },
      {
        "dimension": "governance-safety",
        "recommended_approach": "GOVERNANCE = \"two orthogonal, conjunctive gates over one signed spine, backstopped by a network the agent cannot see past.\" Do NOT merge SIGIL's and CRUCIBLE's authz into one model — compose them. CRUCIBLE answers \"is THIS target/action inside the signed engagement envelope right now?\" (authority/gate.py:authorize_action — order killswitch→window→scope→destructive→live_destructive→budget, fail-closed). SIGIL answers \"what TIER is this action-class, and must the owner approve it now?\" (kernel/src/tiers.rs:classify + governor/core.py:Governor.decide). Every target-touching action must pass BOTH; first failure wins; any error fails closed.\n\nREPLACING assert_no_offense() (sigil/reuse/__init__.py:26). Today it bans any `framework.*` import — the guarantee the owner is trading away. Replace it with a CAPABILITY BOUNDARY, not deletion. (a) Keep the personal-orchestrator process offense-free: it still runs assert_no_offense() and never imports the offense package. (b) Run Strix's agent loop + CRUCIBLE executors as a SEPARATE, untrusted \"offense worker\" trust domain — a SIGIL agent with a ceiling and NO owner key, exactly like today's mesh agents which cannot forge governance events (governor/authn.py:verify_signed — a forged grant grants nothing because the agent lacks the owner key). (c) Loading the offense capability at all becomes `assert_offense_gated()`: an explicit, A3, owner-signed, spine-logged, auto-expiring state transition bound to one verified charter+authority (mirror governor/killswitch.py release semantics). Offense mode cannot be entered without a non-tripped killswitch + verified SignedAuthority (authority/store.py:load_verified_authority) in window. The invariant flips from \"offense code is not loadable\" to \"offense capability is unreachable except through the governed dispatch, and its existence is itself an owner-signed, time-boxed, revocable fact.\"\n\nFORCING EVERY ACTION THROUGH THE GATE. The single choke point is Strix's `_wrap_exec_command` (agents/factory.py:208-232) and the proxy `repeat_request` (tools/proxy/tools.py:352) — TODAY both are cosmetic (exec wrapper only injects shell:bash and formats errors; NO scope/target/authority check). Replace with a three-layer gate:\n- Layer 1 (SOUND, MANDATORY, the load-bearing one): an L3/L4 egress firewall on the sandbox, derived from the signed charter scope (resolve scope hosts→IPs at engagement start, pin them), deny-default, with 169.254.169.254, RFC1918, and host.docker.internal DENIED unless charter-listed. This must live on the HOST/docker-network side, NOT in the container — because docker_client.py:215 unconditionally grants NET_ADMIN, so an in-container agent can tear down any in-container firewall. This is the CRUCIBLE egress_guard.py idea (SovereignHttpxTransport) generalized from httpx-only to the whole netns — necessary because nmap/NET_RAW/raw sockets and any subprocess bypass httpx and the http_proxy env (docker-entrypoint.sh only sets http_proxy/https_proxy; Caido scope_rules is a capture filter with \"empty allowlist = allow all\" and is agent-controllable — NEVER a boundary). The firewall makes off-scope action physically impossible, converting prompt-scope into network-scope.\n- Layer 2 (fast-fail + audit): at the exec/repeat_request wrapper, best-effort extract target host(s), call CRUCIBLE scope_gate.validate_action (charter-signed + in-scope + destructive-classifier) and authority.require_authorization, refuse-before-the-wire, and LOG every invocation to the signed spine via WARDEN.record (warden.rs:44 already records agent/tool/args_hash/tier/decision/approver/result_hash/ts). Fail-closed: a command whose target can't be determined is treated as A3/owner-approval (the shell is opaque — `bash -c 'for h in $(cat hosts); do curl $h; done'` defeats any static parse, which is exactly why Layer 1 is the real guarantee and Layer 2 is audit+UX).\n- Layer 3 (approval/budget): WARDEN tiering. The existing token classifier already fail-closes offense correctly WITHOUT modification — `exec_command`→token `exec`→A3, `repeat_request`→unknown→A3 (tiers.rs:classify). But name-classification is target-blind: `exec_command(\"cat README\")` and `exec_command(\"nmap live-host\")` are the same name→both A3, which is unusable for a 57-skill autonomous agent running thousands of in-container commands. Resolution: WARDEN sets the FLOOR (offense exec ≥ A2, never A0 — there is no auto offense), and Layer-1's firewall is what LETS you safely lower an in-scope target-touching command to a per-engagement-promoted A2 instead of per-action A3. Without the firewall, every command is owner-gated; with it, in-scope recon can be A2-promoted (governor/promotion.py) while exploit/destructive stays A3-per-action with LIVE second-ack (authority/gate.py live_destructive).\n\nUNIFYING THE TWO AUTHZ MODELS on shared primitives. SIGIL already vendors CRUCIBLE's crypto verbatim (reuse/__init__.py re-exports canonical_json/build_chain/sign_head/verify_head/verify_threshold/TrustRoot/Signature; SIGIL's v2 Merkle-prune head is ahead). So: ONE TrustRoot document where the owner key is one AuthorizerKey (required signer of the threshold, so owner-in-the-loop is structural) and the engagement-governance panel keys are others; CRUCIBLE authority/signing.py and SIGIL governor/authn.py both sign over canonical JSON under that root. ONE killswitch: keep the spine-backed, owner-signed-release one (governor/killswitch.py — asymmetric: any engage halts, only owner-signed release un-halts, survives restart, prompt-injection-proof) as authoritative; keep CRUCIBLE's file-backed one (authority/killswitch.py, ambiguous-stat=tripped) as a redundant HALT-only path (redundancy in the safe direction is free); CRUCIBLE's gate reads the spine verdict. ONE append-only signed spine as the single log for governor events + WARDEN action records + CRUCIBLE facts/PCF certs. Budgets stack: CRUCIBLE max_actions (per engagement) AND SIGIL daily caps (governor/budget.py). Preserve all THREE CRUCIBLE authz surfaces (ethics.py): intake-ledger for passive recon, charter signature for scope, SignedAuthority for time/budget/destructive.\n\nTELEMETRY/EGRESS HARDENING (pure subtraction, do early): flip AND remove. TelemetrySettings.enabled defaults True (config/settings.py:66); posthog.py beacons scan_started/finding_reported(severity+CWE!)/scan_ended to us.i.posthog.com with a hardcoded key; scarf.py beacons to strix.gateway.scarf.sh — in an offense tool this leaks operator activity (\"scanned X, found a CRITICAL CWE-89\"). Do not merely gate on config (one env var from firing) — delete the network calls; if usage stats are wanted, write them to the local spine. web_search/tool.py does requests.post to api.perplexity.ai HOST-SIDE (bypasses the container firewall entirely) with the agent's attacker-influenceable query — disable or route via CRUCIBLE's gated, disjoint-from-target collectors (egress_guard.collector_scope_conflicts already enforces disjointness). Strip the OpenRouter attribution headers (config/models.py:_configure_openrouter_attribution leaks HTTP-Referer strix.ai). set_tracing_disabled(True) is already called — keep it. Accept and document that Claude-everywhere means engagement context transits to api.anthropic.com (SIGIL's local-only sovereignty is gone for the offense half); mitigate with a redaction pass (CRUCIBLE common/redact.py) on captured creds/request bodies before they enter model context.\n\nIRREDUCIBLE SAFETY SPEC (non-waivable, must all hold simultaneously): (1) no target-touching action without a verified, in-window, non-tripped, in-scope, in-budget authority — fail-closed on any ambiguity; (2) off-scope egress physically impossible via a host-side, deny-default, charter-derived L3/L4 firewall outside NET_ADMIN's reach; (3) every action logged to the hash-chained, Ed25519-signed, anchored spine at/before execution — nothing acts unlogged; (4) the offense worker holds no owner key and no spine-write authority — it can only PROPOSE; governance events verify only under owner/panel keys; (5) killswitch is absolute, persistent, fail-closed, owner-only to clear, and drops the worker to A0-observe; (6) there is no A0 offense — target-touching recon ≥ A2, exploit/destructive = A3 with LIVE second-ack; (7) a self-reported finding is a PROPOSAL, a FACT+PCF cert exists only after CRUCIBLE oracle re-execution, then the owner key signs it into the spine (Strix report/dedupe.py LLM-judge is demoted to a pre-filter); (8) no third-party beaconing (telemetry removed, not disabled); (9) offense-mode is an explicit, signed, auto-expiring, revocable state; (10) the personal-memory spine and the engagement store are separate trust domains — the worker never gets a handle to personal memory.",
        "key_integration_points": [
          "STRIX EXEC CHOKE POINT — strix/agents/factory.py:_wrap_exec_command (208-232) and _configure_shell_tools (255-266): today cosmetic (only injects shell:bash + formats errors, ZERO authz). This is where CRUCIBLE agents/scope_gate.py:validate_action + authority/gate.py:require_authorization + WARDEN warden.rs:record must be inserted before invoke_tool(). Also wrap _wrap_write_stdin (235) and tools/proxy/tools.py:repeat_request (352).",
          "SANDBOX NET BOUNDARY — strix/runtime/docker_client.py:_create_container:215-220 unconditionally appends NET_ADMIN+NET_RAW and host.docker.internal→host-gateway. Drop NET_ADMIN (it lets the container undo any in-container firewall); apply the charter-derived deny-default egress firewall on the docker network / host netfilter here (or via _apply_sandbox_network:62 to a locked-down network). This is the sound enforcement layer.",
          "WARDEN TIER GATE — sigil/kernel/src/tiers.rs:classify (already sends exec_command→A3, repeat_request→A3, unknown→A3 fail-closed, list_requests→A0) + warden.rs:decide/record. Offense tools register here; the gate must be made target-AWARE (name-only classification is too coarse — same tool name for benign in-container and target-touching commands).",
          "CRUCIBLE AUTHORITY GATE — framework/v2/authority/gate.py:authorize_action / require_authorization (killswitch→window→scope→destructive→live_destructive→budget) fed by authority/store.py:load_verified_authority (m-of-n threshold via TrustRoot). This becomes the per-action envelope check the exec wrapper calls.",
          "SHARED CRYPTO CORE — sigil/reuse/__init__.py already re-exports CRUCIBLE's canonical_json/build_chain/sign_head/verify_head/verify_threshold/TrustRoot/Signature verbatim; sigil/reuse/chain.py:{build_chain,sign_head,verify_head}. Unify the TrustRoot doc here so authority/signing.py and governor/authn.py sign under one root with the owner key as a required threshold signer.",
          "assert_no_offense REPLACEMENT — sigil/reuse/__init__.py:26. Becomes assert_offense_gated(): personal-orchestrator process keeps the import ban; offense worker requires a verified SignedAuthority + non-tripped killswitch to construct the offense capability, logged as an A3 owner-signed spine transition.",
          "UNIFIED KILLSWITCH — sigil/governor/killswitch.py (spine-backed, owner-signed-release, authoritative) reconciled with framework/v2/authority/killswitch.py (file-backed, kept as redundant halt-only path). CRUCIBLE gate.py already reads a killswitch; point it at the spine verdict.",
          "SIGNED SPINE SINK — sigil/spine/store.py:append (v2 Merkle-prune) as the single append-only log; CRUCIBLE facts/PCF certs + WARDEN records + governor events all land here; offense worker gets an engagement-scoped store handle, NEVER the personal-memory store.",
          "TELEMETRY KILL — strix/config/settings.py:TelemetrySettings.enabled (66, default True→remove), strix/telemetry/posthog.py + scarf.py (delete network calls), strix/tools/web_search/tool.py:60 (api.perplexity.ai host-side egress), strix/config/models.py:_configure_openrouter_attribution (strip strix.ai referer).",
          "PROVIDER CONFIG — strix/config/models.py:RECOMMENDED_MODEL_NAMES already lists anthropic/claude-opus-4-8; StrixProvider(MultiProvider)→LiteLLM. Anthropic api endpoint must be on the egress allowlist as the one in-policy external egress; redact captured secrets before model context."
        ],
        "risks": [
          {
            "severity": "fatal",
            "risk": "Sandbox has NET_RAW+NET_ADMIN, host.docker.internal→host-gateway, and NO egress firewall (docker_client.py:215-220); scope is prompt-level only (_wrap_exec_command is cosmetic). A prompt-injected or confused autonomous offense agent can scan/attack the operator's OWN host and LAN, cloud metadata (169.254.169.254), or any third party — from a tool that also holds the owner's personal spine. Targets serve attacker-controlled content the agent reads, so injection is a live vector.",
            "mitigation": "Do NOT run the fused tool until the host-side, deny-default, charter-derived L3/L4 egress firewall exists (deny metadata/RFC1918/host-gateway unless charter-listed) AND NET_ADMIN is dropped so the container cannot undo it. This is the gate on everything else."
          },
          {
            "severity": "fatal",
            "risk": "assert_no_offense() (reuse/__init__.py) is the entire offense-free guarantee and it is being deleted. If offense is simply made importable with nothing replacing the boundary, the fused tool is an ungoverned weapon: any code path (including personal-orchestration agents or a malicious dependency) can reach offense capability directly, unlogged, unauthorized.",
            "mitigation": "Replace with assert_offense_gated() + a separate untrusted offense-worker trust domain holding no owner key and no direct spine-write; offense capability constructible only under a verified SignedAuthority + non-tripped killswitch; entry is an A3 owner-signed, auto-expiring, spine-logged transition."
          },
          {
            "severity": "high",
            "risk": "The shell is opaque: exec_command receives an arbitrary bash string, so no per-command host parser can reliably know which targets it touches (loops, xargs, subshells, DNS tricks defeat it). A Layer-2 command-string gate alone gives false assurance and will be bypassed.",
            "mitigation": "Treat Layer-2 as fast-fail + mandatory audit only; make the L3/L4 egress firewall (Layer 1) the actual guarantee. Fail-closed: any command whose target is indeterminate is A3/owner-approval or refused under strict posture."
          },
          {
            "severity": "high",
            "risk": "Telemetry defaults ON and beacons engagement metadata — model, scan_mode, and per-finding SEVERITY+CWE — to us.i.posthog.com (posthog.py, hardcoded key) and strix.gateway.scarf.sh (scarf.py); web_search egresses attacker-influenceable queries to api.perplexity.ai HOST-SIDE, bypassing the container firewall. This leaks the operator's offensive activity to third parties.",
            "mitigation": "Delete the network calls (not just flip the default); route or disable web_search via CRUCIBLE gated collectors; strip OpenRouter attribution headers. Pure subtraction, land day one."
          },
          {
            "severity": "high",
            "risk": "Confused deputy across the offense/personal boundary: one process now holds the owner's personal memory spine AND an autonomous offense agent. A handle leak lets the offense worker read/write personal memory, or lets a personal agent invoke offense tools — a new class SIGIL's by-construction separation prevented.",
            "mitigation": "Separate trust domains: offense worker gets an engagement-scoped store handle only, no owner key, narrow gated IPC; assert the worker's store is the engagement store, not personal memory; personal orchestrator keeps assert_no_offense()."
          },
          {
            "severity": "high",
            "risk": "WARDEN classifies by tool NAME, not target (tiers.rs:classify). exec_command is A3 whether it runs `cat README` or `nmap live-host`. Naively enforced, every in-container command needs owner approval (unusable for a 57-skill autonomous agent); naively relaxed, target-touching exec silently auto-runs.",
            "mitigation": "WARDEN sets the floor (offense exec ≥ A2, never A0). The egress firewall is what makes it safe to auto-run in-scope target-touching recon at a per-engagement-promoted A2; exploit/destructive stays A3-per-action with LIVE second-ack."
          },
          {
            "severity": "med",
            "risk": "Autonomy × offense = fast in-scope blast radius. Even fully contained to charter scope, an autonomous agent can damage the charter's own LIVE target (or burn budget/exfiltrate in-scope data) faster than a human can intervene.",
            "mitigation": "Default environment TWIN/STAGING (authority/models.py:TargetEnvironment); LIVE requires charter flag + per-action second-ack (gate.py live_destructive); enforce max_actions + daily budgets; owner watches the A3 queue; killswitch always available."
          },
          {
            "severity": "med",
            "risk": "Claude-everywhere sends full agent context (captured requests, discovered creds, target source) to api.anthropic.com — SIGIL's local-only sovereignty no longer holds for the offense half.",
            "mitigation": "Conscious, documented trust decision; litellm.turn_off_message_logging already set; add a redaction pass (common/redact.py) on captured secrets before they enter model context; keep the Anthropic endpoint as the sole in-policy external egress on the allowlist."
          },
          {
            "severity": "med",
            "risk": "Strix's image build pulls dozens of tools from many hosts (nuclei templates, go modules, npm, github clones, caido.download, astral.sh) — each a supply-chain vector now inside a tool that also touches the personal spine.",
            "mitigation": "Pin versions/digests, verify checksums, build offline/from a vetted mirror where possible; the runtime egress firewall limits what a compromised tool can reach at scan time."
          },
          {
            "severity": "low",
            "risk": "Caido scope_rules (proxy/tools.py:512) is agent-controllable and 'empty allowlist = allow all domains'; it governs what the proxy CAPTURES, not what the sandbox can REACH. Mistaking it for a scope boundary gives false assurance.",
            "mitigation": "Document it as a capture filter only; the real scope boundary is the egress firewall + host-side gate. Never let scope_rules be load-bearing for authorization."
          }
        ],
        "sequencing": "STRICT ORDER — governance must land before capability. (0) BLOCKER, before ANY fused offense run: build the host-side, deny-default, charter-derived L3/L4 egress firewall on the sandbox (docker_client.py) AND drop NET_ADMIN. Until this exists the tool can hit the operator's own LAN — do not run it. This gates everything. (1) Unify the shared core: one TrustRoot (owner key a required threshold signer), one signed spine as the single log, one spine-backed owner-signed killswitch (CRUCIBLE gate reads it; keep the file-backed one as a redundant halt-only path). Mostly exists — reuse/ already vendors CRUCIBLE crypto. (2) Replace the cosmetic _wrap_exec_command/repeat_request wrappers with the real gate: CRUCIBLE scope_gate.validate_action + authority.require_authorization (refuse-before-wire) + WARDEN.record (full audit). Fail-closed on indeterminate target. (3) Replace assert_no_offense with assert_offense_gated + the separate untrusted offense-worker trust domain (no owner key, engagement-scoped store, gated IPC); offense-mode entry = A3 owner-signed, auto-expiring spine transition. (4) Wire the no-hallucinated-findings safety half: worker findings are PROPOSALS; oracle re-execution mints the FACT+PCF cert; owner key signs it into the spine; Strix LLM-judge dedup demoted to pre-filter. (5) Continuous: budgets (engagement max_actions + daily caps), LIVE second-ack UX, the A3 approval queue.\n\nINDEPENDENTLY SHIPPABLE / DO EARLY (pure subtraction, no dependency on the above): telemetry removal (settings.py default + delete posthog.py/scarf.py network calls), web_search/perplexity disable-or-gate, OpenRouter attribution strip. Land day one — it reduces attack surface and leaks immediately with zero risk to the rest of the design. The Claude-default config change (RECOMMENDED_MODEL_NAMES already includes claude-opus-4-8) is also independent and cheap.\n\nWHAT MUST COME FIRST is the firewall (0) and the unified spine/killswitch/TrustRoot (1) — no offense capability may be exposed until both hold. The rest layers on top and each layer is independently testable (the gate can be unit-tested with scope_gate's existing ScopeDecision fixtures; the killswitch with governor/authority tests).",
        "open_decisions": [
          "NET_RAW / SYN scans: keep NET_RAW for `nmap -sS` or drop both caps and accept `-sT` connect scans? RECOMMEND: drop NET_ADMIN unconditionally (it lets the container undo an in-container firewall); keep NET_RAW only if SYN scans are required, with the firewall enforced host-side where NET_RAW can't touch it.",
          "Egress enforcement mechanism: host nftables on the docker bridge vs a mandatory transparent filtering proxy the whole container is forced through? RECOMMEND: host nftables keyed to the sandbox network with deny-default + explicit metadata/RFC1918/host-gateway denies — because it also catches raw sockets and non-proxy-aware tools that a proxy alone (like Caido/http_proxy) misses.",
          "Default target environment: RECOMMEND enforce TWIN/STAGING default (authority/models.py:TargetEnvironment); LIVE requires a charter flag AND per-action second-ack (gate.py:live_destructive). Do not let the first fused run default to LIVE.",
          "Offense recon tier: per-engagement signed promotion to A2 (a whole recon sweep runs without per-nmap approval) vs A2-queued-always? RECOMMEND per-engagement owner-signed promotion for in-scope recon (governor/promotion.py), A3-per-action for exploit/destructive, and ENVOY-style no-promotion (promotion.py:NO_PROMOTION_AGENTS) for anything that egresses data off-box — safe only BECAUSE the egress firewall bounds recon to scope.",
          "Process/spine topology: one process+spine shared between personal orchestrator and offense engine, or separate processes/spines linked by signed anchor? RECOMMEND separate trust domains — offense worker holds no owner key and an engagement-scoped store, anchored into the personal spine only via signed heads (sign_head/verify_head). Preserves the most of SIGIL's sovereignty.",
          "LLM data egress: accept full engagement context transiting to api.anthropic.com, or add a redaction pass first? RECOMMEND redact captured request bodies/creds (common/redact.py) before they enter model context; document the residual Anthropic transit as an accepted, conscious trust trade (it is unavoidable given Claude-everywhere).",
          "Killswitch reconciliation: keep CRUCIBLE's file-backed killswitch (authority/killswitch.py) alongside SIGIL's spine-backed one, or remove it? RECOMMEND keep it as a redundant HALT-ONLY trip path (redundancy in the safe direction is free); authoritative 'am I halted' state = the spine, owner-signed-release only.",
          "web_search: fully disable, or re-implement through CRUCIBLE's gated collectors? RECOMMEND disable by default; if research is needed, route through gated crt.sh/DoH/RDAP-style collectors with collector_scope_conflicts enforcing disjointness from the target, never the raw api.perplexity.ai host-side path."
        ]
      }
    ],
    "synthesis": {
      "verdict": "SOUND and buildable — but only in the specific shape all four analyses independently converge on, and NOT safe to run in the naive \"one venv, true fusion\" form. There are two FATAL flaws, but both are design flaws with known mitigations, not dead-ends, so they change the SHAPE of the plan, not the go/no-go. FATAL-1: the Strix sandbox holds NET_ADMIN/NET_RAW with host.docker.internal->host-gateway and NO host-side egress firewall, and scope is prompt-level only (the exec wrapper is cosmetic) — an autonomous, prompt-injectable offense agent could reach the operator's own LAN, cloud metadata (169.254.169.254), or third parties, from a process that also holds the owner's personal spine. This MUST be closed (host-side deny-default charter-derived L3/L4 firewall on the sandbox docker network + drop NET_ADMIN; Caido/http_proxy is capture, never a boundary) before any fused offense run. FATAL-2: collapsing SIGIL and the offense engine into one interpreter either won't boot (assert_no_offense trips on framework.* import) or, if the guard is relaxed, silently destroys SIGIL's offense-free-by-construction identity — and Strix adds a SECOND offense namespace (strix.*) the guard does not even bar; a single lock is also unsatisfiable (SIGIL's cryptography==49.0.0/pydantic==2.13.4 exact pins vs openai-agents[litellm]). Resolution: two isolated environments/processes sharing only an inert, no-framework-namespace integrity core (packages/core), confirmed findings crossing as signed inert JSON, guard widened to bar strix.* too, WARDEN wired via the Rust kernel SUBPROCESS (never import sigil.governor from the offense env). On the three theses: Thesis-1 (shared core) is VERIFIED and low-risk — SIGIL's v2 chain is a byte-compatible superset of CRUCIBLE v1, the domain tag crucible-evidence-v1 is already identical, so adopting v2 as source-of-truth breaks zero existing signatures. Thesis-2 (oracle re-executes findings) is a NO-OP as written — Strix findings carry free text + a PoC string, no FindingContext, no machine-readable Caido link, and CWE is not a canonical bug_class, so reverify returns \"carries no oracle_context\"; it must be rebuilt via an OracleConfirmationAdapter (modeled on oracle_probe_executor) + an extended Strix reporting contract, confirming ONLY oracle-mappable classes as signed facts and honestly demoting the rest to labelled leads. Thesis-3 (WARDEN wraps every action) has the wrong choke point — the hard gate is the external network, WARDEN is tool-class audit/approval, and exec_command's raise-only A3 impedance mismatch must be handled by delegating target-authz to the gateway. Keep openai-agents + LiteLLM for Claude; do NOT port to the Claude Agent SDK (multi-week rewrite of the sandbox exec + agent graph for no architectural gain). No flaw is unrecoverable. Recommendation: PROCEED, network-and-governance-boundary-first, strictly incremental.",
      "top_risks": [
        {
          "severity": "fatal",
          "risk": "Unbounded sandbox egress. The Strix container is granted NET_ADMIN+NET_RAW with host.docker.internal->host-gateway and no host-side egress firewall; scope is enforced only at the prompt level (the _wrap_exec_command/repeat_request wrappers are cosmetic — zero scope/target/authority check). The shell is opaque (loops/xargs/subshells/DNS defeat any static command parse), nmap/raw sockets bypass the http_proxy, and Caido scope_rules is an agent-controllable capture filter ('empty allowlist = allow all'), never a boundary. A prompt-injected or confused autonomous agent (targets serve attacker-controlled content it reads) can scan/attack the operator's own host and LAN, cloud metadata (169.254.169.254), or any third party.",
          "mitigation": "Do not run the fused tool until a HOST-SIDE, deny-default, charter-derived L3/L4 egress firewall exists on the sandbox's docker network (host nftables keyed to the sandbox netns; 169.254.169.254 / RFC1918 / host-gateway denied unless charter-listed), placed OUTSIDE the container so NET_ADMIN cannot tear it down. Drop NET_ADMIN unconditionally; keep NET_RAW only if SYN scans are needed. Layer-2 command-string gate (scope_gate.validate_action + authority.require_authorization, refuse-before-wire) is fast-fail + mandatory audit only, fail-closed on indeterminate target; the firewall is the real guarantee. Caido = capture, not enforcement. This gates everything downstream."
        },
        {
          "severity": "fatal",
          "risk": "The offense-free boundary is removed or defeated. assert_no_offense() (sigil/reuse/__init__.py) IS SIGIL's entire 'offense-free by construction' guarantee, hardcoded to the literal 'framework'. Installing SIGIL + the offense engine into one interpreter trips it (SIGIL won't boot); relaxing it so SIGIL can import framework.v2 to sign findings silently voids the guarantee; Strix (strix.*) is a second offense body the guard does not bar; importing sigil.governor from the offense env crashes it (governor/__init__.py calls assert_no_offense at import); and a single uv lock is unsatisfiable (SIGIL cryptography==49.0.0 & pydantic==2.13.4 exact pins vs openai-agents[litellm]==0.14.6).",
          "mitigation": "Two isolated environments/processes both path-depending on packages/core: env-sovereign (vigil_core + sigil, NEITHER framework NOR strix installed -> assert_no_offense structurally unfailable) and env-offense (vigil_core + crucible + strix + openai-agents), two locks. Widen the guard to a parametrized offense-namespace set {framework, strix, ...} + a unit test asserting each offense top-level import raises. Replace assert_no_offense with assert_offense_gated: offense capability lives in a separate untrusted worker holding NO owner key + an engagement-scoped store handle, constructible only under a verified SignedAuthority + non-tripped killswitch, entered as an A3 owner-signed, auto-expiring, spine-logged transition. Wire the WARDEN gate to the sigil-kernel Rust SUBPROCESS, never `import sigil.governor` from env-offense. CI asserts assert_no_offense still passes in the SIGIL process post-merge."
        },
        {
          "severity": "high",
          "risk": "Thesis-2's oracle-re-execution pipeline is a no-op against Strix findings as-recorded. create_vulnerability_report emits free text + poc_script_code + optional endpoint/method, with NO FindingContext, no machine-readable link to the Caido baseline/mutated request IDs that form the differential, and a CWE string that is not a canonical bug_class (verifier _ALIASES has no CWE keys). Fed to reverify as-is, it returns 'finding carries no oracle_context to re-verify' — 'the oracle re-executes the finding' does literally nothing, and the LLM-judge dedup remains the authority the fusion exists to remove.",
          "mitigation": "Build an OracleConfirmationAdapter modeled on agents/oracle_probe_executor: re-drive a scope-gated baseline+probe pair, build FindingContext.from_http_responses, call OracleVerifier.confirm, and on confirm build+sign a PCF cert (evidence/certify). Extend the Strix reporting contract to carry {bug_class|cwe, injection_point/param, baseline_request_id, probe_request_id} referencing Caido captures + add a CWE->canonical-bug_class map. Confirm ONLY oracle-mappable classes (BUG_CLASS_ORACLES) as signed facts; honestly demote everything else to a labelled lead — signing unconfirmed findings as facts reintroduces exactly the hallucination the veracity firewall forbids. Deterministic dedup by oracle_context_digest/cert_digest replaces the LLM judge for CONFIRMED findings only; keep Strix dedupe for unconfirmed leads. Prefer the narrow OracleVerifier.confirm + evidence.certify path over veracity/firewall.admit() (which drags in intel.predict+worldmodel a bare finding lacks)."
        },
        {
          "severity": "high",
          "risk": "Confused deputy across the offense/personal trust boundary. In a naive fusion one process holds the owner's personal-memory spine AND the autonomous offense agent. A handle leak lets the offense worker read/write personal memory, or lets a personal-orchestration agent invoke offense tools — a whole class of failure SIGIL's by-construction separation prevented.",
          "mitigation": "Keep them as separate trust domains: the offense worker gets an engagement-scoped store handle ONLY (never the personal-memory store), no owner key, and communicates over narrow gated IPC / signed artifacts. The personal orchestrator keeps assert_no_offense() and never imports the offense package. Assert at startup that the worker's store is the engagement store."
        },
        {
          "severity": "high",
          "risk": "WARDEN classifies by tool NAME, not target, and the registry is raise-only. tiers.rs classify('exec_command') -> token 'exec' in A3_TOKENS -> A3/ExplicitRequired, and the registry cannot lower it. exec_command('cat README') and exec_command('nmap live-host') are the same name -> both A3. Naively enforced, every one of the thousands of in-container commands an autonomous 57-skill agent runs demands owner approval (unusable); naively relaxed, target-touching exec silently auto-runs.",
          "mitigation": "WARDEN sets the FLOOR (offense exec >= A2, never A0 — no auto offense) + a coarse destructive-verb block, and gates only the semantic host-side named tools (repeat_request->A2, scope_rules->A2/A3, create_vulnerability_report->A1). Delegate exec_command's TARGET authorization to the egress gateway. The firewall is what makes it safe to auto-run in-scope target-touching recon at a per-engagement owner-signed promotion to A2 (governor/promotion.py); exploit/destructive stays A3-per-action with a LIVE second-ack. Never feed the literal string 'exec_command' to WARDEN as the gating decision."
        },
        {
          "severity": "high",
          "risk": "Third-party telemetry/egress leaks operator offensive activity. TelemetrySettings.enabled defaults True; posthog.py beacons scan_started/finding_reported (severity + CWE!)/scan_ended to us.i.posthog.com with a hardcoded key; scarf.py beacons to strix.gateway.scarf.sh; web_search does requests.post to api.perplexity.ai HOST-SIDE (bypassing the container firewall entirely) with the agent's attacker-influenceable query; OpenRouter attribution leaks an HTTP-Referer of strix.ai. In an offense tool folded under SIGIL's local-first posture this exfiltrates 'scanned X, found a CRITICAL CWE-89' to third parties.",
          "mitigation": "Pure subtraction, land day one: DELETE the network calls (not merely flip the config default — that leaves them one env var from firing); disable web_search by default or route it via CRUCIBLE's gated, target-disjoint collectors (crt.sh/DoH/RDAP with collector_scope_conflicts); strip the OpenRouter attribution headers. If usage stats are wanted, write them to the local spine. Assert no telemetry egress in the sandbox's egress gate."
        },
        {
          "severity": "high",
          "risk": "The budget/governance meter silently disarms on Claude. ReportUsageHooks raises BudgetExceededError only when get_total_llm_cost() >= max_budget_usd, but that total comes from litellm_cost_callback, which returns 0 for models missing from LiteLLM 1.90.1's cost map — and the recommended future-dated IDs (claude-opus-4-8/fable-5/sonnet-5) are absent. Cost stays 0.0 -> --max-budget-usd never fires. A 'governed capability-fusion' whose spend cap is a no-op on its default model is a governance hole.",
          "mitigation": "Add an Anthropic price table (or read real usage + known per-MTok rates) so cost is nonzero; assert at startup that get_total_llm_cost increments after turn 1 for the configured model. Emit the reliable figure to SIGIL's signed governor so the spine holds the authoritative, non-bypassable budget (engagement max_actions + daily caps stack). A Phase-2 direct-Anthropic provider reports real usage/cost directly."
        },
        {
          "severity": "high",
          "risk": "Claude-via-LiteLLM transport correctness is unverified on three seams that only fail at runtime. (a) reasoning_effort: Reasoning(effort=...) is sent only if the model is in litellm.model_cost -> for future-dated IDs either extended thinking is SILENTLY never sent (Opus 4.8 then leaks reasoning into visible text) or LiteLLM emits the legacy thinking:{budget_tokens} that returns HTTP 400 on Opus 4.8/Sonnet 5/Fable 5. (b) No Anthropic prompt caching on a ~65KB static system+skills prompt rebuilt every turn across max_turns=500 (grep finds zero cache_control) -> full-price input tokens every call, a large silent cost/latency regression. (c) dedupe fails OPEN: _extract_text matches only openai ResponseOutputMessage, so a Claude shape it doesn't map yields content='' -> 'not a duplicate' -> duplicate findings leak. test_models.py only string-matches names; nothing issues a live reasoning call.",
          "mitigation": "Live-fire one real Claude scan turn (tests cannot substitute) to learn which branch each seam takes: whether effort 400s or is silently dropped, whether cache_read_input_tokens>0, whether cost increments. Then: inject cache_control:{type:ephemeral} on the byte-stable system/tools prefix; constrain ReasoningEffort to Anthropic-valid levels (map minimal->low; xhigh/max valid on Opus 4.8) and log the resolved effort; make dedupe fail-CLOSED on empty extraction (fall back to same-endpoint/same-class heuristic). If the LiteLLM path can't cleanly do adaptive-thinking + effort + caching + real cost, add a localized direct-Anthropic Model provider returned from StrixProvider._resolve_prefixed_model (adds the `anthropic` dep) — this leaves SandboxAgent/Runner/hooks/sessions untouched. Keep openai-agents; do NOT port to the Claude Agent SDK."
        },
        {
          "severity": "med",
          "risk": "Shared-core crypto migration + v1<->v2 ordering hazard. The migration re-points three divergent copies (CRUCIBLE evidence v1 + entitlement, SIGIL reuse v2) at one core; a wrong reduction or domain-tag change would invalidate every existing signed head/cert. And because CRUCIBLE v1 SignedChainHead is extra='forbid' with a non-version-conditional _head_payload, a not-yet-upgraded CRUCIBLE consumer or old external verifier CANNOT read a v2 pruned head -> verification fails closed on real data.",
          "mitigation": "Adopt SIGIL v2 as source of truth — the domain tag crucible-evidence-v1 is already byte-identical in both trees and verify_head reduces to v1 exactly at base_count=0, so signatures survive. Land behind a byte-identical re-verify gate proving every existing v1 SignedChainHead verifies under v2. Sequence the CRUCIBLE evidence->v2 upgrade (make _head_payload version-conditional, add the six v2 fields with no-prune-identity defaults) BEFORE any producer emits a v2 pruned head, gated on a regression that an OLD pre-fusion v1 signed bundle verifies byte-identically under the new verifier."
        },
        {
          "severity": "med",
          "risk": "Two trust roots collide semantically — the one genuine conflict between the analyses. PCF certs use CRUCIBLE's m-of-n governance TrustRoot; the SIGIL spine head is anchored 1-of-1 by the owner Ed25519 key. The architecture analysis recommends TWO independent anchors; the governance analysis recommends ONE unified TrustRoot with the owner key as a required threshold signer. Left ambiguous, this can produce a cert nobody can fully verify with one key.",
          "mitigation": "Owner picks (see open decisions). Either is defensible: two-anchor (CRUCIBLE governance signs the finding EvidenceCertificate; the owner key signs the spine head that chains it; both independently verifiable, neither verifier needs the other's key) OR one unified TrustRoot document where the owner key is a REQUIRED threshold signer (owner-in-the-loop becomes structural, and authority/signing.py + governor/authn.py sign under one root). Recommend the unified-root-with-required-owner-signer, keeping the two verification surfaces as documented defense-in-depth."
        },
        {
          "severity": "med",
          "risk": "EntitlementError -> IntegrityError semantics change. The shared core must raise a core-local IntegrityError (it cannot import CRUCIBLE's EntitlementError without pulling framework.* and breaking the core's offense-freeness). CRUCIBLE call sites doing `except EntitlementError` around verify_one/_b64decode_exact would stop catching malformed-key/sig errors -> provisioning errors become uncaught.",
          "mitigation": "Define vigil_core's IntegrityError as the canonical shared base; CRUCIBLE re-exports `EntitlementError = IntegrityError` (or subclasses it, or adds IntegrityError to the except tuples). Audit the handful of call sites; blast radius is small (malformed-material is a provisioning path, not attacker-controlled) but must be explicit."
        },
        {
          "severity": "med",
          "risk": "Autonomy x offense = fast in-scope blast radius. Even fully contained to charter scope, an autonomous agent can damage the charter's OWN live target, burn budget, or exfiltrate in-scope data faster than a human can intervene.",
          "mitigation": "Default TargetEnvironment to TWIN/STAGING; LIVE requires a charter flag AND per-action second-ack (gate.py live_destructive). Enforce max_actions + daily budgets; owner watches the A3 approval queue; the killswitch is always available and drops the worker to A0-observe."
        },
        {
          "severity": "med",
          "risk": "Claude-everywhere sends full agent context to api.anthropic.com. Captured requests, discovered credentials, and target source transit to Anthropic — SIGIL's local-only sovereignty no longer holds for the offense half.",
          "mitigation": "A conscious, documented, owner-accepted trust trade (unavoidable given Claude-everywhere). litellm.turn_off_message_logging is already set; add a redaction pass (common/redact.py) over captured secrets/request bodies BEFORE they enter model context; keep the Anthropic endpoint as the sole in-policy external egress on the firewall allowlist."
        },
        {
          "severity": "med",
          "risk": "Strix sandbox supply-chain and reproducibility. The Dockerfile fetches Caido v0.56.0 from caido.download and pulls a prebuilt Kali image from a registry (unpinned, un-hash-verified), and the image build pulls dozens of tools from many hosts (nuclei templates, go modules, npm, github clones, astral.sh) — each a supply-chain vector now inside a tool that also touches the personal spine, and the running sandbox sits outside the signed-evidence trust boundary.",
          "mitigation": "Pin the sandbox image by digest; checksum/mirror the Caido tarball and pin tool versions; treat the sandbox image build as a separate signed release artifact. Document that the Caido-captured request fed to the oracle for re-execution is the trust anchor, NOT the sandbox image. The runtime egress firewall bounds what a compromised tool can reach at scan time."
        },
        {
          "severity": "low",
          "risk": "Build hygiene and Claude edge cases. Python floor mismatch (Strix >=3.12 vs SIGIL/CRUCIBLE >=3.11; CRUCIBLE mypy pins 3.11). Subtree cruft (tracked targets/, graphify-out) would pollute the prefix. apply_patch rides a JSON string round-trip that Claude 4.x may escape differently, and extended-thinking blocks must survive the SQLiteSession round-trip unchanged or later turns 400.",
          "mitigation": "Set env-offense floor to 3.12 and bump CRUCIBLE's mypy python_version to 3.12 (env-sovereign/sigil can stay 3.11). Clean graphify-out/.coverage/targets before `git subtree add`. Add targeted tests feeding Claude-style escaped patch payloads through _extract_custom_input; confirm LiteLLM preserves thinking blocks and that session rewrites (_rewrite_session/strip_all_images) never touch thinking items."
        }
      ],
      "recommended_sequencing": [
        "PHASE 0 — Pure subtraction + Claude default (day one, zero cross-dependency, ships alone). Delete Strix telemetry network calls (posthog.py, scarf.py), strip the OpenRouter attribution header, disable/gate web_search off the host-side perplexity path; pin STRIX_LLM=anthropic/claude-opus-4-8, constrain ReasoningEffort to Anthropic-valid levels, force telemetry off. GREEN: Strix suite passes and a scan emits zero third-party beacons. Reduces attack surface and stops the activity leak immediately with no dependency on anything else.",
        "PHASE 1 — Monorepo scaffold, no code change. `git subtree add` all three at current HEAD into apps/sigil, engine/crucible, vendor/strix (full history for the two owned repos; retain Strix LICENSE + add a NOTICE of modifications); uv workspace root wiring only. GREEN: each member's existing suite passes unchanged under its own backend (SIGIL ~400 py + 26 Rust; CRUCIBLE full; Strix pytest).",
        "PHASE 2 — Extract the shared integrity core. packages/core/vigil_core = SIGIL reuse/{canonical,chain,crypto,models,__init__} (v2) lifted verbatim; deps cryptography+pydantic only; imports NO framework.* and NO strix.*; define IntegrityError as the canonical shared base. GREEN: vigil_core green INCLUDING a v1-head -> byte-identical-signing regression.",
        "PHASE 3 — SIGIL adopts the core + widen the guard. Re-export reuse/ from vigil_core; widen assert_no_offense to a parametrized offense-namespace set barring BOTH framework.* and strix.*. GREEN: SIGIL full suite green + a guard test proving each offense top-level import raises RuntimeError.",
        "PHASE 4 — CRUCIBLE adopts the core (make-or-break gate). Rewire the ~25 modules to import primitives from vigil_core; upgrade evidence.SignedChainHead + evidence.chain to the v2 version-conditional form WITHOUT changing the domain tag; re-export EntitlementError = IntegrityError. GREEN: CRUCIBLE full suite green AND a pre-fusion v1 signed evidence bundle verifies byte-identically under the new v2 verifier. Must land BEFORE any producer emits a v2 pruned head. After this, CRUCIBLE and SIGIL share ONE signed-chain implementation with zero signature breakage — the shared-CORE thesis realized.",
        "PHASE 5 — Two-environment build boundary + inert-data channel. env-sovereign (vigil_core + sigil, NEITHER framework NOR strix installed) vs env-offense (vigil_core + crucible + strix + openai-agents), two locks, both path-depending on packages/core; stand up the CRUCIBLE->SIGIL SignedEvidence channel over SIGIL's WG bridge/queue (no finding flow yet). GREEN: both locks resolve; CI asserts assert_no_offense() still passes in the SIGIL process.",
        "PHASE 6 — HARD EGRESS GATE (the FATAL-1 fix; highest security ROI; ships alone; parallelizable with Phases 2-5 since it's Docker/host-netfilter, not Python-package work). Put the Strix container on an internal-only docker network whose sole route out is a host-side warden-gw running nftables default-DROP keyed on the resolved charter scope (169.254.169.254 / RFC1918 / host.docker.internal denied unless charter-listed) + a scope_gate forward-proxy; DROP NET_ADMIN (keep NET_RAW only if SYN scans required); Caido = capture only; Anthropic endpoint is the one in-policy external egress. GREEN: an off-scope egress attempt is dropped at the wire in a test; in-scope reachable. No fused offense run against a real target happens before this phase.",
        "PHASE 7 — Unified spine/killswitch/TrustRoot + WARDEN tool gate + offense-worker trust domain (the FATAL-2 + confused-deputy fix). One signed spine as the single log; one spine-backed owner-signed-release killswitch authoritative (CRUCIBLE gate reads it; file-backed kept as redundant halt-only); TrustRoot with the owner key as a required threshold signer. Replace the cosmetic _wrap_exec_command/repeat_request with scope_gate.validate_action + authority.require_authorization (refuse-before-wire) + WARDEN.record via the sigil-kernel Rust SUBPROCESS (never `import sigil.governor` from env-offense); add WardenGateHooks on the SDK on_tool_start/on_tool_end seam; exec target-authz delegated to the gateway, WARDEN floors offense exec >= A2. Replace assert_no_offense with assert_offense_gated: offense capability lives in a separate untrusted worker holding no owner key + an engagement-scoped store handle only, offense-mode entry an A3 owner-signed auto-expiring spine transition. GREEN: gate unit-tested with scope_gate fixtures; killswitch drops the worker to A0; the worker provably cannot reach personal memory.",
        "PHASE 8 — Claude runtime hardening on the LiteLLM path (gated by a one-turn live-fire; parallelizable with 6/7). Live-fire one real Claude scan turn to determine the reasoning_effort branch (400 vs silent-off), whether cache_read_input_tokens>0, and whether cost increments. Then: inject cache_control on the byte-stable system/tools prefix; add an Anthropic price table so the budget gate fires and feed it to SIGIL's signed governor as authoritative; make dedupe fail-CLOSED on empty extraction; log the resolved effort. GREEN: each hardening ships independently; the budget cap demonstrably fires; a caching hit is observed. Add the localized direct-Anthropic Model provider ONLY if the live-fire shows LiteLLM can't cleanly do adaptive-thinking+effort+caching+cost (does not touch SandboxAgent/Runner/hooks).",
        "PHASE 9 — Oracle confirmation pipeline (Thesis-2 headline), ONE bug class at a time. Extend create_vulnerability_report to carry {bug_class|cwe, injection_point, baseline_request_id, probe_request_id} + a CWE->canonical map; build the OracleConfirmationAdapter (model: oracle_probe_executor) that re-drives a scope-gated baseline+probe pair, builds FindingContext.from_http_responses, calls OracleVerifier.confirm, and on confirm builds+signs a PCF cert. Start with boolean_sqli end-to-end, then widen class-by-class. Only oracle-mappable classes become signed FACTS; the rest stay labelled leads; the Strix LLM-judge dedup is demoted to a pre-filter. GREEN: one class confirmed end-to-end with a verifiable cert; unconfirmed findings honestly labelled.",
        "PHASE 10 — Spine-sign confirmed findings + the chosen trust-anchor model. Confirmed SignedEvidence crosses as inert JSON; SIGIL does spine.append(kind='finding', payload=<SignedEvidence>) WITHOUT importing framework.*; the owner key signs the chaining spine head (two-anchor or unified-root per the owner's decision); cross-anchor the Rust action-log head via kind='warden_checkpoint'. GREEN: a confirmed finding is signed into the spine, both anchors verify independently, and assert_no_offense still passes in the SIGIL process.",
        "ONGOING — budgets (engagement max_actions + daily caps), default TWIN/STAGING with LIVE gated by charter flag + per-action second-ack, owner-watched A3 queue, always-available killswitch, and pinned/checksummed sandbox supply chain."
      ],
      "open_decisions_for_owner": [
        "MONOREPO ROOT NAME + LOCATION — not derivable from the three trees (the /home/kali/vigil-apex working dirs are a different TS/blockchain stack, not this fusion). The owner must name it; the layout is name-agnostic.",
        "CONFIRM the two-environment / two-lock topology (env-sovereign vs env-offense, both path-depending on packages/core; separate trust domains; offense worker holds no owner key). This is the load-bearing 'no single venv' call — forced by BOTH the sovereignty model and the unsatisfiable single-lock resolver conflict. Recommend: yes.",
        "TRUST-ROOT MODEL — the one genuine conflict between the analyses. Two independent anchors (CRUCIBLE governance m-of-n signs the finding cert; owner Ed25519 signs the chaining spine head) vs one unified TrustRoot with the owner key as a REQUIRED threshold signer. Recommend the unified root with owner-as-required-signer (owner-in-the-loop becomes structural), keeping the two verification surfaces as documented defense-in-depth — but the owner must pick.",
        "SANDBOX NETWORK POSTURE — drop NET_ADMIN unconditionally (it lets the container undo an in-container firewall); confirm whether SYN scans (`nmap -sS`) are needed — if yes keep NET_RAW with the firewall enforced host-side where NET_RAW can't touch it, else accept `-sT` connect scans and drop both. Enforce via host nftables on the sandbox docker network, NOT an in-container/proxy filter.",
        "ACCEPT that only oracle-mappable bug classes (BUG_CLASS_ORACLES) become signed FACTS and everything else is honestly demoted to a labelled lead. Recommend accept — signing unconfirmed findings as facts reintroduces exactly the hallucination the veracity firewall exists to kill.",
        "APPROVE extending the Strix finding contract (add {bug_class|cwe, injection_point, baseline_request_id, probe_request_id} to create_vulnerability_report + a CWE->canonical map). Recommend yes — parsing free-text evidence to reconstruct the differential is too leaky to be load-bearing for a signed-fact pipeline.",
        "DEFAULT TARGET ENVIRONMENT — enforce TWIN/STAGING by default; LIVE requires a charter flag + per-action second-ack. In-scope recon runs at a per-engagement owner-signed promotion to A2; exploit/destructive stays A3-per-action. Recommend accept; do not let the first fused run default to LIVE.",
        "ACCEPT the residual sovereignty trade — Claude-everywhere transits engagement context (captured requests, discovered creds, target source) to api.anthropic.com; SIGIL's local-only guarantee is gone for the offense half. Mitigate with a redaction pass before model context and keep Anthropic as the sole in-policy external egress. Only the owner can accept this.",
        "DELETE Strix third-party telemetry/egress outright (posthog/scarf/perplexity/openrouter attribution) rather than merely gating it; if usage stats are wanted, write them to the local spine. Recommend delete — a config flag leaves it one env var from firing.",
        "CLAUDE DEPTH — land the Phase-1 LiteLLM path now; commit to the Phase-2 direct-Anthropic Model provider (adds the `anthropic` dependency) UNLESS the one-turn live-fire proves LiteLLM 1.90.1 already emits adaptive-thinking + effort + caching + real cost for the target Claude IDs. Keep openai-agents; do NOT port to the Claude Agent SDK. Default model claude-opus-4-8; budget authority = SIGIL's signed governor. Recommend as stated.",
        "SUBTREE PROVENANCE + NAMESPACE FREEZE — full-history subtree for both owned repos and Strix (enables later `git subtree pull` + satisfies Apache attribution alongside retained LICENSE + added NOTICE); clean graphify-out/.coverage/targets cruft before subtreeing; keep CRUCIBLE FROZEN under the framework.* namespace (a rename silently defeats the widened offense guard unless updated in lockstep). Recommend as stated."
      ]
    }
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Design"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "design:architecture",
      "phaseIndex": 1,
      "phaseTitle": "Design",
      "agentId": "a7d1256f172f72ef8",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784511124964,
      "queuedAt": 1784511124937,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "FOUR COMPONENTS, THREE PROCESSES, ONE SHARED PACKAGE, DATA-…",
      "promptPreview": "GOAL: fuse THREE of the owner's systems into ONE Claude-powered monorepo tool (the owner explicitly chose\n\"true capability fusion\" — one tool that does BOTH authorized offense AND personal orchestration — and preserve both\ngit histories via subtree). Also: replace every OpenAI/non-Claude model+agent usage in Strix with Anthropic Claude.\n\nTHE THREE SYSTEMS (all the owner's own; SIGIL+CRUCIBLE are p…",
      "lastProgressAt": 1784511783145,
      "tokens": 180378,
      "toolCalls": 38,
      "durationMs": 658181,
      "resultPreview": "{\"recommended_approach\":\"FOUR COMPONENTS, THREE PROCESSES, ONE SHARED PACKAGE, DATA-ONLY across the SIGIL boundary. This is the cleanest acyclic architecture and it repairs the two theses that break as literally stated.\\n\\nTHESIS 1 (layering) — VERIFIED, stronger than claimed. The shared core is not hypothetical: `sigil/reuse/{canonical,chain,crypto,models}.py` are, by their own headers, \\\"VENDORE…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "design:claude-migration",
      "phaseIndex": 1,
      "phaseTitle": "Design",
      "agentId": "a587cc928e5b4febf",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784511124966,
      "queuedAt": 1784511124937,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "DEPTH DECISION: Keep the openai-agents SDK; force Claude th…",
      "promptPreview": "GOAL: fuse THREE of the owner's systems into ONE Claude-powered monorepo tool (the owner explicitly chose\n\"true capability fusion\" — one tool that does BOTH authorized offense AND personal orchestration — and preserve both\ngit histories via subtree). Also: replace every OpenAI/non-Claude model+agent usage in Strix with Anthropic Claude.\n\nTHE THREE SYSTEMS (all the owner's own; SIGIL+CRUCIBLE are p…",
      "lastProgressAt": 1784511514875,
      "tokens": 306194,
      "toolCalls": 20,
      "durationMs": 389909,
      "resultPreview": "{\"recommended_approach\":\"DEPTH DECISION: Keep the openai-agents SDK; force Claude through the existing LiteLLM route + a small hardening set. Do NOT port to the Claude Agent SDK for this fusion. Rationale grounded in the tree: the entire Strix body is welded to openai-agents 0.14.6 — build_strix_agent() returns agents.sandbox.SandboxAgent with capabilities=[Filesystem(), Shell()] (factory.py:454-4…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "design:monorepo-core",
      "phaseIndex": 1,
      "phaseTitle": "Design",
      "agentId": "a888fa005fe4b8946",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784511124966,
      "queuedAt": 1784511124937,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "MONOREPO = uv workspace of independently-buildable members,…",
      "promptPreview": "GOAL: fuse THREE of the owner's systems into ONE Claude-powered monorepo tool (the owner explicitly chose\n\"true capability fusion\" — one tool that does BOTH authorized offense AND personal orchestration — and preserve both\ngit histories via subtree). Also: replace every OpenAI/non-Claude model+agent usage in Strix with Anthropic Claude.\n\nTHE THREE SYSTEMS (all the owner's own; SIGIL+CRUCIBLE are p…",
      "lastProgressAt": 1784511641718,
      "tokens": 99951,
      "toolCalls": 29,
      "durationMs": 516752,
      "resultPreview": "{\"recommended_approach\":\"MONOREPO = uv workspace of independently-buildable members, each keeping its own backend; SHARED CORE = SIGIL's v2 chain promoted to source of truth; RUNTIME = two isolated environments, not one venv.\\n\\nLAYOUT (verified against the three build files):\\n- `packages/core/` → new member `vigil_core` = SIGIL's `sigil/reuse/*` (canonical.py, chain.py, crypto.py, models.py, __i…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "design:governance-safety",
      "phaseIndex": 1,
      "phaseTitle": "Design",
      "agentId": "a1fa957b49c323e1b",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784511124967,
      "queuedAt": 1784511124937,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "GOVERNANCE = \"two orthogonal, conjunctive gates over one si…",
      "promptPreview": "GOAL: fuse THREE of the owner's systems into ONE Claude-powered monorepo tool (the owner explicitly chose\n\"true capability fusion\" — one tool that does BOTH authorized offense AND personal orchestration — and preserve both\ngit histories via subtree). Also: replace every OpenAI/non-Claude model+agent usage in Strix with Anthropic Claude.\n\nTHE THREE SYSTEMS (all the owner's own; SIGIL+CRUCIBLE are p…",
      "lastProgressAt": 1784511635379,
      "tokens": 140479,
      "toolCalls": 37,
      "durationMs": 510412,
      "resultPreview": "{\"recommended_approach\":\"GOVERNANCE = \\\"two orthogonal, conjunctive gates over one signed spine, backstopped by a network the agent cannot see past.\\\" Do NOT merge SIGIL's and CRUCIBLE's authz into one model — compose them. CRUCIBLE answers \\\"is THIS target/action inside the signed engagement envelope right now?\\\" (authority/gate.py:authorize_action — order killswitch→window→scope→destructive→live…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "synthesize",
      "phaseIndex": 2,
      "phaseTitle": "Synthesize",
      "agentId": "af71eca927315e9b5",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784511783187,
      "queuedAt": 1784511783150,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "SOUND and buildable — but only in the specific shape all fo…",
      "promptPreview": "Synthesize these 4 fusion-design analyses into a consolidated verdict + risk register + sequencing for the owner's\ndetailed plan. Dedupe overlapping risks, rank by severity, and produce an ordered, incremental, always-green sequencing\nfor a SIGIL+CRUCIBLE+Strix Claude-powered capability-fusion monorepo. Flag any FATAL flaw.\n\nANALYSES:\n[\n {\n  \"dimension\": \"architecture\",\n  \"recommended_approach\": \"…",
      "lastProgressAt": 1784512094539,
      "tokens": 59434,
      "toolCalls": 1,
      "durationMs": 311352,
      "resultPreview": "{\"verdict\":\"SOUND and buildable — but only in the specific shape all four analyses independently converge on, and NOT safe to run in the naive \\\"one venv, true fusion\\\" form. There are two FATAL flaws, but both are design flaws with known mitigations, not dead-ends, so they change the SHAPE of the plan, not the go/no-go. FATAL-1: the Strix sandbox holds NET_ADMIN/NET_RAW with host.docker.internal-…"
    }
  ],
  "totalTokens": 786436,
  "totalToolCalls": 125
}