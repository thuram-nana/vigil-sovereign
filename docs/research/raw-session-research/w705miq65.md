{
  "summary": "Map the SIGIL spine invariants, consumer read-surface, tamper-model conflict, and durability contract before designing snapshot/compaction",
  "agentCount": 10,
  "logs": [],
  "result": {
    "invariants": [
      {
        "name": "chain-anchored-from-genesis",
        "statement": "The FIRST entry of whatever set verify_chain / verify_head receives must have prev_hash == _GENESIS_PREV ('0'*64). verify_chain seeds prev=_GENESIS_PREV and rejects entries[0] if e.prev_hash != prev. Note the subtlety: verify_chain does NOT require entries[0].seq == 0 — only prev_hash==genesis + contiguity — so a rebased chain starting at seq k is chain-valid IF k's prev_hash is rewritten to genesis and hashes recomputed.",
        "enforced_at": "sigil/reuse/chain.py:38-41 (verify_chain, i=0 branch); depended on by sigil/reuse/chain.py:66-71 (verify_head), sigil/spine/checkpoint.py:96 (verify_head over entries[:signed_n]), sigil/spine/store.py:284-303 (verify)",
        "violation_failure_mode": "If a compaction drops records [0..k] and keeps the tail with its ORIGINAL prev_hash values, the first retained entry's prev_hash is the entry_hash of record k, not genesis → verify_chain returns False 'chain break at seq k: prev_hash mismatch (entry deleted/reordered)'. Every verify()/verify_checkpoint/SpineTailer.check_anchor then reports a false TAMPERING verdict permanently, and check_anchor fails closed.",
        "snapshot_impact": "The physical read-front after compaction must be genesis-anchored: either prepend a synthetic SNAPSHOT record whose prev_hash == _GENESIS_PREV (and whose cert_digest commits the pruned prefix's final entry_hash so history is bound by hash), or rewrite the earliest retained record's prev_hash to genesis and recompute its entry_hash. entries()/iter_records must present this genesis-anchored front to verify_chain."
      },
      {
        "name": "strict-seq-contiguity-no-gaps",
        "statement": "For every adjacent pair, seq[i] == seq[i-1] + 1. No gaps, no duplicates, no reordering.",
        "enforced_at": "sigil/reuse/chain.py:44-45 (verify_chain seq-gap check); reads skip malformed lines so a torn MIDDLE line becomes a gap surfaced here — sigil/spine/store.py:212-218 (iter_records skip), 372-378 (_scan_from skip)",
        "violation_failure_mode": "A compaction that removes an INTERIOR record (e.g. a superseded one) while keeping records on both sides leaves seq N-1 followed by seq N+1 → verify_chain returns False 'chain break: seq gap at N+1'. Manifests as a hard false TAMPERING on every verify; also the offset index and get(seq) will have a hole that routes to the fallback scan.",
        "snapshot_impact": "Compaction may only remove a CONTIGUOUS PREFIX [start..k] (segment/prefix truncation), never punch interior holes. If interior records must be reclaimed (e.g. superseded bodies), the seq must remain present as a tombstone/summary entry so contiguity holds, or the whole design must re-map to a snapshot that itself is contiguous from its new front."
      },
      {
        "name": "seq-is-immutable-primary-key (no renumbering)",
        "statement": "A record's seq is its permanent identity. entry_hash binds seq; parent_id/supersedes_id are seq references; all external cursors (SpineTailer.cursor, since_seq, get(seq), MCP episodic_range, graph node anchors, audit rows) address records by seq. Records must never be renumbered.",
        "enforced_at": "entry_hash derivation binds seq — sigil/reuse/chain.py:15-16, sigil/spine/verify.py:34; seq references — sigil/spine/models.py:35-36 (parent_id/supersedes_id), sigil/spine/store.py:220-242 (get), 270-272 (next_seq), sigil/spine/tail.py:56-57,78 (cursor); graph anchors — sigil/graph/rebuild.py:174, sigil/graph/query.py:87",
        "violation_failure_mode": "If compaction renumbers retained records to restart at 0, every entry_hash must be recomputed (invalidating every prior signed head), every parent_id/supersedes_id/graph-node/cursor reference dangles or points at the wrong record, and next_seq/get() return silently wrong records. A live SpineTailer cursor set to an old seq would re-emit or skip. Silent corruption, not a crash.",
        "snapshot_impact": "Preserve original seq numbers across compaction. verify_chain tolerates a first seq != 0, so a rebased front can keep its true high seq (e.g. 40000) with prev_hash rewritten to genesis. Never compact by re-basing seq to 0."
      },
      {
        "name": "signed-head-entry_count-is-the-anti-truncation-gate",
        "statement": "The owner-signed head records entry_count and last_seq over the exact prefix it signed. classify_head treats len(entries) < head.entry_count as TAMPERING; verify_head requires head.entry_count == len(entries), head.last_seq == entries[-1].seq, head.head_hash == entries[-1].entry_hash. This is THE central conflict: legitimate pruning reduces len(entries) below a stale head's entry_count and is indistinguishable from a rollback/truncation attack.",
        "enforced_at": "sigil/spine/checkpoint.py:93-94 (n < signed_n → TAMPERING), 96 (verify_head over entries[:signed_n]); sigil/reuse/chain.py:73-74 (head-vs-chain equality)",
        "violation_failure_mode": "Compaction prunes the prefix but the on-disk signed head still anchors the old (larger) entry_count → verify_checkpoint/classify_head/SpineTailer.check_anchor all report 'TAMPERING: chain has N records but the signed head anchors M (truncated/rolled back)'. The UI/mobile mark the spine tamper-broken; the fresh tail shows anchored=False forever.",
        "snapshot_impact": "Compaction MUST be an owner-key operation that IMMEDIATELY re-signs a fresh head whose entry_count == the new physical entry set (snapshot record + retained tail) and whose head_hash == the new tip's entry_hash, written atomically. The head model / classify_head must be extended to understand a snapshot base so a legitimately compacted chain (fewer physical records, same last_seq) is accepted rather than flagged. Until re-signed, the old head correctly makes any prune look like tampering — that property is a feature (it blocks a keyless attacker) and must be retained."
      },
      {
        "name": "monotonic-last_seq-anti-rollback (durable via head, process-local via high-water)",
        "statement": "last_seq / the chain tip seq must never decrease. verify_head rejects head.last_seq < prev_highwater; SpineTailer flags on-disk head_seq < in-memory monotonic high-water. The DURABLE anti-rollback across restarts is the head's last_seq/entry_count vs the on-disk chain (the SpineTailer high-water resets to -1 each process).",
        "enforced_at": "sigil/reuse/chain.py:78-79 (prev_highwater gate); sigil/spine/tail.py:60,90-93 (process-local _high_water); sigil/spine/checkpoint.py:93 (entry_count as durable water mark)",
        "violation_failure_mode": "A compaction that removes TAIL (high-seq) records, or that re-signs a head with a lowered last_seq, is a rollback: check_anchor returns rollback set / verify_head 'rollback rejected'. If instead compaction silently lowers last_seq without tripping any gate (e.g. resets the seq counter), an attacker's tail rollback becomes indistinguishable from compaction and slips through — records acked to consumers vanish.",
        "snapshot_impact": "Compaction must reclaim only OLD/low-seq records and MUST preserve the maximum seq (last_seq) exactly; a re-signed head's last_seq must equal the true current tip, never regress. Prefix pruning reduces entry_count but must hold last_seq constant. Consider persisting the high-water so restart-time rollback detection survives a crash-during-compaction."
      },
      {
        "name": "append-only / offsets-never-move / no-in-place-rewrite",
        "statement": "Committed bytes never move or change; append() only adds at EOF; the seq→offset index only EXTENDS on growth and REBUILDS only on shrink. classify_head relies on 'append-only ⇒ the prefix is byte-identical' to reuse entries[:signed_n]. Lock-free readers open their own fd and stream forward, tolerating a concurrent append precisely because existing bytes are immutable.",
        "enforced_at": "sigil/spine/store.py:157-194 (append at EOF), 330-351 (_ensure_index: grew→extend, shrank→rebuild), 353-382 (_scan_from); readers 197-218, 244-268; checkpoint.py:95 comment (byte-identical prefix)",
        "violation_failure_mode": "A compaction that rewrites the spine file IN PLACE while a lock-free reader is mid-stream gives that reader a torn view (old head + new tail) → parse errors skipped, seq gaps, false verify() failure, or wrong get() results. Worse, _ensure_index detects change only by file SIZE: an in-place rewrite that leaves the byte size unchanged (or grows it) is UNDETECTED — the index extends from a stale _scan_pos over relocated content, so get(seq) seeks to a wrong offset and silently returns the wrong record or None.",
        "snapshot_impact": "Never rewrite the spine in place. Build the compacted file/new segment fully, fsync it, then atomically os.replace() it into position (as checkpoint._atomic_write_text does for the head) so a lock-free reader sees either the whole old or whole new file. Any store instance whose backing file changed must invalidate/rebuild its in-memory index (size-only change detection is insufficient — force a rebuild signal, e.g. via an epoch/generation counter)."
      },
      {
        "name": "torn-tail-truncation-before-append (BLOCK-1)",
        "statement": "Bytes past the last VALID newline-terminated record are never-committed garbage from an interrupted write; append() locates the last valid boundary and truncates the dead tail BEFORE writing, so a new record cannot merge with torn bytes into one unparseable line and be silently lost. Committed records are never touched by this truncate.",
        "enforced_at": "sigil/spine/store.py:83-121 (_last_valid_boundary), 168-173 (truncate to clean_end before write), 50-80 (_last_nonempty_line skips torn tail on read); MEMORY: BLOCK-1 was a real acked-record loss (incl. a missed panic)",
        "violation_failure_mode": "If a segment/snapshot scheme appends to the wrong file, or computes the clean boundary against a stale/other segment, the truncate either (a) fails to remove a torn tail → the new record merges with garbage and is silently lost while verify() stays green (a lost kill-switch panic never halts the mesh), or (b) truncates against a wrong boundary and deletes COMMITTED records.",
        "snapshot_impact": "Preserve truncate-before-append on whatever segment is the ACTIVE (head) append target; _last_valid_boundary must be evaluated on that exact file under the append lock. Compaction must reliably distinguish a torn tail (removable, un-newline-terminated / fails _REQUIRED_KEYS) from committed records (must survive) — never treat a committed record as torn."
      },
      {
        "name": "fsync durability of acked appends and head swap",
        "statement": "An acknowledged append is durable across a crash (fsync the data fd); the signed head is replaced durably+atomically (write temp, fsync temp, os.replace, fsync the directory) so a crash never leaves a torn head and a reader sees old-or-new, never partial.",
        "enforced_at": "sigil/spine/store.py:182-183 (flush+fsync after write); sigil/spine/checkpoint.py:33-60 (_atomic_write_text: fsync file + dir), 83 (atomic head write)",
        "violation_failure_mode": "A snapshot swap or segment rotation that renames/creates files without fsync'ing the new file AND its directory can, after a crash, present a spine whose data is durable but whose head rename was lost — or a half-materialized compacted file. On restart the head/chain mismatch reads as truncation → false TAMPERING, or an acked record that was mid-migration is lost silently.",
        "snapshot_impact": "Every compaction output (new segment, snapshot record, rewritten front, new head) must be fsync'd and swapped in via an atomic, directory-fsync'd rename, ordered so that at no crash point can the chain be non-verifiable: write+fsync new data, then atomically publish the new head last (head is the commit point)."
      },
      {
        "name": "seq→offset index extend/rebuild correctness under external mutation",
        "statement": "The index is built lazily, kept O(1)-current on our own appends, EXTENDED forward when a stat shows the file grew (another process appended — offsets never move), and fully REBUILT when the file shrank/was rewritten smaller. Full scans (verify/count/entries/iter_records(-1)) never consult the index and stay a single byte-identical pass.",
        "enforced_at": "sigil/spine/store.py:130-138, 187-192 (own-append fast path), 315-352 (_start_offset_for / _ensure_index grew|shrank), 353-382 (_scan_from)",
        "violation_failure_mode": "Detection is size-only. A compaction in another process that shrinks the file triggers a correct rebuild — fine. But a same-size or larger in-place mutation is read as 'grew' and _scan_from extends from a stale _scan_pos over content that moved, poisoning every offset it records → get(seq)/iter_records(since_seq>=0) return wrong records or None with no error. Also the own-append fast path (line 188 `_scan_pos == offset`) assumes no external mutation between reads; a compaction mid-session breaks that assumption.",
        "snapshot_impact": "Prefer append-only segment rotation over any in-place rewrite. When the backing file/segment set changes, force index invalidation on all live store instances (e.g. an mtime+size+generation epoch, not size alone). If segments are introduced, the index must key (segment, offset) and the extend/rebuild logic must span the active segment only while treating sealed segments as immutable."
      },
      {
        "name": "replay-stable content digest (ts excluded)",
        "statement": "cert_digest = sha256(canonical_json(content)) over scope/kind/source/actor/payload/parent_id/supersedes_id ONLY — the wallclock ts is excluded, so the chain is replay-stable. Two records with identical content but different ts share a cert_digest but differ in entry_hash (which binds seq+prev).",
        "enforced_at": "sigil/spine/store.py:146-150 (content excludes ts), sigil/spine/models.py:20-22,27-28 (ts informational), sigil/reuse/canonical.py:17-30 (canonical_json/digest_payload), sigil/spine/verify.py:28-33 (binding uses same content)",
        "violation_failure_mode": "If a snapshot/summary record computes its cert_digest over content that includes ts (or in a non-canonical form), its binding check (digest_payload(content) == cert_digest) fails → verify() 'binding break' false TAMPERING. If a compaction 'manifest' is stored off-chain rather than as a properly-digested spine record, the removed history is not bound by the chain at all.",
        "snapshot_impact": "Any snapshot/tombstone/manifest record must be created via the same append() content shape (ts-excluded, canonical_json) so it binds and chains. The snapshot's payload should commit the pruned prefix by hash (e.g. the final pruned entry_hash + count) so removed history remains bound and reconstructible, using only wallclock-free, canonical bytes."
      },
      {
        "name": "two-layer unkeyed integrity vs the keyed signed head (compactor is a recompute-capable writer)",
        "statement": "verify()/verify_record enforce UNKEYED consistency (binding: payload→cert_digest; chain: entry_hash derivation + linkage). These catch corruption and naive tamper but CANNOT resist a writer who recomputes cert_digest+entry_hash (a tip tamper or forward-cascaded fork stays self-consistent). Only the owner-Ed25519-SIGNED head provides tamper-EVIDENCE. A compaction agent is exactly such a recompute-capable writer.",
        "enforced_at": "sigil/spine/store.py:284-303 (verify two-layer + honesty note), sigil/spine/verify.py:14-19 (scope honesty), sigil/spine/tail.py:1-22 (integrity_ok vs anchored), sigil/spine/checkpoint.py:87-101 (classify_head keyed)",
        "violation_failure_mode": "A compaction can produce a chain that passes verify() (self-consistent) while having silently dropped or altered records — verify() alone would NOT flag it. If the design relies on verify()/verify_record to certify a compaction, an unauthorized or buggy prune goes undetected until (and unless) the signed head catches the entry_count/last_seq mismatch.",
        "snapshot_impact": "Compaction must be gated by the owner key: it re-anchors with a fresh owner-signed head, and the pruned prefix must be committed by a signed snapshot so 'records removed' is provable and authorized, never indistinguishable from tampering. Do not let the unkeyed layer be the sole certifier of a compaction; the keyed head is the authority (mirrors the existing doctrine)."
      },
      {
        "name": "read-path abstraction must present ONE contiguous chain from genesis (entries/iter_records span all data)",
        "statement": "entries(), verify(), count(), iter_records(-1) iterate the full log from byte 0 and hand verify_chain a single contiguous, genesis-anchored, seq-ordered sequence.",
        "enforced_at": "sigil/spine/store.py:197-218 (iter_records from 0), 274-282 (count/entries), 284-303 (verify feeds verify_chain); consumers sigil/bridge/envelope.py:88-104 (full-scan high-water), sigil/graph/rebuild.py, sigil/audit.py:17-33",
        "violation_failure_mode": "In a segment-rotation design, if entries()/iter_records read only the active segment (or read segments out of seq order, or with a boundary gap/overlap), verify_chain sees a chain that doesn't start at genesis or has a seq gap at a segment seam → false TAMPERING; count() undercounts; full-scan consumers (device_nonce_highwater, graph rebuild) miss records.",
        "snapshot_impact": "The store's read API must transparently concatenate the snapshot front + all sealed segments + the active segment in seq order, seamlessly across boundaries (no gap, no dup at a seam), so every existing full-scan caller keeps seeing one contiguous genesis-anchored chain without change."
      },
      {
        "name": "durable tip determines next_seq across restart (no seq reuse)",
        "statement": "On construction the store reads the last valid line to seed self._last; next_seq = self._last.seq + 1. This must reflect the true global maximum seq ever appended so seq numbers are never reused.",
        "enforced_at": "sigil/spine/store.py:138 (_read_last_entry at init), 305-312 (_read_last_entry), 270-272 (next_seq)",
        "violation_failure_mode": "If a segment/snapshot scheme leaves the tip in a file _read_last_entry doesn't consult (it reads only self.path), next_seq regresses; the next append reuses a live seq → duplicate seq, entry_hash fork, verify_chain contiguity/linkage break, and a replay/nonce high-water (derived from receipts) that can be defeated. A forked chain.",
        "snapshot_impact": "_read_last_entry / next_seq must resolve the tip from the ACTIVE segment (the true global max seq), never a sealed/snapshot file. The append lock's tip re-read (store.py:168-174) must likewise target the active segment so concurrent writers can't fork from a stale tip."
      },
      {
        "name": "cross-writer serialization (spine_lock + flock) must cover the compactor",
        "statement": "The whole read-tip→truncate→write is serialized by a process-wide re-entrant spine_lock plus a cross-process advisory flock, so concurrent writers can't fork the chain off a stale tip. Compaction mutates the same file and is itself a writer.",
        "enforced_at": "sigil/spine/store.py:37-47 (spine_lock), 154-160 (lock + flock around append); re-entrancy relied on by sigil/bridge/envelope.py:127-133 (atomic check-then-receipt)",
        "violation_failure_mode": "If compaction rewrites/rotates segments WITHOUT holding spine_lock+flock, an append running concurrently reads a tip that compaction is about to relocate or truncate → two divergent chains / a lost or duplicated record. Also the re-entrant nonce replay gate (check highwater + receipt) could interleave with a prune of the very receipts it reads, reopening a replay window.",
        "snapshot_impact": "Compaction must acquire the same spine_lock and flock as append for the duration of any tip-affecting mutation and swap, and must be atomic w.r.t. lock-free readers (build-then-os.replace). Pruning of state-bearing records (receipts) must be serialized against the consumers that scan them."
      },
      {
        "name": "monotonic-state derived by full scan must survive pruning (replay high-water, graph cursor)",
        "statement": "Several security-relevant monotonic values are reconstructed by scanning ALL spine records at read time, not stored separately: the per-device nonce replay high-water (max receipted nonce) and the graph/replay rebuilt_seq high-water. Pruning the records these scans read silently lowers the reconstructed value.",
        "enforced_at": "sigil/bridge/envelope.py:88-104 (device_nonce_highwater full scan), 107-133 (consume uses it to reject replay); sigil/graph/rebuild.py:174 (manifest replay high-water); sigil/audit.py:17-33 (audit trail by scan)",
        "violation_failure_mode": "If compaction prunes old RECEIPT_SIGNAL records, device_nonce_highwater drops (or returns -1) → a previously-consumed effectful envelope with a lower nonce is accepted again: a replay of a captured device command (approve/arm/panic) succeeds. This is a silent SECURITY regression that no integrity check flags — the chain still verifies.",
        "snapshot_impact": "Compaction must not lose the monotonic state carried by pruned records. Fold each device's max nonce (and rebuilt_seq, and any similar high-water) into the signed snapshot record so the reconstruction floor is preserved, and make device_nonce_highwater seed from the snapshot's carried high-water rather than from -1. Audit-trail continuity likewise must survive (or be summarized in) the snapshot."
      },
      {
        "name": "referential integrity of parent_id / supersedes_id across a prune",
        "statement": "parent_id and supersedes_id are seq references into the spine; supersession is the natural signal that an older record is a compaction candidate. Retained records must not dangle to pruned targets, and the supersession relationship must remain resolvable.",
        "enforced_at": "sigil/spine/models.py:35-36 (fields), sigil/spine/store.py:141-149,177 (set at append, digested into cert_digest), sigil/agents/base.py:85-98 (agents set parent_id/supersedes_id), sigil/mcp/server.py:96 (surfaced)",
        "violation_failure_mode": "If compaction prunes a record still referenced by a retained record's parent_id/supersedes_id, the reference dangles — get() returns None mid-graph-rebuild, episodic_range around an anchor loses context, and a superseded-by chain can't be walked. Because parent_id/supersedes_id are part of the digested content, they cannot be edited to re-point without breaking that record's binding (false TAMPERING).",
        "snapshot_impact": "Only prune records whose supersession/parent relationships are fully closed within the pruned prefix, or record the resolution (final superseding seq) in the snapshot manifest so references remain answerable. Never mutate a retained record's parent_id/supersedes_id (it would break its cert_digest binding)."
      },
      {
        "name": "canonical/domain primitives are frozen (no digest or signing-form drift)",
        "statement": "canonical_json (sorted keys, compact separators, UTF-8) and the _EVIDENCE_DOMAIN tag are frozen; changing them invalidates every existing signature and digest. entry_hash inputs are exactly {cert_digest, prev_hash, seq}. verify_record/verify reuse the same _entry_hash and digest_payload as the enforced write path — never a re-implementation.",
        "enforced_at": "sigil/reuse/canonical.py:14 (domain tag 'never change without a schema bump + migration'), 17-35; sigil/reuse/chain.py:15-16 (_entry_hash inputs); sigil/spine/verify.py:22-23,34 (reuse verbatim)",
        "violation_failure_mode": "If a snapshot introduces a new record type or head format that digests/signs over a different canonical form or domain, its binding/signature won't verify against the existing verify_record/verify_head path → false TAMPERING or an unverifiable head; or, if verify logic is re-implemented for snapshots, it can drift from the enforced path and accept what the real path would reject.",
        "snapshot_impact": "Snapshot records, manifests, and any new head must digest/sign using the identical canonical_json + _EVIDENCE_DOMAIN + _entry_hash primitives, computed by the same reuse functions. Any head/schema extension needs a schema_version bump with an explicit migration, not a silent format change."
      },
      {
        "name": "single verifiable signed head is the commit point",
        "statement": "There is exactly one signed head at HEAD_PATH; verify_checkpoint/classify_head/SpineTailer load that one head and judge the whole chain against its entry_count/last_seq/head_hash. It is written atomically as the last, committing step.",
        "enforced_at": "sigil/config.py:54 (HEAD_PATH single file); sigil/spine/checkpoint.py:79-84 (checkpoint writes one head), 104-109 (verify_checkpoint loads one), sigil/spine/tail.py:110-119 (_resolve_head loads one)",
        "violation_failure_mode": "A snapshot design that needs to attest BOTH the pruned prefix and the current tip but still emits a single flat head cannot express 'records [0..k] are covered by a signed snapshot, [k+1..N] by the live tail' — classify_head's n-vs-entry_count logic will either flag the compacted chain as truncated or, if entry_count is naively lowered, lose the ability to prove the pruned prefix ever existed.",
        "snapshot_impact": "Extend the head/anchor model coherently: a single signed head must carry a snapshot base (base_seq, snapshot_hash committing the pruned prefix) alongside entry_count/last_seq of the retained physical set, and classify_head must be taught to accept 'physical entries == retained set, logical coverage == base..last_seq'. Keep it ONE atomically-published head so there is a single commit point and a single fail-closed verdict."
      },
      {
        "name": "genesis-vs-empty and count semantics for a compacted store",
        "statement": "verify_head/sign_head treat an empty chain specially (head_hash = _GENESIS_PREV, last_seq/entry_count = 0), and next_seq is 0 for an empty store. count() and entry_count reflect PHYSICAL records present.",
        "enforced_at": "sigil/reuse/chain.py:31-32,57-58,71-72 (empty-chain genesis defaults), sigil/spine/store.py:271-272 (next_seq 0 when no _last), 274-275 (count)",
        "violation_failure_mode": "If compaction empties the physical file down to just a snapshot record, naive code paths that special-case len==0 (genesis) could misread a snapshot-only front as an empty/new spine (last_seq→0), silently resetting next_seq to 0 and enabling seq reuse and rollback. Or count() (physical) vs the logical record total diverge and mislead consumers/dashboards.",
        "snapshot_impact": "A compacted store is never 'empty' — its snapshot record defines a non-zero logical base. next_seq, last_seq, and the empty-vs-genesis branches must derive from the snapshot base, not from physical len==0. Decide and document count() semantics (physical vs logical) and update every consumer (mcp ingest_status spine_records, dashboards) accordingly."
      }
    ],
    "consumers": [
      {
        "file": "sigil/graph/rebuild.py",
        "spine_calls": "SpineStore() constructed L120; store.iter_records() FULL scan with no since_seq in _accumulate L55; store.next_seq read L171 (spine_head_seq). Reads r.seq/r.entry_hash/r.kind/r.payload/r.ts/r.actor only. Does NOT call get(). Writes a kuzu db + STAGING/manifest.json (L177) and atomically swaps CURRENT — none of that is the spine.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": false,
        "depends_on_old_seq_resolving": true,
        "risk_note": "CENTRAL CONFLICT for prune. rebuild() fully replays the entire spine (iter_records with no cursor) to reconstruct the whole graph; it holds no assumption of contiguity or 0-start (max_seq=r.seq is a value, first_seq/last_seq/anchor_seq are recorded values, no range(count)/positional indexing), so a seq GAP does not crash it. But it NEEDS every historical record present in the live file to mint the corresponding Session/Document/Commit node: a prune of old records silently SHRINKS the graph (old sessions/commits/documents vanish) with no error. Every node also stores anchor_seq+anchor_hash of its minting record; after a prune those seqs no longer resolve, so the graph's own citations dangle. in_sync (rebuilt_seq==next_seq-1) still holds after a prune (head seq unchanged), so mirror-health would falsely read GREEN while the view lost history. A snapshot/segment design MUST let rebuild replay from a compaction snapshot + live segments, or the graph loses everything below the prune point. Not segment-transparent: SpineStore.iter_records must be made to span all segments for this full replay."
      },
      {
        "file": "sigil/graph/query.py",
        "spine_calls": "Reads the kuzu current/ db (read-only), NOT the spine, for query()/entity()/most of health(). Only spine touch: health() constructs SpineStore() and reads .next_seq L117 (spine_head_seq). Reads CURRENT/manifest.json via read_text L116 (sidecar, not the spine). Does NOT call iter_records/get/tail/count.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": false,
        "appends": false,
        "depends_on_old_seq_resolving": false,
        "risk_note": "Segment-transparent for its own operation: it queries the rebuilt kuzu graph and only ever asks the spine for next_seq (a head value, prune-safe). No contiguity/0-start assumption. HOWEVER its OUTPUT is a citation contract: entity() returns anchor_seq, first_seq, last_seq (e.g. Session L84) and the Session branch's hint explicitly tells the caller to `episodic_range(start_seq=first_seq, end_seq=last_seq)`. A prune that removes those old seqs from the live file makes every such returned citation unresolvable downstream even though query.py itself never crashes. So query is safe to run but propagates dangling seq references that a prune creates."
      },
      {
        "file": "sigil/vectors/index.py",
        "spine_calls": "index_spine(store, since_seq) calls store.iter_records(since_seq=since_seq) L148 — INCREMENTAL, driven by an external cursor. Uses r.seq (as the Qdrant point id, id=r.seq L136), r.kind, r.source, r.actor, r.payload, r.ts, r.entry_hash, r.text(). Does NOT call get(). last_indexed_seq()/count()/search() talk to Qdrant, not the spine. The durable cursor lives in Qdrant (max seq via OrderBy), not in the spine.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": false,
        "depends_on_old_seq_resolving": false,
        "risk_note": "Mostly prune-robust. Steady state is incremental (since_seq = highest-indexed seq from Qdrant); a full scan (iter_records since_seq=-1) happens only on cold-start or after reset(). id=r.seq is used as an idempotent upsert key (a VALUE), NOT a positional/array index, and only EMBEDDABLE_KINDS are indexed so the indexed-seq set is ALREADY sparse/gappy — proving no contiguity assumption. Because embeddings persist independently in Qdrant, a prune of old spine records does NOT break the index and does NOT lower the cursor, so incremental indexing keeps working. TWO caveats for a prune/segment design: (1) search payloads carry seq+entry_hash as citations, so hits pointing at pruned records become unresolvable in the spine (same dangling-citation issue as query.py); (2) reset() (used when the embed policy changes) drops the collection and re-embeds via iter_records — after a prune it can NEVER rebuild vectors for pruned records, so a policy change would permanently lose recall over pruned history. A snapshot must retain re-embeddable text if reset()-after-prune is to stay lossless."
      },
      {
        "file": "sigil/consolidate/pipeline.py",
        "spine_calls": "SpineStore() L69; store.next_seq L70 (head_before, to avoid feeding its own promotions); store.iter_records(since_seq=since) L73 — INCREMENTAL, cursor-driven. Passes store (+ window_seqs, a set of RECENT batch seqs) into admit() L80 (gate re-executes over those recent seqs). promote_all(store, admitted) L90 and write_brief(store) L94 APPEND records via store. checkpoint(store) L97 re-signs the owner head. Own cursor is CACHE_DIR/consolidate_cursor.json (read_text L48), a sidecar, not the spine.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "Incremental forward WRITER. window is bounded to seq>cursor AND seq<=head_before, so it never needs old/pruned seqs (cursor -1 forces a one-time full scan only on first run); window_from/window_to are seq VALUES, head_before=next_seq-1, no positional/contiguity assumption. A prune of records BELOW the cursor does not affect the window it reads. The prune-relevant interaction is checkpoint(store) at L97: it re-signs the head under the current model where classify_head treats len(entries) < head.entry_count as TAMPERING — i.e. exactly the invariant that makes a legitimate prune indistinguishable from a rollback. The compaction work must teach checkpoint/classify_head about the snapshot boundary before this call can coexist with pruning, or every consolidation run after a prune will re-sign a head that the truncation-tamper check then rejects. Also promote_all/write_brief append (offsets never move, fine); gate re-execution uses only recent window seqs, so it is prune-safe."
      },
      {
        "file": "sigil/consolidate/revise.py",
        "spine_calls": "consolidation_records() calls store.iter_records() FULL scan with no since_seq L16, filtering source==archivist (CONSOLIDATE_SOURCE). iter_current() and promotion_ledgers() both consume that full scan. Uses r.source, r.kind, r.seq, r.supersedes_id, r.payload. Does NOT call get(). Read-only (no append).",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": false,
        "depends_on_old_seq_resolving": true,
        "risk_note": "Full-scan reader that NEEDS the complete archivist history in the live file. supersedes_id is treated as a seq value (superseded={r.supersedes_id}; filter `r.seq not in superseded` L25) — no positional/0-start assumption — but the supersession semantics assume ALL archivist records persist. A prune is doubly destructive here: (1) promotion_ledgers() rebuilds the grounded/refused idempotency ledgers by scanning every archivist record's promotion_key; pruning old promotions drops those keys, so a previously-promoted fact can be RE-PROMOTED as a duplicate (idempotency broken). (2) iter_current() derives the live view by subtracting superseded seqs; if a SUPERSEDING record is pruned while its now-stale predecessor survives, the stale (superseded) fact RESURRECTS into the current view, and pruning live records simply drops facts the MCP tools serve. So any compaction must preserve the full archivist supersession chain + promotion-key ledger (e.g. via a snapshot that folds supersessions and carries forward promotion keys) rather than raw-prune archivist records. Not segment-transparent for prune; segment-transparent only w.r.t. rotation IF iter_records spans all segments."
      },
      {
        "file": "sigil/cli.py",
        "spine_calls": "Constructs SpineStore in nearly every cmd_*. store.count() (L61 cmd_ingest, L467 cmd_status); store.next_seq (L467 cmd_status); store.append(kind=\"warden_checkpoint\",...) (L260 cmd_warden_anchor_set); store.verify() (L274 warden_anchor_get, L456 cmd_verify, L465 cmd_status); store.iter_records() full scan (L283 warden_anchor_get); store.get(seq) (L159 research, L185 bastion, L213 perceive, L354 scrape); checkpoint()/checkpoint(store) (L73 cmd_sign, L262 warden_anchor_set); verify_checkpoint()/verify_checkpoint(store) (L278/L458/L466). Governor/approvals/agents/mesh subcommands append indirectly (KillSwitch.engage, PromotionPolicy.grant, ApprovalQueue, authorize_device).",
        "reads_raw_file": true,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "NOT segment-transparent. (1) Imports SPINE_PATH (L7) and does SPINE_PATH.unlink(missing_ok=True) on --reset (L22) — a raw single-file delete a segment design must generalize to clearing ALL segment files, else --reset leaves stale rotated segments. (2) cmd_warden_anchor_get (L283-287) FULL-scans iter_records() for the highest-count warden_checkpoint per pubkey — the anti-rollback high-water. A prune dropping the max-count anchor would silently LOWER the high-water and let a rolled-back WARDEN log pass verify: a real security regression, not just completeness loss. Compaction MUST retain the newest warden_checkpoint per pubkey. (3) count()/verify() (L456/465/467) and that scan must span segments after rotation. store.get() targets are freshly-appended res.applied seqs (recent), so no dependence on old-seq resolution. No seq==line / gap assumption anywhere."
      },
      {
        "file": "sigil/audit.py",
        "spine_calls": "store.iter_records(since_seq=since_seq) with default since_seq=-1 (L19), a FULL replay of the whole log; filters r.source==\"agent\" and reads r.seq, r.entry_hash, r.ts, r.actor, r.kind, r.payload, r.parent_id, r.text(). Returns rows[-limit:].",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": false,
        "depends_on_old_seq_resolving": false,
        "risk_note": "API-only and gap-tolerant (seq is opaque), but does_full_scan=true: self_audit defaults to since_seq=-1 and walks every record, so after rotation iter_records must span all segments or the C18 'what did the mesh do and why' trail is truncated to the live segment. Pruning old records silently shrinks the audit history (C18's premise is total recall of governed actions) — a completeness weakening, not a crash. No append, no historical get()."
      },
      {
        "file": "sigil/dashboard.py",
        "spine_calls": "head = store.next_seq - 1 (L19, real tip seq, NOT count-derived); store.iter_records() FULL scan (L28). In-loop: scans kind==\"warden_checkpoint\" -> last_checkpoint (L29-30), kind in (entity,contradiction) -> last_consolidation (L31-32), recent window via r.seq > head - lookback (L40). Also pending(store, owner_pubkey()) and KillSwitch(store).is_engaged().",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": false,
        "depends_on_old_seq_resolving": false,
        "risk_note": "Gap-SAFE: head comes from next_seq (real tip, not count), and the recent window uses seq arithmetic (head - lookback) which tolerates gaps; since prunes remove OLD not recent seqs the lookback window stays intact. But does_full_scan=true (L28) — must span segments after rotation or per-agent/decision counts see only the live segment. Secondary: last_checkpoint_seq/last_consolidation_seq come from scanning ALL records; pruning a warden_checkpoint or consolidation record skews the ingest-lag readout (records_since_checkpoint). No append, no old-seq get. No seq==line assumption."
      },
      {
        "file": "sigil/mcp/server.py",
        "spine_calls": "episodic_range: store.next_seq (L85), store.iter_records(since_seq=lo-1), break at r.seq>hi / limit (L88-100). ingest_status: store.count() (L119), store.next_seq (L120), store.verify() (L127), verify_checkpoint(store) (L128). memory_search uses VectorIndex only; graph_entity/graph_query, threads_open, commitments_due, contradictions_pending pass SpineStore() into graph/consolidate modules. No write path anywhere (read-only server by design).",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": true,
        "does_full_scan": true,
        "appends": false,
        "depends_on_old_seq_resolving": true,
        "risk_note": "The prune-sensitive consumer. episodic_range is built to read HISTORICAL seq windows ('after memory_search gives you a seq, read the seqs around it', L72-73). After a prune those seqs are gone; iter_records(since_seq=lo-1) just seeks to the earliest SURVIVING seq, so a request for a pruned range returns empty/later records with NO error and NO signal data was compacted away — a silent trust hole under the 'each result is a tamper-evident record, cite its seq' contract. assumes_zero_based flagged: the tool contract frames start_seq=0 as 'beginning' (L68/L80) — implementation is gap-tolerant but the API implies seqs start at 0. does_full_scan: ingest_status count()+verify() (L119/L127) walk the whole chain and must span segments; episodic_range default start_seq=0 seeks from the beginning but is limit-bounded (L99). Needs a snapshot/segment-backed get() (or an explicit 'range compacted below seq N' response) to honor historical range reads honestly."
      },
      {
        "file": "/home/kali/sigil/sigil/governor/killswitch.py",
        "spine_calls": "store.append(kind='event',...) at L42 (engage) and L48 (release); store.iter_records() full-scan at L75 (_scan_engaged); ALSO reaches past the API into store.path directly — Path(self.store.path).resolve() at L55 and self.store.path.stat().st_size at L57 — using the raw file SIZE as a cache key.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "TWO risks. (1) The FIX-4 cache (L55-69) assumes the spine 'only changes by GROWTH' — a matching st_size ⇒ no new records ⇒ cached verdict exact. Segment rotation/compaction/prune violates that monotonic-size invariant: a prune SHRINKS the live file (the code's shrink case only rebuilds SpineStore's index, not this cache) and a rotation could change content while the byte size is coincidentally equal, serving a STALE latch verdict — dangerous because un-halting is the guarded direction. The cache MUST key on something rotation-aware (segment head seq/count), not st_size of one file. (2) _scan_engaged derives the latch from the ENTIRE history; iter_records must span all segments after rotation, and pruning the last 'engaged' record (with no later owner-signed release) would silently flip a halted mesh to released — a fail-DANGEROUS behavior change. Kill-switch signal records must be treated as un-prunable, or the latch must be checkpointed."
      },
      {
        "file": "/home/kali/sigil/sigil/governor/promotion.py",
        "spine_calls": "store.append(kind='refusal'|'event',...) at L30, L37 (grant), L42 (revoke); store.iter_records() full-scan at L48 (is_promoted).",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "is_promoted (L48-55) folds the FULL grant/revoke history into a latest-wins map, so iter_records must span all segments after rotation. Pruning an old 'granted' record that has no later 'revoked' would drop an active promotion — is_promoted returns False (de-promote). Direction is fail-SAFE (an A2 proposal falls back to queuing), but it is still a silent policy change: an owner's standing grant would vanish. Not seq-dependent; no get(). Otherwise API-only."
      },
      {
        "file": "/home/kali/sigil/sigil/governor/budget.py",
        "spine_calls": "store.iter_records() full-scan at L30 (spent); over_budget (L44) calls spent. No append, no get.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": false,
        "depends_on_old_seq_resolving": false,
        "risk_note": "spent() full-scans every record (L30) but filters to r.source=='agent', r.actor==agent, and ts.startswith(day_iso) for the CURRENT UTC day only (L31-33). Because it only counts TODAY, pruning OLD-DAY records is harmless to correctness — this is the most prune-tolerant consumer. The only requirement is that iteration reach ALL of today's records: if a single UTC day's records ever span more than one live segment after rotation, the API must iterate across those segments (must not stop at a segment boundary). Segment-transparent provided iter_records is segment-complete. Efficiency note: O(whole-spine) per decision is already wasteful and rotation makes it worse — a since-day/since-seq bound would help."
      },
      {
        "file": "/home/kali/sigil/sigil/agents/approvals.py",
        "spine_calls": "pending() store.iter_records() full-scan at L62; ApprovalQueue._decide store.append(...,supersedes_id=seq) at L102-103; _target (L84-88) calls pending() (no get). Imports SpineStore (L22) and constructs one at L79.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "Uses seq as an OPAQUE spine id, not a line index: r.seq is a dict key (L67), target_seq/supersedes_id carry it, and approve(seq) resolves it by RE-SCANNING pending() (L85), never by get(seq). It sorts queued.items() by seq for oldest-first (L68) — relies only on seq being monotonic (holds under rotation), NOT on contiguity or zero-base. Prune risk: pending() must span all segments to pair a queued proposal (possibly old) with its later approval; if an UNRESOLVED (never-approved) queued proposal sits in a pruned old segment it silently vanishes from pending() and can never be approved. A prune policy MUST NOT remove queued proposals that have no verified superseding approval. A resolved (approved/denied) pair is safe to prune together."
      },
      {
        "file": "/home/kali/sigil/sigil/agents/actor_gate.py",
        "spine_calls": "store.get(seq) at L22; store.iter_records(since_seq=seq) at L27; mesh.authorized_devices(store, trusted_pubkey) at L25 (indirect spine read). No append.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": false,
        "appends": false,
        "depends_on_old_seq_resolving": true,
        "risk_note": "HIGHEST-RISK consumer for prune/rotation. action_approved() resolves an ARBITRARY (potentially old) seq two ways: get(seq) at L22 to fetch the action-step record and confirm its signal/action_token, then iter_records(since_seq=seq) at L27 to find a later verified approval. seq is used correctly as an opaque id (not a line number), but BOTH calls require the historical seq to remain resolvable. If a prune removes the step's seq, get(seq) returns None → action_approved False → a legitimately owner-approved action is DENIED (fail-closed, but a real functional regression). since_seq iteration must also span from a possibly-old segment forward across rotation boundaries. Any prune must retain the step record until its action is consumed, and get()/iter_records(since_seq=) must remain segment-transparent for old seqs."
      },
      {
        "file": "/home/kali/sigil/sigil/agents/actor_scope.py",
        "spine_calls": "creation_allowed() store.iter_records() full-scan at L49. origin_allowed()/_origin() touch no spine. No append, no get.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": false,
        "depends_on_old_seq_resolving": false,
        "risk_note": "creation_allowed (L44-56) counts EVERY historical applied 'web.actor.step'/'account.create' record for a service label or origin and enforces n < creation_cap. This count depends on FULL history completeness, so (a) iter_records must span all segments after rotation, and (b) pruning old applied account.create records UNDERCOUNTS creations → resets the cap → a genuine creation_cap / mass-account-creation BYPASS (security-relevant, unlike the fail-safe consumers). Not seq-dependent and no get(), but account.create audit records must be treated as un-prunable (or the running count must be checkpointed into the compaction snapshot). Otherwise API-only."
      },
      {
        "file": "sigil/bridge/daemon.py",
        "spine_calls": "store.iter_records() full scan in submit_device_approval (L87); store.tail(_DEDUP_WINDOW=2048) in submit_arm_request (L136); store.append(...) at L91 (device approval, supersedes_id=tgt), L141 (arm request), L151 (panic). pending()/recall() delegate to agents.approvals.pending / perception.recall (not in scope) which themselves scan. next_seq not used.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "submit_device_approval does a FULL iter_records() scan for idempotent dedup (L87–90) — after rotation this must span all segments or a replayed device-approval whose original receipt aged into an old segment would be re-appended (dedup silently defeated). It deliberately rejects a bounded window here (comment L83–86), so a segment-aware full scan is REQUIRED, not optional. submit_arm_request's tail(2048) is segment-transparent (bounded, seek-from-end) and is safe because record-time ARM_FRESHNESS already refuses aged replays. supersedes_id=tgt/target_seq are stored opaquely, never resolved via get()."
      },
      {
        "file": "sigil/bridge/server.py",
        "spine_calls": "SpineServer.store() constructs SpineStore(self.spine_path) fresh per request (L114); store().get(seq) in _record (L322) for arbitrary URL-supplied seq; verify_record(rec) (L326); daemon().pending()/recall(); snapshot(store()) (L275); consume(store(),...) via _authed_effectful (L207) → iter_records+append; PushNotifier(store()) for SSE (L352). SPINE_PATH imported (L43) only as the default path arg to SpineStore.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": true,
        "risk_note": "CRITICAL for pruning: GET /api/record/<seq> (L280–283, L317–329) resolves an ARBITRARY historical seq via store.get(seq) and returns 404 'no such record' if absent — after a prune every pruned seq turns into a 404, and the LIVE-verify (verify_record) can never re-attest a pruned atom. The effectful path (_authed_effectful→consume) triggers a full-spine nonce-highwater scan (see envelope.py). The store points at the single live SPINE_PATH file, so get()/scans do not see rotated segments unless SpineStore itself is made segment-aware. No seq==line-number assumption; seq is treated as an opaque key from the URL and the SSE `since` cursor is passed straight to PushNotifier."
      },
      {
        "file": "sigil/bridge/envelope.py",
        "spine_calls": "store.append via record_receipt (L82–85); store.iter_records() FULL scan in device_nonce_highwater (L92–104); spine_lock(store.path) around the atomic check-then-receipt in consume (L129–133); store.path used only as the lock key.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "SECURITY-CRITICAL under pruning: device_nonce_highwater (L88–104) scans EVERY record to find the highest receipted nonce per device — the replay watermark. If old RECEIPT_SIGNAL='mesh.request' receipts are pruned out of the live file, the highwater can REGRESS, re-opening the effectful-request replay window (a captured old envelope with a now-'fresh' nonce would pass). A rotated-segment design must keep this scan spanning all segments, or fold each device's highwater into the snapshot/checkpoint so it survives compaction. nonce is application-level, never conflated with seq; no get() and no positional indexing."
      },
      {
        "file": "sigil/bridge/notifier.py",
        "spine_calls": "store.iter_records(since_seq=self.cursor) once per poll (L35); store.next_seq to seed the cursor at construction (L26, next_seq-1).",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": false,
        "depends_on_old_seq_resolving": false,
        "risk_note": "Largely segment-transparent: it is forward-only from a seq cursor and advances cursor=rec.seq over every walked record (L36), making no gap-free/contiguity assumption. Two caveats: (1) an SSE client passing since=-1 (or the malformed→-1 fallback in server.py L351) makes iter_records scan from genesis — a full scan that must span segments; (2) if a supplied resume cursor names a seq that has been pruned, SpineStore._start_offset_for falls back to a from-byte-0 scan of the live file (safe, forward-only) and simply never re-emits the pruned range — acceptable for a push notifier but worth noting the cursor no longer maps to a resolvable record."
      },
      {
        "file": "sigil/ui/server.py",
        "spine_calls": "UIServer.store() constructs SpineStore(self.spine_path) fresh per request (L48–49); store().get(seq) in _record (L161) for arbitrary URL seq; verify_record(rec) (L164); snapshot(store()) (L125); SpineTailer(store(), since_seq) for SSE (L201); actions.do_action(action, body, store=store()) on POST (L228). SPINE_PATH imported (L27) only as the SpineStore default.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": true,
        "risk_note": "Same pruning hazard as bridge/server.py: GET /api/record/<seq> (L126–127, L156–167) get()s an ARBITRARY historical seq and 404s ('no such record — not fabricated') when absent, and live-verifies the atom — both break for any pruned seq. SSE uses SpineTailer with a `since` cursor (from genesis on the -1 fallback, L200). Appends happen indirectly via actions.do_action. No seq==line assumption; seq is an opaque key from the URL. All access is via SpineStore against the single live file, so it is only as segment-aware as SpineStore is."
      },
      {
        "file": "sigil/ui/actions.py",
        "spine_calls": "SpineStore() default-path construct if none injected (L21); passes that store into ApprovalQueue(store,...) .approve/.deny (L30–31), KillSwitch(store,...).engage/.release (L34–35), PromotionPolicy(store,...).grant/.revoke (L39–40) — those owner-signed cores perform the actual store.append. int(params['seq']) is taken as the approve/deny target_seq (L29) and passed to the core.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": false,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "Segment-transparent AT THIS LAYER: it only appends (through the governor/approval cores) and never itself scans or get()s. The approve/deny target_seq is an opaque id forwarded to ApprovalQueue; whether resolving that seq needs old records to survive a prune is a property of ApprovalQueue/KillSwitch/PromotionPolicy (not in scope here), not of this file. No contiguity or zero-based assumption."
      },
      {
        "file": "sigil/mesh/registry.py",
        "spine_calls": "store.append at L39 (advertise_capability), L57 (authorize_device), L63 (revoke_device), L93 (DeviceApprover._decide, supersedes_id=seq); store.iter_records() FULL scan in capability_map (L46) and authorized_devices (L71).",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "CRITICAL for pruning — this is the authorization ledger the whole bridge trusts: authorized_devices (L66–76) rebuilds the live device-auth set by scanning ALL records and taking the latest verified authorized/revoked state per key; capability_map (L42–50) likewise folds ALL records. A device_authorized record can be arbitrarily OLD; if it is pruned while no newer authorize/revoke supersedes it, the device SILENTLY loses authorization (fail-closed but a functional break, and it also drops the countervailing revoke, so state can be wrong either way). These full scans MUST span every segment, OR the folded latest-state-per-key must be materialized into the snapshot/checkpoint so compaction preserves it. DeviceApprover appends supersedes_id=seq (opaque, not resolved here). No get(), no seq==line assumption."
      },
      {
        "file": "/home/kali/sigil/sigil/agents/base.py",
        "spine_calls": "SpineStore.append() at L83 (AUTO), L88 (QUEUE), L94 (DENY/refusal) — the sole spine surface for all agents; seq return stored opaquely in AgentResult.applied/queued (L86,91).",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": false,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "none — API-only, append-only, seq used only as an opaque handle. Fully segment-transparent; this is where every agent's write funnels."
      },
      {
        "file": "/home/kali/sigil/sigil/agents/sentinel.py",
        "spine_calls": "SpineActivityWatcher.poll: iter_records(since_seq=self.since_seq) L28 (bounded window; runner seeds since_seq=head-500). Sentinel.run appends via base._dispatch L77. Indirect: pending_contradictions(self.store,...) L33 (consolidate.queries — out of my list — iterates the store).",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": false,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "Bounded since_seq window anchored near the tip; below-min since_seq degrades to a safe live-file scan (_start_offset_for L328). Segment risk: if the head-500 window straddles a rotation boundary, iter_records MUST span segments or in-window older records are silently missed. It also delegates a store scan to consolidate.queries.pending_contradictions."
      },
      {
        "file": "/home/kali/sigil/sigil/agents/bastion.py",
        "spine_calls": "_refuse: append L147. _resolutions: iter_records() with NO since_seq — FULL SCAN L253, reads r.kind/r.actor/r.payload/r.seq; emits supersedes_id=r.seq L267. run/probe_target append via _dispatch L162,L276.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "PRIMARY segment-spanning risk: the unbounded iter_records() at L253 must read ALL segment files after rotation, else BASTION stops emitting `resolved` supersessions for findings that aged into an older segment (and a prune of old findings silently drops them from resolution consideration). It only POINTS at r.seq (supersedes_id/resolves_seq) — no get() call — so it needs the finding PRESENT in the scan, not resolvable by seq. NOTE: Path(asset.ref).read_text at L206 is a scanned DEPS MANIFEST, not the spine."
      },
      {
        "file": "/home/kali/sigil/sigil/agents/steward.py",
        "spine_calls": "compose_brief: next_seq L24 (head); iter_records(since_seq=head-lookback) at L16 (_recent_activity), L49, L61 — all bounded windows. run appends via _dispatch L90. Indirect: open_threads/due_commitments/pending_contradictions L22-24,32,44 (consolidate.queries scan the store).",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": false,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "All direct reads are tip-anchored bounded windows (head-60/head-400), segment-transparent for correctness IF iter_records spans a rotation boundary that falls inside a window. head-lookback arithmetic assumes seq is a dense counter only for the cosmetic 'last N records' LABEL — under gaps the window holds fewer than N records, a display drift, not a correctness bug (seqs cited in text are opaque). Real scan burden lives in consolidate.queries (out of my list)."
      },
      {
        "file": "/home/kali/sigil/sigil/agents/operator.py",
        "spine_calls": "append L124,L197,L254,L272,L402; _dispatch L136,L213,L282; get(plan_seq) L241 (execute), get(execute_seq) L391 (undo); iter_records(since_seq=plan_seq) L233 (_approved), iter_records(since_seq=execute_seq) L397 (undo single-shot).",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": false,
        "appends": true,
        "depends_on_old_seq_resolving": true,
        "risk_note": "execute()/undo() resolve a prior seq via get() and iter forward from it. These are RECENT workflow seqs (plan just previewed), not deep history, so a sane retention window won't prune them. If a prune DID remove an un-executed plan seq, get() returns None → 'not an operator plan' (fails closed, no crash, never unsafe). Segment-transparent provided get() resolves any live seq and iter spans segments. The _PLANS/_JOURNAL 0700 files it read_texts (L207,L247,L308) are OFF the spine by design."
      },
      {
        "file": "/home/kali/sigil/sigil/agents/actor.py",
        "spine_calls": "get(step_seq) L193 (execute); iter_records(since_seq=step_seq) L203 (single-shot guard); append L135,L141,L147,L155,L162,L215,L223,L230,L240,L248,L256; _dispatch L177. Indirect: action_approved(store,step_seq,...) L197 (actor_gate), scope.creation_allowed(store,...) L139,L247 (actor_scope).",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": false,
        "appends": true,
        "depends_on_old_seq_resolving": true,
        "risk_note": "Same recent-workflow-seq profile as operator: get(step_seq)+forward since_seq scan; prune of a live step fails closed. EXTRA hazard: scope.creation_allowed (L139,L247) enforces the per-service account-creation cap by counting prior `applied` account.create records — if a prune removes those historical creations the cap count RESETS, potentially re-allowing creation past the intended lifetime cap. Cap correctness depends on those records NOT being pruned (or a durable counter). Journal at L173/L209 is 0700 off-spine."
      },
      {
        "file": "/home/kali/sigil/sigil/agents/runner.py",
        "spine_calls": "store.next_seq L18 (_sentinel_scan, head=next_seq-1) — the only direct spine touch. Constructs Sentinel/Bastion/Steward/Envoy/Archivist and calls .run(); no direct iter_records/get/append.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": false,
        "appends": false,
        "depends_on_old_seq_resolving": false,
        "risk_note": "Orchestrator only — segment-transparent. Uses next_seq as a monotonic counter to seed SENTINEL's head-500 window (L19-22); the segment-spanning obligation for that window lives in SpineStore.iter_records, not here. The JSON it read_texts (bastion-assets.json L36, cve-feed L53, inbox.json) are config files, not the spine."
      },
      {
        "file": "/home/kali/sigil/sigil/perception/egress.py",
        "spine_calls": "store.get(seq) at L34 (resolves a caller-supplied egress-request seq); store.iter_records(since_seq=seq) at L39 (forward scan from that seq looking for a matching approval record). Also mesh.authorized_devices(store, ...) at L38 and verify_approval at L42, both API-level.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": false,
        "appends": false,
        "depends_on_old_seq_resolving": true,
        "risk_note": "HARD dependency on get(old_seq) resolving: egress_approved(store, seq, ...) is handed a historical egress-request seq and calls store.get(seq) at L34; if that seq was pruned, get() returns None and it fail-closes to False (L35) — a still-pending egress whose request-seq was pruned could then NEVER be approved. Also the iter_records(since_seq=seq) forward scan at L39 must span every segment from that seq to the live tail after rotation. Both go through the SpineStore API and make no seq==line / contiguity assumption, so a segment-transparent store keeps this correct; the only exposure is prune-window semantics, not indexing."
      },
      {
        "file": "/home/kali/sigil/sigil/perception/recall.py",
        "spine_calls": "store.iter_records() at L35 — a single unbounded FULL scan over the whole spine, filtering kind=='event' and payload signal=='perception', keeping the last (most-recent, seq-ascending) grounded match. Returns latest.seq/entry_hash/ts at L44-46 as identifiers only.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": false,
        "depends_on_old_seq_resolving": false,
        "risk_note": "Full scan at L35 must iterate ALL records and therefore span every segment file after rotation. It never calls get() and treats seq purely as an opaque identifier (no positional/contiguity/zero-based assumption) — API-only and index-agnostic. The only semantic effect of pruning is a coverage loss: 'where did I last see X' can only find sightings still in retained segments, silently returning None (or a newer sighting) once older perception events are compacted away. Segment-transparent as long as iter_records() spans segments."
      },
      {
        "file": "/home/kali/sigil/sigil/gesture/run.py",
        "spine_calls": "Instantiates SpineStore() at L27-29 when no store injected; store.append(kind='refusal', ...) at L44 (egress-refusal record). Delegates all other spine access to SessionGate (g.arm/g.disarm/g.handle/g.arm_by_device) and to session.pending_device_arms(store, trusted_pubkey) at L78.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "Appends a refusal record (L44) and, on the device-arm path, drives pending_device_arms() (L78) which does a full scan (see session.py). No raw-file access, no seq==line assumption, no get(old_seq). Segment-transparent apart from the full-scan reachability it inherits from pending_device_arms / SessionGate — those must span segments after rotation."
      },
      {
        "file": "/home/kali/sigil/sigil/gesture/session.py",
        "spine_calls": "store.append at L160 (arm), L169 (disarm), L243 (arm_by_device SESSION_ARMED), L256 (_refuse_arm), L301 (A1 action event) and L309 (A2 queued approval, captures returned seq). store.iter_records() FULL scans: pending_device_arms at L56 and the replay-nonce check in arm_by_device at L217. store.path.stat().st_size at L122 (killswitch growth-detector).",
        "reads_raw_file": true,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": true,
        "appends": true,
        "depends_on_old_seq_resolving": false,
        "risk_note": "TWO segment-design hazards. (1) _killswitch_engaged (L120-129) reads store.path.stat().st_size directly and assumes every append monotonically GROWS that one file, only re-running the authoritative KillSwitch scan when size changes. A segment/rotation design MUST keep store.path pointing at the live append target: if appends move to a new segment while store.path goes stale, a panic-append would not change the stat'd size and the kill-switch could be MISSED mid-session (a device-armed session the owner isn't watching). Rotation that shrinks the stat'd size is safe (any size delta just forces a rescan). (2) The replay-nonce scan (L217) and pending_device_arms (L56) are full scans that must span all segments — a prune that drops old SESSION_ARMED records could let a replayed device-arm nonce slip past the replay gate. No seq==line assumption and no get(old_seq); the exposure is the raw stat() growth heuristic plus full-scan completeness, not indexing."
      },
      {
        "file": "/home/kali/sigil/sigil/config.py",
        "spine_calls": "None. Defines SPINE_PATH at L53, HEAD_PATH at L54, KEYS_DIR at L55 (and effective_config() echoes them as strings at L159-162), plus ensure_dirs() which mkdirs SPINE_PATH.parent at L131. It never opens, reads, iterates, or appends to the spine and never calls any SpineStore/checkpoint method.",
        "reads_raw_file": false,
        "assumes_seq_is_line_or_zero_based": false,
        "does_full_scan": false,
        "appends": false,
        "depends_on_old_seq_resolving": false,
        "risk_note": "none — pure path/config module. It DECLARES the SPINE_PATH/HEAD_PATH constants that SpineStore consumes but never touches the file itself, so it is fully segment-transparent. A segment/rotation design would extend this file with the new segment-directory / offset-index / manifest path constants (the natural home for them), but nothing here needs changing to remain correct."
      }
    ],
    "conflict": {
      "prune_vs_rollback": "A front-prune trips THREE independent guards, all in the read/verify path:\n\n(1) GENESIS PREFIX — verify_chain (chain.py:38,40): it seeds `prev = _GENESIS_PREV` (\"0\"*64) and the very first entry is checked `if e.prev_hash != prev: return False \"chain break ... prev_hash mismatch (entry deleted/reordered)\"`. After pruning records 0..K-1, the new first retained record (seq K) has `prev_hash == entry_hash(record K-1)`, which is NOT \"0\"*64, so verify_chain fails on entry[0] immediately. This is the hardest wall: verify_chain is structurally rooted at genesis.\n\n(2) COUNT SHRINK = TAMPERING — classify_head (checkpoint.py:92-94): `n, signed_n = len(entries), head.entry_count; if n < signed_n: return False, \"TAMPERING: chain has {n} records but the signed head anchors {signed_n} (truncated/rolled back)\"`. entry_count is ABSOLUTE (set at sign time to `len(entries)`, chain.py:60, where entries is the whole log from seq 0). Pruning K records drops n by K, so n < signed_n → classified as truncation/rollback. This is exactly the \"pruning is indistinguishable from a rollback attack\" conflict: the check cannot tell a benign archive from a malicious truncation because both present a shorter chain than the last signed count.\n\n(3) PREFIX RE-VERIFY — classify_head (checkpoint.py:96) then calls `verify_head(head, entries[:signed_n], tr)`. Inside verify_head (chain.py:68-74) it re-runs verify_chain (which fails at #1 for a pruned chain), and even if that passed it recomputes `exp_hash = entries[-1].entry_hash`, `exp_seq = entries[-1].seq` and requires `head.head_hash == exp_hash and head.last_seq == exp_seq and head.entry_count == len(entries)` else \"head does not match the chain (log truncated or head rewritten)\". A pruned chain cannot satisfy the head_hash/last_seq/entry_count triple the owner signed over the full history. Note verify_head's `prev_highwater` monotonic check (chain.py:78-79) is DORMANT on this path — classify_head passes no prev_highwater — so on the checkpoint path anti-rollback is carried entirely by the entry_count comparison plus the Ed25519 signature over head_hash/last_seq/entry_count.",
      "what_signed_snapshot_must_change": "Introduce a signed SnapshotAnchor and make three quantities that are currently implicit — the genesis prev, the absolute record count, and the meta-lineage of heads — explicit and signed. Minimal, security-preserving shape:\n\n1. NEW SIGNED RECORD — SnapshotAnchor, Ed25519-signed by the same owner trust root (checkpoint.trust_root, threshold 1), attesting at least: {base_seq K, base_prev_hash H, base_count C, superseded_head_hash P}. Here H = entry_hash of the last PRUNED record (seq K-1) — it becomes the synthetic prev for the re-based chain; C = the absolute number of records that existed at/below K-1 (so absolute counting survives the prune); P = the head_hash of the immediately-prior SignedChainHead this snapshot descends from. Ideally H is (or commits to) a Merkle root over the pruned prefix so archived records can still be proven members later.\n\n2. verify_chain gains a `genesis_prev` parameter (default \"0\"*64). When verifying a re-based segment it is seeded with `genesis_prev = snapshot.base_prev_hash` instead of _GENESIS_PREV (chain.py:38). The first live entry (seq K) then links cleanly because its prev_hash already equals H. Genesis-rootedness is preserved — the root is just relocated to a SIGNED value rather than a hard-coded constant.\n\n3. entry_count / last_seq become explicitly ABSOLUTE and snapshot-aware. classify_head must compute the expected LIVE length as `head.entry_count - snapshot.base_count` and compare THAT to len(live entries), and it must slice/verify against the live window, not `entries[:signed_n]` with an absolute signed_n (checkpoint.py:96 would mis-slice). head.last_seq stays the ABSOLUTE tip seq (unchanged by a front-prune).\n\nTHE HOLE TO NOT OPEN (truncation-forged-as-snapshot): the security of relocating genesis rests entirely on (a) the snapshot being owner-signed AND (b) it being provably the child of exactly one prior anchor. So verify MUST require snapshot.superseded_head_hash == the head_hash of a previously accepted SignedChainHead, and MUST enforce monotonicity: snapshot.base_seq and the new head.last_seq may never regress below any previously accepted high-water, and snapshot.base_count must equal the entry_count that the superseded head asserted at that boundary. This makes the sequence of heads/snapshots an unbroken META-CHAIN even as record-level history is pruned: a verifier holding an older head with last_seq=1000 rejects any snapshot/head presenting last_seq<1000 or a base_count that doesn't reconcile with what the older head signed. Without that binding, anyone could present an arbitrary truncation, wrap it in a \"snapshot,\" and a fresh verifier could not distinguish it from a legitimate rebase — that is the forgery hole. The owner key is what makes the snapshot unforgeable; the superseded-head binding + absolute-count + monotonic-last_seq are what stop a *replayed old snapshot* or a *lower rebase* from erasing records undetectably.",
      "anti_rollback_interaction": "Two distinct anti-rollback mechanisms exist and they key off DIFFERENT quantities:\n\n(A) SpineTailer._high_water (tail.py:60,89-93) is in-memory, per-process, initialized to -1, and tracks the TIP seq: `head_seq = entries[-1].seq` then `if head_seq < self._high_water: rollback=... else self._high_water = head_seq`. Crucially it watches entries[-1].seq (the MAX/tip seq), NOT the record count and NOT head.entry_count. A legitimate FRONT-prune removes low-seq records but leaves the tip seq unchanged, so `head_seq` is unchanged and _high_water does NOT falsely trip. Good — provided seqs stay ABSOLUTE.\n\n(B) head.last_seq (chain.py:59,73; used as _signed_last_seq at tail.py:106) is the ABSOLUTE tip seq the owner signed; a front-prune leaves it correct, and records with seq <= last_seq stay `anchored=True` (tail.py:77).\n\nWHAT FALSELY TRIPS: not the high-water, but classify_head's `n < signed_n` count check (checkpoint.py:93) and verify_chain's genesis-prefix check (chain.py:40) — see the prune-vs-rollback field. So under the current model a legit prune sets anchor_ok=False while rollback stays None; both surface to a consumer as \"not anchored / tampered.\" The high-water and last_seq are the parts that a snapshot design can PRESERVE cleanly; the count and genesis checks are the parts that must be taught about the snapshot.\n\nTHE TRAP: if a naive prune RENUMBERS the surviving records to start at seq 0 (to satisfy the genesis-prefix check the easy way), then `entries[-1].seq` drops by K. That WOULD falsely trip _high_water (head_seq < _high_water) on any process that had already observed the higher tip, and it destroys last_seq monotonicity — turning the one guard that correctly tolerates a prune into a false alarm and simultaneously making a real truncation indistinguishable from a rebase. Keep seq absolute and monotonic across prune; only relocate the genesis prev (via the signed snapshot), never the seq origin.\n\nPersistence caveat: _high_water is NOT persisted (resets to -1 at process start, tail.py:60) and classify_head passes no prev_highwater to verify_head (chain.py:78 dormant). So cross-restart anti-rollback rests entirely on the persisted signed head's entry_count/last_seq/head_hash. A snapshot must therefore persist its monotonic floor IN the signed anchor (and refuse to load a head/snapshot whose last_seq/base_seq is below the last accepted one), because the in-memory water mark cannot carry that guarantee across a restart or across a compaction that runs in a separate process.",
      "segmentation_verify_requirement": "Today SpineStore is one file (self.path); iter_records reads it byte 0..EOF and entries()/verify() are built on iter_records (store.py:197-303). To span ordered segments and still catch missing/reordered/corrupt segments:\n\n1. ORDER MUST COME FROM THE CHAIN, NOT FILENAMES. iter_records must concatenate segments so the record stream is contiguous, but verification of that ordering must rest on the hash linkage + seq contiguity that verify_chain already enforces, not on a glob/mtime sort. Because verify_chain checks (chain.py:40) `e.prev_hash == prev` across every adjacency and (chain.py:44) `e.seq == entries[i-1].seq + 1`, a MISSING interior segment (jump seg N-1 -> seg N+1) breaks BOTH prev_hash linkage and seq contiguity at the seam -> caught. A REORDERED segment presents a prev_hash that doesn't match the running `prev` -> caught. A CORRUPTED record inside a segment fails binding (store.verify digest_payload check, store.py:300) or entry_hash derivation (chain.py:42) -> caught. So as long as segments are fed to verify_chain as ONE ordered stream and the first live segment is rooted at the signed genesis/snapshot prev, structural tamper within or between segments is caught by the existing logic.\n\n2. TAIL-SEGMENT TRUNCATION IS THE GAP THE CHAIN ALONE CANNOT CATCH. Dropping the LAST segment yields a shorter chain that still links cleanly internally (verify_chain passes over seq 0..K). Only the SIGNED head catches it: classify_head's `n < signed_n` (checkpoint.py:93) and verify_head's head_hash/last_seq/entry_count triple (chain.py:73). Therefore the signed head (or a signed segment MANIFEST listing segment ids and their seq ranges + boundary hashes) must attest the tip — otherwise a dropped tail segment between two `sigil sign` runs is guarded only by the ephemeral in-memory high-water, which is lost on restart.\n\n3. THE FIRST SEGMENT MUST BE ROOTED. verify must still require entries[0].prev_hash == the expected genesis (either _GENESIS_PREV or, after a rebase, the signed snapshot.base_prev_hash). If a design lets the first live segment start with any prev_hash, front-truncation of whole segments goes undetected.\n\n4. MECHANICAL CONSEQUENCES in store.py: iter_records/_start_offset_for and the seq->byte-offset index (_offsets, _scan_pos, _ensure_index at store.py:330-352) assume ONE append-only file whose offsets never move; the offset key must become (segment_id, offset). _ensure_index's shrink-detection (`size < self._scan_pos -> rebuild`, store.py:347) will MISFIRE when the active segment rotates to a new small file — rotation must be modeled explicitly, not read as an in-place shrink (a real shrink is a truncation attack and must stay detectable). _read_last_entry/_last_valid_boundary and tail() only look at self.path and must target the ACTIVE (highest) segment; the torn-tail truncate-before-append logic (store.py:168-171) must operate on the active segment only. count() becomes O(all segments).",
      "risks": [
        "Re-signing the head over the pruned chain with a fresh genesis and entry_count = live-count: this is the biggest trap. It makes classify_head's `n < signed_n` check (checkpoint.py:93) always pass (n == entry_count), silently converting the one check that detects truncation/rollback into a no-op. A legitimate prune and a malicious truncation then look identical to every verifier.",
        "Snapshot that does not bind to the prior signed head (no superseded_head_hash): a replayed OLD snapshot or an arbitrary rebase point can erase records K..M and present as a legitimate re-base; a verifier holding an older head with a higher last_seq cannot prove records were dropped. Truncation-forged-as-snapshot.",
        "Renumbering surviving records to seq 0 after a prune (to cheaply satisfy verify_chain's genesis-prefix check, chain.py:40): destroys absolute last_seq monotonicity, falsely trips SpineTailer._high_water (tail.py:90) on any process that saw the higher tip, and makes a real truncation indistinguishable from a rebase. Seq must stay absolute; only the genesis PREV may be relocated, never the seq origin.",
        "Trusting segment order from filenames/glob/mtime instead of the hash linkage: a reordered or dropped interior segment could be silently reassembled; ordering must be derived from prev_hash linkage + seq contiguity (verify_chain) and the segment manifest must be signed.",
        "Dropping the tail segment before the next `sigil sign`: only the ephemeral in-memory _high_water guards the un-notarized tail, and it resets to -1 on restart (tail.py:60). Across a restart, or if the head was re-signed lower, a tail truncation becomes undetectable. The signed head/manifest must attest the tip (head_hash/last_seq/entry_count).",
        "Archiving pruned records to cold storage WITHOUT committing to them: the snapshot's base_prev_hash must be (or commit to, e.g. a Merkle root over) the pruned prefix so an archived record can still be proven a member of the chain later. If the snapshot only carries the boundary link, verify() can no longer recompute binding for archived records and their membership is unprovable.",
        "Leaving classify_head's `entries[:signed_n]` slice (checkpoint.py:96) unchanged with an absolute signed_n while entries is live-only: it slices the wrong window (or IndexErrors), potentially verifying a tampered chain against a mis-aligned prefix or failing benign chains. entry_count must be reconciled with snapshot.base_count before slicing/comparing.",
        "The seq->byte-offset index shrink-detection (`size < _scan_pos -> rebuild`, store.py:347) firing on segment rotation: rotation looks like an in-place shrink and could mask a genuine truncation as a benign rebuild. Rotation must be an explicit, distinguished event so real shrink-attacks stay detectable.",
        "Anti-rollback state is in-memory and per-process (tail.py:60) and classify_head passes no prev_highwater to verify_head (chain.py:78 dormant): a compaction run by a SEPARATE process won't update a live SpineTailer's high-water, so cross-process/cross-restart rollback detection must rely on a PERSISTED monotonic floor carried in the signed snapshot/head, not the water mark."
      ]
    },
    "durability": {
      "append_guarantees": "append() (store.py:141-194) makes these durability guarantees, in order, for every call that RETURNS a seq (= an acked record):\n\n1. Serialization frame: the whole read-tip→write runs under `spine_lock(self.path)` (store.py:154) and, inside it, under the file's `fcntl.flock(fileno, LOCK_EX)` (store.py:158-160). flock failure is SWALLOWED (`except OSError: pass`, store.py:161-162) and fcntl may be None on non-POSIX (store.py:22-25), so cross-process exclusion is best-effort, not guaranteed.\n\n2. Torn-tail truncate BEFORE write (BLOCK-1 fix, store.py:168-173): `clean_end, last = _last_valid_boundary(self.path)` (store.py:83-121) seek-scans from EOF for the byte offset just past the last line that JSON-parses AND carries all _REQUIRED_KEYS (store.py:31). If `pre_size = os.fstat(fileno).st_size > clean_end` it calls `f.truncate(clean_end)`. This removes only never-committed garbage from an interrupted prior write; committed (valid) records are never touched. Without this, an append would MERGE torn+new into one unparseable line and silently lose the ack'd record while verify() stayed green.\n\n3. Chain derivation from the TRUE on-disk tip: `entry = append_entry([last], cert_digest) if last else build_chain([cert_digest])[0]` (store.py:174, chain.py:30-34) — seq and prev_hash come from `last` (the on-disk boundary entry), re-read under the lock, NOT from possibly-stale self._last.\n\n4. Write lands at `offset = clean_end` (store.py:180-181; after any truncate EOF==clean_end, and `a+b` append mode forces every write to EOF).\n\n5. DURABILITY POINT: `f.flush()` then `os.fsync(f.fileno())` (store.py:182-183, FIX 3) execute BEFORE the function returns the seq. So a returned seq means the record's bytes are fsync-durable across a crash. The fsync is of the file data+metadata fd (size growth persists). NOTE: append does NOT fsync the parent directory — fine for an already-existing spine file, but on FIRST creation of the file the directory entry is not dir-fsync'd (unlike checkpoint._atomic_write_text which does dir fsync at checkpoint.py:53-58).\n\n6. In-memory index update under `_index_lock` (store.py:187-192) and `self._last = entry` (store.py:193) — non-durable bookkeeping only, replayable from disk on restart via _read_last_entry (store.py:305-312).\n\nRestart tolerance: _last_nonempty_line (store.py:50-80) and _last_valid_boundary skip a torn/garbage tail, so a crash mid-write never blocks a restart and the next append re-truncates the dead tail.",
      "concurrency_contract": "Two independent layers serialize writers:\n\nLAYER 1 — process-wide RLock. `spine_lock(path)` (store.py:37-47) returns a per-resolved-path re-entrant threading.RLock from a module-global dict `_LOCKS` guarded by `_LOCKS_GUARD` (store.py:33-34). It serializes the read-tip→write of append across THREADS in one process (bridge server, gesture daemon), so two threads can't both fork off a stale tip and break the chain. Being RE-ENTRANT, an outer holder (e.g. the nonce replay check-then-append gate) can make check+append atomic while the inner append re-acquires the same lock.\n\nLAYER 2 — cross-process advisory flock. Inside append, `fcntl.flock(f.fileno(), LOCK_EX)` (store.py:158-160) is held for the lifetime of the `with self.path.open(\"a+b\")` block (store.py:157) — acquired AFTER spine_lock, released when `f` closes (dedent after store.py:192, i.e. after fsync and the in-block index update; self._last at :193 is after flock release but still under spine_lock). Because the read-tip (_last_valid_boundary at store.py:168) runs under flock, an appender always sees the true committed tip, not a racing writer's partial.\n\nCRITICAL for rotation — the flock is bound to the OPEN FILE DESCRIPTION (inode), NOT the path. Appenders in other processes open the spine BY PATH afresh each call and flock whatever inode currently sits at that path. Therefore a rotation that does os.replace() (swapping the inode) CANNOT use flock-on-the-old-spine-file to exclude appenders: an appender that open()s the path after the replace flocks the NEW inode — no mutual exclusion; and an appender holding the OLD inode's fd (a+b) will keep appending to the now-unlinked old file, losing acked records. os.replace-based cutover races cross-process appends by construction.\n\nWhat a rotation/compaction/prune MUST acquire so it can never race an append (or be raced):\n- Acquire `spine_lock(path)` (the same RLock) first — excludes all in-process appends and other rotations. Sufficient ONLY if every writer is in-process.\n- For cross-process safety, the lock must be moved to a PATH-INDEPENDENT, inode-stable lockfile (e.g. `spine.lock`) that BOTH append and rotation flock BEFORE resolving/opening the active segment. The current flock-on-the-target-file is inode-unstable and unsound once os.replace enters the picture. Equivalently, appends must, under the lock, re-verify the file they opened is still the active segment (compare st_dev/st_ino or a manifest epoch) and retry — but the dedicated lockfile is the clean fix.\n- Rotation must guarantee no appender holds an open fd to a segment it is about to replace/unlink; the shared lockfile + appenders opening the active segment only under that lock provides this. It must also fsync the new content before, and unlink old segments only after, the atomic pointer swap (see cutover atomicity).",
      "cutover_atomicity_requirement": "A segment cutover / prune must have the SAME crash-atomicity that checkpoint._atomic_write_text (checkpoint.py:33-60) gives the head file: write new content to a temp file in the SAME dir → flush+os.fsync the fd → os.replace(tmp, target) (atomic rename on POSIX: a reader sees strictly old or strictly new) → open parent O_DIRECTORY and os.fsync it so the rename itself survives a crash. Crash at any point leaves the previous valid state intact, never a torn one.\n\nThe complication: the head is ONE small file, so a single os.replace IS the whole cutover. A rotated spine is MULTIPLE files (segments + an index/manifest); a multi-file swap is NOT atomic under a single os.replace. So the design must introduce ONE authoritative pointer — a MANIFEST file naming the active segment set — and make the manifest the sole linearization point, swapped with the exact _atomic_write_text pattern. Required ordering:\n1. Write the new/compacted segment(s) to temp names in the spine dir; fsync each file's data.\n2. Write the new manifest to a temp file; fsync it.\n3. os.replace() the manifest over the live manifest (the single atomic cutover instant).\n4. fsync the directory so the manifest rename is durable.\n5. ONLY THEN unlink superseded old segments.\n\nThis yields: a crash before step 3 = the pre-rotation spine, with every acked record present (no loss); a crash after step 3 = the post-rotation set. Never a half-pruned file, never a lost ack. To avoid DOUBLE-COUNTING, readers/entries() and count() must derive the record set solely from the manifest — never from a directory glob (a glob would see records present in both old and new segments during the unlink window). The active append tail segment named by the manifest must retain the torn-tail-truncate + fsync-before-ack semantics unchanged, and seq/prev_hash must carry across the segment boundary (append_entry needs the last record's entry_hash+seq — supplied from the prior segment / the baseline head) so verify_chain's strict seq contiguity and prev_hash linkage (chain.py:40-45) never break at a segment seam.\n\nAdditionally the cutover is coupled to the signed head: because classify_head flags len(entries) < signed entry_count as TAMPERING (checkpoint.py:93-94), a prune MUST be paired with a new owner-signed \"compaction baseline\" head, and the manifest swap and the head swap must be crash-consistent together (either both or neither) — otherwise a crash between them yields entries() disagreeing with HEAD_PATH and a false TAMPERING / rollback window.",
      "index_interaction": "The seq→byte-offset index keys its update decision purely on FILE SIZE vs `self._scan_pos` (the absolute byte offset just past the last complete line indexed; store.py:137, 382). _ensure_index (store.py:330-351):\n- not built → full scan from 0 (store.py:339-344).\n- size > _scan_pos → EXTEND, scanning only [_scan_pos, size) — assumes append-only ⇒ existing offsets never move (store.py:345-346).\n- size < _scan_pos → REBUILD from 0 (store.py:347-351) — treats a shrunk file as truncated/rewritten.\n- size == _scan_pos → no-op.\n_scan_from (store.py:353-382) indexes only COMPLETE newline-terminated lines and stores absolute offsets in `_offsets[seq]`. get() (store.py:220-242) seeks to `_offsets[seq]` and re-reads, guarding `rec.seq == seq` (store.py:235). append maintains it O(1) only when `_scan_pos == offset` exactly (store.py:188-192), else it lets the next read's _ensure_index extend.\n\nBehavior under rotation/replace/shrink — the size-only heuristic is UNSOUND across os.replace:\n- Same-or-LARGER in-place replacement (os.replace swaps the inode to a file of size >= _scan_pos): _ensure_index takes the EXTEND branch and scans [_scan_pos, size) of the NEW file, while `_offsets` still holds offsets computed against the OLD inode's layout. The index is now silently corrupt: offsets point into byte positions that mean something different in the new file. get() partially fails safe (rec.seq==seq guard turns a wrong seek into a None miss, store.py:235), but iter_records via _start_offset_for (store.py:315-328) can seek mid-file into the wrong content. Size change detection CANNOT see an equal-or-larger inode swap — the core hazard.\n- SHRINK / prune of the active file: size < _scan_pos → REBUILD (correct for a single-file spine). Pruned low seqs simply won't be in _offsets; get(pruned_seq) falls to the bounded iter fallback (store.py:236-242) and returns None — acceptable only if those records are genuinely gone.\n- Absolute offsets are single-file assumptions: once the spine is multiple segments, a flat `_offsets: dict[seq,int]` keyed to one path is wrong by construction — offsets must become (segment_id, offset).\n- Live consumers each hold their own SpineStore+cursor (SpineTailer, tail.py:51-62): after a prune, poll()'s linkage check `r.prev_hash != self._last_hash` (tail.py:71-73) will FLAG a false linkage break at the first post-prune record because _last_hash references a now-deleted entry, and check_anchor's monotonic high-water (tail.py:89-93) will read a lower head_seq → false ROLLBACK.\n\nWhat the design MUST do:\n- Detect IDENTITY change, not just size: track st_dev/st_ino (and/or a manifest epoch/generation counter) in the store and REBUILD/REBIND whenever the inode or manifest version changes — not only when size shrank. The current `size >` extend / `size <` rebuild logic (store.py:345-347) must gain an epoch check.\n- Make the index SEGMENT-AWARE: key offsets by (segment, offset); on a manifest-epoch change, rebind to the active segment set and rebuild/relocate. The manifest version is the index epoch.\n- Appends must re-resolve the active segment under the path-independent lock and never write through a stale fd.\n- Define tailer re-anchoring: after a compaction baseline, live tailers must re-seed _last_hash and the high-water from the new baseline head, or they emit permanent false integrity/rollback failures.",
      "crash_scenarios": [
        "1. Crash AFTER writing new compacted segment(s) but BEFORE the atomic manifest swap. If cutover follows temp→fsync→replace→dirfsync, NO invariant is violated: the pre-rotation spine is fully intact and every acked record present; the temp segment is orphaned garbage (cleanup on next start). Threatens only disk space. This is the TARGET behavior.",
        "2. Crash AFTER manifest swap but BEFORE old segments are unlinked. No invariant violated IF readers trust ONLY the manifest. Threatens: a disk-space leak, and — if any reader/count/entries derives the set from a directory GLOB instead of the manifest — DOUBLE-COUNTING records present in both old and new segments (breaks the exact len(entries) that classify_head compares to entry_count, checkpoint.py:93,99).",
        "3. Crash BETWEEN the segment/manifest cutover and the new signed-head swap. Threatens the anchor<->chain agreement / anti-rollback. If segments swapped first: store.entries() reflects the pruned baseline but HEAD_PATH still anchors the old entry_count ⇒ len(entries) < signed_n ⇒ FALSE TAMPERING (checkpoint.py:93-94). If head swapped first: entries() exceeds the signed prefix and head_hash won't match ⇒ 'history rewritten' TAMPERING (checkpoint.py:96-98, verify_head chain.py:71-74). Both swaps must be made crash-consistent together (single covering pointer, or ordered write-and-dirfsync of a monotonic superset-safe baseline head before the manifest swap).",
        "4. Crash DURING an append that raced a rotation: the appender opened the OLD inode (a+b) and rotation os.replace()d the path. The appender's fsync'd write lands in the now-unlinked old inode ⇒ an ACKED record vanishes from the live spine. Threatens the core no-lost-ack durability guarantee (store.py:183). Prevented only by a path-independent lockfile + re-resolving the active segment under the lock, NOT by the current flock-on-the-target-file (flock binds to the inode, not the path).",
        "5. Crash mid-append on a freshly-rotated active segment whose 'clean boundary' the appender miscomputes. If _last_valid_boundary (store.py:83-121, called at store.py:168) scans a segment that is not the true seq-contiguous chain tip, the new record's seq/prev_hash derive from the wrong tip ⇒ a seq gap or prev_hash break caught by verify_chain (chain.py:44-45,40-41). Threatens seq contiguity + chain linkage. Rotation must guarantee the active append segment's last valid record IS the true chain tip and carry seq/entry_hash across the segment seam.",
        "6. Crash after unlinking old segments while the manifest still (or again, post-rollback) references them — i.e. unlink ordered BEFORE the manifest drop is durable. The manifest points at missing segments ⇒ acked, committed records become unreadable. Threatens durability/availability of committed data. Mandatory ordering: replace manifest → dir fsync (mirror checkpoint.py:53-58) → THEN unlink.",
        "7. Crash after os.replace of the manifest but BEFORE the directory fsync. On POSIX the rename may not survive, rolling back to the pre-swap manifest. Safe IF old segments still exist (unlink is later, per scenario 6). If unlink already happened, the rolled-back manifest points at unlinked segments ⇒ data loss. Threatens cutover durability; enforce replace→dirfsync→unlink strictly.",
        "8. No-crash-needed index corruption: a live SpineStore whose _ensure_index checks only size (store.py:345-347) after an equal-or-larger inode replacement takes the EXTEND branch and serves offsets computed against the OLD file. Threatens read correctness; get() degrades to a false miss via the rec.seq==seq guard (store.py:235) but iter_records can seek mid-stream into wrong content. Requires inode/epoch-based invalidation.",
        "9. Crash leaving a TORN or duplicated manifest because the manifest was NOT written via the temp+fsync+replace+dirfsync pattern. A torn manifest ⇒ the active segment set is unreadable ⇒ the whole spine is unreadable. Threatens total availability; the manifest MUST reuse _atomic_write_text (checkpoint.py:33-60) exactly.",
        "10. Rotation interrupted such that a record is counted both in the signed baseline head (entry_count) and re-derived from a live segment ⇒ DOUBLE-COUNT / seq duplication, tripping classify_head's len(entries)-vs-entry_count comparison (checkpoint.py:93,99) or verify_chain seq contiguity. Threatens the monotonic-count/anti-rollback invariant; entries() must be defined over the single manifest so len is exact and monotonic."
      ]
    },
    "critique": "I have enough verified detail. Writing the critique now.\n\n---\n\n# COMPLETENESS CRITIC — findings\n\nI re-read `store.py`, `chain.py`, `checkpoint.py`, `tail.py`, `verify.py`, `models.py` (both), and re-grepped `sigil/` for every spine-touch. The four analyses are strong and mostly correct. Below is only what is **missing or wrong**, grouped as requested.\n\n## (a) Invariants missing from the list\n\n**A1 — The signed-head and ChainEntry wire schemas are CLOSED (`extra=\"forbid\"`).** `SignedChainHead` (`reuse/models.py:58`) and `ChainEntry` (`reuse/models.py:49`) both set `model_config = ConfigDict(extra=\"forbid\")`. Every analysis that proposes adding `base_seq / base_prev_hash / base_count / superseded_head_hash / snapshot_hash` **to the head** assumes the head can carry new fields. It cannot without editing the pydantic model. Worse, the failure is asymmetric and the invariants list never states it:\n- An un-upgraded verifier that reads a new-format head hits a `ValidationError`.\n- `SpineTailer._resolve_head` (`tail.py:113-119`) **swallows** it → returns `None` → the whole spine silently reads as **\"no signed head / un-notarized\"** (anti-rollback + anchoring quietly OFF, fail-safe-ish but silent).\n- `checkpoint.verify_checkpoint` (`checkpoint.py:108`) has **no** try/except around `model_validate_json` → the same head **crashes** `sigil verify` and `mcp ingest_status` (which calls `verify_checkpoint`, `mcp/server.py:128`).\n\nSo the head-format migration is a hard, coordinated, all-verifiers-at-once change, and today the two head-consumers disagree on how they fail on an unknown head. This deserves its own invariant (\"head/entry schema is a closed, signed wire format; any snapshot field is a breaking `schema_version` migration; all verifiers must upgrade atomically or lose anchoring silently\").\n\n**A2 — Sealed-segment tail must be clean; the torn-tail truncate only ever protects the ACTIVE append target.** `iter_records` (`store.py:209-216`) and `_scan_from` (`store.py:372-378`) **skip** any malformed/partial line, and `verify_chain` turns a skip into a fatal seq gap (`chain.py:44`). The BLOCK-1 truncate runs only inside `append()` against `self.path` (`store.py:168-171`). If a crash occurs on the record that triggers rotation, the just-sealed segment can end on a **torn line that no future append will ever truncate** (it is no longer the active file) → permanent false `verify()` gap at the seam. New invariant: *rotation must run the same `_last_valid_boundary` truncate on a segment at SEAL time*, because the append-path truncate cannot reach a sealed segment. (Note the within-file merge hazard that BLOCK-1 fixes does NOT recur across two files — `for raw in f` per-file yields the final partial line separately — but the skipped-torn-line-becomes-gap hazard does.)\n\n**A3 — `next_seq` is a CACHED value (`self._last`), not a disk read.** `next_seq` (`store.py:270-272`) returns `self._last.seq + 1` from the in-memory tip captured at construction / own-append. It is read as \"head seq\" by **~8 consumers** (`dashboard.py:19`, `steward.py:25`, `runner.py:18`, `graph/rebuild.py:171`, `mcp/server.py:85,120`, `consolidate/pipeline.py:72`, `graph/query.py:117`). The index/`self._last` staleness invariant only discusses `_offsets`; it omits that a **long-lived store instance** (gesture daemon, agent runner) whose file was compacted by a *separate process* keeps a stale `next_seq`. A front-prune leaves the tip unchanged so this is benign there — but any design that also rotates/renumbers, or that a consumer trusts `next_seq` as \"records exist below,\" must force these instances to re-read. Add `next_seq` to the \"must invalidate on epoch change\" invariant, not just `_offsets`.\n\n**A4 — The \"size-as-change-token\" anti-pattern lives in THREE independent places, not one.** The index invariant flags `store._ensure_index` size-only detection (`store.py:335,345-347`). But the *same unsound heuristic* is re-implemented by two consumers that reach past the API: `killswitch.py:57` (`store.path.stat().st_size` as a verdict cache key) and `gesture/session.py:121` (`store.path.stat().st_size` as a kill-switch growth detector). All three assume \"size only grows.\" A single shared epoch/generation token (dev/ino + manifest version) must replace **all three**, or a same-size rotation serves a stale kill-switch verdict in the guarded (un-halting) direction. The analyses flag each site separately but never state that the fix must be unified.\n\n## (b) Consumers mis-classified / entirely missing\n\n**The 34-consumer set is incomplete — I found 5+ spine-touching files not in it:** `consolidate/queries.py`, `consolidate/gate.py`, `consolidate/promote.py`, `consolidate/brief.py`, and the four `ingest/*` writers. Two matter:\n\n**B1 — `consolidate/queries.py` is UN-ANALYZED and is the single most citation-heavy MCP surface.** It backs the `threads_open`, `commitments_due`, `contradictions_pending` MCP tools and the nightly brief. It runs `iter_current(store, kinds)` (full scan, superseded-subtraction — inherits every prune hazard the analysis assigned to `revise.py`) **and** emits `source_seqs`, `conflicting_seqs`, `verified_seqs` as spine citations (`queries.py:19,31,52,80`). The analysis repeatedly hand-waves this file as \"out of my list\" (in the sentinel/steward/mcp entries) despite it being: full-scan, prune-sensitive, and a **producer of dangling seq citations** exactly like `query.py`/`mcp`. A front-prune silently drops open commitments/threads (the product's core \"never forget a loop\" promise) and hands the owner citations that no longer resolve. This file must be a first-class consumer in the design.\n\n**B2 — `consolidate/gate.py:93` does `store.get(s)` over `cand.source_seqs`** (the grounding re-execution gate) — a `get(seq)` call omitted from the consumer inventory. It is bounded to the fed window (`gate.py:78-80` rejects out-of-window seqs) so it is recent-seq / prune-safe **today**, but if consolidation is ever re-run over historical context, `get()` on a pruned seq returns `None` → fact refused as UNGROUNDED (fail-closed, not dangerous, but a silent correctness change). Worth listing.\n\n**B3 — `tail()` is mislabeled \"segment-transparent.\"** `bridge/daemon.py` `submit_arm_request` uses `store.tail(2048)`. `tail()` (`store.py:244-268`) seeks from the end of `self.path` only. Right after a rotation the active segment holds few records, so `tail(2048)` silently returns **fewer** than 2048, shrinking the arm-replay dedup window across the seam. The analysis calls this \"segment-transparent … safe because ARM_FRESHNESS refuses aged replays\" — the *safety* conclusion holds, but the *transparency* claim is wrong: `tail()` needs new code to span active+sealed segments or the window quietly narrows.\n\n**B4 — `ingest/*` cursors are OFF-spine and NOT seq-based → prune-safe, but were never confirmed.** `git.py` keys on last-commit-hash (`git.py:63,75`); `transcript.py`/`subagents.py` key on transcript line-count `records_seen`/`seen` (`transcript.py:82`, `subagents.py:51,71`). Good news for prune-safety — but `transcript.py:97,102` sets `parent_id=parent_seq` (a real spine seq), so ingested records DO create `parent_id` references that the referential-integrity invariant must cover across a prune. Confirm and list them.\n\n## (c) Hole in the tamper-conflict analysis\n\n**C1 — \"Persist the monotonic floor IN the signed anchor\" is circular against a filesystem-write attacker and useless to a fresh verifier.** This is the sharpest gap. Today there is **no durable anti-rollback floor outside the replayable data**: `_high_water` is in-memory, resets to `-1` each process (`tail.py:60`), and the checkpoint path passes **no** `prev_highwater` (`checkpoint.py:96` → `verify_head` at `chain.py:78` is dormant). Cross-restart/cross-device anti-rollback rests entirely on (owner-key-unforgeability) + (the `(spine.jsonl, HEAD_PATH)` pair being internally consistent). An attacker who can write the filesystem can replace **both** files with any previously-owner-signed pair, and no verifier detects it.\n\nCompaction makes this strictly worse in a way the analysis under-states: **it converts `classify_head`'s `n < signed_n` truncation alarm (`checkpoint.py:93`) from a live guard into a no-op for the pruned range.** Today, rolling back to an older, shorter-count head is caught *because the current spine is longer than that head's count*. Once shrinking is legitimate, replaying an old `(short-spine, low-count-head)` snapshot pair is **indistinguishable from the current state** — and the analysis's proposed remedy (store the floor in the SnapshotAnchor) does not help, because the attacker simply presents the old anchor carrying its old, lower floor. A fresh verifier (new mobile device, post-`--reset`, cold sync from an untrusted mirror) has no higher water mark to reject it with. The \"superseded_head_hash meta-chain\" only protects a verifier that *already holds* a higher accepted head. **DESIGN MUST specify a durable monotonic floor that lives OUTSIDE the compactable data** (per-verifier local state, e.g. each device persists its own last-accepted `last_seq`; and/or the head is delivered over an authenticated channel a mirror cannot roll back) — otherwise \"truncation-forged-as-snapshot\" is closed only for warm, same-host verifiers.\n\n**C2 — The `entries[:signed_n]` slice is an interior-mutation forgery surface post-snapshot, not just a mis-slice.** `classify_head` slices `entries[:signed_n]` and re-verifies (`checkpoint.py:96`). The analysis notes this must be reconciled with `base_count`. Add the security framing: once `signed_n` is redefined as `entry_count - base_count`, the slice covers only the LIVE window — so a verifier **no longer re-hashes the pruned prefix at all**; its integrity rests solely on `base_prev_hash`/`snapshot_hash`. If that field is only the boundary link (not a Merkle commitment over the prefix, as risk #6 warns), then a prefix record altered *before* archival is unprovable — and there is no live re-hash to catch it. The Merkle-commit requirement is listed as a \"risk,\" but it should be an **invariant**: the snapshot's committed digest must bind every pruned record, because the slice change removes the only other check.\n\n## (d) Crash / concurrency scenarios the durability analysis missed\n\n**D1 — Holding the append lock across the O(file) compaction blocks a kill-switch PANIC append.** The durability analysis says compaction \"must acquire the same `spine_lock` and flock for the duration of any tip-affecting mutation and swap.\" Taken literally on a 93 MB spine, a naive implementation holds the exclusive lock across the whole rebuild — during which `KillSwitch.engage → store.append` (`killswitch.py:42`), `session.py` panic append (`session.py:151`... `bridge/daemon.py:151`), and every governor write **block**, so the mesh is not halted while compaction runs. Given the constitution weights a lost/late panic as the worst outcome, the design must state: the heavy copy of immutable old records runs **lock-free** (they never move); only the final `re-read tip → seal active → publish manifest+head` critical section takes the lock, and that section must be bounded (small). This ordering constraint is absent.\n\n**D2 — Live-reader vs. unlink race across the manifest indirection (no crash needed).** All the crash scenarios cover *crash timing*, but not a running reader. A lock-free reader (`verify()`, `iter_records(-1)`) that (1) reads the manifest, then (2) opens the segments it names, races a compaction that swaps the manifest and unlinks old segments **between** steps 1 and 2 → `ENOENT` mid-read. With a single file, `os.replace` is safe (the reader holds the old inode's fd to completion); with a manifest+N-segments indirection it is not, unless readers snapshot **all** segment fds immediately after reading the manifest, and unlink is deferred behind a reader grace/refcount. The \"replace manifest → dirfsync → THEN unlink\" ordering (scenario 6) protects crash-durability but not this live race.\n\n**D3 — `os.replace`-based cutover races cross-process appends by construction (correctly identified, but the consequence is under-propagated).** The concurrency section correctly notes flock binds to the inode, not the path, so an appender holding an old `a+b` fd keeps writing to the unlinked old inode and loses an ack'd record. This must be lifted from prose into a **hard requirement**: the append lock must move to a path-stable lockfile (`spine.lock`) that BOTH append and compaction flock *before* resolving the active segment, and appends must re-validate dev/ino under the lock. Every one of the ~15 `store.append` call sites inherits this; it is the single highest-risk durability change and should be stated as a precondition, not a mitigation.\n\n## The 3–5 questions the DESIGN phase must answer\n\n1. **Where does the durable, non-replayable anti-rollback floor live?** (C1) It cannot be only inside the spine/anchor. Is it per-verifier local state, an authenticated head-delivery channel, or a monotonic counter each device persists? Without an answer, compaction reopens undetectable truncation for fresh/cold verifiers.\n2. **What is the exact `SignedChainHead` schema migration + version-negotiation story?** (A1) Given `extra=\"forbid\"` and the two head-consumers failing differently (swallow vs. crash), how do old mobile clients / `sigil verify` behave against a v2 head, and is there a flag day?\n3. **What is the atomic multi-file commit protocol, and which critical section holds the lock?** (D1/D2/D3/cutover) One manifest as the sole linearization point, path-stable lockfile, lock-free heavy copy, bounded locked cutover, reader-grace before unlink, seal-time torn-tail truncate on the sealed segment (A2). Spell out the fsync/replace ordering across {segments, manifest, head} as one crash-consistent unit.\n4. **Exactly which record classes are un-prunable state-bearers, and how is each folded into the signed snapshot?** Consolidated list from all four analyses **plus my additions**: per-device nonce high-water (`envelope.py`), newest `warden_checkpoint` per pubkey (`cli.py`), `account.create` creation-cap counts (`actor_scope.py`/`actor.py`), latest device authorize/revoke per key (`mesh/registry.py`), kill-switch latch (`killswitch.py`), standing promotion grants (`promotion.py`), unresolved queued approvals (`approvals.py`), open archivist supersession chain + promotion-key ledger (`revise.py`), **and the open threads/commitments/contradictions + their source_seq citations served by `queries.py` (B1)**. The snapshot payload schema must carry all of them; the design must enumerate it exhaustively.\n5. **What is the honest contract for a resolve of a pruned seq?** `get(pruned_seq)` today returns `None`, which UI/bridge (`bridge/server.py:322`, `ui/server.py:161`) turn into `404 \"no such record — not fabricated\"` and `episodic_range` turns into a silent empty/later window. Does the design add an explicit `\"compacted below seq N (see snapshot X)\"` response so a pruned range is *provably archived*, not *indistinguishable from never-existed*? The \"cite its seq / tamper-evident atom\" contract requires a positive signal, not a 404.\n\nFiles verified: `/home/kali/sigil/sigil/spine/store.py`, `/home/kali/sigil/sigil/reuse/chain.py`, `/home/kali/sigil/sigil/spine/checkpoint.py`, `/home/kali/sigil/sigil/spine/tail.py`, `/home/kali/sigil/sigil/spine/verify.py`, `/home/kali/sigil/sigil/spine/models.py`, `/home/kali/sigil/sigil/reuse/models.py`, `/home/kali/sigil/sigil/consolidate/queries.py`, `/home/kali/sigil/sigil/consolidate/gate.py`, `/home/kali/sigil/sigil/ingest/{git,transcript,subagents,docs}.py`.",
    "counts": {
      "invariants": 19,
      "consumers": 34,
      "consumers_reading_raw_file": 2,
      "consumers_assuming_contiguity": 1,
      "consumers_full_scan": 23,
      "consumers_need_old_seq": 9
    }
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Map"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Critique"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "invariants",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "a45e4768bb7ec2a4a",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784466671566,
      "queuedAt": 1784466665598,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "SIGIL is a local-first, offense-free personal AI orchestrator. Its source of truth is an append-only,\nhash-chained, Ed25519-signed JSONL \"spine\" at ~/.sigil/spine/spine.jsonl (currently 93MB / 43,412 records\nand growing). The goal of the WORK THIS ANALYSIS FEEDS is to add snapshot / compaction / segment-rotation\nso the spine does not grow without bound — WITHOUT weakening its tamper-evidence or ap…",
      "lastProgressAt": 1784466953469,
      "tokens": 59910,
      "toolCalls": 12,
      "durationMs": 281903,
      "resultPreview": "{\"invariants\":[{\"name\":\"chain-anchored-from-genesis\",\"statement\":\"The FIRST entry of whatever set verify_chain / verify_head receives must have prev_hash == _GENESIS_PREV ('0'*64). verify_chain seeds prev=_GENESIS_PREV and rejects entries[0] if e.prev_hash != prev. Note the subtlety: verify_chain does NOT require entries[0].seq == 0 — only prev_hash==genesis + contiguity — so a rebased chain start…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "tamper-conflict",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "a2a2529412cb63c71",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784466671966,
      "queuedAt": 1784466665599,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "A front-prune trips THREE independent guards, all in the re…",
      "promptPreview": "SIGIL is a local-first, offense-free personal AI orchestrator. Its source of truth is an append-only,\nhash-chained, Ed25519-signed JSONL \"spine\" at ~/.sigil/spine/spine.jsonl (currently 93MB / 43,412 records\nand growing). The goal of the WORK THIS ANALYSIS FEEDS is to add snapshot / compaction / segment-rotation\nso the spine does not grow without bound — WITHOUT weakening its tamper-evidence or ap…",
      "lastProgressAt": 1784466844200,
      "tokens": 48247,
      "toolCalls": 9,
      "durationMs": 172234,
      "resultPreview": "{\"prune_vs_rollback\":\"A front-prune trips THREE independent guards, all in the read/verify path:\\n\\n(1) GENESIS PREFIX — verify_chain (chain.py:38,40): it seeds `prev = _GENESIS_PREV` (\\\"0\\\"*64) and the very first entry is checked `if e.prev_hash != prev: return False \\\"chain break ... prev_hash mismatch (entry deleted/reordered)\\\"`. After pruning records 0..K-1, the new first retained record (seq…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "durability",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "a0496259f92f7aa3e",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784466671319,
      "queuedAt": 1784466665599,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "append() (store.py:141-194) makes these durability guarante…",
      "promptPreview": "SIGIL is a local-first, offense-free personal AI orchestrator. Its source of truth is an append-only,\nhash-chained, Ed25519-signed JSONL \"spine\" at ~/.sigil/spine/spine.jsonl (currently 93MB / 43,412 records\nand growing). The goal of the WORK THIS ANALYSIS FEEDS is to add snapshot / compaction / segment-rotation\nso the spine does not grow without bound — WITHOUT weakening its tamper-evidence or ap…",
      "lastProgressAt": 1784466881997,
      "tokens": 45410,
      "toolCalls": 6,
      "durationMs": 210677,
      "resultPreview": "{\"append_guarantees\":\"append() (store.py:141-194) makes these durability guarantees, in order, for every call that RETURNS a seq (= an acked record):\\n\\n1. Serialization frame: the whole read-tip→write runs under `spine_lock(self.path)` (store.py:154) and, inside it, under the file's `fcntl.flock(fileno, LOCK_EX)` (store.py:158-160). flock failure is SWALLOWED (`except OSError: pass`, store.py:161…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "consumers:graph+vectors+consolidate",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "a81ecd4aec21da800",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784466671143,
      "queuedAt": 1784466665637,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "SIGIL is a local-first, offense-free personal AI orchestrator. Its source of truth is an append-only,\nhash-chained, Ed25519-signed JSONL \"spine\" at ~/.sigil/spine/spine.jsonl (currently 93MB / 43,412 records\nand growing). The goal of the WORK THIS ANALYSIS FEEDS is to add snapshot / compaction / segment-rotation\nso the spine does not grow without bound — WITHOUT weakening its tamper-evidence or ap…",
      "lastProgressAt": 1784466829001,
      "tokens": 51250,
      "toolCalls": 9,
      "durationMs": 157858,
      "resultPreview": "{\"consumers\":[{\"file\":\"sigil/graph/rebuild.py\",\"spine_calls\":\"SpineStore() constructed L120; store.iter_records() FULL scan with no since_seq in _accumulate L55; store.next_seq read L171 (spine_head_seq). Reads r.seq/r.entry_hash/r.kind/r.payload/r.ts/r.actor only. Does NOT call get(). Writes a kuzu db + STAGING/manifest.json (L177) and atomically swaps CURRENT — none of that is the spine.\",\"reads…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "consumers:cli+audit+dashboard+mcp",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "a100c4c587059ad69",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784466670802,
      "queuedAt": 1784466665638,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "SIGIL is a local-first, offense-free personal AI orchestrator. Its source of truth is an append-only,\nhash-chained, Ed25519-signed JSONL \"spine\" at ~/.sigil/spine/spine.jsonl (currently 93MB / 43,412 records\nand growing). The goal of the WORK THIS ANALYSIS FEEDS is to add snapshot / compaction / segment-rotation\nso the spine does not grow without bound — WITHOUT weakening its tamper-evidence or ap…",
      "lastProgressAt": 1784466824476,
      "tokens": 60815,
      "toolCalls": 9,
      "durationMs": 153672,
      "resultPreview": "{\"consumers\":[{\"file\":\"sigil/cli.py\",\"spine_calls\":\"Constructs SpineStore in nearly every cmd_*. store.count() (L61 cmd_ingest, L467 cmd_status); store.next_seq (L467 cmd_status); store.append(kind=\\\"warden_checkpoint\\\",...) (L260 cmd_warden_anchor_set); store.verify() (L274 warden_anchor_get, L456 cmd_verify, L465 cmd_status); store.iter_records() full scan (L283 warden_anchor_get); store.get(seq…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "consumers:governor+gates",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "acd7986c639cc5059",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784466670917,
      "queuedAt": 1784466665638,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "SIGIL is a local-first, offense-free personal AI orchestrator. Its source of truth is an append-only,\nhash-chained, Ed25519-signed JSONL \"spine\" at ~/.sigil/spine/spine.jsonl (currently 93MB / 43,412 records\nand growing). The goal of the WORK THIS ANALYSIS FEEDS is to add snapshot / compaction / segment-rotation\nso the spine does not grow without bound — WITHOUT weakening its tamper-evidence or ap…",
      "lastProgressAt": 1784466759318,
      "tokens": 37620,
      "toolCalls": 7,
      "durationMs": 88401,
      "resultPreview": "{\"consumers\":[{\"file\":\"/home/kali/sigil/sigil/governor/killswitch.py\",\"spine_calls\":\"store.append(kind='event',...) at L42 (engage) and L48 (release); store.iter_records() full-scan at L75 (_scan_engaged); ALSO reaches past the API into store.path directly — Path(self.store.path).resolve() at L55 and self.store.path.stat().st_size at L57 — using the raw file SIZE as a cache key.\",\"reads_raw_file\":…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "consumers:bridge+ui+mesh",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "a1daf5adc55d2f09d",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784466671365,
      "queuedAt": 1784466665638,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "SIGIL is a local-first, offense-free personal AI orchestrator. Its source of truth is an append-only,\nhash-chained, Ed25519-signed JSONL \"spine\" at ~/.sigil/spine/spine.jsonl (currently 93MB / 43,412 records\nand growing). The goal of the WORK THIS ANALYSIS FEEDS is to add snapshot / compaction / segment-rotation\nso the spine does not grow without bound — WITHOUT weakening its tamper-evidence or ap…",
      "lastProgressAt": 1784466793529,
      "tokens": 70801,
      "toolCalls": 11,
      "durationMs": 122163,
      "resultPreview": "{\"consumers\":[{\"file\":\"sigil/bridge/daemon.py\",\"spine_calls\":\"store.iter_records() full scan in submit_device_approval (L87); store.tail(_DEDUP_WINDOW=2048) in submit_arm_request (L136); store.append(...) at L91 (device approval, supersedes_id=tgt), L141 (arm request), L151 (panic). pending()/recall() delegate to agents.approvals.pending / perception.recall (not in scope) which themselves scan. ne…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "consumers:agents",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "a1f759613955dd5ba",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784466671108,
      "queuedAt": 1784466665638,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "SIGIL is a local-first, offense-free personal AI orchestrator. Its source of truth is an append-only,\nhash-chained, Ed25519-signed JSONL \"spine\" at ~/.sigil/spine/spine.jsonl (currently 93MB / 43,412 records\nand growing). The goal of the WORK THIS ANALYSIS FEEDS is to add snapshot / compaction / segment-rotation\nso the spine does not grow without bound — WITHOUT weakening its tamper-evidence or ap…",
      "lastProgressAt": 1784466822202,
      "tokens": 63459,
      "toolCalls": 11,
      "durationMs": 151094,
      "resultPreview": "{\"consumers\":[{\"file\":\"/home/kali/sigil/sigil/agents/base.py\",\"spine_calls\":\"SpineStore.append() at L83 (AUTO), L88 (QUEUE), L94 (DENY/refusal) — the sole spine surface for all agents; seq return stored opaquely in AgentResult.applied/queued (L86,91).\",\"reads_raw_file\":false,\"assumes_seq_is_line_or_zero_based\":false,\"does_full_scan\":false,\"appends\":true,\"depends_on_old_seq_resolving\":false,\"risk_n…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "consumers:perception+gesture+config",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "aa608359add9ef97e",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784466671338,
      "queuedAt": 1784466665639,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "SIGIL is a local-first, offense-free personal AI orchestrator. Its source of truth is an append-only,\nhash-chained, Ed25519-signed JSONL \"spine\" at ~/.sigil/spine/spine.jsonl (currently 93MB / 43,412 records\nand growing). The goal of the WORK THIS ANALYSIS FEEDS is to add snapshot / compaction / segment-rotation\nso the spine does not grow without bound — WITHOUT weakening its tamper-evidence or ap…",
      "lastProgressAt": 1784466749026,
      "tokens": 46074,
      "toolCalls": 6,
      "durationMs": 77688,
      "resultPreview": "{\"consumers\":[{\"file\":\"/home/kali/sigil/sigil/perception/egress.py\",\"spine_calls\":\"store.get(seq) at L34 (resolves a caller-supplied egress-request seq); store.iter_records(since_seq=seq) at L39 (forward scan from that seq looking for a matching approval record). Also mesh.authorized_devices(store, ...) at L38 and verify_approval at L42, both API-level.\",\"reads_raw_file\":false,\"assumes_seq_is_line…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "completeness-critic",
      "phaseIndex": 2,
      "phaseTitle": "Critique",
      "agentId": "a4e34289cd9e12256",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784466965775,
      "queuedAt": 1784466962171,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/kali/sigil && echo \"=== ingest spine usage ===\" &&…",
      "promptPreview": "SIGIL is a local-first, offense-free personal AI orchestrator. Its source of truth is an append-only,\nhash-chained, Ed25519-signed JSONL \"spine\" at ~/.sigil/spine/spine.jsonl (currently 93MB / 43,412 records\nand growing). The goal of the WORK THIS ANALYSIS FEEDS is to add snapshot / compaction / segment-rotation\nso the spine does not grow without bound — WITHOUT weakening its tamper-evidence or ap…",
      "lastProgressAt": 1784467327134,
      "tokens": 105563,
      "toolCalls": 13,
      "durationMs": 361359,
      "resultPreview": "I have enough verified detail. Writing the critique now.\n\n---\n\n# COMPLETENESS CRITIC — findings\n\nI re-read `store.py`, `chain.py`, `checkpoint.py`, `tail.py`, `verify.py`, `models.py` (both), and re-grepped `sigil/` for every spine-touch. The four analyses are strong and mostly correct. Below is only what is **missing or wrong**, grouped as requested.\n\n## (a) Invariants missing from the list\n\n**A1…"
    }
  ],
  "totalTokens": 589149,
  "totalToolCalls": 93
}