{
  "summary": "Extract exact current scan logic + associative fold spec for each hard-prune state bearer",
  "agentCount": 12,
  "logs": [],
  "result": {
    "specs": [
      {
        "bearer": "sigil/bridge/envelope.py device_nonce_highwater(store, device_pubkey) -> int — per-device monotonic nonce highwater used as the effectful-request replay floor (consume() rejects nonce <= highwater). Sole producer of the receipts it scans is record_receipt() in the same file.",
        "file_line": "/home/kali/sigil/sigil/bridge/envelope.py:88-104 (def at 88; the full-scan loop body is lines 92-103). Depends on: record_receipt() lines 79-85 (payload shape) and RECEIPT_SIGNAL=\"mesh.request\" line 34; SpineStore.iter_records() in sigil/spine/store.py:727-766 (full scan, seq order, malformed-line skip); SpineRecord in sigil/spine/models.py:25-50 (payload coerced None->{} in from_dict).",
        "current_logic": "Exact step-by-step of the current full scan:\n1. hi = -1 (the floor / empty-state return; a first nonce of 0 is > -1 so it is accepted).\n2. for r in store.iter_records(): — DEFAULT since_seq=-1, so this is a FULL scan from genesis, in ascending seq order, ACROSS ALL segments as one contiguous chain. Torn/malformed lines are skipped by iter_records BELOW this loop (they never reach the fold).\n3. p = r.payload — SpineRecord.from_dict sets payload = d.get(\"payload\") or {}, so p is ALWAYS a dict; p.get(...) can never raise.\n4. Filter predicate (PAYLOAD-ONLY, both must hold): p.get(\"signal\") == \"mesh.request\"  AND  p.get(\"device\") == device_pubkey. NOTE: the scan does NOT test r.kind / r.source / r.actor / r.seq / r.ts at all — only these two payload keys. Non-matching records are silently ignored.\n5. nonce_raw = p.get(\"nonce\"); if nonce_raw is None: continue (skip — receipt with no recorded nonce).\n6. try: n = int(nonce_raw) — the accumulated quantity is the INT-NORMALIZED nonce, not the raw value. except (TypeError, ValueError): continue (skip). OverflowError is NOT in the except tuple, so int(float('inf')) would PROPAGATE (crash) — unlike consume() which explicitly catches it.\n7. if n > hi: hi = n  (strict >; equal values do NOT update — no tie-break needed).\n8. return hi.\nReturns the max int-normalized nonce over all matching receipts, or -1 if none. It is a per-device monotone max (a join-semilattice), order-independent.",
        "record_shapes": "Receipts are appended ONLY by record_receipt() (envelope.py:79-85) via store.append(kind=\"event\", source=\"mesh\", actor=\"DEVICE\", payload={\"signal\":\"mesh.request\", \"device\":<core.device str>, \"action\":<core.action str>, \"nonce\":<core.nonce>, \"ts\":<core.ts>, \"tier\":\"A0\", \"decision\":\"auto\"}).\nRecord attributes the SCAN actually reads: ONLY r.payload. Within payload it reads exactly three keys: payload[\"signal\"] (compared == \"mesh.request\"), payload[\"device\"] (compared == device_pubkey, a str), payload[\"nonce\"] (int-cast). It does NOT read seq/scope/kind/source/actor/parent_id/supersedes_id/ts/cert_digest. nonce as written by consume() is a validated non-negative int (consume() rejects None/NaN/inf/negative/non-castable BEFORE receipting), but the scan does not assume this — it re-normalizes and tolerates junk. The signal string \"mesh.request\" (RECEIPT_SIGNAL, line 34) is produced nowhere else in sigil/ (grep-confirmed), so payload.sign al==\"mesh.request\" uniquely selects these receipts.",
        "fold_state": "A per-device highwater MAP:  device_nonce_highwater: dict[str, int]  =  { device_pubkey : max_int_nonce }.  Type: dict from device-pubkey string to a Python int. Store the whole map (not just one device) because a pruned prefix may hold receipts for many devices and any device may be queried. A device whose max over the prefix is -1 (no valid nonce) MAY be omitted — the consumer's .get(device, -1) default makes absent == -1, byte-identical. CRITICAL: the stored value per device is the max of the INT-NORMALIZED nonces (int(nonce_raw)), NOT the max of raw nonces — the scan compares n=int(...), so raw 3.9 and 3 both collapse to 3; storing a raw 3.9 would diverge. Identity/bottom element per device is -1. The snapshot head already carries base_seq (the prune boundary K) and base_count/entry_count (floor.py) which the consumer needs to bound the live tail.",
        "fold_op": "Join = pointwise integer MAX over the device map. Identity element = {} (empty map; every device defaults to -1). combine(A, B): for every key d in A∪B, out[d] = max(A.get(d,-1), B.get(d,-1)). Per-record fold step (identical filter+normalization to the scan): given map m and live record r with p=r.payload, IF p.get(\"signal\")==\"mesh.request\" AND p.get(\"device\")==d0: nonce_raw=p.get(\"nonce\"); if nonce_raw is None -> no-op; else try n=int(nonce_raw) except (TypeError,ValueError) -> no-op; else m[d0]=max(m.get(d0,-1), n). Reproduction of the scan EXACTLY: load(snapshot).device_nonce_highwater is the folded map over pruned [0..K); then fold each live record [K..T] with the step above; the query answer is folded_map.get(device_pubkey, -1). Because max is commutative+associative+idempotent with bottom -1, max(max over [0..K), max over [K..T]) == max over [0..T] == the current full scan's hi, for ANY K and ANY ordering.",
        "consumer_rewrite": "Keep the public signature and return type EXACTLY: def device_nonce_highwater(store: SpineStore, device_pubkey: str) -> int. Body:\n  st = SnapshotState.load(store)                       # folded state of pruned [0..base_seq)\n  hi = st.device_nonce_highwater.get(device_pubkey, -1)  # seed = per-device highwater of the prefix (-1 if absent)\n  base = st.base_seq                                    # prune boundary K; 0 for the empty Slice-C snapshot\n  for r in store.iter_records(since_seq=base - 1):      # LIVE records with seq >= base; base-1 == -1 => full scan\n      p = r.payload\n      if p.get(\"signal\") == RECEIPT_SIGNAL and p.get(\"device\") == device_pubkey:\n          nonce_raw = p.get(\"nonce\")\n          if nonce_raw is None:\n              continue\n          try:\n              n = int(nonce_raw)\n          except (TypeError, ValueError):               # NOTE: keep exactly this tuple — do NOT add OverflowError\n              continue\n          if n > hi:\n              hi = n\n  return hi\nSlice C ships an EMPTY snapshot: base_seq==0 -> since_seq=-1 -> iter_records() full scan and st map empty -> hi seeds at -1 -> byte-identical to the current loop. consume() at line 131 calls this unchanged. The snapshot BUILDER that folds [0..K) at prune time must apply the IDENTICAL payload-only filter + None-skip + int()-cast + (TypeError,ValueError)-skip so the persisted per-device max equals what the scan would have computed over that prefix.",
        "edge_cases": "Ordering: NONE — max is commutative/associative/idempotent, so the fold is fully order-insensitive; the prune boundary is trivially safe and no seq ordering must be preserved. Tie-break: NONE — strict n>hi, equal nonces don't change hi. Verified-only vs any: the scan does NOT gate on verification/tier/kind/source/actor — it filters ONLY on payload.signal==\"mesh.request\" and payload.device; the builder must NOT add kind/source/actor filters (even though the only producer uses kind=\"event\"/source=\"mesh\"/actor=\"DEVICE\") or it will diverge if any record ever carries that payload. Normalization must match byte-for-byte: (a) None nonce -> skip; (b) int(nonce_raw) semantics — float truncates toward zero (int(3.9)==3), numeric str parsed (int(\" 42 \")==42), bool True->1 (bool is an int subclass), non-numeric str/list/dict -> ValueError/TypeError -> skip; store the INT-normalized max, never raw; (c) NaN -> int() raises ValueError -> caught -> skip; inf -> int() raises OverflowError -> NOT caught -> propagates (the builder must replicate this, i.e. catch ONLY (TypeError,ValueError)); in practice consume() rejects None/NaN/inf/negative before receipting so these never appear. Negative nonce: int(-5) > -1 is False -> never raises the floor (floor stays -1); consume() rejects nonce<0 anyway. Empty state: returns -1; absent device in map -> -1 via .get default. payload is always a dict (from_dict coerces None->{}), so p.get never crashes. Malformed/torn spine lines are dropped inside iter_records (below the fold), so the built-prefix and the live tail both see only well-formed records.",
        "associativity_risk": "TRULY associative / split-exact. This is a genuine join-semilattice: per-device integer MAX, with identity/bottom -1, which is commutative, associative, and idempotent. Therefore fold(fold(empty,[0..K)),[K..T]) == fold(empty,[0..T]) == the current full scan for ANY prune point K and ANY record ordering (including reordering across the prune boundary). No order sensitivity, no last-write-wins sequencing, no engage/release latch — the simplest safe case. To stay EXACT the snapshot must carry only: (1) the per-device map of the max INT-NORMALIZED nonce (int(nonce), NOT raw — two distinct raw nonces like 3.9 and 3 collapse to the same 3 under the scan's int() cast, so persisting raw would make the fold != scan); devices with max -1 may be omitted. (2) base_seq (the prune boundary K), so the consumer folds live records with seq >= base_seq (since_seq=base_seq-1). Nothing else (no ts, no ordering info) is needed because max discards everything but the winning integer. Ordering across the prune boundary is preserved for free since the operation is order-free. Slice C's empty snapshot (base_seq=0, empty map) reduces the rewrite to since_seq=-1 full scan seeded at -1 == byte-identical to today."
      },
      {
        "bearer": "KillSwitch._scan_engaged — the governor kill-switch latch (a single boolean that halts the agent mesh while leaving A0 observe alive). Called by KillSwitch.is_engaged() (which caches the verdict keyed on the store's rotation-aware change_token + trusted_pubkey) and consulted on every governor dispatch decision.",
        "file_line": "/home/kali/sigil/sigil/governor/killswitch.py:70-83 (def _scan_engaged at :70; scan loop `for r in self.store.iter_records()` at :74)",
        "current_logic": "Exact step-by-step of _scan_engaged(self) -> bool:\n1. `engaged = False` — initial/identity value (line 73).\n2. `for r in self.store.iter_records():` — iterate EVERY record from genesis (since_seq=-1) in strictly ascending seq order, across all sealed+active segments as one contiguous chain. Malformed/torn lines are SKIPPED by iter_records; a missing referenced segment RAISES SpineError (fail-closed, never a short chain). No kind/source/actor/scope filter is applied by the scan.\n3. `p = r.payload` (a dict; SpineRecord.from_dict coerces a missing/None payload to {}).\n4. `if p.get(\"signal\") != SIGNAL: continue` — SIGNAL == \"governor.killswitch\". Any record whose payload signal != this exact string is ignored entirely, regardless of kind/source/actor.\n5. `state = p.get(\"state\")`.\n6. `if state == \"engaged\": engaged = True` — honor ANY engage unconditionally (no signature check); halting is the fail-safe direction.\n7. `elif state == \"released\" and verify_signed(p, _CORE, self.trusted_pubkey): engaged = False` — a release un-halts ONLY if it is an owner-signed release that verifies. _CORE == (\"signal\",\"state\"). verify_signed returns True iff: trusted_pubkey is truthy AND p[\"sig\"] is truthy AND p[\"pubkey\"] == self.trusted_pubkey AND Ed25519 verify_one(trusted_pubkey, canonical_json({\"signal\": p.get(\"signal\"), \"state\": p.get(\"state\")}), p[\"sig\"]) passes — i.e. the sig is over canonical_json({\"signal\":\"governor.killswitch\",\"state\":\"released\"}).\n8. IMPLICIT ELSE (no-op): a `released` record that FAILS verification, or any other state value, or state None, leaves `engaged` UNCHANGED. (The fail-safe \"an unverified release can't revive a halted mesh\" is an emergent property of this no-op — NOT an explicit force-True. The task's \"ambiguous->True\" is a loose paraphrase; the code force-Trues nothing on release.)\n9. `return engaged` — the latch value after the last state-changing record (highest seq) wins.\nNet: result = the effect of the highest-seq record among {engage (any) OR verified-release}; True if that record is an engage, False if a verified release; False if no such record exists.",
        "record_shapes": "SpineRecord (sigil/spine/models.py): fields seq:int, scope, kind, source, actor, payload:dict, parent_id, supersedes_id, ts, cert_digest, prev_hash, entry_hash. The scan reads ONLY r.payload and relies on iter_records for ascending-seq ordering; it does not read r.seq/kind/source/actor/supersedes_id itself. Engage/release records are appended (KillSwitch.engage/release) as kind=\"event\", source=\"governor\", actor=\"WARDEN\" — but the scan does NOT filter on these; the sole selector is the payload. Payload fields the scan depends on: \"signal\" (must == \"governor.killswitch\"); \"state\" (\"engaged\" | \"released\"); and for releases \"sig\" (Ed25519 sig, base64) and \"pubkey\" (must == trusted_pubkey). Payload also carries by/reason/tier/decision but those are irrelevant to the scan. Superseded records are still processed (iter_records does not collapse supersedes).",
        "fold_state": "A single boolean: killswitch_engaged: bool. Identity/empty element = False (matches the initial `engaged = False`). This is exact because the scan is a 2-value latch whose forward evolution depends only on the current boolean, not on how that boolean was reached — so the prefix [0..K) collapses losslessly to its resulting boolean. (No need to distinguish \"no transition\" from \"explicit verified-release-to-False\": both are False and both behave identically under forward folding.) CAVEAT: the boolean is a function of the trusted_pubkey used to fold, so the snapshot MUST also pin/record the trusted_pubkey it was computed under — see associativity_risk.",
        "fold_op": "The \"last-write-wins override\" monoid on {ID, SET_TRUE, SET_FALSE} under sequential application to a boolean, identity=False. Per live record r (in seq order): let p=r.payload; if p.get(\"signal\") != \"governor.killswitch\" → ID (no change); elif p.get(\"state\")==\"engaged\" → SET_TRUE (engaged=True); elif p.get(\"state\")==\"released\" and verify_signed(p, (\"signal\",\"state\"), trusted_pubkey) → SET_FALSE (engaged=False); else → ID. Combine = function composition (later record's non-ID effect overrides earlier). load(snapshot).killswitch_engaged gives the folded boolean of [0..K); folding the live records [K..T] forward with this op yields the same boolean the full scan produces, because fold(g_prefix(False), [K..T]) == g_live(g_prefix(False)) == fold(empty, [0..T]).",
        "consumer_rewrite": "Keep the signature/return `_scan_engaged(self) -> bool`. Replace the loop body's seed and range:\n  st = SnapshotState.load(self.store)          # or the store's already-loaded snapshot head\n  engaged = st.killswitch_engaged              # bool; False under an empty/identity snapshot\n  base_seq = st.base_seq                       # first LIVE seq; pruned = [0..base_seq)\n  for r in self.store.iter_records(since_seq=base_seq - 1):   # yields seq >= base_seq\n      p = r.payload\n      if p.get(\"signal\") != SIGNAL:\n          continue\n      state = p.get(\"state\")\n      if state == \"engaged\":\n          engaged = True\n      elif state == \"released\" and verify_signed(p, _CORE, self.trusted_pubkey):\n          engaged = False\n  return engaged\nUnder Slice C's empty snapshot: killswitch_engaged=False, base_seq=0 → iter_records(since_seq=-1) → the identical genesis-to-tip scan seeded at False → byte-identical to current. is_engaged()'s change_token cache wraps this unchanged.",
        "edge_cases": "- Ordering: strictly ascending seq (iter_records guarantee); the HIGHEST-seq effective record wins. No ties possible (seq is unique+monotonic). Ordering across the prune boundary IS preserved: the prefix is folded in seq order into the boolean, then live records continue in seq order from base_seq.\n- Unverified release is a NO-OP (leaves latch as-is), NOT forced True. Do not add a force-True branch; matching the code means an unverified release must not change the folded boolean.\n- Empty/no-signal spine → False. Empty snapshot → False (identity).\n- Selector is payload.signal ONLY — do NOT add kind/source/actor filters (the scan has none). A stray non-governor record that happens to carry signal==\"governor.killswitch\" would be honored by both scan and fold identically.\n- Superseded records are still counted (no supersedes collapse) — the fold must not dedup by supersedes_id.\n- Malformed/torn lines: iter_records skips them; since the consumer reuses iter_records, behavior is identical. A missing referenced segment RAISES (fail-closed) in both.\n- verify_signed re-canonicalizes {\"signal\",\"state\"} from the record's own fields; only \"released\" reaches that branch so the signed message is always canonical_json({\"signal\":\"governor.killswitch\",\"state\":\"released\"}). Fold-time verification of pruned releases must use the exact same verify_signed(p, (\"signal\",\"state\"), trusted_pubkey).",
        "associativity_risk": "TRULY associative / split-exact: YES. The per-record effects live in {ID, const_True, const_False}, which is closed under composition (a later constant overrides; ID passes through) — the classic last-write-wins latch monoid, identity False. A single boolean snapshot is exact (proof: the full result is g_live(g_prefix(False)); folding the stored boolean b0=g_prefix(False) forward computes exactly g_live(b0)). The \"no transition\" vs \"verified-False\" distinction is lossy-but-safe: both are False and compose identically forward.\nTHE ONE REAL RISK — trust-root parameterization: the fold's SET_FALSE branch calls verify_signed(..., trusted_pubkey), so whether a prefix release un-halts depends on trusted_pubkey. Split-exactness fold(fold(empty,[0..K),pk),[K..T],pk)==fold(empty,[0..T],pk) holds ONLY when the same pk folds both sides. Therefore SnapshotState MUST record the trusted_pubkey the killswitch boolean was folded under, and the consumer must use the snapshot only when its trusted_pubkey matches self.trusted_pubkey (mirror the existing is_engaged cache, which already keys on trusted_pubkey and would otherwise leak a wrong verdict across trust roots); if the trust root differs, the prefix must be re-folded from live records, not trusted from the snapshot. Under Slice C (empty snapshot, identity False, base_seq=0) this is moot — no prefix releases are folded — so the rewired consumer is byte-identical regardless of pubkey."
      },
      {
        "bearer": "sigil/agents/actor_scope.py — ActorScope.creation_allowed(store, service, url=\"\"): the DELEGATE account-creation cap, a per-service-label AND per-origin monotonic count of applied `account.create` steps, enforced at both preview (actor.py:139) and execute (actor.py:247). Returns True while n < creation_cap (default 1).",
        "file_line": "/home/kali/sigil/sigil/agents/actor_scope.py:44-56 (the full scan is the for-loop at line 49; the OR-predicate is lines 51-55; return n < self.creation_cap at line 56). Helper _origin at lines 17-27.",
        "current_logic": "origin = _origin(url) if url else None  (line 47). Note: url=\"\" => origin=None by short-circuit; a non-empty but malformed/non-http url => _origin returns None => origin=None. n=0. Then `for r in store.iter_records()` — a FULL scan from genesis, ALL records, no since_seq, kind-agnostic (it never reads r.kind/r.seq/r.actor — only r.payload). For each record p=r.payload it increments n by 1 iff ALL of: (1) p.get(\"signal\")==\"web.actor.step\"; (2) p.get(\"step_kind\")==\"account.create\"; (3) p.get(\"status\")==\"applied\"; AND (4) the OR-predicate: p.get(\"service\")==service  OR  (origin is not None AND _origin(p.get(\"url\",\"\"))==origin). Each matching record contributes EXACTLY +1 (single `if`, inclusive OR — a record matching BOTH branches is still counted once). Returns n < self.creation_cap. No sorting, no tie-break, no last-write, no dedup — pure count-add. self.creation_cap is instance config (default 1), NOT read from the spine.",
        "record_shapes": "COUNTED record = the applied account.create step, appended ONLY at actor.py:256-260 in execute(): SpineRecord(kind=\"event\", source=\"agent\", actor=\"DELEGATE\", payload={\"signal\":\"web.actor.step\", \"status\":\"applied\", \"target_seq\":<int>, \"service\":<str>, \"step_kind\":<str e.g. \"account.create\">, \"url\":<str>, \"action_token\":<str>, \"result_ok\":<bool>, \"tier\":\"A1\", \"decision\":\"auto\", \"summary\":<str>}). The scan depends on payload keys ONLY: signal, step_kind, status, service, url. It does NOT read record.kind/seq/source/actor/ts/parent_id. EXCLUDED look-alikes: (a) the preview dispatch record (actor.py:177-183) carries signal=\"web.actor.step\" + service + step_kind + url but has NO \"status\" key => fails filter (3); (b) refusal/finding records (kind=\"refusal\"/\"finding\") lack signal==\"web.actor.step\" or status==\"applied\". _origin(u): urlsplit(u if \"//\" in u else \"https://\"+u); None unless scheme in {http,https} and hostname present and port in range; else f\"{scheme}://{hostname.lower()}:{port}\" with default port (80/443) made explicit. _origin(\"\")=None; _origin of a bad-port url=None. This is the ONLY producer of applied web.actor.step records (grep-confirmed).",
        "fold_state": "A PAIR-keyed multiset (Counter) — the histogram of applied account.create steps bucketed by the two dimensions the predicate discriminates on: created: dict[(service, canon_origin) -> int], where for each counted record service = p.get(\"service\") (raw string, may be None if absent) and canon_origin = _origin(p.get(\"url\",\"\")) (canonicalized via the SAME _origin helper, may be None for empty/malformed/non-http urls). Type/shape: Counter[Tuple[Optional[str], Optional[str]], int]; for JSON in the snapshot, serialize as a list of [service_or_null, origin_or_null, count] triples (a real origin is always \"scheme://host:port\" and never null/empty, so the null sentinel cannot collide with a real origin). Store the ORIGIN CANONICALIZED (not the raw url) so port-variant urls (\"https://h\" and \"https://h:443\") fold into ONE bucket and match the query's canonicalized origin. This is the ENTIRE sub-state — no seq, no ts, no ordering info is needed (see associativity_risk).",
        "fold_op": "Multiset sum (Counter addition), identity = empty Counter {}. fold(acc, record): if record passes filters (1)+(2)+(3) then acc[(p.get(\"service\"), _origin(p.get(\"url\",\"\")))] += 1; else acc unchanged. Combining two partial folds = elementwise count addition: (A ⊕ B)[k] = A[k] + B[k]. So SnapshotState.load(store).created gives the folded Counter of the pruned prefix [0..base_seq); the consumer builds Counter_live over live records [base_seq..T] with the identical bucketing; created ⊕ Counter_live == the Counter you would get by folding the whole log [0..T]. QUERY-TIME reduction reproduces the scan exactly: n = Σ over buckets (svc,org) of count where svc==service OR (origin is not None AND org==origin). Because each counted record lives in EXACTLY one bucket and each qualifying bucket is summed at most once, Σ equals the number of records the scan's single-`if` OR would have +1'd — inclusive-OR, counted-once, byte-identical.",
        "consumer_rewrite": "Keep signature `creation_allowed(self, store, service: str, url: str = \"\") -> bool` and return type unchanged. Body: origin = _origin(url) if url else None  (verbatim, line 47). st = SnapshotState.load(store)   # its .created Counter, empty in Slice C. base = getattr(store.head(), \"base_seq\", 0)  # 0 in Slice C. n = sum(cnt for (svc, org), cnt in st.created.items() if (svc == service) or (origin is not None and org == origin)). Then fold forward over the LIVE tail only, applying the EXISTING predicate verbatim so the tail code is unchanged behavior: `for r in store.iter_records(since_seq=base - 1):` (iter_records yields seq > since_seq, so base-1 gives seq >= base_seq — the same live window as checkpoint.py:116) then the current lines 50-55 body incrementing n. Finally `return n < self.creation_cap`. Under Slice C (empty snapshot, base_seq=0): st.created is empty => the pre-count contributes 0, since_seq=-1 => iter_records yields the whole log => this is line-for-line the current scan => BYTE-IDENTICAL. Do NOT re-count buckets in st AND records < base (mutual exclusivity holds because the live-tail filter starts at base_seq exactly).",
        "edge_cases": "ORDERING: fully order-insensitive — count-add is a commutative monoid, so records may be pruned/folded in any order and across the prune boundary with no effect; the snapshot needs NO seq/ts. TIE-BREAKS: none (no last-write, no max, no latch). INCLUSIVE-OR / COUNT-ONCE: a record whose service==S and whose origin==O is counted once in the scan and appears in one bucket => once in the fold. VERIFIED-ONLY: the scan does NOT check chain-verification, result_ok, tier, or decision — it counts every applied account.create regardless of result_ok true/false; the fold must too (do not filter on result_ok). NULL/MALFORMED URL: records with missing/empty/non-http url bucket under canon_origin=None and are reachable ONLY via the service branch (query origin, when non-None, is a real \"scheme://host:port\" that never equals None) — preserved by keying (service, None). PORT CANONICALIZATION: bucket by _origin(url), not raw url, so \"https://h\" ≡ \"https://h:443\" but \"https://h:8443\" stays distinct (port confusion refused) — mirrors the scan's _origin(record.url)==origin. MISSING service: p.get(\"service\") may be None; keying by None preserves that a query for a real string S won't match it (None==S is False) — matches p.get(\"service\")==service. PREVIEW EXCLUSION: preview records (no \"status\" key) must NOT enter the fold — filter (3) status==\"applied\" excludes them; the fold applies the identical triple filter. KIND-AGNOSTIC: the scan filters on payload only, so the fold/live-filter must be payload-driven (do not restrict to kind==\"event\"). EMPTY-STATE: base_seq=0/base_count=0 + empty created => folds the whole log => identical to today. QUERY url variants: an empty url => service-only match (origin=None); a malformed url => _origin=None => origin=None => also service-only — both reproduced by `origin = _origin(url) if url else None` plus the `origin is not None` guard.",
        "associativity_risk": "TRULY associative AND commutative — this is the count-add bearer class (a free commutative monoid over the pair-key). Because addition commutes, the fold is order-free: the snapshot must carry ONLY the {(service, canon_origin) -> count} histogram; it needs NO sequence numbers, timestamps, or tie-break metadata, and ordering across the prune boundary is irrelevant. The genuine correctness risk is NOT associativity but KEY GRANULARITY. The predicate is an inclusive OR over TWO independent dimensions (service label, canonical origin). If the snapshot keyed by a SINGLE flat dimension — {service: count} OR {origin: count} — the query could only test the dimension that was kept: a flat {service} map is blind to a pruned record that matches by ORIGIN under a DIFFERENT service label (attacker relabels the service, same origin), and a flat {origin} map is blind to a pruned record matching by SERVICE at a different origin. Either flat form UNDER-counts the OR across the prune boundary => n stays below cap when it should not => FATAL false-clean (the cap silently un-caps for pruned history). The PAIR key is mandatory and sufficient: it lets the query sum over EITHER dimension while, because each record occupies exactly one (service, origin) bucket, never double-counting the both-match case. Second sub-risk: the origin dimension MUST be stored canonicalized via _origin (not the raw url), or port-variant urls fail to fold together / fail to match the query's canonicalized origin. With the pair key + canonical origin, fold(fold(empty,[0..K)),[K..T]) == fold(empty,[0..T]) == the current full scan, exactly."
      },
      {
        "bearer": "sigil/mesh/registry.py:42 capability_map(store, trusted_pubkey=None) -> dict — the \"latest VERIFIED (owner-signed) capability per host_id\" map. Task-named at line 46 (the scan loop). Full-spine god-node consumer: {host_id: cap_dict} where cap_dict is the owner-signed descriptor from the HIGHEST-seq verified advertisement for that host_id.",
        "file_line": "/home/kali/sigil/sigil/mesh/registry.py:42-50 (function body; scan loop is lines 46-49). Helpers: verify_signed + signed_payload at /home/kali/sigil/sigil/governor/authn.py:19-35; owner_pubkey at /home/kali/sigil/sigil/governor/identity.py:21; _CAP_CORE + CAP_SIGNAL at registry.py:24-27; SpineStore.iter_records (seq-ordered, cross-segment) at /home/kali/sigil/sigil/spine/store.py:727-766; SpineRecord at /home/kali/sigil/sigil/spine/models.py:25-50.",
        "current_logic": "Step by step:\n1. tp = trusted_pubkey if trusted_pubkey is not None else owner_pubkey(). (owner_pubkey() reads KEYS_DIR/owner.pub; returns None if absent → tp falsy → nothing verifies → {}.)\n2. latest = {} (a plain dict).\n3. for r in store.iter_records():  # NO since_seq → full scan, ALL records, ascending seq order across every segment (sealed then active). iter_records skips torn/corrupt lines; yields strictly increasing seq.\n4.   p = r.payload  (SpineRecord.from_dict guarantees payload is a dict, never None: `d.get(\"payload\") or {}`).\n5.   FILTER predicate (both must hold): p.get(\"signal\") == CAP_SIGNAL  (== \"mesh.host_capability\")  AND  verify_signed(p, _CAP_CORE, tp).\n      verify_signed returns True iff: tp truthy; p[\"sig\"] truthy; p[\"pubkey\"] == tp; and Ed25519 verify_one(tp, canonical_json({k: p.get(k) for k in _CAP_CORE}), p[\"sig\"]) passes. Fail-closed on any miss.\n6.   On pass: latest[p.get(\"host_id\")] = {k: p.get(k) for k in _CAP_CORE if k != \"signal\"}  → OVERWRITES any prior entry for that host_id (last-write-wins in seq order).\n7. return latest.\nNET: for each host_id, the value is the extracted core-dict of the HIGHEST-seq record that both carries signal==\"mesh.host_capability\" and verifies as owner-signed over _CAP_CORE. Tie-break: seq monotonic + unique, so \"later record wins\", no other tiebreak. NOTE the scan reads ONLY r.payload (and implicitly r.seq via iteration order); it does NOT read/filter r.kind, r.source, r.actor, r.ts, r.parent_id, r.supersedes_id — a signed CAP record of ANY kind/source/actor is accepted.",
        "record_shapes": "Producer: advertise_capability (registry.py:32-39) appends kind=\"event\", source=\"mesh\", actor=\"OWNER\", but the scan does NOT filter on these — they are informational to this bearer.\nPayload fields the scan depends on (all inside r.payload):\n  • \"signal\": str, must equal CAP_SIGNAL = \"mesh.host_capability\" (the discriminator).\n  • The _CAP_CORE authenticated tuple = (\"signal\",\"host_id\",\"os\",\"has_screen\",\"has_camera\",\"has_gpu_vlm\",\"always_on\",\"has_hid_inject\",\"has_camera_stream\"). \"host_id\":str, \"os\":str, \"has_screen\"/\"has_camera\"/\"has_gpu_vlm\"/\"always_on\"/\"has_hid_inject\"/\"has_camera_stream\": bool (writer coerces via bool()/descriptor.get(...,False)). These are the signature-bound core.\n  • \"sig\": str|None (Ed25519 sig over canonical_json of the _CAP_CORE subdict).\n  • \"pubkey\": str|None (must equal tp).\n  • Also present but IGNORED by this scan: \"tier\":\"A0\", \"decision\":\"auto\".\nRecord attributes used: r.payload (dict) and seq ORDERING (iter_records is ascending seq). host_id is the map key via p.get(\"host_id\"); the stored value is {host_id, os, has_screen, has_camera, has_gpu_vlm, always_on, has_hid_inject, has_camera_stream} (i.e. _CAP_CORE minus \"signal\", values via p.get(k)).",
        "fold_state": "SnapshotState sub-state key e.g. `capability_map`: a dict {host_id: cap_dict} covering exactly the prune prefix [0..K), where cap_dict = {k: <owner-signed value> for k in _CAP_CORE if k != \"signal\"} of the HIGHEST-seq VERIFIED (owner-signed over _CAP_CORE, signal==\"mesh.host_capability\") record for that host_id within [0..K). Type: Dict[str, Dict[str, Any]]. It stores ONLY host_ids that had at least one verified advertisement in the prefix.\nRECOMMENDED robust form (to make the monoid genuinely order-independent and multi-prune-composable): {host_id: {\"seq\": int, \"cap\": cap_dict}} keeping the max-seq entry per host_id. The bare {host_id: cap_dict} form is sufficient for a single prune-at-K because every pruned seq < every live seq (see associativity_risk); the seq-carrying form is the safe general one. Identity/empty snapshot: {} (Slice C ships this).\nIMPORTANT: the folded map is valid ONLY for the pubkey it was folded under (owner_pubkey() at prune time). The snapshot must also record base_seq = K (first live seq) so the consumer knows where the live tail begins.",
        "fold_op": "Monoid: state = {host_id: cap_dict}; combine(A,B) = right-biased dict merge {**A, **B} (B's entries overwrite A's); identity = {}. This IS associative: {**{**A,**B},**C} == {**A,**{**B,**C}} == {**A,**B,**C}. Correctness of last-write-wins requires B (later stream/seq segment) always on the RIGHT — guaranteed because prefix [0..K) is entirely lower-seq than live [K..T].\nPer-record fold step (identical to the scan's inner body): for a live record r in ascending seq, if r.payload.get(\"signal\")==\"mesh.host_capability\" and verify_signed(r.payload,_CAP_CORE,tp): st[r.payload.get(\"host_id\")] = {k: r.payload.get(k) for k in _CAP_CORE if k!=\"signal\"}.\nReproduction: load(snapshot).capability_map gives fold(empty,[0..K)); then applying the per-record step over live [K..T] in ascending seq yields fold(empty,[0..T]) == the full scan, because (a) the extraction is byte-identical, (b) verify predicate is byte-identical (same verify_signed call), and (c) any live host_id overwrite supersedes the snapshot entry since live seq > prefix seq. For the seq-carrying form, combine keeps max seq per host_id (still associative + order-independent).",
        "consumer_rewrite": "Preserve the public signature `capability_map(store, trusted_pubkey=None) -> dict`:\n\n    def capability_map(store, trusted_pubkey=None) -> dict:\n        tp = trusted_pubkey if trusted_pubkey is not None else owner_pubkey()\n        head = SnapshotState.load(store)                     # folded prefix [0..head.base_seq)\n        latest = dict(head.capability_map)                   # {host_id: cap} from the prune, COPY\n        for r in store.iter_records(since_seq=head.base_seq - 1):   # live records seq >= base_seq, ascending\n            p = r.payload\n            if p.get(\"signal\") == CAP_SIGNAL and verify_signed(p, _CAP_CORE, tp):\n                latest[p.get(\"host_id\")] = {k: p.get(k) for k in _CAP_CORE if k != \"signal\"}\n        return latest\n\nNotes: iter_records(since_seq=head.base_seq-1) yields exactly seq >= base_seq (its contract: seq > since_seq). Start from a COPY of the snapshot map, then overwrite in ascending seq (last-write-wins preserved). Under Slice C's EMPTY snapshot: head.base_seq==0, head.capability_map=={} → since_seq=-1 → full scan → BYTE-IDENTICAL to the current code. GUARD: if the snapshot map was folded under owner_pubkey() but the caller passes a DIFFERENT trusted_pubkey, the snapshot is invalid — either (a) refuse the snapshot path and full-scan when tp != snapshot.folded_under_pubkey, or (b) only ever consult the snapshot when trusted_pubkey is None/owner (the sole real usage). Recommend (a) for exactness.",
        "edge_cases": "• Ordering sensitivity: HIGH — last-write-wins is ordered by seq. Exactness relies on iter_records yielding ascending seq (it does) and on live [K..T] being entirely higher-seq than pruned [0..K) (the prune boundary guarantees this). Ordering across the prune boundary is preserved: pruned prefix seqs < live suffix seqs universally, so folding snapshot-then-live reproduces global seq order.\n• Tie-break: none beyond \"higher seq wins\"; seq is unique+monotonic. No ts/actor tiebreak.\n• Verified-only: only owner-signed records (sig present, pubkey==tp, valid Ed25519 over _CAP_CORE) count; forged/unsigned/wrong-key advertisements are silently ignored by BOTH scan and fold (verify_signed reused verbatim → identical).\n• host_id==None: if a signed record somehow omits host_id, both scan and fold key on None identically (edge preserved).\n• Empty state: no verified CAP records → {} (both). tp falsy (owner_pubkey()==None) → nothing verifies → {} (both).\n• Value dict identity: fold uses the exact `{k: p.get(k) for k in _CAP_CORE if k!=\"signal\"}` comprehension → same keys, same order, same values as the scan.\n• trusted_pubkey argument: a non-owner tp passed by a caller makes a prefolded (owner-keyed) snapshot WRONG — must bypass the snapshot and full-scan for that call (see consumer_rewrite guard). Under Slice C (empty snapshot) this is moot: every record is re-verified live against tp, so byte-identical for any tp.\n• A host advertised in the prefix then never again: snapshot retains it, no live overwrite → correct (matches scan keeping the prefix's latest). A host revoked/downgraded via a NEW lower-capability advertisement is just another CAP record → normal last-write-wins (there is no separate \"revoke\" for capability, unlike the device ledger).",
        "associativity_risk": "The fold is TRULY associative as a right-biased-dict-merge monoid ({host_id:cap}, {**A,**B}, {}), BUT last-write-wins is ORDER-SENSITIVE: the winner per host_id is the highest-seq verified record, so B must always be the later-seq segment (on the RIGHT of the merge). This holds automatically for a single prune-at-K because pruned [0..K) are all lower-seq than live [K..T]; folding snapshot-on-left, live-on-right is exact.\nTo stay exact and safe under ARBITRARY re-splitting / multiple successive prunes, the snapshot should carry per-host seq: store {host_id: {\"seq\": seq, \"cap\": cap_dict}} and combine by keeping the MAX seq per host_id (an order-independent commutative-idempotent merge). Then load()+forward-fold reproduces the scan regardless of split point. The minimal {host_id: cap_dict} form (no seq) is exact ONLY under the prefix<suffix invariant of a single K-boundary prune — which Slice C satisfies trivially (empty snapshot). Additionally the snapshot MUST carry base_seq=K (so the live tail is filtered seq>=K) and the pubkey it was folded under (owner_pubkey at prune time), because verify is key-dependent; a call with a different trusted_pubkey must not reuse the snapshot. No timestamp/wallclock is involved, so no clock-skew tiebreak risk."
      },
      {
        "bearer": "sigil/mesh/registry.py authorized_devices — set of currently-authorized device pubkeys (owner-signed authorize minus later revoke), latest-verified state per key",
        "file_line": "/home/kali/sigil/sigil/mesh/registry.py:66-76 (scan loop at line 71: for r in store.iter_records():)",
        "current_logic": "Exact steps of authorized_devices(store, trusted_pubkey=None) -> Set[str]:\n1. Resolve trust anchor ONCE: tp = trusted_pubkey if trusted_pubkey is not None else owner_pubkey(). owner_pubkey() reads KEYS_DIR/owner.pub, returns the b64 str or None. If tp is None/empty, verify_signed is False for every record → state stays {} → returns empty set (fail-closed).\n2. state: dict = {}.\n3. Iterate ALL records: for r in store.iter_records(): — full genesis→tip scan, strictly ascending seq, contiguous across sealed+active segments. No since_seq. Unparseable lines are skipped by the reader (not valid chain records; irrelevant to the fold).\n4. Per record read p = r.payload (dict). Reads ONLY payload; does NOT filter on r.kind, r.source, r.actor, r.seq, r.ts, parent_id, supersedes_id.\n5. Predicate (all three): p.get(\"signal\") == DEV_SIGNAL (=\"mesh.device\"); AND p.get(\"state\") in (\"authorized\",\"revoked\"); AND verify_signed(p, _DEV_CORE, tp) is True.\n6. On match, LAST-WRITE-WINS accumulate: state[p.get(\"device_pubkey\")] = p[\"state\"]. Bracket p[\"state\"] is safe (step 5 confirmed it); key via .get so a missing device_pubkey yields a None key. Ascending seq means the highest-seq verified record per device_pubkey overwrites earlier ones.\n7. Return set comprehension: {pub for pub, st in state.items() if st == \"authorized\"} — device_pubkeys whose LAST verified state is authorized; keys whose last state is revoked are excluded. Return type Set[str].",
        "record_shapes": "SpineRecord (sigil/spine/models.py): seq:int, scope:str, kind:str, source:str, actor:str, payload:dict, parent_id, supersedes_id, ts, cert_digest, prev_hash, entry_hash. Scan reads ONLY r.payload.\n\nProducing records (registry.py authorize_device/revoke_device) append kind=\"event\", source=\"mesh\", actor=\"OWNER\", payload = signed_payload(core, owner_key) + {\"tier\":\"A0\",\"decision\":\"auto\"}, core = {\"signal\":\"mesh.device\",\"state\":\"authorized\"|\"revoked\",\"device_id\":<str>,\"device_pubkey\":<str>}; signed_payload adds \"sig\" (Ed25519 over canonical_json(core)) and \"pubkey\" (owner pub b64).\n\nPayload fields the scan depends on:\n- payload[\"signal\"] == \"mesh.device\" (DEV_SIGNAL).\n- payload[\"state\"] in {\"authorized\",\"revoked\"}.\n- payload[\"device_pubkey\"] — the map key.\n- payload[\"sig\"], payload[\"pubkey\"] — consumed by verify_signed.\n- verify_signed(p, _DEV_CORE, tp), _DEV_CORE=(\"signal\",\"state\",\"device_id\",\"device_pubkey\"): True iff tp truthy AND p[\"sig\"] truthy AND p[\"pubkey\"]==tp AND Ed25519 verify(tp, canonical_json({k:p.get(k) for k in _DEV_CORE}), p[\"sig\"]). device_id participates in the signed core though the scan never branches on it. verify_signed is STATELESS (pure fn of payload+tp) → applied identically at snapshot-time and to live records.\nNOTE: kind/source/actor are NOT in the predicate — any record whose payload is a valid owner-signed mesh.device authorize/revoke counts regardless of kind/source/actor.",
        "fold_state": "dev_state: dict[str, str] — a LAST-WRITE-WINS map { device_pubkey (str; possibly the None key if a matching record lacked device_pubkey) : last_verified_state ∈ {\"authorized\",\"revoked\"} } with an entry for EVERY device_pubkey that had >=1 verified mesh.device authorize/revoke record in the pruned prefix [0..K). CRITICAL: retain \"revoked\" entries — store the full latest-state map, not just the authorized set. The map is bound to the trust anchor it was folded under (owner_pubkey()); persist that pubkey too (dev_snapshot_pubkey) so a trusted_pubkey mismatch is detectable.",
        "fold_op": "Identity: {} (empty dict). Combine (associative under the prune-boundary ordering guarantee): merge two same-order sub-states by key with the higher-seq operand winning per key — merge(a,b) = {**a, **b} when every write in b has strictly greater seq than every write in a. Reconstruction reproducing the scan byte-identically:\n1. st = dict(SnapshotState.load(store).dev_state)   # = fold(empty,[0..K))\n2. for each live record r in ascending seq over [K..T]: if p.get(\"signal\")==\"mesh.device\" and p.get(\"state\") in (\"authorized\",\"revoked\") and verify_signed(p,_DEV_CORE,tp): st[p.get(\"device_pubkey\")] = p[\"state\"]   # live overwrites snapshot because every live seq > every pruned seq (LWW)\n3. return {pub for pub,stt in st.items() if stt==\"authorized\"}\nThis equals fold(fold(empty,[0..K)),[K..T]) == fold(empty,[0..T]) == the full scan, because dict-update in ascending seq is exactly the scan's state[key]=value last-write-wins and the prune boundary makes all snapshot writes strictly precede all live writes.",
        "consumer_rewrite": "Keep the public signature authorized_devices(store, trusted_pubkey=None) -> Set[str]. Body:\n```\ntp = trusted_pubkey if trusted_pubkey is not None else owner_pubkey()\nhead = SnapshotState.load(store)\nif tp is not None and tp == head.dev_snapshot_pubkey:   # snapshot folded under owner_pubkey()\n    state = dict(head.dev_state)\n    since = head.base_seq - 1        # iter_records yields seq >= base_seq (live [K..T])\nelse:\n    state = {}                       # custom/absent anchor: fold from genesis\n    since = -1\nfor r in store.iter_records(since_seq=since):\n    p = r.payload\n    if p.get(\"signal\") == DEV_SIGNAL and p.get(\"state\") in (\"authorized\", \"revoked\") \\\n            and verify_signed(p, _DEV_CORE, tp):\n        state[p.get(\"device_pubkey\")] = p[\"state\"]\nreturn {pub for pub, st in state.items() if st == \"authorized\"}\n```\nUnder Slice C (empty snapshot: dev_state={}, base_seq=0): since=-1, state={} → current full scan byte-for-byte. base_seq = first LIVE seq K; iter_records(since_seq=base_seq-1) returns exactly seq>=base_seq.",
        "edge_cases": "- Empty snapshot (Slice C): dev_state={}, base_seq=0 → since_seq=-1 → identical to the current genesis→tip scan. Guaranteed byte-identical.\n- Ordering across the prune boundary IS preserved: iter_records yields strictly ascending seq and every live seq > every pruned seq, so live overwrites snapshot exactly as the scan's later-record-wins.\n- Tie-break: valid chain has unique strictly increasing seq; iter_records yields each once → no ties; total order by seq; later seq wins on the same device_pubkey.\n- Verified-only: unsigned/forged/wrong-pubkey/tampered-core records fail verify_signed and never mutate the map (rogue device can't self-authorize). Fold MUST re-run verify_signed on live records with the SAME tp the scan used.\n- states other than authorized/revoked are ignored (skipped), never written.\n- device_pubkey missing → key None (mirror p.get); p[\"state\"] bracket is safe post-predicate. Mirror both accessors exactly.\n- TRUST-ANCHOR BINDING (real gotcha): dev_state is tp-specific — folded under owner_pubkey() at prune time. If a caller passes a non-default trusted_pubkey (or owner_pubkey() differs from the snapshot anchor), the pre-folded map is invalid; fall back to a genesis scan (state={}, since=-1). Persist dev_snapshot_pubkey to detect this. In practice trusted_pubkey is None (default) so the fast path is taken.\n- tp None/empty (no owner key) → verify_signed False for all → empty set (fail-closed); the empty-snapshot fold reproduces this.",
        "associativity_risk": "Order-SENSITIVE: last-write-wins, NOT set-union. Exact/associative ONLY because the prune boundary guarantees strict seq ordering — all snapshot writes [0..K) precede all live writes [K..T] — so \"live dict-update overwrites snapshot\" is exactly the scan. What the snapshot MUST carry to stay exact: the FULL per-key latest state INCLUDING \"revoked\" entries (a {device_pubkey: authorized|revoked} map), not just the authorized set. Critical trap: a monotone set-UNION of authorized pubkeys (the tempting associative op) DROPS revocations and re-authorizes a revoked device; likewise omitting a key whose last state is \"revoked\" from the folded snapshot silently loses a revoke committed inside the pruned prefix → re-authorize. Retaining every key's revoked state makes the revoke an explicit override live records / future re-prunes cannot erase. (Completeness caveat: because the OUTPUT keeps only authorized keys, an authorized-SET with add-on-authorize/remove-on-revoke would coincidentally match THIS bearer's output — but it is fragile and non-composable; the spec-required, provably-exact form is the full LWW map, which also stays correct under re-prune/state-merge.) No per-key seq is needed for THIS design because the boundary orders everything; a per-key {seq,state} would only be required if two same-side partial states were ever merged out of order."
      },
      {
        "bearer": "sigil/governor/promotion.py — PromotionPolicy.is_promoted(agent, scope=\"*\") -> bool. Answers \"may this (agent, scope) auto-approve its A2 records?\" as the LATEST owner-signature-VERIFIED grant/revoke decision for that (agent, scope), evaluated over the whole spine at K-1. This is a per-key LAST-WRITE-WINS map ({(agent,scope): \"granted\"|\"revoked\"}), NOT a monotone latch — a later verified revoke overrides an earlier grant and vice-versa.",
        "file_line": "/home/kali/sigil/sigil/governor/promotion.py:44 (is_promoted; scan loop lines 47-55). Helpers: authn.verify_signed at /home/kali/sigil/sigil/governor/authn.py:27; signed_payload at authn.py:19; _CORE=(\"signal\",\"state\",\"agent\",\"scope\") and SIGNAL=\"governor.promotion\" and NO_PROMOTION_AGENTS={\"ENVOY\",\"DELEGATE\"} at promotion.py:17-19; store.iter_records at /home/kali/sigil/sigil/spine/store.py:727; SpineRecord at /home/kali/sigil/sigil/spine/models.py:25.",
        "current_logic": "1) if agent in NO_PROMOTION_AGENTS ({\"ENVOY\",\"DELEGATE\"}): return False immediately (structural, query-time, independent of any record). 2) state = {} (a dict keyed by the tuple (agent, scope)). 3) for r in self.store.iter_records(): iterate EVERY record on the spine in strictly-increasing seq order across all segments (sealed then active), no repeats. 4) p = r.payload. 5) FILTER: if p.get(\"signal\") != \"governor.promotion\"  OR  p.get(\"state\") not in (\"granted\",\"revoked\"): continue (skip). NOTE: NO filter on r.kind / r.source / r.actor — filtering is purely on payload.signal and payload.state. 6) SIGNATURE GATE (fail-closed): if not verify_signed(p, (\"signal\",\"state\",\"agent\",\"scope\"), self.trusted_pubkey): continue. verify_signed returns True iff trusted_pubkey is truthy AND p[\"sig\"] is truthy AND p[\"pubkey\"] == trusted_pubkey AND verify_one(trusted_pubkey, canonical_json({signal,state,agent,scope} taken via p.get), p[\"sig\"]) succeeds. A record failing any of these is fully ignored (does NOT overwrite an existing key). 7) ACCUMULATE: state[(p.get(\"agent\"), p.get(\"scope\"))] = p[\"state\"]  — unconditional overwrite, so the LATEST verified grant/revoke for a given (agent,scope) pair wins (later seq overwrites earlier). 8) RETURN: state.get((agent, scope)) == \"granted\"  OR  state.get((agent, \"*\")) == \"granted\". Wildcard is resolved at QUERY time by checking the (agent,\"*\") key in addition to the exact (agent,scope) key; both are stored as distinct keys, never collapsed.",
        "record_shapes": "The scan reads ONLY r.payload (dict); it does not read r.seq/scope/kind/source/actor/ts except that iter_records yields in seq order. Producing records (PromotionPolicy.grant/revoke, promotion.py:28-42): store.append(kind=\"event\", source=\"governor\", actor=\"WARDEN\", payload=P). P = signed_payload(core) merged with {\"by\":\"owner\",\"tier\":\"A0\",\"decision\":\"auto\"}, where core={\"signal\":\"governor.promotion\",\"state\":\"granted\"|\"revoked\",\"agent\":<str>,\"scope\":<str, default \"*\">} and signed_payload adds \"sig\" (Ed25519 sign over canonical_json(core)) and \"pubkey\" (owner public key b64). So the payload fields the scan depends on are: payload.signal, payload.state, payload.agent, payload.scope, payload.sig, payload.pubkey. grant() on a NO_PROMOTION_AGENT instead writes kind=\"refusal\" with payload {signal:\"governor.promotion\",\"decision\":\"refused\",\"agent\",\"scope\",\"tier\":\"A0\",\"reason\":...} and NO \"state\" key → p.get(\"state\") is None → filtered out by step 5. SpineRecord attributes exist (seq:int, scope:str, kind:str, source:str, actor:str, payload:dict, parent_id, supersedes_id, ts, cert_digest, prev_hash, entry_hash) but ONLY payload and seq-ordering are load-bearing here.",
        "fold_state": "A LAST-WRITE-WINS map: promotion: dict[(agent:str, scope:str), state:str] where state ∈ {\"granted\",\"revoked\"}. It must carry EVERY (agent,scope) pair that had at least one verified grant/revoke in [0..K), each mapped to that pair's LATEST verified state at seq K-1. Do NOT store a single agent's value and do NOT collapse the exact-scope key and the \"*\"-scope key — is_promoted can be queried for any (agent,scope) and separately checks the (agent,\"*\") key. Persisted shape (tuples aren't JSON keys): serialize as {f\"{agent}\\x00{scope}\": state} (NUL-joined per the bearer note), reconstructed to (agent,scope) tuples by SnapshotState.load(). Values are the literal strings \"granted\"/\"revoked\". This sub-state depends on trusted_pubkey (the key under which the prefix was verified) — record it alongside so the snapshot is only reused while the trusted owner pubkey is unchanged.",
        "fold_op": "Identity element: {} (empty map). combine per live record rec, applied in increasing seq order: p = rec.payload; if p.get(\"signal\") != \"governor.promotion\" or p.get(\"state\") not in (\"granted\",\"revoked\"): keep map unchanged (skip); elif not verify_signed(p, (\"signal\",\"state\",\"agent\",\"scope\"), trusted_pubkey): keep map unchanged (skip); else map[(p.get(\"agent\"), p.get(\"scope\"))] = p[\"state\"]. The binary map-merge is combine(A,B) = A overlaid by B where B's keys win (B = the later/higher-seq operand). This overlay is associative: (A⊕B)⊕C == A⊕(B⊕C), both yield \"highest-seq write per key\". Therefore load(snapshot).promotion (= fold over verified [0..K)) then folding the same combine over live records [K..T] in seq order == fold over [0..T] == the current full scan's `state` dict, and the query state.get((agent,scope))==\"granted\" or state.get((agent,\"*\"))==\"granted\" is applied identically.",
        "consumer_rewrite": "Keep the public signature/return exactly: def is_promoted(self, agent: str, scope: str = \"*\") -> bool. Body: (1) keep the early guard `if agent in NO_PROMOTION_AGENTS: return False` verbatim (query-time, NOT part of the fold). (2) load folded prefix: `state = dict(SnapshotState.load(self.store).promotion)` (copy so folding forward doesn't mutate the cached snapshot); also read K = snapshot base_seq (0 in Slice C). (3) fold forward over live records only: `for r in self.store.iter_records(since_seq=K-1):` (iter_records yields seq > since_seq, so since_seq=K-1 gives seq>=K = live [K..T]; with empty snapshot K=0 → since_seq=-1 → the current full scan, byte-identical). Inside the loop reuse the EXACT current body: p=r.payload; the same signal/state filter; the same `if not verify_signed(p, _CORE, self.trusted_pubkey): continue`; the same `state[(p.get(\"agent\"), p.get(\"scope\"))] = p[\"state\"]`. (4) return `state.get((agent, scope)) == \"granted\" or state.get((agent, \"*\")) == \"granted\"` unchanged.",
        "edge_cases": "ORDERING: last-write-wins is order-sensitive; correctness requires strictly-increasing seq order, which iter_records guarantees (yields only rec.seq > yielded_upto). VERIFIED-ONLY: unverified/forged grant/revoke are skipped before the assignment, so a later FORGED revoke must NOT clear an earlier VALID grant — the fold reuses verify_signed verbatim so this holds. NO KIND FILTER: the scan filters on payload only; a granted/revoked-signal payload on any kind counts — the fold must NOT add a kind filter. REFUSAL RECORDS: have no payload.state → skipped by both scan and fold. WILDCARD: (agent,scope) and (agent,\"*\") are distinct keys resolved at query time; store both, never merge; when scope==\"*\" both query lookups hit the same key (still correct). EMPTY STATE: no verified grant for a key → state.get(...) is None != \"granted\" → False; empty snapshot map reproduces this. NONE KEYS: p.get(\"agent\")/p.get(\"scope\") could be None if absent, but a verified record signed those exact fields, so keys equal the signed values; NUL-join serialization assumes str keys (a literal \\x00 inside agent/scope could collide on the persisted key — real values don't contain it, but validate on write). TRUSTED_PUBKEY DEPENDENCE: the fold's verification predicate depends on self.trusted_pubkey; the snapshot is only valid while trusted_pubkey is unchanged (owner-key rotation must invalidate/recompute the snapshot). PRUNE-BOUNDARY ORDERING: preserved because all prefix seqs (<K) precede all live seqs (>=K), so overlaying live-over-prefix reproduces global seq order.",
        "associativity_risk": "The fold is associative/split-exact ONLY as a per-key last-write-wins overlay, and ONLY because operands are consumed in strictly increasing seq order and the prune boundary is a clean seq cut (prefix seq<K, live seq>=K). The snapshot MUST carry: (a) the FULL map of every (agent,scope) pair seen in verified [0..K) with its latest verified state at K-1 (dropping any pair, or storing only \"granted\" as if it were a monotone latch, would be WRONG — a grant-then-revoke prefix must persist \"revoked\", and a query for an untouched pair must find nothing); (b) the trusted_pubkey used to verify the prefix (the fold's verify_signed gate is parameterized by it — a snapshot folded under a rotated key is stale). It is NOT a boolean latch and NOT max/set-union: revoke can lower state back from granted, so only the ORDER-CARRYING latest-write value is exact. With Slice C's empty/identity snapshot (K=0, empty map), folding the current filter+verify+assign over iter_records(since_seq=-1) is the current scan line-for-line → byte-identical."
      },
      {
        "bearer": "sigil/agents/approvals.py :: pending() — \"queued proposals with no VERIFIED owner/authorized-device approval yet, oldest first\". A set-difference between the queued-items map and the resolved-target set, computed in one full scan of the spine.",
        "file_line": "/home/kali/sigil/sigil/agents/approvals.py:53 (function `pending`); the full scan is the loop at lines 62-68. Helpers it depends on: `verify_approval` (lines 33-50), `_approval_message` (28-30), constants `SIGNAL=\"governor.approval\"` (24) and `_QUEUED_STATUS=\"awaiting-approval\"` (25). Record producers: queued items written by `sigil/agents/base.py:104-106`, `sigil/gesture/session.py:308-312`, envoy drafts; approvals written by `ApprovalQueue._decide` at `approvals.py:98-103`.",
        "current_logic": "One pass over `store.iter_records()` (default since_seq=-1 → ALL records, ascending seq order across all segments). Two independent accumulators are built:\n(1) RESOLVED set: for each record r, let p=r.payload; IF p.get(\"signal\")==\"governor.approval\" AND verify_approval(r, trusted_pubkey_b64, extra_pubkeys=extra_pubkeys) returns True, THEN add p.get(\"target_seq\") to `resolved`. (It keys off the SIGNED target_seq, NOT r.supersedes_id — RP-APPROVAL-1/4.)\n(2) QUEUED map: for each record r, IF p.get(\"decision\")==\"queued\" AND p.get(\"status\")==\"awaiting-approval\", THEN set queued[r.seq]=r. Note these two branches are independent `if`s, not elif — a single record is tested against both predicates (in practice disjoint: an approval record carries decision==\"auto\", never \"queued\").\nReturn: `[r for seq, r in sorted(queued.items()) if seq not in resolved]` — the RECORD objects (SpineRecord), sorted ascending by their spine seq (oldest first), keeping only those queued seqs absent from RESOLVED. If trusted_pubkey_b64 arg is None it is first defaulted to governor.identity.owner_pubkey(). Both \"approved\" and \"denied\" approvals resolve (a denial IS a decision → removed from pending); an unsigned/forged/not-in-allowed-set approval does NOT resolve (fail-closed, item stays pending).",
        "record_shapes": "TWO families, both matched purely on payload fields (NO kind/source/actor filter anywhere in pending).\nQUEUED item — kind is the proposal's own kind (email/draft/operation/event/...); source \"agent\"|\"gesture\"|... ; actor = agent/PERCEPTION/etc. Matched on payload.decision==\"queued\" AND payload.status==\"awaiting-approval\". Carries agent-specific fields (agent, tier, subject, tool/args, signal like \"gesture.action\" or \"web.actor.step\"); pending is signal-AGNOSTIC on this branch — any record with those two payload strings counts. Its spine `seq` (record attribute, not payload) is the key used in both `queued` and the final sort.\nAPPROVAL — written by ApprovalQueue._decide: kind=\"event\", source=\"governor\", actor=\"OWNER\", supersedes_id=the queued seq. payload = {signal:\"governor.approval\", approval:\"approved\"|\"denied\", target_seq:<queued seq>, target_kind, target_tier, approver, reason, pubkey:<b64>, sig:<b64>, msg_digest, tier:\"A0\", decision:\"auto\"}. pending's RESOLVED branch reads payload.signal, payload.sig, payload.pubkey, payload.target_seq, payload.approval, payload.approver (last three via verify_approval → _approval_message → canonical_json({\"target\":target_seq,\"decision\":approval,\"approver\":approver}) Ed25519-verified with verify_one). Crucial: because supersedes_id/target_seq is a BACKWARD reference (the approval is appended after the queued item), always target_seq < approval.seq.",
        "fold_state": "The sub-state is a PAIR of monotone unions, each associative on its own:\n  QUEUED_carry : dict[int seq -> SpineRecord]  — queued items in the pruned prefix [0..base) (keys are globally-unique spine seqs)\n  RESOLVED_carry : set[int target_seq]         — target_seqs resolved by verified OWNER-key approvals in [0..base)\nFor the bearer's actual DESIGN the persisted state COLLAPSES to identity: the only part of the prefix that can affect the live output is a queued item in [0..base) that is still OPEN (its seq in QUEUED_carry but NOT in RESOLVED_carry). The referential-floor guarantee makes that set empty, so the snapshot carries NOTHING and instead persists a single boolean assertion `open_queued_below_base == {}` (an ASSERT, per the bearer note). Type/shape to reserve if the assert is ever relaxed: a list[SpineRecord] of any open queued<base (needed because the output emits whole records, not seqs) — expected empty.",
        "fold_op": "Combine of two prefix-states: (Qa,Ra) ⊕ (Qb,Rb) = (Qa | Qb  [dict merge; keys are unique seqs so it is disjoint union], Ra ∪ Rb [set union]). Identity element: ({}, {}). Both operands are associative AND commutative. FINALIZE (applied exactly ONCE, after the whole fold, never per-step): sorted((r for seq,r in Q.items() if seq not in R), key=<the dict key seq>) → the oldest-first record list. Exactness: load(snapshot).state seeds (Q,R) with the prefix's contribution; folding the live records [base..T] with the SAME two predicates (queued-match populates Q, verify_approval-passing governor.approval populates R) then finalize reproduces fold(({},{}), all[0..T]) == the current scan. Why the collapse is exact: (a) a pruned RESOLVED target_seq<base can only reference a pruned queued seq<base (backward supersedes) so dropping RESOLVED_carry removes NO live output; (b) a pruned QUEUED item that is resolved is excluded from output anyway; (c) the ONLY prefix contribution to live output is an OPEN pruned queued item — asserted empty. So Q=R={} at load, and the live fold alone is byte-exact. Slice C: base=0 → seeds empty, live=[0..T]=all → identical.",
        "consumer_rewrite": "Preserve the signature/return exactly: `pending(store, trusted_pubkey_b64=None, *, extra_pubkeys=None) -> List`. Keep the owner_pubkey() defaulting for trusted_pubkey_b64. Then:\n    st = SnapshotState.load(store)\n    # referential-floor ASSERT (the bearer's design): no open queued item straddles the prune point\n    if st.approvals.open_queued_below_base:            # empty under the guarantee; identity under Slice C\n        raise ... / carry+re-emit those records\n    resolved, queued = set(), {}\n    for r in store.iter_records(since_seq=st.head.base_seq - 1):   # live records seq >= base_seq\n        p = r.payload\n        if p.get(\"signal\") == SIGNAL and verify_approval(r, trusted_pubkey_b64, extra_pubkeys=extra_pubkeys):\n            resolved.add(p.get(\"target_seq\"))\n        if p.get(\"decision\") == \"queued\" and p.get(\"status\") == _QUEUED_STATUS:\n            queued[r.seq] = r\n    return [r for seq, r in sorted(queued.items()) if seq not in resolved]\nOnly edit vs today: `store.iter_records()` → `store.iter_records(since_seq=st.head.base_seq - 1)` plus the load()+assert. Under an EMPTY snapshot head.base_seq==0 ⇒ since_seq=-1 ⇒ iter_records() over ALL records ⇒ byte-identical to the current scan (same set-diff, same sorted-by-seq record list). NB `iter_records(since_seq=k-1)` yields seq>=k, so the boundary record at base_seq is included exactly once.",
        "edge_cases": "Ordering: FULLY order-insensitive — RESOLVED is a set, QUEUED is a dict keyed on unique seq, both order-free; the only ordering is the terminal sorted() on the seq key (total order, no ties possible since spine seqs are unique). A resolving approval and its queued item may appear in either relative position; pending collects both in one pass and diffs at the end, so ordering ACROSS THE PRUNE BOUNDARY is irrelevant — the fold is split-exact. Verified-only: unsigned/forged/pubkey-not-in-allowed-set approval does NOT resolve (fail-closed); both \"approved\" and \"denied\" DO resolve. Empty state: no records → resolved={}, queued={} → returns [] (matches under empty snapshot). Approval record never self-counts as queued (its decision==\"auto\", no status). Gesture (\"gesture.action\") and actor (\"web.actor.step\") queued records DO count (queued branch is signal-agnostic) and are resolved by governor.approval records referencing their seq — replicate with NO signal filter on the queued branch. A re-queue (queued item superseded by another queued item) leaves BOTH seqs in the map and both appear unless separately resolved — preserve as-is.",
        "associativity_risk": "The two accumulators ARE associative/commutative (set-union, disjoint dict-merge) and the finalize is applied once, so the fold is split-exact FOR A FIXED predicate. The real risk is that RESOLVED is NOT a pure function of the record bytes: verify_approval depends on the QUERY-TIME `trusted_pubkey_b64` and `extra_pubkeys` (the live, revocable device-authorization ledger from sigil.mesh). Therefore a RESOLVED set folded into the snapshot would be UNSOUND — a device-key-only resolution valid at prune time can become invalid at a later query with a narrower/revoked key set, at which point the pruned queued<base item is OPEN and MUST reappear, but it is gone → output diverges. To make the ASSERT sound for ALL future key sets, prune must require every queued<base to be resolved by the OWNER key specifically (owner_pubkey is unconditionally in the allowed set and never revocable) i.e. verify_approval(r, owner_pubkey, extra_pubkeys=None) — that is the \"referential floor\". Under it, open-queued-below-base is empty for every possible caller, nothing is carried, and order/parameter-independence holds. Backward-supersedes (target_seq < approval.seq) additionally guarantees a live approval can never resolve a pruned queued seq's ABSENCE incorrectly and a pruned approval never touches live output. Slice C (base=0): the assert is vacuous, snapshot=identity, fold over [0..T] == the current full scan byte-for-byte. If a future prune finds open queued<base, it MUST refuse (or carry those full records for re-emission) rather than silently drop them."
      },
      {
        "bearer": "sigil/consolidate/revise.py — the archivist current-view + promotion-ledger bearer: consolidation_records (L14), iter_current (L21), promotion_ledgers (L28). Feeds consolidate/queries.py _grounded_current → open_threads/due_commitments/pending_contradictions (and promote.py's idempotency check). All three do a full genesis→T scan of store.iter_records() filtered to source==\"archivist\".",
        "file_line": "sigil/consolidate/revise.py:14-18 (consolidation_records), :21-25 (iter_current), :28-39 (promotion_ledgers). Consumers: sigil/consolidate/queries.py:23-25 (_grounded_current), :27-40 (open_threads), :43-68 (due_commitments), :71-82 (pending_contradictions). Ledger consumer: sigil/consolidate/promote.py:40.",
        "current_logic": "Three full-scan primitives, each a single pass over store.iter_records() (since_seq=-1 → every record, ascending seq across all segments):\n\n(1) consolidation_records(store, kinds): for r in store.iter_records(): if r.source == CONSOLIDATE_SOURCE (\"archivist\") and (kinds is None or r.kind in kinds): yield r. Pure filter, preserves ascending-seq order, no accumulation.\n\n(2) iter_current(store, kinds): recs = list(consolidation_records(store, kinds)); superseded = { r.supersedes_id for r in recs if r.supersedes_id is not None }; return [ r for r in recs if r.seq not in superseded ]. i.e. among the kind-filtered archivist records ONLY, drop any record whose seq is named as some sibling's supersedes_id. Note the supersession set is computed strictly WITHIN the kind-filtered set (a superseding record of a kind outside `kinds` does NOT count). Result preserves recs' ascending-seq order. TODAY superseded is ALWAYS EMPTY: no archivist writer (promote.py:72/81, brief.py:36) ever passes supersedes_id — they use parent_id only — so iter_current currently returns every archivist record of `kinds` unchanged, in seq order.\n\n(3) promotion_ledgers(store): grounded=set(); refused=set(); for r in consolidation_records(store) [kinds=None → ALL archivist kinds incl refusal & brief]: k = r.payload.get(\"promotion_key\"); if k is None: continue; (refused if r.kind==\"refusal\" else grounded).add(k); return grounded, refused. Two independent monotone sets keyed on the superseding record's kind==refusal. Supersession is IGNORED here (it scans all archivist records regardless of live/superseded). brief records have no promotion_key → skipped.\n\nDownstream (queries.py) does NOT participate in the fold — it operates on the record objects iter_current returns: _cite reads payload; open_threads sorts by (min(source_seqs or [seq]), seq); due_commitments dedups by (subject.lower, owner.lower) keeping max (max(source_seqs or [seq]), seq) then sorts by (due_iso, seq); pending_contradictions filters not payload.resolved & is_grounded then sorts by seq. All these sorts are Python-stable and their tie-breaks resolve on the seq value, so the ONLY requirement the fold must honor for byte-identity is: iter_current/consolidation_records must return the identical record objects in identical ascending-seq order.",
        "record_shapes": "Only source==\"archivist\" (CONSOLIDATE_SOURCE) records matter; three writers, none set supersedes_id (all use parent_id):\n\npromote.py:72 GROUNDED branch — kind ∈ {\"decision\",\"commitment\",\"entity\",\"contradiction\"} (=cand.kind); source=actor=\"archivist\"; parent_id=min(distinct verified_seqs) or None; supersedes_id=None. payload: subject, statement, quote, source_seqs(list[int]), verified_seqs(sorted distinct list[int]), grounding (\"ingest:seq=<n>\"), alpha(=1+len(distinct)), beta(=1), confidence_basis, model_confidence, promotion_key(16-hex), extractor; commitment ALSO: owner, due_iso; contradiction ALSO: conflicting_seqs(=distinct), resolved(=False).\n\npromote.py:81 DEMOTE branch — kind=\"refusal\"; payload: candidate_kind, subject, statement, grounding (\"llm:ungrounded\"), reason, source_seqs, promotion_key, extractor. supersedes_id=None.\n\nbrief.py:36 — kind=\"brief\"; NO promotion_key (skipped by promotion_ledgers). Never queried by iter_current.\n\nRecord attributes actually read by this bearer + its queries.py consumers: seq, kind, source (==archivist filter), supersedes_id (always None today), entry_hash (served by _cite), ts (served by _cite). Payload fields read by consumers: subject, quote, statement, verified_seqs, source_seqs, grounding, alpha, beta, owner, due_iso, conflicting_seqs, resolved, promotion_key. NOT read anywhere by this bearer: scope, actor, parent_id, cert_digest, prev_hash. grounding taxonomy: is_grounded == grounding.startswith(\"ingest:\").",
        "fold_state": "Three-part sub-state in SnapshotState for the pruned prefix [0..K):\n\n1) archivist_view: dict[int seq -> full record] of source==\"archivist\" records whose kind ∈ {\"decision\",\"commitment\",\"entity\",\"contradiction\"} (the promoted-fact kinds any iter_current consumer serves) that are NON-SUPERSEDED at K-1. Each entry retains FULL fields (seq, kind, entry_hash, ts, supersedes_id, and the whole payload dict) — a summary is NOT enough because (a) queries.py serves ~13 payload fields verbatim, and (b) a LIVE record in [K..T] can carry supersedes_id pointing back at a pruned seq, which must tombstone the retained record. Type: Map<int, SpineRecord-or-record-dict>. (Today this == every decision/commitment/entity/contradiction archivist record in [0..K), since nothing is ever superseded.)\n\n2) grounded_keys: set[str] — promotion_keys of every NON-refusal archivist record in [0..K) that has a promotion_key (i.e. decision/commitment/entity/contradiction; brief has none).\n\n3) refused_keys: set[str] — promotion_keys of every kind==\"refusal\" archivist record in [0..K).\n\nRefusal records are reduced to just their key in refused_keys (full fields dropped — no iter_current consumer serves refusals); brief records are dropped entirely. That reduction IS the compaction win over keeping the raw prefix.",
        "fold_op": "Identity element: archivist_view={}, grounded_keys=∅, refused_keys=∅ (the EMPTY snapshot in Slice C → the rewrite collapses to the current scan, byte-identical).\n\narchivist_view combine: disjoint dict-union keyed on seq (seqs are globally unique + append-only, so no key collides). load(snapshot).archivist_view ∪ {r.seq: r for r in live[K..T] if r.source==\"archivist\" and r.kind in fact-kinds}. The current-view is then recomputed AT QUERY TIME as a pure function of that record set exactly like the scan: for a given `kinds`, recs = [records of `kinds` in ascending seq], superseded = {r.supersedes_id for r in recs if r.supersedes_id is not None}, return [r for r in recs if r.seq not in superseded]. Because superseded is recomputed from the merged set (never pre-baked), the combine is associative and order-independent (dict merge is associative; the superseded set-comprehension is order-independent). Ascending seq order is guaranteed because all snapshot seqs < K <= all live seqs, and each part is emitted in seq order.\n\ngrounded_keys / refused_keys combine: set-union, split at insert by kind==\"refusal\". load(snapshot).grounded_keys ∪ {k for live non-refusal archivist r if (k:=r.payload.get(\"promotion_key\")) is not None}; symmetric for refused_keys over refusal records. Pure monotone set-union → associative, order-independent, idempotent. Supersession is irrelevant here (the scan never filters the ledger by liveness). A key may legitimately appear in BOTH sets (demoted once, grounded another) — the two sets are kept independent, matching promote.py reading grounded_seen and refused_seen separately.\n\nfold(load(snapshot), live[K..T]) == fold(empty, [0..T]) == current full scan, because (a) every archivist record of the fact-kinds in [0..K) is either retained in archivist_view or (only if superseded, which never happens today) provably dead, and (b) every promotion_key in [0..K) is retained in the two ledgers regardless of liveness.",
        "consumer_rewrite": "Add a shared live-scan helper and rewrite the three primitives; queries.py and promote.py are UNTOUCHED (they keep calling iter_current/promotion_ledgers with the same signatures/returns).\n\n  def _live_archivist(store, kinds, base_seq):\n      for r in store.iter_records(since_seq=base_seq - 1):   # yields seq >= base_seq\n          if r.source == CONSOLIDATE_SOURCE and (kinds is None or r.kind in kinds):\n              yield r\n\n  def consolidation_records(store, kinds=None):   # keep Iterator[SpineRecord]\n      st = SnapshotState.load(store)\n      for r in st.archivist_view_of(kinds):        # snapshot records of `kinds`, ascending seq, all seq < base_seq\n          yield r\n      yield from _live_archivist(store, kinds, st.head.base_seq)\n\n  def iter_current(store, kinds=None):             # unchanged body — pure function of consolidation_records\n      recs = list(consolidation_records(store, kinds))\n      superseded = {r.supersedes_id for r in recs if r.supersedes_id is not None}\n      return [r for r in recs if r.seq not in superseded]\n\n  def promotion_ledgers(store):                    # keep tuple[set,set]; use the ledgers, do NOT re-scan the prefix\n      st = SnapshotState.load(store)\n      grounded, refused = set(st.grounded_keys), set(st.refused_keys)\n      for r in _live_archivist(store, None, st.head.base_seq):\n          k = r.payload.get(\"promotion_key\")\n          if k is None: continue\n          (refused if r.kind == \"refusal\" else grounded).add(k)\n      return grounded, refused\n\nUnder the empty Slice-C snapshot: base_seq==0 → since_seq==-1 (identical to today's store.iter_records()); archivist_view_of()==[]; grounded/refused start ∅ → every function reduces EXACTLY to the current scan. NB: after this rewrite consolidation_records is only ever called with fact-kinds (by iter_current) — promotion_ledgers no longer routes the ALL-kinds scan through it — so the snapshot need not retain refusal/brief full records.",
        "edge_cases": "- Ordering across the prune boundary: PRESERVED. Snapshot seqs are all < base_seq, live seqs all >= base_seq, each half emitted ascending → global ascending seq, matching iter_records. Required because open_threads / due_commitments-dedup rely on stable-sort tie-breaks that fall back to original (seq-ascending) order.\n- Cross-boundary supersession: a live record in [K..T] whose supersedes_id points at a pruned seq correctly tombstones the retained snapshot record, because archivist_view keeps the full record (with its seq) and superseded is recomputed over the merged set. This is WHY the retained records must be full, not summarized.\n- Kind-scoped supersession: iter_current counts supersedes_id ONLY from records whose kind is in `kinds`; recomputing superseded at query time from the merged record set (not pre-baking a global superseded set) reproduces this exactly.\n- Empty state: empty snapshot → identity → byte-identical to current scan (Slice C requirement).\n- resolved / due_iso / grounding filters (pending_contradictions, due_commitments, _grounded_current): these are per-record payload reads applied AFTER iter_current, so they need only the full payload preserved on retained records — not fold state. is_grounded == grounding.startswith(\"ingest:\").\n- Ledger vs view divergence: promotion_ledgers ignores supersession and includes superseded records' keys; keeping grounded_keys/refused_keys as separate monotone sets (not derived from archivist_view) makes ledger correctness independent of any future view-pruning.\n- brief kind: has no promotion_key (skipped by ledger) and is never a `kinds` arg to iter_current → safely droppable from the snapshot for this bearer.\n- Duplicate keys across sets: a promotion_key may be in both grounded_keys and refused_keys; keep them independent.",
        "associativity_risk": "The fold is TRULY associative/order-independent AS SPECIFIED, provided archivist_view retains EVERY fact-kind archivist record of the prefix (no pruning of superseded records) so that superseded is recomputed per-query. Under that rule both sub-folds are order-free: dict disjoint-union + a set-comprehension recomputed from the merged set (view), and monotone set-union (ledgers). Identity = empty. Snapshot need carry only: the record MAP (seq→full record) + the two key SETS — no sequence/counter/last-write ordering token is required, because nothing here is last-write-wins or latch-style; the \"state\" is a set membership test.\n\nTHE ONE RISK is the compaction optimization of DROPPING superseded-at-K-1 records from archivist_view. That is exact ONLY if supersession is kind-total w.r.t. the query kind-sets: because iter_current(kinds) counts a supersession only when the SUPERSEDING record's kind ∈ kinds, a record superseded by a different-kind record is still LIVE for a query that excludes that kind — so a naive \"drop if superseded by any kind\" prune would wrongly hide it. TODAY this is vacuous and safe: NO archivist writer ever sets supersedes_id (promote.py:72/81 and brief.py:36 all use parent_id, never supersedes_id), so the superseded set is always empty, \"non-superseded-at-K-1\" == all fact records, and the prune removes nothing. Guardrail for the future: if a revise/supersede path is ever added, the snapshot must key retained-vs-dropped on the SUPERSEDING record's kind (i.e. keep per-kind supersession, or simply never prune a superseded record whose seq could still be served by a query whose kinds exclude the superseder). The ledger sub-fold has no such risk — it is a pure monotone union that never consults liveness."
      },
      {
        "bearer": "device-arm (HID-injection) nonce replay ledger — the consumed (device_pubkey, nonce) set read by BOTH pending_device_arms (the `armed` set, session.py:54-63) and arm_by_device's replay guard (session.py:214-221).",
        "file_line": "/home/kali/sigil/sigil/gesture/session.py:56 (pending_device_arms full scan) AND /home/kali/sigil/sigil/gesture/session.py:216 (arm_by_device replay-check full scan)",
        "current_logic": "TWO scan sites share ONE monotonic sub-state = the set of consumed device-arm nonces.\n\nSITE A — pending_device_arms(store, trusted_pubkey=None), session.py:54-63. iter_records() yields every record in strict ascending seq order, deduped by seq (see store.py:727-766). For each r, p=r.payload:\n  - if p.get(\"signal\")==\"gesture.session_armed\" AND p.get(\"armed_by\")==\"device\": armed.add((p.get(\"device_pubkey\"), p.get(\"nonce\")))  [builds the consumed-nonce set]\n  - ELIF p.get(\"signal\")==\"gesture.arm_request\" AND p.get(\"decision\")==\"auto\" AND p.get(\"sig\") (truthy) AND p.get(\"pubkey\") (truthy): cands.append(dict(p))  [collects pending candidates, in seq order]\n  The two branches are mutually exclusive (distinct signal values; the elif also guarantees a record is at most one).\n  Return: [c for c in cands if (c.get(\"pubkey\"), c.get(\"nonce\")) not in armed]  — i.e. every candidate payload copy whose (pubkey,nonce) has NOT been consumed, preserved oldest-first (seq-ascending). Returns a list (possibly empty).\n\nSITE B — arm_by_device replay guard, session.py:214-221. nonce=request.get(\"nonce\"); pub was set earlier = request.get(\"pubkey\"). Scans iter_records() and refuses (self._refuse_arm(request,\"replayed arm nonce\"); return None) on the FIRST record where p.get(\"signal\")==\"gesture.session_armed\" AND p.get(\"armed_by\")==\"device\" AND p.get(\"device_pubkey\")==pub AND p.get(\"nonce\")==nonce. This is exactly a membership test: refuse iff (pub,nonce) ∈ the consumed-nonce set. No tie-break needed — any match refuses; ordering is irrelevant to the boolean outcome. (It short-circuits on first hit only as an optimization.)\n\nBoth sites read the SAME set, defined by the SAME predicate/fields.",
        "record_shapes": "Only ONE record kind feeds the consumed-nonce set: the DEVICE-armed SESSION_ARMED record, written by arm_by_device (session.py:242-250): kind=\"event\", source=\"gesture\", actor=\"DEVICE\"; payload = {\"signal\":\"gesture.session_armed\", \"session_id\":str, \"armed_by\":\"device\", \"device_id\":str, \"device_pubkey\":pub(str, b64), \"nonce\":<JSON-native, int or str as the phone sent it>, \"ts\":float, \"ttl_seconds\":float(original signed), \"effective_ttl\":float, \"sig\":str, \"pubkey\":pub, \"authorization\":\"device-signed\", \"tier\":\"A0\", \"decision\":\"auto\", \"summary\":str}. The set uses ONLY payload[\"device_pubkey\"] and payload[\"nonce\"] (read via .get, so a missing field yields None).\n\nExplicitly EXCLUDED from the set (all lack armed_by==\"device\"):\n  - Owner SESSION_ARMED (arm(), session.py:158-162): actor=\"OWNER\", signed_payload core {signal, session_id}; NO armed_by, NO nonce/device_pubkey → never in the set.\n  - SESSION_DISARMED (\"gesture.session_disarmed\").\n\nRecords feeding pending_device_arms' `cands` (NOT the replay ledger, but co-computed at the same scan): ARM_REQUEST written by bridge submit_arm_request (daemon.py:141-145): kind=\"event\", source=\"mesh\", actor=\"DEVICE\"; payload={\"signal\":\"gesture.arm_request\", \"device_id\":str, \"nonce\":<JSON-native>, \"ts\":<orig>, \"ttl_seconds\":<orig>, \"pubkey\":pub, \"sig\":str, \"tier\":\"A0\", \"decision\":\"auto\"}. The refusal ARM_REQUEST (_refuse_arm, session.py:255-258) has decision==\"refused\" and no sig/pubkey → fails the cands predicate, correctly excluded.\n\nRecord attributes used: r.payload only. r.seq matters solely for ITERATION ORDER (oldest-first output of cands); neither source/actor/ts/kind is read by the predicates. iter_records order is ascending seq, contiguous across sealed+active segments, seq-deduped.",
        "fold_state": "consumed_arm_nonces: a SET of 2-tuples (device_pubkey, nonce), built exactly as {(p.get(\"device_pubkey\"), p.get(\"nonce\")) for records where p.get(\"signal\")==\"gesture.session_armed\" and p.get(\"armed_by\")==\"device\"}. Serialize in SnapshotState as a JSON list of [device_pubkey, nonce] pairs; load back into a Python set of tuples. CRITICAL: preserve each element's JSON-native type verbatim — device_pubkey is a b64 str; nonce is whatever the device sent (int OR str), NOT coerced — because both scan sites compare with raw ==/set-membership against un-coerced request/record values (arm_by_device: p.get(\"nonce\")==request.get(\"nonce\")). Stringifying nonce would break the int-nonce case. None is a legitimate element only for a malformed record (missing field); mirror that faithfully (don't drop None). This is the ONLY sub-state this bearer needs; the replay ledger is fully captured by this set.",
        "fold_op": "Combine = SET UNION. Identity = empty set (∅). fold(S, records) = S ∪ {(p[\"device_pubkey\"], p[\"nonce\"]) : p=r.payload, p.get(\"signal\")==\"gesture.session_armed\", p.get(\"armed_by\")==\"device\"}. Set-union is associative, commutative, AND idempotent, so load(snapshot).consumed_arm_nonces ∪ fold(∅, live[K..T]) == fold(∅, all[0..T]) == the current full scan's `armed` set, independent of split point K and of record order. Membership (Site B) and non-membership filter (Site A) are pure functions of this set, so they reproduce byte-for-byte.",
        "consumer_rewrite": "Let head = the loaded snapshot; head.base_seq = first live seq (0 when the snapshot is empty), head.consumed_arm_nonces = folded set of [0..base_seq). Live records are iter_records(since_seq=head.base_seq - 1) (iter yields seq>since_seq, so this is seq>=base_seq; empty snapshot → since_seq=-1 → all records).\n\nSITE A pending_device_arms — keep signature (store, trusted_pubkey=None) -> list:\n  armed = set(head.consumed_arm_nonces)   # seed from snapshot instead of {}\n  cands = []\n  for r in store.iter_records(since_seq=head.base_seq - 1):   # was: store.iter_records()\n      p = r.payload\n      if p.get(\"signal\")==SESSION_ARMED and p.get(\"armed_by\")==\"device\":\n          armed.add((p.get(\"device_pubkey\"), p.get(\"nonce\")))\n      elif (p.get(\"signal\")==ARM_REQUEST and p.get(\"decision\")==\"auto\" and p.get(\"sig\") and p.get(\"pubkey\")):\n          cands.append(dict(p))\n  return [c for c in cands if (c.get(\"pubkey\"), c.get(\"nonce\")) not in armed]\n\nSITE B arm_by_device replay block (replace session.py:215-221), keep it returning None on replay:\n  nonce = request.get(\"nonce\")\n  if (pub, nonce) in head.consumed_arm_nonces:      # snapshot-folded prefix\n      self._refuse_arm(request, \"replayed arm nonce\"); return None\n  for r in self.store.iter_records(since_seq=head.base_seq - 1):   # live tail only\n      p = r.payload\n      if (p.get(\"signal\")==SESSION_ARMED and p.get(\"armed_by\")==\"device\"\n              and p.get(\"device_pubkey\")==pub and p.get(\"nonce\")==nonce):\n          self._refuse_arm(request, \"replayed arm nonce\"); return None\n  # (fall through to step 5, single-live-session, unchanged)\n\nUnder Slice C's empty/identity snapshot: consumed_arm_nonces=∅, base_seq=0, since_seq=-1 → both sites re-scan every record exactly as today → byte-identical.",
        "edge_cases": "- Ordering: the consumed-nonce SET is order-INSENSITIVE (union commutative+idempotent); Site B's outcome is a boolean membership so ordering across the prune boundary is irrelevant. Site A's OUTPUT list order (oldest-first) comes purely from iterating live records in seq order — preserved because iter_records stays seq-ascending; the `armed` set never affects order. No tie-break exists.\n- Verified-only: the consumed marker is written ONLY after arm_by_device fully verifies (auth+freshness+sig); the set inherits that. The scan itself does no re-verify — it trusts the recorded SESSION_ARMED marker. Snapshot must fold the same trusted markers, no re-verification.\n- Owner arms and disarms carry no armed_by==\"device\" → never enter the set; must stay excluded in the fold predicate.\n- Empty-state: no device arms → set is ∅ → Site A returns all unconsumed cands, Site B never refuses on replay. Matches.\n- nonce/pubkey type fidelity (see fold_state): int-vs-str nonce must round-trip un-coerced or membership silently misses (a replay would be admitted, or a live cand wrongly filtered). None-valued tuples from malformed records must be retained, not dropped.\n- Idempotent submit_arm_request dedup and refusal ARM_REQUESTs don't touch the consumed set (wrong signal/decision) — irrelevant to this bearer.\n- Ordering across the prune boundary IS preserved for the set (order-free) and for Site A's cands list IF cands are handled correctly — see associativity_risk.",
        "associativity_risk": "The replay ledger itself (the consumed (device_pubkey,nonce) set) is TRULY split-exact: pure set-union, order-free, idempotent — safe under any prune, and the snapshot need carry ONLY that set. Site B (arm_by_device) is therefore fully safe.\n\nTHE REAL RISK is co-located in Site A: pending_device_arms computes a SECOND value at the same scan — the `cands` list of un-consumed ARM_REQUEST payloads — which is NOT a monotonic associative fold. A candidate is emitted while unconsumed and REMOVED once its (pubkey,nonce) later enters `armed` (a retract, not a monotone add). So if a prune deletes [0..K), an ARM_REQUEST at seq<K that is still UNCONSUMED would be silently dropped from the output — NOT byte-identical. (An already-consumed pruned ARM_REQUEST is fine: it wasn't in the output anyway, and its consumption is preserved in the folded set.)\n\nUnder Slice C's EMPTY snapshot this cannot bite (base_seq=0, all records live), so the rewrite is byte-identical today. But to make pending_device_arms exact under a REAL prune you MUST do ONE of:\n  (a) Give the snapshot a SECOND sub-state for this consumer: a QUEUE of the surviving-unconsumed ARM_REQUEST payload dicts from [0..K), ordered oldest-first (i.e. fold = append candidates, then delete any whose (pubkey,nonce) is later consumed — a last-write-wins/retract fold, order-sensitive, so the snapshot must carry the full ordered candidate payloads, not just a set). Then Site A prepends that queue (minus anything the live scan consumes) to the live-derived cands. This is a genuinely different, non-set fold and is where associativity breaks.\n  (b) Invoke the \"ARM_FRESHNESS << retention\" argument: since submit_arm_request refuses to even record an ARM_REQUEST older than ARM_FRESHNESS=30s (daemon.py:130), and arm_by_device re-rejects any ts older than 30s (session.py:211), any pruned ARM_REQUEST is far older than 30s and can NEVER successfully arm. This preserves SECURITY and downstream BEHAVIOR (no valid arm is lost) but is NOT byte-identical: pending_device_arms does NO freshness filter of its own, so today it still lists a stale-unconsumed candidate; option (b) would stop listing it. Acceptable ONLY if callers treat pending_device_arms output as \"candidates to re-verify\" (they do — the daemon consumer re-runs arm_by_device), in which case dropping provably-stale candidates is behavior-preserving even though it is not list-identical.\n\nRECOMMENDATION: fold the consumed-nonce set into the snapshot for BOTH sites (exact, cheap). Treat pending_device_arms' cands list as a SEPARATE bearer — either give it queue sub-state (a) for strict byte-identity, or document (b) as an accepted, freshness-justified equivalence. Do NOT claim the single set makes pending_device_arms byte-identical under a non-empty prune."
      },
      {
        "bearer": "sigil/governor/budget.py — BudgetLedger.spent() (scan loop) and its enforcement wrapper over_budget(), the per-UTC-day action/interrupt/token/cost meter. Query is per (agent, day_iso); over_budget compares each dimension >= its cap (at/over → deny). report() at :218 is a sibling scan with identical semantics (all-agents variant).",
        "file_line": "/home/kali/sigil/sigil/governor/budget.py:181 (the `for r in self.store.iter_records()` scan inside spent()); accumulator seeded at :179-180; over_budget wrapper at :200; twin scan report() at :218-240. Record shape: /home/kali/sigil/sigil/spine/models.py:26 (SpineRecord). Store iterator: /home/kali/sigil/sigil/spine/store.py:727 (iter_records, seq order across all segments). day_iso derivation: /home/kali/sigil/sigil/governor/core.py:34 (_utc_day → datetime.now(timezone.utc).date().isoformat()).",
        "current_logic": "spent(agent, day_iso) -> Spend. Init actions=interrupts=tokens=0, cost=0.0. Iterate EVERY record from genesis via self.store.iter_records() (since_seq=-1 → whole spine, seq order). Per record, apply four skip predicates in order: (1) if r.source != \"agent\" → skip; (2) if r.actor != agent → skip; (3) if not (r.ts or \"\").startswith(day_iso) → skip (wallclock UTC-date bucket, prefix match on the ISO date string); (4) if r.payload.get(\"decision\") not in (\"auto\",\"queued\") → skip (count only actions actually TAKEN — a denial/refusal record carries no usage and consumes no budget). For each surviving record: actions += 1; if r.kind == \"event\": interrupts += 1; u = r.payload.get(\"usage\"); if isinstance(u, dict): tokens += _as_int(u.get(\"input_tokens\")) + _as_int(u.get(\"output_tokens\")); cost += _as_float(u.get(\"cost_usd\")). Return Spend(actions, interrupts, tokens, cost_usd=cost). _as_int (budget.py:138) clamps each value to max(0,int(v)); junk/OverflowError → 0 (so a negative token count can't cancel real spend). _as_float (budget.py:152) → float(v) clamped to >=0.0 with NaN/±inf/OverflowError → 0.0. over_budget(agent, day_iso): if all four caps are None → (False,\"\"); else s = spent(...); return (True, reason) on the FIRST dimension where s.<dim> >= cap (actions, then interrupts, then tokens, then cost_usd), else (False,\"\"). Comparison is >= (at-cap is over).",
        "record_shapes": "SpineRecord (frozen dataclass, models.py:26). Attributes the scan reads: r.source (str; predicate == \"agent\"), r.actor (str; predicate == agent), r.ts (str, informational ISO-8601 UTC e.g. \"2026-07-19T12:34:56.789+00:00\"; None-safe via `r.ts or \"\"`; bucketed by .startswith(day_iso)), r.kind (str; only the literal \"event\" is distinguished, for interrupts), r.payload (dict). r.seq (int) is NOT read by the current scan but IS needed by the rewrite for the live-window filter. Not read: scope, parent_id, supersedes_id, cert_digest, prev_hash, entry_hash. payload keys read: payload[\"decision\"] (str; only \"auto\"/\"queued\" pass the gate; anything else incl. missing → skip); payload[\"usage\"] (dict, optional) with sub-keys \"input_tokens\", \"output_tokens\" (→ _as_int), \"cost_usd\" (→ _as_float). day_iso is always a full zero-padded \"YYYY-MM-DD\" (10 chars) from date().isoformat(), so the startswith prefix cannot alias a neighbouring day.",
        "fold_state": "PER THE STATED DESIGN: the budget bearer contributes NO sub-state to SnapshotState — its folded state is the identity Spend (actions=0, interrupts=0, tokens=0, cost_usd=0.0) for whatever (agent, day_iso) is queried. This is sound ONLY under the retention invariant: the prune boundary K is chosen so that the pruned prefix [0..K) contains NO record whose ts falls on the current UTC day — i.e. retention always keeps the entire current UTC day live. Then the enforcement query spent(agent, today) is fully determined by the live records and the snapshot adds nothing. If you instead want a general fold (needed to keep PAST-day report()/CLI queries exact after a real prune), the associative sub-state is a nested additive map budget: {agent(str): {utc_day(str): {actions:int, interrupts:int, tokens:int, cost_usd:float}}}, i.e. each pruned record's clamped contribution pre-bucketed by (actor, ts[:10]). Type: dict[str, dict[str, tuple[int,int,int,float]]]. I recommend the empty/no-fold design (matches the task) and treating the retention invariant as the correctness contract.",
        "fold_op": "Combine is component-wise addition with identity zero, per (agent, day) cell: (a1,i1,t1,c1) ⊕ (a2,i2,t2,c2) = (a1+a2, i1+i2, t1+t2, c1+c2); identity = (0,0,0,0.0); empty map = all-zero. Integer addition (actions/interrupts/tokens) is associative AND commutative, so the fold is trivially split-exact for those three dimensions. Under the no-fold design: SnapshotState.load(store) yields the identity Spend for (agent, day_iso); the consumer then re-runs the EXACT per-record filter+accumulate loop over the live records only, starting from that identity; the result equals the full scan because (a) under Slice C base_seq=0 so \"live records\" == all records (byte-identical by construction), and (b) under a real prune the retention invariant guarantees every record matching (agent, today) is live, so the pruned prefix's contribution to today's cell is exactly zero. load(snapshot).state(agent,day) + fold(live matching agent,day) == fold(empty,[0..T] matching agent,day).",
        "consumer_rewrite": "Keep the public signature spent(self, agent: str, day_iso: str) -> Spend and over_budget unchanged. Minimal edit: (1) head = SnapshotState.load(self.store) (or read the store's floor/head — spine/floor.py already carries base_seq/base_count); (2) seed = head.budget_spend(agent, day_iso) — the identity Spend(0,0,0,0.0) under the no-fold design; (3) replace `for r in self.store.iter_records():` with `for r in self.store.iter_records(since_seq=head.base_seq - 1):` (iter_records yields seq > since_seq, so since_seq=base_seq-1 yields seq >= base_seq = the live window [K..T]); (4) leave the four skip predicates and the accumulate block byte-for-byte identical, initialising the accumulators from `seed` (actions=seed.actions, interrupts=seed.interrupts, tokens=seed.tokens, cost=seed.cost_usd) instead of 0. Under Slice C: base_seq=0 → since_seq=-1 → full scan, and seed is the zero Spend → byte-identical to current code. Apply the identical rewrite to report() (:218) if you want it snapshot-aware; over_budget needs no change (it only calls spent).",
        "edge_cases": "(1) UTC-day bucket is a STRING PREFIX match `(r.ts or \"\").startswith(day_iso)`, not a parsed-date comparison; day_iso is a full 10-char YYYY-MM-DD so no cross-day aliasing; a record with empty/None ts never matches a real day query. (2) The decision gate counts only payload.decision in {\"auto\",\"queued\"} — refusals/denials (no usage) never advance any meter, so pruning them is a no-op anyway. (3) _as_int/_as_float clamp per-record BEFORE summing (negatives→0, NaN/±inf→0.0, OverflowError→0) — these are per-record transforms, so summing clamped values is stable under any grouping; a folded prefix must store the ALREADY-CLAMPED sums to match. (4) CURRENT-DAY enforcement (over_budget, always _utc_day()) is exact under the no-fold design given the retention invariant. PAST-DAY report()/CLI (`sigil budget --day X`) and dashboard.snapshot CAN query a historical day; under the no-fold design a report for a day that later got pruned would under-count (return only the live remnant). Under Slice C (empty snapshot / no prune) ALL queries are byte-identical; the past-day divergence only arises once a real prune ships and is the accepted semantic of a live meter — flag it if past-day historical reporting must stay exact (then use the additive per-(agent,day) fold instead). (5) Ordering across the prune boundary is preserved for the int dimensions regardless (addition commutes); for cost see associativity_risk.",
        "associativity_risk": "Order-INSENSITIVE. The fold is pure count-add (actions/interrupts/tokens) and float-sum (cost) with identity 0 — commutative and associative, with NO last-write-wins, NO latch, NO tie-break, so the snapshot need carry no sequencing information for correctness of the counts. The one genuine hazard is FLOATING-POINT non-associativity of the cost dimension: the scan sums per-record cost_usd left-to-right in seq order, and (folded_prefix_sum) + (live_seqordered_sum) can differ from sum(all_seqordered) in the last ULP. The no-fold design AVOIDS this entirely: the current-day query sums cost only over live records in seq order, reproducing the scan's exact grouping → byte-identical. If a real per-(agent,day) cost fold is ever introduced, it would NOT be guaranteed byte-identical for cost and should be documented as an at-most-1-ULP divergence (or store cost as scaled integer micro-dollars to make it exact). CORRECTNESS CONTRACT to assert in retention: prune boundary K must satisfy — every record with seq < K has ts strictly before 00:00:00Z of the current UTC day (equivalently ts[:10] < today), so no record matching the current-day enforcement bucket is ever in the pruned prefix. Under Slice C this holds vacuously (K=0, empty snapshot)."
      },
      {
        "bearer": "sigil/cli.py cmd_warden_anchor_get — `sigil warden-anchor-get <pubkey>` prints the highest WARDEN action-log head-count ever anchored FOR ONE pubkey as JSON {count, head_hash}. It is a monotonic anti-rollback high-water: a full genesis-to-tip scan taking MAX(count) per pubkey plus the head_hash of the last record at that max. Same family as the head/checkpoint verifier but computes a SEMANTIC payload aggregate the head snapshot does not carry.",
        "file_line": "/home/kali/sigil/sigil/cli.py:283 (def cmd_warden_anchor_get); the scan loop is lines 299-305; record producer is cmd_warden_anchor_set at lines 267-280 (store.append kind=\"warden_checkpoint\").",
        "current_logic": "Exact sequence: (1) store = SpineStore(). (2) ok,msg = store.verify() — if not ok, print REJECTED and sys.exit(2) (chain-integrity gate, unrelated to the aggregate). (3) hok,hmsg = verify_checkpoint(store) — if not hok AND \"TAMPER\" in hmsg.upper(), print REJECTED and sys.exit(2) (signed-head gate). (4) Initialize best_count=0, best_hash=\"\". (5) for r in store.iter_records():  (iter_records yields every record with seq ascending, across all sealed+active segments as one contiguous chain — confirmed store.py:727-766). (6) Filter predicate: `r.kind == \"warden_checkpoint\" AND r.payload.get(\"pubkey\") == a.pubkey` (the query pubkey; positional required arg, argparse cli.py:632-633). No filter on source, actor, ts, or any verified flag. (7) c = int(r.payload.get(\"count\", 0)). (8) Tie/select: `if c >= best_count: best_count, best_hash = c, r.payload.get(\"head_hash\", \"\")`. The comparator is `>=`, so on a tie at the running max the LATER (higher-seq) record OVERWRITES best_hash. best_count starts at 0, so it is effectively max(0, max-count-seen): a negative count never registers. (9) print(json.dumps({\"count\": best_count, \"head_hash\": best_hash})). Net result: best_count = max(0, max over matching records of int(count)); best_hash = head_hash of the HIGHEST-SEQ matching record whose int(count) == best_count, or \"\" if no matching record reached a count >= 0.",
        "record_shapes": "record.kind == \"warden_checkpoint\" (the only kind read). payload fields consumed: `pubkey` (str, the scope key — equality-filtered against the query arg; produced by warden-anchor-set), `count` (int WARDEN action-log high-water; setter writes int(a.count); read as int(payload.get(\"count\",0))), `head_hash` (str WARDEN head hash; read as payload.get(\"head_hash\",\"\")). Record attributes used: `seq` (int) — the iteration and tie-break order (iter_records is strictly seq-ascending); `kind`; `payload`. NOT used: source (always \"warden\"), actor (always \"warden\"), ts, cert_digest/prev_hash/entry_hash, parent_id, supersedes_id. No `verified`/status field exists on the aggregate path.",
        "fold_state": "Per-bearer sub-state `warden_best`: a map keyed by pubkey → a 3-tuple (max_count:int, head_hash:str, tiebreak_seq:int). Shape: dict[str, tuple[int,str,int]]. Identity / empty-snapshot value = {} (empty dict). A pubkey ABSENT from the map is semantically (max_count=0, head_hash=\"\", tiebreak_seq=-1) — matching the scan's initial best_count=0/best_hash=\"\". tiebreak_seq stores the seq of the record that supplied head_hash (i.e. the highest-seq record at max_count for that pubkey); it exists purely so a general A⊕B combine can break count-ties deterministically. Keyed by ALL pubkeys (the fold runs before the query pubkey is known), whereas the scan filters to one; load then selects a.pubkey.",
        "fold_op": "Per-record transition (folding a live warden_checkpoint record r in seq-ascending order): let p=r.payload, k=p.get(\"pubkey\"), c=int(p.get(\"count\",0)), h=p.get(\"head_hash\",\"\"), s=r.seq; cur=state.get(k,(0,\"\",-1)); if c >= cur.max_count: state[k]=(c,h,s). This is byte-identical to `if c >= best_count: best_count,best_hash = c,h` because the transition, identity (0,\"\"), and `>=` tie-rule are the same. Non-warden_checkpoint records are ignored (identity on kind). Associative monoid combine of two per-pubkey sub-states A (seq-earlier segment) and B (seq-later segment): argmax lexicographically by (max_count, tiebreak_seq) — higher max_count wins; on equal max_count the higher tiebreak_seq (the later record) wins, so B's head_hash wins a tie (B is entirely later in seq and always holds a record at that max_count). Identity element = (0,\"\",-1) per key, {} across keys. To reproduce the scan: st = SnapshotState.load(store).warden_best; seed (best_count,best_hash) = (st.get(a.pubkey) or (0,\"\",-1))[:2]; then apply the transition over the live records in seq order. Under the empty Slice-C snapshot st={}, the seed is (0,\"\") and the fold IS the current scan verbatim.",
        "consumer_rewrite": "Keep the two integrity gates (store.verify() and verify_checkpoint(store)) exactly as-is; replace only the accumulator seed. Minimal edit:\n```\nst = SnapshotState.load(store)                       # folded state of pruned prefix [0..base_seq)\nbest_count, best_hash, _ = st.warden_best.get(a.pubkey, (0, \"\", -1))\nfor r in store.iter_records():                        # live window only [base_seq..T]\n    if r.kind == \"warden_checkpoint\" and r.payload.get(\"pubkey\") == a.pubkey:\n        c = int(r.payload.get(\"count\", 0))\n        if c >= best_count:\n            best_count, best_hash = c, r.payload.get(\"head_hash\", \"\")\nprint(_json.dumps({\"count\": best_count, \"head_hash\": best_hash}))\n```\nPublic signature unchanged: still one positional pubkey arg, still prints the same {\"count\":int,\"head_hash\":str} JSON, still exits 2 on the two integrity gates. iter_records already yields only the live window (records with seq >= base_seq), so no explicit seq>=head.base_seq filter is needed; the pruned prefix arrives solely via the seeded st.warden_best.get(a.pubkey,...). Under Slice C (empty snapshot) the seed is (0,\"\") → byte-identical to today.",
        "edge_cases": "ORDERING-SENSITIVE (yes): the count aggregate is pure MAX (order-free), but head_hash is LAST-WRITE-WINS tied to max_count via `>=`, so seq order matters. iter_records guarantees strict seq-ascending order, and every pruned record has seq < base_seq <= every live seq, so a live record at the same max_count ALWAYS post-dates the snapshot and correctly overrides the tie under plain `>=` — ordering across the prune boundary is preserved. TIE-BREAK: equal count → the HIGHEST-SEQ record's head_hash wins (the fold's tiebreak_seq/`>=` both encode this). VERIFIED-ONLY: no — the scan reads EVERY warden_checkpoint regardless of source/actor/verification; the fold must include all of them (no verified filter). ZERO-FLOOR: best_count initialized 0 with `>=` means a pubkey whose records are ALL negative-count yields {count:0,head_hash:\"\"}; the fold identity max_count=0 reproduces this (negative c never satisfies c>=0). COUNT==0: a count-0 record satisfies 0>=0 and DOES set head_hash, so a pubkey with only count-0 checkpoints returns the last count-0 record's head_hash — fold identity (0,\"\") + `>=` reproduces it. COERCION: replicate int(payload.get(\"count\",0)) (missing count→0) and payload.get(\"head_hash\",\"\") (missing→\"\") exactly. EMPTY-STATE / no matching record: {count:0,head_hash:\"\"}. MISSING pubkey field: payload.get(\"pubkey\")→None ≠ any real query pubkey → excluded (fold keys it under None, harmless). Query pubkey scoping is per-key, so a key rotation naturally starts fresh (matches the docstring intent).",
        "associativity_risk": "Needs its OWN fold — it does NOT ride the head snapshot. The SignedChainHead / classify_head \"warden anchor\" folds the pruned prefix only for CHAIN INTEGRITY: base_seq (live-window left edge), base_count, base_prev_hash, cumulative_merkle_root, prev_head_hash. None of those carry the per-pubkey MAX(count) or the tied head_hash. If cmd_warden_anchor_get were rewired to scan only live records [base_seq..T] WITHOUT a dedicated sub-state, a pruned checkpoint holding a pubkey's high-water would silently vanish and best_count would DROP — precisely the \"spine tail-truncation removing recent anchors silently lowers the high-water\" rollback this command exists to reject. So SnapshotState must carry warden_best per pubkey. TRUE ASSOCIATIVITY: the count component is a clean commutative-associative MAX. The head_hash component is last-write-wins, associative ONLY as an ordered fold: the snapshot must carry, per pubkey, (max_count, head_hash-of-last-record-at-max, tiebreak_seq). Because every pruned seq < every live seq, for the load-then-fold-forward pattern the plain `>=` transition already gives live ties priority, so (max_count, head_hash) alone is sufficient and byte-identical for Slice C; tiebreak_seq is required only if two snapshot sub-states are ever combined out of linear seq order (a general A⊕B monoid merge of two pruned segments), where the tie is broken by max(tiebreak_seq). Ordering across the prune boundary is preserved by construction (pruned seqs strictly precede live seqs). With Slice C shipping an EMPTY snapshot (warden_best={}), the seed is (0,\"\") and the fold degenerates to the exact current genesis-to-tip scan."
      },
      {
        "bearer": "sigil/bridge/daemon.py submit_device_approval — device-approval replay/idempotency dedup (the census EXTRA). Full-spine scan that returns the seq of the first previously-recorded record carrying the same (signal=\"governor.approval\", pubkey, sig) triple, else appends a new device-approval record. Its comment (lines 82-86) claims the FULL scan is what keeps replay-bloat bounded to the number of DISTINCT captured bodies forever (approvals carry no freshness field, so a bounded window would re-open a bloat sink).",
        "file_line": "/home/kali/sigil/sigil/bridge/daemon.py:87 (the dedup scan loop); method submit_device_approval spans lines 65-92. Helpers: SIGNAL=\"governor.approval\" and verify_approval in /home/kali/sigil/sigil/agents/approvals.py (lines 24, 33-50); the scan engine SpineStore.iter_records in /home/kali/sigil/sigil/spine/store.py:727-759; record shape SpineRecord in /home/kali/sigil/sigil/spine/models.py:25-50.",
        "current_logic": "Step by step, exactly what the scan at lines 87-90 computes:\n1. PRE-SCAN GUARDS (not part of the fold, but they pin the query key): line 75 raises unless payload[\"signal\"]==APPROVAL_SIGNAL (\"governor.approval\"); line 77 raises unless payload[\"pubkey\"] is in the authorized-device set self._authorized(); line 79 raises unless verify_approval(payload,...) passes — and verify_approval (approvals.py:47) returns False unless BOTH sig and pubkey are truthy. So by scan time the query key (QUERY_PUBKEY=payload.get(\"pubkey\"), QUERY_SIG=payload.get(\"sig\")) are both non-empty strings, and QUERY_PUBKEY is an owner-minted authorized device pubkey.\n2. sig, tgt = payload.get(\"sig\"), payload.get(\"target_seq\")  (line 81). sig == QUERY_SIG.\n3. THE SCAN (line 87): iterate self.store.iter_records() with default since_seq=-1 → a FULL scan of the whole spine, across all segments (sealed then active) as one contiguous chain, in STRICTLY ASCENDING seq order (iter_records only yields rec.seq > yielded_upto; malformed/torn lines are skipped, never raising here in Slice-C's single active segment).\n4. For each record r, read p = r.payload, and test the match predicate — ALL THREE must hold: p.get(\"signal\") == \"governor.approval\"  AND  p.get(\"pubkey\") == QUERY_PUBKEY  AND  p.get(\"sig\") == QUERY_SIG. Pure string/None equality; NO signature re-verification of historical records; NO filter on r.kind / r.source / r.actor / r.ts.\n5. TIE-BREAK / RETURN: on the FIRST matching record it returns r.seq (line 90) and stops. Because iteration is strictly ascending and seq is globally unique (hash chain), \"first match\" == the record with the MINIMUM seq among all records matching the triple. That returned seq is the idempotent answer (the seq of the already-recorded approval).\n6. NO MATCH after the full scan → line 91 appends a NEW record: kind=\"event\", source=\"mesh\", actor=\"DEVICE\", payload={**payload, \"tier\":\"A0\", \"decision\":\"auto\"}, supersedes_id=tgt (=target_seq); returns the newly-minted seq (int).\nNet: return = min{ r.seq : r.payload matches (signal,pubkey,sig) } if any exists, else store.append(...). Return type is always int.",
        "record_shapes": "Record attributes the scan reads: r.seq (int) and r.payload (dict) ONLY — r.kind, r.source, r.actor, r.ts, r.parent_id, r.supersedes_id are NOT read by the dedup. Payload fields in the predicate: \"signal\" (str; must == \"governor.approval\"), \"pubkey\" (str, base64 Ed25519 device/owner pubkey), \"sig\" (str, base64 Ed25519 signature over canonical {target,decision,approver}).\nThe scan's UNIVERSE (records that can carry signal==\"governor.approval\") is emitted by THREE writers, and the scan admits ALL of them because it never filters on kind/source/actor:\n (a) THIS method, bridge/daemon.py:91 — kind=\"event\", source=\"mesh\", actor=\"DEVICE\", payload={**incoming, tier:\"A0\", decision:\"auto\"} where incoming has {signal, approval, target_seq, approver, pubkey(=device), sig, msg_digest, ...};\n (b) agents/approvals.py:102 ApprovalQueue._decide (OWNER approvals) — source=\"governor\", actor=\"OWNER\", payload {signal, approval, target_seq, target_kind, target_tier, approver, reason, pubkey(=OWNER), sig, msg_digest, tier, decision};\n (c) mesh/registry.py:93 DeviceApprover._decide — source=\"mesh\", actor=\"DEVICE\", payload {signal, approval, target_seq, approver, reason, pubkey(=device), sig, msg_digest, device:True, tier, decision}.\nBecause Ed25519 signing is deterministic, the SAME (target_seq,decision,approver) signed by the SAME device key yields the SAME sig, so a device approval first written via (c) and later resubmitted via (a) collapses — which is exactly why the universe must span (a),(b),(c), not just this method's own appends. (Owner records (b) simply never match a device QUERY_PUBKEY, but the scan still iterates them.)",
        "fold_state": "A per-key first-seq MAP, not a bare set (the return value is a seq, so membership alone is insufficient):\n  device_approval_dedup: Dict[Tuple[Optional[str], Optional[str]], int]\nkeyed by (pubkey, sig) == (p.get(\"pubkey\"), p.get(\"sig\")) taken verbatim (including None if a field is absent), value = the MINIMUM seq (int) among records with that key.\nBuilt over ONLY records whose payload.get(\"signal\") == \"governor.approval\" (the scan's exact universe — do NOT restrict by kind/source/actor). In the SnapshotState this is the folded map of the pruned prefix [0..K); Slice C ships an EMPTY prune, so the snapshot value is the identity {} (empty map).",
        "fold_op": "Combine op = per-key MINIMUM over seq (a keep-first / lowest-seq-wins semilattice). Identity element = {} (empty map).\ncombine(A, B) = { k: min(A.get(k, +inf), B.get(k, +inf)) for k in keys(A) ∪ keys(B) }.\nFolding a stream of records into state S: for each record r with p=r.payload and p.get(\"signal\")==\"governor.approval\", let k=(p.get(\"pubkey\"), p.get(\"sig\")); S[k] = min(S.get(k, +inf), r.seq).\nReproduction: st = SnapshotState.load(store).device_approval_dedup gives fold({}, [0..K)); then fold st forward over live [K..T]. Because min is associative AND commutative, fold(fold({}, [0..K)), [K..T]) == fold({}, [0..T]) == { key: min-seq }, and a lookup at (QUERY_PUBKEY, QUERY_SIG) returns exactly the current scan's first-match seq (or absent → append). Under the empty Slice-C snapshot, st={} and base_seq=0, so the fold degenerates to fold({}, [0..T]) — byte-identical to the current full scan.",
        "consumer_rewrite": "Minimal edit: replace ONLY the scan loop at lines 87-90 (leave guards 75-81 and the append at 91-92 untouched; public signature `submit_device_approval(self, payload: dict) -> int` and int return unchanged).\n\n  head = SnapshotState.head(self.store)              # base_seq K = 0 in Slice C\n  st = dict(SnapshotState.load(self.store).device_approval_dedup)   # {(pubkey,sig): min_seq} of [0..K)\n  # fold live records seq >= K forward with the SAME min-combine\n  for r in self.store.iter_records(since_seq=head.base_seq - 1):    # since_seq=K-1 → yields seq >= K; K=0 → full scan\n      p = r.payload\n      if p.get(\"signal\") == APPROVAL_SIGNAL:                         # SAME filter as the scan; NO kind/source/actor filter\n          k = (p.get(\"pubkey\"), p.get(\"sig\"))\n          cur = st.get(k)\n          if cur is None or r.seq < cur:                            # MIN — keep the earliest (matches first-in-ascending)\n              st[k] = r.seq\n  hit = st.get((payload.get(\"pubkey\"), sig))                        # sig == payload.get(\"sig\")\n  if hit is not None:\n      return hit                                                    # idempotent — same int the scan returned\n  return self.store.append(kind=\"event\", source=\"mesh\", actor=\"DEVICE\",\n                           payload={**payload, \"tier\": \"A0\", \"decision\": \"auto\"}, supersedes_id=tgt)\n\nNotes: keep it OUTSIDE the append lock (as today — the comment at 82 says so). Keep filtering ONLY on payload[\"signal\"]. Do NOT re-verify historical records. The lookup key is the incoming (pubkey, sig) exactly as at line 89.",
        "edge_cases": "• Tie-break: seqs are globally unique (hash chain), so there are no real ties; \"first record in ascending iter_records order\" is identically the MINIMUM seq — the fold MUST take min, and min reproduces it exactly.\n• Ordering sensitivity: NONE for the final map (min is order-independent), BUT the RETURN VALUE is order-sensitive in the sense that it must be the EARLIEST duplicate's seq — so the combine must be min, never last-write/overwrite. Storing last-write would return the latest duplicate's seq and diverge from the scan.\n• Ordering across the prune boundary: PRESERVED — a key present in both [0..K) (snapshot) and live keeps the snapshot's (earlier) seq via min, exactly as the full scan (which scans [0..K) first) would return the earliest occurrence.\n• verified-only vs any: the scan does a PURE string compare on historical records (no re-verification); the fold must likewise index every signal-matching record by (pubkey,sig) without verifying. (Only the INCOMING request is verified, before the scan — unchanged.)\n• Universe breadth: MUST include owner (ApprovalQueue) and DeviceApprover records, not just this method's appends — the scan has no kind/source/actor filter. Narrowing to actor==\"DEVICE\"/source==\"mesh\" is an UNSOUND optimization (see associativity_risk).\n• None components: a signal-matching record missing pubkey or sig yields a key with a None element; harmless and must be kept for exactness (a real query always has non-None pubkey+sig via verify_approval, so it never hits a None key, but including them keeps fold==scan).\n• Empty-state: no matching record → scan appends and returns the new seq; fold lookup returns None → append. Identical.\n• Empty snapshot (Slice C): base_seq=0, st={}, since_seq=-1 → fold over [0..T] == the current full scan, byte-identical.",
        "associativity_risk": "TRULY associative AND commutative — the accumulator is a per-key MIN over seq (a bounded semilattice), and min is associative+commutative with identity +inf, so fold(fold({},[0..K)),[K..T]) == fold({},[0..T]) regardless of fold order or the prune point. The state is therefore NOT order-sensitive.\nThe ONE thing the snapshot MUST carry to stay exact: for EACH distinct (pubkey, sig) key seen in [0..K) among signal==\"governor.approval\" records, the MINIMUM seq (the earliest occurrence) AND that key's presence. Two failure modes that would silently diverge from the current scan:\n (1) Storing last-write/overwrite (or max) instead of min → the idempotent return would become the LATEST duplicate's seq, not the earliest the scan returns.\n (2) Storing membership-only (a set, per the design's phrasing \"consumed device-approval id/nonce set\") → loses the seq, and the method's return value (the recorded seq) can no longer be reproduced; you MUST store the seq, so it is a {(pubkey,sig): min_seq} MAP, not a set.\nNo timestamp/sequence ordering beyond the per-key min is needed; genesis→K and K→T fold in any order. Also: the fold universe must equal the scan's (all signal==\"governor.approval\" records, any kind/source/actor) — restricting it more narrowly than the scan predicate is the only other way to break byte-identity (a device query would still be safe in practice because owner records never share a device (pubkey,sig), but the scan does not rely on that, so neither may the fold)."
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "FoldSpec"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "foldspec:nonce_highwater",
      "phaseIndex": 1,
      "phaseTitle": "FoldSpec",
      "agentId": "a6ffb076185f903eb",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784498961100,
      "queuedAt": 1784498947696,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "sigil/bridge/envelope.py device_nonce_highwater(store, devi…",
      "promptPreview": "SIGIL hard-prune state-folding (Slice C). A prune deletes records [0..K) from the live spine but keeps\na signed \"snapshot\" record committing a folded summary of that pruned prefix. Every consumer that currently\nscans ALL records from genesis to compute a MONOTONIC security state must instead: st = SnapshotState.load(store)\n(the folded state of [0..K)); then fold st forward over the LIVE records [K…",
      "lastProgressAt": 1784499169492,
      "tokens": 69557,
      "toolCalls": 8,
      "durationMs": 208392,
      "resultPreview": "{\"bearer\":\"sigil/bridge/envelope.py device_nonce_highwater(store, device_pubkey) -> int — per-device monotonic nonce highwater used as the effectful-request replay floor (consume() rejects nonce <= highwater). Sole producer of the receipts it scans is record_receipt() in the same file.\",\"file_line\":\"/home/kali/sigil/sigil/bridge/envelope.py:88-104 (def at 88; the full-scan loop body is lines 92-10…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "foldspec:killswitch_latch",
      "phaseIndex": 1,
      "phaseTitle": "FoldSpec",
      "agentId": "a93b51e572b8b72cf",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784498960441,
      "queuedAt": 1784498947696,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "KillSwitch._scan_engaged — the governor kill-switch latch (…",
      "promptPreview": "SIGIL hard-prune state-folding (Slice C). A prune deletes records [0..K) from the live spine but keeps\na signed \"snapshot\" record committing a folded summary of that pruned prefix. Every consumer that currently\nscans ALL records from genesis to compute a MONOTONIC security state must instead: st = SnapshotState.load(store)\n(the folded state of [0..K)); then fold st forward over the LIVE records [K…",
      "lastProgressAt": 1784499166257,
      "tokens": 43944,
      "toolCalls": 10,
      "durationMs": 205815,
      "resultPreview": "{\"bearer\":\"KillSwitch._scan_engaged — the governor kill-switch latch (a single boolean that halts the agent mesh while leaving A0 observe alive). Called by KillSwitch.is_engaged() (which caches the verdict keyed on the store's rotation-aware change_token + trusted_pubkey) and consulted on every governor dispatch decision.\",\"file_line\":\"/home/kali/sigil/sigil/governor/killswitch.py:70-83 (def _scan…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "foldspec:creation_cap",
      "phaseIndex": 1,
      "phaseTitle": "FoldSpec",
      "agentId": "a96160112c0a1a5dd",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784498960560,
      "queuedAt": 1784498947696,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "sigil/agents/actor_scope.py — ActorScope.creation_allowed(s…",
      "promptPreview": "SIGIL hard-prune state-folding (Slice C). A prune deletes records [0..K) from the live spine but keeps\na signed \"snapshot\" record committing a folded summary of that pruned prefix. Every consumer that currently\nscans ALL records from genesis to compute a MONOTONIC security state must instead: st = SnapshotState.load(store)\n(the folded state of [0..K)); then fold st forward over the LIVE records [K…",
      "lastProgressAt": 1784499230065,
      "tokens": 51378,
      "toolCalls": 13,
      "durationMs": 269505,
      "resultPreview": "{\"bearer\":\"sigil/agents/actor_scope.py — ActorScope.creation_allowed(store, service, url=\\\"\\\"): the DELEGATE account-creation cap, a per-service-label AND per-origin monotonic count of applied `account.create` steps, enforced at both preview (actor.py:139) and execute (actor.py:247). Returns True while n < creation_cap (default 1).\",\"file_line\":\"/home/kali/sigil/sigil/agents/actor_scope.py:44-56 (…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "foldspec:mesh_capability",
      "phaseIndex": 1,
      "phaseTitle": "FoldSpec",
      "agentId": "a4b318e5a4462eb7f",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784498965259,
      "queuedAt": 1784498947696,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "sigil/mesh/registry.py:42 capability_map(store, trusted_pub…",
      "promptPreview": "SIGIL hard-prune state-folding (Slice C). A prune deletes records [0..K) from the live spine but keeps\na signed \"snapshot\" record committing a folded summary of that pruned prefix. Every consumer that currently\nscans ALL records from genesis to compute a MONOTONIC security state must instead: st = SnapshotState.load(store)\n(the folded state of [0..K)); then fold st forward over the LIVE records [K…",
      "lastProgressAt": 1784499176470,
      "tokens": 60393,
      "toolCalls": 8,
      "durationMs": 211210,
      "resultPreview": "{\"bearer\":\"sigil/mesh/registry.py:42 capability_map(store, trusted_pubkey=None) -> dict — the \\\"latest VERIFIED (owner-signed) capability per host_id\\\" map. Task-named at line 46 (the scan loop). Full-spine god-node consumer: {host_id: cap_dict} where cap_dict is the owner-signed descriptor from the HIGHEST-seq verified advertisement for that host_id.\",\"file_line\":\"/home/kali/sigil/sigil/mesh/regi…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "foldspec:mesh_authorized",
      "phaseIndex": 1,
      "phaseTitle": "FoldSpec",
      "agentId": "ab9824a85de03f5b8",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784498960478,
      "queuedAt": 1784498947696,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "sigil/mesh/registry.py authorized_devices — set of currentl…",
      "promptPreview": "SIGIL hard-prune state-folding (Slice C). A prune deletes records [0..K) from the live spine but keeps\na signed \"snapshot\" record committing a folded summary of that pruned prefix. Every consumer that currently\nscans ALL records from genesis to compute a MONOTONIC security state must instead: st = SnapshotState.load(store)\n(the folded state of [0..K)); then fold st forward over the LIVE records [K…",
      "lastProgressAt": 1784499341498,
      "tokens": 58759,
      "toolCalls": 15,
      "durationMs": 381020,
      "resultPreview": "{\"bearer\":\"sigil/mesh/registry.py authorized_devices — set of currently-authorized device pubkeys (owner-signed authorize minus later revoke), latest-verified state per key\",\"file_line\":\"/home/kali/sigil/sigil/mesh/registry.py:66-76 (scan loop at line 71: for r in store.iter_records():)\",\"current_logic\":\"Exact steps of authorized_devices(store, trusted_pubkey=None) -> Set[str]:\\n1. Resolve trust a…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "foldspec:promotion",
      "phaseIndex": 1,
      "phaseTitle": "FoldSpec",
      "agentId": "ac0dadedf5b13606f",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784498961010,
      "queuedAt": 1784498947696,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "sigil/governor/promotion.py — PromotionPolicy.is_promoted(a…",
      "promptPreview": "SIGIL hard-prune state-folding (Slice C). A prune deletes records [0..K) from the live spine but keeps\na signed \"snapshot\" record committing a folded summary of that pruned prefix. Every consumer that currently\nscans ALL records from genesis to compute a MONOTONIC security state must instead: st = SnapshotState.load(store)\n(the folded state of [0..K)); then fold st forward over the LIVE records [K…",
      "lastProgressAt": 1784499141521,
      "tokens": 36195,
      "toolCalls": 9,
      "durationMs": 180511,
      "resultPreview": "{\"bearer\":\"sigil/governor/promotion.py — PromotionPolicy.is_promoted(agent, scope=\\\"*\\\") -> bool. Answers \\\"may this (agent, scope) auto-approve its A2 records?\\\" as the LATEST owner-signature-VERIFIED grant/revoke decision for that (agent, scope), evaluated over the whole spine at K-1. This is a per-key LAST-WRITE-WINS map ({(agent,scope): \\\"granted\\\"|\\\"revoked\\\"}), NOT a monotone latch — a later…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "foldspec:approvals_pending",
      "phaseIndex": 1,
      "phaseTitle": "FoldSpec",
      "agentId": "a7f47eec4d062c4b9",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784498962131,
      "queuedAt": 1784498947697,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "sigil/agents/approvals.py :: pending() — \"queued proposals …",
      "promptPreview": "SIGIL hard-prune state-folding (Slice C). A prune deletes records [0..K) from the live spine but keeps\na signed \"snapshot\" record committing a folded summary of that pruned prefix. Every consumer that currently\nscans ALL records from genesis to compute a MONOTONIC security state must instead: st = SnapshotState.load(store)\n(the folded state of [0..K)); then fold st forward over the LIVE records [K…",
      "lastProgressAt": 1784499228504,
      "tokens": 68601,
      "toolCalls": 9,
      "durationMs": 266373,
      "resultPreview": "{\"bearer\":\"sigil/agents/approvals.py :: pending() — \\\"queued proposals with no VERIFIED owner/authorized-device approval yet, oldest first\\\". A set-difference between the queued-items map and the resolved-target set, computed in one full scan of the spine.\",\"file_line\":\"/home/kali/sigil/sigil/agents/approvals.py:53 (function `pending`); the full scan is the loop at lines 62-68. Helpers it depends …"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "foldspec:archivist_currentview",
      "phaseIndex": 1,
      "phaseTitle": "FoldSpec",
      "agentId": "a43f4ec36a80e7ea9",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784498961094,
      "queuedAt": 1784498947697,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "sigil/consolidate/revise.py — the archivist current-view + …",
      "promptPreview": "SIGIL hard-prune state-folding (Slice C). A prune deletes records [0..K) from the live spine but keeps\na signed \"snapshot\" record committing a folded summary of that pruned prefix. Every consumer that currently\nscans ALL records from genesis to compute a MONOTONIC security state must instead: st = SnapshotState.load(store)\n(the folded state of [0..K)); then fold st forward over the LIVE records [K…",
      "lastProgressAt": 1784499351108,
      "tokens": 72963,
      "toolCalls": 11,
      "durationMs": 390013,
      "resultPreview": "{\"bearer\":\"sigil/consolidate/revise.py — the archivist current-view + promotion-ledger bearer: consolidation_records (L14), iter_current (L21), promotion_ledgers (L28). Feeds consolidate/queries.py _grounded_current → open_threads/due_commitments/pending_contradictions (and promote.py's idempotency check). All three do a full genesis→T scan of store.iter_records() filtered to source==\\\"archivist\\\"…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "foldspec:gesture_arm_ledger",
      "phaseIndex": 1,
      "phaseTitle": "FoldSpec",
      "agentId": "abaae93621d3ea703",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784498960498,
      "queuedAt": 1784498947697,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "device-arm (HID-injection) nonce replay ledger — the consum…",
      "promptPreview": "SIGIL hard-prune state-folding (Slice C). A prune deletes records [0..K) from the live spine but keeps\na signed \"snapshot\" record committing a folded summary of that pruned prefix. Every consumer that currently\nscans ALL records from genesis to compute a MONOTONIC security state must instead: st = SnapshotState.load(store)\n(the folded state of [0..K)); then fold st forward over the LIVE records [K…",
      "lastProgressAt": 1784499207728,
      "tokens": 50247,
      "toolCalls": 8,
      "durationMs": 247230,
      "resultPreview": "{\"bearer\":\"device-arm (HID-injection) nonce replay ledger — the consumed (device_pubkey, nonce) set read by BOTH pending_device_arms (the `armed` set, session.py:54-63) and arm_by_device's replay guard (session.py:214-221).\",\"file_line\":\"/home/kali/sigil/sigil/gesture/session.py:56 (pending_device_arms full scan) AND /home/kali/sigil/sigil/gesture/session.py:216 (arm_by_device replay-check full sc…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "foldspec:budget_daycap",
      "phaseIndex": 1,
      "phaseTitle": "FoldSpec",
      "agentId": "a945b7e80662bd383",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784498961167,
      "queuedAt": 1784498947697,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "sigil/governor/budget.py — BudgetLedger.spent() (scan loop)…",
      "promptPreview": "SIGIL hard-prune state-folding (Slice C). A prune deletes records [0..K) from the live spine but keeps\na signed \"snapshot\" record committing a folded summary of that pruned prefix. Every consumer that currently\nscans ALL records from genesis to compute a MONOTONIC security state must instead: st = SnapshotState.load(store)\n(the folded state of [0..K)); then fold st forward over the LIVE records [K…",
      "lastProgressAt": 1784499201058,
      "tokens": 49920,
      "toolCalls": 12,
      "durationMs": 239891,
      "resultPreview": "{\"bearer\":\"sigil/governor/budget.py — BudgetLedger.spent() (scan loop) and its enforcement wrapper over_budget(), the per-UTC-day action/interrupt/token/cost meter. Query is per (agent, day_iso); over_budget compares each dimension >= its cap (at/over → deny). report() at :218 is a sibling scan with identical semantics (all-agents variant).\",\"file_line\":\"/home/kali/sigil/sigil/governor/budget.py:1…"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "foldspec:warden_anchor_cli",
      "phaseIndex": 1,
      "phaseTitle": "FoldSpec",
      "agentId": "a1c248b82b740fbdc",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784498960062,
      "queuedAt": 1784498947697,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "sigil/cli.py cmd_warden_anchor_get — `sigil warden-anchor-g…",
      "promptPreview": "SIGIL hard-prune state-folding (Slice C). A prune deletes records [0..K) from the live spine but keeps\na signed \"snapshot\" record committing a folded summary of that pruned prefix. Every consumer that currently\nscans ALL records from genesis to compute a MONOTONIC security state must instead: st = SnapshotState.load(store)\n(the folded state of [0..K)); then fold st forward over the LIVE records [K…",
      "lastProgressAt": 1784499241783,
      "tokens": 63035,
      "toolCalls": 14,
      "durationMs": 281721,
      "resultPreview": "{\"bearer\":\"sigil/cli.py cmd_warden_anchor_get — `sigil warden-anchor-get <pubkey>` prints the highest WARDEN action-log head-count ever anchored FOR ONE pubkey as JSON {count, head_hash}. It is a monotonic anti-rollback high-water: a full genesis-to-tip scan taking MAX(count) per pubkey plus the head_hash of the last record at that max. Same family as the head/checkpoint verifier but computes a SE…"
    },
    {
      "type": "workflow_agent",
      "index": 12,
      "label": "foldspec:device_approval_dedup",
      "phaseIndex": 1,
      "phaseTitle": "FoldSpec",
      "agentId": "a14db087dc7711c86",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1784498961045,
      "queuedAt": 1784498947697,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "sigil/bridge/daemon.py submit_device_approval — device-appr…",
      "promptPreview": "SIGIL hard-prune state-folding (Slice C). A prune deletes records [0..K) from the live spine but keeps\na signed \"snapshot\" record committing a folded summary of that pruned prefix. Every consumer that currently\nscans ALL records from genesis to compute a MONOTONIC security state must instead: st = SnapshotState.load(store)\n(the folded state of [0..K)); then fold st forward over the LIVE records [K…",
      "lastProgressAt": 1784499219423,
      "tokens": 72439,
      "toolCalls": 9,
      "durationMs": 258378,
      "resultPreview": "{\"bearer\":\"sigil/bridge/daemon.py submit_device_approval — device-approval replay/idempotency dedup (the census EXTRA). Full-spine scan that returns the seq of the first previously-recorded record carrying the same (signal=\\\"governor.approval\\\", pubkey, sig) triple, else appends a new device-approval record. Its comment (lines 82-86) claims the FULL scan is what keeps replay-bloat bounded to the n…"
    }
  ],
  "totalTokens": 697431,
  "totalToolCalls": 126
}